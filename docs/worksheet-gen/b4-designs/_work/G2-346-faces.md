# G2-346 `rounding` : the FIVE variation faces (Phase 2, 2026-09-21)

Built from `G2-346-rounding.md` §3 (+ §1 / §4 / §5 for the rulings, data and gates) under `_FACE-BRIEF.md` + the README cross-type rulings, on the base substrate of `_work/G2-346-build.md` (its "open items for the faces" closed here: `_buildFace` for sort / estimate / inverse, `_buildBoth` for the two-box face, the base composer serving F2 unchanged, the face render sections + the design's P8-P12 / P15 in the gate, `gate-variation-distinct --batch=b4` already present). Ids from `_records/b4var-id-allocation.json`: **all five are G3 ids** (3.NBT.A.1 is a Grade 3 code; the base is the locked G2 id with G3 content) in `types/g3/` with `extra {gradeBand:'G3'}`. Nothing shared was edited; nothing committed; the baseline was NOT captured. Every number below is measured (node against the real files, or puppeteer through `render/render-instance.js` with the shell's `file://` fonts). No em-dashes.

## Files (all family-scoped)
| file | what |
|---|---|
| `scripts/worksheet-gen/tools/b4var-rows/rounding.js` | NEW: 5 rows (G3-387 G3-388 G3-389 G3-390 G3-391), `HANDWRITTEN` empty; every row spreads the base's d2 and re-sets what its face reads, carries `extra {gradeBand:'G3'}`; the only file the emitter reads |
| `scripts/worksheet-gen/types/g2/G2-346-rounding.js` | the Phase 2 stub replaced: `_buildFace` dispatches on `d.mode` to `_buildSort / _buildEstimate / _buildInverse`; `_buildWith` dispatches a two-entry `steps` array to `_buildBoth` BEFORE `resolveBase` (F5's quotas are per place) and no longer throws on it; pure exported resolvers + composers `resolveSort` (the base `compose` re-used with the split quotas), `resolveEstimate / pairPool / samplePairs`, `resolveInverse / sampleField`, `resolveBoth / poolBoth / composeBoth`; `verify()` reads the root's `data-lcs-mode` / `data-lcs-face` first and hands a face to `VERIFY_FACE` (a second evaluate with its own copies of the rule, the strip contract and the page-wide bans); the base's own evaluate, its `_buildWith` path, its configs and its output are byte-untouched (baseline PASS). Every face resolver computes its stack and REFUSES above 677 |
| `scripts/worksheet-gen/templates/components-b4/rounding.js` | the base components byte-untouched; NEW `roundChipLane` (F1's `.ws-chip` strip in a `.ws-lane`, `data-lcs-val` only); `updownBins` gains `cellGap / pillH / pillPx` + a cells-fit-the-bin throw + the 84 cell-width floor + `data-lcs-bin-text` on the literal; `targetPill` gains `h / px`; `numberField` gains `rowGap / px` and a REAL stagger (`position:relative; left`) — a margin on a centred flex item shifts it by half (measured 27 for a 54 margin); `estimateRow` / `twoTargetRow` `rowH` were already parameters. The barrel accepted all 14 names (0 duplicates) |
| `scripts/worksheet-gen/types/g3/G3-387-rounding-up-or-down-sort-the-numbers.js` · `G3-388-rounding-to-the-nearest-100.js` · `G3-389-round-then-estimate-the-sum.js` · `G3-390-which-numbers-round-to-50.js` · `G3-391-rounding-to-the-nearest-10-and-100.js` | emitted by `node tools/gen-b4var-specs.js` (`emitted 45 variation specs + 0 handwritten (45 faces from 9/10 families; no rows yet: question-words)`) |
| `scripts/worksheet-gen/data/b4/rounding.js` (tracked, force-added by the reviewer) | UNCHANGED: the EN `strings.sort / hundred / estimate / inverse / both` the base authored are the rows' titles + instructions verbatim (the gate asserts the pair is ONE source); the faces print only the six literals + the numerals, so no new bank material was needed. The validator grew instead (rule 6b, rule 10 over the faces) |
| `scripts/worksheet-gen/qa/verify-b4-rounding.js` | the gate grows: section 5 (rows ≡ emitted specs ≡ bank strings; ids from the allocation; every face G3 + themeless; the title-numeral rule; 5 faces × 4 chromes with the body pinned to 722 / 677 (fits) and to 400 (the MINIMAL stack = the SPARSE floor); node cross-checks re-deriving every answer / quota / literal / floor from the stamps; F2 also through the base measure + `assertRender`; a synthetic de (722) + fi (677) render per face; a 20-seed sweep per face with locale-neutral seeds; 27 face-config refusals + 33 face render / validator poisons). `validateBank` rule 6b: a title that names a number names what the d2 page shows; rule 10 builds every non-refused face over the block. Final line `PASS (N assertions, M/M poisons killed)` |
| `scripts/worksheet-gen/i18n/strings.en.json` | regenerated by `node i18n/build-en.js` (`653 types (title lint clean)`; left as generated, it also carries the sibling builders' faces) |
| `docs/worksheet-gen/b4-designs/_work/G2-346-faces.md` | this record |

## The five faces

### G3-387 : Rounding Up or Down? Sort the Numbers : CODE `mode:'sort'` (F1, G3)
- **Row:** `{...base.d2, mode:'sort', steps:[10], min:11, max:994, items:12, mix:{2:6,3:6}, fiveMin:2, carryMax:2, splitMin:5, splitMax:7, cells:9, chip:72, chipGap:10, chipRowGap:12, chipCols:6, chipPx:28, cellW:96, cellH:76, cellGap:14, cellCols:3, binW:330, pillH:40, pillPx:18}`.
- **Child:** reads the rule box (as the base: `47 [glyph] 50`, the 7 coral and ringed), then 12 numeral chips (72 px `.ws-chip` discs at Baloo 28, two rows of six in a centred cream strip), then WRITES every numeral into one of two DRAWN bins headed by a pill `↓ round down` / `↑ round up` (the bank literal + the in-font arrow), each bin a 3 x 3 grid of EMPTY 96 x 76 dashed cells. Nothing written but the sorting; the rounded value never appears. The split is 5..7 a side (7 / 5 at the shipped seed) and the 9 cells EXCEED the larger side and equal neither count, so the empty cells never force the last decisions (the G1-351 leak rule); the 5 case (45 / 85 / 55-class) at least twice, always UP.
- **Build:** the base `compose` over `resolveSort` (upMin = downMin = max(splitMin, items - splitMax) = 5, so both sides land in 5..7; digitCover over 1..9 with digitMax 2; carries <= 2; mix 6 + 6); root rows `auto 184px 336px` TOP-ANCHORED (the slack falls under the bins, outside the stage).
- **Stamps:** root `data-lcs-mode="sort"` + step / items / mix / fivemin / splitmin / splitmax / cells / carrymax / digitcover / digitmax / min / max / example / rule / chip / chipcols / cellw / cellh / laneh / binh / stack / down / up / rel; chips `.ws-chip[data-lcs-val]` (nothing else); bins `[data-lcs-bin="down"|"up"][data-lcs-cells]` with `[data-lcs-bin-label] > [data-lcs-arrow] + [data-lcs-bin-text]` and `.ws-blankbox[data-lcs-answer=""]`.
- **verify():** 12 chips = the stamp, distinct, in range, never a multiple of 10, deciding digit != 0, never the worked 47, the text = the value, font >= 22, disc >= 52, no overflow, NO direction / membership / answer attribute on a chip; the split within 5..7, cells > the larger side and != either count, fives >= 2, carries <= 2, the mix, digitCover / digitMax; the chips on exactly 2 rows; bins down-LEFT / up-RIGHT (DOM order AND geometry), each 9 EMPTY boxes >= 84 x 44 inside its bin, the pill = the bank literal + the right arrow, no overflow, >= 40 high, font >= 16, narrower than the bin; NO text node outside the rule box equals any chip's rounded value; no `[data-lcs-row]`; the rule box = the base contract (one worked line, the ring on 7, the captions); the page-wide bans (U+2248 / U+2192 as text, `<` `>`, 4-digit numbers, a number line, a pattern slot, an img, an SVG that is not the glyph).
- **Poisons owned:** P8 (bins rebuilt with 6 cells -> "cells 6 <= the larger split 7"), PF1 (a chip's rounded value printed in a bin), PF2 (the up bin left of the down bin), PF3 (a pill literal that is not the bank's), PF4 (chips at 18 px), PF5 (every chip rewritten to round up -> "split up 12 / down 0"), PF6 (a chip 50), PF20 (a number line injected), PF22 (the ring on 3), PF24 (a 400 px rule box under the fi title -> footer overlap), PS1 (the DESIGN's geometry, chips 56 + cells 84 x 44 -> "SPARSE stage 500 < 631"); config: cells 6 / 7, fiveMin 1, chip 40, cellW 60, cellH 100 (stack 712), split 2..11, mode "lines".
- **PNG:** `scripts/worksheet-gen/out/dev/G3-387-null-d2-en.png` (gate: `G3-387-gate-d2-en{,-chrome722,-chrome677,-chrome1}.png`, `G3-387-gate-d2-de-syn-chrome722.png`, `G3-387-gate-d2-fi-syn-chrome677.png`, poisons `G3-387-gate-poison-*.png`). **Read (final render):** the rule box; a cream strip of twelve white discs 481 52 329 418 85 65 / 549 647 426 24 72 13; two teal bins side by side, the dashed top edge, a capsule label `↓ round down` / `↑ round up`, nine empty dashed cells each in a 3 x 3 grid; ~130 px of paper under the bins at the 2-line chrome (766), nothing clipped, nothing near the footer. Measured: stage 640 at every chrome (top-anchored), slack under the stage at the one-line chrome 811 - 640 = 171 <= 180; fits 722 / 677 (lowest 884 vs the foot 921 under the 4-line title); de pills `abrunden` 130.8 / `aufrunden` 137.9, fi `alaspäin` 120.4 / `ylöspäin` 121.3 (<= 300).

### G3-388 : Rounding to the Nearest 100 : PARAM `steps:[100]` (F2, G3)
- **Row:** `{...base.d2, steps:[100], min:101, max:949, mix:null, items:14, cols:2, fiveMin:2, upMin:5, downMin:5, carryMax:null}` (differs from the base d2 on steps / min / max / mix / carryMax).
- **Child:** the base's WRITE move one place up: 14 three-digit numbers in two columns of 60 px lines, `472 [glyph] 500` in the rule box with the TENS digit coral and the strip's ring on the 7, captions `round down` / `round up` (the en literal is place-neutral); the child reads the tens digit and writes the hundred. The 5 case = tens digit 5 (350 at the shipped seed) at least twice; >= 5 up, >= 5 down; every tens digit 1..9 on the page (digitCover), none more than twice.
- **Build:** the base path verbatim (`resolveBase` keys the deciding digit on `steps[0]`; the pool excludes multiples of 100 and a tens digit 0; `round(n,100) <= 999` caps the top at 949). No face stamp: the root is the base's (`data-lcs-step="100"`), verify() is the base branch, the gate runs it through the base measure + `assertRender` AND the face harness.
- **verify():** the base's list with step = 100 (deciding digit `floor(n/10) % 10`, `n % 100 !== 0`, answers <= 999, the worked 472 -> 500 with "7" coral, the ring on 7, captions = `down[100]` / `up[100]`, column-major badges, the quotas re-derived).
- **Poisons owned:** the base's P1-P18 / PRa-PRi run on the base path this face shares; face-side: rule 6b (a hundred title naming 10 -> "names 10, the d2 page shows 100"); config: the base's `steps [5]`.
- **PNG:** `out/dev/G3-388-null-d2-en.png` (gate `G3-388-gate-d2-en*.png` + `-base.png` twins). **Read:** the rule box `472 [glyph] 500` with a coral 7 and the ring on 7; fourteen lines 462 350 572 467 236 397 489 / 328 317 874 540 851 848 532 with badges 1-7 down the left and 8-14 down the right, the boxes empty; the lane spreads evenly to the footer rule (rows 34 px apart at the 1-line title, 19 under the 4-line fi title); nothing clipped. Measured: stage = the body (the lane grows), minimal stack 636 (the base's), fits 722 / 677.

### G3-389 : Round, then Estimate the Sum : CODE `mode:'estimate'` (F3, G3; en 3.NBT.A.1 + 3.OA.D.8)
- **Row:** `{...base.d2, mode:'estimate', steps:[10], addMin:11, addMax:89, sumMax:100, roundedSumMax:100, items:8, fiveMin:2, upMin:5, downMin:5, digitCover:true, digitMax:3, rowH:52, rowGap:14, exprW:80}`.
- **Child:** the rule box shows `23 + 48 [glyph] 20 + 50 = 70` (the 8 coral, the ring on 8); eight lines `a + b [glyph] [ ] + [ ] = [ ]` in one column (badge, the expression at Baloo 24 right-aligned in 80 px, the glyph, two 68 x 44 boxes joined by a printed `+`, a printed `=`, a 76 x 44 sum box: 433 wide); the child rounds both addends, writes them, adds the rounded. The exact sum is never printed and never asked; the `=` follows a BOX (the lock's ban is on `number = rounded number`).
- **Build:** `samplePairs` draws 8 distinct pairs from the pool of unordered pairs a < b in 11..89 (both roundable, exact sum <= 100, rounded sum <= 100, never 23 + 48) until the 16 addends carry every ones digit 1..9 (digitMax 3), >= 2 fives, >= 5 up and >= 5 down; each pair in a random orientation. DEVIATION: `a !== b` (the design's pool allowed `a <= b`; a doubled addend reads as a trick, not an estimate). Lane `minmax(542px, 1fr)` with `align-content:space-evenly` (the base's own shape: the slack opens between the rows, bounded by the gate).
- **Stamps:** root `data-lcs-mode="estimate"` + step / items / addmin / addmax / summax / roundedsummax / fivemin / upmin / downmin / digitcover / digitmax / example="23+48" / rule / rowh / rowgap / stack / down / up / rel; rows `[data-lcs-row][data-lcs-a][data-lcs-b][data-lcs-step]` with `[data-lcs-expr]` and three `.ws-blankbox[data-lcs-role=a|b|sum][data-lcs-answer]`.
- **verify():** 8 lines = the stamp; addends in 11..89, never a multiple of 10, never equal; a + b <= 100; round(a) + round(b) <= 100; pairs distinct as sets; never the worked pair; the expression prints `a + b`, font >= 22, no overflow, the node after it IS the SVG glyph; three boxes in DOM order with roles a / b / sum and answers round(a) / round(b) / their sum, EMPTY, >= 68 x 44 (76 for a 3-digit sum); the row's only text = the badge, the expression, one `+`, exactly one `=` that follows a box; no text node = the exact sum; rows in order, <= 639; the addend quotas (fives, up / down, digitCover, digitMax); `[data-lcs-rounded-expr]` only inside the rule box, printing `20 + 50 = 70`, the worked stamp `23+48 -> 70`, the ring on 8; the page-wide bans. **Node gate:** the `=` count on the page = 8 rows + the worked line.
- **Poisons owned:** P15 (the exact sum printed in a row -> "the exact sum 63 is printed"), P3f (a text `=` after the expression), PF7 (an addend 20), PF8 (88 + 88 -> the same number / over the cap), PF9 (the boxes' roles reordered), PF23 (a pre-filled box), PS3 (the DESIGN's 48 px rows / gap 12 -> "SPARSE stage 604 < 631"), PS3b (six rows 44 px apart: the stage 668 clears the floor while the rows float 58 apart -> the row-band rule); config: steps [100], fiveMin 1, three-digit addends at exprW 80, rowH 40, 12 lines (stack 914).
- **PNG:** `out/dev/G3-389-null-d2-en.png`. **Read (first build; the final differs by the 4 px lane pad only, gate-verified):** the rule box with `23 + 48 [glyph] 20 + 50 = 70`; eight badged lines 12 + 51 · 29 + 14 · 59 + 19 · 31 + 32 · 35 + 28 · 15 + 36 · 38 + 52 · 63 + 27, each with two dashed boxes, a teal `+`, a grey `=` and a wider dashed sum box, the rows spread evenly down the cream lane to the footer rule; the 2-line title; nothing clipped. Measured: minimal stack 650, rows 27 px apart at the 2-line chrome (32 at the one-line, 17 under the fi title), fits 722 / 677 (lowest 904 vs 921).

### G3-390 : Which Numbers Round to 50? : CODE `mode:'inverse'` (F4, G3)
- **Row:** `{...base.d2, mode:'inverse', steps:[10], target:50, pills:16, members:6, range:[30,69], nearMiss:[44,55], fiveIn:true, digitCover:true, pillW:108, pillH:74, gap:30, rowGap:42, stagger:40, perRow:4, px:30, targetH:60, targetPx:32, fieldGap:24}`.
- **Child:** the ONLY face where the child circles: the rule box's example is a DIFFERENT ten (`83 [glyph] 80`, the 3 coral and ringed, so the example never leaks a member); a coral target pill `[glyph] 50` at the top of the lane; a FIELD of 16 white numeral pills (108 x 74 at Baloo 30) in four rows of four, the even rows shifted 40 px right so it reads as a field, not a table, 30 / 42 px of ring room between pills; the child circles every pill that rounds to 50. EXACTLY 6 members of {45..54} \ {50} (45, the 5 case, always in) and 10 non-members that ALWAYS include the near misses 44 and 55 (55 goes UP, to 60); every ones digit 1..9 somewhere in the field.
- **Build:** `sampleField` (members = 45 + 5 of the other 8; others = 44, 55 + 8 of the remaining 25 non-members; re-drawn until the digits cover; ONE shuffle); root rows `auto 534px` TOP-ANCHORED.
- **Stamps:** root `data-lcs-mode="inverse"` + step / target / pills / members / range-lo / range-hi / nearmiss / fivein / digitcover / example / rule / pillw / pillh / laneh / stack / down / up / rel; `[data-lcs-target-pill]`; `[data-lcs-field][data-lcs-target][data-lcs-step] > [data-lcs-field-row] > .ws-nchip[data-lcs-val]` (NOTHING else on a pill: no membership stamp of any kind, asserted by name).
- **verify():** the target pill prints the field's target, has the glyph, font >= 22, sits above the field; 16 pills = the stamp, distinct, in 30..69, never a multiple of 10, never 50, never 83, text = value, font >= 22, >= 52 x 52, no attribute matching correct / member / answer / dir, every pill's border colour / background / border width / weight identical to pill 1 (no visual cue); exactly 6 members re-derived; 44 and 55 present; 45 present; digitCover; neighbours >= 20 px apart in x and rows >= 20 apart in y; rows <= 639; the rows stagger (a table fails); no `.ws-blankbox`, no `[data-lcs-row]`; the worked line's `-to !== 50`; the rule box contract; the page-wide bans.
- **Poisons owned:** P9 (a member stamped `data-lcs-correct`), P10 (44 rewritten away), P11 (the example 47 -> 50 = the target -> "rounds to the target 50 (it leaks a member)"), PF10 (45 with a coral border -> "a visual member cue"), PF11 (the target among the pills), PF12 (a non-member rewritten to a free member -> "7 members"), PF13 (pills 18 px apart), PF21 (an HTML `≈` in the target pill), PS4 (the DESIGN's 96 x 52 pills at 24 gaps -> "SPARSE stage 484 < 631"); config: target 55, near miss 46, 15 pills, gap 12, pillH 40, target 80 (the worked 83 rounds to it).
- **PNG:** `out/dev/G3-390-null-d2-en.png`. **Read (final render):** `83 [glyph] 80` with the ring on 3; the coral `[glyph] 50` capsule centred; four rows 31 46 36 55 / 49 44 63 51 / 47 45 35 58 / 52 64 57 32, rows 2 and 4 visibly shifted right, wide air between the capsules; the six members 46 49 51 47 45 52 look exactly like the ten others; ~125 px of paper under the lane at the 2-line chrome; nothing clipped. Measured: stage 642 at every chrome, one-line slack 169 <= 180, fits 722 / 677 (lowest 872 vs 921 under the fi title), pill gaps 30.0 / 42.0, stagger 40.0.

### G3-391 : Rounding to the Nearest 10 and 100 : PARAM `steps:[10,100]` (F5, G3)
- **Row:** `{...base.d2, steps:[10,100], min:101, max:949, items:8, cols:1, fiveMin:{10:2,100:2}, upMin:3, downMin:3, digitMax:2, digitDistinct:7, carryMax:2, rowH:52, rowGap:12}`.
- **Child:** the rule box shows the same number twice, each line led by a language-free place pill (`10` `472 [glyph] 470` with the ones digit coral; `100` `472 [glyph] 500` with the tens digit coral), the strip's ring on the ones digit 2; a header row prints the lock's GIVEN place words once (`nearest ten` | `nearest hundred`, Nunito 800 13 in 160 px cells centred on the two box columns); eight lines `[numeral] [glyph] [box ten] [glyph] [box hundred]` (380 wide, boxes 84 x 44 at row-x 178 / 338); the child rounds each number twice. Numbers 101..949 with ones != 0 (both roundings non-trivial); the 5 case for the ONES on >= 2 lines AND for the TENS on >= 2 lines; >= 3 up and >= 3 down at the ones; 7 of the 8 ones digits distinct (the 5 twice); carries at the ten <= 2.
- **Build:** `_buildBoth` over `resolveBoth` + `composeBoth` (ones digits = two 5s + three from 1-4 + three from 6-9, no digit over digitMax, >= 7 distinct; two random slots forced to tens digit 5; one number per slot from the pool; ONE shuffle); lane = a flex column with `placeHeader` then a rows grid `minmax(…, 1fr)` spreading `space-evenly` (the base's shape). It is a PARAM row (the knob is the `steps` array) that needed the dispatch + the per-place composer the base record left as a seam.
- **Stamps:** root `data-lcs-face="both" data-lcs-steps="10,100"` + items / min / max / fivemin10 / fivemin100 / upmin / downmin / digitmax / digitdistinct / carrymax / example / rule / rowh / rowgap / stack / head10 / head100 / down / up / rel; rows `[data-lcs-row][data-lcs-n][data-lcs-steps="10,100"]` with two `.ws-blankbox[data-lcs-step=10|100][data-lcs-answer]`; `[data-lcs-heads] > [data-lcs-head=10|100]`; two `[data-lcs-worked]` lines led by `[data-lcs-place=10|100]`.
- **verify():** 8 lines = the stamp, in range, never a multiple of 10, distinct, never 472; the numeral prints n, font >= 22, followed by the glyph; two boxes in DOM order for step 10 then 100 with answers round(n,10) / round(n,100) <= 999, EMPTY, >= 84 x 44; two glyphs per row; the row's only text = badge + numeral; lines in order; the per-place fives, up / down at the ones, digitMax, >= 7 distinct ones digits, carries <= 2; exactly two header cells `10`, `100` = the bank heads, no overflow, font >= 12, each centre within 1 px of its box column (measured on line 1), above the first line, not overlapping; two worked lines of 472 -> 470 (digit "2") then 472 -> 500 (digit "7"), led by the 10 then the 100 pill; the ring on 2; the page-wide bans.
- **Poisons owned:** P12 (the second header cell moved 32 px -> "head 2 centre 467.5 is 32.0 px off its box centre 499.5"), PF14 (the two boxes swapped), PF15 (the header removed), PF16 (a head that is not the bank literal), PF17 (one worked line), PF18 (a line 470), PF19 (numerals at 18 px), PS5 (the DESIGN's 44 px rows / gap 10 -> "SPARSE stage 588 < 631"); config: steps [100, 10], a number fiveMin, min 50, max 994, rowH 40, 12 lines (stack 922), cols 2, a bank without heads[100] (refuses), the base d2 pushed through the both path (refuses on its range).
- **PNG:** `out/dev/G3-391-null-d2-en.png`. **Read (first build; the final differs by the 4 px lane pad only, gate-verified):** the rule box with two lines led by small `10` / `100` capsules, the ring on 2; the header `nearest ten` / `nearest hundred` over the two box columns; eight lines 159 494 791 305 353 535 567 556, each numeral followed by a glyph, a box, a second glyph, a second box; the lane spreads to the footer rule; the 2-line title; nothing clipped. Measured: minimal stack 666 (11 under the fi budget), rows 23 apart at the 2-line chrome (13 under the fi title), fits 722 / 677 (lowest 906 vs 921), heads en 69.8 / 101.1, de `Zehner` 42.9 / `Hunderter` 64.0, fi `kymmenen tarkkuudella` 149.1 / `sadan tarkkuudella` 118.8 (<= 156: the lock's fi labels survive the 160 cell).

## Deviations from §3 (each measured)
1. **Every face's stack GREW past the design's (the nt10-D SPARSE rule; the base's own 12 x 48 -> 14 x 60 lesson).** The design's §3 stacks 496 (F1) / 600 (F3) / 480 (F4) / 584 (F5) would leave 315 / 211 / 331 / 227 px of blank paper under the 811 one-line chrome (the base's rule: <= 180, i.e. a stack >= 631). Shipped: F1 chips 72 (not 56) + cells 96 x 76 (not 84 x 44) at gap 14 -> **640**; F3 rows 52 / gap 14 (not 48 / 12) -> **650**; F4 pills 108 x 74 at Baloo 30 with 30 / 42 gaps, a 60 px target pill (not 96 x 52 / 24 / 52) -> **642**; F5 rows 52 / gap 12 (not 44 / 10) -> **666**; every stack <= 677 by the resolver's own guard. The gate measures the MINIMAL stack (the body pinned to 400 px, where a `1fr` lane cannot grow) and asserts >= 631 and <= 677; the four design geometries are the sparse poisons (500 / 604 / 484 / 588 with the lane pad below).
2. **The `.ws-lane` is border-box: padding 12 + border 2 each side = 28, not the design's 24.** The first build used 24 and F4's target pill shrank 60 -> 56 inside its fixed-height lane (measured by the gate's `targetH` floor). Every face lane now budgets 28 (the base's lane min-height 528 is content 504 + 24: its last row eats 4 px of bottom padding, invisible and untouched, byte-identical).
3. **F4's stagger is a real offset (`position:relative; left:40`), 40 px, not the design's `margin-left:48`.** A margin on an item of a centred flex column shifts it by HALF (measured 27 for a 54 margin), and the design's 48 would have read as 24. The row is centred first, so both rows fit when 522 + 2 x 40 <= 639; the gate asserts the measured offset = the config within 0.6.
4. **F3 pairs are `a < b` in the pool, never `a === b`** (the design's "unordered pairs a <= b"); a doubled addend reads as a trick. Each pair is shown in a random orientation.
5. **F5's rule box captions are the step-10 literals** (`down[10]` / `up[10]`) under a strip whose ring sits on the ones digit; the second worked line marks the tens digit coral without a second strip (the design's layout). For fr, whose literals are place-bound (`dizaine inférieure` under a box the child also uses for the hundred), the fr panel decides whether the F5 strip caption should be a place-neutral form or F5 should carry `down[100]` — recorded as OPEN, not resolved here.
6. **F1's chip strip is a centred 518 px cream lane** (6 x 72 + 5 x 10 + 36), not a page-wide lane: the strip is narrower than the bins by design of the reading order (rule box, the numbers to sort, the bins); the gate asserts the chips sit on exactly two rows.
7. **F2 keeps the base's 14 lines at 60 px** (the design's 12 at 48 was the base's own rejected geometry); mix null (every number is three-digit), carryMax null (a carry cannot occur under 949).
8. **The verify() of each face is its own evaluate branch** (the base's `VERIFY_BASE` still refuses a `data-lcs-mode`, so a face never falls into the base's checks by accident); F2 has no face stamp and IS the base branch at step 100.
9. **The component smoke's `targetPill` width tolerance reads the measured 105.7** (padding 18 for the 32 px target font), not the design's 98 +- 6.

## Per-locale refusals / contingencies visible from the bank shape (Phase 4 `hub-expectations.json`)
- **No face is refused in any locale by construction:** no picture, no noun, no inflected form on any face; the six literals per locale have measured reserves and the synthetic §4 candidates all fit: captions <= 118 in the 130 block (fr `centaine supérieure` 111.3 at N12 per the base), F1 pills <= 300 (fr `dizaine supérieure` 150 -> ~200; de 137.9; fi 121.3 measured here), F5 heads <= 156 in the 160 cell (fi 149.1 / 118.8 measured here). Expected rows stay **6 x 11 = 66**.
- **fr F1 / pt F1 title candidates** (the base record's open item 1) still fail rule 6 / 7 as the design authored them (adjective- / adverb-only differences); the synthetic controls carry rewrites; the fr / pt panels write a noun / verb in.
- **fr F5** (deviation 5): the place-bound captions under a two-place page; the panel rules.
- **The fi F5 title check is weak** (the base's deviation 7): a panel / reviewer item.
- **`apps.rounding.default_age_range` is `7-9`** in the taxonomy, the design says `8-10` (registrar; kept as the base left it).

## Open items for the panels
- Every panel audits the EN as a SOURCE: the F1 instruction says "bin" (the drawn bins), F3 "boxes", F4 "circle" + "the ten shown at the top", F5 "first box / second box"; never "chip" / "hill" / "number line" / "dot".
- A title that names a number must name the shipped one (rule 6b: 50 on F4, 100 on F2, 10 and 100 on F5; the base 10).
- Each panel reads one render per face in its locale (`node render/one.js <id> null 2 <loc>` once its block exists): the strip captions, the F1 pills, the F5 header cells at their measured widths.

## Gate output
```
node scripts/worksheet-gen/qa/verify-b4-rounding.js
bank en: clean (relation approx, faces 6, refuse [])
render d1/d2/d3 en + 3+3 chrome + 677 chrome + one-line chrome: verify 0 lints 0 (the base sections unchanged; d2 stack 636, slack 74 / 41 / 175)
components: bins 330x232 pill 149.4 / 125.1 · estimateRow 433.0 / 433.0 (F3 worked 256.5, rule box 96) · field rows 456/456/456/456 gap 24.0 · targetPill 105.7x52.0 · F5 centres 178.0/338.0 heads 178.0@69.8 338.0@101.1
render G3-387 sort en: verify 0 lints 0 body 766 stage 640 (677 pin 640) row-blank 0 lowest 795 vs foot 921 [722:ok 677:ok] chips 481,52,329,418,85,65,549,647,426,24,72,13 split 7/5   (chrome722 lowest 851 · chrome677 884 · chrome1 750, stage 640 at all)
render G3-388 hundred en: verify 0 lints 0 body 799 stage 799 (677 pin 677) row-blank 34 lowest 887 vs foot 921 [722:ok 677:ok] items 462,350,572,467,236,397,489,328,317,874,540,851,848,532   (+ the base measure / assertRender twin)
render G3-389 estimate en: verify 0 lints 0 body 766 stage 766 (677 pin 677) row-blank 27 lowest 894 vs foot 921 [722:ok 677:ok] pairs 12+51 29+14 59+19 31+32 35+28 15+36 38+52 63+27   (minimal stack 650; chrome677 row-blank 17)
render G3-390 inverse en: verify 0 lints 0 body 766 stage 642 (677 pin 642) row-blank 42 lowest 783 vs foot 921 [722:ok 677:ok] pills 31,46,36,55,49,44,63,51,47,45,35,58,52,64,57,32 members 46,49,51,47,45,52
render G3-391 both en: verify 0 lints 0 body 766 stage 766 (677 pin 677) row-blank 23 lowest 896 vs foot 921 [722:ok 677:ok] lines 159,494,791,305,353,535,567,556 heads nearest ten@69.8 / nearest hundred@101.1   (minimal stack 666; chrome677 row-blank 13)
render G3-387..391 de syn chrome722: verify 0 lints 0 [722:ok 677:ok] pills abrunden@130.8 / aufrunden@137.9 · heads Zehner@42.9 / Hunderter@64.0
render G3-387..391 fi syn chrome677: verify 0 lints 0 [722:ok 677:ok] pills alaspäin@120.4 / ylöspäin@121.3 · heads kymmenen tarkkuudella@149.1 / sadan tarkkuudella@118.8
sweep d1 / d2 / d3: 20 seeds, 20 distinct sets, 20 distinct orders (each)
sweep G3-387 sort · G3-388 hundred · G3-389 estimate · G3-390 inverse · G3-391 both: 20 seeds, 20 distinct sets, 20 distinct orders (each; the de build draws the same numbers)
poisons (158 = 41 validator [36 base + rule 6b x3 + P10 + P10-both] + 15 base config refusals [11 resolveBase + relation wave + the base d2 through the both / sort paths + a bank without down[10]] + 27 face config refusals [25 resolvers + mode "lines" + a bank without heads[100]] + 18 component throws + 21 base render + 36 face render [PS1 PS3 PS3b PS4 PS5 · P8 P9 P10 P11 P12 P15 P3f · PF1..PF24]): every one killed for its OWN reason; the shipped faces are the controls
verify-b4-rounding: 1451 assertions, 0 failures, 158/158 poisons killed in 103s
PASS (1451 assertions, 158/158 poisons killed)          --quick: PASS (603 assertions, 158/158 poisons killed, --quick: sweeps skipped)
```
- `node tools/gate-variation-distinct.js --batch=b4 --diffs=2 --family=rounding` -> `[b4:rounding] compared 15 pairs over 5 faces against their bases + pairwise within family` / **`every variation differs from the deck its base publishes and from its siblings`**.
- `node tools/b3-baseline.js --check --quick` -> **`checked build 3620 + enum 255 in 19s: 0 drifted (0 expected), 0 missing` / `PASS`** (the base path byte-identical; the five face ids are the accepted Phase-2 drift, not captured).
- `node i18n/build-en.js` -> `653 types (title lint clean)` (left as generated).
- `node render/one.js <id> null 2 en` for G3-387 · G3-388 · G3-389 · G3-390 · G3-391 -> lints clean / verify clean each; PNGs `scripts/worksheet-gen/out/dev/<id>-null-d2-en.png`.

## Traps paid for in this build (the gate accused correct pages first, twice)
- **A border-box lane is 4 px taller than its padding says:** the first F4 build shrank the target pill 60 -> 56 and the gate's `targetH` floor caught it; the pad constant is 28 everywhere a face computes a lane.
- **A margin on a centred flex item moves it by HALF:** the first stagger check accused a correct field of "no stagger" (27 measured for 54 declared); the offset is now real and asserted equal to the config.
- **A pinned 677 body cannot measure a `1fr` lane's minimum** (the lane fills whatever it is given: "stage 677" for every lane face); the SPARSE floor is measured under a 400 px pin, where the grid row's `minmax` minimum is what shows.
- **A poison must be a defect:** "split 2..10 for 12 chips" is a legal split and the resolver rightly built it; the poison became 2..11. PS3b at 90 px gaps overflowed the budget before the row-band rule could speak; six rows 44 apart (stage 668, rows 58 apart) is the poison that exercises it.
- Heredocs ate a quote and a backslash twice; every multi-line script went to a file and ran by path.
