/** G1-342 — Read and Draw. nt20-C variation of G1-308 (generated by tools/gen-b3var-specs.js from tools/b3var-rows/). */
'use strict';
const base = require('./G1-308-read-and-do.js');
// One object for all three levels: the waves ship d2 only, so a face must
// render identically whichever level is asked for. Spreading the base entry
// (not a JSON literal) carries function-valued params through intact.
const D = { ...base.difficulty[2], ...{"mode":"draw","cards":6,"cols":2,"rows":3,"nMin":2,"nMax":4,"drawW":300,"drawH":140} };
module.exports = {
  ...base,
  id: 'G1-342',
  slug: 'read-and-draw',
  difficulty: { 1: D, 2: D, 3: D },
  i18n: { en: { title: "Read and Draw", instruction: "Read each sentence carefully. Then draw exactly what it says inside the box." } },
};
