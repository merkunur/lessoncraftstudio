---
name: project-german-secondbatch-fanout
description: "Active program — fanning the second-batch (122 EN-only) activities out to German, one at a time, native-ensemble + rebuild-not-translate"
metadata: 
  node_type: memory
  type: project
  originSessionId: 9bd9fed5-5c5b-40e4-bae6-2e1d5b76891b
---

# German second-batch fan-out (ACTIVE, started 2026-06-29)

**What:** The "second batch" = the **122 EN-only lcs-shell activities** built in the games build-walk and deployed together in commit `12327ce5`. (Manifest scan: 122 `mini tools/*-activities.json` have no German; the 11 WITH German are the original first-batch engines — ten-frame/place-value/match-pairs/clock/fractions/etc.) This program fans them out to **German, ONE activity per plan-mode session**, employing a **3-agent native German ensemble** (linguist + K-3 Grundschule educator + B2C/SEO content creator) per §A.13.48/§A.13.54/§A.13.56.

**DEPLOY POLICY (operator 2026-06-29):** build **ALL** German activities **LOCALLY**; **deploy NOTHING until every one is built**; then **deploy ALL in one go**. (Activity #1 seriation was deployed live BEFORE this policy — it stays.) Per-activity: **commit + push only, NO deploy** (accumulates on `pivot/printable-business-toolkit`; local visual-qa serves from `mini tools/` so no deploy needed for QA). At the end: ONE `git pull` (picks up all commits) + cp ALL changed mini-tools files + one `deploy.sh`.

**Operator cadence:** operator says **"switch to plan mode + continue"** before each → EnterPlanMode → run the ensemble (content into the plan file) → ExitPlanMode for approval → build → full LOCAL DoD → show operator the local screenshots → operator approves → commit+push (NO deploy). ONE at a time, approve each.

**Locked operator rulings (whole program):**
1. **NEVER skip (operator-locked 2026-06-29).** American & German kids have different curricula AND a different language. For EVERY activity — incl. English-specific literacy — **find the relevant German teaching point and REBUILD for the German Lehrplan / Schriftspracherwerb**. Do NOT default to "no analogue → skip". E.g. English irregular plurals → German umlaut plurals (Fuß→Füße); contractions don't → in dem→im/an dem→am; CVC phonics → German Anlaut/Silben; pronoun-case → German Nominativ/Akkusativ (er/ihn). Genuine pedagogy forks still surface in that activity's own plan-mode session.
2. **rebuild, not translate** — genuinely sound for a German classroom, native 3-agent ensemble per activity.
3. Build order: **math/measurement/geometry first** (port cleanly), literacy rebuilt (NOT skipped) when reached.

**The per-activity edit recipe (activity-layer ONLY; cores/lcs-shell/Direction-A CSS = 0 lines, git-diff proof):**
- `mini tools/<engine>-activity.js`: module `var LANG='en'` set in `init` from `api.lang`; make `txt()` read `L[LANG]||L.en`; add `L.de` block; add `de:` to `strings`; locale-aware labels (e.g. a `COLOR_L` map + helper) for any hardcoded-English labels; refactor any pre-resolved-English sr-only text to locale-driven.
- `mini tools/<engine>-activities.json`: add `de` to `slug`/`page_title`/`page_intro` (slug ASCII-folded ä→ae etc.; page_intro cites Lehrplan, **NO "Common Core"/CCSS code** per §20.10; check slug not colliding with an existing de slug + not cannibalizing a sibling).
- `frontend/messages/activity-content/de.json`: add `prose[<id>]` full-override (about≥2/practices≥3/howToPlay≥2/learningGoals≥2; no `{placeholder}`; no code; no "Common Core"; unique opener).
- `mini tools/<engine>-activity.html`: bump `<engine>-activity.js?v=N`.
- `frontend/app/[locale]/activities/[slug]/page.tsx`: bump `ACTIVITY_WRAPPER_VERSION`.
- NOT touched: `<engine>-core.js`, `lcs-shell.{js,css}`, Direction-A CSS, `strand-names.ts` (German strands already present), `activities.ts` (engine already registered). Previews are language-neutral (EN render reused) → no new thumbnail.

**Gates (lean per §feedback-lean-activity-qa):** `node scripts/verify-activity-content-de.js` + `node scripts/preflight-activity-routes.js` + `node scripts/visual-qa-activity.js --activity=<id> --locale=de` + EN-regression visual-qa + personal Read 360/768/1024 + git-diff 0-core. NO whole-catalog sweeps.

**Deploy = DEFERRED to the very end (single batch).** Per-activity does NOT deploy — commit+push only. After ALL built+approved: ONE cp-before-build §20.4 chain `plink … "cd /opt/lessoncraftstudio && git pull && cp 'mini tools'/<all-changed-engines>-* /var/www/lcs-media/mini-tools/ && chown lcs-media:lcs-media /var/www/lcs-media/mini-tools/* && bash deploy.sh"` (one git pull grabs all accumulated commits; cp ALL changed mini-tools before the one build) then live-verify a sample of de pages (200 after the 308 trailing-slash redirect; German title; „Größen und Messen"/„Lehrplan"; no „Common Core"). (mini-tools is NOT immutable — direct cp.) `indexnow-submit.js MODULE_NOT_FOUND` at end of deploy.sh = pre-existing harmless wart.

## Done (built locally; only #1 deployed pre-policy — rest await the end batch deploy)
1. **seriation `compare-length.1-md-a-1`** → „Tillys fernes Regal" / `laengen-vergleichen-und-ordnen` — LIVE 2026-06-29, commit `94e8ec11` (wrapper 9.04, seriation-activity.js?v=2; deployed pre-policy). Rulings: **„die Schnur"** not Maßband (Maßband = standardized measuring, wrong stage = trust-killer); **„das Band"**; warm „du"; sr-only colon form `Band Rot: länger als die Schnur` to dodge the `orange` adjective-inflection trap. Distinct from K.MD.A.2 Kindergarten page (`laengen-vergleichen-groesser-laenger-kuerzer`) by grade + "und ordnen" + mechanic.
2. **lay-units `measure.1-md-a-2`** → „Inchies Gartenweg" / `laengen-messen-mit-einheiten` — built + committed `b08c1c32`, **NOT deployed** (wrapper 9.05, lay-units-activity.js?v=5). Lehrplan Stufe 3 „Messen durch Auslegen" (the next step after #1's comparison — coherent Längen progression, educator-confirmed). Rulings: unit = **„Helfer"** (not „Würmchen" — keeps big/little inverse round crisp); char **„Inchie" KEPT** (Tilly precedent; linguist suggested „Wurmi" — operator may flip on review); „lückenlos und ohne Überlappung"; **not Lineal/cm** (that's a later stage). New localization wrinkles handled (reusable): per-round prompts via `promptL10n.de` in manifest + `_pPrompt(r)` reader; inline hardcoded English → `api.t` (added strings keys countBig/countLittle/judgeMiss/invMidA/invDone/sayWinSpoken); **two number-word forms** — tap-count „eins…zehn" vs win „Ein Helfer" (not „Eins"); `speak()` lang=LANG.

3. **skipcount `fill.2-nbt-a-2`** → „Hoppers Seerosen-Sprünge" / `in-schritten-zaehlen-5-10-100` — built + committed `4ae6f6d9`, **NOT deployed** (skipcount-activity.js?v=2, wrapper 9.06). Lehrplan Klasse 2, Zahlenraum bis 1000, „in Schritten zählen". Rulings: title „Hoppers Seerosen-Sprünge"; **„Sprung"** (frog hop, narrative) vs **„{step}er-Schritte"** (math term, task) — deliberate split; „rückwärts" explicit; spelling „5er-Schritte"/„Fünferschritte" (never „Fünfer Schritte"); digits only (no number words). Domain label kept = „Zahlen und Rechnen im Zehnersystem" (Base-Ten shared mapping; `strand-names.ts` untouched — educator's preferred „Zahlen und Operationen" would need a cross-locale shared edit = out of scope). Shape ≈ seriation (custom L+txt+LANG; question prompts via shell promptKey).

4. **shapeforge `compose.1-g-a-2`** → „Mims Glühwerkstatt" / `formen-zusammensetzen` — built + committed `be4523cf`, **NOT deployed** (shapeforge-activity.js?v=4, wrapper 9.07). First GEOMETRY activity. Lehrplan Klasse 1 Geometrie (Raum und Form), „Formen zusammensetzen" mit Musterplättchen. Rulings: compose verb **„bauen"** (not „schmieden"); piece **„Plättchen"/„Leuchtplättchen"** (not „Scherbe"); **Klasse-1 shape names Dreieck/Raute/Sechseck — educator-authoritative: „Rhombus" forbidden (Sekundarstufe), „Trapez" NOT required vocab → „Dach", „Parallelogramm" → „Laterne"**. Biggest surface so far (strings via api.t + 6 new keys incl. localized aria-labels; PIECE_L/pname map; 11 promptL10n.de; speak lang). Shape ≈ lay-units.

5. **place-value-regroup `add-compose-ten.1-nbt-c-4`** (compose-a-ten ONLY) → „Tuck bündelt einen Zehner" / `zehner-buendeln-mit-zehneruebergang-addieren-klasse-1` — built + committed `cdb452e6`, **NOT deployed** (activity.js?v=17, wrapper 9.08). Klasse 1 Zehnerübergang/Bündeln (bis 100). **Engine strings + `_t`/`lockAndCaption` live in the CORE** (`Object.assign({},Core,…)` pattern) — localized by OVERRIDING `_t` (core read nonexistent `api.locale`; shell exposes **`api.lang`**) + `lockAndCaption` (German speech „…macht…", lang=de) in the activity + de strings merged over `Core.strings`; **0 core lines**. Verb pair **bündeln/entbündeln** (educator's Lehrplan term over linguist's „auflösen"); Einer/Zehner/Hunderter; Mehrsystemblöcke. Engine FULLY localized (all 4 variants' buttons/hints/titles authored) so the 3 deferred variants are quick follow-ons.

6. **place-value-regroup — 3 bis-1000 variants + per-locale grade override** → built + committed `bd3b49d3`, **NOT deployed** (wrapper 9.09). **CLOSES the flagship (engine now 4/4 German).** Shipped break-ten `zehner-entbuendeln-mit-uebergang-subtrahieren-klasse-3` / make-hundred `hunderter-buendeln-mit-uebergang-addieren-klasse-3` / break-hundred `hunderter-entbuendeln-ueber-null-subtrahieren-klasse-3` (all „Tuck entbündelt/bündelt …", Klasse-3 prose). **Per-locale grade override** added to the activity route: new `GRADE_OVERRIDE` (activity-id→locale→grade) + `effGrade()` used for the chip + JSON-LD educationalLevel + typicalAgeRange; the 3 ids → `de:'3'` (chip „3. Klasse"); **EN + all other activities/locales unaffected** (gated locale≠en + map entry); sits beside the route's framework/strand maps. 0 engine/core/shell lines (engine localized in #5). de `educational_level.grade_3`=„3. Klasse" confirmed. SSR grade-chip render → live-verify at batch deploy.

**REUSABLE: the per-locale grade override** (`GRADE_OVERRIDE`/`effGrade` in `frontend/app/[locale]/activities/[slug]/page.tsx`) now exists for ANY future US-CCSS-grade ≠ German-grade-by-Zahlenraum activity — add an `<id>: {de:'N'}` entry. German grade-by-Zahlenraum: bis 20/100 = Klasse 1/2, **bis 1000 ARITHMETIC = Klasse 3** (but counting/skip-counting bis 1000 stayed Klasse 2 — counting bis 1000 is Klasse-2-introduced).

7. **graph-it `bar-graph.2-md-d-10`** → „Pips Stapelzaun" / `saeulendiagramm-lesen-und-bauen-klasse-2` — built + committed `e700ff83`, **NOT deployed** (graph-it-activity.js?v=5, wrapper 9.10). **First DATA domain.** Klasse 2 Säulendiagramm (aus Strichliste bauen + ablesen). Activity-layer: `CAT_L`+`clabel` (Blätter/Beeren/Eicheln/Tannenzapfen/Pilze; clabel reads `LCS.i18n.current` like its `txt`), de strings + L.de, announce + sr-caption localized. **Comparative-agreement solved**: qMore/qFewer „Wie viele {a} mehr/weniger als {b}?" use article-less plural labels (agreement-proof); qVerify/miss-lines use „Es gibt"/colon-count to dodge the plural-verb trap (§A.13.56). **NEW: per-activity STRAND override** (`STRAND_OVERRIDE`/`effStrand` in the route, parallel to #6's grade override) — educator ruled „Größen und Messen" a genuine DOMAIN ERROR for a data activity → de chip+JSON-LD show **„Daten und Häufigkeit"** (full „Daten, Häufigkeit und Wahrscheinlichkeit"); other M&D (length/time/money) keep „Größen und Messen"; `strand-names.ts` untouched. 0 core/shell lines.

**REUSABLE OVERRIDES now in the route** (`frontend/app/[locale]/activities/[slug]/page.tsx`): `GRADE_OVERRIDE`/`effGrade` (#6, US-grade≠German-grade) + `STRAND_OVERRIDE`/`effStrand` (#7, CCSS-strand≠German-Leitidee). Add an `<id>:{de:'…'}` entry for any future mismatch. Line rule (strand): measure a Größe→„Größen und Messen"; collect/display data→„Daten und Häufigkeit".

**LESSON (cost a round-trip):** never let a `*/` sequence appear inside a JS block comment — my „miss*/qVerify" closed the comment early → SyntaxError → blank activity. After editing any `mini tools/*-activity.js` with comments, run `node --check "<file>"` BEFORE visual-qa (visual-qa just times out at 15000ms on a load-time JS error, masking the cause).

## PROGRESS — through #114 (~9 EN-only remain; WRAPPER pattern proven 16×; SL + RF/Reime + Schreiben + Größen/Zeit + RF/Lautanalyse + RF/Vokallänge + RF/Erstlesen + RF/Lautsynthese + RF/Silbenlesen + Mathematik/Brüche + Grammatik/Pluralbildung + Geld/Euro-Sachrechnen + RI/Hauptaussage-Hörverstehen facets OPENED. **grade_4/Klasse-4 support ADDED** (operator-approved) — GRADE_KEY_MAP + gradeToAgeRange + seo.educational_level.grade_4 ×11 locales; index still uses raw US grade. **mims-baskets REJECTED** (vocab in category-sort-core.js English-only = not clean 0-core; + olive-kind-of already de'd for Oberbegriffe). **DEFINITIVE remaining list = 28 EN-bearing rows** (enumerated 2026-07-04): grapheme/decoding phonics (cvc-builder×3, syllable-builder vowel-teams/endings[EN-orthography poor-fit], bingo-word-hunt[EN-CVC-minimal-pair poor-fit], choice-board read-cvc[DONE #108]/onset-rime[DONE #109], pepper-sound-swap[REJECTED], domino[DONE #113 Silbenlesen], word-clinic[core-dirty EN phonics], snippy[dup #104]), grammar/vocab (daisy-plate plurals[DONE #111], wordclass[German adj≈adverb poor-fit], shades[REJECTED — roary #34 dup], mims-baskets[REJECTED], sunny-side-diner[core-dirty EN agreement]), comprehension (mending-basket[RF.1.4, 7-mechanic adversarial anti-guess — heavy], nila-pond[DONE #114 RI.K.2 Hauptaussage], willow[RL.K.9 compare — comic-panel IMAGE-BOUND, the last clean RL/RI slot]), math (4 clock-digital[2.MD.C.7 owned by E10], coin-stall[DONE #112 Euro], fraction-equiv 3.NF[DONE #110]), storybook. **cvc-builder WRITING (lautgetreues Schreiben) DEFERRED to its own session** — build-cvc-word char-splits targetWord (only build-blend-word supports chunks) + template dispatch is global-not-per-locale → tiling/template fork. **pepper-sound-swap REJECTED** (German has almost no picturable 3-phoneme minimal pairs; DON'T retry)), as of 2026-07-04

**#1–#114 ALL DEPLOYED + LIVE (2026-07-05, operator "deploy all of them now").** The end-of-batch deploy: server was at `007451444` (Admin mode); the 20 recent activities #94–#114 were the undeployed slice (#2–#93 already live from prior deploys). Sequence = the §20.4 pattern `git pull (007451444→fb423d5b, clean fast-forward) → cp 'mini tools'/*.{js,json,html} → /var/www/lcs-media/mini-tools/ (BEFORE build — the served dir is a symlink target the Next build indexes; nila/rhyme/linc were 0-de-keys→4) → bash deploy.sh` (build + zero-downtime release flip + pm2 reload; smoke ALL PASSED, 43/43 chunks, 0 deck-drift, IndexNow 22 URLs). Verified: all 20 de slugs HTTP 200 at origin AND edge (Cloudflare bare-curl), German SSR content (Otterdame/Hauptaussage/Sprechen-und-Zuhören chip/etc.), EN unchanged. **NOTE the edge 403 gotcha: a fake-browser-UA from a datacenter IP gets Cloudflare-403'd on EVERY page (incl. known-good /en) — use BARE curl (200) to verify the edge.** Future activities: resume per-activity build+commit; deploy-batched or per operator.
**(historical) #1 deployed pre-policy; #2–#114 committed + pushed on `pivot/printable-business-toolkit`, then deployed 2026-07-05** (was deploy-ALL-once-at-the-end). #114 = nila-pond „Nila die Otterdame" **Die Hauptaussage eines Sachtexts finden** (RI.K.2→**Klasse 1**, `fb423d5b`; 16th 0-core WRAPPER; NEW **RI/Hauptaussage-Hörverstehen** facet — hear a read-aloud 4-Satz-Sachtext → net the big-idea fish + bring true details home). ONLY fully text-only clean pick left (fish `picture:null`); clean core (`main-idea-core.js` grades authored `topicId`/`belongs`). NOT a marina dup (marina RI.2.2 = Klasse-3 Selberlesen; this = Klasse-1 read-aloud + Schlüsselinformationen). **STRAND „Sprechen und Zuhören"** (read-aloud → Hörverstehen, NOT the RI „Lesen…" chip; rhyme-shop/sock-and-shadow precedent); GRADE_OVERRIDE de:'1' (K-cluster, atlas RI.K.1 precedent). **The build-gate is STRICT** — swap-invariance (every fish topic once → 4 rounds/group = 12) + an armed surface-cue solver ≤0.55 (topic key once/paragraph, seated mid, equal-length fish phrases/group); took 3 linguist passes; **gate GREEN first try** + I made `verify-main-idea-core.js` **locale-aware** (`--locale=de`, was untracked→committed) so it guards the German deck. Fixed a 1024×900 cut-off (long H1 prompt wrapped 3 lines → „Worum geht es?"). Mascot „Nila die Otterdame". 6 files. **Only willow RL.K.9 (comic-panel image-bound) now remains among RL/RI.** #113 = domino-two-part „Pauli der Pinguin" **Silben lesen / Silbenlesen** (RF.1.3.e→**Klasse 1**, `1e9faca7`; 15th 0-core WRAPPER; NEW **RF/Silbenlesen** facet — read a pre-split 2-syllable word → tap picture; the German Silbenmethode). PERFECT German fit (silbenweises Erlesen = core Kl-1 Anfangsunterricht). Distinct from #108 CVC-Erstlesen / #109 onset-rime-blend (sub-syllabic) / the already-German syllable-BUILDER (encode/drag-tiles). **0-core bridge (coin-stall philosophy):** `read-bisyllable-core.js` grades `choice.noun===round.word`; kept `word`=correct choice's ENGLISH image-key (Core.grade UNTOUCHED) + added `displayWord`/`displaySyl` (German split, shown) + per-choice `de` (German tile label); wrapper `setupTask` overlays de display, `speak` LANG-branched. 9 rounds (3/3/3 bands), all Duden-clean splits from the asset-verified pool, homonym-free (dropped Drachen=Kite/Dragon; Tiger/Flugzeug/Pfirsich as onset-orphans); decode-forcing 8/9 shared-onset + R6 Glocke/Socke shared-RIME „-ocke". NO GRADE_OVERRIDE (grade '1'→„1. Klasse"); STRAND_OVERRIDE de:„Lesen – mit Texten und Medien umgehen" (=#108/#109). Mascot „Pauli der Pinguin" (penguin SVG stays). **shades REJECTED** (dup of roary #34 „Bedeutungsnuancen", L.1.5.d/L.2.5.b word-strength twin); nila-pond/willow/word-clinic/wordclass/mending-basket/sunny-side-diner/bingo-word-hunt/syllable-vowel-teams+endings all deferred (dup / core-dirty / poor-fit / heavy-authoring). #112 = coin-stall „Otto der Otter" **Rechnen mit Euro und Cent** (2.MD.C.8→**Klasse 2**, `04b06f20`; 14th 0-core WRAPPER; NEW **Geld/Euro-Sachrechnen** strand — the canonical US$→Euro localization). `coin-stall-core.js` is **denomination-agnostic** (coinSetOf reads round.coinSet; valueOf/minCoins/fewestDens/gradeCompose/gradeChoose all use coinSet values; coins render programmatically from {glyph,tint,diameter} → NO images; Euro greedy-optimal 1-2-5 → fewest grades right) → clean 0-core. `coinSetL10n.de` (8 Euro coins 1c…2€, glyph=value, Wert≠Größe 10c<5c) + `roundsL10n.de` (14 rounds, all `legendTier:'none'` since Euro coins show value); `fmt` LANG-aware **Klasse-2 word form, NO comma** („30 Cent"/„1 Euro"/„1 Euro 50 Cent" — educator RULED comma-decimals are Kl.3/4, overriding the money-agent's „1,50 €"); `strings.de`; `_goalChip`/`_renderChoose` enough-opts („Zu wenig/Genau genug/Zu viel")/tray/`speak`/aria LANG-aware. NO GRADE_OVERRIDE (US '2'→de „2. Klasse"); STRAND_OVERRIDE de:„Größen und Messen" (Geld=KMK-Standardgröße). Mascot „Otto der Otter". **nila-pond REJECTED** (48+48-sentence content lift). #111 = daisy-plate-stack „Ella die Ente" **Einzahl und Mehrzahl / Pluralbildung** (L.K.1.c→**Klasse 2**, `4938555f`; 13th 0-core WRAPPER; NEW **Grammatik** strand). Text-only (word chips, no images). Genuine rebuild: English single +s/+es rule → German's 5 plural patterns. **The EN `plural-noun-core.js` is English-morphology-hardcoded (can't produce German plurals)** → German rounds carry EXPLICIT `{sentence,chips,answer}` + the wrapper `setupTask`/`isCorrect` LANG-branch to use them (0-core override, roundsL10n-with-explicit-data pattern). 10 rounds; linguist caught hazards (Ei/**Eis**=ice-cream→„Eien"; Buchs/Apfels/Balls genitive-word collisions avoided). GRADE_OVERRIDE de:'2' (Numerus=systematische Grammatik); STRAND „Sprache und Sprachgebrauch untersuchen" (morphologisch; ≠ olive semantic „Wortschatz"). Mascot „Ella die Ente". #110 = fraction-equiv „Krümel die Maus" **Äquivalente Brüche: gleich viel** (3.NF.A.3→**Klasse 4**, `759fbe42`; 12th 0-core WRAPPER; NEW **Brüche/fractions** strand). Pure-numeric engine (cross-multiply, universal rounds, NO roundsL10n) → only UI strings + prose localized. **Educator RULED Klasse 4 (not 3)** → AskUserQuestion → **operator chose "add Klasse-4 support"** → `grade_4` added system-wide (GRADE_KEY_MAP + gradeToAgeRange '4'→9-10 + seo.educational_level.grade_4 ×11: de „4. Klasse"/fr „CM1"/nl „Groep 6"/…); GRADE_OVERRIDE de:'4'; STRAND „Zahlen und Operationen". Child-facing „gleich viel" (NOT „gleichwertig"); TTS speaks fraction as word („ein Halb") via `bruchwort` helper; mascot „Krümel die Maus"; **fixed a hardcoded 'less'→txt('less')='weniger' in the nudge**. Non-dup (E14=geometry-partition; this=number-fraction equivalence). 17 files (incl. grade_4 infra). #109 = choice-board onset-rime-blend „Suse die Schnecke" **Lautsynthese: Anlaut+Reim zusammenschleifen** (RF.K.2.c→**Klasse 1**, `f54b8a6e`; 11th 0-core WRAPPER; **extends the existing en/nl/da onset-rime fan to de** via `params.byLocale.de`). See „onset · rime" („H · aus"), hear the rime (German TTS), blend → tap the picture; 8 rounds, rime-mate invariant ≥1 distractor shares the rime (Haus/Maus, Hund/Mund, Bein/Schwein, Fisch/Tisch, Zahn/Hahn). UPPERCASE onset (matches nl/da precedent; linguist recommended lowercase but I kept upper for family+prose+no-case-shift). GRADE_OVERRIDE de:'1'; STRAND „Lesen – mit Texten und Medien umgehen" (Vorstufe of #108). Mascot „Suse die Schnecke" (snail). **Bonus:** fixed onset-rime wrong/empty hints (generic „shape" → hintBlend/hintPickPicture, EN+de). wall/stone MISSING → -and/Stein dropped. #108 = choice-board read-cvc-word „Emil die Eule" **Erstlesen: Wörter lesen** (RF.K.3→**Klasse 1**, `c96587e2`; TENTH 0-core WRAPPER; on the familiar choice-board engine). Read a printed German word → tap its picture; `roundsL10n.de` = 9 rounds; decode-forcing invariant **≥1 distractor shares target's first 2 letters** (Hut/Hund/Huhn, Sonne/Socke/Sofa, Ball/Banane/Bagger). NO audio. **Image survey forced drops: `tree`=only-a-christmas-tree(„Tannenbaum"≠„Baum"), beard/needle/man/hare/digger MISSING → Baum/Bart/Nadel/Mann dropped.** GRADE_OVERRIDE de:'1'; STRAND „Lesen – mit Texten und Medien umgehen" (KMK reading Kompetenzbereich). Mascot „Emil die Eule" (owl). Wrapper = `choice-board-activity.js` read-cvc-word template `rwLoc`+roundsL10n branch + `promptReadWord.de` + **fixed the wrong/empty hints** (were generic „Form"/shape strings → reading-appropriate hintReadWhole/hintPickPicture, EN+de). read-cvc-word uses `_pool`+`nextTask` → per-pass reshuffle. In-iframe title „Auswahlaufgabe" = generic choice-board chrome (mascot lives on SSR page). #107 = stretch-giraffe „Gina die Giraffe" **Vokallänge: langer & kurzer Vokal** (RF.1.2.a→**Klasse 1**, `f460081d`; NINTH 0-core WRAPPER; grades locale-neutral `vowel:'long'|'short'` enum → needs stressed-vowel length NOT minimal pairs). `roundsL10n.de` = 9 rounds, ask strictly alternating (5 lang/4 kurz), **Band 3 = same-vowel-quality length minimal pairs** (Ofen/Sonne /o/, Hund/Hut /u/, Zahn/Kamm /a/ → "immer die lange" fails). Native-linguist SAFETY: every kurz/lang tag verified; **REJECTED Bus (regional length split), Banane (mid-stress), Wurm (r-vowel)**; **diphthongs Maus/Haus/Baum/Bein/Eule EXCLUDED** (neither kurz noch lang — the safety analogue of #106's length keys). NO GRADE_OVERRIDE (Klasse 1 = default for grade "1"); STRAND_OVERRIDE „Sprache und Sprachgebrauch untersuchen" (analytic, like #106). Mascot „Gina die Giraffe" (name carries a long vowel). `api.t` auto-picks `.de` (simpler than #106 — no custom txt); ask-aware nudge (hintWrongLong/Short). **pepper-sound-swap was the natural sibling but REJECTED** (German minimal-pair scarcity). #106 = sound-boxes „Coco der Koala" **Lautanalyse: Anlaut/Inlaut/Auslaut** (RF.K.2.d→**Klasse 1**, `3839b2aa`; **OPENS phoneme-analysis — operator-authorized**; EIGHTH 0-core WRAPPER). `roundsL10n.de` = 9 rounds (4 Anlaut/3 Auslaut/2 Inlaut, A→Aus→In); each token `{noun=EN img key, word=de, b/m/e sound keys}`; empty Lautkästchen = grapheme-safe. **Phoneme-safety = native-linguist verification + node-assert (cross-word key consistency) + image verification.** Linguist SAFETY catch: bare „a" for Ball+Zahn was WRONG → length-marked keys (a-kurz≠a-lang; u-lang Fuß=Hut; au/ei/ts/sch=1 Laut). **Image-safety fix: DROPPED Baum** (only tree image is a decorated christmas-tree → reads „Tanne"/„Weihnachtsbaum", unsafe /t/ vs /b/); A4→Bein→Ball, I1→Maus→Haus (identical /au/, safe). GRADE_OVERRIDE de:'1'; STRAND_OVERRIDE de:„Sprache und Sprachgebrauch untersuchen" (analytic — distinct from #102 Reime „Sprechen und Zuhören"). Mascot Coco der Koala (koala SVG stays). Runtime GREEN. **PATTERN established for remaining RF phonics** (pepper-sound-swap/cvc-builder/stretch-giraffe): native-verify phonemes + Read every image for word-ambiguity + node-assert. #105 = clock-convert „Kiko der Hahn" **24-Stunden-Zeit lesen / 12↔24h time** (2.MD.C.7→**Klasse 2**, `f6907633`; back to math/Größen; SEVENTH 0-core WRAPPER). German has no AM/PM → 12h side = „{h12}:{mm} {Tageszeit}" („3:00 nachmittags"). Rounds locale-neutral integers → NO roundsL10n; grading integer; shadowed `Core.givenStr/optionStr/to12str` via activity `de12str`+24-entry Tageszeit table (0-4 nachts/5-8 morgens/9-11 vormittags/12 mittags/13-17 nachmittags/18-21 abends/22-23 nachts). NO GRADE_OVERRIDE (Klasse 2 = manifest); STRAND_OVERRIDE de:„Größen und Messen". Mascot Kiko der Hahn (Kikeriki pun; rooster SVG stays). Build-fix: long German cards („9:00 abends") cramped at 360 → removed white-space:nowrap so they wrap 2 lines (EN-safe). Runtime GREEN. **⚠️ CLEAN TAIL EXHAUSTED after #105 — next fork is the operator's:** phoneme-analysis (sound-boxes/pepper-sound-swap/etc, need OK on hand-authored phonemes vs §20.7) / nila-pond (content-heavy) / accept dups / snippy (redundant handwriting). #104 = penny-alphabet-trace „Penny der Stift" **Buchstaben schreiben / letter formation** (L.K.1.a→**Klasse 1**, `4368eacd`; **OPENS the „Schreiben"/handwriting Kompetenzbereich** — last untouched KMK Bereich; SIXTH 0-core WRAPPER). Cleanest rebuild: A-Z/a-z glyph shapes are language-neutral → **NO roundsL10n** (EN 52 rounds verbatim); whole rebuild = string/framing localization. Educator: base 26+26 only (Umlaute = Grundbuchstabe+2 Punkte, not distinct formation letters; ß Klasse-2+; trace-dots awkward), letter order irrelevant for free-practice. GRADE_OVERRIDE de:'1' + STRAND_OVERRIDE de:„Schreiben – Schreibfertigkeiten" (NOT „Richtig schreiben"). Mascot Penny der Stift (pencil SVG stays). Build-fix: long German headline wrapped +66px at 320 → shortened prompt to „Am Punkt starten und nachspuren." (<EN len) → passes, EN untouched. Runtime trace-test GREEN (in-order→formed + „Wunderschön!" lang=de; out-of-order rejected). **Survey rejected sunny-side-diner (LEANS DUP: recombines Satzbau+Bindewörter-„und"+Mehrzahl).** #103 = mango-animal-groups „Mango der Affe" **Sammelnamen für Tiere / collective nouns** (L.2.1.a→**Klasse 2**, `52cbf834`; FIFTH 0-core WRAPPER, data-swap). `collective-noun-core.js` = pure word-matcher; `roundsL10n.de` = 8 all-animal rounds, 4 collectives (Herde ×3 Kühe/Schafe/Elefanten, Rudel ×2 Wölfe/Löwen, Schwarm ×2 Fische/Vögel, Volk ×1 Bienen); German twist (few Sammelnamen cover many animals) IS the lesson. Dropped grapes→Elefanten (all-animal frame). bee=Volk. **NO GRADE_OVERRIDE** (Klasse 2 = manifest grade "2"). STRAND_OVERRIDE de:„Wortschatz untersuchen und erweitern". Article-free Q „Wie nennt man viele {Plural}?" + bare capitalized tiles (gender-trap avoidance). **DEFERRED: sound-boxes/pepper-sound-swap** (clean-code RF wrappers BUT phoneme-analysis → need operator OK on hand-authored phoneme content vs the §20.7 phonics-safety pipeline; don't build ad-hoc). #102 = rhyme-shop „Rudi Reimbär" **Reime hören / phonological awareness** (RF.K.2.a→**Klasse 1**, `f141e515`; **OPENS the RF/Reime facet**; FOURTH 0-core WRAPPER). `rhyme-shop-core.js` = pure `rimeKey` matcher, all content manifest-authored (no phonics-safety pipeline). Full round rebuild via **`roundsL10n.de`** (German rhymes ≠ English) = 10 rounds from 9 ear-perfect Reim families (each token: `noun`=EN image key for the picture + German `rimeKey`/`spelledRime` + new `word`=German rhyme word); every picture verified on-disk NON-bw. Only -anne(Pfanne/Tanne/Kanne) is 3-member → anchors odd/sort/field/chain. **Kanne=TEAPOT image** (unambiguous; watering-can/kettle/pot rejected). Dropped Strauß (ostrich/Blumenstrauß homonym). Audio-first: tile label+TTS anchor each word (pictures secondary). Mascot Rudi Reimbär (raccoon SVG stays). GRADE_OVERRIDE de:'1' + STRAND_OVERRIDE de:'Sprechen und Zuhören' (fresh facet, shared Kompetenzbereich w/ #101). Wrapper edit note: rhyme-shop's `txt` read `LCS.i18n.current` — switched to module `LANG` from api.lang. Runtime shape-test GREEN (judge/pick-correct/pick-wrong/field + TTS lang=de). #101 = sock-and-shadow „Socke & Schatten" **referential description / genau beschreiben** (SL.K.6→**Klasse 1**, `7072f167`; **OPENS the Sprechen-und-Zuhören strand** — first SL activity in DE; THIRD 0-core WRAPPER). Simplest wrapper yet: the 7 rounds are pure enum keys → **no roundsL10n**, whole DE surface = label maps + `deUtter()`. Nominative naming fragment → uniform -e adjective (base+'e'), only article varies by gender (per-object `ART_DE`; das Auto=neuter trap; `welche {cat}?` forces feminine cats Farbe/Größe/**Stelle**). Re-theme Socke & Schatten; kept the mood round (Gefühle+weil-Begründung); GRADE_OVERRIDE de:'1' + **STRAND_OVERRIDE de:'Sprechen und Zuhören'** (no SL entry in strand-names.ts). Added a 0-core truthfulness fix: vertical-stack the position round (identical balls) so „oben/unten" is real. Two build-time visual-QA misfires caught: over-stacked the redundant-position car round (scoped `needsVert` to same-object+color) + long German mood chips wrapped 3 rows at 320 (shortened „Teddy weg"/„nicht ran"). Runtime grade-test GREEN (clear/ambiguous/overwhelmed/mood). #100 = contraction „Zwirbels Kurzform-Wirbel" **Präposition-Artikel-Verschmelzung** in dem→im/an dem→am/zu dem→zum (L.2.2.c→**Klasse 3**, `87c1c898`; **SECOND WRAPPER** — English apostrophe engine bypassed for de via activity-layer `deChips`/`deIsAnswer` + de-branched chip-gen/tap-grade/result/Hear/srMirror, 0 core lines; runtime grade-test confirmed „zu der"→„zur" resolves + foil doesn't; char Nib→**Zwirbel** [coral curl = Wirbel, no apostrophe needed]; `GRADE_OVERRIDE de:'3'`; NO strand override — „Language"→„Sprache untersuchen" grammar, foils = same-prep-diff-article + diff-prep-same-article over {im,am,zum,zur,beim,vom,ins,ans}). **This supersedes the earlier "contraction = DEFERRED, needs a new verschmelzung-core.js" note (lines 59/71) — it was buildable as a 0-core WRAPPER after all.** #99 = hattie-whose-is-it „Hatties Fundkiste" Namen-Genitiv/„Annas"-statt-„Anna's" (L.1.1.b→Kl.3, `510fcc25`; **FIRST WRAPPER build** — core-dirty engine localized via an activity-layer wrapper: German form-gen `deForms` [Name/Names/Name's] + de-branches on BOTH grade paths + inline sentence, 0 core lines; runtime grade-test confirmed; NEW `STRAND_OVERRIDE de:'Richtig schreiben'`; strand L/Rechtschreibung). **The WRAPPER pattern is now proven for the remaining core-dirty engines** (`contraction` Verschmelzung [needs the chips generated in the activity too, or a new core], `sunny-side-diner` [HARDEST — agreement engine is core-resident], `word-clinic`, `sock-and-shadow`). The **per-#N blow-by-blow (#1–#98) + every ensemble ruling lives in the out-of-tree plan file** `C:\Users\rkgen\.claude\plans\the-previous-session-you-groovy-pie.md` (the session working doc — read it to resume). Coverage now spans Math/Measurement/Geometry/Data + L (grammar + vocab) + RI + RL + W. Latest: #95 linc-fact-chain „Kettis Faktenkette" (RI.K.3→Kl.2, `3aea8cef`; Linc→Ketti), #96 two-tales „Zwei Monde, zwei Geschichten" (RL.1.9→Kl.2, `5bd54447`; Mossbeard→Moosbart; first COMPARE facet), #97 wake-up-pip „Wach auf, Pip!" Nacherzählen (RL.K.2→Kl.2, `5e276825`; Duke→Rex/Gus→Ben; added missing `instruction` key), #98 otto-picture-book „Ottos Bilderbuch" Bild-Text-Bezug/welches-Bild-zeigt-den-Satz (RL.K.7→Kl.1, `fe4ad003`; Otto die Eule; Mae→Mia/Bo→Ben; the fresh RL.K.7 picture↔text facet, clean 0-core caption-loc like #97).

**⚠️ CLEAN-PICK POOL NOW GENUINELY THIN after #98.** Reading is exhausted for clean picks (`nila-pond` RI.K.2 ≈marina dup; `willow-story-corner` RL.K.9 ≈#96 compare dup + hardcoded 'Both'/'Neither' core). **Next tier = harder (surface the approach at plan time): core-dirty → activity-layer WRAPPER (#24/#28/#30 pattern, NEVER edit the core):** `hattie-whose-is-it` (possessive→**Genitiv-s/Wessen**, L — RECOMMENDED NEXT, varies the strand; surveyed clean-wrapper: override `setupTask`+`isCorrect`+**`makeTasks.check` [2nd grade path — don't miss]**+the 2 inline render lines [`'This '+thing+' belongs to the '+noun`, `'Whose '+thing+' is it?'`]+strings+speak; German = „Annas"/„Anna's"[Deppenapostroph]/„Anna" 3-chip pick; 8 rounds; text/SVG, not image-bound), `contraction` (Verschmelzung, needs a NEW `verschmelzung-core.js`), `sunny-side-diner` (Satzbau — but the German agreement engine `utter()`/`agree()`/`FOOD`/`NUMWORD` is in the PROTECTED core, no lang-branch → HEAVIEST), `word-clinic` (Phonem-Graphem, poor German fit), `sock-and-shadow` (SL `utter()`); **image-bound → `_DE` noun map:** `mango-animal-groups` (≈olive-kind-of dup); **RF phonics (8 — gated behind the phonics safety pipeline);** **handwriting** `penny-alphabet-trace`/`snippy`. Dups to AVOID: `mims-baskets`/`daisy-plate-stack` (Oberbegriffe/Mehrzahl built), `nila-pond` (≈marina), `willow-story-corner`/`shades` (≈#96/#34), `wordclass` (adj/adverb poor-fit).

**Accumulated reusable doctrine (emerged #8–#93 — apply to every new build):**
- **Route override maps** in `frontend/app/[locale]/activities/[slug]/page.tsx`: `GRADE_OVERRIDE`/`effGrade` (US-CCSS-grade ≠ German-grade → add `<id>:{de:'N'}`) + `STRAND_OVERRIDE`/`effStrand` (CCSS-strand ≠ German-Leitidee). **K-cluster K-label:** grade-K school-content → `de:'1'` (Klasse 1) UNLESS content drives higher (bis-100 → Kl.2, bis-1000 arith → Kl.3); en stays Kindergarten. Strand rules: bar-graph/data→„Daten und Häufigkeit"; area/perimeter→„Raum und Form"; Zeichensetzung→„Richtig schreiben"; vocabulary→„Wortschatz untersuchen"; text-features/Textsorten/Autorabsicht/Buchrollen→„Lesen – mit Texten und Medien umgehen".
- **`strand-names.ts` ADDITIVE entries** (open a strand ONCE, de-only + en-fallback → auto-localizes the chip, no per-activity override): „Language"→„Sprache untersuchen" (#20) · „Vocabulary Acquisition and Use"→„Wortschatz untersuchen" (#22) · „Number & Operations—Fractions"→„Zahlen und Operationen" (#50) · „Reading: Informational Text"→„Sachtexte verstehen" (#78) · „Reading: Literature"→„Literarische Texte verstehen" (#82) · „Writing"→„Texte verfassen" (#90).
- **Two localization shapes** (both: add module `var LANG` set in `init` from `api.lang`): (a) custom `L`/`txt()` → make `txt` read `(L[LANG]&&L[LANG][k])||L.en[k]`, add `L.de`; (b) shell `api.t()` → just add `de:` to `strings`. Per-round content = `roundsL10n.de`/`storiesL10n.de` in the manifest (**keep the `kind`/`role`/`id`/`band` tags VERBATIM = the locale-neutral answer**; `_loadActivity` LANG-branch picks it). `speak()`/aria → `lang: LANG`.
- **0-core is the norm:** engines grading by a `kind`/`role`/`pos`/enum tag are locale-neutral → 0 core lines. English `words`/`overlap`/`matchesPurpose` a-z helpers are **AUDIT-ONLY**, NOT the play grader — don't be fooled into thinking the core is English-locked.
- **Image-bound engines** need a `_DE` noun map (English image-key for the webp; German label for caption/aria/speak); drawn-SVG/emoji engines need none. **Core-blocked engines** (English table baked in the core, e.g. `FORMS=['am','is','are']`) → activity-layer wrapper fns (`FORMS_DE` + grade-wrapper, the #24/#28/#29/#30 pattern), NEVER edit the core.
- **de.json prose:** inner quotes MUST be curly **U+201E „ / U+201C "** (straight `"` breaks JSON); §A.13.29 examples must match the SHIPPED rounds; plain „&" not „&amp;"; unique opener; about≥3/practices4/howToPlay3/learningGoals3. Char names KEPT (proper); ß not ss; Akkusativ „einen" (m -en); trennbare Verben.
- **§A.13.62 DoD per build:** `node --check` + JSON-valid + a node-assert (deep-equal EN tags + the activity's trap invariant) + `verify-activity-content-de.js` + `preflight-activity-routes.js` + `visual-qa-activity.js --activity=<id> --locale=de` then `--locale=en` (regression) then `--locale=de` again (restore de shots) + **personal Read 360/768/1024 DESKTOP-first** + EN-leak grep + `git diff --name-only` 0-core → commit+push, **NO deploy**. CC-adjudicated forks recorded per-#N in the plan file.

## Next
On operator "continue" → EnterPlanMode → survey the EN-only set (Explore, or the list below) → pick the next CLEAN, fresh-facet, non-dup, 0-core, non-image-bound candidate that **VARIES the strand** → 3-agent ensemble → build → §A.13.62 DoD → commit+push NO deploy. **~28 remain:** RF phonics (8 — hard/gated, English-phoneme-specific, mostly poor fit); RL — cleanest next pick `wake-up-pip` RL.K.2 retell (0-core beat-DAG, shell `api.t()` — cleanest scaffolding; BUT sequence/order mechanic ≈ #92 Erzählstruktur, dup-risk — differentiate as Nacherzählen), then `willow-story-corner` RL.K.9 [core has hardcoded 'Both'/'Neither' — wrapper it], `otto-picture-book` RL.K.7 [image-bound]; (`marlo-magnifier` RL.1.3 = DONE #94; `two-tales` RL.1.9 compare = DONE #96, char Mossbeard→Moosbart, storiesL10n/tagTextL10n pattern); L (mostly poor-fit/core-dirty — `wordclass` [adj/adverb absent in German], `daisy-plate-stack`/`hattie-whose-is-it`/`sunny-side-diner`/`mims-baskets`/`word-clinic` [hardcoded-English/image cores], `mango-animal-groups` [image-bound], `shades` [≈roary #34 dup], `penny-alphabet-trace`/`snippy` [handwriting, low payoff]); **`contraction` L.2.2.c = DEFERRED (CORE-DIRTY): English apostrophe-position mechanic (`insertApos`/`chipStrings`) can't map onto German im/am/zum Verschmelzung — a good future rebuild but needs a NEW `verschmelzung-core.js` (data is rich: im/am/zum/beim/vom/ins/ans/fürs/zur), NOT a 0-core fan-out;** RI (`nila-pond` RI.K.2 [≈marina Hauptthema dup]; `linc-fact-chain` RI.K.3 = DONE #95); `sock-and-shadow` SL.K.6 [core-dirty `utter()` template — AVOID]. **Math DONE** — all 3 math picks built: `clock-convert`=DONE #105 / `coin-stall`=DONE #112 (genuine Euro rebuild, the „money-already-DE" note was wrong) / `fraction-equiv`=DONE #110 (grade_4 added). ~11 remain are RF phonics (hard/gated) + L (core-dirty/poor-fit) + RI/RL comprehension + storybook. **Lessons: (1) §A.13.61 — confirm term/shape names + GRADE are German-appropriate (native educator authority; US CCSS grade ≠ German grade-by-Zahlenraum → reuse `GRADE_OVERRIDE`). (2) For `Object.assign({},Core,…)` engines, localize by OVERRIDING core methods (`_t` etc.) — the shell exposes `api.lang`, NOT `api.locale`. (3) never `*/` inside a JS block comment; `node --check` before visual-qa.**
