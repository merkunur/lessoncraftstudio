---
name: project-nt10d-design
description: "nt10-D (2026-09-21) — the LAST batch: 10 NEW printable worksheet types × (1 base + 5 variations) × 11 locales = 660 sheets; DESIGN COMPLETE (10 of 10 final design files, hub matrix 656 of 660), NOTHING BUILT; the lock, the rulings, the build trigger + recipe pointer, the facts the build must respect"
metadata:
  node_type: memory
  type: project
  originSessionId: 6e3efc5f-a5a4-461a-ab61-ea0e550bb5f1
  modified: 2026-09-21T11:07:37.123Z
---

# nt10-D — 10 new worksheet types + 5 variations each, all 11 locales (DESIGN COMPLETE 2026-09-21; NOTHING BUILT)

**Operator /goal (2026-09-21):** design 10 new types (after analysing the 115 live keys) with 5 pedagogically meaningful variations each, native-rebuilt in 11 locales, SEO-led (≥1,000 clicks/day from 660 sheets), one detailed design file per type; memory + CLAUDE.md updated so a later session builds them. **Deliverable of the DESIGN phase (done):** `docs/worksheet-gen/b4-designs/` = README (status, files, the 10 types + ids, the 12-step build recipe re-pointed to b4 names, why the hub failed, honest click model, open items 1-6, cross-type rulings, the hub-gate expectation matrix) · `_PANEL-FINDINGS.md` (the lock + per-locale heads/tiers/harvest counts + per-type pedagogy/faces/traps + click model) · `_STUDIO-BRIEF.md` · `_SUBSTRATE.md` · `_ROLE-{PEDAGOGY,DESIGN,CRITIC}.md` · **10 finals `<ID>-<key>.md`** (7 sections each: identity · base page · faces 2-6 · native rebuild ×11 · data + gates · SEO · hub contract) · `_work/` (4 panel reports + per type pedagogy/design/critic = 34 files) · `_records/` (seeds, 11-locale harvest + Nordic v2 + water-cycle probe, summary) · `hub-expectations.json` (exported by `tools/export-hub-expectations.js --batch=b4`, `--check` clean). CLAUDE.md §26 points here. Plan file: `C:\Users\rkgen\.claude\plans\resilient-spinning-goblet.md`.

**BUILD TRIGGER (a later session): "build the last batch of 10 worksheet types and their expansion"** → README first → `_PANEL-FINDINGS.md` → the 10 finals → run the recipe (steps 1-12). Never re-research selection/demand; never re-design (each final is the contract; native panels refine per the brief). ⚠ CLAUDE.md §20.9's "continue" mapping (Swedish fan-out) is back in force now that this design is COMPLETE.

## The lock (2026-09-21) — do not re-litigate
tangram K-353 · human-body K-354 · five-senses K-355 · weather K-356 · recycling K-357 · cloze G1-350 · odd-and-even G1-351 · pronouns G1-352 · **question-words G1-353** · rounding G2-346. Faces from K-358+ / G1-354+ / G2-347+ / G3-385+. `dominoes` DROPPED (content-swap faces; 0 Nordic school demand; pedagogy 3) → backlog materials family. `cube-nets` backlog (above band in 6 of 11). `finger-counting`, `spelling-practice` (duplicates sight-words / write-the-word / spelling-rules; de head owned by sight-words), `comparatives`, `daily-routine`, `number-sequences`, copywork, word snake, units conversion, sequencing, cursive rejected with reasons in `_PANEL-FINDINGS.md`.

## Hub matrix (design-time; the build's contract) = 656 of 660
66 each for tangram · human-body · five-senses · weather · odd-and-even · question-words · rounding; **recycling 65** (en F4 colour-the-bins refused: no national colour convention; it/nl/sv/da/no F4 contingent on national colour tokens → floor 651); **cloze 65** (da F3 plural gap refused: the `singular-plural` head owns the only honest da title); **pronouns 64** (possessives refused es `su` + fi `hänen`). Every gap is a recorded refusal, never a filler.

## Rulings every build step obeys (each in a final's §1-§7; the README's cross-type rulings hold them)
- Compound heads ×11 for human-body (`body_parts` theme slug) and weather (`weather` theme slug); fi recycling never "Lajittelu"; title-collision rule vs G2-221/G3-323 shipped titles (rounding: noun/verb token difference, stem-compared).
- **Printable-only decks ship NO answer key** → no title/meta/landing may promise "with answers" (a validator ban in rounding/tangram); the en/de tail is a recorded loss.
- The 4 science types need an 11-locale science strand literal (`strand-names.ts` has no Science row): de Sachunterricht · es Conocimiento del Medio · pt Ciências · fr Questionner le monde · it Scienze · nl Oriëntatie op jezelf en de wereld · sv Naturorienterande ämnen · da Natur/teknologi · no Naturfag · fi Ympäristöoppi — add additively in recipe step 2. The OA row (pt Álgebra / sv Algebra / nl Bewerkingen) and NBT row (de/da/no/nl suffixes) render wrongly for odd-and-even / rounding → per-locale strand literal from each final's table B.
- NEW primitives with render-measuring verifies: `primitives/tangram.js` (+ `data/b4/tangram-figures.js`, node gate: 7 tans, no overlap, area 1, unique seams for compose outlines, concave-capable), `primitives/body-figure.js` (viewBox 300×560, 16 anchors, 12 regions, hideable ids), `primitives/water-cycle.js` (viewBox 640×400, 5 anchors), `primitives/bin.js` (px geometry, `fill:'lid'|'all'|'none'`, mono lid marks). NOT new: the rounding hill (ruled OUT: the flat digit strip with in-font ↑/↓ wins), a pronouns/cloze/question-words/odd-and-even primitive. Additive options on `primitives/thermometer.js`: `bands` + `numerals:false` (G3-345 byte-identical, re-render poison).
- `lib/b4-common.js` (b3's `bank()` reads `data/b3/` only); `templates/components-b4/<key>.js` behind a `components-b4.js` barrel; `tools/gate-variation-distinct.js` needs a `b4` branch; `verify-hub-type-rows.js` reads only the b3 JSON today → wire the b4 JSON with `--warn-missing-keys` until the batch lands.
- Measured font facts: `≈` U+2248 and `→` U+2192 are OUTSIDE every `unicode-range` in `assets/fonts/fonts.css` → the rounding relation glyph is an SVG (both codepoints banned as text); `qa/lints.js` fails text <9 px so a `clock.js` face needs ≥74 px; `objForms.unique` in de is the ACCUSATIVE phrase for masculine nouns (cloze: bare noun + typed article); the b3 science factories (`makeSciencePairMatch` / `makeScienceCategorySort`) overflow the fi 677 stack or the 5-bin pill width → b4 `senseMatch` / `senseSortStage` / `binRow` replace them (never edit the factories).
- Pictures ruled by OPENING (every final's §4 closing line): weather sun→`sunny`, cloud→`cloudy` (the `cloud` file is a pink faced blob), rain→`rainy`; body: shoulder (bare torso) / neck (necklace) / chin / tongue / thumb excluded from chips, hair/toe carry arrows; recycling `kitchen tools/bottle` glass-or-PET excluded, composites/ceramics excluded, 37-item bank; five-senses 45 items (38 strong + 7 signed), nose is the weak organ picture (chips ≥46 px); pronouns 32 portraits (m 20 / f 12) with per-picture `minPx`, actor/firefighter/paramedic/veterinarian/baby_girl excluded, 4 no-vocab portraits added by `fileUri`; question-words subject portraits (constituent pictures were solvable without reading).
- Body 722 / fi 677 stacks proven per face in every final; `.ws-lane` 639; widths measured with the shell woff2 from `file://` (a bare page reads ~6 % narrow; one agent's early harness read empty spans → discarded); fi four-line titles capped at 3 lines by validators where a stack needs it (cloze base/plural/story, pronouns base/F1).
- The "free" lead: SEO metadata only, never visible (operator 2026-09-14). `coordinate.theme:''` for themeless types; `coordinate.mode` = the face string.

## Facts established (do not re-derive)
- 115 live keys → 125 after the build; next free ids at design time K-353 · G1-350 · G2-346 · G3-385; `apps.<key>` + axis keys ABSENT for all ten (register in step 2); 0 slug collisions across axes + 34,068 landings for every proposed head; `4-trinn` EXISTS (35 landings) → no G3 key.
- Demand: `scripts/seo-research/harvest-candidates.js` (NEW, seeded; the inventory-led harvester cannot see novel heads) + `summarize-candidate-harvest.js`; round 1 ×11 + Nordic v2 + water cycle in `_records/`; the panels' click model ≈ 970-2,050/day at 9-15-month maturity over 660 landings (tier arithmetic 1,060-2,970); en heads all HARD (win on long-tail face titles); es/pt worksheet-word register finding is a locale-config decision for the operator (README open item 2).
- Six `[NSR-FLAG]`s for the build panels: da stedord vs pronominer · no "Kroppen vår" · fi "Säätilat" · fi hän/se · sv "hen" · no glass+metal bin.
- Traps bought: backticks inside `node -e "…"` / `python -c "…"` in bash are command substitution → write scripts to files; a Nordic seed returning fewer hits after correction means the genre is thin; agents die at the session limit AFTER writing (check disk before relaunching); the Fable limit kills agents at their first step (relaunch on the current model).

## Commits (docs + `scripts/seo-research/*` + the exporter flag only; no push)
`0da5b1a8` (Phase A + first studio outputs) · `60159678` human-body · `db36a5ad` · `86f43f8b` · `e8745842` five-senses · `72452a00` tangram · `15376504` · `373b4695` weather · `fd5dcb8d` · `99fbb5bd` · `272f2282` · `c8321754` recycling · `a82f2619` · `e2671e1c` · `c60540fb` cloze · `62cec55f` · `ef880508` · `ebadc75c` · `2515f8b8` odd-and-even · `1711f941` · `1a51e534` pronouns · `3bc6d864` · `d501c990` question-words · + the close-out commit (rounding FINAL, README total 656, `hub-expectations.json`, exporter `--batch`, CLAUDE.md §26 COMPLETE).

## Backlog (evidence recorded, not built)
dominoes (cut-and-play materials family), cube-nets (de A Kl 3 / fr A-), an answer-key page for printable decks (generator change, ~republish), the rounding hill (Alt A), PARAM-999 odd/even (spare), the 8-target rounding inverse (spare), sentence true/false, word snake, comparatives, units conversion, sequencing (asset-blocked), cursive (glyph-blocked), es/pt worksheet-word register.
