# K-370 `family` (K/G1/G2) : FINAL design (editor merge, 2026-09-23)

Merged from `_work/K-370-pedagogy.md` + `_work/K-370-design-A.md` ("the tree the word names": busts in a soft tree, measured sex rule, derived tree placement) + `_work/K-370-design-B.md` ("The Portrait Wall": picture frames, placards, the rail legend, the frame tree for the template); every ruling and why: `_work/K-370-critic.md`. Concept: **A's tree skin and A's figure primitive on every given-family face, B's picture frame and B's grand-pair centring, B's layouts for F1 / F3 / F5, A's layouts for the base word block and F2, the pedagogy's kinship graph and six moves, and the LEAD'S BINDING RULING: every closed face shows ONE conventional invented family structure; only the open F5 template presumes no shape; every landing says every family is welcome.** (m) = re-measured 2026-09-23 by read-only node (scratch `K-370-measure.js`: Nunito 800 22 / 18 widths with the shell woff2 from `assets/fonts/fonts.css` over `file://`; `K-370-trace.js`: tracing widths from `data/tracing/letter-strokes.js textGlyphs` x `primitives/trace-path.js LM`; `K-370-oe.js`: `œ` glyph presence; plus `data/b4/pronouns.js`, `data/b4/locales/pronouns.<loc>.json`, `REFERENCE TRANSLATIONS/image-vocabulary.js`, `frontend/config/topics-taxonomy.json`, `scripts/seo-landing/gen-b3-landings.js LEVEL_KEYS`, `page/page.css`, `types/k/K-343-all-about-me-my-family.js`, `i18n/strings.<loc>.json`). *est.* = the engineer measures in the real render (`render/one.js`). No em-dashes.

**Boundary (load-bearing).** The catalogue already owns the child's OWN family: **K-343** "All About My Family: Draw and Count" (m: "Draw your family. Then show how many people, brothers, sisters and pets on the ten-frames"; de "Meine Familie: Zeichnen und zählen", es "Mi familia: dibuja y cuenta", pt "Minha família", fr "Ma famille", it "La mia famiglia", nl "Mijn gezin", sv "Min familj", da "Min familie", no "Familien min", fi "Minun perheeni") and **K-323** "All About Me" (+ **K-342** favourite things, **K-344** label the face, **K-345** "I can", **K-346** my name). No face here asks about the reader's own family, counts a family, labels a face or writes the child's own name, and no title starts with a possessive ("my / Meine / Mi / Minha / Ma / La mia / Mijn / Min / Minun"). Taxonomy names that carry the word family and must never be echoed (m): `fact-families` (Fact Families · Aufgabenfamilien · Familias de operaciones · Família de fatos · Familles d'opérations · Sommenfamilies · Räknefamiljer · Talfamilier · Regnefamilier · Laskuperheet), `syllable-reading` (**en "Word Families"**, **pt "Família silábica"**), `compound-words` (**es "Familias de palabras"**, **fr "Mots de la même famille"**). Other neighbours: **`pronouns` G1-352 + G1-371/372, G2-353..355** (no pronoun or possessive the child chooses; riddle frames never end in a possessive agreeing with the answer) · **K-225** picture-vocabulary (a kin word is never matched to a lone picture; it is read off the drawn RELATION lines) · **K-032..040** comparing-sizes (every portrait on a page is the SAME size; height never carries age or order) · **K-284** word-tracing (theme picture nouns; kin words are in no theme) · **question-words G1-353** (no wh-frames) · **articles K-288/306/307, G1-292** (no article chips; fr/es/it/pt articles live INSIDE the kin literal). **This family owns:** kin terms as a closed RELATIONAL system read off a GIVEN invented family, the generation concept, the family-tree diagram (read it, complete it from clues) and one open tree template; plus the lineage vocabulary a locale lexicalises (sv/da mormor/farmor/morfar/farfar, moster/faster, morbror/farbror; fi eno/setä). **Visual signature:** head-and-shoulders portraits in picture frames, all one size, hanging in one soft tree (tealSoft crown over the older rows, creamDeep trunk under the child's row), teal relationship lines drawn ON TOP: the tree is decoration, the lines are the fact.

## 1 Identity

| field | value |
|---|---|
| id / key / bands | `K-370` / `family` / base **K**; F2 **K** (`K-371+ TBD by the emitter`); F1 F3 F5 **G1** (`G1-381+ TBD`); F4 **G2** (`G2-360+ TBD`); allocated by a `tools/alloc-b5var-ids.js` clone of `alloc-b4var-ids.js` (exists, m) in `_PANEL-FINDINGS` table order. |
| subject | `apps.family = {default_subject:'letters', default_age_range:'5-7', exercise_type_axis_key:'family'}` (the lock default; `_PANEL-FINDINGS` open item 1 may move it to a social-studies bucket at Phase C, by measurement). Registered by a `register-b4-taxonomy.js` clone (exists, m). `assetClass: geometry`, `exerciseType: family`. |
| theme axis | **OFF** on all six faces: `themeAxis:{applicable:false}`, `coordinate.theme:''`. `family` is NOT a theme key (m: no `axes.theme` key matches fam/people/person). **0 library pictures** (§2 NOT used). |
| unitAxis | not applicable (variety comes from the seeded family, not a fan). |
| CCSS en (honest) | **none** (kin terms and family trees are social-studies readiness; no CCSS). No `educationalAlignment`. en landing: "kindergarten to grade 2 social studies readiness; no Common Core standard". Strand: NEW additive row `'Family'` in `frontend/lib/seo/strand-names.ts` (only `'Science'` `:74` and `'Language'` `:207` exist, m; neither carries family). |
| new primitives / data | `primitives/family-figure.js` + `qa/verify-b5-family-figure.js` · `primitives/family-tree.js` + `qa/verify-b5-family-tree.js` · `primitives/frame-tree.js` (F5) · `templates/components-b5/family.js` behind `templates/components-b5.js` · `data/b5/family.js` + `data/b5/locales/family.<loc>.json` via `lib/b5-common.js bank()` (§5). |

| loc | genre head (panels) | ASCII slug (collision-free, m) | level K / G1 / G2 (`LEVEL_KEYS`, m) | K-register core kin (M · F · grandSide pair · Z · B; panel audits) | national strand (NEW `'Family'` row, panel confirms) |
|---|---|---|---|---|---|
| en (US) | Family Members (face heads: Family Tree) | `family-members` | `kindergarten` / `grade-1` / `grade-2` | mom · dad · grandma · grandpa · sister · brother | Social Studies: Family |
| de | Familie (never "Meine Familie") | `familie` | `vorschule` / `1-klasse` / `2-klasse` | Mama · Papa · Oma · Opa · Schwester · Bruder (Kl 2: Mutter Vater Großmutter Großvater) | Sachunterricht: Zusammenleben, Familie (Lehrplan) |
| es (MX) | La familia (never "Familias de …") | `la-familia` | `preescolar` / `primer-grado` / `segundo-grado` | mamá · papá · abuela · abuelo · hermana · hermano | Formación Cívica y Ética (SEP/NEM) |
| pt (BR) | Membros da família (never bare "família") | `membros-da-familia` | `educacao-infantil` / `1o-ano` / `2o-ano` | mãe · pai · vovó/avó · vovô/avô · irmã · irmão (panel register) | História: eu, minha família (BNCC EF01HI02) |
| fr | Le vocabulaire de la famille | `vocabulaire-de-la-famille` | `maternelle` / `cp` / `ce1` | maman · papa · mamie · papi · sœur · frère | Questionner le monde: le temps, les générations |
| it | I membri della famiglia (never "La famiglia" alone) | `membri-della-famiglia` | `infanzia` / `classe-prima` / `classe-seconda` | mamma · papà · nonna · nonno · sorella · fratello | Storia (Indicazioni nazionali) |
| nl | Familie (+ K word gezin) | `familie` | `kleuters` / `groep-3` / `groep-4` | mama · papa · oma · opa · zus · broer | Oriëntatie op jezelf en de wereld (SLO kerndoelen) |
| sv | Familj och släkt (never "släktord") | `familj-och-slakt` | `forskola` / `ak-1` / `ak-2` | mamma · pappa · **mormor+morfar / farmor+farfar** · syster · bror | SO: familj och släkt (Lgr22) [NSR] |
| da | Familien | `familien` | `boernehaveklasse` / `1-klasse` / `2-klasse` | mor/mamma · far/pappa (panel) · **mormor+morfar / farmor+farfar** · søster · bror | Historie/dansk indskoling: familien (Fælles Mål) [NSR] |
| no | Familien | `familien` | `1-trinn` / `2-trinn` / `3-trinn` | mamma · pappa · **mormor+morfar / farmor+farfar** · søster · bror | Samfunnsfag: familie og slekt (LK20) [NSR] |
| fi | Perhe ja suku (never "sukupuu") | `perhe-ja-suku` | `esikoulu` / `1-luokka` / `2-luokka` | äiti · isä · mummo/isoäiti · vaari/isoisä (panel register) · sisko · veli | Ympäristöoppi: perhe ja suku (OPS 2014) [NSR] |

Slugs: none equals an existing `axes.*` slug in its locale (m: the only family-bearing slugs are `aufgabenfamilien`, `familias-de-operaciones`, `familia-de-fatos`, `familles-d-operations`, `sommenfamilies`, `raknefamiljer`, `talfamilier`, `regnefamilier`, `laskuperheet`, `familia-silabica`, `familias-de-palabras`, `mots-de-la-meme-famille`, `syllable-reading`, `fact-families`). de and nl both `familie`, da and no both `familien`: slugs are per locale, no clash.

**The rule that locks the type.** Every closed answer is re-derived from the family GRAPH and, on tree faces, from the DRAWN LINES, never from a label: each person carries a kinship `path` from the ego (M mother, F father, Z sister, B brother, `y` younger/baby; `MM` = mother's mother, `FB` = father's brother, `MZD` = mother's sister's daughter); the locale `kin` map turns a path into a word literal; an item is legal only if its question has **exactly one** referent. **The ONE structure (lead ruling, binding):** every closed face draws the conventional invented pedigree: a mother and a father joined by a couple bar, their children below, ONE grandparent couple above the `grandSide` parent (seed alternates M / F, so the Nordic word follows the drawn side), and, where the face needs it, that parent's sibling (aunt/uncle) and, at d3, a baby or a cousin. The seed varies sexes, looks, names, which side, which sibling; it never varies the shape. This is a property of the teaching tool (kin words are relational and need one fixed diagram to have one right answer), not a claim about families: F5 is shape-free and every landing carries the welcome sentence (§6). **Kin words are native literals in the family bank, never read from `image-vocabulary.js`** (m: it stores sv `grandfather = "Morfar"`, `grandmother = "Mormor"`, da `Bedstefar/Bedstemor`, no `Bestefar/Bestemor`, it `baby = "Bambino"` (= child), fr `Sœur`, and has no aunt/uncle/cousin keys).

## 2 The base page

**Concept.** "Who is who in Mia's family?" (K). One tree fills the upper two thirds: three generation rows of picture-framed busts (grandparents in the crown, parents on the branches, the child row in front of the trunk), joined by couple bars and descent lines. The ego's frame alone is coral, with a coral star and a name plate; every other frame carries a teal number disc 1..5 in a shuffled order. Below the tree, five kin-word chips, each with an empty dashed numeral box; the child writes the number of the person the word names. From across the room: one tree, six calm faces, five words; one move (word → relation → person) five times. Every bust is the same size (no size cue), sex reads from hair reach (measured by the gate), age from head:shoulder proportion and grey hair, so the two classic errors (mom ↔ grandma, mom ↔ dad) point at visibly different frames even on a mono copier.

**Chrome budget.** Body **722** (3-line title + 3-line instruction); every stack is designed to **677** (4-line fi title); slack goes to `minmax` rows.

**Layout d2**
```
.ws-body 675 x 722 : root <div class="fam-page" data-ws-content> grid rows: minmax(457px,1fr) 12px auto
familyStage 675 x 457 (svg, data-lcs-stage)                        frame = 104 x 120 picture frame, bust 96 tall
 y 10..130  row 0 : grand pair, centres c-66 / c+66 over M (grandSide M; mirrored for F)
 y 130..162 gap 32 : grand couple bar at y 70, descent x=c down to M's frame top
 y 162..282 row 1 : M centre c, F centre c+186 (pushed outward: grand frame edge c+118, sepGap 16, F edge c+134)
 y 282..314 gap 32 : couple bar y 222 from c+52 to c+134 ; descent x=c+93 ; sibling bar y 298 (16 above row 2)
 y 314..434 row 2 : ego + sibling, centres c+29 / c+157 (pitch 104 + 24)
 y 434..447 ego name plate overhang 13 (plate 26 high centred on the frame bottom edge)
 y 447..457 ground: teal line 3 + creamDeep band 7
 bbox x c-118 .. c+238 = 356 (+ disc overhang 11 left = 367) <= 675 ; centred (c = 277.5)
gap 12
kinWordBlock 639 x 190 : 2 columns [chip 196 x 56 | 10 | box 64 x 56] = 270 ; 270 + 48 + 270 = 588 <= 639
 rows 3 x 58 + 2 x 8 = 190 ; five words fill column 1 top-down (3), then column 2 (2)
stack 457 + 12 + 190 = 659 <= 677 <= 722
```
- **Picture frame** (B): rounded rect 104 x 120, rx 10, `teal` 3; inner line inset 5, `grid` 1.5 (the double "picture frame" edge); mat `white`; bust (`familyFigure`, 96 tall, 80 wide) bottom-centred in the 94 x 110 inner mat. **Number disc**: `teal` circle Ø 30, centre at the frame's top-left corner (+4, +10), Baloo 2 700 20 `white` numeral (`data-lcs-badge`); the bottom centre of every frame stays free for lines. **Ego**: frame stroke `coral` 4, coral 5-point star (outer r 13, inner r 5.5) centred at (96, 12), `namePlate({text:name, px:18, h:26})` centred on the bottom edge.
- **Tree skin** (A; behind everything, no stroke on the crown): crown = union of circles r 64 on every row-0 / row-1 frame centre + the rows' bbox inflated by 20 as a rect rx 60, fill `tealSoft`; its bottom (row-1 bbox + 20 = y 302) stays above row 2 (y 314). Trunk = rect 56 wide centred on the ego row's parent-couple midpoint (c+93), from the crown bottom to the ground, fill `creamDeep`, `teal` 2 outline, two 1.5 px bark strokes; row-2 frames stand in front of it. Ground: `teal` 3 line across the stage + 7 px `creamDeep` band.
- **Connectors** (teal 3, round caps; drawn after the skin, before the frames): couple bar at frame mid-height between inner frame edges; descent from the bar midpoint (or a lone child's parent-bar midpoint) straight down to a sibling bar 16 above the children's frame tops, drops into each child's frame top centre. No junction dot, no heart (no marriage is asserted).
- **Word block** (A): `kinChip` white, `teal` 2.5, radius 28, Nunito 800 **22**, centred, nowrap, inner **176**; box `blankNumeralBox({w:64, h:56, answer:n})` (`templates/components-b3/ordinal-numbers.js:151`, m: `.ws-blankbox`, dashed coral 2.5, stamps `data-lcs-answer` verbatim, never printed). A 30 px written numeral sits in 56 with 13 px margin (K answer floor 30, element floor 56). Widest d2 chip word (m, Nunito 800 22): de `Schwester` 109.0, es `hermano` 92.1, en `grandma` 93.1: all ≤ 176. d3 words: `kleine Schwester` **177.8 > 176** (m) → the de d3 literal is a panel matter; d3 never ships.

**Ladder** (guards key on these config keys, never the level index):

| key | d1 | d2 (ships) | d3 |
|---|---|---|---|
| `mode` | `base` | **`base`** | `base` |
| `people` | 4 (M, F, ego, Z or B) | **6 (grand pair on `grandSide`, M, F, ego, Z or B)** | 7 (+ one baby sibling) |
| `rows` | 2 | **3** | 3 |
| `grandSide` | n/a | **seed alternates M / F** | seed |
| `asked` | 3 | **5 (every non-ego person)** | 6 |
| `frame` w x h | 120 x 138 | **104 x 120** | 96 x 110 |
| `chipPx` | 24 | **22** | 22 |
| `register` | K | **K** | K |

**d3 ruling:** d3 adds the diminutive sibling word (baby sister / kleiner Bruder / lillebror / pikkusisko), a harder page of the same move; it never ships and no copy describes it. d1 has no grandparents (the generation row where the Nordic point lives), so it is not a face either.

**Composer (seeded, locale-neutral: the same graph, sexes, looks, badge numbers and word order in all 11).** Draw `grandSide`, ego sex, sibling sex; assign looks so no two people of one (age, sex) cell share a look; the ego's name = the seed's index into the locale's names of the ego's sex (`names`, §5; sex-tagged). Badge numbers = a shuffle of 1..5, redrawn until the badges read top-down / left-right are NOT monotone in generation (no "1 = grandma" habit). Word order = a shuffle of the 5 asked words, redrawn until the answer column (box numbers down column 1 then column 2) is neither 1..5 ascending nor descending and differs from the tree's reading order. Over 20 seeds incl. the shipped one, no box position holds the same answer > 40 % (the nt10-D staircase lesson: one bad order ships to 11 locales).

**Answer-hiding + uniqueness.** No kin word anywhere in the stage; the only text inside `[data-lcs-stage]` is the ego's name and the badge numerals. Stamps: frames `data-lcs-person data-lcs-path data-lcs-sex data-lcs-age data-lcs-look data-lcs-badge`, ego `data-lcs-ego`; chip rows `data-lcs-kin="<wordKey>"`; box `data-lcs-answer="<badge>"`. **verify():** for each chip, `persons.filter(p => kin[loc][p.path] === wordKey)` has exactly one member and its badge equals the box stamp (re-derived from the graph, never from the stamp under test); the tree gate re-derives every person's parents FROM THE RENDERED LINES and compares with the graph; asked words pairwise distinct; ego never asked; no page offers two words for one person. The printable ships no answer key (nt10-D ruling).

**Reused (exact).** `blankNumeralBox({w, h, answer, attrs})` (`templates/components-b3/ordinal-numbers.js:151`, m) · `namePlate({text, px, h})` (`templates/components-b4/pronouns.js:108`, m; px ≥ 16 enforced) · `strokeWordLane({text, w, h, glyphH, reps, stack, modelless, emptyLast, padLeft})` (`primitives/trace-path.js:597`, m) · `writingRow({w, h, glyphH, xHeight})` (`primitives/trace-path.js:681`, m) · `wordBank({words, wordPx})` (`templates/components-b2.js:228`, m) · `primitives/_svg.js` (`svgRoot el roundedRect line circle label esc`) · tokens `primitives/_tokens.js` · `.ws-blankbox` (`page/page.css:445`, m) · `.ws-tile--word` · `rng.shuffle/sample/int`.

**NOT used.** Any library picture: opened and rejected (pedagogy contact sheets `K-370-a.png` / `K-370-b.png`, re-opened by the editor): all 49 `occupations` people wear a job costume (chef hat, hard hat, uniform, stethoscope: "who is Mia's dad?" answered "the chef"); the only older-looking person is `occupations/judge` (robe); **no older woman exists in any theme**; `toys/baby`, `toys/baby_girl`, `toys/girl`, `classroom/student` are usable singly but mismatched as one family. `primitives/body-figure.js` (a full neutral body with no sex cue by design; full bodies bring HEIGHT onto the page; this family borrows only its line style) · `portraitStrip` / `portraitCard` (library `<img>` carriers) · `answerBox` (stamps `undefined`) · `cardGrid` (a grid severs the relation lines) · `sceneStage` · `codeColors` · `image-vocabulary.js`, `objForms` at render.

**NEW `primitives/family-figure.js`** (A's geometry; pure SVG, Node-testable; exports `familyFigure`, `LOOKS`, `AGE_GEOM`, `MIN_PX = 72`).
`familyFigure({age:'baby'|'child'|'adult'|'elder', sex:'f'|'m', look, tint:'tealSoft'|'coralSoft'|'creamDeep'|'white', px, id}) -> {svg, width, height, meta:{age, sex, look, hairBottom, jawY, earTopY}}`; viewBox **0 0 100 120** (a head-and-shoulders bust, bottom edge = mat bottom); `width = px * 100/120`; throws below `px 72` (the adult eye disc r 3 falls under 1.8 px). Outline `teal` 3 px at every size (user units = 3/scale, the body-figure rule), round joins; face and neck `white`; garment = `tint`; detail lines `teal` 1.5. No skin tone, no earrings, no make-up, no props (no stick, apron, tie, skirt, knitting, pipe); every tint appears in BOTH sexes' looks (gated), so colour never carries sex.

| age | head | neck | shoulders (garment) | eyes (teal discs) | extra |
|---|---|---|---|---|---|
| `adult` | ellipse cx 50 cy 44 rx 19 ry 23 (chin 67) | rect x 43 y 64 w 14 h 22 | `M 8 120 Q 10 92 32 86 L 68 86 Q 90 92 92 120 Z` (84 wide) | r 3 at (43,44) (57,44) | mouth `M 44 55 Q 50 60 56 55` |
| `elder` | as adult | as adult | as adult | as adult | **hair fill `grid`**; cheek arcs `M 34 50 Q 36 56 40 58` + mirror, stroke 1.5; optional `glasses` circles r 6.5 at the eyes, stroke 2, bridge `M 49.5 44 L 50.5 44` |
| `child` | ellipse cx 50 cy 50 rx 26 ry 28 (chin 78) | rect x 45 y 74 w 10 h 12 | `M 20 120 Q 22 96 38 90 L 62 90 Q 78 96 80 120 Z` (60 wide) | r 3.6 at (41,52) (59,52) | mouth `M 43 64 Q 50 70 57 64` |
| `baby` (d3 only) | circle cx 50 cy 60 r 30 | none | collar `M 24 120 Q 26 100 42 96 L 58 96 Q 74 100 76 120 Z` + collar arc | r 3.6 at (41,62) (59,62) | ONE curl `M 50 30 q 6 -6 2 -10 q -5 -2 -6 4` stroke 2; cheek discs r 3 `coralSoft` |

Head:shoulder width ratio adult 38/84 = 0.45 · child 52/60 = 0.87 · baby 60/52 = 1.15 (the age cue is proportion + hair tone, never size). **Sex = hair reach, gated by geometry:** stamps `hairBottom` (lowest hair y), `jawY = chin - 6`, `earTopY = head cy - 4`; every `f` look has `hairBottom >= jawY + 8` on at least one side; every `m` look has `hairBottom <= earTopY` except a beard / moustache (separate path touching the chin; never on `f`). Hair fill `ink` (54 luma) for child/adult, `grid` (192) for elder: the strongest mono contrast on the page. `LOOKS` ≥ 6 per (age, sex): adult/elder f `long-straight` (to y 84) · `ponytail` (tail to y 78) · `bun` (crown circle r 9 + side locks to y 72) · `braid` (to y 96) · `curly-long` (scalloped to y 82) · `bob-fringe` (sides to y 76); adult/elder m `short` · `side-part` · `curly-short` · `crew` · `short+beard` (`M 31 50 Q 32 72 50 74 Q 68 72 69 50`, hair tone) · `short+moustache` (`M 42 52 Q 50 48 58 52 Q 50 55 42 52`); elder-only m `bald-crown` (side tufts); child f `pigtails` (to y 84) · `long-straight` · `ponytail` · `braids` (to y 92) · `bob-fringe` (sides to y 84) · `bun` (locks to y 82); child m `short` · `spiky-fringe` · `curly-short` · `side-part` · `crew` · `swoop`; baby ONE look (sex not drawn; at most one baby per page). Elders: grey hair + cheek arcs always, glasses on 3 of 7 looks per sex. Root `<svg data-lcs-figure data-lcs-age data-lcs-sex data-lcs-look role="img" aria-label="">`, clip id `ff-<id>`.
**Gate `qa/verify-b5-family-figure.js`** (parses the RENDERED svg, never `meta`): per look x age, hair bbox bottom vs head chin (the f/m rule); eye radius ≥ 1.8 px at the rendered size; elder hair fill `grid`, others `ink`; tint set of f looks = tint set of m looks; token hexes only; figure inside its clip. Poison: f look with hair to y 62 · m look with a ponytail · elder with `ink` hair (each must FAIL).

**NEW `primitives/family-tree.js`** (the only place tree geometry is derived). `familyTree({persons, frame:{w,h}, nodeW = frame.w, labelH = 0, coupleGap = 28, sibGap = 24, sepGap = 16, rowGap = 32, skin:'tree'|'none', badges, egoId, plates, w = 675}) -> {svg, nodes:[{id,x,y,w,h}], width, height, trunkX}`. **Rows** = generation (grandparents 0, parents + their siblings 1, ego row 2). **Placement, in order:** (1) the ego row centred under its parents' couple midpoint at pitch `nodeW + sibGap`; (2) a parent's sibling stands on that parent's OUTWARD side at pitch `nodeW + sibGap`, a cousin centred under it; (3) the grand couple is centred over its child, or over the midpoint of the sibship (B); (4) **nobody stands above a person they do not descend to**: the in-law parent is pushed outward until its node clears every row-0 node by `sepGap` (A); (5) the bbox is centred in `w`; throws if it exceeds `w`. Connectors and skin as above. **Gate `qa/verify-b5-family-tree.js`** (render-measured): no two nodes overlap and every gap ≥ 14; rule (4) as an x-overlap test on the rendered nodes; every connector segment lies outside every frame and plate except at its endpoints and ≥ 10 px from any disc; every drop ends on its child's frame top ± 2; the crown bottom sits ≥ 8 above row 2; **every person's parents re-derived FROM THE DRAWN LINES (bar → drop → frame) equal the graph** (poison: swap two children's drops, must FAIL).

**NEW `primitives/frame-tree.js`** (F5 only; B's geometry, §3 F5).

**NEW `templates/components-b5/family.js`** (behind `templates/components-b5.js`, the b4 barrel clone that refuses duplicate export names; class prefix `fam-`, inline CSS; every stage stamps `data-ws-content`): `pictureFrame({person, w, h, badge, ego, name})` · `familyStage({family, frame, nodeW, labelH, skin, badges})` · `kinChip({text, px, w, h})` · `kinWordBlock({rows, cols:2, chipW:196, boxW:64, boxH:56})` · `genRail({pairs, frameW, frameH})` + `genPlacard({text, gen, answer})` (F1) · `traceRows({rows:[{badge, word}], w, glyphH})` (F2) · `nameBox({w:130, h:44, answer})` (F3: a `.ws-blankbox`-styled span holding `writingRow({w:126, h:40, glyphH:22})`) · `clueList({clues})` (F3) · `riddleRow({n, parts, slotW:150, slotH:36})` (F4) · `templateTree({meWord})` (F5).

**Alternatives (rejected).** (1) **Pedigree chart with full-body figures**: a full figure in a 120 px cell has a ~22 px head, and height re-enters the page (the comparing-sizes fence). (2) **One family photograph** (A): no relation lines, so "grandma" becomes "the old woman" and mormor vs farmor is invisible. (3) **The family house** (B): floors assert who lives with whom, which the lead's ruling forbids implying; windows cannot show which child belongs to which parent. (4) **The picnic row** (B): no lines, so the page degrades to age/size reading. (5) **B's plain portrait wall on the base**: equally buildable (622 px); rejected for identity, since ten of eleven genre heads are a TREE (family tree, Stammbaum, árbol genealógico, árvore genealógica, arbre généalogique, albero genealogico, stamboom, släktträd, stamtræ, slektstre) and the tree skin makes all given-family faces one look; B's frame and centring are kept.

**Risks → mitigations.** Sex / age legibility (answer-critical for mom ↔ dad, grandma ↔ grandpa, aunt ↔ uncle): a MEASURED rule plus a **human read-test before the wave**: print the d2 base and F3 in mono at 100 %, three adults name every frame; a look misread once leaves `LOOKS`. Mono print (m, Rec. 601 luma of the tokens): ink 54 · teal 80 · coral 150 · grid 192 · tealSoft 230 · coralSoft 233 · creamDeep 234 · white 255: hair ink vs grid survives any copier; garment tints may vanish (they carry nothing); the crown (230) behind white frames goes faint and the teal lines (80) stay the strongest marks, which is correct. Crown clutter at K: if the operator's mono print shows it competing with the lines, `skin:'none'` is a one-flag fallback (the gate's control already renders it). **Palette:** tokens only; no `codeColors` on any face. **Font floor:** smallest text 16 px (plates, clues); badge numerals 20 px.

## 3 Faces 2-6

All five are CODE faces (a `mode` knob on `build()` + a `verify()` branch, stamped only when declared, so the base stays byte-identical); none is PARAM because no base level is a different teaching move. Each has a distinct resolved d2 config (`tools/gate-variation-distinct.js`, exists, m), a distinct apparatus (tree + word chips / rail legend + word placards / tree key + tracing lanes / tree with name plates + clue list / ego frame + bank + riddle lines / an empty frame tree) and a distinct query face. Face numbers follow the pedagogy (the lead's ruling names the template F5).

### F1 : Family Generations: Oldest to Youngest (G1, CODE `mode:'generations'`)
**Move:** ORDER kin WORDS by generation (grandparents → parents' generation → children's generation) from what the word MEANS; no figure beside any word, so height and face can never carry the order.
**Layout** (B): top `genRail` = a `teal` 3 rail line across 639 with three hung frames, each 150 x 96 holding TWO busts side by side (`familyFigure` px 76, w 63, gap 8: elder f + elder m / adult f + adult m / child f + child m, so no sex attaches to a numeral), hanging by 16 px cords, a `teal` Ø 30 disc 1 / 2 / 3 (Baloo 2 700 20 white) 6 below each frame: rail 16 + 96 + 6 + 30 = **148**; no words in the legend. Then 6 rows x 3 `genPlacard`s (placard 205 = pad 4 + chip + 8 + `blankNumeralBox({w:48, h:44})` + pad 4 → chip inner **141**, Nunito 800 **18**, may wrap to 2 lines at a space, row h **52**); 3 x 205 + 2 x 12 = 639. **148 + 24 + 6 x 52 + 5 x 12 = 544 ≤ 677.** Widest row words (m, Nunito 800 18): `kleine Schwester` 145.5 (> 141: wraps at the space into the 52 row), `kleiner Bruder` 122.5, `baby brother` 110.7, `Großmutter` 101.1, `grand-mère` 101.1; no single WORD over 141 (a panel literal that is one word > 141 is refused from the row pool).
**Config d2** `{mode:'generations', rows:6, perRow:3, mixSideline:true}`: each row holds exactly one word of each generation (gen 0 grandX, gen 1 M F aunt uncle, gen 2 siblings, diminutives, cousins); ≥ 4 of 6 rows carry a sideline word (aunt / uncle / cousin / diminutive) so "grand-" is not the only cue; no row printed in answer order (1 2 3); at most one row in reverse order; the gen-0 word sits in each slot ≤ 3 times per page. d1 `{rows:4, paths:'direct'}`; d3 `{rows:6, noDirect:true}` (sideline words only).
**Verify:** each word stamps `data-lcs-gen` from its PATH (`gen = 2 - ancestorDepth`), never from the literal; boxes `data-lcs-answer` = rank; the three gens in a row pairwise distinct; the order / slot rules above; nl `neef` / `nicht` absent (cousin AND nephew/niece: word → generation is ambiguous); 0 figures inside any row.
**Query face:** generations ("family generations worksheet grade 1", "Generationen in der Familie Klasse 1", "las generaciones de la familia primer grado", "gerações da família 1º ano", "les générations cp", "le generazioni classe prima", "generaties groep 3", "generationer i släkten åk 1", "generationer i familien 1. klasse", "generasjoner 2. trinn", "sukupolvet 1. luokka").
**Refusals:** none (nl pool without neef/nicht: oom tante opa oma zus broer mama papa zusje broertje = 10 words, 6 rows x 3 with reuse across rows allowed).

### F2 : Family Words: Trace and Write (K, CODE `mode:'trace-words'`)
**Move:** PRODUCE the kin word for a person marked on a small family tree: find the numbered person, trace that person's family word, write it once alone. The K handwriting face of the family words (kin words are in no `word-tracing` theme pool, so this is new content, not K-284 again).
**Layout** (A, frames resized to the figure floor): left, the same `familyStage` at frame **80 x 96** (bust 72), 6 people, 4 of them with a `teal` Ø 28 disc 1..4 (Baloo 2 700 18): bbox (grand pair centred, in-law pushed) c-94 .. c+190 = **284** (+ disc overhang 10 = 294 ≤ 300); height 10 + 3 x 96 + 2 x 28 + 13 + 10 = **377**, centred vertically. Right, four rows 359 wide: disc Ø 30 + 8 + `strokeWordLane({text, w:321, h:58, glyphH:40, reps:1, stack:true, modelless:true, emptyLast:true})` = a dashed hollow trio + an empty trio (58 + 2 + 58 = 118). **4 x 118 + 3 x 16 = 520 ≤ 677**; width 300 + 16 + 359 = **675**. Trio h 58 keeps glyphH 40 (scale = min(40/70, (58 - 6)/82) = 0.571, m). Widest word at glyphH 40 (m): de `Schwester` 259 px + pad 18 = 277 ≤ 321; es `hermano` 220, en `grandma` 219, sv/da/no `mormor` 200: every K word in all 11 fits without the lane's silent shrink.
**Config d2** `{mode:'trace-words', people:6, badged:4, rows:4, model:'hollow', write:1, glyphH:40, frame:[80,96]}`. d1 `{rows:3, model:'solid+hollow'}`; d3 `{rows:4, model:'none', bank:5}`.
**Verify:** row `data-lcs-badge`, lane `data-lcs-text`; `kin[loc][person(badge).path] === lane text` for every row (the relation → word mapping is asserted because the model IS the word); every glyph in tracing coverage (`data/tracing/letter-strokes.js`); **the RESOLVED glyph height ≥ 40** (the lane shrinks a long word silently, `trace-path.js:620`: a shrunk word is a FAIL, never a pass); badged words pairwise distinct; no kin word in the tree; starters and school lines from `primitives/font-metrics.json` (`qa/verify-ruling-starters.js` discovers the lane).
**Query face:** writing the family words ("family words tracing kindergarten", "Familienwörter schreiben Vorschule", "palabras de la familia para trazar preescolar", "palavras da família para cobrir", "écrire les mots de la famille maternelle", "parole della famiglia da ricalcare", "familiewoorden schrijven kleuters", "skriv familjeord", "skriv familieord", "skriv familieord", "perheen sanat kirjoitus").
**Refusals:** none. Item-level: fr `sœur` / `petite sœur` excluded from the badge pool (m: `œ` has NO stroke data, `letter-strokes: no stroke data for "œ"`); a fr page still badges 4 of maman papa mamie papi frère.

### F3 : Family Tree: Read the Clues (G1, CODE `mode:'tree-clues'`)
**Move:** COMPLETE a given invented family tree by reading four sentence clues ("Leo is Mia's uncle.") and writing each name on the right name plate; the dominant "complète l'arbre généalogique cp" intent as a closed reading task.
**Layout** (B; A's tree-left / clues-right does not fit, m: node 130 x 3 + gaps + grand pair = 442-523 wide + a 214 clue card > 675): full-width tree, then the clues. Node = frame **80 x 98** (bust 80) + 6 + plate **130 x 44** = 148 high, `nodeW` 130. Rows: pad 10 + 3 x 148 + 2 x 28 + ground 10 = **520**. d2 row 1 (grandSide M): aunt, M, F at centres 65 / 219 / 377 (sibGap 24, coupleGap 28); grand pair centred over the aunt-M sibship midpoint 142 → 63 / 221; F clears the right grand node by 26 ≥ 16; row 2 ego / sibling at 221 / 375; bbox **442** ≤ 639, centred. Descent lines run in the 28 px gaps between plates (14 px clear each side). Then `clueList` 4 x 34 (Nunito 700 **18**, one line each, ≤ 80 chars *est.*, teal numeral bullets) = 136. **520 + 14 + 136 = 670 ≤ 677.** Given names (ego + 2) printed in SOLID plates (`namePlate` px 18, h 44 variant, stretched to 130); the 4 empty plates are `nameBox`es (dashed coral, a school line inside, glyphH 22). No number discs on this face.
**Config d2** `{mode:'tree-clues', people:7, sideline:1, given:3, clues:4, frame:[80,98], plate:[130,44]}`: the sideline person is the `grandSide` parent's sibling (so sv/da/no clues say moster/morbror or faster/farbror and fi eno/setä for real); **the 4 empty plates always include two people of the same (age, sex) cell** (e.g. uncle + dad, or aunt + mom), so a name cannot be placed from the figure alone: the clue's relation must be read on the drawn lines. d1 `{people:6, sideline:0, given:3, clues:3}`; d3 `{people:8, sideline:2, cousin:1, clues:5}`.
**Verify:** each empty plate stamps `data-lcs-path` + `data-lcs-answer="<name>"`, each clue `data-lcs-clue-path`; every clue path has exactly one plate; every empty plate is the target of exactly one clue; the name's `sex` tag equals the frame's `data-lcs-sex`; names on a page pairwise distinct; given names appear in no clue; no clue targets the ego; the same-cell pair rule; the tree gate's line re-derivation.
**Query face:** family tree + clues ("family tree worksheet grade 1", "Stammbaum Klasse 1 Arbeitsblatt", "árbol genealógico para primer grado", "árvore genealógica atividade 1 ano", "complète l'arbre généalogique cp", "albero genealogico da completare classe prima", "stamboom invullen groep 3", "släktträd åk 1", "stamtræ 1. klasse", "slektstre 2. trinn", fi "sukulaiset 1. luokka" (never sukupuu)).
**Refusals:** none.

### F4 : Family Riddles: Who Is It? (G2, CODE `mode:'relation-riddles'`)
**Move:** COMPOSE two relations in the head ("Mia's mom's sister is Mia's ___.") and write the one kin word from a word bank. This is where the Nordic compounds (mormor = mor + mor), fi eno / setä and the aunt / uncle layer are genuinely taught. No tree (a drawn tree would let the child read the answer off it and turn the face back into the base).
**Layout** (A + B): a top band: the ego's picture frame **80 x 96** (bust 72, coral, star, name plate overhang 13) + 16 + `wordBank({words, wordPx:18})` in the remaining 543 (1 row ~59 en, 2 rows ~106 sv/da *est.*): band **≤ 118**; gap 12; 8 `riddleRow`s: number disc Ø 26 + 10 + the riddle as inline text (Nunito 700 **18**, line-height 26) with the gap rendered INLINE at the `___` position as a 150 x 36 slot (`writingRow` glyphH 22 inside a dashed coral inline-block, `vertical-align:-12px`; G2 element floor 36, answer floor 22). Rows `minmax(52px, auto)`, gap 8: 8 x 52 + 7 x 8 = **472**; stack 118 + 12 + 472 = **602**; worst allowed case (2 rows wrapping to 78) = **654 ≤ 677**.
**Config d2** `{mode:'relation-riddles', rows:8, depth:2, paths:[MM,FM,MF,FF,MZ,FZ,MB,FB], bank:'distinctAnswers+2'}`: the 8 depth-2 paths once each, shuffled; the bank = the locale's DISTINCT answer words + 2 distractors that share a generation with an answer and fit no riddle (en {grandma, grandpa, aunt, uncle} + {mom, dad} = 6; sv/da {mormor, morfar, farmor, farfar, moster, faster, morbror, farbror} + {mamma, pappa} = 10). **Answers may repeat** where the locale has one word for two paths (en MM = FM = grandma: that sameness IS the lesson set against the Nordic pair); two riddles with the same answer are never adjacent; the instruction carries the "a word can fit more than one riddle" clause only in locales whose d2 answers repeat (a data flag, `repeatsNote`). d1 `{rows:6, depth:1}` (M F Z B + parent-of-sibling); d3 `{rows:8, depth:[2,3]}` (cousins, only where the panel authors cousin literals).
**Verify:** each slot `data-lcs-riddle-path` + `data-lcs-answer="<wordKey>"`; the gate recomputes `kin[loc][path]` and asserts equality; every answer is in the bank; no distractor is valid for any riddle; bank words pairwise distinct; no frame contains, immediately before the gap, an article or possessive that agrees with the answer (rule 11); no first-person frame (rule 12); each riddle measured ≤ 2 lines and ≤ 2 wrapped rows per page.
**Query face:** relationships riddles ("family relationships worksheet 2nd grade", "Verwandtschaft Klasse 2 Arbeitsblatt", "¿quién es quién? la familia segundo grado", "graus de parentesco 2º ano", "les liens de parenté ce1", "i gradi di parentela classe seconda", "familierelaties groep 4", "vem är vem i släkten åk 2", "slægtninge 2. klasse", "slektninger 3. trinn", "sukulaiset arvoitukset 2. luokka").
**Refusals:** none. nl `neef` / `nicht` legal here (relation → word is single-answer). fi frames are whole literals with genitive name forms ("Aadan äidin sisko on Aadan ___."; answer nominative).

### F5 : Family Tree Template (G1, CODE `mode:'tree-template'`, OPEN, no verify())
**Move:** the child builds THEIR OWN tree: draws faces and writes names in empty frames that presume no family shape. The one personal face; every frame is optional.
**Layout** (B's `frameTree`; A's 3 x 3 version needs 684 > 677, m): a tree drawn in `teal` 3 outline on cream, **no canopy fill**: trunk from (292,520)-(348,520) narrowing to (304,300)-(336,300); tapered branches 8-10 px wide from the trunk top to the hooks. Nine empty mats (shapes cycle `oval, rect, round, arch`; drawing area ≥ 96 x 96; `teal` 3) hanging 10 px below their hooks by two-line cords (`teal` 1.5), each over a 130 x 40 `writingRow` name line: tier 1 mat centres y 70 at x 120 / 320 / 520; tier 2 y 230 at x 90 / 230 / 410 / 550; tier 3 y 400: the `me` mat on the trunk at x 320 (the ONLY printed label: a plate with the locale `meWord`, Baloo 2 700 20) and one free mat at x 530. Name lines tier 2: 25-155 / 165-295 / 345-475 / 485-615 (10 px apart); tier 2 lines end y 324 < tier 3 mats at 352. Tree 639 x **520**; below, a `teal` 3 shelf line with 4 small empty mats 72 x 72 standing on it (104 high): **520 + 16 + 104 = 640 ≤ 677**. No couple bar, no descent line, no rows, no kin label: a child in care, with one parent, two mothers, two fathers, grandparents, step-family or friends fills the same frames without meeting a wrong box.
**Config d2** `{mode:'tree-template', mats:9, shelf:4, meWord:true, links:0}` (d1 = d3 = d2; recorded, only d2 publishes).
**Gate (layout only):** no `kin[loc]` literal and no M / F literal anywhere in the rendered text; ≥ 12 empty mats; 0 connector paths between mats; no branch passes through a mat or name line; the instruction contains none of the locale's `forbiddenInTemplate` list (M / F words, "your family", "draw your family" equivalents).
**Query face:** the template head ("family tree template for 1st grade", "Stammbaum Vorlage Grundschule", "árbol genealógico para rellenar", "árvore genealógica para completar", "arbre généalogique vierge", "albero genealogico vuoto da stampare", "stamboom invullen leeg", "släktträd mall", "stamtræ skabelon", "slektstre mal", fi "sukulaiset: täytettävä pohja" (panel; never sukupuu)).
**Refusals:** none; a panel may add a `pets` mat label only as optional data (never printed as a required frame).

**Rejected non-moves.** varied family SHAPES on closed faces (one-parent, grandparent-home, two mothers / fathers: the lead's ruling; the pedagogy's `allowShapes` is withdrawn) · "types of families" sort (no single answer; it judges real families) · "how many in the family? count and write" (K-343) · boy or girl chips (sex reading, not kin) · male / female pair matching (a lexical pairing F4 already carries, and it presumes a couple) · pronoun choice in family sentences (pronouns G1-352) · first-person riddles ("My mom's mom is my ___" presumes the reader has a mom and "I am my mom's ___" has two answers) · "draw your family" as a closed or labelled task · the reserve face "Mom's side and Dad's side" (a good face, held in reserve: the lead confirmed F5) · theme-swapped families · relabelling base d1 / d3.

## 4 Native rebuild plan x11

Per locale the panel authors: the **kin map** (path → wordKey, ~22-26 paths; incl. diminutive siblings and cousins) + **words** (wordKey → literal, with the article INSIDE the literal in fr/es/it/pt where a frame needs one; each tagged `register:'K'|'G2'`) · **names** (≥ 6 f + 6 m, sex-tagged, initialised from `data/b4/locales/pronouns.<loc>.json` / `data/b4/pronouns.js PRONOUNS.en.names`, m: en 12, de 12, es 13, pt 13, fr 13, it 12, nl 12, sv 12, da 12, no 12, fi 16, longest 6 chars) + a `gen` literal per name where the locale inflects (en Mia's, de Mias / Max', nl Mia's, sv/da/no Mias, fi Mian; fr/es/it/pt `null`, the frame says "de Léa" / "da Alice") · **clue frames** (F3, one whole literal per path, slots `{name}` `{egoName}` / `{egoGen}`) · **riddle frames** (F4, 8 whole literals, slot `{egoName}`/`{egoGen}` only) · `generationOK` (F1 row pool) · `meWord` · `forbiddenInTemplate` · `repeatsNote` flag · `familyHead`, 6 titles, 6 instructions. **0 noun forms read from the vocabulary, 0 `objForms`.** Frames never inflect a token; a name is the only slot, and its genitive is a stored literal. The EN is a SOURCE TO AUDIT (critic §2), not a target.

| loc | literals | forms / slots | refusal | traps |
|---|---|---|---|---|
| en | kin ~22 · names 12 + `gen` · clues ~8 · riddles 8 · 12 strings | `{egoGen}` "Mia's" | none | US "mom"; en MM = FM = grandma (F4 repeats; base draws one pair only); never "Word Families" in a title |
| de | ~24 (K Mama Papa Oma Opa · Kl 2 Mutter Vater Großmutter Großvater) · 12 names + gen | "… ist Mias ___." | none | nouns capitalised in every literal; never "Meine Familie"; Cousin/Cousine; one register per page |
| es (MX) | ~24 incl. hermanito/a; article inside ("la tía") | "… es ___ de Sofía." | none | MX register mamá/papá (abuelito/a is a panel call); never "Familias de …" |
| pt (BR) | ~24 (vovó/vovô vs avó/avô tagged); gen `null`, name article in the frame literal ("da Alice" / "do Davi") | "… é ___ da Alice." | none | never bare "família" (Família silábica, Família de fatos); "atividade" never in a title |
| fr | ~24 with article (la tante, le cousin); K mamie / papi | "… est ___ de Léa." | F2 badge pool drops sœur (œ not traceable, m; printable elsewhere: the glyph IS in the Nunito / Baloo 2 subset, m) | never "La famille maternelle" (= the mother's side); "Mots de la même famille" collision; NBSP before `?` `:` |
| it | ~24 with article (la zia, il cugino) | "… è ___ di Sofia." | none | third-person frames avoid "mia madre" vs "la mia mamma"; never "La famiglia" alone (CQU genre); a name that is a word ("Mia" = my) never in the names list |
| nl | ~20 (neef / nicht one pair) | "… is Mia's ___." | F1 pool excludes neef / nicht | familie vs gezin (K-343 "Mijn gezin") |
| sv | ~26 (mormor morfar farmor farfar moster faster morbror farbror lillebror lillasyster kusin) | "… är Mias ___." | none | never "släktord" (= word families); "Min familj" owned by K-343; `\b` is ASCII-only in any sv lint [NSR] |
| da | ~26 (as sv; mor / far vs mamma / pappa is a panel call; fætter / kusine) | "… er Mias ___." | none | lineage words keyed on path (MM → mor-) [NSR] |
| no | 22-26 (generic tante / onkel OR lineage moster / faster: ONE convention, panel) | "… er Mias ___." | none | bokmål; "Familien min" owned by K-343 [NSR] |
| fi | ~20 (isoäiti / mummo ONE register; eno / setä; täti; serkku); 16 names + gen | whole literals ("{egoGen} äidin sisko on {egoGen} ___.") | none | **"sukupuu" in NO field** (titles, slugs, meta, landings: it autocompletes to "sukupuutto" = extinction); a nominative token dropped mid-sentence is ungrammatical, so every frame is written out [NSR] |

Every panel OPENS one render of every face in its locale (the busts, the tree, its own kin words in place) and signs the kin table against the paths.

## 5 Data + gates

`data/b5/family.js` (locale-neutral, hand-authored) + `data/b5/locales/family.<loc>.json` GENERATED from `i18n/.draft-b5-<loc>.json` by `tools/apply-b5-locale.js` after `tools/validate-b5-draft.js` (clones of `apply-b4-locale.js` / `validate-b4-draft.js`, both exist, m; `data/` is gitignored, `git add -f`), read through `lib/b5-common.js bank('family', loc)`. `apply-b5-locale.js` writes BOTH `strings.<mode>` AND `i18n/strings.<loc>.json[<ID>]`; `tools/check-b5-string-parity.js` (clone of `check-b4-string-parity.js`, exists, m) asserts they are identical.
```
// data/b5/family.js
module.exports = {
  structure: 'conventional',                       // the ONE structure (lead ruling); build() throws on anything else
  paths: ['', 'M','F','Z','B','Zy','By','MM','MF','FM','FF','MZ','MB','FZ','FB','MZD','MZS','MBD','MBS','FZD','FZS','FBD','FBS'],
  genOf: (path) => 2 - ancestorDepth(path),        // gen 0 grandX, 1 parents + their siblings, 2 ego row
  looks: { /* familyFigure LOOKS ids per (age, sex) */ },
  riddlePathsD2: ['MM','FM','MF','FF','MZ','FZ','MB','FB'],
};
// data/b5/locales/family.<loc>.json
{ "kin":   { "M":"mom", "F":"dad", "MM":"grandma", "FM":"grandma", "MF":"grandpa", "FF":"grandpa", "Z":"sister", "B":"brother",
             "Zy":"babySister", "By":"babyBrother", "MZ":"aunt", "FZ":"aunt", "MB":"uncle", "FB":"uncle", "MZD":"cousin", … },
  "words": { "mom":{"text":"mom","register":"K"}, "grandma":{"text":"grandma","register":"K"}, … },
  "names": [ {"name":"Mia","sex":"f","gen":"Mia's"}, {"name":"Ben","sex":"m","gen":"Ben's"}, … ],
  "clueFrames":   { "MZ":"{name} is {egoGen} aunt.", "F":"{name} is {egoGen} dad.", … },
  "riddleFrames": { "MZ":"{egoGen} mom's sister is {egoGen} ___.", … },
  "distractors": ["mom","dad"], "repeatsNote": true,
  "generationOK": ["grandma","grandpa","mom","dad","aunt","uncle","sister","brother","cousin","babySister","babyBrother"],
  "meWord": "me", "forbiddenInTemplate": ["mom","dad","mother","father","your family"],
  "familyHead": "Family Members",
  "strings": { "base":{"title":"…","instruction":"…"}, "generations":{…}, "trace-words":{…}, "tree-clues":{…}, "relation-riddles":{…}, "tree-template":{…} } }
```
Helper contract: `bank('family', loc)` returns the merged object; `kinWord(loc, path)`, `nameFor(loc, sex, index)`, `stringsFor(loc, mode)` THROW on a missing literal (never fall back to en).

**`tools/validate-b5-draft.js` (`family` block; exit 1 on any):**
1. Every path in `paths` used by any face has a `kin` entry and every `kin` value exists in `words`; `kin` is keyed on the path ONLY (no name- or page-keyed entry).
2. sv / da (and no when `lineage:true`): every path whose first step is M maps to a literal starting `mor`, every F-path to `far` (mormor, morfar, moster, morbror / farmor, farfar, faster, farbror): the lineage inversion is mechanical, never left to review.
3. `names`: ≥ 6 f + ≥ 6 m, each ≤ 7 chars, pairwise distinct, none equal (case-fold) to any `words` literal, `meWord`, or a frame token (it "Mia" = my is refused); `gen` present where the locale's frames use `{egoGen}`.
4. Word → person uniqueness for every asked item on base / F2 under the ONE structure with either `grandSide`: `|{p : kin[p.path] = w}| = 1` (en draws one grand pair only).
5. `brother` and `By` (or `sister` and `Zy`) are offered together only when the graph holds both an older sibling and a baby of that sex; at most one baby per page.
6. F3: name `sex` = frame sex; names on a page distinct; no given name in a clue; the empty plates contain one same-(age, sex) pair.
7. F4: every answer in the bank; bank words distinct; distractors share a generation with an answer and fit no riddle; `repeatsNote` true IFF the locale's d2 answers repeat.
8. F1: the three words in a row have distinct `genOf`; `generationOK` contains no word whose generation is ambiguous in the locale (nl `neef`, `nicht`); each is ≤ 141 px per single word at Nunito 800 18 (render-measured).
9. F2: every glyph of every K word in tracing coverage; resolved glyphH ≥ 40 at lane w 321 (render-measured).
10. No clue / riddle frame carries, immediately before `___`, an article or possessive from the locale list that agrees with the answer (panel tags `agreesWithAnswer:false`; the validator also greps).
11. No frame is first person (locale list: my / mein / meine / mon / ma / mes / mi / mis / meu / minha / mio / mia / mijn / min / mitt / mine / minun / -ni).
12. Titles ≤ 70 chars, no worksheet-word, no visible free-word, unique across the 6; fi: no field contains `sukupuu`; pt: no title is bare `família`; it: no title is bare `La famiglia`; de: no title starts `Meine Familie`; sv: no field contains `släktord`; no title starts with the locale's "my".
13. One register per page (`words[w].register` identical across a page's words; K faces use `K`).
14. `strings.tree-template.instruction` contains no `forbiddenInTemplate` literal.
15. Instructions ≤ 150 chars, ONE sentence, naming only apparatus on the face (base: tree, numbers, boxes, words; F1: numbers, words, boxes; F2: numbered person, dotted word, line; F3: clues, name boxes; F4: riddles, box of words, lines; F5: frames, lines); no "tick", no "cut", no "colour".
16. No title / meta / landing literal promises an answer key; `strings` keys === the 6 mode strings.

**`qa/verify-b5-family.js`:** renders base + 5 faces x 11 locales x d2 x 20 seeds through `render/render-instance.js` (file:// fonts) under the 722 stack (3-line de title + 150-char instruction) AND the 677 fi case; asserts `verify()` empty, `qa/lints.js` clean, both new primitive gates clean; floors itself (no size lint exists): K busts ≥ 72 tall and frames ≥ 80 wide, K chips ≥ 56 tall, K numeral boxes 64 x 56; G1 plates / boxes ≥ 44 tall; G2 riddle slots ≥ 36; every text ≥ 16; no kin literal inside any `[data-lcs-stage]`; every chip / placard `scrollWidth <= clientWidth`; F4 ≤ 2 wrapped rows; stamped uniqueness re-derived on the RENDER; 0 `data-lcs-pic`; everything above the footer; every printed literal === the locale bank.

**Poison (each must FAIL for its OWN reason; the correct draft is the control):** P1 en base drawing BOTH grandparent couples (grandma has two referents) → rule 4 · P2 a closed face built with a one-parent graph → `structure` throw · P3 d3 offering `brother` + `By` with only a baby → rule 5 · P4 F3 name Leo (m) on a grandmother plate → rule 6 · P5 F3 whose empty plates are four different (age, sex) cells → rule 6 · P6 F1 row `grandma · grandpa · cousin` → rule 8 · P7 nl `generationOK` with `neef` → rule 8 · P8 fr F2 badge on `sœur` → rule 9 · P9 fr riddle "… est ma ___." → rule 10 · P10 en riddle "My mom's mom is my ___." → rule 11 · P11 fi title "Sukupuu: lue vihjeet" → rule 12 · P12 sv `kin.MM = "farmor"` → rule 2 · P13 F5 instruction "Draw your mom and dad." → rule 14 · P14 de page mixing `Oma` and `Großvater` → rule 13 · P15 F4 distractor `grandma` on an en page → rule 7 · P16 it name "Mia" → rule 3 · **render:** PR1 a kin word printed under a frame · PR2 badge order monotone by generation on the shipped seed · PR3 swap two children's descent drops (the tree gate must FAIL; the stamps unchanged) · PR4 an f look with hair to y 62 · PR5 de `Schwester` in a 250 px lane (silent shrink below glyphH 40) · PR6 F5 with one connector between two mats · PR7 base guard written `difficulty === 2` fed a face config → the config guard fires · PR8 a `codeColors` fill anywhere.

**Page reads:** `bank('family', loc)`, the three new primitives, `data/tracing/letter-strokes.js` (F2), `primitives/font-metrics.json`; NEVER `image-vocabulary.js`, `pictureFor`, `objForms`, `approved-words-*.json` or any library picture at render.

## 6 SEO plan

| face | title pattern (Germanic en / de / nl · Romance es / pt / fr / it · Nordic sv / da / no + fi; panels rewrite) | meta MIDDLE (en; the child's instruction; whole 120-170 measured by `tools/measure-instruction-window.js`, exists, m) | coordinate |
|---|---|---|---|
| base | Family Members: Who Is Who? · Familienmitglieder: Wer ist wer? · Familie: wie is wie? · La familia: ¿quién es quién? · Membros da família: quem é quem? · Le vocabulaire de la famille : qui est qui ? · I membri della famiglia: chi è chi? · Familj och släkt: vem är vem? · Familien: hvem er hvem? · Familien: hvem er hvem? · Perhe ja suku: kuka on kuka? | Find each person in Mia's family tree and write their number in the box next to the family word | `{type:'family', mode:'base', theme:'', level:<K>}` |
| F1 | Family Generations: Oldest to Youngest · Generationen in der Familie: von Alt nach Jung · Generaties in de familie: van oud naar jong · Las generaciones de la familia · As gerações da família · Les générations dans la famille · Le generazioni in famiglia · Generationer i släkten · Generationer i familien · Generasjoner i familien · Sukupolvet perheessä | In each row, number the three family words 1, 2 and 3, from the oldest generation to the youngest | `mode:'generations'`, G1 |
| F2 | Family Words: Trace and Write · Familienwörter: nachspuren und schreiben · Familiewoorden: overtrekken en schrijven · Palabras de la familia: traza y escribe · Palavras da família: cubra e escreva · Les mots de la famille : trace et écris · Le parole della famiglia: ricalca e scrivi · Familjeord: spåra och skriv · Familieord: spor og skriv · Familieord: spor og skriv · Perheen sanat: jäljennä ja kirjoita | Find the person with the same number on the tree, trace their family word and write it again on the line | `mode:'trace-words'`, K |
| F3 | Family Tree: Read the Clues · Stammbaum: Lies die Hinweise · Stamboom: lees de aanwijzingen · Árbol genealógico: lee las pistas · Árvore genealógica: leia as pistas · Complète l'arbre généalogique · Albero genealogico da completare con gli indizi · Släktträd: läs ledtrådarna · Stamtræ: læs ledetrådene · Slektstre: les hintene · Sukulaiset: lue vihjeet | Read the clues about Mia's family and write each name in the right name box on the family tree | `mode:'tree-clues'`, G1 |
| F4 | Family Relationships: Riddles · Verwandtschaft: Wer ist das? · Familierelaties: raadsels · Parentesco: adivinanzas de la familia · Graus de parentesco: charadas · Les liens de parenté : devinettes · I gradi di parentela: indovinelli · Vem är vem i släkten? Gåtor · Slægtninge: gåder · Slektninger: gåter · Sukulaiset: arvoitukset | Read each riddle about Mia and write the right family word from the box on the line | `mode:'relation-riddles'`, G2 |
| F5 | Family Tree Template · Stammbaum-Vorlage · Stamboom om in te vullen · Árbol genealógico para rellenar · Árvore genealógica para completar · Arbre généalogique vierge · Albero genealogico vuoto · Släktträd att fylla i · Stamtræ til udfyldning · Slektstre å fylle ut · Sukulaiset: täytettävä pohja | Draw the people who are important to you in the frames and write their names, using as many frames as you like | `mode:'tree-template'`, G1 |

All titles are candidates the panels rewrite (≤ 70, no worksheet-word, the base owns the family head, each face adds ONE element; validator rule 12). `coordinate.mode` is ALWAYS the face's mode string (base `'base'`), `theme:''`, level = K for base / F2, G1 for F1 / F3 / F5, G2 for F4. h1 = title; eyebrow = level label; strand chip = the NEW `'Family'` row; non-EN prose names the framework NAME only (Lehrplan, SEP/NEM, BNCC, programmes officiels, Indicazioni nazionali, SLO kerndoelen, Lgr22, Fælles Mål, LK20, OPS 2014); no `educationalAlignment`. The ego name in a meta is the locale's seeded ego name. No title, meta or landing promises an answer key. `seo.words.free_printable` is metadata only; no visible copy claims "free". `topicMeta.family` ≥ 50 chars x11 + `skill-sentences.<loc>.json.family` via a `register-b4-en-content.js` clone (exists, m).

**Every landing carries the welcome sentence** (panel-authored per locale; en source): "Families come in many shapes and every family is welcome: the practice pages use one invented family so each family word has one right answer, and the family tree template fits any family."

**Non-cannibalisation** (whole-landing 3-gram Jaccard, `scripts/seo-landing/gate.js` FAIL ≥ 0.80 / WARN 0.65; *est.*):

| pair | what the copy says differently | est. |
|---|---|---|
| base vs F2 | who is who, numbers into boxes vs trace and write the word | 0.08 |
| base vs F4 | a drawn tree, numbers vs riddles, a word bank, no tree | 0.10 |
| F3 vs F5 | complete a given tree from clues vs an empty template for your own people | 0.18 (highest; distinguisher kept in title positions 3-4) |
| F1 vs any face | generations, oldest to youngest, no pictures beside words | ≤ 0.06 |
| base vs K-343 "All About My Family" / K-323 | an invented family's words vs draw and count your own; never "my" | 0.07 |
| F5 vs K-343 | a tree template vs draw and count | 0.05 |
| F2 vs K-284 word-tracing | family words from a tree vs theme picture words | 0.04 |
| any face vs G1-352 pronouns family | kin words, never he / she / they | 0.03 |
| any face vs fact-families / syllable-reading / compound-words | no bare family word as a head (rule 12) | ≈ 0 |

Boundary sentence on every landing (after the welcome sentence): "Your child learns the family words and reads an invented family tree; drawing and counting their own family is on the All About My Family page."

## 7 Hub visibility contract

A face appears under `family` on `/[locale]/worksheets` IFF all four hold: (1) `apps.family` exists in `frontend/config/topics-taxonomy.json` with `default_subject:'letters'` (ABSENT today, m; missing = rendered NOWHERE in the rail); (2) `axes['exercise-type'].family` has `slug` + `name` in all 11 locales (§1 table B, collision-free, m); (3) exactly one landing per face per locale with `coordinate.type === 'family'` verbatim, `mode` per §6, `theme:''`, the §1 level key (K: base / F2; G1: F1 / F3 / F5; G2: F4), a unique slug and `canonicalDeckSlug` = the published deck; (4) the landing JSON committed AND deployed (per-process cache).

Gate: `node scripts/verify-hub-type-rows.js --keys=family` (exists, m; expectations from `b5-designs/hub-expectations.json`, exported by `tools/export-hub-expectations.js --batch=b5`, exists, m; poison = drop one landing / drop `apps.family` must FAIL).

**Expected rows per locale:** en 6 · de 6 · es 6 · pt 6 · fr 6 · it 6 · nl 6 · sv 6 · da 6 · no 6 · fi 6 = **66**. No face is refused at design time. Recorded contingencies, each lowering the matrix explicitly if it fires, never padded: (a) a locale whose panel cannot author 8 non-agreeing, non-first-person riddle frames REFUSES F4 there; (b) a look that fails the three-adult read-test is removed from `LOOKS` (costs no row unless a (age, sex) cell drops below 3 looks, which blocks the whole family until redrawn); (c) no locale may ship F5 with a printed kin label (the gate blocks the deck, not the row count, until fixed).
