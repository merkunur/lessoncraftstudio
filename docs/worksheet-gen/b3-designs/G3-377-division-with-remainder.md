# G3-377 `division-with-remainder` (G3) : FINAL design (editor merge, 2026-09-14)

Merged from `_work/G3-377-pedagogy.md` + `_work/G3-377-design.md`. Every file, class, primitive and option below was read in the repo. (m) = re-measured 2026-09-14 (node over `topics-taxonomy.json`, `seo-landing/<loc>.json`, `lib/b2-common.js` + `cache/manifest.json`; puppeteer with the shell's woff2 for the notation widths, scratch `g3377-critic-measure.js`); *est.* = the engineer measures. Resolutions + open items: `_work/G3-377-critic.md`. No em-dashes.

**Boundary (load-bearing).** This type is the LEFTOVER: every item is `n = q*d + r` with `0 < r < d` (F3 admits `r = 0` as the decision); the child writes q AND r in the locale's own notation. G3-309 (`division-equal-groups`, `array-tasks.js` share-bins, m) = exact sharing, `total % bins !== 0` is a verify FAIL (`:259`). G2-217 = exact rings. G3-310 (`repeated-subtraction-division`, `number-line-tasks.js:87-95`, m) = hops PRINTED to zero, `n = hops*k`. G3-311 / G3-312 = fact families / missing factor. G3-370 (`word-problems`) = stories. Never exact by construction, never printed hops, never a story, never the long-division algorithm.

## 1 Identity

| field | value |
|---|---|
| id / key / band | `G3-377` / `division-with-remainder` / G3 every face (`G3-378+ TBD by the emitter`). `default_subject:'math'`, `default_age_range:'8-10'`, `assetClass:'icon-placement'`, `exerciseType:'division-with-remainder'`. `apps[key]` + `axes['exercise-type'][key]` ABSENT (m, 0 hits; `apps` has 94 keys): register via the `tools/register-b2-taxonomy.js NEW_FAMILIES` shape before the wave. |
| theme axis | **ON for F0, F1** (`{applicable:true, minNouns:4, excludeBw:true}`; nouns `entriesFor(theme, loc)` filtered `countable`, `lib/b2-common.js:40-50`, m). Pool (m): 47 of 50 colour themes have >= 6 countable nouns in every locale; `colors` 3, `emotions` 0, `post office` 5 are excluded by `minNouns:4` + `countable`. **OFF for F2..F5** (`{applicable:false}`; landings carry `coordinate.theme:''`): a picture on a numbers row reads as a clue, and 50 themes of one numbers page is churn. |
| fan lever | the DIVISOR SET via `unitAxis` (README cross-type ruling): `2-5` (exemplar, every face), `6-9`, `2-9`. A divisor range is NEVER a face (brief rule 2): the design's "up to 35" face is refused, see section 3. `{U}` resolves to "by 2 to 5" / "durch 2 bis 5"; `deckIdFor` appends `-u2-5`. Until the mechanism lands the exemplar ships. |
| CCSS (en, honest) | **4.NBT.B.6** on every face (`educationalAlignment.targetName`, en only, no `targetUrl`); 3.OA.A.2 in prose for F0 / F1 / F5. The en landing keys `grade-3` (no `grade-4` key exists in `LEVEL_KEYS`, `gen-b2var-landings.js:112-124`, m) and says "a Grade 4 standard, begun in Grade 3". Non-EN names the national framework only (§20.10). |
| data | `data/b3/division.js` (section 5): per locale ONE `notation` block + 6 `strings`; numbers are code, locale-neutral. The sv `÷` ban is a validator rule AND a gate poison (P3). |

**Heads x11 (m: the bare head is FREE in all 11: en title/h1 "remainder" 0, de "mit Rest" 0, sv "med rest" 0; every slug below collides with no `exercise-type` slug in its locale).**

| loc | head (F0 title root) / slug | level key | notation at d2 (`boxStyle`) | strand row (name only) |
|---|---|---|---|---|
| en | Division with Remainders / `division-with-remainders` | `grade-3` (Grade 4 in prose) | `23 ÷ 4 = [ ] R[ ]` (inline) | Number & Operations in Base Ten |
| de | Teilen mit Rest (meta: Division mit Rest) / `teilen-mit-rest` | `2-klasse` (LehrplanPLUS; other Lander Kl. 3: panel may key `3-klasse`) | `23 : 4 = [ ] R [ ]` (inline; "Rest": panel) | Zahlen und Rechnen im Zehnersystem |
| es (MX) | Divisiones con residuo / `divisiones-con-residuo` | `tercer-grado` | casita (dividend left, divisor right, cociente under the divisor, residuo under the dividend) | Sentido numerico |
| pt (BR) | Divisao com resto / `divisao-com-resto` | `3o-ano` (EF03MA08) | chave = casita geometry, "resto" | Numeros |
| fr | Division avec reste / `division-avec-reste` | `ce2` (posee = CM1, excluded) | `23 = (4 × [ ]) + [ ]` (inline; parentheses: panel) | Nombres et calcul |
| it | Divisioni con il resto / `divisioni-con-il-resto` | `classe-seconda` (in riga; colonna = terza) | `23 : 4 = [ ] r. [ ]` (inline; `casita` is the classe-terza re-target, a data switch) | Numeri |
| nl | Delen met rest / `delen-met-rest` | `groep-5` | `23 : 4 = [ ] rest [ ]` | Getallen en het tientallig stelsel |
| sv | Division med rest / `division-med-rest` | `ak-3` | `23/4 = [ ] rest [ ]` (`/` only; `÷` = historic minus, BANNED) | Taluppfattning och tals anvandning |
| da | Division med rest / `division-med-rest` | `3-klasse` | `23 : 4 = [ ] rest [ ]` | Tal og algebra (positionssystemet) |
| no | Divisjon med rest / `divisjon-med-rest` | `4-trinn` | `23 : 4 = [ ], rest [ ]` (comma: panel) | Tall og titallssystemet |
| fi | Jakojaannos (meta: jakolasku jakojaannoksella) / `jakojaannos` | `3-luokka` | `23 : 4 = [ ], jaa [ ]` ("jaa" vs "j.": panel) | Luvut ja laskutoimitukset |

Strand = the `Number and Operations in Base Ten` row of `frontend/lib/seo/strand-names.ts:100-112` (m; all 11 present; an ampersand twin sits at `:86-98`). The deck `gradeBand` is G3 in every locale; the landing LEVEL key is the locale's honest band (de / it sit one key lower, both legal for the hub gate).

## 2 The base page

**Concept.** QUOTATIVE division (sv innehallsdivision, no malingsdivisjon, de Aufteilen): "How many groups of 4 fit in 23, and what is left?" Four wide cards (1x4). Each holds a pile of n themed pictures in ROWS OF TEN (ten is neutral: the row count never restates q) and, right of it, the locale's notation with n and d printed and two dashed coral boxes for q and r. The child rings groups of d with a pencil (a ring may cross a row break, the classroom norm), counts rings and loose pictures, writes q and r. The pile shows n only. The design's pre-boxed pile (q boxes + a leftover zone) is REJECTED as the shipped page: it prints the group count as a picture and turns the act into "count the boxes" (critic 1).

**Chrome budget.** Body **722** with a 3-line title + 3-line instruction (README ruling), 814 one-line; `.ws-page` `padding 0 14` -> inner 675 (`page.css:25`, m); `.ws-card` padding 12 + border 2 -> inner = card - 28 (`page.css:126-137`, m); **every `.ws-card-stage` on this type carries inline `padding:0`** (G1-310 precedent; the default `6px 4px`, `page.css:162`, m) so the stage = the card's inner box.

```
 cardGrid({cols:1, rows:4, numbered:true})  gap 14  card 675 x 170 (m: (722-42)/4) -> inner 647 x 142
 +-(1)---------------------------------------------------------------------+
 |  (36)(36)(36)(36)(36)(36)(36)(36)(36)(36)        23 : 4 =               |  pile 414 x 120 (3 rows of ten)
 |  (36)(36)(36)(36)(36)(36)(36)(36)(36)(36)     [ 44 ]  R  [ 44 ]         |  zone 219 x 88, two lines
 |  (36)(36)(36)                                                           |
 +-------------------------------------------------------------------------+
 (2) (3) (4) identical
```
- **Pile** = `iconRows({theme, noun, n, iconPx:36, perRow:10, rng, gapX:6, gapY:6})` (`templates/components.js:10`, m; +-4 deg warmth, `.ws-icon-row` / `.ws-icon-rows` with inline gaps): 10 x 36 + 9 x 6 = **414** wide; 3 rows = 3 x 36 + 12 = **120** <= 142. 36 = the G23 `minElement` (`_tokens.js:70`, m); the 6 px gaps are the pencil lane. n 7..30 at d2.
- **Notation zone** = `divisionLine({..., lines:2})` (section 2 "NEW") in the remaining **219 = 647 - 414 - 14**, height **88** in every locale (2-line inline = 40 + 8 + 40; casita = 88). Widths (m, puppeteer, border-box boxes 44 x 40): one line = en 203 / de 198 / it 196 / sv 212 / nl 213 / fi 219 / fr 228, so a single line fits only en / de / it; **the zone wraps at `=` on this face**: line 1 `23 : 4 =` **79**, line 2 `[44] rest [44]` **129** (en 113, fi 134, fr `(4 × [ ]) + [ ]` 176), all <= 219. Casita 200 x 88 fits. Numerals Baloo 2 700 26 ink (`data-lcs-num`; 13 px per digit, m), operator Baloo 2 700 24 teal, `=` inkSoft, remainder word Nunito 800 18 ink, boxes 44 x 40 white with **dashed coral** 2.5 (`remBox`; `.ws-answerbox` alone dashes `grid #C8BFAE`, `page.css:225`, m).
- Stack: row height 120 <= 142 (slack 22 per card; 45 at 814). Written numerals at d2 = 8 in 4 items; items 4 (the G3-309 count; G23 `items [8,16]` is not asserted by `qa/lints.js`, m, and picture pages in this family ship 4).
- **d1**: same geometry, `{divisors:[2,3], nMax:15, iconPx:40}`: 2 rows of ten at 40 = 454 x 86, zone 179 (line 2 fr 176 fits). **d3**: `layout:'scatter'` = `iconScatter({theme, noun, n, w:414, h:120, iconPx:36, rng})` (`components.js:43`, m: n = 30 -> 11 x 3 cells of 37.6 x 40 >= 36, jitter 0, +-8 deg), divisors 2..9.

**Ladder** (guards key on `d.layout` / `d.divisors` / `d.nMax`, never the level index):

| | d1 | d2 (ships) | d3 |
|---|---|---|---|
| layout / cards | rows / 4 | rows / 4 | scatter / 4 |
| divisors / nMin..nMax / iconPx | [2,3] / 5..15 / 40 | unit (2-5) / 7..30 / 36 | [2..9] / 11..30 / 36 |
| r | 1..d-1 | 1..d-1 | 1..d-1 |
| per page | 4 distinct (n,d) | + q >= 2 on >= 3 cards, r not constant, >= 3 distinct d | same |

**Answer hiding + verify.** Stage root `data-ws-content data-lcs-item data-lcs-n data-lcs-d data-lcs-q data-lcs-r` (`data-lcs-mode` only when declared); pile `[data-lcs-pile]`; boxes `remBox[data-lcs-answer][data-lcs-role="q|r"]`, empty. `verify(page)` (browser; values from stamps, never from text): per item `q === floor(n/d)`, `r === n - q*d`, `1 <= r < d`, `d` in the unit set, `n <= nMax`; `[data-lcs-pile] img` === n, every `img` >= 36 px, `complete && naturalWidth > 0`; no `[data-lcs-group]` / `[data-lcs-ring]` at `layout:'rows'|'scatter'`; the two `[data-lcs-answer]` are q then r with empty `textContent`; **no `<text>` / text node inside the item equals `String(q)` or `String(r)` unless it also equals n or d**; `data-lcs-num` are n and d only; `(n, d)` distinct across cards; items === `d.cards`; **sv: `document.body.textContent` contains no `÷`** (P3); `[data-lcs-notation]` `scrollWidth <= clientWidth`.

**Reused (exact).** `cardGrid({cards, cols, rows, numbered})` (`templates/layouts/card-grid.js`, `minmax(0,1fr)`, gap 14, m) · `iconRows` / `iconScatter` (`components.js:10 / :43`) · `pillChoice({items:[{key,label}], fontPx})` (`components-b2.js`, `.ws-pill`, `data-lcs-pill`, m; F3 + F4 d3) · `numberLine({min, max, tickStep, labelEvery, width, marks})` (`primitives/number-line.js`, m: `H = 14 + 56 = 70` with marks only, `W = width + 52`; F5) · `.ws-groupbox` / `.ws-groupbox--empty` (`page.css:439-443`; F1) · `entriesFor countable sampleEntries fileUri` (`lib/b2-common.js`) · `svgRoot roundedRect line label esc` (`primitives/_svg.js`) · tokens `T.*`, `F.display`, `F.body`. **NOT used:** `equalGroups` (`components-b2.js:185`, m: its `share` slot row cannot take the leftover home without editing the file; its rows-of-ten strip is `iconRows` without rotation) · `answerBox` (grid dash) · `groupRing` / share-bins (`÷` hard-coded, `array-tasks.js:141/151`) · `countBadge` (a numeral beside a pile leaks q) · any BW theme.

**NEW in `templates/components-b3.js`** (HTML + inline SVG on the tokens; no new primitive):
- `remBox({role, w:44, h:40})` -> `<span class="ws-answerbox" style="width;height;border-color:#F2784B" data-lcs-answer data-lcs-role>`, empty.
- `divisionLine({template, n, d, boxStyle:'inline'|'casita', remWord, lines:1|2, shown?, ghost?})` -> `<div data-lcs-notation data-lcs-style data-lcs-lines>`: tokenises `template`; `{n}` / `{d}` -> printed numerals (`data-lcs-num`); `{q}` / `{r}` -> `remBox` (with `shown:{q, r}` a printed numeral in a solid `grid`-bordered 44 x 30 box, `data-lcs-shown-q/-r`, coral ink; the error face); `÷ : / × · + ,` -> operator span teal 24; `=` -> inkSoft; any other run -> word span Nunito 800 18; `lines:2` breaks after `=`; `ghost:true` renders every non-box token `visibility:hidden` so two boxes sit exactly under a `shown` line (F4). `casita` calls `casitaFrame` and keeps the template for `aria-label` only. sv `/` and fr `= (d × q) + r` are template characters, no glyph code (the G3-369 `DIV_SLASH` / `DIV_COLON` sets, `types/g3/G3-369:22-23`, m, are the precedent; here the glyph lives in DATA).
- `casitaFrame({n, d, remWord, w:200, h:88, shown?, ghost?})` -> SVG: dividend `label` 26 at (40, 22); vertical `line` x 100, y 2..44 + horizontal `line` 100..196 at y 44, teal 3; divisor `label` at (148, 22); q box `roundedRect` 44 x 36 dashed coral at (126, 48) `data-lcs-answer data-lcs-role=q`; r box at (18, 48) `role=r`; `remWord` (it `r.`) Nunito 16 left of the r box, es / pt `''`. `shown` prints q / r in solid boxes (h 80). Quotient under the divisor, remainder under the dividend = the es galera, the pt-BR chave, the it colonna: one drawing, three names.
- `dealBoxes({d, slotW, slotH:44, leftoverW:64, leftoverLabel})` -> one flex row: d `.ws-groupbox` slots (white, tealSoft rim, `data-lcs-slot="k"`) + gap 10 + one `.ws-groupbox--empty` (dashed coral) `data-lcs-leftover` carrying `leftoverLabel` in Nunito 800 14 inkSoft (de keeps the capital). F1 only.

**Locale slot structure.** The apparatus carries numerals, one operator glyph and one remainder word; nothing agrees with a noun (a pile is counted, never named). `data/b3/division.js[loc]` = the `notation` block + 6 `{title, instruction}`; `i18n/strings.<loc>.json` carries the strings per id. Text growth touches only the instruction (<= 150 chars; 3 lines = the 722 case) and the zone's word span (fi ", jaa" measured 134 on line 2, m).

**Alternatives (rejected).** The design's 2x2 pre-boxed pile (cards 354, cells 95, pictures 40): buildable, but the picture prints q + r (critic 1). 2x3 cards at 36 px: the zone drops under the pile, 203 - 120 - 8 = 75 < 88 casita. Rows of d: the row count restates q. Scatter at d2: in a 414 x 120 stage `iconScatter` is a jittered grid anyway (m); kept for d3.

**Risks + print.** 722 floor: base slack 22 per card. Rings crossing a row break (critic OPEN 6): d2 switches to `layout:'scatter'` by config if the operator's render dislikes them. Print: 36 px = 9.5 mm pictures, 6 px = 1.6 mm ring lanes plus the transparent PNG's own air; coral dash ~55 % grey and the teal 3 px bracket survive mono; smallest text 14 (F1 label) vs the 9 px lint (`qa/lints.js:86`, m). Engineer measures: de / fi / fr d2 renders of every face under 3-line chrome; fr line 2 (176) in the d1 zone (179); the F3 pill height.

## 3 Faces 2-6

All five are CODE faces: an additive `mode` knob on `build()` + a `verify()` branch, `data-lcs-mode` stamped only when declared (base byte-identical). Guards key on `d.mode`. `tools/gate-variation-distinct.js` needs the b3 wave file + ROWS (README step 4); expected 5 distinct d2 configs. Every face takes `divisors` from the unit (exemplar 2-5) at d2. Ruling: **the pedagogy's six moves, the design's per-face geometry where the casita forced it.** The design's F5 "up to 35" (= the d3 config plus one exact card) is REJECTED: a range is the fan, one exact card is F3 diluted (brief rules 2 + 4); its "scatter + circle" is F0's act on d3's pile. Quotative / partitive = TWO faces: the child's act differs (ring groups of d vs deal one-at-a-time into d homes), sv / no / de / it / pt name both, and the query faces differ in every locale.

### F1 : Share It Out, What Is Left Over? (`mode:'share'`, themed)
**Move:** PARTITIVE division (sv delningsdivision, no delingsdivisjon, de Verteilen, it ripartizione): deal n into d homes; what cannot be dealt is the remainder. **Layout:** 1x4, inner 647 x 142. Left: strip `iconRows({perRow:10, iconPx:36, gapX:4, gapY:4})` = **396 x 76** (2 rows, n <= 20) + 8 + `dealBoxes({d, slotW:(396 - 64 - 10 - (d-1)*10)/d, slotH:44})` (d = 5 -> 56; d = 2 -> 156) = **128 <= 142**. Right: zone 647 - 396 - 14 = **237**, `divisionLine({lines:2})` 88. The child draws one tally per picture into the homes in turn, draws the loose ones into the leftover home, writes q (each home) and r. Pictures 36 = floor (the pedagogy's 34 is withdrawn). d1 `{divisors:[2,3], nMax:12}`; d3 `{divisors:[3..6], nMin:13, nMax:20}`. **Verify:** `[data-lcs-slot]` === d, `[data-lcs-leftover]` === 1, strip `img` === n, slots and leftover empty (no `img`, no text), plus the common rules. **Query face:** "share / left over" · "Verteilen mit Rest" · "repartir con residuo" · "dela lika med rest" · "partager, reste" · "eerlijk verdelen met rest" · "jaa tasan, mika jaa yli". **Boundary:** G3-309 deals EXACTLY into `.ws-bin`s with no leftover home and `÷`; da's shipped stories say "del ligeligt", F1 copy must not reuse it.

### F2 : Practice Rows (`mode:'practice'`, themeless)
**Move:** FLUENCY in the notation; every row leaves a remainder. **Layout:** `cardGrid({cols:2, rows:5})`: card (722 - 56) / 5 = **133** -> inner 302 x 105; one `divisionLine({lines:1})` per card (inline 40 / casita 88 <= 105; single line <= 228 <= 302, m). **Ten items** (G23 [8, 16]); the pedagogy's 12 at 2x6 (inner 80) cannot hold the casita and is withdrawn. `{mode:'practice', items:10, cols:2, rows:5, divisors:unit, qMax:10}`: n <= 10d + d - 1 (54 at d = 5) keeps every item inside the tables (the design's flat "to 50" would ship `49 ÷ 2 = 24 R1`, short division, above the ceiling); r 1..d-1; (n, d) distinct; each d at most 3 times. d1 `{items:8, rows:4, qMax:5}`; d3 `{items:10, divisors:[2..9], qMax:10}`. **Verify:** common rules; every r >= 1; `q <= qMax`; no `img`. **Query face:** "practice" · "Ubungen" · "oefeningen / deelsommen met rest" · "ejercicios" · "exercices" · "ovningar" · "harjoituksia".

### F3 : Exact or Not? (`mode:'exact'`, themeless)
**Move:** DECISION: does d go into n exactly; half the rows are exact (pt "divisao exata e nao exata", de "geht auf", nl "gaat op"). **Layout:** 2x4: card 170 -> inner 302 x 142; row 1 `divisionLine({lines:1})` with r allowed 0; row 2 `pillChoice({items:[{key:'exact', label:notation.exactWord}, {key:'rest', label:notation.restWord}], fontPx:20})` h *est.* 42 (`.ws-pill` padding 6 x 24 + border 2, `page.css:422`, m). Casita 88 + 8 + 42 = **138 <= 142** (engineer measures the pill); inline 90. **Eight items, 4 exact** (the pedagogy's 10 at 2x5 / inner 105 cannot hold casita + pills; withdrawn). `{mode:'exact', items:8, cols:2, rows:4, exact:4, divisors:unit, qMax:10}`; the child circles one pill and writes r (0 allowed) and q. d1 `{items:6, rows:3, exact:3, qMax:5}`; d3 `{items:8, exact:3, divisors:[2..9]}`. **Verify:** `data-lcs-exact === (r === 0)`; exact items === `d.exact`; exact items not all first or all last; pills glyph-free, fixed order, exactly two `data-lcs-pill` per item, none marked; common rules with `minR 0`. **Refusal:** needs `notation.exactWord` + `restWord`; absent -> F3 refused in that locale, the hub gate expects 5. **Query face:** "exact or not" · "geht auf?" · "gaat op?" · "exata ou nao exata" · "tombe juste?" · "gar jamnt ut?" · "meneeko tasan?".

### F4 : Find the Error, the Remainder Is Too Big (`mode:'error'`, themeless)
**Move:** CHECK `r < d`: a worked division with a wrong remainder; cross it out, write the right pair. **Layout:** 2x4, inner 302 x 142; row 1 `divisionLine({shown:{q:shownQ, r:shownR}})` (inline 40 / casita `shown` 80; wrong numerals in coral, solid grid boxes); row 2 `divisionLine({ghost:true})` = two empty `remBox` exactly under the shown q and r (inline 40 / casita 44). Casita 80 + 8 + 44 = **132 <= 142**; inline 88. Withdrawn: the design's free `.ws-blankbox` rewrite (unverified) and the pedagogy's full rewrite line (casita 88 + 8 + 88 fails 2x4). **Eight items, all wrong at d2** (no verdict pills: with no clean item a verdict is furniture): `rBig` 6 (shown q - 1, r + d: `23 : 4 = 4 R 7`, the misconception the face exists for) + `sum` 2 (shown r < d but `q*d + r !== n` by 1..2). `{mode:'error', items:8, cols:2, rows:4, kinds:{rBig:6, sum:2}, clean:0, divisors:unit, qMax:10}`. d1 `{items:6, rows:3, kinds:{rBig:6}}`; d3 `{items:6, rows:3, kinds:{rBig:4, sum:1}, clean:1}` + `pillChoice` yes / no (`notation.rightWord` / `wrongWord`) under row 2. **Verify:** `(shownQ, shownR) !== (q, r)`; `rBig`: `shownR >= d && shownQ*d + shownR === n`; `sum`: `shownR < d && shownQ*d + shownR !== n`; kinds counted as configured; clean items === `d.clean`; the ghost boxes are the only `[data-lcs-answer]`, empty, and the true pair prints nowhere; error positions vary over 20 seeds. **Query face:** "find the error" · "Fehler finden" · "zoek de fout" · "encuentra el error" · "trouve l'erreur" · "hitta felet" · "etsi virhe".

### F5 : Hop Back on the Number Line (`mode:'line'`, themeless)
**Move:** MEASUREMENT division as motion: from n, hop back in d's until a whole hop no longer fits; the hop count is q, the landing is r. The child DRAWS the hops (G3-310 prints them and lands on zero). **Layout:** `cardGrid({cols:1, rows:3})`: card (722 - 28) / 3 = **231** -> inner 647 x 203; `numberLine({min:0, max:lineEnd, tickStep:1, labelEvery:5, width:560, marks:[n]})` = **612 x 70** (m) + 8 + `divisionLine({lines:1})` 88 (inline centred) = **166 <= 203**. Three lines = the G3-310 count (`rows:3` at every level, m). `lineEnd` = the smallest multiple of 5 >= n + d (<= `lineMax`); **composer rule: `d * 560 / lineEnd >= 36`** (a hand-drawn hop must clear the G23 element floor; at d = 2 this bounds n <= 27), never an invented 24 px. `{mode:'line', lines:3, lineMax:35, divisors:unit, nMin:10, nMax:30}`; d1 `{lineMax:20, divisors:[2,3]}`; d3 `{lineMax:50, divisors:[3..9]}`. **Verify:** `[data-lcs-mark]` === n; `[data-lcs-hop]` === 0 (P7); `lineEnd >= n + d`, labels every 5 at 17 px; hop px >= 36; r >= 1; landings take >= 2 values over the page; common rules. The design's rejection ("540 px in a 302 card = 23 px ticks") assumed 2-column cards; at 1x3 the hop floor is a composer constraint, not a wall. **Query face:** "number line" · "am Zahlenstrahl" · "op de getallenlijn" · "en la recta numerica" · "sur la droite numerique" · "pa tallinjen" · "lukusuoralla". **Boundary:** G3-310 = printed hops, exact, to zero; here hops drawn, `r >= 1`, the landing is the answer.

**Rejected non-moves (confirmed).** A divisor RANGE or dividend ceiling as a face (the `unitAxis` fan) · one exact card among three (F3 diluted) · a pre-boxed / pre-ringed pile (a read scaffold) · scatter + circle (F0 d3) · exact sharing (G3-309) · exact rings (G2-217) · printed hops to zero (G3-310) · word problems with a remainder (the `word-problems` family; `wp-muldiv-frames.js` has no `{rest}` clause; a future G3-370 face) · the long-division ALGORITHM (CM1 / Klasse 4 / Grade 4; it classe 3 = the `casita` data switch) · divisibility rules (4.OA.B.4) · remainder as fraction or decimal (Grade 5) · "make your own" (no verify).

## 4 Native rebuild plan x11

The panel authors 6 titles (head + ONE element, <= 70, no worksheet-word, unique in the band) + 6 instructions (<= 150, one imperative an 8-10-year-old follows), the `notation` block (op glyph, template, remainder word, F3's two pill words, F1's leftover label, F4 d3's right / wrong words, the two division-KIND names where the locale teaches them) and audits the EN as a SOURCE. Numerals are plain Baloo 2 x11 (no `FORM_BY_LOCALE`). EN d2 titles (chars): F0 "Division with Remainders: Ring the Groups" 41 · F1 "Share It Out: What Is Left Over?" 32 · F2 "Division with Remainders: Practice Rows" 39 · F3 "Exact or Not? Divisions With and Without a Remainder" 52 · F4 "Find the Error: The Remainder Is Too Big" 40 · F5 "Hop Back on the Number Line: What Is Left?" 42.

| loc | title roots (F0 · F1 · F2 · F3 · F4 · F5) | notation pins | traps |
|---|---|---|---|
| en | Division with Remainders: Ring the Groups · Share It Out · Practice Rows · Exact or Not · Find the Error · Hop Back on the Number Line | `{n} ÷ {d} = {q} R{r}`, `R` | Grade 4 (4.NBT.B.6) said plainly; "R1" not "r 1" |
| de | Teilen mit Rest: Gruppen einkreisen (Aufteilen) · Verteilen mit Rest · Division mit Rest Ubungen · Geht die Division auf? · Fehler finden · am Zahlenstrahl | `{n} : {d} = {q} R {r}` (or "Rest": one form) | Kl. 2 vs 3 per Land (level key); nouns keep the capital in the leftover label; never `÷` |
| es (MX) | Divisiones con residuo: encierra los grupos · Repartir con residuo · Ejercicios · Exacta o con residuo · Encuentra el error · en la recta numerica | `casita`, `remWord:''`; residuo (never "resto" as the MX head) | the casita is on the critical path (section 5); labels are bare words, no sentence with a noun |
| pt (BR) | Divisao com resto: circule os grupos · Repartir com resto · Exercicios · Divisao exata e nao exata (the BNCC phrase) · Encontre o erro · na reta numerica | `casita` (chave), `remWord:'resto'` or `''` (panel) | "resto" also = change: meta says "resto da divisao" |
| fr | Division avec reste : entoure les groupes (groupement) · Partage avec reste · Exercices · Tombe juste ou pas · Trouve l'erreur · sur la droite numerique | `{n} = ({d} × {q}) + {r}`, reste | never "division posee" (CM1); "fiche" banned |
| it | Divisioni con il resto: cerchia i gruppi (contenenza) · Ripartizione con il resto · Esercizi · Esatta o con il resto · Trova l'errore · sulla linea dei numeri | inline `{n} : {d} = {q} r. {r}` (classe 2); `casita` = classe 3 re-target | one form of r. / resto everywhere; "resto" also = change; "scheda" banned |
| nl | Delen met rest: omcirkel de groepjes · Eerlijk verdelen met rest · Deelsommen met rest (the head query) · Gaat het op? · Zoek de fout · op de getallenlijn | `{n} : {d} = {q} rest {r}` | "staartdeling" (groep 6+) never |
| sv | Division med rest: ringa in (innehallsdivision) · Delningsdivision med rest · Ovningar · Gar det jamnt ut? · Hitta felet · pa tallinjen | `{n}/{d} = {q} rest {r}`; **`÷` in ANY string fails the validator** | never «grupp» as a headline (the counting word); "kort division" is a G4 algorithm; `[NSR-FLAG]` |
| da | Division med rest: sat ring om · Del ud med rest · Ovelser · Gar det op? · Find fejlen · pa tallinjen | `{n} : {d} = {q} rest {r}` | the shipped da stories say "del ligeligt": F1 must not reuse it; `[NSR-FLAG]` |
| no | Divisjon med rest: ring inn (malingsdivisjon) · Delingsdivisjon med rest · Oppgaver · Gar det opp? · Finn feilen · pa tallinjen | `{n} : {d} = {q}, rest {r}` (comma: panel) | bokmal; `4-trinn`; `[NSR-FLAG]` |
| fi | Jakojaannos: ympyroi ryhmat · Jaa tasan, mika jaa yli · Harjoituksia · Meneeko jako tasan? · Etsi virhe · lukusuoralla | `{n} : {d} = {q}, jaa {r}` ("jaa" vs "j.": one form) | nominative numerals only, no case slot; `[NSR-FLAG]` |

Refusal / re-targeting is a DATA decision: `boxStyle`, the template, the pill words, the kind names. Numbers refuse nowhere; F3 refuses only where `exactWord` is not authored. Three-agent native panel per locale (§A.13.48); the EN is handed over as a SOURCE TO AUDIT.

## 5 Data + gates

`data/b3/division.js`, GENERATED from `i18n/.draft-b3-<loc>.json` by `tools/apply-b3-locale.js` after `tools/validate-b3-draft.js` (the `apply-b2-locale.js` pattern, exists, m; the b3 tools are owned by the batch's first design; `data/` gitignored, `git add -f`):
```
DIVISION[loc] = {
  notation: { op: ':' | '÷' | '/' | null,        // null = fr's product form; sv MUST be '/'
              template: '{n} : {d} = {q} R {r}',  // each slot exactly once; casita keeps it for aria-label
              boxStyle: 'inline' | 'casita',
              remWord: 'R', exactWord: 'geht auf', restWord: 'Rest',   // F3 pills
              leftoverLabel: 'Rest', rightWord: 'richtig', wrongWord: 'falsch',   // F1 home, F4 d3 pills
              kinds: { quotative: 'Aufteilen', partitive: 'Verteilen' } | null },
  strings: { 'G3-377': {title, instruction}, F1..F5: {title, instruction} }   // apply- also writes i18n/strings.<loc>.json
}
```
Numbers are code: `pickItem(rng, {divisors, nMin, nMax, qMax, minR})` draws `d`, then `q` in `max(1, ceil((nMin - d + 1)/d))..min(qMax, floor((nMax - 1)/d))`, then `r` in `minR..d-1`, sets `n = q*d + r`, rejects duplicate `(n, d)`; F3 draws `r = 0` for `exact` items and shuffles; F4 derives the shown pair by kind; F5 rejects `d * 560 / lineEnd < 36`; 200 tries then throw (refusal, never padding).

**`tools/validate-b3-draft.js` (division block; exit 1 on any):** (1) `template` contains each of `{n}{d}{q}{r}` exactly once, no digit, glyphs only from `÷ : / × · = + , ( )`; `op` non-null -> the template contains it; (2) `op` in the enum; **(3) sv: `op === '/'` and no string in the whole block contains `÷`**; (4) `remWord` 0..8 chars; `exactWord` + `restWord` both present or F3 flagged refused for the locale; `leftoverLabel` present or F1 flagged; (5) `boxStyle` in the enum; casita locales still carry all four slots; (6) 6 titles <= 70, worksheet-word guard, unique in band, each = head + one element (a title equal to the family head, or to a shipped G3-309 / G3-310 / G3-370 landing title, FAILS); instructions <= 150 with an end mark; (7) `kinds` null or both keys; (8) `rightWord !== wrongWord`.

**`qa/verify-b3-division.js`:** renders face x 11 locales at d2 under 3-line chrome (the 722 case: a 3-line title + 150-char instruction); `verify()` empty; `qa/lints.js` clean (overflow `:37-45`, footer `:50-72`, font `:86`); floors asserted (icons >= 36, numerals >= 22, boxes >= 40 tall, F5 hop >= 36 px, `[data-lcs-notation]` `scrollWidth <= clientWidth`, F3 pill row inside the card); 20-seed sweep: F0 / F1 >= 3 distinct divisors per page, F2 remainders not constant, F3 exact items not all first or last, F4 error positions vary, F5 landings take >= 2 values. Non-vacuity: 0 items checked = FAIL. **Poison cases (each must FAIL; the unpoisoned build is the control; a poison that does not flip its assertion fails the gate itself):** P1 an item stamped `r = d` (`23 : 4 = 4 R 4`, "remainder too big") · P2 the quotient printed as text inside its item ("answer printed") · **P3 the sv page rendered with `op:'÷'`** ("historic minus") · P4 `n !== q*d + r` (q one too big) · P5 F3 `data-lcs-exact="1"` with `r = 2` · P6 F4 shown pair equal to the true pair, and a `rBig` card whose shown r < d · P7 F5 with a printed `[data-lcs-hop]` at d2, and a hop of 24 px · P8 F0 d2 with a `[data-lcs-group]` present ("pre-boxed pile") · P9 two cards with one `(n, d)` · P10 an F1 slot containing an `img` ("dealt by the machine") · P11 a `zoo animals bw` picture path · P12 (validator) a de title "Teilen mit Rest" bare, a sv instruction containing `÷`, an fr template without `×` · P13 F2 `49 ÷ 2` at d2 (`q > qMax`).

**`tools/gate-variation-distinct.js`:** 5 distinct d2 configs (poison: F2 with `mode` removed -> equals the base -> FAIL). The page reads `DIVISION[loc]` only: never `number-words.js`, never a frame bank, never a noun in a sentence.

## 6 SEO plan

| face | title pattern (Germanic / Romance / Nordic+fi) | meta MIDDLE (whole description 120-170; the instruction) | coordinate |
|---|---|---|---|
| F0 | "Division with Remainders: Ring the Groups" · "Teilen mit Rest: Gruppen einkreisen" / "Divisiones con residuo: encierra los grupos" / "Division med rest: ringa in grupperna" · "Jakojaannos: ympyroi ryhmat" | Ring groups of the same size in each pile, then write how many groups you made and how many are left over | `{type:'division-with-remainder', mode:'base', theme:<t>, level:<§1 key>}` |
| F1 | "Share It Out: What Is Left Over?" · "Verteilen mit Rest" / "Repartir con residuo" / "Dela lika med rest" · "Jaa tasan, mika jaa yli" | Deal the pile into the boxes one at a time; write how many each box gets and how many cannot be shared | `mode:'share', theme:<t>` |
| F2 | "Division with Remainders: Practice Rows" · "Division mit Rest Ubungen" / "Ejercicios de division con residuo" / "Division med rest: ovningar" · "Jakojaannos: harjoituksia" | Ten divisions inside the times tables, every one leaves something over; write the answer and the remainder in each row | `mode:'practice', theme:''` |
| F3 | "Exact or Not? Divisions With and Without a Remainder" · "Geht die Division auf?" / "Division exacta o con residuo" / "Gar det jamnt ut?" · "Meneeko jako tasan?" | Decide whether each division comes out exactly; circle your choice and write the remainder, 0 if there is none | `mode:'exact', theme:''` |
| F4 | "Find the Error: The Remainder Is Too Big" · "Teilen mit Rest: Fehler finden" / "Encuentra el error en la division" / "Hitta felet: resten ar for stor" · "Etsi virhe jakolaskusta" | Every division has been worked out wrongly; cross out the wrong numbers and write the right quotient and remainder in the boxes | `mode:'error', theme:''` |
| F5 | "Hop Back on the Number Line: What Is Left?" · "Teilen mit Rest am Zahlenstrahl" / "Division con residuo en la recta numerica" / "Division med rest pa tallinjen" · "Jakojaannos lukusuoralla" | Start at the marked number and draw hops back of the same size until a whole hop no longer fits; write the hops and where you land | `mode:'line', theme:''` |

h1 = title; eyebrow = the level label of §1 (en: "Grade 4 standard, Grade 3 practice"); strand = the NBT row (§1); JSON-LD `LearningResource`, `educationalAlignment.targetName:'4.NBT.B.6'` en only, no `targetUrl`; `topicMeta['division-with-remainder']` (>= 50 chars) + `skill-sentences.<loc>.json` via `tools/register-b3-en-content.js` (absent, m). Meta lead inherits `seo.words.free_printable` (README open item 1). `coordinate.mode` is always the face's string, base `'base'` (README ruling). Boundary sentence on every landing: "Every division on this page leaves something over; nothing comes out exactly" (F3: "half of them do, and deciding which is the task").

**Non-cannibalisation** (3-gram Jaccard, `scripts/seo-landing/gate.js` FAIL >= 0.80; *est.*, the gate measures). The shipped neighbours (m, slugs): G3-309 `division-equal-groups` (share-bins, exact) · G3-310 `repeated-subtraction-division` (printed hops to zero) · G3-311 `multiplication-division-fact-family` · G3-370 `multiplication-division-word-problems` (stories, 2 per page) · the `arrays-multiplication` G3 hub landing; every one keeps its own head, no landing carries "remainder" (m).

| pair | what the copy says differently | est. |
|---|---|---|
| F0 vs G3-309 | rings on a pile, "left over" in every sentence; never "share equally" | 0.20 |
| **F1 vs G3-309** (same strip-and-boxes idiom) | the leftover home ("the box for what cannot be shared") leads; sharing is the verb, the remainder the noun; the pair to measure FIRST | 0.35 |
| F5 vs G3-310 | "until a hop no longer fits", "where you land"; never "to zero", never "how many hops" alone | 0.30 |
| F0 / F1 vs G3-370 | pictures and notation, no story | 0.10 |
| F2 vs the `arrays-multiplication` hub landing | notation rows with a remainder word; no array | 0.10 |
| F3 / F4 vs G3-311 | a decision / an error hunt; no fact family | 0.05 |
| any face vs it / pt money "resto" landings | "resto da divisao" / "resto della divisione" in the meta | 0.05 |

## 7 Hub visibility contract

A face appears under `division-with-remainder` on `/[locale]/worksheets` IFF all four hold (brief): (1) `apps['division-with-remainder'] = {default_subject:'math', default_age_range:'8-10', exercise_type_axis_key:'division-with-remainder'}` exists in `frontend/config/topics-taxonomy.json` (ABSENT, m; missing = rendered NOWHERE in the rail, `worksheets-catalog.ts` buckets by `default_subject`); (2) `axes['exercise-type']['division-with-remainder']` has `slug` + `name` in all 11 locales (§1 slugs; 0 collisions, m); (3) exactly one landing per face per locale with `coordinate.type === 'division-with-remainder'` verbatim, the §1 level key, `coordinate.theme` = the wave theme for F0 / F1 and `''` for F2..F5, `coordinate.mode` per §6, a unique slug and `canonicalDeckSlug` = the published deck; a face REFUSED in a locale (F3 without `exactWord`) has NO landing there and the gate's expectation is lowered explicitly, never padded; (4) the landing JSON committed AND deployed (per-process cache).

Gate: `node scripts/verify-hub-type-rows.js --keys=division-with-remainder`. Script absent (m; siblings `scripts/verify-worksheets-hub-order.js` / `verify-worksheets-hub-render.js` exist); write + poison-test it (a short locale, a wrong `coordinate.type`, a dropped `apps.<key>` must FAIL) before the registration lands.

**Expected rows per locale: 6 in every locale x 11 = 66** (every locale has an exact-word; nothing refused at design time). Level mix: one key per locale for all six rows: en `grade-3` · de `2-klasse` (or `3-klasse`, panel) · es `tercer-grado` · pt `3o-ano` · fr `ce2` · it `classe-seconda` · nl `groep-5` · sv `ak-3` · da `3-klasse` · no `4-trinn` · fi `3-luokka`. A future `unitAxis` fan counts distinct FACES, not landings.
