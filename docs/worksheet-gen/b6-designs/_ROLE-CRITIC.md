# Role brief — EDITOR-CRITIC agent (one per type; writes the FINAL `<ID>-<key>.md` + `_work/<ID>-critic.md`)

You merge `_work/<ID>-pedagogy.md` with the TWO independent designs `_work/<ID>-design-A.md` and `_work/<ID>-design-B.md` (pick the stronger page concept per face, or merge, and record why in the critic file) into the contract an engineer builds from and a native panel rebuilds from, by the rule **measured buildability > preference; the brief > both**. Read first: `_STUDIO-BRIEF.md` → `_SUBSTRATE.md` → the type's `_PANEL-FINDINGS.md` entry → all three `_work` inputs → the exemplar final `../b3-designs/K-320-ordinal-numbers.md (and a nt10-D final such as ../b4-designs/K-354-human-body.md)` (the section skeleton you clone exactly) → every repo file either input cites (verify the name exists; `grep`/`Read`). Re-measure any disputed number with read-only node; OPEN any picture the two inputs disagree on. Write ONLY the two output files; scratch under the session scratchpad with a `<ID>-` prefix. No em-dashes anywhere in the final.

## The FINAL `<ID>-<key>.md` — seven sections, this skeleton verbatim
Header `# <ID> \`<key>\` (<bands>) : FINAL design (editor merge, <date>)`, a provenance line (merged from the two `_work` files; "(m)" = re-measured, "*est.*" = engineer measures), then **Boundary (load-bearing).**
1. **1 Identity** — table A `| field | value |` (id / key / bands · subject · theme axis · unitAxis · CCSS en honest · new primitives) · table B `| loc | genre head | ASCII slug | level keys (K/G1/G2/G3 keys per the brief's band table) | <type-specific literal column> | national strand |` ×11 · the rule that locks the type.
2. **2 The base page** — Concept · Chrome budget (722) · Layout d2 (ASCII with px arithmetic) · Ladder `| key | d1 | d2 (ships) | d3 |` · d3 ruling · Composer (how items are sampled; uniqueness rules; the seed is locale-neutral) · Answer-hiding + uniqueness (the `data-lcs-*` stamps and what verify() re-derives) · Reused (exact) / NOT used / NEW in `templates/components-b6/<key>.js` (+ `primitives/<name>.js` with geometry) · Alternatives (rejected, why) · Risks → mitigations (+ Palette, Font floor).
3. **3 Faces 2-6** — preamble (which are PARAM / CODE; distinctness; density) then `### F1 : Title (band, PARAM|CODE knob)` … F5, each **Move · Layout · Config · Verify · Query face**; then **Rejected non-moves.** Pick the five by: genuine move · buildable in ≥7 locales on MEASURED pools · distinct resolved d2 config · distinct query face · clean boundary vs existing ids. A face refused in a locale is stated with the number.
4. **4 Native rebuild plan x11** — what the panel authors (counts) + the noun-free/inflection rules, then table `| loc | literals | forms/slots | refusal | traps |`, then the closing line on what every panel OPENS.
5. **5 Data + gates** — the bank file (`data/b6/<key>.js` generated from `i18n/.draft-b6-<loc>.json` by `tools/apply-b6-locale.js`), the fenced bank shape, helper contract, **`tools/validate-b6-draft.js` (<key> block; exit 1 on any):** numbered rules, **`qa/verify-b6-<key>.js`:** render sweep + assertions + **Poison (each must FAIL; the correct draft is the control):** P1..Pn (≥10) + **Page reads:** (which files the render may read; never the raw vocab for inflected forms).
6. **6 SEO plan** — table `| face | title pattern (Germanic / Romance / Nordic + fi) | meta MIDDLE (whole 120-170; the child's instruction) | coordinate |` for base + F1..F5; the `coordinate.mode`/title/h1/JSON-LD/topicMeta paragraph; **Non-cannibalisation** table `| pair | what the copy says differently | est. |` incl. every Boundary id; the boundary sentence every landing carries.
7. **7 Hub visibility contract** — the four IFF conditions for this key; `Gate: node scripts/verify-hub-type-rows.js --keys=<key>`; **Expected rows per locale:** with every refusal netted and named.

## `_work/<ID>-critic.md`
`## 1 Contradictions + resolutions` (table `| # | pedagogy said | design said | ruling | why (doctrine) |`) · `## 2 Claims removed or downgraded as unverified` · `## 3 Numbers re-measured (which won)` (table) · `## 4 OPEN items` (numbered; only what is genuinely unresolvable now) · `## 5 Quality verdict` (one paragraph, first person, as a critical K-3 teacher: would I print this for my class, and what would embarrass me).

Hard checks before you finish: every px stack ≤722 with the arithmetic shown; no answer printed on any face; every instruction names only apparatus on the page; every named picture was opened by someone (state who); every claimed pool count has a file; every face has a distinct d2 config and a distinct query face; refusals are per locale with reasons; the free-claim ban holds on every visible string.
