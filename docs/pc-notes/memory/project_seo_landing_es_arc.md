---
name: project-seo-landing-es-arc
description: "SEO/Landing-Page Program — es (Mexican Spanish) locale arc; BOTH K-band numeric operations LIVE + production-verified 2026-06-09: addition 198 + subtraction 187 = 385 landings. First locale under per-deck-gold + demand-first. + carousel data-fix + page.tsx carousel-empty gate + non-empty-body author-time guard."
metadata: 
  node_type: memory
  type: project
  originSessionId: 36c5c918-cb8f-47c5-9c31-d5e1fd2d5745
---

# SEO Landing Program — es (MX) arc: find-addend family LIVE (2026-06-09)

The SEO landing-page program's **es locale opened and shipped its first family to production**. Plan/ledger SoT = `C:\Users\rkgen\.claude\plans\commission-to-cc-fancy-orbit.md` (full blow-by-blow). This is the cross-session recall pointer.

**es = MEXICAN SPANISH, locked once for the whole arc** (operator ruling): "hojas de trabajo" anchors titles (NOT Spain's "fichas"); MX lexis (computadora/carro); grade bands **Educación Preescolar (por años) + primer/segundo grado de primaria**; framework name **Nueva Escuela Mexicana (SEP)** human-facing (CCSS code machine-only in JSON-LD); SEP phrasing **"encontrar el número que falta"** (not Spain's "sumando desconocido"); onset by sílaba + seseo for phonetic coords; gender concordance no-neuter (anchor to fixed noun).

**LIVE now: the FULL es addition straight-addition cluster = 198 landings** at `/es/worksheets/suma-*`, all honest **span Preescolar (5 años)–Primer grado** (typicalAgeRange 5-7):
- **find-addend 50** (`suma-buscar-sumando-*`) — carries **1.OA.D.8**, angle "Encuentra el Número que Falta" (commits `3fd23eb9` span-display → `3f990b8a` es wiring → `11f1036b`).
- **mixed 50** (`suma-mixto-*`) — **K.OA.A.2**, angle "Cuenta y Suma / el Total" (`7132bcc1`).
- **image-image 48** (`suma-imagen-imagen-*`) — **K.OA.A.2**, angle "Cuenta los Dibujos" (both groups pictures) (`e0794cc8`).
- **image-number 50** (`suma-imagen-numero-*`) — **K.OA.A.2**, angle "Dibujos y Números" (picture group + a written numeral) (`faf5dbad`).
**4-way mode distinctness verified ON LIVE** (theme=animals: 4 distinct titles, find-addend 1.OA.D.8 + 3 straight K.OA.A.2; 0 dup titles GLOBAL across all 198). og-images regenerated 198/198 (XMP honest grade, Kínder=0 via `--landing-locale=es`). **Cluster CLOSED + production-verified 2026-06-09.**
**Straight-addition ledger-lock (rides all 3 straight modes):** count two groups/a numeral and add within 10 → **K.OA.A.2** (machine anchor, no targetUrl); mode-true distinct query faces per mode (anti-cannibalization, §22.1 multi-mode); collective-noun themes → discrete-count copy; the per-batch QC fix-pass (eyebrow normalize + meta-trim to 150-168 + same-theme title de-dupe) is a recurring step the MX ensemble's metas need.
**SUBTRACTION CLUSTER LIVE (2026-06-09) = 187** (find-subtrahend 46 [1.OA.D.8] + cross-out 45 + image-number 47 + mixed 49 [all K.OA.A.2]) at `/es/worksheets/resta-*`. Per-mode angles: find-subtrahend "¿Cuánto se Quitó?" · cross-out "Tacha y Cuenta" (tachar/visual take-away) · image-number "Resta con Dibujos y Números" · mixed "Cuántos Quedan". SEP take-away anchor "¿cuántos quedan?"; quitar/quedan/tacha lexicon. Commit range find-subtrahend `7d18cf71` → cross-out `4ff6a929` → image-number `0a3fa98a` → mixed `0bf25667`. **FULL 8-WAY distinctness verified live on a shared theme (animals, 8/8 distinct titles).**
**NEW DURABLE MACHINERY this cluster:** (1) **carousel data-fix** — the MX-ensemble path left `carousel:[]` (en/de populate it); reusable `scripts/seo-landing/populate-es-carousel.js` (same-(type,mode) `[1,2,5,11]` ring, honest-fit op-matched labels, run post-assembly every batch) backfilled all es + is standing doctrine. (2) **page.tsx carousel-empty GATE** — the related `<section>` renders only when `carousel.length>0` (first-of-mode landing omits cleanly, no bare heading; catalog-wide safe). (3) **non-empty-body author-time retry guard** in the fan workflow (re-author once if any para empty / body<200 — proven 0-empty). (4) per-batch **deterministic meta-banding + same-theme title de-dupe** is a standing post-fan step (the ensemble overshoots the 170 char cap + collides aves/forest_creatures titles every batch).
**EN/DE STRAIGHT-ADDITION RE-AUDIT DONE + LIVE (2026-06-09, commit `ab605fef`):** en+de addition landings were all `standard:none`; set **260 straight-addition (image-image/image-number/mixed) → K.OA.A.2** + **1 en find-addend (`addition-find-addend-animals`, the original gold exemplar) → 1.OA.D.8** (de has no find-addend). Within-10 gate passed per-deck across all 261 (0 held out). Data-only en.json/de.json edit; route auto-emits educationalAlignment + locale framework chip (en "Common Core" / de "Lehrplan"); grade chip + og UNCHANGED (no regen); machine code now consistent across en/de/es addition hreflang siblings. **The Landing.standard field + FRAMEWORK_BY_LOCALE mechanism is identical across all 3 locale-models** (en/de older arc + es per-deck-gold differ only in slug shape).
**ES STRAND-CORRECTION DONE + LIVE (2026-06-09) — the live es strand label was a calque, now NEM-Plan-2022-accurate end-to-end.** The es strand "Pensamiento Matemático — Número" was the OLD Planes-2011/CCSS calque; NEM Plan 2022 puts math in the campo formativo **"Saberes y Pensamiento Científico"** (disciplina Matemáticas). Fixed in 2 parts: field `ee7b3cfa` (385 strand fields → chip + JSON-LD teaches) + body prose `f9b5389d` (334 campo-citations across 336 bodies: 316 deterministic regex + 18 MX-ensemble; **35 legitimate generic-skill phrases preserved** — "primeros pasos del pensamiento matemático" etc. are NOT the campo label). Live-verified 12 samples, 0 old campo-citations on prod. og/deck/hreflang untouched.
**STANDING LEDGER RULE (es) — strand is campo-formativo-by-subject:** math → **"Saberes y Pensamiento Científico — Matemáticas"**; literacy/ELA → **"Lenguajes — [disciplina]"** (confirm the Lenguajes disciplina natively at that batch's ledger-lock; do NOT pre-guess). CCSS code = machine/SEO anchor only.
**STANDING DOCTRINE (all locales, CLAUDE.md-worthy):** a framework/strand/campo **ledger-label defect's fix-scope = EVERYWHERE the label was authored** (authoring prompt + ALL rendered fields: chip + JSON-LD + body prose), not just the structured field — because a ledger-lock bakes the label into the authoring prompt, it propagates into every field. Framework-name/strand/campo authenticity must be **native-curriculum-verified AT ledger-lock**, not discovered mid-arc (this calque ran 385 landings + eyeballs before a math-puzzle base-ten sub-strand check surfaced it).
**ES MATH-PUZZLE CLUSTER DONE + LIVE (2026-06-09) = 142** (45 Segundo 2.NBT.B.5 + 97 Primer 1.OA.C.6) at `/es/worksheets/rompecabezas-matematico-mixto-*`. Commits Segundo `d11d6982` → Primer `51e10d1f`. **es total = 527 live** (addition 198 + subtraction 187 + math-puzzle 142). The FIRST es coordinate to land in Segundo grado + exercise the **band-split**.
**Math-puzzle doctrine (durable):** (1) **band-split per-deck by max child-seen quantity** (`check-sum-ceiling` sweep at author time): ≤20 → 1.OA.C.6/Primer grado (single band 6-7); 21-100 → 2.NBT.B.5/Segundo grado (single band 7-8); >100 → remove. SINGLE band per deck (first es non-span coordinate — within-20 honestly starts at Primer, no span down to Preescolar). (2) **title rule** = type-word "Rompecabezas" anchors (NOT "hojas de trabajo" → eyebrow); "Rompecabezas de Sumas y Restas hasta <N>: <Tema>" with **band-fence "hasta 100"/"hasta 20"** distinguishing the bands. (3) **same-display de-dupe ladder A→B→C→D** = "Sumas y Restas / Matemáticas / Cálculo / Operaciones" (escalate by collision count; D needed for the 4-deck "Aves" = birds+birds_2 share display "Aves"); group by **display-name** not themeKey. (4) **title robustness:** derive titles by canonical `coordinate.theme→display-name`, NOT slotTokens[0] (the hand-inserted lead had an equation there → mistitle). (5) **images-DECORATIVE** (math-puzzle has no countable images; body says "no se cuentan"). (6) **carousel** = populate-es-carousel.js extended with math-puzzle prefix "Rompecabezas con <Tema> (hasta 100/20)" band-suffix → cross-band same-theme distinct; re-run across both bands after both land.
**HOLD — next es coordinate is operator's call.** Queue: chart-count K.MD.B.3 (clean single-mode, new strand, en-only sibling) · pattern-train readiness (biggest es type, both siblings, visual/light) · en/de countability-exclusion re-audit · deck.html grade re-label (catalog-wide) · Quiz one-deck proof · math-worksheet Gr3-6 LEVELS · landing→1200×630 og-image image-eligibility.

**Reusable machinery built this arc (durable):**
- **Grade-as-SPAN display:** `Landing.levels?: string[]` (+ `title?`/`metaDescription?`) in `frontend/lib/seo/landing-content.ts`; route `frontend/app/[locale]/worksheets/[slug]/page.tsx` resolves a span → range chip + array `educationalLevel` + range `typicalAgeRange` (backward-compat: absent levels → single-band). es LEVELS bands added (preescolar/primer-grado/segundo-grado) + es FRAMEWORK + es MX UI_STRINGS.
- **`regenerate-og-images.js --landing-locale=<loc>` mode:** sources title + honest grade + meta from the landing entry (not deck.html-title / age_range-level), filtered to landed slugs — fixes the catalog-wide SERP-thumbnail "Kínder"-vs-honest-grade mismatch (overrides title + levelName + **description** in both buildXmpPacket and og-image.derive). HARD pre-deploy gate per locale; verify XMP shows honest grade, Kínder=0.

**Standing doctrine confirmed (catalog-wide):** validity judged on **rendered content, never the theme name** — the 8 "countability-invalid" addition themes (colores/emociones/clima/seasons/actividades/partes-del-cuerpo) were a NAME-based error; they render countable (decorative image + numeric find-addend) → all IN. Images are **first-class content**. Per-deck gold via the **MX-native ensemble** (Workflow pipeline, chunked concurrency to avoid burst rate-limits).

**Queued (own gated steps, NOT done):** en/de addition-exclusion re-audit (lift name-based exclusions, content-countable → liftable; genuine-non-countable ≈ empty); **Quiz/Practice-Problem rich-result** one-deck proof (`addition-find-addend-animals`); **math-worksheet Gr3-6 (+ crossword grade-3) LEVELS extension** before those types open; **landing → 1200×630 og-image as JSON-LD `ImageObject`** image-eligibility upgrade (landing currently uses 480×524 thumbnail for OG/JSON-LD). RRT + Search Console URL Inspection on live es URLs are operator-side.

**Next es cluster** (operator go per batch): other addition modes (image-image/image-number/mixed) → other types, ledger-lock-first, reviewable batches, eyeball + og-image regen + live-verify per batch. Locale order: en(done) → es(in progress) → pt → it → de(older coordinate-model arc, 1779) → fr → nl → sv → da → no → fi.
