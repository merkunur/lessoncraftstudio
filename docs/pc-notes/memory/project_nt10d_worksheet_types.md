---
name: project-nt10d-worksheet-types
description: "nt10-D BUILD (started 2026-09-21) — the last batch: 10 printable worksheet types × (1 base + 5 variations) × 11 locales = 656 decks + landings; the per-phase RESUME POINTER, the build rulings, the traps"
metadata:
  node_type: memory
  type: project
  originSessionId: 6e3efc5f-a5a4-461a-ab61-ea0e550bb5f1
  modified: 2026-09-21T13:38:15.303Z
---

# nt10-D BUILD — 10 types × 6 faces × 11 locales (IN PROGRESS)

**Operator /goal:** "build the last batch of 10 worksheet types and their expansion (5 pedagogically meaningful variations of each worksheet type)". Plan (approved) = `C:\Users\rkgen\.claude\plans\resilient-spinning-goblet.md` (Phases 0-8). Contract = the 10 design files in `docs/worksheet-gen/b4-designs/` + README (recipe, open items, cross-type rulings) + `hub-expectations.json` (656). Design record: [[project-nt10d-design]]. Nothing is re-designed.

## RESUME POINTER (update at every session end)
- **Phase 0 — DONE** (`07777114` registrars: taxonomy 115 → 125 axis keys, EN skills/topicMeta, Science strand row ×11; `cc931652` the b4 spine: gen-b4var-specs · alloc-b4var-ids (50 ids: K 10 · G1 22 · G2 11 · G3 7) · lib/b4-common · components-b4 barrel · b4-probe-child · validate/apply-b4 · gen-b4-waves/probe-jobs/faces-table · publish-readiness b4 · gate-variation-distinct b4 · lint-locale b3+b4 refusals · verify-hub-type-rows `--expect=` + a warn-mode deploy.sh call · b4-publish-locale.sh). Release baseline captured BEFORE any edit (`out/b3-baseline.json`); `--quick --check` 0 drift after. `_BUILD-BRIEF.md` (b4) written. Not yet cloned (Phase 6): `gen-b4-landings.js` (needs the 60 face ids + each final's §6 STANDARD map); `validate-b4-draft.test.js` (adapt at the first b4 bank).
- **Phase 1 (EN base specs) — DONE 2026-09-21 (10 of 10):** K-355 five-senses `b307c42b` · G1-352 pronouns `79e15474` (name rule 2 is a FLOOR; chrome 710) · K-353 tangram `1588b0b9` · K-354 human-body `f012979e` · K-356 weather-symbols `d43ae6e1` · K-357 recycling `48f19424` (two sparse rounds) · G1-350 cloze `0a2f91af` · G1-351 odd-and-even `2fba3072` (sparse round) · G1-353 question-words `4648b690` + `d9c2df18` (title = the d2 chips) · G2-346 rounding `36bfe081` (+ `i18n/strings.en.json` with all ten). Release baseline RE-CAPTURED after the last commit: 28,314 coordinates = phase-0 27,984 + 330 new (the 10 bases × 3 × 11), 0 changed (`out/b3-baseline.json`; the phase-0 copy at `out/b3-baseline.phase0.json`). Open items carried to the panels: `apps.rounding` age 7-9 vs the design's 8-10 (kept 7-9 — the base key is G2); fr/pt rounding F1 title candidates fail rules 6/7.
- **Phase 2 (EN variation faces, 50) — DONE 2026-09-21 (10 of 10 families, 50/50 face specs):** brief `docs/worksheet-gen/b4-designs/_FACE-BRIEF.md`. Commits per family: K-355 five-senses `967a4e8c` · K-354 human-body `67e475be` (review: the design-excluded `neck` picture was in the F5 pool; K-361 colour face sparse at 540 → body-figure MAX_H 680) · K-356 weather-symbols `9e50eefb` (review: G1-362 write rows sparse → cream lanes + boxes sized to the LOCALE's longest bank word) · K-353 tangram `3538d495` (`tree` ≡ `arrow` turned 270°) · K-357 recycling `c0325367` (K-367 colour REFUSED in en by name → en wave must not list it; hub en 5) · G1-350 cloze `93d880f9` (da F3 refused by name) · G1-352 pronouns `9d6dc6f0` (es + fi F3 refused by name) · G1-351 odd-and-even `f452904c` (F3 retitled "Under 100" — panel item) · G2-346 rounding `307fc74c` · G1-353 question-words `363f4b9a` · close `8e6f2985` (strings.en.json 658 types; distinct 150 pairs). `verify-ruling-starters.js` MUST_HAVE gained G1-361 + G2-357. Release baseline RE-CAPTURED after the close: 30,030 coordinates = 28,314 + 1,716 (exactly the 50 face ids), 0 changed (`out/b3-baseline.json`; earlier captures at `out/b3-baseline.phase0.json` / `.phase1.json`). Every family's `_work/<ID>-faces.md` lists its panel items (the design's per-locale refusals stand: recycling en F4, cloze da F3, pronouns es+fi F3 → 656).
- **Phase 3 (EN waves + generation) — DONE 2026-09-21 (`0eb82da4`):** `waves/wave-b4-en.json` (10) + `wave-b4var-en.json` (49; K-367 in `_refused`; G1-369 zoo animals / G1-370 vehicles pinned); 59 decks generated 0 failed → pooled `out/upload/wave-b4-en-all/`; desc band all in 121-170; title similarity 0 FAIL; `_records/instruction-window.en.txt` (K-359 title too long for a window). `publish-readiness --batch=b4 --locales=en` = "no landing draft" until Phase 6. `scan-duplicate-problems.js` is b2var-story-card-specific — not applicable.
- **Phase 4 (native panels ×10) — IN PROGRESS (`ec2fcc1d` prep):** brief `docs/worksheet-gen/b4-panel-brief.md`; validator `tools/validate-b4-draft.js` (+ `.test.js` 24 poisons); apply `tools/apply-b4-locale.js <loc>`; lint `i18n/lint-locale.js <loc>`. ⚠ OPERATOR: ONE agent at a time while the session limit is close. Order: de → es → fr → pt → it → nl → sv → da → no → fi. Per locale: launch the panel (general-purpose) → it writes `i18n/.draft-b4-<loc>.json` + validates 0 errors → I run `apply-b4-locale.js <loc>` + `lint-locale.js <loc>` → commit (`git add -f data/b4/locales/*.<loc>.json` + the bank files + taxonomy/messages/skill-sentences the apply touches) → `b3-baseline --check --quick` will show the b4 ids under that locale as DRIFT (EN fallback → authored; nothing of theirs is published) — confirm NOTHING outside the b4 ids drifted, then `--capture` → next locale. ⚠ CHAIN FIX `5ff334e6`: five family gates asserted the taxonomy slug apply writes AFTER validation → validator passes `B4_DRAFT_SLUGS`, gates accept the draft slug (poisoned both ways). ⭐ TWO BASE DEFECTS THE PANELS CAUGHT BY RENDERING (both fixed; expect more): the pronouns base handed the 3-chip width to the 4-chip locales (fr/es/pt) so EVERY card overflowed → design §2 four-chip numbers 66/20 (`34d2f854`); K-357 F1's chip column stranded at the right edge at N≤3 → `chipsAlign` centre (`e0622cba`; the row is HEIGHT-bound — growing chips threw 1128 > 677). ⚠ ALWAYS have the panel render a sample in its locale. DONE — **ALL 11 LOCALES**: **de `5ff334e6`** · **es `34d2f854`** (59/60, G2-354 refused — `su` invariant) · **it `c359dcec`** (60/60; K-367 ships on UNI/TS 11686) · **fr/pt/nl `87a15e02` + `404cabe6`** (fr 60 · pt 60 · nl 59) · **sv / no / da / fi 2026-09-22** (sv 59 — K-367 refused on the ITEM bank, not the colours: an honest Metallförpackningar bin holds ONE item family · no 60, 0 refusals, the LOOP/Avfall Norge marking is genuinely national and glass+metal is ONE bin · da 58 — K-367 refused because Denmark standardises the PICTOGRAM not a colour per fraction, + G2-349 cloze plural as designed · fi 58 — K-367 refused although the design said "confirmed": KIVO/Rinki marks differ by waste company and the merged Paperi+kartonki bin has no one honest colour, + G2-354 `hänen` owner-invariant).
  ⭐⭐ **EVERY PANEL PAID FOR ITSELF BY RENDERING.** Six defects no node gate could see, all fixed: (1) the pronouns base gave the 3-chip width to the 4-chip locales → every card overflowed in fr/es/pt (`34d2f854`); (2) K-357 F1's chip column stranded at the right edge at N≤3 — the row is HEIGHT-bound so the fix is `chipsAlign` centre, not bigger chips (`e0622cba`); (3) `data/b2/articles.js` defines `itKey` but never exports it → the it cloze gate THREW and rule 5 was dead (`c359dcec`); (4) `diaryDays` is indexed (first+i)%7 against the Sunday-first calendar → a Monday-first list silently rotates the week, now gated + poisoned; (5) the pronouns thing-gender BALANCE was unreachable in nl → scoped to thing-agreeing locales, which RECOVERED G2-354 for Dutch; (6) `K-354` folded the render with `\s` (which eats U+00A0) and compared it to the RAW literal → correct French typography could never pass, and the workaround orphaned a `?` onto its own line. Also: the question-words look-alike fence was DECORATION (twinOf is consulted for PLACES only and `usedGroups` was declared after the thing loop) — measured 200 pages, fence off 3 with two look-alikes, on 0.
  ⚠ RECIPE per locale: validate → apply → `lint-locale` → re-validate any draft authored while the tree moved → `gen-b4-waves --locales=<loc>` → generate both waves → desc band + title similarity → pool to `out/upload/wave-b4-<loc>-all/` → probe jobs + `render/batch.js` → READ pages → commit (`git add -f data/b4/locales/*.<loc>.json`). ⚠ A stale apply is invisible: re-run apply after ANY draft edit (nl shipped a refusal that was already lifted).
- **Phase 5 (per-locale waves + generation + critic) — DONE, ALL 11 LOCALES (2026-09-22).** Shipped counts: en 59 · de 60 · es 59 · pt 60 · fr 60 · it 60 · nl 59 · sv 59 · da 58 · no 60 · fi 58 = **652 decks**, every gap a recorded refusal. Every locale: 0 generation failures, descriptions all in band, title similarity 0 FAIL, pooled at `out/upload/wave-b4-<loc>-all/`, full sweep rendered to `out/b4-sweep/<loc>/`. `publish-readiness --batch=b4` correctly reports "no landing draft" for all 11 — that is Phase 6.
  ⭐ **The hub matrix moved and the README was corrected + re-exported**: recycling 65 → 61 (F4 ships ONLY where a genuine national per-fraction colour convention exists — de/es/pt/fr/it/no; refused en/nl/sv/da/fi), design total 656 → **652**, measured off the shipped waves with 0 cells differing. The README's own recycling row had PREDICTED this ("it/nl/sv/da/no F4 contingent on national colour tokens", floor 651) — four contingencies resolved as refusals and fi overturned its "confirmed".
  ⭐ **Four gate defects found and fixed while applying the Nordic block, each poisoned both ways:**
  1. **`HEADS.sv` in the question-words gate was MUTUALLY UNSATISFIABLE** — it listed four question words (from the design §1 line that predates the three-chip ruling) while d2 ships three, so the four-word head fired "lists … when" and any three-word head fired "!= the measured head". sv was the ONLY row of eleven listing four. ⚠ My first poison mutated `types[id].title` and scored SILENT: rule 9 reads the BANK's `strings` block, not `types`.
  2. **A `types` ↔ bank-`strings` title DRIFT was invisible to the validator** (the deck title vs the page title). The sv panel had verified byte-identity with a script of its own precisely because no gate did. Added per-FAMILY multiset equality to `validate-b4-draft.js`; measured 100/100 (locale × family) pairs already clean, so it convicts nothing.
  3. **The odd-and-even absent-apparatus ban is WHOLE-WORD, so the Nordic DEFINITE plural walks past it** (`prikkerne` ≠ `prikker`, `prickarna` ≠ `prickar`, `prikkene` ≠ `prikker`). Measured 60 authored instructions × 96 apparatus words: **0 live evasions**, so it is a latent narrowness — closed by listing the definite/inflected forms EXPLICITLY (sv/da/no + de dative plurals), never a stem: `par\p{L}*` would swallow sv `park` / da `parat` and `pari\p{L}*` would swallow fi `Pariisi`. 16/16 must-fire caught, 7/7 must-pass survived. ⚠ One of my own must-pass examples was MIS-SORTED (it really was the defect) — sort each example by what it MEANS.
  4. **K-357 d3 cannot build in sv** — 10 items, two per bin, but the honest Metallförpackningar bin has ONE item family. Measured 11 locales × d1-d3 × 5 seeds: sv/d3 is the ONLY cell, and the waves ship d2, so no deck is lost. Declared as an auditable `LEVEL_REFUSALS` list asserted BOTH ways (a declared cell must throw; an undeclared cell must build).
  ⭐⭐ **THE LOOK-ALIKE FENCE REACHED ONLY QUESTION-WORDS — and the theme-fanned odd-and-even faces were leaking.** Measured over the two themes the waves actually pin (`zoo animals`, `vehicles`) × 11 locales × 25 seeds: **37 of 550 pages drew two members of one group** (jaguar+leopard, bus+van, boat+sailboat). Fenced in `G1-351._picturePool` (0 lines to any shared component); re-measured **0 of 550**. Pool shrinkage measured FIRST: at most 2 nouns per (theme × locale), smallest fenced pool 19 against the 6 lanes a face needs, so it can never force a refusal. All 22 themed decks regenerated.
  ⚠ **TWO POISONS HAD EXPIRED BECAUSE THEY NAMED A REAL LOCALE.** cloze PR15 and five-senses PR15 both used **`sv`** as their example of an *unauthored* locale — the moment Swedish shipped, both became pages that BUILD and the gates reported 90/91 and a silent pass. Re-pointed at `zz`, which can never be authored (`human-body` already used `xx` — that is the right shape). **A needle that encodes the current state has a half-life; a poison that names a real locale expires when that locale ships.**
  ⚠ **A message edit silently blinds a poison keyed on its text.** Widening the recycling slack guard from one-line-only to every chrome changed the string, and `PR-SHORT one-line slack` stopped matching — the gate reported 30/31 instead of failing. Both slack guards + both needles are now consistent.
  ⚠ **I nearly filed two defects against correct data.** `toys/girl` as a portrait on the sv possessive page is a DELIBERATE, picture-opened bank entry ("a PERSON, never an object caption"); and the recycling base's wide blank band is the reviewer-ruled 265 px line-drawing zone, measured at the pinned body (267 dot→pill, 265 by the gate's own strip→pill anchors) — the layout matches the rule. **Read what the data says about a picture before inferring from its directory name.**
  (historical, de) waves committed; 60 decks pooled at `out/upload/wave-b4-de-all`, 60 review PNGs at `out/b4-probe-de/` all clean, 3 read by me). Per locale after its panel: `gen-b4-waves.js --locales=<loc>` → `cli.js generate --wave waves/wave-b4-<loc>.json` + `wave-b4var-<loc>.json` → `scan-staged-desc-band.js` + `gate-deck-title-similarity.js` → pool to `out/upload/wave-b4-<loc>-all/` → `gen-b4-probe-jobs.js waves/wave-b4-<loc>.json --out=out/b4-probe-<loc>-base-jobs.json` (+ var) → `render/batch.js <jobs> --out out/b4-probe-<loc>` → read de/pt/fi myself, critic agents for the rest (agent cap!). ⚠ `scan-duplicate-problems.js` is b2var-only.
- **Phase 6 (landings ×11) — STARTED 2026-09-22.** Substrate READY for every locale: 11 face tables at `out/b4-faces.<loc>.json` with **0 missing PNGs**, and all **652 renders** swept to `out/b4-sweep/<loc>/` (the table looks for `<ID>-<theme|null>-d2-<loc>.png` there — render with `render/batch.js <jobs> --out out/b4-sweep/<loc>`). Brief = `docs/worksheet-gen/b4-landing-brief.md`, which briefs each panel as an **AUDIT OF THE RENDER** and carries the refusal map, the strand rules and the no-answer-key rule.
  ⭐ **The brief claimed the composer refuses an answer-key promise and it did NOT** — `gen-b4-landings.js` enforced only the free-claim ban. Added `ANSWER_KEY_CLAIM` over every authored field, poisoned both ways: 20/20 must-fire across all 11 locales, 14/14 must-pass (the bare word *answer* / *svar* / *Lösung* / *respuesta* is CORRECT prose on these pages — only the answer-KEY collocations are banned). **Write the gate before the brief that promises it.**
  ✓ Verified before launching panels: the 60-id `STANDARD` map is per-FACE correct against the design (odd-and-even base/F2/F5 readiness, F1/F3 `2.OA.C.3`, F4 `3.OA.D.9`; the four science families readiness) — I nearly filed a defect against it by assuming the family carried one code. The `Science` row in `strand-names.ts` is complete ×11.
  IN FLIGHT: sv · no · da · fi landing panels (≤4 cap). REMAINING: en · de · es · pt · fr · it · nl.
- **Phases 6-8: NEXT.** Phase 6 = landings ×11 (`scripts/seo-landing/gen-b4-landings.js` exists with the 60-id STANDARD map; still to write: `docs/worksheet-gen/b4-landing-brief.md`, cloned from b3's and briefed as an AUDIT OF THE RENDER — that is what found ~40 shipped-string defects in nt20-C). The panels' input is `out/b4-faces.<loc>.json` + the sweep PNGs at `out/b4-sweep/<loc>/` (⚠ the faces table looks for `<ID>-<theme|null>-d2-<loc>.png` under `out/b4-sweep/<loc>` — render with `render/batch.js <jobs> --out out/b4-sweep/<loc>`; the 4 Nordic sweeps are already there, the other 7 still need theirs). Then `gate.js` 0 FAIL → `verify-hub-type-rows.js --expect=…/b4-designs/hub-expectations.json`. Phase 7 = publish ×11 on Hetzner. Phase 8 = hub gate HARD (drop `--warn-missing-keys`), CLAUDE.md §26 → BUILT + LIVE, §14.10 115 → 125.

## Rulings made at the build (binding; also in the README)
- **K-356's family key is `weather-symbols`** (the THEME axis owns `weather`; `topicMeta`/`topicProse` are keyed per axis-key across axes; the K-319 feelings/emotions precedent). `register-b4-taxonomy.js` guards theme-key collisions. Read `weather-symbols` wherever the design file says `weather`; `withFixedTheme(type,'weather')` stays.
- The Science strand row exists now (`strand-names.ts`); odd-and-even / rounding per-locale strand literals come from their finals' table B via the landing generator (their OA/NBT rows misrender).
- `tools/b3-baseline.js` hashes EVERY spec + wave on disk → no b4 clone; re-capture at Phase 2 close (the 60 new ids = the only accepted drift).
- `verify-hub-type-rows.js` reads only the b3 JSON by default; the b4 gate is the second deploy.sh call with `--expect=…/b4-designs/hub-expectations.json --warn-missing-keys` (drop the flag at Phase 8).

## Rulings made while reviewing renders (apply to every remaining family and every face)
- **SPARSE is a defect no gate measures.** K-357's base put a 72 px item strip at the top, bins at the bottom and ~430 px of blank paper between (the design's 425 px "line zone" was a MAXIMUM, read as a target). Rule: size an inter-element zone to what the task needs (measured), let the page's slack fall OUTSIDE the stage (top-anchor the stage under the instruction), and give every base gate a "sparse" assertion (max blank band between consecutive rows / a stage-height floor). K-354 had the same shape more mildly (stage centred with ~100 px above it under the en chrome).

- **A title that lists the chips lists EXACTLY the shipped d2 chips** (G1-353 said "Who, What, Where" while d2 asked When). Fix at the CONFIG (make the page match the measured head), never by renaming the head the panels chose; and when a page's item count exceeds its on-chip frame pool, order the dealer by scarcity (specific kinds first).

## Traps bought this build
- A Python `-c`/heredoc with a Windows path in a docstring dies on `\U` (unicodeescape) — write files with the Write tool or sed.
- The spine clone must ASSERT every literal it substitutes (a tolerant clone silently ships a b3 path); `_clone-b4-spine.js` refuses to overwrite.
- Agents die at the session/usage limit AFTER writing (check disk first) or at their FIRST step on a model limit (relaunch on the current model).

---

## BUILT + LIVE — 2026-09-23 (Phases 6-8 closed)

**652 decks + 652 landings live in 11 locales.** en 59 · de 60 · es 59 · pt 60 · fr 60 · it 60 · nl 59 · sv 59 · da 58 · no 60 · fi 58; every gap a recorded refusal (recycling F4 en/nl/sv/da/fi · cloze F3 da · pronouns F3 es+fi). Publish audit N/N clean per locale; repoint missing=0 in all eleven; IndexNow 652/652 HTTP 200; taxonomy 125 keys; `verify-hub-type-rows.js --expect=…b4-designs/hub-expectations.json` HARD in `deploy.sh` (143 cells / 839 rows PASS); hub render + order verifiers PASS ×11; `/<loc>/worksheets?type=<key>` returns 6 faces live in de, fi, pt.

### The four traps, each bought by a real defect

1. ⭐⭐ **A b4 string lives in TWO places and the family gate reads the one the page does not print.** `data/b4/locales/<fam>.<loc>.json → strings.<mode>` is what `qa/verify-b4-<fam>.js` asserts on; `i18n/strings.<loc>.json → <ID>` is what the generator PRINTS and stamps into the manifest. `apply-b4-locale.js` writes both from one draft, so they agree until a hand-edit touches a bank — then the gate goes green on a string no child sees. Five corrected strings (fr + it possessive instruction, nl title + instruction, fr rounding title) did exactly that; found only because a similarity FAIL refused to clear after I rewrote the title it named. Gate: `tools/check-b4-string-parity.js`, non-vacuity-guarded, 0 across 1,186 strings in 100 banks.
2. ⭐⭐ **An answer-position tell must be measured in BOTH directions and on the SHIPPED instance.** G1-369: perRow 6 = 100% same-parity; my "fix" to perRow 5 over [5,10] INVERTED it to 83.3% opposite and I wrote "no rule left to learn" into the row file; the Danish landing panel named the inversion from the render. Range [3,8] straddles the one-row/two-row boundary and measures exactly **50.0%** over 792 lanes. G2-348: `oddIdx [3,2,1,0]`, a perfect staircase, **identical in all eleven locales** because `instanceSeed` carries no locale — a sweep of random seeds read 0.0% descending and looked like a refutation. Builder + verify now refuse three consecutive rows walking the same way (poisoned both ways). G1-370 is a recorded 100% exception: every narrower width refused 132/132 builds under its two-row cap.
3. ⭐ **`git status` is part of a deploy.** 20 taxonomy slugs, 20 names and 40 topicMeta lines for sv/da/no/fi sat modified and uncommitted while I read the local file and called it registered. `verify-hub-type-rows.js` failed the deploy with 43 findings — the gate doing its job.
4. ⚠ **A stale server taxonomy silently publishes ENGLISH slugs, and unpublish does not undo it.** sv/da/no/fi published before that commit reached the server, so publish-cli fell through to `slug.en` for all ten families and 235 decks shipped as `cloze-g1350` etc. (repoint reported missing=52-54 per locale). Unpublish → republish is BLOCKED: the archived rows keep `@@unique([language,titleHash])`/`descriptionHash`, so all 235 re-INSERTs errored `TITLE_NON_UNIQUE`/`DESC_NON_UNIQUE`, and UPDATE-via-manifest is invalid for archived rows (§15.10). Recovery: delete the archived rows under a strict predicate (locale ∈ the four, status archived, slug on my own published list, created < 6 h ago, matched count must equal the list or REFUSE), then republish; 235/235 clean, native slugs, repoint missing=0. **Check the server taxonomy carries the locale before publishing that locale.**

### Landing panels
All 11 briefed as an AUDIT of the render, and re-run after the pages moved. They convicted the generator of a water-cycle stage labelled with the word for RUNOFF in four languages, an owner-focused possessive instruction in two languages whose possessive agrees with the object, a Dutch numeral printed without its accents, a milk bottle sv/no children do not read as glass, and the G2-348 staircase in three languages independently. **They also refuted me five times** (the it panel: this face never prints the euphonic `ed`; the no panel: the glass bottle is in row ONE in Norwegian and two of the four columns did not move; the es panel: the 72-char title I cited is on the deck, not the landing; pt + de: the file body-length band is wider than my brief said; de: the composer runs no similarity gate). Two panels declined to write a claim they could not see from one sheet. Brief them as auditors.

### Open follow-ups (not blocking)
- es `numeros-pares-e-impares-animales-del-zoologico-g1369` deck `<title>` measures 72 chars against the audit's 70 (WARN class, 1 of 652).
- `oil`/`bolt` art: the es panel notes `tools/bolt@3x.webp` already has a nut threaded on it, beside a bare `nut` in the same row; the en panel notes the hex nut is drawn multi-coloured in a row whose shared property is *metal*. Both handled in prose, neither a render mismatch.
- G1-370 parity tell (100%, recorded) needs a design ruling — raise the two-row cap or vary perRow per lane.
- nl `G1-371` offers a bank word (`Het`) that fits none of its eight sentences.
