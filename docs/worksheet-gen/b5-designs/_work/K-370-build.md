# K-370 `family` — base build record (2026-09-23)

Contract: `../K-370-family.md` §2 (base) + §5 (data + gates). Faces (§3) NOT emitted. Nothing shared edited except `i18n/strings.en.json` (regenerated by `node i18n/build-en.js`, as the brief requires).

## Files (all new, type-scoped; never git-added)
- `scripts/worksheet-gen/primitives/family-figure.js` — the bust (viewBox 100x120, MIN_PX 72, 3 px outline at every size). LOOKS: adult f 6 / adult m 6 / elder f 6 / elder m 7 (+`bald-crown`) / child f 6 / child m 6 / baby 1. Sex = hair reach (f >= jaw+8, m <= ear top; beard/moustache a separate shape). Elder = `grid` hair + cheek arcs, glasses on 3 looks per sex. No skin tone, no props, no skirts/aprons/earrings; every tint in both sexes' looks.
- `scripts/worksheet-gen/primitives/family-tree.js` — the ONE place tree geometry is derived (placement steps 1-5 of §2; couple bar / descent / sibling bar / drop; crown + trunk + ground skin; badge discs). Exports `parentsOf`, `genOf`, `isAncestor` (the graph).
- `scripts/worksheet-gen/templates/components-b5/family.js` — `pictureFrame`, `familyStage`, `kinChip`, `kinWordBlock` (reuses components-b3 `blankNumeralBox`, components-b4 `namePlate`).
- `scripts/worksheet-gen/types/k/K-370-family.js` — the base spec (d1/d2/d3 ladder, config guards, `assertConventional`, `uniqueReferents`, in-browser verify incl. parents re-derived from the drawn lines).
- `scripts/worksheet-gen/data/b5/family.js` — `FAMILY.en` (kin 22 paths, 11 words, 12 sex-tagged names + genitives, clue frames 22, riddle frames 8, distractors, generationOK, strings for the 6 modes) + `FAMILY_NEUTRAL`. **gitignored — force-add.**
- `scripts/worksheet-gen/qa/verify-b5-family-figure.js`, `qa/verify-b5-family-tree.js`, `qa/verify-b5-family.js` (exports `validateBank`, `checkClueTree`, `checkGenRow`, `f2GlyphH`, `bankRule7`).

## Gate output
- `verify-b5-family-figure.js`: **341 assertions, 0 failures, PASS**; 4 poisons all FAIL for their reason (f hair to y 62, m ponytail, elder ink hair, off-palette). Rendered hair bottoms agree with the node parse within 1 unit (76 figures).
- `verify-b5-family-tree.js`: **54 assertions, 0 failures, PASS** over 42 trees (d1/d2/d3 x both grand sides x every child order + F2/F3 shapes); poisons FAIL: PR3 child re-hung from the grand bar (`the drawn lines give ego the parents [MF+MM], the graph says [F+M]`), drop deleted, crown lowered, in-law under the grand pair (rule 4).
- `verify-b5-family.js` (full, after the review fixes): **323 assertions, 0 failures, PASS**. d1/d2/d3 + 722 chrome (body 710) + 677 chrome (body 677, stack 653, lowest = footer top 921): verify 0, lints 0, floors held (busts 110/96/88 box, chips 56, boxes 64x56, text >= 16). Sweep d2 x 20 seeds: verify clean, both grand sides, worst box/answer 8/20 (= 40 %, at the line; a 400-seed build sweep is uniform 15-25 % per cell — small-sample noise, but flagged). **30 poisons killed** (incl. the review's SP1/SP2/T1/GC1/GC2 below): P1-P16 (P14 twice: validator + build throw), refusal (de block absent), PR1, PR2, PR3, PR4, PR5, PR7, PR8 (note: `qa/lints.js` alone PASSES a codeColors fill — it whitelists them; the gate's own ban catches it). **PR6 deferred** (F5 not built).
- `node i18n/build-en.js`: 662 types, title lint clean.
- `node tools/b3-baseline.js --check --quick`: `checked build 3656 + enum 277 in 20s: 0 drifted (0 expected), 0 missing` **PASS**.

## Deviations (each measured)
1. **Crown top.** The design's crown rect (row bbox inflated 20) starts at y -10 and is cut flat by the stage edge. Now: rect top clamped to y 28 (= 2 + bump radius) plus leafy bumps (r 26, step 48) along the top and down both sides, so it reads as a tree canopy, not a card. Crown bottom unchanged (302 at d2, 12 above row 2).
2. **Trunk outline `grid`, not teal.** In the d2 render the teal trunk sides beside the sibling drops read as extra relation lines; only relation lines are teal now.
3. **Layout packed to the top** (flex column `flex-start`: stage | 12 | words; ALL slack below the word block) instead of `minmax(457px,1fr) 12px auto`, which parked ~60-300 px above the tree. (An intermediate `space-evenly` left a ~130 px band under the ground; see the review section.) Stack unchanged: d2 653, fits 677 with 24 spare.
4. **Answer-column rule stronger:** no three consecutive boxes stepping +/-1 (the design only barred a fully monotone column; the first d2 render shipped 1,2,3,5,4).
5. **Rule 2 prefix `mo`/`fa`, not `mor`/`far`:** moster = mor+syster, faster = far+syster contract; the design's rule would reject correct sv/da words.
6. **Rule 9 scoped to the F2 pool** (the d2 family words), not every K word: en `baby brother` resolves to glyphH 36.2 < 40 at lane 321 — F2 must never badge the baby (Phase-2 note).
7. **Disc clearance** measured from the connector centre-line (design arithmetic: sibling bar 298 vs disc top 309 = 11 >= 10); from the stroke edge it is 9.5.
8. Elder f has 6 looks (glasses 3 of 6); elder m 7 (glasses 3 of 7).
9. Clue frames added for Zy/By (rule 6 requires one per path).
10. Bust floor measured on the bust BOX (a nested svg's client rect is its ink: 81 px at d2).
11. **Not built (Phase 2):** `genRail genPlacard traceRows nameBox clueList riddleRow templateTree` and `primitives/frame-tree.js` (F5).

## Renders to read (colour + greyscale)
`scripts/worksheet-gen/out/dev/K-370-null-d{1,2,3}-en.png` + `-grey.png` · `out/dev/K-370-gate-d2-en-chrome-{three,four}.png` · figure sheets `out/dev/K-370-figure-sheet-{72,120}{,-grey}.png` · tree sheet `out/dev/K-370-tree-sheet{,-grey}.png`. Read by me: busts read as child / parent / grandparent (grey hair + glasses + cheek lines vs dark hair; child head:shoulder 0.87 vs adult 0.45); mono keeps hair ink vs grey and every teal line; the ego is carried by star + name plate in mono.

## Open items for faces / panels
- **Three-adult mono read-test** (design risk mitigation) before the wave; the elder looks without glasses (ponytail, braid, curly-long / side-part, curly-short, beard, moustache, bald-crown) are the ones to watch for "grandpa = dad with light hair".
- d1 is sparse (475 stack on a 766 body); it does not ship.
- Subject bucket (`letters`) and the `'Family'` strand row are Phase C (`_PANEL-FINDINGS` items 1-2).
- The EN strings are a source to audit; F2 instruction says "tree" (F2 draws one), F4 "box" = the word bank.

## Review fixes (coordinator, 2026-09-23)
1. **Garment colour tied to sex** (the grandmother alone in coralSoft: the look-bound tints). Now ONE seeded tint per generation row, shared by everyone in it (grand couple, parents, children), drawn from its own rng stream (`<seed>|garment`) so a cosmetic draw never shifts the badge / word-order draws. verify(): one fill per generation row; gate sweep: every fill is worn by both sexes across the 20 seeds. Poisons: GC1 look-bound tints (verify FAILS: "garment fills differ within a generation row") · GC2 sex-keyed fills (sweep FAILS: "fill #FBE3D8 is worn only by sex f").
2. **Sparse band under the tree.** Layout packed to the top; bands (body top -> stage, stage -> words) measured 0 / 12 px at 814 (one-line chrome), 766 (default), 722 and 677. Gate: max band <= 40 and no negative band. Poisons both ways: SP1 `space-evenly` at 814 (91 px band, FAILS) · SP2 word block ridden 80 px into the stage (OVERLAP, FAILS).
3. **Orphaned "Who?"** RULING: keep the final's SEO title text "Family Members: Who Is Who?" and bind the question with NBSPs (`Who Is Who?`, the fr-title NBSP precedent: 103 fr titles carry NBSP), so it breaks after the colon: "Family Members: / Who Is Who?" (2 / 3 words). Gate ORPHAN check: no title line holds a single word; poison T1 = the same title with plain spaces wraps 4 / 1 and FAILS. The NBSP rides into strings.en.json and SEO metadata as a space-equivalent; panels may do the same in their locale.
- Sweep after the fix: worst box/answer 8/20 (40 %, at the line, unchanged); a 400-seed build sweep is uniform (max 26 % per cell).
- Re-verified: figure gate 341 / tree gate 54 / family gate 323, all PASS; build-en clean (665 types); `b3-baseline --check --quick`: `checked build 3668 + enum 277: 0 drifted (0 expected), 0 missing` PASS. Renders re-read: `out/dev/K-370-null-d{1,2,3}-en{,-grey}.png`, `out/dev/K-370-gate-d2-en-chrome-{one,three,four}.png`.
