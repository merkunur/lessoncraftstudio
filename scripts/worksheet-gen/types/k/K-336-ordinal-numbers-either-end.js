/** K-336 — Ordinal Numbers from Either End: Start at the Flag. nt20-C variation of K-320 (generated by tools/gen-b3var-specs.js from tools/b3var-rows/). */
'use strict';
const base = require('./K-320-ordinal-numbers.js');
// One object for all three levels: the waves ship d2 only, so a face must
// render identically whichever level is asked for. Spreading the base entry
// (not a JSON literal) carries function-valued params through intact.
const D = { ...base.difficulty[2], ...{"start":"mixed"} };
module.exports = {
  ...base,
  id: 'K-336',
  slug: 'ordinal-numbers-either-end',
  difficulty: { 1: D, 2: D, 3: D },
  i18n: { en: { title: "Ordinal Numbers from Either End: Start at the Flag", instruction: "Look for the flag first: it can stand at either end. Start there, count to the number of each chip and make the mark it shows." } },
};
