# G2-317 `verb-forms` — the FIVE variation faces (Phase 2, 2026-09-14)

Built from `G2-317-verb-forms.md` §3 (+ §1/§4/§5 for the per-locale bank shapes, refusals and the two deferred poisons) under `_FACE-BRIEF.md`, on the base substrate of `_work/G2-317-build.md`. Ids from `_records/b3var-id-allocation.json` (G2-334 … G2-338, all `types/g2/`). Nothing shared was edited (`git status`: only the family's four files + the six new ones below; the other modified `components-b3/*` files belong to sibling builders); nothing committed. Every measurement is from the real render pipeline (file:// fonts) at d2 en under the shipped chrome (body 766-799), the WORST chrome (**710** — the base record's measured floor, every face budgets it) and the gate's de (persons) + nl (stack) fixtures.

## Files (all family-scoped)
| file | what |
|---|---|
| `scripts/worksheet-gen/tools/b3var-rows/verb-forms.js` | NEW — 5 rows (G2-334 … G2-338), `HANDWRITTEN []`; the only file the emitter reads |
| `scripts/worksheet-gen/types/g2/G2-317-verb-forms.js` | additive knobs dispatched in `_buildWith` BEFORE the base path touches the RNG: `match` → `_buildMatch` (kind = the bank's: tense / persons / stack), `tables:0` → `_buildSentences`, `choice` → `_buildChoice`, `hunt` → `_buildHunt`; `poolFor('irregular')` → the `irregularCore` (`core:true`, chip-only), `withForms` (a picture optional), `paradigmOf`, `pickRows` (distinct verbs + frames, form ≠ infinitive, banned tokens, pictured-first with a varying count), `_laneGrid`, `_root`; the base path appends `data-lcs-face="irregular" data-lcs-pool="irregular"` ONLY for the core pool and relaxes its persons floor to `d.tables` there; `verify()` reads `data-lcs-face` and dispatches to `VERIFY_FACE` (match / sentences / choice / hunt), the base branch drops its three picture rules for the core pool |
| `scripts/worksheet-gen/templates/components-b3/verb-forms.js` | `sentenceGap` gained ADDITIVE options (`padding`, `lineHeight`, `chipsGap`, `pillPx`/`pillH`/`pillPad` own pill markup, `answerBox`, `idx`, gapH/gapW already there) — absent = today's markup; `formMatch` gained per-column widths/heights, `padY`/`padX`, `fontPx`, `leftPad`, `iconPx`, per-item `attrs`, a `spacer` for unpictured left items; NEW `matchBlock` (a bordered block = optional header strip + formMatch). `verbTable` untouched. |
| `scripts/worksheet-gen/data/b3/verb-forms.js` | EN bank: 8 NEW frames f18-f25 the irregular core can fill (Face 4's lanes — no base frame fit be/have/do/go). Their `fits` name ONLY core + UNPICTURED verbs, so the base's table-slot filter (pictured verbs only) never sees them and the base's lane RNG path is byte-identical (baseline PASS after the edit) |
| `scripts/worksheet-gen/qa/verify-b3-verb-forms.js` | the gate grows: section **F** (`renderFace` / `renderIrregular` / `runFaces`: 21 renders — en shipped + WORST chrome, the de persons fixture (+ core frames d15-d20), the nl fixture (+2 pictured regular verbs → the F2 stack variant) — per-face geometry + Node re-derivation of every stamp + the **stem bot** + 5 seed sweeps), the two deferred poisons **P6 + P10** and 23 face poisons (PF1 · PF2a-d · PF3a-c · PF4a-b · PF5a-d · PF6a-e · PB1-4); final line in the brief's format |
| `types/g2/G2-334-verb-forms-match-the-past-form.js` · `G2-335-verb-forms-in-sentences.js` · `G2-336-verb-forms-helper-verbs.js` · `G2-337-verb-forms-choose-the-form.js` · `G2-338-verb-forms-find-the-verb.js` | emitted by `node tools/gen-b3var-specs.js` |
| this file | the record |

## The five faces

### G2-334 — Match the Verb to Its Past Form — CODE `match:true` (F2, L.1.1.c)
- **Row:** `{...base.d2, match:true, items:[6,16], mTense:{verbs:6, lanes:0, pictured:4, leftW:250, rightW:250, leftH:106, rightH:46, padY:0, padX:30, fontPx:20, iconPx:44}, mPersons:{verbs:2, lanes:2, blockW:330, headerH:60, iconPx:44, itemH:56, leftW:110, rightW:144, padY:6, padX:8, fontPx:18, leftPad:8}, mStack:{minPersons:4, verbs:3, lanes:0, blockW:675, headerH:48, iconPx:36, itemH:48, leftW:200, rightW:300, padY:2, padX:30, fontPx:18, leftPad:14, gap:12}}` — ONE row, the KIND is the bank's (`mode:'tense'` → tense; persons with `matchPersons.length < 4` → stack; else persons).
- **Child (tense, en sv da no):** RECOGNISE — six infinitives on the left (pictured first, ≥ 4 pictured; verbs whose past IS the infinitive — en `read` — are excluded, the answer would be printed on the left), **twelve** forms on the right = the past (target) AND the present (distractor) of every verb in a DERANGEMENT (no verb sits level with its own past); the child draws six lines to the past forms. **Child (persons):** two pictured blocks side by side (header = action picture + the infinitive), pronoun → form (`bank.matchPersons`, forms pairwise distinct, a derangement), + 2 gap lanes with the chip. **Child (stack, nl):** three full-width blocks × 3 pronouns, no lanes (9 lines).
- **Stamps:** root `data-lcs-face="match" data-lcs-matchkind data-lcs-blocks data-lcs-lanes data-lcs-minpictured data-lcs-items`; block `data-lcs-matchblock data-lcs-verb data-lcs-inf` (+ `[data-lcs-thead] [data-lcs-inf]` + img in persons/stack); left `data-lcs-match-left data-lcs-verb|data-lcs-col`; right `data-lcs-match-right data-lcs-role data-lcs-verb data-lcs-col`.
- **verify():** blocks = stamp; right texts pairwise distinct; no left text printed on the right (gap = anchor); every item ≥ 36 with its dot; tense: 2n right = n targets (col past) + n distractors (col pres), one pair per verb, the target ≠ the infinitive, derangement, ≥ minpictured pictures; persons/stack: ≥ 3 pronouns, one target per column, derangement, the header prints the infinitive over a picture; lanes as the base (verb on a block, form ≠ chip, no printed form). **Node gate:** every left label === `bank.inf`, every right text/stamp === `forms[unit][col]`, role ⇔ column, `past !== inf` per verb, pictured ⇔ `bank.pic`; persons: the block verb is a pictured `match !== false` regular verb, the pronoun labels === the column labels (or `labelOverride`) in `matchPersons` order; **stem bot** (longest-common-prefix over the rendered right column, a tie = no information = a miss) **must score < n/n — measured 0/6 on every one of 20 seeds** (every present distractor shares ≥ the past's prefix); items text inside their items; the two dot columns ≥ 6 px apart.
- **Poisons owned:** **P10** (the deferred one: every present distractor stripped → stem bot 6/6), PF2a (a target moved level with its verb → derangement), PF2b (a hand-edited right form → re-derivation), PF2c (a distractor printing the infinitive → printed on the right), PF2d (a persons row without its column's form), PB2 (left items 130 high → outside the body at the worst chrome).
- **PNG:** `scripts/worksheet-gen/out/dev/G2-334-null-d2-en.png` (gate: `out/dev/g2317-gate/G2-334-{en,worst-en,fixture-de,worst-de,fixture-nl,worst-nl}.png`). Read: sing / gather (no picture, a spacer keeps the words aligned) / jump / wander / dance / run → ran wandered danced jumped sang runs gathered gathers wanders dances jumps sings; no verb level with its past; block 700 high at the worst chrome, nothing under the footer. de fixture: springen + wandern (ich du er/sie/es wir) + 2 lanes; nl fixture: rennen / lopen / dansen × ik jij wij.

### G2-335 — Verb Forms in Sentences with Pictures — CODE `tables:0, lanes:6` (F3, L.1.1.c + L.2.1.d)
- **Row:** `{...base.d2, tables:0, lanes:6, pictured:4, hint:true, laneMin:96, laneMax:132, items:[6,16]}`.
- **Child:** APPLY — six gap sentences over six DIFFERENT verbs (4-6 pictured; the count varies per seed so the verb set is not constant), the infinitive in the chip, a 170 × 38 box in the sentence, no table. Lanes `repeat(6, minmax(96, 1fr))` gap 12 = 636 at the floor, flexing to 132 each.
- **verify():** 0 tables; 6 lanes; distinct verbs + frames; one box; the chip === the infinitive; the form never printed and never the infinitive; ≥ 4 pictured. **Node gate:** every lane re-filled from the bank frame (text / col / unit / fits / form), the chip === `bank.inf`, lane pictures 56, sentence ≤ 2 lines; sweep: 17 verbs / 17 frames over 20 seeds.
- **Poisons owned:** PF3a (a chip ≠ the infinitive), PF3b (a verb twice), PF3c (the sentence printing its form), PB3 (a pictured floor of 7 over 6 pictured verbs → REFUSED).
- **PNG:** `out/dev/G2-335-null-d2-en.png`. Read: Lily ___ in the garden today (read) · Today Ben ___ with his sister (run) · On Mondays Anna ___ at the club (sing) · Every day Mia ___ in the park (wander) · Tom ___ at the pool yesterday (jump) · After school Leo ___ at home (dance); lanes 118 (en) / 108 (worst).

### G2-336 — The Helper Verbs: be, have, do, go — PARAM `pool:'irregular'` (F4, L.2.1.d)
- **Row:** `{...base.d2, pool:'irregular', tables:2, verbsPerPage:2, given:1, minHardGaps:4, tenseRows:4, tenseGaps:6, minPerCol:3}`.
- **Child:** the base's own table on the bank's `irregularCore`: tense (en sv da no) = ONE 4-row table (the whole en core be / have / do / go), 8 cells, 6 gaps (≥ 1 per row, ≥ 3 per column), 2 printed; persons = two chip-only tables (sein / haben), 1 given + ≥ 4 hard gaps, anchors as the base (de `wir/sie haben`, `sie sind` when `wir sind` is the given); + 3 lanes over the core verbs (the NEW frames f18-f25; de fixture d15-d20). Items 9 (en) / 11 (de).
- **Knob:** one additive pool NAME — `poolFor(bank,'irregular')` → `{core:true}` reads `bank.irregularCore`, `eligible` drops the picture requirement for it, the persons floor is `d.tables` (the design's own "irregularCore ≥ 2" floor) instead of 3. The base's default pools never set `core`; its output is byte-identical (baseline PASS).
- **verify():** the base branch with `data-lcs-face="irregular"` accepted and the three picture rules (row / header / lane picture) dropped when `data-lcs-pool="irregular"`. **Node gate:** the base `renderCheck` (cells re-derived — `findVerb` already spans the core — lanes re-filled, hard gaps, box 36) + every table AND lane verb ∈ `irregularCore`.
- **Poisons owned:** PF4a (the `go` row renamed `jump` → not in irregularCore), PF4b (3 tables in persons mode over a 2-verb core → REFUSED).
- **PNG:** `out/dev/G2-336-null-d2-en.png`. Read: go goes ___ · have ___ ___ · be ___ was · do ___ ___; lanes "Every morning Leo ___ to school (go)", "Yesterday Tom ___ at the zoo (be)", "Yesterday Lily ___ a bad cold (have)"; de fixture: sein (ich du er ihr gaps, wir sind given, sie sind anchor) + haben (4 gaps, wir/sie haben anchors).

### G2-337 — Choose the Right Verb Form — CODE `choice:true` (F5, L.1.1.c/e)
- **Row:** `{...base.d2, choice:true, rows:8, candidates:3, pictured:3, rowMin:80, rowMax:110, rowGap:8, gapW:110, gapH:26, lanePad:'5px 14px', pillPx:20, pillH:36, pillPad:18, hint:false}`.
- **Child:** DISCRIMINATE — eight gap sentences, each over three printed forms of the SAME verb's paradigm (tense: `ran / runs / run`; persons: `läuft / laufe / laufen`), never another verb, never invented; the child circles one. Verbs with fewer than 3 distinct forms are F5-excluded (en `read`); the frame prints none of the candidates; the correct pill's position covers 0, 1 and 2 over the page (`data-lcs-idx`, shuffled `[0,1,2] + 5 random`). Rows `repeat(8, minmax(80, 1fr))` gap 8 = 696 at the floor.
- **verify():** 8 rows; 3 pills each, texts distinct, exactly one === the form, none printed in the sentence, idx === the DOM index, every index reached; pills == pillH ≥ 36 inside their row; one slot box; no chip; distinct verbs + frames; ≥ 3 pictured. **Node gate:** re-fill + every pill ∈ `paradigm(bank verb)`, the correct one present, paradigm ≥ 3; **the sentence ONE line at the worst chrome** (measured 24 px); sweep: idx 0/1/2 all seen, 24 verbs / 21 frames.
- **Poisons owned:** **P6** (the deferred one: a pill rewritten outside the paradigm), PF5a (the correct pill always first → never at position 2/3), PF5b (two identical pills), PF5c (the sentence printing a candidate), PF5d (idx ≠ DOM), PF1 (pills squashed to 24 → < 36), PB1 (rowMin 120 under the worst chrome → footer overlap / outside the body), PB4 (9 candidates over 8 rows → cannot cover).
- **PNG:** `out/dev/G2-337-null-d2-en.png`. Read: sit sits **sat** · **swims** swam swim · **climbs** climb climbed · hopped **hops** hop · shouts **shouted** shout · wanders wander **wandered** · **runs** run ran · **dances** danced dance; 3 pictured (wander run dance); rows 93 (en) / 82 (worst), pills 36.

### G2-338 — Find the Verb, Write Its Base Form — CODE `hunt:true` (F6, L.1.1.b + L.2.1.d)
- **Row:** `{...base.d2, hunt:true, rows:8, pictured:4, rowMin:80, rowMax:110, rowGap:8, lanePad:'6px 14px', infBox:{w:170,h:38}, hint:false}`.
- **Child:** ANALYSE — eight sentences printed WHOLE with the conjugated verb inside (render `text`); the child underlines the verb and writes its base form in a right-aligned 170 × 38 dashed box. The printed form is never the infinitive itself (nothing to write), the frame prints NO other bank verb form (one verb to find) and no `hunt.nounHomographs` token (`dance / play / jump`).
- **verify():** 8 rows; `data-lcs-inf` === the verb; the printed span === the form; the form exactly ONCE as a token; the infinitive not printed; no homograph token; exactly one EMPTY box of the stamped size inside the row; no chip; distinct verbs + frames; ≥ 4 pictured. **Node gate:** re-fill (text with the form in place) + the form's bank OWNER === the stamped infinitive + no other bank form among the sentence tokens; sweep: 25 verbs / 21 frames.
- **Poisons owned:** PF6a (the box carrying text), PF6b (the printed form removed → 0 occurrences), PF6c ("At the dance, " prepended → noun-homograph), PF6d (a hand-edited inf stamp), PF6e ("Ben sleeps and " prepended → another bank form printed).
- **PNG:** `out/dev/G2-338-null-d2-en.png`. Read: After school Leo dances at home · Yesterday Max jumped at the party · Last night Mia sang for an hour · Yesterday Tom shouted at the zoo · Today Ben reads with his sister · Last week Leo wandered in the forest · Anna ran at the picnic yesterday · Ben hides a red bike; 6 pictured; box 170 × 38 right-aligned; rows 89 (en) / 82 (worst).

## Gate output
```
node qa/verify-b3-verb-forms.js --quick
… [F] G2-334-en: body 766, instr lines 2, tense: 1 block(s) (6→12), 0 lanes, bot 0/6 → ok
    [F] G2-334-worst-en: body 710, instr lines 3, tense: 1 block(s) (6→12), 0 lanes, bot 0/6 → ok
    [F] G2-334-fixture-de: … persons: 2 block(s) (4→4, 4→4), 2 lanes → ok · G2-334-fixture-nl: … stack: 3 block(s) (3→3, 3→3, 3→3), 0 lanes → ok
    [F] G2-335-worst-en: body 710, instr lines 3, 6 rows (6 pictured; h 108) → ok
    [F] G2-336-en: … 1 table(s), 8 cells (6 gaps), 3 lanes → ok · G2-336-fixture-de: … 2 table(s), 12 cells (8 gaps), 3 lanes → ok
    [F] G2-337-worst-en: body 710, instr lines 3, 8 rows (3 pictured; h 82; pills h 36, idx 20011200) → ok
    [F] G2-338-worst-en: body 710, instr lines 3, 8 rows (6 pictured; h 82; box 170x38) → ok
    [F] sweep match: 6 seeds, 0 fails, 6 distinct pages, 11 verbs, 6 orders, bot max 0 · sentences 9 verbs 15 frames · irregular 4 verbs 7 frames · choice 21 verbs 18 frames, idx 012 · hunt 16 verbs 17 frames
    [F] killed P10 … P6 … PF2a-d, PF3a-c, PF4a-b, PF5a-d, PF6a-e, PF1, PB1-4 (25/25)
G2-317 gate: PASS (224 assertions, 53/53 poisons killed; base 28/28 + faces 25/25, the 2 deferred now land in the faces)
```
Full run (20 seeds; match 24 verbs / 20 orders / bot max 0; sentences 17 verbs; choice 24 verbs, idx 012; hunt 25 verbs; 0 SILENT / 0 THREW): **`G2-317 gate: PASS (322 assertions, 53/53 poisons killed; base 28/28 + faces 25/25, the 2 deferred now land in the faces)`**

`node tools/gate-variation-distinct.js --batch=b3 --diffs=2 --family=verb-forms` → `[b3:verb-forms] compared 15 pairs over 5 faces against their bases + pairwise within family` / **`every variation differs from the deck its base publishes and from its siblings`**.
`node tools/b3-baseline.js --check --quick` → **`checked build 3392 + enum 211 in 17s: 0 drifted (0 expected), 0 missing` / `PASS`** (also PASS before the first edit — the control — and after each of the component / bank / spec edits: the base's default path is byte-identical).
`node i18n/build-en.js` → `build-en: 598 types … (title lint clean)`; `git checkout -- i18n/strings.en.json` restored the committed file.

## Deviations from §3 (each with the measurement)
1. **F2 tense draws ≥ 4 pictured + unpictured fill, not "6 pictured infinitives".** en has 6 pictured verbs and one of them (`read`) has past === infinitive — on a match page that prints the answer on the left, so it is excluded, leaving 5. The sixth (and, some seeds, a fifth) comes from the unpictured pool with a 44-px spacer keeping the words aligned; `mTense.pictured:4` is the floor (gate-asserted). sv/da/no (7 pictured, past ≠ infinitive everywhere) fill 6/6 pictured.
2. **F2 tense geometry: left 106 / right 46, block padding 0** (design: 100 / 48 / 708 total). 12 × 48 + 11 × 12 = 708 was computed against a 736 body and without the block's 4 px border; under the MEASURED 710 floor: 6 × 106 + 5 × 12 + 4 = 700 and 12 × 46 + 11 × 12 = 684 (space-around). PB2 (left 130) proves the floor bites.
3. **F2 and F3 item window `[6,16]`, not the base's `[8,16]`.** The tense match draws 6 lines (over 12 candidates) and F3 has 6 lanes — the design's own layouts; the 8-floor is the base's gaps + lanes count. Persons F2 = 8-14 items, stack = 9.
4. **F4 tense = a 4-row table (`tenseRows:4`, 6 gaps).** The en core is exactly be / have / do / go; the base's 6-row tense table cannot be filled from a 4-verb core (`REFUSED` at build, measured). Items 6 + 3 = 9. Persons: `minHardGaps:4` (design "≥ 4 gaps"), the core floor = `d.tables` (2 — the design's "irregularCore ≥ 2"), so d3's "3 verbs" would REFUSE de/nl until `werden` / a third core verb is authored (PF4b proves the refusal).
5. **F4 needed 8 EN frames (f18-f25): no base frame fit be / have / do / go**, so "lanes stay" was unbuildable as authored. Their `fits` are core + unpictured verbs only (the base's table slots are pictured verbs, so its lane filter never sees them — baseline PASS is the proof). The de fixture gained d15-d20 the same way. Panels author ≥ 3 core-fitting frames (≥ 1 per core verb at a non-anchor person).
6. **F4 is PARAM in the row but needed one additive pool NAME** (`irregular` → `irregularCore`, chip-only, picture-free) — "no code" cannot hold when the pool lives in a different array than `verbs`; the G1-308 `maxPerPair` shape.
7. **F5 rows 80 (not 84), slot box 26 × 110 (not 38 × 170), own pill markup h 36 (not pillChoice's ~40), row padding 5.** 8 × 84 + 7 × 8 = 728 > 710. And the design's "line 1 = the sentence (25) + pills (40) = 71 ≤ 72" ignored that the INLINE gap box sets the line box: a 38-px box lifts the sentence line to ~40 px and the pills left the row at the worst chrome (measured: `a pill outside its row` ×24 + footer overlap 925 > 921, first run). A 26-px slot marker (≥ the 22 answer floor — the child circles a pill here, the box marks where the word goes) keeps the line at 28: 28 + 4 + 36 = 68 ≤ 70. PB1 (rowMin 120) proves the floor.
8. **F5 carries a `pictured:3` floor** the design does not name (it names none): without it a seed can draw 8 unpictured rows and the page reads as a text sheet; en has 5 F5-eligible pictured verbs. A locale below 3 pictured F5-eligible verbs would REFUSE — none is visible today (see refusals).
9. **F6 rows 80 (not 84), padding 6 14** → inner 68 = 24 + 4 + 38 (+2). Box 170 × 38 as designed. A frame that prints ANY other bank verb form is skipped by the composer (design: "the form occurs exactly once") — a second finite verb in the sentence would make "underline the verb" ambiguous; the node gate asserts it (PF6e).
10. **The stem bot treats a tie as a miss** (the design says "longest-common-prefix matcher < 6/6"): with a present distractor per verb the prefix never discriminates (`jump` → `jumps` = `jumped` = 4), so the bot has no information; a bot that breaks ties by rendered order would fail a CORRECT page by shuffle luck (an artefact, not a measure). Measured 0/6 on every seed; P10 (distractors stripped) → 6/6.
11. **F2 persons dots sit 12 px apart** (left dot span w+14..w+26, right dot at gap−26; gap 64 in a 330 block; gate floor 6). The lit-letter-knowledge original spans 675 px. Wider needs the stack layout for every persons locale — an operator/panel taste call, not built.
12. **d1 / d3 variants of the faces are not built** (the emitter ships ONE config); the code accepts what the design lists where it is a config (F2 d1 2 verbs / no lanes; F5 d1 2 candidates; F6 rows) and refuses what it cannot (F4 d3 3 core verbs on a 2-verb core).

## Per-locale refusals / at-risk faces visible from the bank shape (Phase 4 `hub-expectations.json`)
Nothing beyond en is authored yet; these read the design's §1/§4 pool counts against the faces' floors as BUILT:
- **fi — F3 and F6 at risk (pictured floor 4 over the `noGradation` pool).** The d2 pool name `regular` → `noGradation` per §2; of the 7 pictured fi verbs (juosta lukea hyppiä tanssia laulaa vaeltaa maalata) only **tanssia laulaa maalata** are gradation-free (lukea → luen, hyppiä → hypin, vaeltaa → vaellan, juosta → juoksen) = **3 < 4** → F3 (`pictured:4`) and F6 (`pictured:4`) REFUSE fi unless the panel pictures a 4th gradation-free verb (`kävellä` via `activities/hiking` is on the design's fi list and is the natural cue) — then exactly the floor. F5 (`pictured:3`) clears at 3. F2 persons (2 pictured) clears. Rows 6 with kävellä pictured, else **4**.
- **fr — F3 and F6 at the knife-edge.** The exemplar unit `er` is a GROUP unit: the pool is -er verbs only; pictured -er = sauter danser chanter = **3**, **4 only with `marcher` via hiking** (the design's own panel note) → F3/F6 ship at exactly the floor, else REFUSE (rows 6 or 4). F2 persons: `je saute = il saute` (design's matchPersons je tu nous vous ils avoids it) ✓.
- **es — F2 left labels.** `ellos/ellas` / `nosotros` at Nunito 800 18 in a 110-px item overflow (≈ 116 / 101 px + 16 padding; measured de `er/sie/es` 69 + 16 = 85 fits). The design says Face 2 lists ONE of ellos/ustedes — a short label (`ellos`, via `labelOverride`) or the gate's overflow assertion refuses the render. Not a face refusal.
- **nl — F2 ships as the STACK variant** (matchPersons ik jij wij = 3 < 4 → 3 full-width blocks × 3, 9 lines, no lanes; rendered from the fixture). Design: "a DATA decision the panel may still refuse" → 6 rows, 5 (65 total) if refused.
- **de / nl — F4 persons at exactly the floor** (sein/haben, zijn/hebben = 2 = `tables`); fine at d2, d3's 3 verbs would refuse.
- **de / it / es / pt** — no refusal visible: pictured 8 / 6 / 7 / 6 (all clear 4), matchPersons 4 / 6 / 5 / 4, irregular verbs are d3-only so the regular pool keeps ≥ 6.
- **sv / da / no** — tense: F2 needs 6 verbs with past ≠ infinitive (7 pictured, Nordic pasts never equal the infinitive) ✓; F4 tense needs ≥ 4 core verbs (the design lists 8) ✓; F5 paradigm inf/pres/past distinct for every weak verb (`hoppa hoppar hoppade`) ✓. Rows 6.
- **en** — 6 rows (`read` excluded from F2 and F5 by rule, not a refusal).
Expected rows: **en de es pt it nl sv da no = 6 · fr 6 (4 without marcher pictured) · fi 6 (4 without kävellä pictured)** → 66 as §7 expects, floor 62.

## Open items for the panels / Phase 4
- The `tools/validate-b3-draft.js` verb-forms block should mirror the faces' data rules: F2 tense excludes past === infinitive verbs and needs ≥ 6 with ≥ 4 pictured; F2 persons needs ≥ 2 pictured `match !== false` regular verbs with pairwise-distinct `matchPersons` forms (stack: ≥ 3); F3/F6 need ≥ 4 pictured verbs WITH a fitting frame at a non-anchor column (F5 ≥ 3); F4 needs ≥ `tables` core verbs (tense: ≥ 4) AND ≥ 3 frames fitting the core; F6 frames must print no other bank form; F5 frames must print no paradigm form of a fitting verb. The report line, never a fill.
- Panels author the core frames (≥ 1 per core verb, non-anchor person) — the EN f18-f25 are a SOURCE TO AUDIT; de/nl third-person core frames carry a NAME (d16 `Emma hat …`, d19 `Ben und Mia sind …`).
- Face 2 persons pronoun labels ≤ ~90 px at Nunito 800 18 (es `ellos`, pt `eles`, fr `ils/elles` 75 ✓) — `labelOverride` where the column label is long.
- Hub `coordinate.mode` for the five landings (§6: match / sentences / irregular / choose / infinitive) vs the stamps (`data-lcs-face` = match / sentences / irregular / choice / hunt) — the registrar maps; `verify-hub-type-rows.js --keys=verb-forms` expects 6 per locale minus the refusals above.
- The persons F2 dot spacing (12 px) — widen only by moving every persons locale to the stack layout (taste call).
- Registration (`apps['verb-forms']`, `axes['exercise-type']['verb-forms']` × 11, landings, `hub-expectations`) is the batch's registrar step, not this build.
