/** G2-340 — {U} Fact File with a Word Bank. nt20-C variation of G2-318 (generated by tools/gen-b3var-specs.js from tools/b3var-rows/). */
'use strict';
const base = require('./G2-318-animal-fact-file.js');
// One object for all three levels: the waves ship d2 only, so a face must
// render identically whichever level is asked for. Spreading the base entry
// (not a JSON literal) carries function-valued params through intact.
const D = { ...base.difficulty[2], ...{"cell":"write","bank":true,"bankExtra":4,"bankPx":17} };
module.exports = {
  ...base,
  id: 'G2-340',
  slug: 'animal-fact-file-with-a-word-bank',
  difficulty: { 1: D, 2: D, 3: D },
  i18n: { en: { title: "{U} Fact File with a Word Bank", instruction: "Find each true fact in the word bank and copy it into the right row of the fact file. Then finish the sentence at the bottom." } },
};
