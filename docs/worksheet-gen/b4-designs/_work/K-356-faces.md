# K-356 `weather-symbols` : the FIVE variation faces (Phase 2, 2026-09-21)

Built from `K-356-weather.md` §3 (+ §1 / §4 / §5 for the rulings, data and gates) under `_FACE-BRIEF.md`, on the base substrate of `_work/K-356-build.md`. **The family KEY is `weather-symbols`** (Phase 0 ruling: the THEME axis owns `weather`); the design file's `weather` is read as `weather-symbols` in every file name, export and stamp; `withFixedTheme(type, 'weather')` stays and every face keeps the FIXED theme (`coordinate.theme:''`, `coordinate.mode` = the layout string). Ids from `_records/b4var-id-allocation.json`. Nothing shared was edited (`git status` on `scripts/worksheet-gen`: this family's files + the regenerated `i18n/strings.en.json`; the sibling families' files in the same status are the parallel builders'); nothing committed; the baseline was NOT captured. Every number below is measured (node against the real files, or puppeteer through `render/render-instance.js` with the shell's `file://` fonts). No em-dashes.

## Files (all family-scoped)
| file | what |
|---|---|
| `scripts/worksheet-gen/tools/b4var-rows/weather-symbols.js` | NEW: 5 rows (G1-362 K-364 K-365 G3-385 G1-363), `HANDWRITTEN` empty; the only file the emitter reads |
| `scripts/worksheet-gen/types/k/K-356-weather-symbols.js` | the additive `layout` knob wired: `_buildFace` dispatches to `_buildWrite / _buildDiary / _buildThermometer / _buildWaterCycle / _buildForecast` (+ `_refuseFace _faceRoot _srcOf _gear _cycleLabel _ask _reorder`); `verify()` reads the root stamp first and hands a face to `_verifyFace` (a second `page.evaluate` carrying its OWN copies of the six nouns, the gear-to-band map, the band table, the water-cycle ANCHORS and the cycle order; the gate asserts every copy against the global bank / `water-cycle.js`). The base path (`_buildWith` without `layout`) is untouched: `node tools/b3-baseline.js --check --quick` PASS below. **One structural addition at the export:** `gen-b4var-specs.js` emits a face as `{ ...base, id, slug, difficulty, i18n }`, and an object spread copies OWN enumerable properties only; `withFixedTheme`'s wrapper owns just `themeAxis` + `build`, so every method the wrapper's `build` / `verify` reach through `this` (`_buildWith`, `_buildFace`, `verify`, ...) is now hoisted onto the wrapper as own properties (the same functions; output unchanged). Without it every emitted face threw `this._buildWith is not a function`. (K-354 human-body, the other fixed-theme family, needed the same hoist.) |
| `scripts/worksheet-gen/templates/components-b4/weather-symbols.js` | the base's `symbolBadge` / `symbolMatch` byte-untouched; NEW `spellBox` (the locale's letter-box size) and `spellRow` becomes a `.ws-lane` card (padding 6 16, `data-lcs-box`); `diaryWeek` gains `stamps`; `bandThermo` gains `band` (`data-lcs-band-of`) + zero stage padding (the 677 fit); `forecastQuestion` becomes a GRID lane (`auto minmax(chipH, chipMax)`, `align-content:center`; the pills stretch their track) so the pills grow with the lane instead of leaving a blank band (deviation 4) |
| `scripts/worksheet-gen/types/g1/G1-362-weather-symbols-write-the-weather-word.js` · `types/k/K-364-weather-symbols-weather-chart-monday-to-sunday.js` · `types/k/K-365-weather-symbols-how-warm-is-it.js` · `types/g3/G3-385-weather-symbols-water-cycle-label-the-diagram.js` · `types/g1/G1-363-weather-symbols-read-the-weekly-forecast.js` | emitted by `node tools/gen-b4var-specs.js` (F1 / F5 carry `gradeBand: "G1"`, F4 `gradeBand: "G3"`) |
| `scripts/worksheet-gen/data/b4/weather-symbols.js` (gitignored, the reviewer force-adds) | EN bank: `strings['G1-362'..'G1-363']` = the §3 face titles + the §6 instructions (title / instruction per face, the apply- slots the ten panels fill); F4's instruction reworded (deviation 5) |
| `scripts/worksheet-gen/qa/verify-b4-weather-symbols.js` | the gate grows: sections 6-8 (the five faces x every authored locale + the de 3-line / fi 4-line chromes, the body PINNED to 722 and 677, every floor measured, a SPARSE assertion per face, node cross-checks, the verifyFace tables asserted against the global bank + the primitive, 9/9 pictures asserted ON DISK, `qa/verify-water-cycle.js` re-run, the rows = bank-strings check, the title-lists-config rule, the day-name room probe x11, a 20-seed face sweep, 29 face poisons) |
| `scripts/worksheet-gen/i18n/strings.en.json` | regenerated by `node i18n/build-en.js` (633 types, title lint clean); left as generated |
| `docs/worksheet-gen/b4-designs/_work/K-356-faces.md` | this record |

## The five faces

### G1-362 : Write the Weather Word : CODE `layout:'write'` (F1, **G1**) : REWORKED after the coordinator's read (the first build was sparse: bare 44 px rows floating in white, the K-357 class)
- **Row:** `{...base.d2, layout:'write', rows:6, bank:true, box:44, boxMax:60, gap:4, maxLetters:11, hyphenGiven:true, badge:72, iconPx:56}` + `extra {gradeBand:'G1'}` (`box` is the FLOOR, `boxMax` the cap; the shipped box is the LOCALE's).
- **Child:** finds each symbol's word in the dashed word bank (six `.ws-bankword` pills, Nunito 800 18, a shuffled order that is never the row order nor its reverse) and writes it letter by letter into the dashed letter boxes beside the 72 px round badge (icon 56), one `.ws-lane` CARD per row (cream, padding 6 16) that OWNS its band; `letterBoxes({n, box, gap:4})` per hyphen GROUP with a printed 14 px hyphen glyph between groups (fr `arc-en-ciel` = 3 + 2 + 4 boxes). The word is never printed on its row.
- **Box sizing (the coordinator's rule, `spellBox`):** the largest box in 44..60 at which the locale's WIDEST bank word (letters + printed hyphens at 30 px each) fits the lane room 639 - 72 - 12 = **555**: en (`rainbow` 7) -> **60**, de (`Regenbogen` 10) -> **51**, fr (`arc-en-ciel` 9 + 2 hyphens) -> 51, fi (`sateenkaari` 11) -> **46**; a word that does not fit at 44 REFUSES `write` (never a smaller box). Re-derived by the gate from the bank and asserted === the root stamp.
- **Build:** literals before the first draw; rows = `rng.shuffle(keys)`, bank = `_reorder`; lanes in a grid `minmax(88px, 1fr)` (88 = badge 72 + 2 x 6 + border 4) with 5 px gaps that FILLS the body: 117 px lanes at the en 2-line chrome, 103 at the de 3-line + 3-line chrome (94 with the de two-row bank), 101 at the fi 4-line chrome; under the PINNED 677 body with a two-row bank the lanes are 88.2 (6 x 88 + 5 x 5 + bank 122 = 675 <= 677: the reason the gap is 5, measured, an 8 px gap failed the de pin by 0.5 px).
- **Stamps:** root `data-lcs-layout="write" data-lcs-rows/bank/box/box-min/box-max/gap/max-letters/badge-px/icon-px/lane-pad`; rows `.ws-lane[data-lcs-spell="<concept>"] data-lcs-len data-lcs-groups data-lcs-box`; `[data-lcs-letterboxes]` per group, `[data-lcs-hyphen]` per glyph; the bank `[data-lcs-bank-banner]` / `[data-lcs-bank-word]`.
- **verify():** rows === stamp, 4..6, distinct concepts; box within box-min..box-max and every row's box stamp === the root's; one picture per row (weather / the noun / >= 44); boxes n-list === groups, sum === len <= maxLetters, hyphen glyphs === groups - 1, box svg h === box + 2 and w === n x box + (n - 1) x gap + 2; no letters printed on a row; **every row IS a `.ws-lane` card**, its badge + boxes INSIDE the lane (a row running past its lane fails: the fi 11-box row measures 634 of 639), **sparse:** the blank above / below the content inside a lane <= 24 px, consecutive lanes <= 10 px apart, the lane grid ends within 3 px of the body bottom; bank present iff `bank`, `rows` distinct single-token words, no picture; top-anchored; above the footer. **Node gate:** `data-lcs-len` / `data-lcs-groups` re-derived from `symbolWords[c]`; the box === `spellBox` over the locale bank; the bank set === the row words; bank order !== row order; no bank word a vocab adjective; every lane >= 88.
- **Measured:** en d2 (2-line title, body 799): box 60, lanes 117 x 6, blank-in-lane max 23, between 5, widest row 548 of 639; de synthetic §4 words under the 722 long chrome (body 710, two-row bank): box 51, lanes 94, blank 11, widest row (Regenbogen 10 boxes) 634; fi synthetic §4 words under the 677 4-line chrome (body 700): box 46, lanes 101, blank 14, `sateenkaari` 11 boxes = 634 of 639 inside its lane; en long / fi4 chromes: lanes 103 / 101. Fits 722 AND 677 (pinned) in every case.
- **Poisons owned:** PR3 (bank stripped -> "bank absent at d2"), PR4 (fr `arc-en-ciel` rendered as 11 boxes under the 3-2-4 stamps -> "letter boxes 11 ≠ groups 3-2-4"), PS1 (lanes `minmax(140px,1fr)` -> "28 px of blank inside the lane > 24"), **PS1b (the rejected build: bare rows without a lane card, 44 px boxes, `minmax(76px,1fr)` with 12 px gaps -> "row 1: is not a lane card" + the blank rules)**, PC3 (`hyphenGiven:false` -> the guard), PC5 (fi `sateenkaaret` 12 letters -> REFUSES).
- **PNG:** `scripts/worksheet-gen/out/dev/G1-362-weather-d2-en.png` (gate: `G1-362-gate-d2-en{,-chrome-long,-chrome-fi4}.png`, `G1-362-gate-d2-de-syn-chrome-long.png`, `G1-362-gate-d2-fi-syn-chrome-fi4.png`, poisons `G1-362-gate-poison-PR3/PR4/PS1/PS1b.png`). Read en: six cream lanes edge to edge, 60 px boxes (5/5/3/4/4/7), each badge the picture its concept names, no bare paper between lanes, the last lane on the footer rule. Read de (3-line title + 3-line instruction): the bank wraps `Regen` to a second row, 51 px boxes, `Regenbogen` 10 boxes inside its lane, lanes 94 px, nothing under the footer. Read fi (4-line title): 46 px boxes, `sateenkaari` 11 boxes reaching the lane's right padding and staying inside it, lanes 101, nothing clipped.

### K-364 : Weather Chart, Monday to Sunday : CODE `layout:'diary'` (F2, K, OPEN-ENDED)
- **Row:** `{...base.d2, layout:'diary', days:7, start:'mon', box:'rect', legend:6, legendPx:56}`.
- **Child:** every day of the week looks outside and DRAWS the matching symbol into that day's dashed box, copying from the wordless key card (six 56 px symbols, 2 x 3) in cell 8 of the 2 x 4 grid; day names VERBATIM from `calendar.js dayNames` (`diaryDays` overrides), Monday first (`start`, `diaryStart` may flip to `'sun'` for en / pt).
- **Build:** NO rng draw (seed-invariant, asserted: 20 seeds hash to 1 page); `wordLane` (d3) refuses as unbuilt; a day name carrying a numeral refuses.
- **Stamps:** root (the `diaryWeek` grid itself) `data-lcs-layout="diary" data-lcs-days="7" data-lcs-box data-lcs-start data-lcs-legend data-lcs-legend-px`; cells `data-lcs-day="<0-6>"` with `[data-lcs-day-name]` + one `[data-lcs-drawbox]`; the key `[data-lcs-key]` with six `[data-lcs-symbol]`.
- **verify() (structure only, nothing graded):** 7 cells in `(start + i) % 7` order, names distinct, the pill not clipped and INSIDE its cell (a pill wider than the 160 cell fails: "the panel sets diaryDays short forms, never a smaller font"); one EMPTY draw box per cell >= 136 x 200 (no img / text / svg) inside its cell; one key with the six distinct symbols, each picture complete >= legendPx, no text; no numeral anywhere on the page; no `[data-lcs-answer]`; **sparse:** the grid fills the body; above the footer. **Node gate:** names === `dayNames` from `start`.
- **Measured (en d2):** body 766, rows 377 / 377, draw boxes 136 x 309 (long chrome 281, fi4 276; >= 200 under the 677 pin), key icons 56 x 6, lowest = footer top 921 (the grid fills the body).
- **Poisons owned:** PR5 (a date numeral on the Monday pill -> "a numeral is printed"), PR22 (rows `minmax(340px,1fr)` at the fi 4-line chrome -> under the PINNED 677 body "the stage overflows the body"; see deviation 7), PS2 (the grid `flex:0 0 auto` with 326 px rows -> "ends N px above the body bottom"), PT2 (title "Sunday to Saturday" over `start:'mon'` -> the title-lists-config rule).
- **PNG:** `out/dev/K-364-weather-d2-en.png` (gate: `K-364-gate-d2-en{,-chrome-long,-chrome-fi4}.png`, poisons `K-364-gate-poison-PR5/PR22/PS2.png`). Read: Monday..Sunday name pills, seven tall empty dashed boxes, the key card bottom-right with sun / cloud / rain / storm / snow / rainbow at 56 px, the 2-line title, nothing clipped, the grid ending on the footer rule.

### K-365 : How Warm Is It? Cold, Warm or Hot : CODE `layout:'thermometer'` (F3, K)
- **Row:** `{...base.d2, layout:'thermometer', items:4, cols:2, rows:2, bands:['cold','warm','hot'], chips:['scarf','t-shirt','sunglasses'], thermH:220, keyH:120, chip:64, iconPx:56, min:-10, max:40, step:5, edges:[10,25]}`.
- **Child:** reads the KEY row (three band thermometers at the band middles 0 / 17.5 / 32.5, each beside its gear: scarf = cold, T-shirt = warm, sunglasses = hot), then judges each of the four numbered item thermometers (no picture on them, no numeral, no degree sign) by its band and RINGS the gear chip that fits (three 64 px chips per card, shuffled, never the same order on every card). Bands tealSoft / white / coralSoft with 3 px ink edge ticks at 10 and 25 (`thermometer.js` additive `bands` + `numerals:false`); mercury values from the band INTERIORS only (cold -10 -5 0 5 · warm 15 20 · hot 30 35).
- **Build:** ONE band table: the config must restate the global `bands / min / max / step / edges` exactly (a two-band d1 shape REFUSES as unbuilt); every band at least once, no row with one band twice, values without replacement, chip orders `rng.shuffle` with a re-draw if every card agrees. `cardGrid 2 x 2 numbered` (the CSS 14 px gap) fills the body under a 148 px key lane.
- **Stamps:** root `data-lcs-layout="thermometer" data-lcs-items/cols/rows/bands/chips/therm-h/key-h/chip/icon-px/min/max/edges`; the key `[data-lcs-key]` > `[data-lcs-key-band]`; cards `[data-lcs-thermo] data-lcs-n data-lcs-value data-lcs-band-of` > `[data-lcs-thermo-box]` + `[data-lcs-chip]` (`data-lcs-correct="1"` on one, never a visual).
- **verify():** the band stamps === the fixed table; every thermometer svg carries the three `data-lcs-band` rects tiling -10..40 in order + 2 edge ticks and NO `<text>`; no digit / degree sign outside the card ordinals (deviation 2); ONE key row (first on the stage) with the bands in order, mercury inside each band, the band's gear picture; `items` cards: value a legal interior value of its stamped band, the thermometer draws that value, >= 180 and inside its card, **sparse:** the thermometer fills >= 0.6 of the card's inner height and the grid fills the body; no picture beside an item thermometer; chips === the gear triple, one correct === the band's gear, >= 56, pictures dirs weather / clothing, no chip text; rows never share a band; every band once; distinct values; not one chip order; no symbol picture on this face; no fenced gear (`mittens umbrella raincoat hot cold`). **Node gate:** key values === `keyValues`; card values ∈ `values[band]`; the correct chip === `gear[band]`.
- **Measured (en d2):** body 766, key 148, cards hot@35 warm@20 cold@-10 hot@30 (rows hot/warm · cold/hot), thermometers 220 in 296 px cards (0.82; long chrome 268 -> 0.92; fi4 263 -> 0.94), chips 64 / icons 56, orders tss / sts / sts / tss, 0 `<text>`, lowest = footer top. Under the 677 pin the cards are 251.5 and the 220 thermometer stays inside (the zero stage padding buys the 3.5 px).
- **Poisons owned:** PR6 (a `label()` numeral injected into an item thermometer, what `numerals:true` emits -> "text inside the thermometer"), PR7 (card 1 rebuilt at value 10 -> "not inside any band (an edge)"), PR8 (card 2 = a copy of card 1 -> "row 1: two cards share the hot band"), PR9 (a `mittens` chip -> "not gear"), PR10 (a 32 px scarf beside an item thermometer -> "the legend on an item prints the answer"), PR11 (the key row removed -> "0 key rows"), PS3 (120 px item thermometers -> "fills 0.45 of the card (< 0.6: sparse)"), PC1 (`bands:['cold','hot']` -> the guard), PT1 (title "Cold or Hot" over three bands -> the title-lists-config rule).
- **PNG:** `out/dev/K-365-weather-d2-en.png` (gate: `K-365-gate-d2-en{,-chrome-long,-chrome-fi4}.png`, poisons `K-365-gate-poison-PR6..PR11/PS3.png`). Read: the key row with an almost empty tube + scarf, a half tube + green T-shirt, a high tube + red sunglasses; four cards, mercury high / middle / empty / high, the three tinted bands with black edge ticks, three white chips per card in varying orders; the 2-line title; nothing clipped, the grid ending on the footer rule; the fi4 render (4-line title) still holds every thermometer inside its card.

### G3-385 : The Water Cycle: Label the Diagram : CODE `layout:'water-cycle'` (F4, **G3**; titled from the water-cycle head)
- **Row:** `{...base.d2, layout:'water-cycle', stages:4, given:0, bank:true, w:675, laneW:270, laneH:52, glyphH:26}` + `extra {gradeBand:'G3'}`.
- **Child:** reads the four stage words in the dashed bank (shuffled: never the cycle order nor its reverse), finds each numbered coral marker on the line diagram (1 evaporation on the arrow off the sea · 2 condensation in the cloud · 3 precipitation among the rain lines · 4 collection on the water; CYCLE order, the ruling) and copies the word onto the writing lane with that number (four `writingRow 270 x 52 glyphH 26` lanes, two per row, each with a 44 px numeral chip).
- **Build:** `waterCycle({w, markers})` in cycle order; bank = `_reorder`; `given` lanes (d1) print their literal in a cream tile; `stages:5` adds `runoff`; a labelled space refuses unless `multiWord`; the diagram sits in a `flex:0 0 auto` row under the bank, the lanes 12 px below: TOP-ANCHORED, the page's slack falls below the lanes.
- **Stamps:** root `data-lcs-layout="water-cycle" data-lcs-stages/given/bank/w/lane-w/lane-h/glyph-h/cycle`; the primitive's `[data-lcs-marker] data-lcs-n`, `[data-lcs-arrow]`, `[data-lcs-part]`; lanes `[data-lcs-label] data-lcs-n data-lcs-anchor (data-lcs-given)`.
- **verify():** cycle stamp === the fixed order; ONE diagram >= max(480, w) wide inside the stage; every arrow group + part present; `stages` markers, each numbered by its anchor's cycle index, numeral === n and >= 16 px, the DISC centre within 2 px of `ANCHORS[a] x (svg width / 640)` (measured on the render, independent of the marker's own attributes), pairs >= 50 px apart; lanes in cycle order, numbered to their markers, EMPTY (no text, no `<text>` on the writing row, row >= 200 x 44), every label box BELOW the diagram and overlapping NO marker, inside the stage; given count === stamp; the bank present with `stages` distinct words above the diagram; no `<img>` on the face; **sparse:** top-anchored and the stage CONTENT (root top to the lowest child) >= 600 (deviation 3: measured, never the root box). **Node gate:** the bank set === `cycleLabels` of the cycle keys, order !== cycle and !== reversed. The gate also re-runs `node qa/verify-water-cycle.js` (PASS 50 assertions, 5 poisons) and asserts the ANCHORS copy inside verify === the primitive's export.
- **Measured (en d2):** body 766, diagram 675 x 422 (deviation 1), marker numerals 17.0 px, lanes 270 x 52 in 330 px rows, stage content 616 (bank 69 + diagram 422 + 12 + lanes 120, the bank margin folded), lowest 771 vs footer 921: ~150 px of air under the lanes at the 2-line en title (99 at the 722 chrome, 54 at 677), OUTSIDE the stage.
- **Poisons owned:** PR13 (markers numbered 2 1 4 3 -> "numbered 2 ≠ 1"), PR14 (the bank in cycle order -> the node gate "bank order = cycle"), PR15 (lane 1 pre-filled -> "lane not empty"), PR16 (the condensation marker moved 20 viewBox units -> "sits 21.1 px off its anchor"), PS4 (the diagram at 480 -> "stage ~494 px < the 600 floor"), PC4 (`stages:6` -> the guard).
- **PNG:** `out/dev/G3-385-weather-d2-en.png` (gate: `G3-385-gate-d2-en{,-chrome-long,-chrome-fi4}.png`, poisons `G3-385-gate-poison-PR13..PR16/PS4.png`). Read: bank evaporation / condensation / collection / precipitation; the full-width diagram, coral sun top-left, the teal-outlined cloud with "2", "1" on the coral evaporation arrow between the vapour lines, "3" among the dashed rain lines on the cream hill, "4" on the tealSoft water, the coral run-off arrow into the sea; four numbered lanes with school lines; air below. The human-eye item the base recorded stands: the land path strokes its bottom / right edge in teal (a thin line along the bottom-right of the hill) while the water's bottom edge is unstroked; it reads as the diagram's frame at 675 wide and is a §2 primitive matter (not changed).

### G1-363 : Read the Weekly Forecast : CODE `layout:'forecast'` (F5, **G1**)
- **Row:** `{...base.d2, layout:'forecast', days:5, asks:3, askBy:'word', symbolPx:88, chipH:44, chipMax:64, distinctStrip:true}` + `extra {gradeBand:'G1'}`.
- **Child:** reads the Monday-Friday strip (a name pill + an 88 px symbol per day; five DISTINCT symbols of the six, so every ask has exactly one day) and answers three printed questions (whole literals from `forecastAsks`, e.g. "Which day has a rainbow?") by ringing one of five day chips printing the SAME literal as the strip header (never `dayAbbr`).
- **Build:** literals before the first draw (an ask missing / not ending in "?" REFUSES); strip = `rng.sample(KEYS, 5)`; asks = 3 of the strip keys re-drawn until the answer days are not in Mon..Fri order; `distinctStrip:false` is refused as a non-face; `askBy:'symbol'` (d1) puts a 64 px badge in place of the text.
- **Stamps:** root `data-lcs-layout="forecast" data-lcs-days/asks/ask-by/symbol-px/chip-h/distinct`; the strip `[data-lcs-strip]` > `[data-lcs-fday] data-lcs-concept` + `[data-lcs-fday-name]`; lanes `[data-lcs-q] data-lcs-n data-lcs-concept data-lcs-ask-by` > `[data-lcs-q-text]` + `[data-lcs-dchips]` > `[data-lcs-dchip] (data-lcs-correct="1")`.
- **verify():** five strip days ascending Mon..Fri, distinct concepts, one picture each (weather / the noun / >= symbolPx), a name pill not clipped and inside its cell; `asks` questions with distinct concepts each in the strip exactly once; the question text present, >= 16 px, one line, not clipped, naming NO strip header (the in-DOM day-name check); five chips whose indices === the strip and whose text === the strip headers; exactly one correct at the strip day carrying the concept; pills >= chipH and >= 16 px, the pill row inside its lane; the answer days not ascending, not all equal; **sparse:** the lanes fill the body and the blank inside a lane (lane - 28 - ask - 8 - pill) <= 90; above the footer. **Node gate:** question === `forecastAsks[c]`; chip labels === `forecastDays` or `dayNames` Mon..Fri; pills within chipH..chipMax; row <= 639.
- **Measured (en d2):** body 811 (1-line title, the widest chrome), strip 152, lanes 205 with 64 px pills and 79 px blank (long chrome: lanes 171, blank 45; fi4: 168 / 42), row 465 <= 639, lowest = footer top. The pills reach their 64 px cap at every chrome measured.
- **Poisons owned:** PR17 (day 1 re-pictured with day 2's symbol -> "strip concepts repeat"), PR18 (every ask "... on Monday?" -> "names the day"), PR19 (chips labelled `dayAbbr` -> "chip 1 prints Mon ≠ Monday"), PS5 (a fixed 44 px pill track -> "99 px blank inside the lane > 90"), PC2 (`distinctStrip:false` -> the guard).
- **PNG:** `out/dev/G1-363-weather-d2-en.png` (gate: `G1-363-gate-d2-en{,-chrome-long,-chrome-fi4}.png`, poisons `G1-363-gate-poison-PR17..PR19/PS5.png`). Read: Monday sun · Tuesday cloud · Wednesday storm · Thursday rain · Friday rainbow; "Which day has a rainbow? / is rainy? / has a storm?" over five white capsule chips each (answers Fri / Thu / Wed: not ascending); nothing clipped, the last lane ending on the footer rule; ~40 px of air above and below each question block inside its bordered lane at the 1-line chrome (bounded by the gate's 90).

## Deviations from §3 (each measured or ruled)
1. **F4 diagram at w 675, not 608.** §3 places `waterCycle({w:608})` (380 high) in a `minmax(380px,1fr)` row; at the en chrome that leaves 230 px of paper under a 380 px diagram (141 at 722). The diagram now takes the page inner width (675 x 422, `w` is the config FLOOR the gate asserts) and the stage is top-anchored so the remaining air (150 / 99 / 54 px at the en / 722 / 677 chromes) falls below the lanes, outside the stage; the marker numerals grow to 17.0 px (>= 16), the closest four-marker pair to 80 px (>= 50). A 1fr diagram row was rejected: the svg keeps its 640:400 ratio, so a taller row only centres a blank band around it (the K-357 shape).
2. **F3 "no digit anywhere in the body" excludes the card ordinals.** §3 asks for `cardGrid 2x2 numbered` AND no digit on the body; the badges print "1".."4" (measured). verify() walks the text nodes outside `.ws-card-badge`; the bank rule (no digit / degree sign in the F3 strings) and the `<text>`-free thermometer rule are unchanged.
3. **The stage floors measure CONTENT, never the root box.** Every face root is a flex child that stretches to the body, so `root.height` is the body height whatever the content (the first PS4 run scored a 480 px diagram "stage 766"). The F4 floor now measures root top to the lowest child bottom (616 >= 600); the fill checks measure the fill unit (the row grid / the card grid / the lanes; the diary root is its own grid).
4. **F5 lanes are grid lanes whose pill track grows from 44 to 64 px.** §3's `minmax(106px,1fr)` lanes with fixed 44 px pills leave 99 px of blank inside each lane at the 1-line en chrome (measured; PS5 is that layout). The pill track grows with the lane (`auto minmax(44px,64px)`, centred) and the blank is bounded at 90 by the gate; a taller cap made "Friday" a balloon, a shorter one failed the 1-line chrome.
5. **F4 instruction reworded:** "... on the line with the same number as its arrow" named an arrow the condensation (in the cloud) and collection (on the water) markers do not sit on; shipped as "... on the line that has the same number as on the diagram." (EN is the panels' SOURCE TO AUDIT.)
6. **PR14 is a node-gate poison, not a verify() poison:** verify() has no literals, so "the bank in cycle order" is caught by the gate's node cross-check (the bank order vs `cycleLabels`), exactly as the base's word-column checks are split.
7. **PR22 is judged under the PINNED 677 body.** The design's "footer lint" assumes a 677 body; the 4-line fi title fixture measures a 700 body on the letter page (the title IS four lines, asserted), so 2 x 340 + 12 = 692 fits it. Pinning the body to 677 (the base's technique) makes the 340 px rows overflow ("under the 677 budget the stage overflows the body"); every face is pinned to 722 AND 677 on every render.
8. **A title that lists options lists the shipped config (nt10-D):** the gate's `titleConfigFindings` parses the LISTED segment (after the last "?" / ":") of K-365's title into band words and requires it to equal d2's `bands` in band order, and requires any day names in K-364's title to be exactly the diary's first and last day for d2's `start`; both are locale-driven (`bandWords`, `dayNames`) so a panel title that lists nothing passes and one that lists the wrong thing fails.
9. **F1 rows are lane cards with locale-sized boxes (coordinator ruling after reading the first render):** §3's bare rows of 44 px boxes in a body-filling grid distributed the blank instead of filling it (~80 px of paper between rows, the right half of every en row empty). Each row is now a `.ws-lane` card (88 px min, 5 px gaps) and the box is the locale's largest in 44..60 that fits its widest bank word (en 60, de 51, fi 46); the sparse rule is the blank INSIDE a lane <= 24. The rejected build is poison PS1b.

## Per-locale refusals / contingencies visible from the bank shape (Phase 4 `hub-expectations.json`)
- **No face is refused in any locale by construction:** every §4 candidate symbol word is a single or hyphenated token of <= 11 letters (fi `sateenkaari` = 11 = the F1 lane, 526 px), day names exist x11, pictures are global, the four G3 stage literals exist in every curriculum. Expected rows stay **6 x 11 = 66**.
- **pt needs data, not a refusal (measured, the gate's day-name probe):** `segunda-feira` = 114.2 px at Baloo 2 700 18 exceeds BOTH the diary name-pill room (112: cell 160 - 20 - 24 - 4) and the strip pill room (99: cell 123 - 20 - 4); `quarta-feira` 99.2 sits on the strip room. The pt panel sets `diaryDays` (7) and `forecastDays` (5) short forms (the design's own `segunda, terça, quarta, quinta, sexta` + `sábado, domingo`); the gate FAILS an authored pt block without them (or a `refuse`), never shrinks the font or widens a cell (4 x 160 + 3 x 11 = 673 and 5 x 123 + 4 x 15 = 675 are the page). The F5 QUESTION-lane pill row of the pt full names (585.3 <= 639) still passes; the STRIP cells are the binding room.
- All other locales' widest day name fits both rooms: en 94 · de 95 (`Donnerstag`) · es 79 · fr 79 · it 81 · nl 88 · sv/da/no 65 · fi 91 (`keskiviikko`).
- F1 bank: de (capitals, `Regenbogen`) wraps the bank to two rows (122 with its margin; the fi §4 words fit one row); the lane grid absorbs it (lanes 88.2 under the 677 pin, the 5 px gap is what buys it). F1 letter boxes per locale: en 60 · de 51 · fr 51 · fi 46 (measured); every candidate <= 11 letters; a panel word of 12+ letters must name `write` in `refuse` (PC5 proves the refusal).
- F4 lanes: 270 px at glyphH 26 hold ~14 handwritten glyphs; `Niederschlag` 12 / `tiivistyminen` 13 fit; a `multiWord:true` label (pt "escoamento superficial"-class) is accepted on the lane but refuses d3 letter-box variants (unshipped).
- F3: no language on the page beyond the chrome (the gear pictures are global); the fi panel's inverse move stays a non-face.

## Open items for the panels
- `OPEN 1` pt base title (unmeasured) · `OPEN 2` fi h1 `Säätilat` vs `Sääsymbolit` · `OPEN 3` cloud singular / plural · `OPEN 4` the stage-4 literal (collection vs runoff / infiltration; whichever the grade-3 book prints) · `OPEN 5` `diaryStart` for en / pt (a `'sun'` start changes K-364's listed days: the title must then name the first and last day, which the gate enforces) · `OPEN 6` a locale publishing F3 at its G1 key (a landing-file swap).
- Every panel OPENS the 9 pictures (the contact sheet at 160 px colour + 56 px greyscale was read for this build: sunny, cloudy, rainy, thunderstorm, snowflake, rainbow, scarf, green T-shirt, red sunglasses are what their names say) and reads one render of every face in its locale; `forecastAsks` x6 with the weather word inflected and NO day name (the gate's rule 6 + verify's in-DOM header check); the F3 title, if it lists the bands, lists all three in band order; the F2 title, if it names days, names the first and last.
- The F4 landing states its placement per locale (nl groep 5-6, no 4. trinn, en no K-3 NGSS anchor); every F4 meta says G3.

## Gate output
```
node scripts/worksheet-gen/qa/verify-b4-weather-symbols.js
faces: the verifyFace tables agree with the global bank + water-cycle.js; 9/9 pictures on disk
faces: rows = bank strings; F3 title lists the three bands in band order; F2 title names the first + last diary day
water-cycle primitive gate: PASS (50 assertions, 5 poisons killed)
thermometer: G3-345 d1/d2/d3 bodies === the frozen pre-edit sha1s (7cc3ace7/42e80762/113c379b)
render d1/d2/d3 en + long chrome: verify 0 lints 0 (base sections unchanged)
day names (Baloo 2 700 18; rooms diary 112 / strip 99): en 94 · de 95 · es 79 · pt 114 · fr 79 · it 81 · nl 88 · sv 65 · da 65 · no 65 · fi 91
  CONTINGENCY (unauthored locales whose calendar names exceed a room; their panels set the short forms): pt: diary segunda-feira 114.2 · strip segunda-feira 114.2
render G1-362 write en: verify 0 lints 0 body 799 lowest 921 vs foot 921 [722:ok 677:ok] rows 6 box 60 (room 555) lanes 117 x 6 blank-in-lane max 23 between 5 widest row 548 bank sun/storm/rainbow/cloud/snow/rain   (chrome long: lanes 103 blank 15 · fi4: lanes 101 blank 14)
render G1-362 write de (synthetic §4 words) chrome long: verify 0 lints 0 body 710 [722:ok 677:ok] box 51 lanes 94 blank-in-lane max 11 widest row 650 (634 of the 639 inner) bank Sonne/Gewitter/Regenbogen/Wolke/Schnee/Regen
render G1-362 write fi (synthetic §4 words) chrome fi4: verify 0 lints 0 body 700 [722:ok 677:ok] box 46 lanes 101 blank-in-lane max 14 widest row 650 (sateenkaari, 11 boxes) bank aurinko/ukkonen/sateenkaari/pilvi/lumi/sade
render K-364 diary en: verify 0 lints 0 body 766 lowest 921 vs foot 921 [722:ok 677:ok] boxes 136 x 309 / 309 rows 377/377 days Monday..Sunday   (long: boxes 281 · fi4: 276)
render K-365 thermometer en: verify 0 lints 0 body 766 lowest 921 vs foot 921 [722:ok 677:ok] key 148 cards hot@35 warm@20 cold@-10 hot@30 therm 220/296 chips tss sts sts tss   (long: 220/268 · fi4: 220/263)
render G3-385 water-cycle en: verify 0 lints 0 body 766 lowest 771 vs foot 921 [722:ok 677:ok] diagram 675 x 422 numeral 17.0 px stage 616 bank evaporation/condensation/collection/precipitation   (long / fi4: same stage 616)
render G1-363 forecast en: verify 0 lints 0 body 811 lowest 921 vs foot 921 [722:ok 677:ok] strip sun,cloud,storm,rain,rainbow h 152 asks rainbow,rain,storm lanes 205/205/205 pills 64/64/64 blank 79/79/79   (long: lanes 171 blank 45 · fi4: 168 / 42)
  fi4 fixture: the title renders on 4 line(s)
sweep: 20 seeds clean, 20 distinct left orders, 20 distinct right orders
sweep faces: write 19 row / 19 bank orders · thermometer 18 band-value sets / 20 chip orders · water-cycle 15 bank orders, markers always in cycle order · forecast 20 strips / 19 ask sets · diary 1 page (invariant)
poisons: P1..P19, PR1, PR2, PR20, PR21, PR12, P-theme, P-d3 (the base 27, all KILLED) · PR3 PR4 PR5 PR6 PR7 PR8 PR9 PR10 PR11 PR13 PR14 PR15 PR16 PR17 PR18 PR19 PR22 · PS1 PS1b PS2 PS3 PS4 PS5 · PC1 PC2 PC3 PC4 PC5 · PT1 PT2 (all KILLED; PT control PASSES)
PASS (922 assertions, 57/57 poisons killed)          --quick: PASS (695 assertions, 57/57 poisons killed, --quick: sweeps skipped)
```
- `node tools/gate-variation-distinct.js --batch=b4 --diffs=2 --family=weather-symbols` -> `[b4:weather-symbols] compared 15 pairs over 5 faces against their bases + pairwise within family` / **`every variation differs from the deck its base publishes and from its siblings`**.
- `node tools/b3-baseline.js --check --quick` -> **`checked build 3532 + enum 255 in 20s: 0 drifted (0 expected), 0 missing` / `PASS`** (the base path + the thermometer options unchanged; the five face ids are the accepted Phase-2 drift, not captured).
- `node i18n/build-en.js` -> `633 types (title lint clean)` (the sibling families' faces landed meanwhile); `node qa/verify-ruling-starters.js --quick` -> `PASS: 101 starters measured over 31 renders, 0 failure(s)` (no face prints a starter on a writing row; F4's lanes are empty).
- `node render/one.js <id> weather 2 en` for G1-362 · K-364 · K-365 · G3-385 · G1-363 -> lints clean / verify clean each; PNGs `scripts/worksheet-gen/out/dev/<id>-weather-d2-en.png`.

## Traps paid for in this build (the gate accused correct pages first, three times)
- **An object spread drops a wrapper's prototype:** the emitter's `{...base}` over a `withFixedTheme` wrapper carried no `_buildWith`; every face threw. Hoisted at the base's export (the fixed-theme family's cost; K-354 needed it too).
- **The "stage" of a flex child is the body:** `rr.height` was the body height for every face, so the first stage floor could not fail (PS4 scored "stage 766" on a 480 px diagram). Measure the content span / the fill unit.
- **A digit ban reads the card ordinals:** `cardGrid numbered` prints "1".."4"; the ban is the temperature numeral, scoped outside `.ws-card-badge`.
- **A title parser reads the question as an option:** "How Warm Is It?" contains "warm"; parse the listed segment after the last "?" / ":"; and collect day names in TITLE order, not calendar order ("Monday to Sunday" scored as Sunday, Monday).
- **A design poison assumes a chrome that the fixture does not produce:** PR22's footer overflow needs a 677 body; the real 4-line title gives 700 on the letter page. Pin the body; judge the pinned assertion.
- Heredocs ate `\p{L}` and `\\` twice; every script went to a file and ran by path.
