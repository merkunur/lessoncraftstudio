# K-357 `recycling` : PEDAGOGY + CONTENT (nt10-D, 2026-09-21)

Read: `_ROLE-PEDAGOGY.md`, `_STUDIO-BRIEF.md`, `_SUBSTRATE.md`, `_PANEL-FINDINGS.md` (the lock + the K-357 entry + Facts 1/7/8/9), `K-354-human-body.md` (density target), `types/_shared/science-category-sort.js` (the K-214 / K-235 factory: `build()` + `verify()`), `types/k/K-214-natural-manmade.js` + `data/science/natural-vs-manmade.json`, `wants-vs-needs.json`, `living-vs-nonliving.json`, `types/_shared/sort-to-bins.js` (K-042 / G1-160), `types/_shared/odd-one-out.js` (K-043 / K-044 / K-039), `types/g1/G1-244-write-the-word.js`, `types/k/K-241-color-by-code.js`, `templates/components-b2.js` (`colorLegend`, `wordBank`, `pillChoice`, `letterBoxes`, `SWATCH`), `templates/components-b3/read-and-do.js` (`drawBox`, `drawCards`), `primitives/trace-path.js writingRow`, `page/page.css` (`.ws-bin`, `.ws-bin-label`, `.ws-card`, `.ws-pill`, `.ws-scene-banner`), `lib/b2-common.js` (`entriesFor`, `B2_EXCLUDE`), `lib/b3-picture-index.js`, `lib/b3-common.js bank()`, `data/color-words.js`, `tools/validate-b3-draft.js WORKSHEET_WORD`, `scripts/seo-landing/gen-b3-landings.js LEVEL_KEYS`, `frontend/config/topics-taxonomy.json`, `frontend/lib/seo/strand-names.ts`. Every number below was measured 2026-09-21 with read-only node (scratch `K-357-pool.js`, `K-357-measure.js`); (m) = measured, *est.* = the engineer measures in the real render with the shell woff2 from `file://`. **69 candidate pictures were OPENED at `cache/themes-512/<theme>/<noun>@3x.webp`; 38 are kept, 31 excluded, each with its reason in B/D.** No em-dashes.

**Boundary (load-bearing).** The science-sort family (`science-category-sort.js`: a shuffled strip of pictures over WORD-labelled `.sci-bin`s, draw a line) already owns natural / man-made (K-214: 12 items incl. `vehicles/car`, `around the house/cup`, `classroom/book`), wants / needs (K-212: `At the Supermarket/water`, `bread`, `fruits/apple`, `toys/ball`), living (K-201: `around the house/cup`, `classroom/book`, `kitchen tools/spoon`), K-207 / K-211 / K-208; `sorting-categories` (`sort-to-bins.js`, K-042 / G1-160) sorts DUPLICATE icons into ICON-labelled bins; K-235 sorts vocabulary categories; K-043 odd-one-out is 3 identical + 1 different noun of one theme; G1-244 writes a picture's NAME; K-241 / K-265..269 colour shapes by a code; G1-342 draws from a sentence. **This family owns exactly one thing nobody has: the MATERIAL of a finished product as the sorting key, under the LOCALE's own bin set.** The bins are drawn (NEW `primitives/waste-bin.js`: a lidded bin with a colour token and a word pill; `_SUBSTRATE.md`: "No primitive exists for a waste bin with a colour"), the bin set is per-locale data (2 to 5 bins, es-MX 2, no glass+metal one bin `[NSR]`, da 5 of the national 10, fi never titled "Lajittelu"), and the material of every item is fixed ONCE, locale-neutral, by opening the picture (`data/b4/recycling.json`). It never sorts by recyclable-yes / no alone (that is es-MX's 2-bin instance, valid there), never by natural / man-made, wants / needs or living, and never prints a noun: the only words on any face are bin words, material words and colour words, all panel literals. No face uses the `science-sort` factory or its heads (fr "Tri en sciences", it "Classifichiamo le scienze", sv "Sortera i naturen", fi "Luonnontieteen lajittelu" are neighbours, m); the shared item set with K-212 / K-201 (`water`, `bread`, `apple`, `spoon`) is allowed because the QUESTION differs (where does it go, not is it alive / a need), and the copy says so.

## A. Identity

`apps.recycling` ABSENT, `axes['exercise-type'].recycling` ABSENT (m; register with the `register-b4-taxonomy.js` shape, `default_subject:'science'`, `default_age_range:'5-7'`; `science` disc today = science-sort, science-sequence, science-match, seasons, animal-fact-file, m). Every candidate head slug below grepped against every `axes.*` slug AND name in its locale: **all free; the only collision is fi `lajittelu` = `exercise-type.sorting-categories.slug.fi` (name "Lajittelu"), which the lock already bans as a title** (m). No theme slug is involved (no `axes.theme` key names waste, m), so no compound-head rule applies; the fi ban is the one head constraint. `strand-names.ts` has 0 Science rows (m): the strand column below IS the row the build adds (README open item 1; the literals are identical to K-354 / K-355 / K-356 by construction).

| loc | genre head (the panels, verbatim) | ASCII slug | levels K / G1 / G2 (`LEVEL_KEYS`, m) | national strand literal (framework NAME) + curriculum anchor the landing may cite | CCSS (en only, honest) |
|---|---|---|---|---|---|
| en | **Recycling Sort** (Tier B+; never "Recycling" alone = adult intent; WINNABLE "recycling sorting worksheet kindergarten", "which bin worksheet") | `recycling-sort` | `kindergarten` / `grade-1` / `grade-2` | Science (readiness); NGSS K-ESS3-3 ("communicate solutions that will reduce the impact of humans on the land, water, air") named in prose only | none: readiness class on every face; NO `educationalAlignment` |
| de | **Mülltrennung** (Tier A; 40+ of 67; "Müll richtig trennen" in the meta) | `muelltrennung` | `vorschule` / `1-klasse` / `2-klasse` | Sachunterricht (Lehrplan 1./2. Klasse "Müll, Mülltrennung"; 5 fractions Papier / Gelb (Verpackung) / Bio / Rest / Glas) | n/a |
| es (MX) | **Separación de basura** (Tier A-; "reciclaje" in the meta only; "basura" is the child word, "residuos" adult; bins "botes") | `separacion-de-basura` | `preescolar` / `primer-grado` / `segundo-grado` | Conocimiento del Medio (SEP / NEM 1º-2º "separación de residuos orgánicos e inorgánicos"; the landing SAYS the 2-bin model is the SEP one and that CDMX runs 4) | n/a |
| pt (BR) | **Coleta seletiva** (Tier A; "reciclagem" only paired with "coleta seletiva" (detran noise); "lixeira" never "caixote") | `coleta-seletiva` | `educacao-infantil` / `1o-ano` / `2o-ano` | Ciências (BNCC EF01CI01: "comparar características de diferentes materiais presentes em objetos de uso cotidiano, discutindo sua origem, os modos como são descartados e como podem ser usados de forma mais consciente"; the panel confirms the verbatim) | n/a |
| fr | **Le tri des déchets** (Tier A, the largest fr head after body; "tri sélectif" in the meta; "poubelle" is the K word) | `le-tri-des-dechets` | `maternelle` / `cp` / `ce1` | Questionner le monde (cycle 2 "les matières: trier les déchets"; GS "Explorer le monde" named in landing prose only, the K-354 OPEN 1 ruling applies) | n/a |
| it | **Raccolta differenziata** (Tier A; "riciclo" in the meta; NEVER "riciclaggio" = money laundering; "differenziata" agrees with "raccolta") | `raccolta-differenziata` | `infanzia` / `classe-prima` / `classe-seconda` | Scienze (Indicazioni nazionali classe prima / seconda "i materiali e la raccolta differenziata") | n/a |
| nl | **Afval scheiden** (Tier B; "afval sorteren" BE-leaning, meta only; "de bak, het glas, het papier, de plastic fles") | `afval-scheiden` | `kleuters` / `groep-3` / `groep-4` | Oriëntatie op jezelf en de wereld (SLO kerndoel 39 "met zorg omgaan met het milieu") | n/a |
| sv | **Källsortering** (Tier A; "sopsortering", "återvinning" in the meta; NEVER a title starting "Sortera": `sorting-categories` "Sortera i kategorier", `science-sort` "Sortera i naturen", m) | `kallsortering` | `forskola` / `ak-1` / `ak-2` | Naturorienterande ämnen (Lgr22 NO åk 1-3, kemi: "vilka material olika vardagliga föremål är tillverkade av och hur de kan källsorteras"; the panel confirms the verbatim) | n/a |
| da | **Affaldssortering** (Tier B+; "sorter affald" in the meta; "genbrug" = thrift shops, keep out of titles) | `affaldssortering` | `boernehaveklasse` / `1-klasse` / `2-klasse` | Natur/teknologi (Fælles Mål 1.-2. kl; the panel names the kompetenceområde) | n/a |
| no | **Kildesortering** (Tier A-; "sortere søppel" + "hva skal hvor" in the meta) | `kildesortering` | `1-trinn` / `2-trinn` / `3-trinn` | Naturfag (LK20 etter 2. trinn; the panel confirms the kompetansemål wording) `[NSR-FLAG][no]` bin set | n/a |
| fi | **Kierrätys** (Tier A-; "Kierrätys ja lajittelu" / "jätteiden lajittelu" in the meta; NEVER bare "Lajittelu" = `sorting-categories.slug.fi`, m) | `kierratys` | `esikoulu` / `1-luokka` / `2-luokka` | Ympäristöoppi (OPS 2014 vuosiluokat 1-2, S6 kestävä elämäntapa: "jätteiden lajittelu") | n/a |

**Theme axis: THEMELESS.** The items are fixed `(theme, noun)` pairs spanning 12 themes (classroom, post office, At the Supermarket, kitchen tools, breakfast, beach, around the house, tools, fruits, vegetables, spring; m); every kept key has a colour picture and a vocab singular in all 11 locales and none is `B2_EXCLUDE`d (m, `hasPicture` + `entriesFor` ×11 = 0 missing). `fileUri(theme, noun)` reads the OPENED picture (never `pictureFor`, which may pick another theme's picture of the same key: `apple` has 5, `orange` 5, `water` 3, m). Landings carry `coordinate.theme:''`. **No `unitAxis`** (the bin set is locale DATA, not a fan; every locale ships one bin set).

**The one rule that locks the type.** *The material is a fact of the PICTURE, stored once; the bin is a fact of the LOCALE, stored per locale; the code joins them and never decides:* `bin(item, loc) = loc.route[item.id] ?? first bin in loc.bins whose materials include item.material and (item.packaging || !bin.packagingOnly) ?? EXCLUDED-for-this-locale`. Every printed word is a whole panel literal (bin `label`, `materialWords`, `colorWords`); no noun of the item bank is ever printed on any face; ambiguous material = excluded from the bank, never signed.

## B. The six faces

Common: `templates/components-b4/recycling.js` (strip, bins row, which-bin grid, material cards, odd rows, colour bins, open cards) + NEW `primitives/waste-bin.js` (`wasteBin({w, h, color|null, label|null, fill:'token'|'white'}) -> {svg, w, h, anchor}`; a lidded bin, teal 3 px stroke, body filled with the locale colour token or white, `data-lcs-bin=<key> data-lcs-color=<token|none>`; the design agent fixes the geometry, min body 56 x 50 at F1, 120 x 150 on the base and F4). Every face is a CODE face on ONE additive `layout` knob (`d.layout` undefined = base path, byte-identical; a face stamps `data-lcs-layout` only when declared). Guards key on the CONFIG (`d.items`, `d.cols`, `d.rows`, `d.trio`, `d.legend`), never the level index. Same seed = same items and positions in all 11 locales, except that a locale whose bin set EXCLUDES an item re-draws that slot from the same material pool (deterministic, `rng` order preserved). Item picture floor: K faces 56 (shipped 78 / 72), G1 44 (shipped 80), G2 36 (shipped 76).

### Base: Sort into the Bins (K, `K-357`, layout undefined)
**Move:** CLASSIFY a finished product by what it is made of and draw a line to the locale's bin for that material (the sci-sort move with a material key and drawn coloured bins).
**Child:** "Each thing is empty or finished. Draw a line from it to the bin it goes in." (77 chars; apparatus: the pictures, the bins.)
**Params:** d1 `{items:5, perBinMin:1, itemPx:96, binW:120, binH:150}` (one per bin) · **d2 (ships)** `{items:8, perBinMin:1, perBinMax:2, itemPx:78, binW:120, binH:150, labelPx:17}` · d3 `{items:10, perBinMin:2, perBinMax:2, itemPx:66}`. Composer: bins = `loc.bins` (2-5); each bin first takes 1 item from its resolved pool (`rng.pick`), then the remaining `items - nBins` slots are filled round-robin over bins with pool left, never above `perBinMax` (es-MX 2 bins: `perBinMax` is raised to `ceil(items / nBins)` = 4, stated in copy as "two bins"); strip = `rng.shuffle`, re-drawn while any bin's items are all adjacent (position leak) or two items share a `family`. Layout = the sci-sort shape: strip (8 x 78 + gaps <= 675, one row) over a row of N `wasteBin`s (N x 120 + (N-1) x 18 = 672 at N = 5, m arithmetic) with the word pill above each (`.sci-bin-label` 17 px Baloo, `white-space:nowrap`: **a label wider than binW + 10 overflows its neighbour; the panel's `label` is capped at 12 chars and the design agent measures every pill** *est.*). Stack *est.*: strip ~110 + bins 26 + 150 + gaps = ~330 <= 722 (the factory ships today at 185 px bins).
**verify():** stamps `data-lcs-item=<id> data-lcs-bin=<binKey> data-lcs-material` per strip item, `data-lcs-bin=<key>` per bin, `data-lcs-bins` (ordered keys) on the root. Re-derived (node cross-check against the bank + the locale block): every item's bin === `bin(item, loc)`; every stamped bin is rendered; every bin receives >= `perBinMin` and <= `perBinMax`; nBins === `loc.bins.length` (2..5); no two items share `family`; no bin's items are all adjacent in the strip; no text anywhere but the pills; every pill text === `loc.bins[k].label`; every `img` complete, `src` === `fileUri(theme, noun)` of the bank item; no `data-lcs-answer`.
**Refusals:** none by face. Per-locale POOL after the default maps (design defaults in D; m from the 38 kept items): de Papier 6 / Verpackung 5 / Bio 13 / Glas 7 / Rest 4 · nl papier 6 / GFT 13 / PMD 5 / glas 7 / rest 4 · fr emballages+papiers 11 / verre 7 / biodéchets 13 / ordures 4 · it carta 6 / plastica+metallo 5 / vetro 7 / umido 13 / secco 4 · es orgánico 13 / inorgánico 25 · pt papel 6 / plástico 8 / vidro 7 / metal 4 / orgânico 13 · **sv papper 6 / plast 4 / metall 1 / glas 7 / matavfall 13** (packaging-only, no restavfall among the 5: the metal bin gets exactly one item on every page, allowed at `perBinMin:1`) · da madaffald 13 / papir 4 (+2 if pap merges) / plast 8 / glas 7 / metal 4 · no papir 6 / plast 4 / glass og metall 8 / matavfall 13 / restavfall 4 · fi bio 13 / paperi+kartonki 6 / lasi 7 / metalli 4 / muovi 4 · en paper 6 / plastic 8 / glass 7 / metal 4 / compost 13. Smallest pool sv metall = 1 >= `perBinMin`; every other bin >= 4.
**Query face:** the bare head: "recycling sort" · de "Mülltrennung Arbeitsblatt" · fr "tri des déchets fiche" · sv "källsortering arbetsblad".

### F1: Which Bin? (K, K-3xx TBD, CODE `layout:'which'`)
**Move:** DECIDE for ONE thing at a time: a grid, one row per item, one column per bin; the child circles the bin cell in the item's row (the panel's "circle 1 of N bin icons", laid out as a table so the bin WORD is printed once per column and every cell is a mono-safe target). es-MX N = 2 stays a task.
**Child:** "Look at each thing. Circle the bin it goes in when it is empty or finished." (74)
**Params:** d1 `{layout:'which', rows:4, pic:96, cell:64}` · **d2** `{layout:'which', rows:6, pic:72, cell:56, headPx:16}` · d3 `{layout:'which', rows:8, pic:56, cell:48}` (d3 cell 48 < K floor 56: NOT a K page; d3 is unshipped and no copy describes it). Grid: item column 100 + N x (cell + 12) <= 675 at N = 5 (100 + 340 = 440, m arithmetic; centred); header = N word pills (Baloo 16, `label`); each cell = `wasteBin({w:56, h:50, color: loc.bins[k].color, label:null})` outline. Rows: 6 items from >= 4 distinct bins, no two same `family`, no two consecutive rows with the same answer column.
**verify():** stamps `data-lcs-row=<id> data-lcs-bin=<key>` per row, `data-lcs-cell=<binKey>` per cell, `data-lcs-cols` on the root. Re-derived: exactly one cell per row whose key === `bin(item, loc)`; columns === `loc.bins` order; every row has N cells; no pre-marked cell; no run of 2 equal answers; pictures complete; cell >= `cell` px.
**Refusals:** none (needs only the bin set). es "botes", da "hvilken skraldespand", no "hvilken dunk", fi "mihin astiaan" (the panels' heads).
**Query face:** "which bin worksheet" (en verbatim) · de "Welche Tonne?" · no "hva skal hvor" (verbatim demand) · fi "mihin astiaan".

### F2: What Is It Made Of? (G1, G1-3xx TBD, CODE `layout:'write'`)
**Move:** NAME the material: 8 picture cards, a bank of the locale's material words on top, the child writes the MATERIAL (not the name) on the card's writing row. Distinct from G1-244 (writes the picture's name) and from the base (no line, a written word). es re-target (panel-confirmed): the two words are `reciclable` / `orgánico`.
**Child:** "What is each thing made of? Write the word from the bank on the line." (68)
**Params:** d1 `{layout:'write', cards:6, cols:2, rows:3, pic:104, rulingW:190, glyphH:36, starter:true}` · **d2** `{layout:'write', cards:8, cols:2, rows:4, pic:80, rulingW:214, glyphH:30, starter:false, bankPx:18}` · d3 `{layout:'write', cards:8, cols:2, rows:4, pic:80, rulingW:214, glyphH:30, bank:false}` (no bank: retrieval). Pool = items whose `material` has an entry in `loc.materialWords` (default: paper, cardboard, glass, plastic, metal; organic only if the panel authors a single-token word: de "Bioabfall" ok, en "food scraps" is two words, excluded); cards use each bank word >= 1 and <= 3 times; card order != bank order and != its reverse (the G1-244 idiom); bank = the DISTINCT words of the locale (es 2, most 4-5: cardboard may share the paper word). Ruling = `writingRow({w:214, h:58, glyphH:30, xHeight:true})` (G1 floor 26); d1 starter = the first letter on the row, sized from `primitives/font-metrics.json` (`verify-ruling-starters.js` discovers it by `build()`).
**verify():** stamps `data-lcs-material=<m> data-lcs-word=<literal>` per card (the answer lives in the stamp, never as text), `data-lcs-bank-word` per bank chip. Re-derived: card word === `loc.materialWords[item.material]`; the bank set === the set of card words; no card prints its word (no text node inside a card except the badge); bank order != card order; every word matches `^\p{L}[\p{L}'’-]*$` and `[...word].length <= 12` (rulingW 214 at glyphH 30 *est.*; de "Kunststoff" 10 fits, "Plastik" 7 preferred by the panel); pictures >= 44.
**Refusals:** none measured: the packaging-material pool is 6 + 2 + 7 + 8 + 4 = 27 items over >= 20 families in every locale (organic never needed). es bank 2 words (reciclable / orgánico) with pool 38: buildable, copy says "two words".
**Query face:** "what is it made of worksheet" · de "Woraus ist es? Material" · fr "en quelle matière" · sv "vilket material" · it "di che materiale è fatto".

### F3: Odd One Out by Material (G2, G2-3xx TBD, CODE `layout:'odd'`)
**Move:** COMPARE four DIFFERENT objects; three share a material, one does not; circle it. Distinct from K-043 (three identical pictures + one other noun, one theme) because every picture differs and the shared property is invisible: the child must reason "glass, glass, glass, plastic".
**Child:** "In each row, three things are made of the same material. Circle the one that is different." (89)
**Params:** d1 `{layout:'odd', rows:3, items:4, pic:88}` · **d2** `{layout:'odd', rows:4, items:4, pic:76}` (16 pictures = the G2 8-16 band, floor 36) · d3 `{layout:'odd', rows:5, items:4, pic:66, organicOdd:false}` (the odd one is never organic: the hard version). Material classes for the trio: paper+cardboard (6 items, 5 families), glass (7 / 6), plastic (8 / 8), metal (4 / 4), organic (13 / 13): every class >= 3 families (m). Rows: trio class distinct across the 4 rows where possible (5 classes for 4 rows), odd class != trio class, odd index differs across rows, no `family` repeat on the page. Locale-neutral: this face never reads `loc.bins` (material, not bin), so a sv "metal is packaging-only" rule does not touch it and a `nut` + `bolt` + `saucepan` trio is legal everywhere.
**verify():** stamps `data-lcs-material` per img + `data-lcs-odd="1"` on the odd one + `data-lcs-items` per row. Re-derived: exactly 3 equal materials + 1 different per row; 4 distinct nouns per row; odd indices not all equal; all pictures the same rendered size (the K-043 leak rule); no text in the rows.
**Refusals:** none (no locale data). fi title uses the standard "mikä ei kuulu joukkoon" under the "Kierrätys:" head (K-043 fi title is K-band: unique-in-band holds; the gate measures the pair).
**Query face:** "odd one out materials" · de "Was passt nicht? Materialien Klasse 2" · fr "l'intrus matières" · it "l'intruso materiali classe seconda".

### F4: Colour the Bins (K, K-3xx TBD, CODE `layout:'color'`)
**Move:** READ a bin word on a white bin, find it in the legend (colour chip + colour word + bin word) and colour the bin in the LOCALE's bin colour; each bin holds two correctly sorted example pictures, so the finished page is the class's own bin sign ("cartelli", "cores das lixeiras"). Genuine only because bin colours are locale DATA; grey (de Rest / nl rest / fr ordures / es inorgánico) = the `inkSoft` token (no grey in `codeColors`), the colour word is a panel literal ("grau" / "grijs" / "gris"; `COLOR_WORDS` has no grey, m).
**Child:** "Read the name on each bin. Colour it in the colour the legend shows." (67)
**Params:** d1 `{layout:'color', bins:'locale', examples:1, binW:150, binH:190}` · **d2** `{layout:'color', bins:'locale', examples:2, binW:120, binH:180, pic:56, legendPx:17}` · d3 `{layout:'color', bins:'locale', examples:3, pic:48}` (48 < 56: unshipped). Legend = `colorLegend`-shaped banner but two literals per entry (colour word + bin word; never one composed string). Bins white (`fill:'white'`), label pill above, 2 example pictures inside from the bin's resolved pool (distinct families).
**verify():** stamps `data-lcs-bin=<key> data-lcs-color=<token>` per bin (the token is the legend's, never painted on the bin: bin body `fill` === white), `data-lcs-legend=<key> data-lcs-color` per entry. Re-derived: legend keys === bin keys; colours distinct within the page and each ∈ `codeColors` ∪ {inkSoft}; every example's `bin(item, loc)` === its bin; N === `loc.bins.length`; pictures >= 56.
**Refusals (the contingent face):** requires `loc.colorConvention ∈ {'national','classroom'}` + a colour and a colour word for every bin. National conventions on record: de (Papier blau / Verpackung gelb / Bio braun / Glas grün / Rest grau), pt (CONAMA azul / vermelho / verde / amarelo / marrom), es-MX (SEP verde / gris), fr (jaune / verte / marron / grise), fi (HSY lid colours: the panel confirms), nl (papier blauw / GFT groen / PMD oranje / rest grijs; GLAS collides with GFT on green: the nl panel picks, OPEN 5). No national convention: **sv** ("symboler": refusal expected unless the panel adopts a classroom key), **no** (kommune bags: OPEN), **da** (the national pictograms carry colours: the panel confirms), **en** (classroom key blue / green / grey is common: OPEN), **it** ("colours vary by comune": the panel fixes a canonical set or refuses). A `classroom` convention is allowed only with the honest landing sentence "the colours on this page are our class key". Estimated refusals 1-2 (sv, maybe no): below the 4-locale replacement threshold; recorded per locale in `refuse:['color']`.
**Query face:** "recycling bins colouring" · de "Mülltonnen ausmalen" · pt "lixeiras coleta seletiva para imprimir e colorir" (verbatim) · it "cartelli raccolta differenziata da stampare" (verbatim) · sv / da "symboler".

### F5: Three Things I Recycle (G1, G1-3xx TBD, CODE `layout:'open'`)
**Move:** OWN it: draw three things you recycle at home and write what each is (open; no verify; the pt / es / fr "je trie" reflection page).
**Child:** "Draw three things you recycle at home. Write what each one is." (62)
**Params:** d1 `{layout:'open', cards:2, drawW:300, drawH:260}` · **d2** `{layout:'open', cards:3, cols:3, rows:1, drawW:195, drawH:300, rulingW:195, glyphH:30}` · d3 `{layout:'open', cards:3, caption:'bin'}` (a second ruling "it goes in the ... bin"). Card = `drawBox({w:195, h:300})` + `writingRow({w:195, h:58, glyphH:30})`; three cards in `cardGrid({cols:3, rows:1})` (3 x 219 + 2 x 14 = 685 > 675: the design agent sets card padding 8 or drawW 185 *est.*). Numbers 1-3 as `.ws-card-badge` only.
**verify():** open-ended: no verify(); lints only (one empty `[data-lcs-drawbox]` + one empty ruling per card; no text inside).
**Refusals:** none. sv title never starts with "Sortera"; the panel may prefer "Källsortering: tre saker hemma" (OPEN 8).
**Query face:** "things I recycle" · de "Das trenne ich" · fr "je trie mes déchets" · es "tres cosas que reciclo".

**Rejected non-moves.** A theme swap (the type is themeless) · "recyclable yes / no" as a face (that is es-MX's 2-bin base, valid only there) · a da "10 fraktioner" face (the panel REFUSES it) · a "sort by natural / man-made" or "living" face (K-214 / K-201) · a picture-NAME writing face (G1-244) · relabelling d1 (one item per bin) or d3 as a face (same move) · a colour-by-code where the code is a number (K-266 family) · a "what happens to it" sequence face (no art; the library draws products, not processes).

## C. Native rebuild x11

Frames NEVER inflect: this type has no cloze frame and prints no noun, so it needs no `objForms` (m: `bank('instructions', loc).objForms` holds 0-3 of the 38 nouns outside fi; nothing is read). Per locale the panel authors: `strings` (6 titles + 6 instructions = 12), `bins` (2-5 x {key, label <= 12 chars, longLabel, color token | null, colorWord literal | null, materials[], packagingOnly}), `route` (per-item overrides with a reason), `excludeItems` (with a reason), `materialWords` (2-6), `colorConvention`, `refuse[]`, `enAudit` (the panel audits the EN source per the §23.6 rule: every EN instruction above is a SOURCE to audit, not a target).

| loc | literals the panel authors (counts) | bin set (design default; material first, colour second) | refusal / re-target | traps |
|---|---|---|---|---|
| en (US) | 12 strings · 5 bins x 4 literals = 20 · 5 materialWords (paper, cardboard, glass, plastic, metal) · 5 colorWords = ~42 | paper (+cardboard) · plastic · glass · metal · compost (food scraps); non-packaging plastic / metal allowed (single-stream); no trash bin (toothbrush etc. go to plastic in en) | F4 `colorConvention:'classroom'` (OPEN 6) or refuse | title never "Recycling" alone; "color" US spelling; "printable" belongs to the meta; NGSS in prose only |
| de | 12 · 5 x 4 · 4-5 materialWords (Papier, Pappe, Glas, Plastik (not Kunststoff), Metall; Bioabfall optional) · 5 colorWords (blau gelb braun grün grau) = ~46 | Papier (paper + cardboard) · Verpackung (plastic pk + metal pk, "Gelbe Tonne" in longLabel) · Bio · Glas · Rest (non-packaging plastic: toothbrush, comb, spoon, bucket); `saucepan`/`bolt`/`nut` EXCLUDED (Wertstoffhof, no bin on the page) | none | nouns keep the capital but are never printed; bin word "Glas" = the vocab noun of `jar` (fine, it is the material); "Wohin kommt es, wenn es leer ist?" is the instruction frame; `nut` vocab "Mutter" = mother (prose trap only) |
| es (MX) | 12 · 2 x 4 · 2 materialWords (reciclable, orgánico) · 2 colorWords (verde, gris) = ~26 | Orgánico (organic) · Inorgánico (everything else; grey = `inkSoft`) | base / F1 / F4 at N = 2 stay tasks; F2 re-target confirmed | "basura" child word, "residuos" adult, bins "botes"; the landing states SEP 2 bins vs CDMX 4; `pepper` vocab "Pimiento" (MX would say "chile"? the panel checks the picture is a bell pepper: it is, opened) |
| pt (BR) | 12 · 5 x 4 · 5 materialWords (papel, papelão, vidro, plástico, metal) · 5 colorWords (azul, vermelho, verde, amarelo, marrom) = ~47 | Papel (azul) · Plástico (vermelho; pk + non-pk) · Vidro (verde) · Metal (amarelo; can, saucepan, bolt, nut) · Orgânico (marrom); CONAMA 275 | none | "lixeira" never "caixote"; "reciclagem" only beside "coleta seletiva"; EF01CI01 in prose; "atividade" never in a title |
| fr | 12 · 4 x 4 · 5 materialWords (papier, carton, verre, plastique, métal) · 4 colorWords (jaune, vert, marron, gris) = ~41 | Emballages et papiers (jaune: paper, cardboard, plastic pk, metal pk) · Verre (verte) · Biodéchets (marron; 2024 tri à la source) · Ordures (grise: non-packaging plastic); the panel may drop Biodéchets to the national 3 (OPEN 4: then organic routes to Ordures, which a K teacher will contest) | none | `science-sort` fr = "Tri en sciences": no instruction may read "trie en sciences"; "poubelle jaune" = paper AND packaging; NBSP before `?` / `:`; Belgian PMC bleu is out of scope (fr = France) |
| it | 12 · 5 x 4 · 5 materialWords (carta, cartone, vetro, plastica, metallo) · 5 colorWords = ~47 | Carta (paper + cardboard) · Plastica e metallo (label "Plastica" <= 12, longLabel with metallo; pk only) · Vetro · Umido (organic) · Secco (label; longLabel "Indifferenziato", 15 chars, never on a pill) | F4 only with a canonical colour set the panel fixes and states (OPEN 6) | never "riciclaggio"; "differenziata" agrees with "raccolta"; "scheda" never in a title; colours vary by comune: the landing says so |
| nl | 12 · 5 x 4 · 5 materialWords (papier, karton, glas, plastic, metaal) · 5 colorWords = ~47 | Papier (blauw) · GFT (groen; organic) · PMD (oranje; plastic pk + metal pk) · Glas (colour OPEN 5: glasbak is not green in the Milieu Centraal key) · Rest (grijs; non-packaging plastic) | none | "de bak" / "het glas" / "het papier" in prose; IJ never arises in the bank words; engine appends "werkblad" |
| sv | 12 · 5 x 4 · 5 materialWords (papper, kartong, glas, plast, metall) · colorWords only if a classroom key is adopted = ~38-43 | Papper (pappersförpackningar + tidningar) · Plast (pk only) · Metall (pk only: `can` alone) · Glas · Matavfall; non-packaging plastic + metal EXCLUDED (no restavfall among the 5) | F4 expected REFUSED (`colorConvention:null`, "symboler") unless the panel adopts a classroom key; recorded either way | title the compound noun, never "Sortera …"; "Burk" is the vocab word of BOTH `jar` and `can` (prose trap; nothing printed); definite forms only in prose |
| da | 12 · 5 x 4 · 5 materialWords · 5 colorWords (if the pictogram colours are adopted) = ~47 | Madaffald · Papir (+ Pap merged, or `cardboard` EXCLUDED: OPEN 7) · Plast (all plastic per the national fraction) · Glas · Metal (small metal allowed); 5 of the national 10 | "10 fraktioner" face REFUSED by the panel (not designed) | "genbrug" out of titles; "hvilken skraldespand" is the F1 head; engine appends "opgaver" |
| no | 12 · 5 x 4 · 5 materialWords (papir, papp, glass, plast, metall) · colorWords per kommune key (OPEN) = ~43-47 | Papir (papp, papir og kartong) · Plast (emballasje) · Glass og metall (label "Glass/metall" 12 chars; pk only) `[NSR-FLAG][no]` · Matavfall · Restavfall (non-packaging plastic; small metal OPEN) | F4 OPEN (bag colours differ by kommune) | "hva skal hvor" in the meta; "dunk" is the F1 word; bokmål only |
| fi | 12 · 5 x 4 · 5 materialWords (paperi, pahvi, lasi, muovi, metalli) · 5 colorWords (HSY lid colours, the panel confirms) = ~47 | Bio · Paperi (+ Kartonki merged into one pill "Paperi", longLabel "Paperi ja kartonki"; OPEN 7) · Lasi · Metalli (small metal allowed per HSY) · Muovi (pk only); Sekajäte dropped, so non-packaging plastic EXCLUDED | none | title NEVER "Lajittelu" (validator regex); instructions in the illative ("Lajittele jätteet oikeaan astiaan.", "Ympyröi astia, johon roska kuuluu."); the 4-line fi title = the 677 budget: every stack above survives it (base ~330, F1 ~500, F2 4 x 120 + bank 69 = 549, F3 4 x 110 = 440, F4 ~330, F5 ~400 *est.*) |

## D. Data + gates

Bank = `data/b4/recycling.json` (locale-neutral ITEMS, hand-authored, `picOpened:true` per member) + `data/b4/recycling-locales/<loc>.json` (GENERATED from `i18n/.draft-b4-<loc>.json` by the `apply-b4-locale.js` clone; read via `lib/b4-common.js bank('recycling', loc)`, README open item 3). `data/` is gitignored (`git add -f`).

```
RECYCLING (data/b4/recycling.json, GLOBAL; 38 items, every picture OPENED 2026-09-21):
  materials: ['paper','cardboard','glass','plastic','metal','organic']
  items: [ {id, theme, noun, vocabKey, material, packaging, family, picOpened:true, note}, ... ]
    paper     np: classroom/paper (sheet) · classroom/notebook ·  pk: post office/envelope · post office/letter   (family 'env' = envelope+letter)
    cardboard pk: post office/package (taped box) · At the Supermarket/cereal (box with a cartoon face)
    glass     pk: kitchen tools/jar (blue plastic lid) · kitchen tools/bottle (red juice, metal cap; the design agent RE-OPENS: glass vs PET, OPEN 1) ·
                  At the Supermarket/jam · breakfast/honey (wooden lid) · At the Supermarket/milk (glass milk bottle) · At the Supermarket/oil (corked) ·
                  At the Supermarket/sauce (corked; family 'redbottle' = bottle+sauce)
    plastic   pk: At the Supermarket/shampoo (pump bottle) · At the Supermarket/water (PET) · beach/sunscreen (tube) · classroom/glue (squeeze bottle)
              np: At the Supermarket/toothbrush · beach/bucket (toy bucket) · around the house/comb · kitchen tools/spoon (purple plastic)
    metal     pk: At the Supermarket/can (tin)   np: kitchen tools/saucepan (all-metal copper) · tools/bolt (brass) · tools/nut (hex nut)
    organic   np: fruits/apple · fruits/banana · At the Supermarket/bread · vegetables/carrot · fruits/orange · spring/leaf · vegetables/corn ·
                  fruits/pear · vegetables/lettuce · vegetables/potato · vegetables/tomato · fruits/strawberry · At the Supermarket/pepper (bell pepper)
  _excluded (opened, with the reason; the panels read this, never re-add):
    kitchen tools/glass (a DRINKING glass: not container glass, wrong in de/sv/fi) · At the Supermarket/bag (paper or laminated: ambiguous) ·
    At the Supermarket/soda (glass or PET: ambiguous) · At the Supermarket/juice (ambiguous) · kitchen tools/lid (material unreadable) ·
    kitchen tools/colander (enamel or plastic) · At the Supermarket/salt (baked "SALT" + ambiguous) · post office/stamp (baked text, tiny) ·
    At the Supermarket/egg (eggs IN a pulp carton: two materials in one picture) · classroom/folder (card or plastic) ·
    COMPOSITE (never signed): around the house/toothpaste (tube) · classroom/binder (metal mechanism) · kitchen tools/fork (steel + green handle) ·
    kitchen tools/knife · whisk · grater · kettle · tongs · spatula · ladle · sponge · kitchen tools/pan (pot + glass lid) · around the house/scissors ·
    tools/screwdriver · tools/hammer · CERAMIC: kitchen tools/cup, mug, bowl, pitcher, jug · around the house/vase · NOT WASTE: classroom/book ·
    camping/map (a scene) · around the house/key · hospital/tissue (used tissue is Rest, a box is composite) ·
    VOCAB TRAP: tools/nail = the FINGERNAIL in es/pt/fr/it/da/no/fi (Uña/Unha/Ongle/Unghia/Negl/Kynsi, m) even though the picture is a spike
RECYCLING_LOC[loc] = {
  bins: [ {key, label (<= 12 chars, the pill), longLabel, color: 'codeBlue'|...|'inkSoft'|null, colorWord: literal|null,
           materials: ['paper','cardboard',...], packagingOnly: bool} ],           // 2..5, ORDER = page order
  route:        { toothbrush:{bin:'rest', reason:'…'} },                             // audited overrides, may be empty
  excludeItems: { saucepan:'Wertstoffhof: no bin on the page' },                      // per-locale, with a reason
  materialWords:{ paper:'paper', cardboard:'cardboard', glass:'glass', plastic:'plastic', metal:'metal' }, // F2; organic optional; es: 2 values
  colorConvention: 'national'|'classroom'|null,                                       // F4 gate
  refuse: [],                                                                         // 'which'|'write'|'odd'|'color'|'open'
  enAudit: '…',                                                                       // the panel's findings on the EN source
  strings: { 'K-357':{title, instruction}, <F1..F5 ids>:{title, instruction} } }
```

**`tools/validate-b4-draft.js` (recycling block; every rule runs, exit 1 on any):**
1. Every item: `(theme, noun)` resolves in `cache/manifest.json` with a `vocabKey` that has a singular in ALL 11 locales (`entriesFor`), is not `B2_EXCLUDE`d, `picOpened === true`, `material` ∈ the 6, `packaging` boolean, `family` non-empty; no two items share a `vocabKey`; no item id from `_excluded`.
2. Per material class (paper+cardboard, glass, plastic, metal, organic): >= 3 distinct families (the F3 trio floor) and >= 1 packaging item for glass / plastic / metal / paper+cardboard.
3. Per locale: 2 <= bins <= 5; keys unique; `label` non-empty, <= 12 chars, no digit, no `{`; `materials` ⊂ the 6; `color` ∈ `codeColors` keys ∪ {inkSoft} ∪ {null}; `route[id].bin` is a bin of this locale whose `materials` include the item's material and (item.packaging || !packagingOnly); every material is either covered by >= 1 bin or every item of it is in `excludeItems` (es: all 6 covered by 2 bins).
4. Resolution: `bin(item, loc)` is unique (first-match over bin order is deterministic; a second matching bin is a WARN, the first wins); every bin's resolved pool >= 1 (ERROR) and >= 2 (WARN, printed with the bin key: sv metall is the known 1).
5. F2: `materialWords` keys ⊂ materials; >= 2 distinct values; each value matches `^\p{L}[\p{L}'’-]*$` and `[...v].length <= 12`; items whose material has no word are F2-excluded; the F2 pool >= 8 items over >= 8 families, else `refuse` names `write`.
6. F4: for every bin `color !== null` AND `colorWord` non-empty AND `colorConvention ∈ {'national','classroom'}` AND colours distinct within the locale, else `refuse` includes `color`; a `classroom` convention requires the landing sentence flag `classKey:true`.
7. Titles <= 70, no `WORKSHEET_WORD` (the b3 regex, m), unique in the band; fi titles never match `/(^|[^\p{L}])lajittelu/iu` as the head (first token) and never equal `sorting-categories.name.fi`; sv titles never match `/^sortera/i`; it strings never contain "riciclaggio"; pt titles / metas containing "reciclagem" must also contain "coleta seletiva"; fr instructions never contain "en sciences".
8. Instructions <= 150 chars, name only apparatus of their face (a lexicon per face: base line + bin, F1 circle + bin, F2 bank + line, F3 circle + row, F4 legend + colour, F5 draw + write; a stray "tick" / "letter box" fails).
9. es: exactly 2 bins with keys `organico` / `inorganico` unless `panelOverride.bins` carries a reason (the lock).
10. `strings` ids === {base + the 5 allocated face ids}; the six `mode` strings are fixed (`base which write odd color open`); the six titles differ pairwise by >= 1 noun / verb (never adjective-only).
11. `enAudit` non-empty in every non-EN block (the panel audited the EN source).

**Poison (each must FAIL for its OWN reason; the correct EN draft is the control):** P1 an item with `packaging` missing -> rule 1. P2 `{theme:'kitchen tools', noun:'glass', material:'glass', picOpened:false}` -> rule 1 (and it is in `_excluded`). P3 a second item with `vocabKey:'bottle'` -> rule 1. P4 de bins with 6 entries -> rule 3. P5 de `route.toothbrush = {bin:'verpackung'}` (non-packaging into a `packagingOnly` bin) -> rule 3. P6 sv `plast` with `materials:[]` -> rule 4 (pool 0). P7 en `materialWords` all `'paper'` -> rule 5 (1 distinct). P8 nl GFT and Glas both `codeGreen` with `colorConvention:'national'` -> rule 6. P9 fi base title `'Lajittelu'` -> rule 7. P10 sv F1 title `'Sortera soporna'` -> rule 7. P11 fr base instruction `'Trie en sciences les déchets.'` -> rule 7. P12 F1 instruction `'Tick the bin it goes in.'` -> rule 8 (no tick apparatus). P13 es bins = 3 without `panelOverride` -> rule 9. P14 F2 title `'Recycling Sort: Big Bins'` vs F4 `'Recycling Sort: Colour the Bins'` -> rule 10. Control: the EN draft passes 1-11 (sv metall pool 1 raises only the WARN).

**`qa/verify-b4-recycling.js` (render gate; clone the b3 shape):** renders base + 5 faces x 11 locales x d2 through `render/render-instance.js` (file:// fonts) under the 722 stack (3-line de title + 150-char instruction) AND the 4-line fi title (677); asserts `verify()` empty and `qa/lints.js` clean, then the floors ITSELF (no size lint exists): pictures >= 56 on K faces (base 78, F1 72, F4 56), >= 44 on F2 (80), >= 36 on F3 (76); base bins >= 120 x 150 with a pill >= 17 px whose rendered width <= binW + 10 (measured `getBoundingClientRect`, every locale); F1 cells >= 56; legend chips 22 with words >= 17; every `[data-lcs-answer]` absent; base: every item's stamped bin present + each bin >= `perBinMin` + no bin's items adjacent + no `family` repeat; F1: one correct cell per row, columns === `loc.bins`; F2: bank set === card word set, bank order != card order, no word text inside a card; F3: 3 + 1 per row, 4 distinct nouns; F4: every bin body `fill` white, legend colours distinct tokens; F5: 3 empty drawboxes + 3 empty rulings; every `img` complete and `src` === `fileUri` of a bank item; everything above the footer. SWEEP: 20 seeds x d2 per face render distinct pages (item sets vary; F3 odd indices vary). Render poisons (each must FAIL): PR1 base strip with the two Bio items adjacent -> position leak; PR2 base with `envelope` + `letter` on one page -> family; PR3 F1 with two consecutive rows answering the same column -> run; PR4 F2 card printing its word -> "word printed"; PR5 F3 row with 2 glass + 2 plastic -> "not 3 + 1"; PR6 F4 bin pre-filled `codeBlue` -> "bin not white"; PR7 an `<img src>` from `pictureFor` (another theme's `apple`) -> "src not the opened picture"; PR8 sv page with `saucepan` in the strip -> "item excluded for locale"; PR9 it pill "Indifferenziato" at 17 px -> pill width > binW + 10; PR10 base d2 rendered with `perBinMax` read off the level index -> the config guard fires before render; PR11 F1 d3 (cell 48) under the K floor -> floor.

Page reads at render: `RECYCLING`, `RECYCLING_LOC[loc]`, `fileUri`, tokens; never `image-vocabulary.js`, `objForms`, `COLOR_WORDS` (the colour words are panel literals because grey is missing) or `approved-words-*.json`.

## E. SEO

`coordinate = {type:'recycling', mode:<face>, theme:'', level:<band key of the locale>}`; modes: base `'base'` · F1 `'which'` · F2 `'write'` · F3 `'odd'` · F4 `'color'` · F5 `'open'`. Levels: base / F1 / F4 = the K key (`kindergarten` / `vorschule` / `preescolar` / `educacao-infantil` / `maternelle` / `infanzia` / `kleuters` / `forskola` / `boernehaveklasse` / `1-trinn` / `esikoulu`, m `LEVEL_KEYS`), F2 / F5 = G1, F3 = G2. Meta lead inherits `seo.words.free_printable` (metadata only, never visible). The MIDDLE is the instruction stem (en 62-89 chars, inside a ~60-105 window for a ~60-char lead *est.*); the per-locale window is measured with `tools/measure-instruction-window.js` on the staged ZIPs.

| face | Germanic (en / de / nl) | Romance (es / pt / fr / it) | Nordic + fi (sv / da / no / fi) | meta MIDDLE (en) |
|---|---|---|---|---|
| base | Recycling Sort · Mülltrennung · Afval scheiden | Separación de basura · Coleta seletiva · Le tri des déchets · Raccolta differenziata | Källsortering · Affaldssortering · Kildesortering · Kierrätys | Each thing is empty or finished: draw a line from it to the bin it goes in |
| F1 | Recycling Sort: Which Bin? · Mülltrennung: Welche Tonne? · Afval scheiden: welke bak? | Separación de basura: ¿en qué bote va? · Coleta seletiva: qual lixeira? · Le tri des déchets : quelle poubelle ? · Raccolta differenziata: in quale bidone? | Källsortering: vilket kärl? · Affaldssortering: hvilken skraldespand? · Kildesortering: hvilken dunk? · Kierrätys: mihin astiaan? | Look at each thing and circle the bin it goes in when it is empty or finished |
| F2 | Recycling Sort: What Is It Made Of? · Mülltrennung: Woraus ist es? · Afval scheiden: waar is het van gemaakt? | Separación de basura: ¿reciclable u orgánico? · Coleta seletiva: de que é feito? · Le tri des déchets : en quelle matière ? · Raccolta differenziata: di che materiale è? | Källsortering: vilket material? · Affaldssortering: hvad er det lavet af? · Kildesortering: hva er det laget av? · Kierrätys: mitä materiaalia? | What is each thing made of? Write the word from the bank on the line |
| F3 | Recycling Sort: Odd One Out · Mülltrennung: Was passt nicht? · Afval scheiden: welke hoort er niet bij? | Separación de basura: ¿cuál no va? · Coleta seletiva: qual é o diferente? · Le tri des déchets : l'intrus · Raccolta differenziata: l'intruso | Källsortering: vilken passar inte? · Affaldssortering: hvad passer ikke? · Kildesortering: hva passer ikke? · Kierrätys: mikä ei kuulu joukkoon? | In each row three things are made of the same material: circle the one that is different |
| F4 | Recycling Sort: Colour the Bins · Mülltrennung: Tonnen ausmalen · Afval scheiden: kleur de bakken | Separación de basura: colorea los botes · Coleta seletiva: pinte as lixeiras · Le tri des déchets : colorie les poubelles · Raccolta differenziata: colora i bidoni | Källsortering: färglägg kärlen · Affaldssortering: farvelæg spandene · Kildesortering: fargelegg dunkene · Kierrätys: väritä astiat | Read the name on each bin and colour it in the colour the legend shows |
| F5 | Recycling Sort: Three Things I Recycle · Mülltrennung: Das trenne ich · Afval scheiden: drie dingen die ik scheid | Separación de basura: tres cosas que reciclo · Coleta seletiva: três coisas que eu reciclo · Le tri des déchets : je trie · Raccolta differenziata: tre cose che riciclo | Källsortering: tre saker hemma · Affaldssortering: tre ting jeg sorterer · Kildesortering: tre ting jeg sorterer · Kierrätys: kolme asiaa, jotka kierrätän | Draw three things you recycle at home and write what each one is |

All titles are candidates the native panels rewrite (<= 70, no worksheet word, base = the bare head, each face adds one NOUN / verb, never an adjective). JSON-LD `LearningResource` with NO `educationalAlignment` on any face (readiness; no K-2 CCSS code is honest); en prose names NGSS K-ESS3-3 on base / F1 / F5; every non-EN landing names the framework + the strand literal of A (the build adds the Science row to `strand-names.ts`, never the English fallback); pt cites EF01CI01 in prose; es states the SEP 2-bin model; every F4 landing with `classroom` states the class-key sentence; every sv landing says the plastic and metal bins take packaging. `topicMeta.recycling` >= 50 chars x11 + `skill-sentences.<loc>.json.recycling` via the `register-b4-en-content.js` clone.

**Non-cannibalisation** (whole-landing 3-gram Jaccard, `scripts/seo-landing/gate.js` FAIL >= 0.80 / WARN 0.65; *est.*, the gate measures):

| pair | what the copy says differently | est. |
|---|---|---|
| base vs K-214 / K-235 / K-207 (science-sort, "draw a line from each picture to the group") | material + the locale's named bins + "empty or finished" vs a concept pair; the base p1 names the bins by word and never says "group it belongs to" | 0.30 |
| base vs K-042 / G1-160 (`sorting-categories`, icon-labelled bins) | word-labelled drawn bins, different objects, a material key vs identical icons | 0.15 |
| base vs F1 | a line to a bin vs a grid of bins per item, one decision per row; the F1 copy leads with "which bin" | 0.35 |
| F1 vs F4 | both name the bins; F1 is a decision per item, F4 is colouring + a legend + example pictures | 0.25 |
| F2 vs G1-244 (write the picture's NAME) | "made of" + material bank vs the picture's name; F2 never says "name of the picture" | 0.20 |
| F3 vs K-043 (odd one out by category, one theme) | four different objects, an invisible property (material), grade 2 vs identical pictures at K | 0.20 |
| F3 vs K-039 / K-044 (same-different, size) | material vs size / identity | 0.10 |
| F4 vs K-241 / K-266 (colour by code / number) | the code is a bin WORD in the locale's real colours, the shapes are bins with example pictures | 0.15 |
| F5 vs G1-342 (read and draw) / K-332 (draw the feeling face) | no sentence to read; "at home", "recycle"; three own objects | 0.15 |
| any face vs K-212 wants / needs, K-201 living (shared `water`, `bread`, `apple`, `spoon`) | the question is "where does it go", the boundary sentence names it | 0.10 |
| any face vs the `science-sort` hub heads (fr "Tri en sciences", it "Classifichiamo le scienze", sv "Sortera i naturen", fi "Luonnontieteen lajittelu") | the head is the national waste-sorting term; no title or instruction echoes the hub head (rule 7) | 0.10 |

Boundary sentence on every landing: "The child sorts everyday things by what they are made of into the bins used where you live: paper, glass, plastic, metal and food. Sorting living things, natural things or needs is on the science-sort pages."

## F. Open questions + summary

**Engineer must measure:** (1) the `.sci-bin-label` pill at 17 px Baloo 2 700 for every locale's 5 labels against binW 120 (the 12-char cap is *est.*; a 2-line pill or 15 px is the fallback, never a wider bin); (2) the 722 / 677 stack of F2 (8 cards + bank) and F1 (6 rows) under the fi 4-line title; (3) F5's 3-column width (3 x 219 + 28 = 685 > 675: card padding 8 or drawW 185); (4) the per-locale meta window per face; (5) the F3 picture size at 76 with 4 rows x 4 (16 x 76 + gaps inside 675 x 722: ~440 tall, fits); (6) `verify-hub-type-rows.js` expectation: **66 rows if every panel adopts a colour convention; 64-65 with the expected F4 refusals (sv; possibly no)**, exported to `hub-expectations.json`; poison a wrong `coordinate.type` before `apps.recycling` lands; (7) the `waste-bin` primitive's node gate (only token hexes, `fill` white on F4, label pill width) with a poison per assertion.

**Only a native panel can rule:** OPEN 1 `kitchen tools/bottle` glass vs PET (the design agent re-opens; if PET it moves to plastic pk and glass keeps 6). OPEN 2 `bucket` (bulky, a toy) in the non-packaging plastic pool: keep or exclude per locale. OPEN 3 whether `organic` gets a single-token material word on F2 (de "Bioabfall", sv "matavfall", fi "biojäte"; en excluded). OPEN 4 fr 3 national bins vs 4 with biodéchets (2024 obligation). OPEN 5 nl glas colour in the F4 key (GFT owns green). OPEN 6 F4 `colorConvention` per locale: en / it / no / da / sv (the honest classroom-key sentence or refusal). OPEN 7 the merges: da pap into papir (else `cardboard` excluded in da), fi kartonki into paperi (the pill says "Paperi", longLabel "Paperi ja kartonki"). OPEN 8 sv F5 title without a "sorterar" verb ("tre saker hemma"). OPEN 9 `[NSR-FLAG][no]` glass og metall as ONE bin: a Norwegian teacher confirms the five literals before the bank is authored (the lock). OPEN 10 es `pepper` word (Pimiento vs chile; the picture is a bell pepper) in landing prose only. OPEN 11 whether en F4 uses compost (green) / recycling (blue) / trash (grey) as the classroom key.

**Summary.** Six faces on one locale-neutral item bank (38 opened pictures: paper 4 + cardboard 2 + glass 7 + plastic 8 + metal 4 + organic 13; 31 excluded with reasons, incl. the `nail` = fingernail vocab trap and every composite, ceramic or ambiguous picture) and one per-locale bin set (2 to 5 drawn bins; es-MX 2; sv packaging-only with a 1-item metal bin; no glass+metal `[NSR]`; da 5 of 10; fi never "Lajittelu"): draw a line to the bin (K) · which bin, a grid with one decision per row (K) · write the material from a bank (G1; es reciclable / orgánico) · odd one out by material over four different objects (G2) · colour the bins by the locale's legend with example pictures (K; the contingent face: sv expected refused, no / en / it / da need a colour ruling) · three things I recycle, open (G1). One additive `layout` knob, one join rule (`route` > first matching bin > excluded), zero printed nouns, zero inflection, 26-47 panel literals per locale, no `objForms`. New assets: `primitives/waste-bin.js` + `templates/components-b4/recycling.js` + `data/b4/recycling.json` (+ `recycling-locales/<loc>.json`). Expected hub rows 64-66 of 66.
