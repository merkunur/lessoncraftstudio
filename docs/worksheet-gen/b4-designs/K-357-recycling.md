# K-357 `recycling` (K/G2) : FINAL design (editor merge, 2026-09-21)

Merged from `_work/K-357-pedagogy.md` + `_work/K-357-design.md` (record of every ruling: `_work/K-357-critic.md`). (m) = re-measured 2026-09-21 by read-only node (`lib/b2-common.js entriesFor` + `lib/b3-picture-index.js hasPicture/candidates` over the 37-item bank ×11; `cache/manifest.json` noun lists of kitchen tools / tools / At the Supermarket / around the house / post office / classroom / beach / breakfast; `scripts/seo-landing/gen-b3-landings.js LEVEL_KEYS`; `frontend/config/topics-taxonomy.json`; `frontend/lib/seo/strand-names.ts`; `templates/components-b2.js`, `templates/components-b3/read-and-do.js`, `templates/layouts/card-grid.js`, `primitives/trace-path.js`, `primitives/_tokens.js`, `page/page.css`, `types/_shared/science-category-sort.js`, `types/g1/G1-244-write-the-word.js`) or in puppeteer with the shell's Baloo 2 / Nunito woff2 loaded from `assets/fonts/fonts.css` over `file://` (`document.fonts.check` true; scratch `K-357-critic-measure.js`: the design's `Indifferenziato` pill 140.3 reproduced to the decimal) or by OPENING the picture (the editor built a 140 / 64 / 64-greyscale contact sheet of the 42 disputed pictures, `K-357-critic-sheet.png`, and read it; the pedagogy opened 69, the design 80). *est.* = the engineer measures in the real render (`render/one.js`). No em-dashes.

**Boundary (load-bearing).** The science-sort family (`types/_shared/science-category-sort.js`: a shuffled strip of DIFFERENT pictures over WORD-labelled `.sci-bin` boxes 185 px high, a line per picture; K-201 living, K-207 clothes, K-208 day / night, K-211 hot / cold, K-212 wants / needs incl. `At the Supermarket/water`, `bread`, `fruits/apple`, K-214 natural / man-made incl. `around the house/cup`, `classroom/book`, m by the pedagogy) sorts by CONCEPT; `sorting-categories` (`sort-to-bins.js`, K-042 / G1-160) sorts DUPLICATE icons into ICON-labelled circular bins (`.ws-bin-label` = a 56 px circle that cannot hold a word, `page.css:248`, m); K-235 sorts vocabulary categories; K-043 odd-one-out is N identical pictures + 1 other noun of one theme; G1-244 writes a picture's NAME on a `writingRow` with an optional `letterBoxes` scaffold and a `wordBank` (`G1-244:64-74`, m); K-241 / K-265..269 colour SHAPES by a code; G1-242 colours BW art from a sentence; G1-342 draws from a sentence; K-324 owns cut-and-play. **This family owns exactly one thing nobody has: the MATERIAL of a finished PRODUCT as the sorting key, under the LOCALE's own drawn bin set** (NEW `primitives/bin.js`: a lidded bin with a coloured lid, a position-keyed mono lid mark and a word pill; `_SUBSTRATE.md`: "no primitive exists for a waste bin with a colour"). The bin set is per-locale DATA (2 to 5 bins; es-MX 2 = orgánico / inorgánico; no `glass og metall` ONE bin `[NSR-FLAG][no]`; da 5 of the national 10; fi never titled "Lajittelu" = `exercise-type.sorting-categories.slug.fi`, m); the material of every item is fixed ONCE, locale-neutral, by opening the picture (`data/b4/recycling.json`); the library draws PRODUCTS, so every instruction says "when it is empty or finished". It never sorts recyclable yes / no alone (that is es-MX's 2-bin instance, valid there), never natural / man-made, wants / needs or living, and never prints an item noun: the only words on any face are bin words, material words and colour words, all panel literals. No face calls `makeScienceCategorySort.build()` (its 8-item `itemPx` = 52 < the K floor 56 and its label pill has no overhang budget, m) or echoes a `science-sort` hub head (fr "Tri en sciences", it "Classifichiamo le scienze", sv "Sortera i naturen", fi "Luonnontieteen lajittelu", es "Clasificar en ciencias", pt "Classificar em Ciências", de "Sachunterricht-Sortieren", m); the shared item set with K-212 / K-201 / K-214 (`water`, `bread`, `apple`, `spoon`) is allowed because the QUESTION differs (where does it go), and every landing says so. Visual signature: a row of N teal-outlined bins with coloured lids, one cream word pill each, product pictures from many themes above, one pencil line per product; the same bins, at three sizes, on every face.

## 1 Identity

| field | value |
|---|---|
| id / key / bands | `K-357` / `recycling` / base **K**; F1 **K**, F4 **K** (`K-358+ TBD by the emitter`); F2 **G1**, F5 **G1** (`G1-354+ TBD`); F3 **G2** (`G2-347+ TBD`). `default_subject: science`, `default_age_range: 5-7`, `assetClass: icon-placement`, `exerciseType: recycling`. `apps.recycling` + `axes['exercise-type'].recycling` ABSENT (m); register with `tools/register-b4-taxonomy.js` (the `apps.seasons` shape) + slug / name ×11 from table B. Every head slug of table B grepped against every `axes.*` slug AND name in its locale: 0 collisions; the only neighbour is fi `lajittelu` (`sorting-categories.slug.fi`, name "Lajittelu") and `luonnontieteen-lajittelu` (`science-sort.slug.fi`), both banned as a fi HEAD by validator rule 7 (m). No `axes.theme` key names waste (m): no compound-head rule applies. |
| theme axis | **THEMELESS.** Items are fixed `(theme, noun)` pairs over 11 themes (classroom, post office, At the Supermarket, kitchen tools, breakfast, beach, around the house, tools, fruits, vegetables, spring); every kept key resolves through `hasPicture` + `entriesFor` in all 11 locales with a singular, none `B2_EXCLUDE`d (m: 37 / 37, 0 missing). `fileUri(theme, noun)` reads the OPENED picture, never `pictureFor` (which may pick another theme's picture of the same key: `apple` has 5 candidates, `orange` 5, `water` 3, `bucket` 3, m). Landings carry `coordinate.theme:''`. `themeAxis:{applicable:false}`. |
| unitAxis | **NOT APPLICABLE, RULED.** The bin set is locale DATA (one set per locale), not a fan; nothing numeric or unit-bearing reaches a page. |
| CCSS (en, honest) | NONE on any face (no `educationalAlignment`; `teaches` = "Recycling sort (readiness)"). en prose names NGSS **K-ESS3-3** ("communicate solutions that will reduce the impact of humans on the land, water, air") on the base, F1 and F5. Non-EN names the national framework + the science strand literal of table B (`strand-names.ts` has 0 Science rows, m; the build adds the row from table B, identical to K-354 / K-355 / K-356; README open item 1). |
| new primitives / data | `primitives/bin.js` (§2 geometry + node gate `qa/verify-b4-bin-primitive.js`); `templates/components-b4/recycling.js` (9 components, §2) behind the `templates/components-b4.js` barrel; `data/b4/recycling.json` (global item bank, 37 opened) + `data/b4/recycling.js` (en block) + `data/b4/locales/recycling.<loc>.json` (§5); `lib/b4-common.js bank('recycling', loc)` (README open item 3). |

| loc | genre head (the panels, verbatim) | ASCII slug | level K / G1 / G2 (`LEVEL_KEYS`, m) | bin set (N; material first, colour second; F4 colour convention) | national strand literal (this table IS the strand row) |
|---|---|---|---|---|---|
| en (US) | **Recycling Sort** (B+; never "Recycling" alone = adult intent; "which bin worksheet" verbatim demand; US spelling "color") | `recycling-sort` | `kindergarten` / `grade-1` / `grade-2` | 5: paper (+cardboard) · plastic · glass · metal · compost; no trash bin (non-packaging plastic / metal sort single-stream); F4 **REFUSED** (no national colour convention; a classroom key collapses the face into K-241, §3 F4) | Science (readiness; NGSS K-ESS3-3 in prose on base / F1 / F5) |
| de | **Mülltrennung** (A; "Müll richtig trennen" in the meta) | `muelltrennung` | `vorschule` / `1-klasse` / `2-klasse` | 5: Papier · Verpackung (Gelbe Tonne / Gelber Sack in `longLabel`) · Bio · Glas · Rest; F4 `national` (blau / gelb / braun / grün / grau) | Sachunterricht |
| es (MX) | **Separación de basura** (A-; "reciclaje" in the meta only; child word "basura", bins "botes") | `separacion-de-basura` | `preescolar` / `primer-grado` / `segundo-grado` | **2**: Orgánico · Inorgánico (SEP; the landing says CDMX runs 4); F4 `national` (verde / gris = `inkSoft`) | Conocimiento del Medio |
| pt (BR) | **Coleta seletiva** (A; "reciclagem" only beside "coleta seletiva"; "lixeira" never "caixote") | `coleta-seletiva` | `educacao-infantil` / `1o-ano` / `2o-ano` | 5: Papel · Plástico · Vidro · Metal · Orgânico; F4 `national` CONAMA 275 (azul / vermelho / verde / amarelo / marrom) | Ciências (BNCC EF01CI01 in prose) |
| fr | **Le tri des déchets** (A; "tri sélectif" in the meta; "poubelle" is the K word) | `le-tri-des-dechets` | `maternelle` / `cp` / `ce1` | **4**: Emballages et papiers (pill "Emballages") · Verre · Biodéchets · Ordures (OPEN 4: the panel may drop to the national 3); F4 `national` (jaune / vert / marron / gris) | Questionner le monde |
| it | **Raccolta differenziata** (A; "riciclo" in the meta; NEVER "riciclaggio") | `raccolta-differenziata` | `infanzia` / `classe-prima` / `classe-seconda` | 5: Carta · Plastica (`longLabel` "Plastica e metallo") · Vetro · Umido · Secco (`longLabel` "Indifferenziato", never on a pill); F4 `national` on the UNI 11686 colour set (blu / giallo / verde / marrone / grigio) `[NSR]`, else refused | Scienze |
| nl | **Afval scheiden** (B; "afval sorteren" BE-leaning, meta only) | `afval-scheiden` | `kleuters` / `groep-3` / `groep-4` | 5: Papier · GFT · PMD · Glas · Rest; F4 `national` IFF the panel fixes a glas token distinct from GFT's green (OPEN 5), else refused | Oriëntatie op jezelf en de wereld |
| sv | **Källsortering** (A; "sopsortering" / "återvinning" in the meta; NEVER a title starting "Sortera") | `kallsortering` | `forskola` / `ak-1` / `ak-2` | 5: Papper · Plast · Metall · Glas · Matavfall (packaging-only plast + metall; no restavfall); F4 `national` on the Nordic pictogram colours (Avfall Sverige skyltsystem) `[NSR]`, else refused | Naturorienterande ämnen |
| da | **Affaldssortering** (B+; "sorter affald" in the meta; "genbrug" out of titles) | `affaldssortering` | `boernehaveklasse` / `1-klasse` / `2-klasse` | 5 of the national 10: Madaffald · Papir (+ pap merged, OPEN 7) · Plast · Glas · Metal; F4 `national` on the pictogram colours `[NSR]`, else refused | Natur/teknologi |
| no | **Kildesortering** (A-; "hva skal hvor" verbatim demand) `[NSR-FLAG][no]` | `kildesortering` | `1-trinn` / `2-trinn` / `3-trinn` | 5: Papir · Plast · Glass og metall (pill "Glass/metall") · Matavfall · Restavfall; F4 `national` on the pictogram colours `[NSR]`, else refused | Naturfag |
| fi | **Kierrätys** (A-; "Kierrätys ja lajittelu" in the meta; NEVER "Lajittelu") | `kierratys` | `esikoulu` / `1-luokka` / `2-luokka` | 5: Bio · Paperi (kartonki merged, `longLabel` "Paperi ja kartonki", OPEN 7) · Lasi · Metalli · Muovi (packaging only; no sekajäte); F4 `national` HSY lid colours (the panel confirms) | Ympäristöoppi |

**The rule that locks the type.** *The material is a fact of the PICTURE, stored once; the bin is a fact of the LOCALE, stored per locale; the code joins them and never decides:* `bin(item, loc) = loc.route[item.id]?.bin ?? first bin in loc.bins (page order) whose materials include item.material and (item.packaging || !bin.packagingOnly) ?? EXCLUDED-for-this-locale`. Every printed word is a whole panel literal (bin `label`, `materialWords`, `colorWords`, titles, instructions); no item noun is ever printed; no inflection exists (no cloze frame, no `objForms`); an ambiguous material is excluded from the bank, never signed.

## 2 The base page

**Concept.** "Recycling Sort." Eight everyday products in one calm row at the head (a tin can, a water bottle, a jam jar, an envelope, an apple, a plastic comb, ...); under a tall empty line zone, the locale's N bins stand side by side at the foot, each with a coloured lid, a white mono mark on the lid and its word on a cream pill. The child draws one line from each product to the bin it goes in "when it is empty or finished". From across the room: N coloured lids in a row at the bottom, eight bright pictures at the top, nothing between but the lines the child draws. The design's measured geometry (tile 72 / icon 60, bins 117 × 176, margins 17) replaces the pedagogy's 78 / 120 × 150 (8 × 78 + 7 × 12 = 708 > 675: the pedagogy's strip does not fit one row); the pedagogy's composer (one item per bin first, then round-robin to a cap of 2) replaces the design's fixed per-locale `quota`.

**Chrome budget (README ruling).** Body **722** with 3-line title + 3-line instruction (814 one-line; 677 with a 4-line fi title). `.ws-page` inner 675. Root `<div class="rc-sort" data-ws-content style="flex:1 1 auto;display:flex;flex-direction:column;justify-content:space-between;min-height:0">` (the `science-category-sort.js SCOPED_CSS` look, `.sci-sort/.sci-strip/.sci-item/.sci-dot/.sci-bins` lines 26-31, cloned as `.rc-*` in the b4 component; the factory's `build()` is NOT called). Slack opens the line zone; strip and bins never move.

**Layout d2 (5-bin locale; es-MX N = 2 and fr N = 4 change only `binW`)**
```
x 0  7                                                                    667 675
y 0   [tile 72][12][tile][12][tile][12][tile][12][tile][12][tile][12][tile][12][tile 72]   strip 8 x 72 + 7 x 12 = 660 <= 675 (margin 7.5)
      tile = cream, border 2 #F0E4CB, r 14, padding 6, .ws-icon 60 x 60; coral dot 11 px on the bottom edge (.sci-dot idiom)
y 91  strip end (4 top + 72 + 8 bottom + 7 dot overhang)   .......... line zone 425 (space-between) ..........
y 516 bins block: padding-top 30 (pill overhang 26 + 4)
y 546 [ bin 117 ][14][ bin 117 ][14][ bin 117 ][14][ bin 117 ][14][ bin 117 ]   row 5 x 117 + 4 x 14 = 641, margins 17 each side
      pill: absolute top -26, centred, Baloo 2 700 17 teal on cream, border 2.5 teal, r 999, padding 5 10, nowrap
y 722 bins end (bin 176)                                          = 91 + 425 + 206 = 722; at 677 the zone is 380
```
`binW = min(210, floor((675 - 34 - (N - 1) * 14) / N))` (m arithmetic): N = 5 → **117**; N = 4 → 146; N = 3 → 204; N = 2 → 210 (the `.ws-bin` cap; `page.css:236` caps its own class at 260, ours is fixed by the formula). `binH` 176 on every N (`primitives/bin.js` computes px geometry from `w, h`; no viewBox scaling, so a 210-wide es bin is not 315 tall). K floors: tile 72 / icon 60 >= 56; items 8 in [4, 8]. **Pill overhang is BUDGETED, not forbidden (the K-355 finding, re-measured, m):** at 17 px Baloo 2 700 with padding 10 + border 2.5 the pills are it `Indifferenziato` 140.3 · no `Glass og metall` 140.0 · no `Glass/metall` 118.9 · de `Gelber Sack` 114.3 · `Verpackung` 115.2 · fr `Emballages` 111.7 · `Biodéchets` 110.1 · `Poubelle grise` 132.6 · es `Inorgánico` 106.1 · `Orgánico` 93.4 · da `Madaffald` 103.1 · no `Restavfall` 101.3 · `Matavfall` 96.4 · nl `Restafval` 96.8 · fi `Sekajäte` 90.2 · `Kartonki` 90.1 · en `Compost` 92.1 · sv `Pappersförpackningar` 195.8 (REFUSED as a pill: > 151). Rules the render gate enforces with the real fonts on every locale's table: **pill <= 151** (a 151 pill over a 117 bin overhangs 17 per side = the row margin; worst right edge 675 - 17 - 58.5 + 75.5 = 675) and **adjacent half-widths sum <= 125** (centres 131 apart; it 70.2 + 36.9 = 107 ok; no 70.0 + 48.2 = 118 ok). A table that breaks either rule is a DATA failure: the panel shortens a label (it "Secco" 68.4 for the residual bin; no "Glass/metall" 118.9), never a wider bin and never a second bin row (§ Alternatives).

**Ladder** (resolved config keys; guards key on THESE, never the level index):

| key | d1 | d2 (ships) | d3 |
|---|---|---|---|
| items | 6 | **8** | 10 |
| perBinMin / perBinMax | 1 / 2 | **1 / 2** (es-MX N = 2: `perBinMax` = `ceil(items / N)` = 4; copy says "two bins") | 2 / 2 |
| tile / icon | 92 / 78 (6 x 92 + 5 x 12 = 612) | **72 / 60** (660) | 84 / 70, two strips of 5 (2 x 84 + 12 = 180; zone 341) |
| binW / binH | 117 / 176 | **117 / 176** | 117 / 176 |
| twinsAllowed | false | **false** | false |

**d3 ruling.** 10 items = the G1 register of the same move (above the K ceiling 8); not a face (same act; fails `gate-variation-distinct.js` on move, not on config); unpublished; no copy describes it.

**Composer.** bins = `loc.bins` in page order (N = 2..5). Per bin, the resolved pool = bank items whose `bin(item, loc)` is that bin (per-locale, m from the 37-item bank in §5: de Papier 6 / Verpackung 5 / Bio 13 / Glas 6 / Rest 4 · nl papier 6 / GFT 13 / PMD 5 / glas 6 / rest 4 · fr emballages 11 / verre 6 / biodéchets 13 / ordures 4 · it carta 6 / plastica 5 / vetro 6 / umido 13 / secco 4 · es orgánico 13 / inorgánico 24 · pt papel 6 / plástico 8 / vidro 6 / metal 4 / orgânico 13 · **sv papper 6 / plast 4 / metall 1 / glas 6 / matavfall 13** · da madaffald 13 / papir 4 (6 if pap merges) / plast 8 / glas 6 / metal 4 · no papir 6 / plast 4 / glass og metall 7 / matavfall 13 / restavfall 4 · fi bio 13 / paperi 6 / lasi 6 / metalli 4 / muovi 4 · en paper 6 / plastic 8 / glass 6 / metal 4 / compost 13). Step 1: each bin takes 1 item (`rng.pick` from its pool). Step 2: the remaining `items - N` slots are filled round-robin over bins in `rng.shuffle` order, skipping a bin at `perBinMax` or with an empty pool. Constraints, re-drawn (bounded 60 tries, then the seed's next permutation): no two items share `family`; no two items share `vocabKey` (twins across themes never on one page); `classroom/paper` and `post office/letter` never both (both flatten to pale rectangles in mono, opened) and never adjacent to `package`. Strip = `rng.shuffle`, re-drawn while any bin's items are all adjacent in the strip (position leak). Seed is locale-neutral: the same slot sequence and positions in all 11; a locale whose bin set EXCLUDES the drawn item (sv `saucepan`, de `bolt`) re-draws THAT slot from the same material pool in `rng` order (deterministic), so the sv page differs from the de page only in the excluded slot. sv metall pool = 1: the metal bin always shows `can` (allowed at `perBinMin:1`; a WARN in the validator, not a refusal; recorded in §4).

**Answer-hiding + uniqueness.** Bins are drawn EMPTY; nothing on the page pairs an item with a bin except stamps: tile `data-lcs-item="<id>" data-lcs-material="<key>" data-lcs-bin="<binKey>"`, bin `data-lcs-bin="<key>" data-lcs-materials="paper,cardboard" data-lcs-color="<token|none>" data-lcs-mark="<0..4>"`, root `data-lcs-bins` (ordered keys) + `data-lcs-layout` ABSENT on the base (the base stays byte-identical; a face stamps its `layout`). Uniqueness = every material drawn on a page maps to EXACTLY ONE bin in that locale (validator rule 4; first-match over bin order). `verify(page)`: every tile's stamped bin === `bin(item, loc)` re-derived in node from the bank + the locale block; every stamped bin is rendered; N === `loc.bins.length` (2..5); every bin receives >= `perBinMin` and <= `perBinMax` tiles; no two tiles share `family` or `vocabKey`; no bin's tiles all adjacent; every pill text === `loc.bins[k].label`; no other text node inside the root; every `img` complete, `src` === `fileUri(theme, noun)` of a bank item (never a `pictureFor` candidate); every icon >= 60 rendered; no `[data-lcs-answer]` anywhere.

**Reused (exact).** `image-cache/resolve.js fileUri(theme, noun)` · `lib/rng.js int pick sample shuffle` · `lib/b3-picture-index.js hasPicture` (bank existence gate) · `lib/b4-common.js bank('recycling', loc)` (README open item 3; an unauthored locale REFUSES, never falls back to en) · `page/page.css` `.ws-icon .ws-card .ws-card-badge .ws-card-stage .ws-scene-banner .ws-bankword .ws-lane` · `templates/layouts/card-grid.js cardGrid({cards, cols:1, rows:4})` (F3) · `templates/components-b2.js wordBank({words, wordPx})` (F2; `components-b2.js:228`, m; 59 px + `margin-bottom:10`) and `rulingBlock({rows, w, h, glyphH, starters, gap})` (F5; `:76`, m; starters sized by `starterFontPx` from `primitives/font-metrics.json`) · `primitives/trace-path.js writingRow({w, h, glyphH, xHeight:true})` (F2; `:681`, m) · `templates/components-b3/read-and-do.js drawBox({w, h})` (F5; `:124`, m; its 30 px teal `badge` idiom cloned, the b3 barrel does not export `badge`) · `primitives/_svg.js svgRoot el roundedRect esc` · tokens `color.*`, `codeColors.*` (8 keys), `stroke.primitive 3 / grid 1.5`, `radius.cell 10 / chip 999` · the `.sci-*` LOOK (cloned as `.rc-*`, numbers of the layout above).

**NOT used.** `makeScienceCategorySort.build()` (`.sci-bin` word boxes not drawn bins; `itemPx = min(78, floor(648 / 8) - 12)` = 52 < 56; label pill padding 16 with no overhang budget: `Indifferenziato` would be 152, m) · `makeSortBinsType` (identity sort, icon circles) · `makeOddOneOutType` (identity, theme-bound; only the `.ws-card-stage` box look is cloned) · `colorLegend` (`components-b2.js:95`: `SWATCH[key]` is undefined for grey → an invalid fill; one word per swatch; F4 needs colour word + bin word and `inkSoft` for grey) · `letterBoxes` (F2 writes on school lines: a grapheme-count scaffold gives the word away and is the `write-the-word` family's look, Fact 9) · `pillChoice`, `answerBox` (stamps `data-lcs-answer="undefined"` without an answer), `blankNumeralBox` (no numeral on any face) · `data/color-words.js COLOR_WORDS` at render (no grey; the colour words are panel literals) · `image-vocabulary.js`, `objForms` (0-3 of the 37 nouns outside fi; nothing read, m by the pedagogy), `approved-words-*.json` at render.

**NEW `primitives/bin.js`** (pure SVG on tokens; Node-testable). `bin({ w = 117, h = 176, colour, mark = 0, fill = 'lid', strokePx = 3, data = {} }) -> { svg, w, h }`. Geometry in px from `w, h` (no viewBox scaling): `handle` rect x 0.38w..0.62w, y 0.07h..0.14h, r 0.04w · `lid` rect x 0.06w..0.94w, y 0.13h..0.28h, r 0.07w · `collar` rect x 0.14w..0.86w, y 0.27h..0.37h · `body` path x 0.14w..0.86w, y 0.27h..0.97h, bottom corners r 0.18w, top corners square (under the lid). Stroke teal `strokePx`, `stroke-linejoin:round`; at w < 64 the stroke is `max(2, strokePx * w / 117)` (a 44 px chip keeps a 2 px line; 1.5 vanishes in print). Each part is a `<g data-lcs-bin-part="handle|lid|collar|body">`. `fill:'lid'` (base, F1 chips, F1 key): handle + lid + collar in `colour` (a `codeColors` key or `'inkSoft'`, resolved to its hex; any other key THROWS), body white. `fill:'all'`: every part in `colour`. `fill:'none'` (F4): every part white, no mark; the four parts share `<g data-lcs-fill="<colourKey>" data-lcs-bin-fillgroup>` so verify() reads the intended colour off the group, never off a fill attribute. `mark` = the MONO cue drawn in white on the lid, keyed on the bin's POSITION in the locale table (identical on every face in that locale): 0 none (solid) · 1 three dots r 0.16 lidH at 0.3 / 0.5 / 0.7 w · 2 three bars 0.06w × 0.6 lidH at 0.28 / 0.44 / 0.60 w · 3 one rounded bar 0.5w × 0.24 lidH · 4 zigzag 5 points, stroke 0.14 lidH; every mark feature clamped `max(2px)` (the mock at 44 px shows `dots` as the weakest, still legible; the design's greyscale mock: blue `#2E6DA4` and brown `#8C5A2B` lids print as ONE dark grey, green and `inkSoft` as two mid greys, so the mark + the pill word are what separate five bins in mono; the mark is mandatory, not decorative). Min sizes: chip 44 × 66 (F1 rows), key 56 × 84 (F1 key strip), base / F4 117 × 176 / 117 × 260, es-MX 210 × 176. Node gate `qa/verify-b4-bin-primitive.js`: four parts present; fill group present; colour keys resolve (an unknown key throws); mark index 0..4 and its features >= 2 px at w 44; stroke clamp at w 44; `fill:'none'` has NO non-white fill and NO mark (poison: a leaked lid colour on the colour face); px geometry scales in width only (a 210 × 176 bin is 176 tall).

**NEW `templates/components-b4/recycling.js`** (barrel `templates/components-b4.js`; token palette only; scoped inline CSS; stamps carry the truth): `binRow({bins, binW, binH = 176, pillPx = 17, gap = 14, fill = 'lid'})` → `.rc-bins` flex centred, `padding-top:30px`, N `.rc-binslot` (`position:relative; flex:0 0 ${binW}px; height:${binH}px`, `data-lcs-bin data-lcs-materials data-lcs-color data-lcs-mark`) each with `.rc-pill` (absolute top -26, centred, nowrap, cream, border 2.5 teal, r 999, padding 5 10, Baloo 2 700 `pillPx` teal; stamps `data-lcs-pill-px`) + `bin({w:binW, h:binH, colour, mark, fill})` · `sortStrip({items, tile = 72, iconPx = 60, gap = 12})` → the `.rc-strip` above (`flex-wrap` OFF; d3's second row is a second `.rc-strip`) · `keyBins({bins, slotW = 117, binW = 56, binH = 84, pillPx = 15})` → the F1 key: N slots of `slotW` (same centres as `binRow`; at 15 px it `Indifferenziato` 122.6, `Secco` 59.2, m; same adjacency rule) with a 56 × 84 lid-filled bin centred under its pill; height 24 + 4 + 84 = 112; stamps `data-lcs-key` · `binChips({bins, tile = 64, tileH = 80, binW = 44, gap = 10})` → N white rounded-square tiles (r 12, border 2 creamDeep; NOT rings: the pencil circle must be the only circle in the row) each holding `bin({w:44, h:66, fill:'lid'})`; stamps `data-lcs-chip="<binKey>"`; width 5 × 64 + 4 × 10 = 330 · `whichBinRow({n, item, chips})` → white card r 12, border 2 creamDeep, `display:grid;grid-template-columns:30px 72px 1fr 330px;column-gap:12px;align-items:center;padding:0 12px` (30 + 12 + 72 + 12 + 330 + 24 = 480 <= 675; the spacer takes the rest): badge, product tile 72 × 72 (icon 60), chips; stamps `data-lcs-row data-lcs-item data-lcs-material data-lcs-bin data-lcs-chip-order` · `materialRow({n, item, rulingW = 400})` → white card r 12, `grid-template-columns:30px 64px 1fr;column-gap:12px;padding:0 12px` (30 + 12 + 64 + 12 + 400 + 24 = 542 <= 675): badge, tile 64 (icon 52 >= G1 44), `writingRow({w:400, h:58, glyphH:30, xHeight:true})` right-aligned; stamps `data-lcs-row data-lcs-item data-lcs-material data-lcs-word="<literal>"` (never printed) · `oddRow({items, oddIdx, box = 120, iconPx = 84})` → `.ws-card-stage` `justify-content:space-evenly;padding:6px` of four 120 × 120 white boxes r 10 (the K-043 look) holding `.ws-icon` 84; every icon `data-lcs-material`; the odd one `data-lcs-odd="1"`; stamps `data-lcs-items="4"` · `legendChips({entries})` → `.ws-scene-banner` (white, dashed coral 2.5, r 12, 59 px) of `[22 px swatch rect r 5, fill = token(colourKey), stroke ink 1][8][colour word Nunito 800 17 ink][6][bin word Baloo 2 700 17 teal]` items, gap 22, `flex-wrap:wrap`; colour keys resolve `codeColors.*` OR `color.inkSoft`, anything else THROWS; stamps `data-lcs-legend="<binKey>" data-lcs-color="<colourKey>"` · `exampleShelf({items, pic = 56})` → a cream strip 117 × 64 under an F4 bin holding two 56 px pictures (2 × 56 + 4 = 116 <= 117) with `data-lcs-example="<id>" data-lcs-bin` · `recycleLane({n, draw = {w:170, h:150}, rows = 2, starter = null})` → `.ws-lane` with the inline `padding:10px 12px` override (inner 651, stated) `display:grid;grid-template-columns:30px 170px 1fr;column-gap:12px;align-items:center` (30 + 12 + 170 + 12 + 400 = 624 <= 651): badge, `drawBox({w:170, h:150})`, `rulingBlock({rows:2, w:400, h:56, glyphH:36, starters:{0: starter}, gap:8})`.

**Alternatives (rejected).** Alt A, the 4 + 1 bin split (four bins on the bin row, the fifth on a second row above): bins 4 × 146 + 3 × 14 = 580 on row 2 and one bin on row 1; row 1 sits 176 + 30 above row 2, so every line to a row-2 bin crosses row 1's zone and pill; the K line zone shrinks 425 → 219; the lone fifth bin (always the residual one) reads as "the odd bin", a false cue. Alt B, cut-and-glue sort: K-324 owns cut-and-play; cut lines eat ~40 px; the line delivers the same classification in one pencil. Alt C, the pedagogy's 120 × 150 bins + 78 px items: 8 × 78 + 84 = 708 > 675 on the strip; the design's 72 / 60 keeps one row above the K floor. Alt D, the pedagogy's F1 grid (one row per item, one COLUMN per bin, header pills): the design's rows + key strip carry the bin WORD once in a 56 px key with lid marks AND a mini bin per cell (the same drawing the child lines to on the base), where a header-pill grid makes the cells empty circles.

**Risks → mitigations.** Long labels: only the residual / composite bin is a width risk (it 140.3, no 140.0, fr a colour-named bin 132.6); the render gate measures pill <= 151 + adjacency <= 125 on every locale table with the real fonts; the fix is a shorter panel literal. Greyscale: the position-keyed lid mark + the pill word separate five bins in mono (blue and brown print as one grey); the engineer prints one d2 of the base, F1 and F4 on a mono laser at 100 % and lines a can to the metal bin in HB. Mini bins at 44 (F1 chips): marks are 2-3 px, `dots` weakest; the `max(2px)` clamp, the fixed row order and the 56 px key strip with words carry it; never a bin under 40 wide. Picture ambiguity: one material per item fixed by OPENING (`picOpened:true`), 32 excluded with reasons (§5), twins never on one page, the "empty or finished" instruction. Thin pools: plastic packaging 4 (shampoo, water, sunscreen, glue) and sv metall 1; d2 asks any bin for <= 2; F3 uses plastic packaging + non-packaging as one class (8); the validator refuses a config a pool cannot fill. Overflow in long locales: no wrapping text exists (pills nowrap + measured; bank one measured row: worst en 502.7, it 501.5, fr 497.7 <= 663, m; ruling rows fixed; the gate asserts one `getClientRects()` box per pill and per bank). fi four-line title (677): base zone 380; F1 672 (rows `minmax(85px,1fr)`); F2 621; F3 666; F4 431; F5 612: every face survives 677 by arithmetic (§3), the pedagogy's "fi F1 title <= 3 lines" validator check stays as belt and braces. **Palette:** cream, creamDeep, white, teal, coral, ink, inkSoft, grid + the 8 `codeColors` on lids / swatches; the white lid marks are `color.white`. **Font floor:** smallest text 15 (F1 key pills) vs the 9 px lint; pills 17; bank 18; badges 16. Every root stamps `[data-ws-content]`.

## 3 Faces 2-6

All five are **CODE** faces on ONE additive `layout` knob of the base spec (`types/k/K-357-recycling.js`; `d.layout` undefined = the base path, byte-identical; a face stamps `data-lcs-layout` only when declared; `verify()` branches on the stamp, never on the level index). Every face changes the child's ACT (line-to-bin → circle-one-of-N → write-the-material → circle-the-odd → colour-by-the-locale-legend → draw-and-write) and owns a measured or panel-named query head; none differs from another by an adjective; each is a distinct resolved d2 config, so `gate-variation-distinct.js` cannot collapse two. Density: K faces min element 72 tile / 60 icon (base), 64 tile / 60 icon (F1), 56 pictures (F4); G1 64 tile / 52 icon (F2); G2 84 icons in 120 boxes (F3). The same `bin()` drawing recurs on every face but F2 and F3 (which are about MATERIAL, not bins, and never read `loc.bins`).

### F1 : Which Bin? (K, CODE `layout:'which'`)
**Move:** DECIDE for ONE thing at a time: six numbered rows, each one product and the N mini bins in the FIXED locale order; the child circles ONE bin per row; a key strip at the top shows the N bins with their words once (the panel's "circle 1 of N bin icons"; es-MX N = 2 stays a task). **Layout:** `keyBins` 112 + 10 · rows `repeat(6, minmax(85px, 1fr))` gap 8 = 6 × 85 + 5 × 8 = 550 → stack **672 <= 677** (at 722 rows open to 93); row grid `30 72 1fr 330`, chip tiles 64 × 80 (bin 44 × 66; 80 + 4 border = 84 <= 85), product tile 72 / icon 60. **Config d2** `{layout:'which', rows:6, tile:72, icon:60, chipTile:64, chipBin:44, keyPx:15, shuffleChips:false}`; d1 `{rows:4, tile:84, icon:72}`; d3 `{rows:8, shuffleChips:true, chipTile:56, chipBin:40}` (the G1 register; unpublished). **Composer:** 6 items from >= 4 distinct bins (N = 2: 3 + 3), no two same `family` or `vocabKey`, no two consecutive rows with the same answer bin; excluded-for-locale items re-drawn per slot as on the base. **Verify:** N chips per row in `loc.bins` order (`data-lcs-chip-order` === the key order at d2); exactly one chip per row whose key === `bin(item, loc)` (stamped `data-lcs-bin` on the row); no pre-marked chip; no run of 2 equal answers; pictures complete, icon >= 60; chip tile >= 64; marks === the locale table index. **Query face:** en "which bin worksheet" (verbatim) · de "Welche Tonne?" · fr "dans quelle poubelle" · es "¿en qué bote va?" · da "hvilken skraldespand" · no "hva skal hvor" (verbatim) · fi "mihin astiaan".

### F2 : What Is It Made Of? (G1, CODE `layout:'write'`)
**Move:** NAME the material: eight product rows, a bank of the locale's material words on top; the child WRITES the material (not the object's name) on a school-line row. Distinct from G1-244 (writes the picture's NAME, optional letter boxes) and from the base (a written word, no line to a bin). es re-target (panel-confirmed): the bank is `reciclable` / `orgánico`. **Layout:** `wordBank` 59 + 10 · rows `repeat(8, minmax(62px, 1fr))` gap 8 = 8 × 62 + 56 = 552 → stack **621 <= 677** (at 722 rows open to 74); row grid `30 64 1fr` with `writingRow({w:400, h:58, glyphH:30, xHeight:true})` right-aligned (58 + 4 border = 62). **Config d2** `{layout:'write', rows:8, tile:64, icon:52, rulingW:400, glyphH:30, bankPx:18, bank:true}`; d1 `{rows:6, icon:72, glyphH:36, rulingW:360}`; d3 `{rows:8, bank:false}` (retrieval; unpublished). **Composer:** pool = items whose `material` has a `materialWords` entry (default paper, cardboard, glass, plastic, metal; organic only if the panel authors a single-token word, OPEN 3: de "Bioabfall", sv "matavfall", fi "biojäte"; en "food scraps" is two words, excluded); each DISTINCT bank word used >= 1 and <= 3 times; no two rows share `family` or `vocabKey`; bank = the distinct words of the locale (es 2; most 4-5; cardboard may share the paper word), `rng.shuffle`d until it differs from the row order and its reverse (the G1-244 idiom). **Verify:** `data-lcs-word` per row === `loc.materialWords[item.material]`; the bank set === the set of row words; no text node inside a row except the badge; bank order !== row order; every word matches `^\p{L}[\p{L}'’-]*$` and `[...word].length <= 12` (a 12-grapheme word at glyphH 30 is ~290 px on the 400 ruling *est.*; de "Kunststoff" 10 fits, the panel prefers "Plastik" 7); bank one row (`getClientRects().length === 1`, m worst 502.7 <= 663); icons >= 52. **Refusals:** none measured: the packaging-material pool is 4 + 2 + 6 + 8 + 4 = 24 items over >= 20 families in every locale; es pool 37 with a 2-word bank (copy says "two words"). **Query face:** "what is it made of worksheet" · de "Woraus ist es? Material" · fr "en quelle matière" · it "di che materiale è fatto" · pt "de que material é feito" · sv "vilket material".

### F3 : Odd One Out by Material (G2, CODE `layout:'odd'`)
**Move:** COMPARE four DIFFERENT products; three share a material, one does not; circle it. Distinct from K-043 (three identical pictures + one other noun, one theme): every picture differs and the shared property is invisible, so the child reasons "glass, glass, glass, plastic". Band G2 by density: 16 pictures exceed the G1 ceiling 12 and sit in the G2 8-16 band (the design's G1 label is corrected). **Layout:** `cardGrid({cards, cols:1, rows:4})`: rows `minmax(156px, 1fr)` gap 14 = 4 × 156 + 42 = **666 <= 677** (at 722 rows open to 170); card padding 12 → stage >= 132 = box 120 + 2 × 6; four 120 × 120 boxes `space-evenly` inside 651. **Config d2** `{layout:'odd', rows:4, items:4, box:120, icon:84, organicOdd:true}`; d1 `{rows:3, icon:92}`; d3 `{rows:5, box:100, icon:72, organicOdd:false}` (the odd one never organic; unpublished). **Composer:** material classes for the trio: paper+cardboard (6 items / 5 families), glass (6 / 6), plastic (8 / 8), metal (4 / 4), organic (13 / 13): every class >= 3 families (m). Trio class distinct across the 4 rows (5 classes for 4 rows); odd class != trio class; odd index not equal on two consecutive rows; no `family` or `vocabKey` repeat on the page; `paper` + `letter` never in one trio (both flatten in mono). Locale-neutral: this face never reads `loc.bins` (material, not bin), so a sv "metal is packaging-only" rule does not touch it and a `nut` + `bolt` + `saucepan` trio is legal everywhere. **Verify:** exactly 3 equal `data-lcs-material` + 1 different per row; `data-lcs-odd="1"` on that one only; 4 distinct `vocabKey`s per row; odd indices not all equal; all icons the same rendered size >= 84 (the K-043 leak rule: the odd one is never the odd SIZE); no text in the rows. **Refusals:** none (no locale data). fi title uses the standard "mikä ei kuulu joukkoon" under the "Kierrätys:" head (K-043's fi title is K-band; unique-in-band holds). **Query face:** "odd one out materials" · de "Was passt nicht? Materialien Klasse 2" · fr "l'intrus matières" · it "l'intruso materiali classe seconda".

### F4 : Color the Bins (K, CODE `layout:'color'`; the CONTINGENT face)
**Move:** READ a bin word on a white bin, find it in the legend (colour chip + colour WORD + bin word) and colour the bin in the LOCALE's bin colour; under each bin a shelf shows two correctly sorted example pictures, so the finished page is the class's own bin sign (it "cartelli", pt "cores das lixeiras"). Genuine ONLY because bin colours are locale DATA (a national convention the panel confirms): a page coloured by an arbitrary class key is K-241's colour-by-code with bins for shapes, which the Boundary forbids. Grey (de Rest / nl Rest / fr Ordures / es Inorgánico / it Secco) = the `inkSoft` token (`codeColors` has no grey, m); its colour word is a panel literal ("grau" / "grijs" / "gris" / "grigio"; `COLOR_WORDS` has no grey, m). **Layout:** `legendChips` 59 + 10 · `binRow` with `bin({w:117, h:260, fill:'none'})` (pills 30 + 260) · 8 · `exampleShelf` 64 → stack **431 <= 677**, container `justify-content:center` (slack above and below; the page stays generous); crayon area = the whole bin (body 84 × 182 at 117 wide + lid + collar + handle); N = 2 (es-MX): bins 210 × 260. **Config d2** `{layout:'color', bins:'locale', binH:260, examples:2, pic:56, legendPx:17, legendOrder:'shuffled'}`; d1 `{examples:1, binH:300}`; d3 `{binPills:false, examples:2}` (the pills leave the bins; a 56 px key strip of lid-filled bins with words sits above the legend, so the child must READ the legend word and find the bin by its mark: G1 register; unpublished). Legend order != bin order in >= 3 positions (N = 2: swapped). **Verify:** N fill groups with distinct `data-lcs-fill` keys === the legend keys === `loc.bins` keys; every part `fill` white (poison: a leaked lid colour); no mark drawn; legend colour keys resolve `codeColors` ∪ {inkSoft} and are distinct within the page; every example's `bin(item, loc)` === its bin, the two examples of a bin from distinct families, no example repeated on the page; pictures >= 56; legend order !== bin order. **Refusals (per locale, RULED):** the face requires `colorConvention:'national'` + a colour token AND a colour word for every bin, distinct tokens within the locale. de (blau / gelb / braun / grün / grau), es-MX (SEP verde / gris), pt (CONAMA 275), fr (Citeo jaune / vert / marron / gris), fi (HSY lid colours): **confirmed by the pedagogy panel, expected 6 rows.** it: the UNI 11686 colour set (blu carta / giallo plastica / verde vetro / marrone umido / grigio indifferenziato) is the national standard comuni adopt; the it panel confirms it `[NSR]`, else refuses. nl: papier blauw / GFT groen / PMD oranje / rest grijs are the national code; `glas` has no distinct token unless the panel fixes one (OPEN 5), else refused. sv / da / no: the common Nordic pictogram system (Dansk Affaldsforening 2017, adopted by Avfall Sverige and Avfall Norge in 2020) carries a colour per fraction; each panel maps its five to distinct `codeColors` tokens `[NSR]`, else refuses (a national colour with no distinct token, e.g. two blues, is a refusal, never a nearest-hue fudge that teaches a wrong colour). **en: REFUSED** (no national convention in the US; a `classroom` key would make the page K-241's move; recorded `refuse:['color']`, hub 5). A `classroom` convention is NOT accepted anywhere (the pedagogy's classroom option is withdrawn; the operator may overrule per locale, then the landing carries "the colours on this page are our class key" and the expectation rises by one). **Query face:** "color the recycling bins" (US spelling) · de "Mülltonnen ausmalen" · pt "lixeiras coleta seletiva para imprimir e colorir" (verbatim) · it "cartelli raccolta differenziata da stampare" (verbatim) · fr "colorie les poubelles de tri" · sv / da "symboler".

### F5 : Three Things I Recycle (G1, CODE `layout:'open'`)
**Move:** OWN it: draw three things you recycle at home and write what each is (open; no verify; the pt / es / fr "je trie" reflection page). **Layout:** three `recycleLane`s `repeat(3, minmax(196px, 1fr))` gap 12 = 3 × 196 + 24 = **612 <= 677** (at 722 lanes open to 232; the grid centred); lane inner 651 = badge 30 + 12 + `drawBox` 170 × 150 + 12 + `rulingBlock` 2 × 56 (glyphH 36, gap 8; 2 × 56 + 8 + ~6 wrapper = ~126 *est.*) 400 wide = 624 <= 651. **Config d2** `{layout:'open', lanes:3, draw:{w:170, h:150}, rows:2, rulingW:400, glyphH:36, starters:false}`; d1 `{rows:1, draw:{w:220, h:170}}` (draw-led); d3 `{rows:3, draw:{w:120, h:150}, rulingW:450, starters:true}` (writing-led; 30 + 12 + 120 + 12 + 450 = 624; unpublished). **Starters ruled OFF at d2:** `starterFontPx({h:56, glyphH:36})` sizes a stem from the measured x-band (`components-b2.js:69-75`, m), roughly 36 px for this ruling *est.*; the eleven candidate stems measured at Nunito 700 22 (m: "I recycle" 83.7 · "Ich recycle" 106.9 · "Jag återvinner" 144.2 · "Jeg genbruger" 145.5 · "Jeg resirkulerer" 156.7) scale to ~137-256 px, above the 0.35 × 400 = 140 px budget in five locales, and fi's stem forces a partitive the child would not write. A panel MAY author `starters` (1 literal, reused per lane) only if the gate measures it <= 140 px at the metric size; empty is the default and the honest one. **Verify:** none (open); lints only: one empty `[data-lcs-drawbox]` + two empty `[data-lcs-ruling-row]` per lane; no text inside a lane but the badge (and a starter, if authored); `qa/verify-ruling-starters.js --quick` discovers any `[data-lcs-starter]` by `build()`. **Refusals:** none. sv title never starts with "Sortera" (panel candidate "Källsortering: tre saker hemma", OPEN 8). **Query face:** "things I recycle" · de "Das trenne ich" · fr "je trie mes déchets" · es "tres cosas que reciclo" · pt "três coisas que eu reciclo".

**Rejected non-moves.** A theme swap (the type is themeless) · "recyclable yes / no" as a face (that is es-MX's 2-bin base, valid only there) · a da "10 fraktioner" face (the da panel REFUSES it) · sort by natural / man-made or living (K-214 / K-201) · a picture-NAME writing face (G1-244) · letter boxes on F2 (the `write-the-word` look; gives the grapheme count away) · relabelling d1 (six items) or d3 (ten items) of the base as a face (same move) · a colour-by-code where the code is a number (K-266 family) · a "what happens to it" sequence face (no art; the library draws products, not processes) · a cut-and-glue sort (K-324 owns cut-and-play) · a classroom-key F4 in locales without a national convention (K-241 in disguise).

## 4 Native rebuild plan x11

Frames NEVER inflect: no cloze frame, no printed noun, no `objForms` read. Per locale the panel authors: `strings` (6 titles + 6 instructions = 12; the EN is a SOURCE TO AUDIT → `enAudit`), `bins` (2-5 × {key, label <= 15 chars (measured <= 151 px at render), longLabel, color token | null, colorWord literal | null, materials[], packagingOnly}), `route` (per-item overrides with a reason), `excludeItems` (with a reason), `materialWords` (2-6, single token, <= 12 graphemes), `colorConvention` (`'national'` | null), `refuse[]`, `starters` (optional, <= 140 px), the landing `scheme` sentence, `enAudit`. Every panel OPENS every picture its bins keep (the bank's `picOpened` is the global gate; the locale's `route` / `excludeItems` decisions are per-picture).

| loc | literals (counts) | forms / slots | refusal | traps |
|---|---|---|---|---|
| en (US) | 12 strings · 5 bins × 4 = 20 · 5 materialWords (paper cardboard glass plastic metal) = ~37 | none (no colour words: F4 refused) | **F4 REFUSED** (no national colour convention) | title never "Recycling" alone; "color" / "colour" US; "printable" belongs to the meta; NGSS in prose only; "with answers" never promised (printable-only decks ship no key) |
| de | 12 · 5 × 4 · 4-5 materialWords (Papier Pappe Glas Plastik Metall; Bioabfall optional) · 5 colorWords (blau gelb braun grün grau) = ~47 | nouns keep the capital on bank words (the panel decides "Papier" vs "papier" for a K-1 writing bank) | none; `saucepan` / `bolt` / `nut` in `excludeItems` (Wertstoffhof, no bin on the page); `toothbrush` / `comb` / `spoon` / `bucket` route to Rest | "Wohin kommt es, wenn es leer ist?" is the instruction frame; bin word "Glas" = the vocab noun of `jar` (fine: it is the material); `nut` vocab "Mutter" = mother (prose trap only; nothing printed); "Gelber Sack" 114.3 fits |
| es (MX) | 12 · 2 × 4 · 2 materialWords (reciclable orgánico) · 2 colorWords (verde gris) = ~26 | none | none: base / F1 / F4 at N = 2 stay tasks (copy says "two bins"); F2 re-target confirmed | "basura" child word, "residuos" adult; bins "botes"; the landing states SEP 2 bins vs CDMX 4; `pepper` picture = a bell pepper (opened; "Pimiento" in prose only, OPEN 10) |
| pt (BR) | 12 · 5 × 4 · 5 materialWords (papel papelão vidro plástico metal) · 5 colorWords (azul vermelho verde amarelo marrom) = ~47 | none | none | "lixeira" never "caixote"; "reciclagem" only beside "coleta seletiva"; EF01CI01 in prose; "atividade" never in a title; `saucepan` / `bolt` / `nut` → Metal (CONAMA takes small metal) |
| fr | 12 · 4 × 4 · 5 materialWords (papier carton verre plastique métal) · 4 colorWords (jaune vert marron gris) = ~41 | NBSP before `?` / `:` | none; OPEN 4 (3 national bins vs 4 with biodéchets: if 3, organic routes to Ordures, which a K teacher will contest; the panel rules) | `science-sort` fr = "Tri en sciences": no instruction may read "trie en sciences"; "poubelle jaune" = paper AND packaging; pill "Emballages" 111.7 (never "Poubelle grise" 132.6 next to it); Belgian PMC bleu out of scope |
| it | 12 · 5 × 4 · 5 materialWords (carta cartone vetro plastica metallo) · 5 colorWords (blu giallo verde marrone grigio) = ~47 | "differenziata" agrees with "raccolta" | F4 only on the UNI 11686 set the panel confirms `[NSR]`, else `refuse:['color']` | never "riciclaggio"; "scheda" never in a title; pill "Secco" (68.4) not "Indifferenziato" (140.3 passes the budget but crowds "Umido" at 107); the landing says colours vary by comune and names the standard |
| nl | 12 · 5 × 4 · 5 materialWords (papier karton glas plastic metaal) · 5 colorWords = ~47 | de/het only in prose | F4 contingent on a distinct `glas` token (OPEN 5), else `refuse:['color']` | "de bak" / "het glas" / "het papier" in prose; IJ never arises in the bank words; the engine appends "werkblad" |
| sv | 12 · 5 × 4 · 5 materialWords (papper kartong glas plast metall) · 5 colorWords if the pictogram colours map to distinct tokens = ~42-47 | indefinite forms only on the sheet | F4 contingent on the Nordic pictogram colours `[NSR]`, else `refuse:['color']`; metall pool = 1 (`can` on every page; validator WARN) | title the compound noun, never "Sortera …"; "Burk" is the vocab word of BOTH `jar` and `can` (prose trap; nothing printed); "Pappersförpackningar" 195.8 REFUSED as a pill (the panel writes "Papper"); non-packaging plastic + metal EXCLUDED (no restavfall among the 5) |
| da | 12 · 5 × 4 · 5 materialWords · 5 colorWords if the pictogram colours map = ~42-47 | definite forms only in prose | "10 fraktioner" face REFUSED by the panel (not designed); F4 contingent `[NSR]`; OPEN 7 pap into papir (else `cardboard` in `excludeItems`) | "genbrug" out of titles; "hvilken skraldespand" is the F1 head; the engine appends "opgaver"; pill "Madaffald" 103.1 |
| no | 12 · 5 × 4 · 5 materialWords (papir papp glass plast metall) · 5 colorWords per the pictogram key = ~43-47 `[NSR-FLAG][no]` | bokmål only | F4 contingent `[NSR]`; OPEN 9: a Norwegian teacher confirms glass og metall as ONE bin + the five literals before the bank is authored; small metal (`saucepan` / `bolt` / `nut`) → glass og metall or excluded (panel) | "hva skal hvor" in the meta; "dunk" is the F1 word; pill "Glass/metall" 118.9 (not "Glass og metall" 140.0 beside "Matavfall" 96.4 = 118 half-sum, legal but tight) |
| fi | 12 · 5 × 4 · 5 materialWords (paperi pahvi lasi muovi metalli) · 5 colorWords (HSY lid colours, the panel confirms) = ~47 | illative in instructions ("Lajittele jätteet oikeaan astiaan.", "Ympyröi astia, johon roska kuuluu.") | none; OPEN 7 kartonki merged into "Paperi" (`longLabel` "Paperi ja kartonki"); non-packaging plastic EXCLUDED (no sekajäte) | title NEVER "Lajittelu" (validator regex on the head token); the 4-line fi title = the 677 budget: every face survives it by the §3 arithmetic |

Every panel OPENS every picture its bins keep (the 37 at `cache/themes/<theme>/<noun>@3x.webp`), audits the EN source (`enAudit`), and never re-adds an id from `_excluded`.

## 5 Data + gates

`data/b4/recycling.json` (GLOBAL item bank, hand-authored, `picOpened:true` per member, 37 items) + `data/b4/recycling.js` (the en locale block) + `data/b4/locales/recycling.<loc>.json` ×10, GENERATED from `i18n/.draft-b4-<loc>.json` by `tools/apply-b4-locale.js` after `tools/validate-b4-draft.js`; read via `lib/b4-common.js bank('recycling', loc)` (README open item 3; `data/` is gitignored, `git add -f`).

```
RECYCLING (data/b4/recycling.json; every picture OPENED 2026-09-21 by the pedagogy AND re-opened by the editor where disputed):
  materials: ['paper','cardboard','glass','plastic','metal','organic']            // 'ceramic' and 'rest' are NOT materials
  items: [ {id, theme, noun, vocabKey, material, packaging, family, picOpened:true, note}, ... ]
    paper     np: classroom/paper (lined sheet; pale in mono) · classroom/notebook   pk: post office/envelope · post office/letter (family 'env')
    cardboard pk: post office/package (taped box) · At the Supermarket/cereal (box with a face)
    glass     pk: kitchen tools/jar (blue jar) · At the Supermarket/jam (jar, cloth cover) · breakfast/honey (jar, wooden lid) ·
                  At the Supermarket/milk (glass milk bottle, red cap) · At the Supermarket/oil (corked bottle) · At the Supermarket/sauce (red bottle; family 'redbottle')
    plastic   pk: At the Supermarket/shampoo (pump bottle) · At the Supermarket/water (PET) · beach/sunscreen (tube) · classroom/glue (squeeze bottle)
              np: At the Supermarket/toothbrush (bare brush) · beach/bucket (toy bucket; OPEN 2) · around the house/comb · kitchen tools/spoon (purple plastic)
    metal     pk: At the Supermarket/can (tin)   np: kitchen tools/saucepan (all-metal, metal handle) · tools/bolt (brass) · tools/nut (hex nut)
    organic   np: fruits/apple · fruits/banana · At the Supermarket/bread · vegetables/carrot · fruits/orange · spring/leaf · vegetables/corn ·
                  fruits/pear · vegetables/lettuce · vegetables/potato · vegetables/tomato · fruits/strawberry · At the Supermarket/pepper (bell pepper)
  _excluded (opened, with the reason; the panels read this and never re-add):
    AMBIGUOUS: kitchen tools/bottle (glass or PET, metal-look cap: the editor re-opened, OPEN 1 CLOSED as excluded) · At the Supermarket/juice (glass or PET) ·
      At the Supermarket/soda · At the Supermarket/bag (paper or laminated) · classroom/folder (card or plastic) · kitchen tools/lid · kitchen tools/colander ·
      kitchen tools/pot (enamel or ceramic) · At the Supermarket/salt (baked "SALT") · post office/stamp (baked text, tiny) · At the Supermarket/egg (eggs IN a pulp carton)
    COMPOSITE (never signed): kitchen tools/pan (pot + glass lid) · around the house/kettle (metal + plastic handle) · kitchen tools/fork (steel + green handle) ·
      knife · whisk · grater · tongs · spatula · ladle · sponge · around the house/toothpaste · classroom/binder · around the house/scissors · tools/screwdriver · tools/hammer
    CERAMIC (the lock: not a material on any page): kitchen tools/cup · mug · bowl · pitcher · jug · plate · around the house/vase
    NOT WASTE: classroom/book · around the house/key · camping/map · kitchen tools/glass (a DRINKING glass: Restmüll, not Altglas) · around the house/glass (a WINE glass)
    VOCAB TRAP: tools/nail = the FINGERNAIL in es/pt/fr/it/da/no/fi (Uña / Unha / Ongle / Unghia / Negl / Kynsi, m) though the picture is a spike
RECYCLING_LOC[loc] = {
  bins: [ {key, label (<= 15 chars; the pill), longLabel, color: 'codeBlue'|...|'inkSoft'|null, colorWord: literal|null,
           materials: ['paper','cardboard',...], packagingOnly: bool} ],            // 2..5, ORDER = page order = mark index
  route:        { toothbrush:{bin:'rest', reason:'…'} },                              // audited overrides, may be empty
  excludeItems: { saucepan:'Wertstoffhof: no bin on the page' },                       // per-locale, with a reason
  materialWords:{ paper:'paper', cardboard:'cardboard', glass:'glass', plastic:'plastic', metal:'metal' }, // F2; organic optional; es: 2 values
  colorConvention: 'national'|null,                                                  // F4 gate; 'classroom' is NOT accepted
  refuse: [],                                                                        // 'which'|'write'|'odd'|'color'|'open'
  starters: [],                                                                      // F5 stem, optional; gate-measured <= 140 px
  scheme: '…',                                                                       // the landing sentence (es SEP 2 vs CDMX 4; da 5 of 10; no [NSR])
  enAudit: '…',                                                                      // the panel's findings on the EN source (non-EN required)
  strings: { 'K-357':{title, instruction}, <F1..F5 ids>:{title, instruction} } }
```
Helper contract: `bin(item, loc)` (pure; `route` > first matching bin in page order > `null` = excluded for this locale); `poolFor(binKey, loc)`; `materialPool(loc)` (F2); `trioClasses()` (F3; locale-free); `colorFor(key)` resolves `codeColors[key] ?? (key === 'inkSoft' ? color.inkSoft : throw)`.

**`tools/validate-b4-draft.js` (recycling block; every rule runs, exit 1 on any):**
1. Every item: `(theme, noun)` resolves in `cache/manifest.json` with a `vocabKey` that has a singular in ALL 11 locales (`entriesFor`), is not `B2_EXCLUDE`d, `picOpened === true`, `material` ∈ the 6, `packaging` boolean, `family` non-empty; no two items share a `vocabKey`; no item id from `_excluded`; bank size 37 (a change is a re-open + a note).
2. Per material class (paper+cardboard, glass, plastic, metal, organic): >= 3 distinct families (the F3 trio floor) and >= 1 packaging item for paper+cardboard / glass / plastic / metal.
3. Per locale: 2 <= bins <= 5; keys unique; `label` non-empty, <= 15 chars, no digit, no `{`; `longLabel` never on a pill; `materials` ⊂ the 6; `color` ∈ `codeColors` keys ∪ {inkSoft} ∪ {null}; `colorWord` non-empty IFF `color` non-null; `route[id].bin` is a bin of this locale whose `materials` include the item's material and (item.packaging || !packagingOnly); every material is either covered by >= 1 bin or every item of it is in `excludeItems` (es: all 6 covered by 2 bins).
4. Resolution: `bin(item, loc)` is unique (first-match over bin order; a second matching bin is a WARN, the first wins); every bin's resolved pool >= 1 (ERROR) and >= 2 (WARN with the bin key: sv metall is the known 1); every bin's pool holds >= 2 distinct families (F4 examples).
5. F2: `materialWords` keys ⊂ materials; >= 2 distinct values; each value matches `^\p{L}[\p{L}'’-]*$` and `[...v].length <= 12`; the F2 pool >= 8 items over >= 8 families, else `refuse` names `write`.
6. F4: for every bin `color !== null` AND `colorWord` non-empty AND `colorConvention === 'national'` AND colours distinct within the locale, else `refuse` includes `color`; `colorConvention:'classroom'` is a FAIL (not accepted); en `refuse` includes `color`.
7. Titles <= 70, no `WORKSHEET_WORD` (the b3 regex), unique in the band; fi titles never match `/^\s*lajittelu/iu` and never equal `sorting-categories.name.fi` or `science-sort.name.fi`; sv titles never match `/^sortera/i`; it strings never contain "riciclaggio"; pt titles / metas containing "reciclagem" must also contain "coleta seletiva"; fr instructions never contain "en sciences"; da titles never contain "genbrug"; es titles never contain "residuos".
8. Instructions <= 150 chars, name only apparatus of their face (a lexicon per face: base line + bin; F1 circle + bin; F2 bank + line; F3 circle + row; F4 legend + colour + bin; F5 draw + write; a stray "tick" / "letter box" / "sticker" fails).
9. es: exactly 2 bins with keys `organico` / `inorganico` unless `panelOverride.bins` carries a reason (the lock).
10. `strings` ids === {base + the 5 allocated face ids}; the six `mode` strings are fixed (`base which write odd color open`); the six titles differ pairwise by >= 1 noun / verb (never adjective-only).
11. `enAudit` non-empty in every non-EN block; `starters` (if any) single literal, no `{`, <= 40 chars (the px check is the render gate's).
12. Label WIDTH pre-check by a char-width table is NOT attempted (fonts are measured at render only); a label > 15 chars fails here, the rest is rule R1 below.

**`qa/verify-b4-recycling.js` (render gate; clone the b3 shape):** renders base + F1..F5 × 11 locales × d2 through `render/render-instance.js` (file:// fonts) under the 722 stack (3-line de title + 150-char instruction) AND the 677 stack (4-line fi title); asserts `verify()` empty, `qa/lints.js` clean, then the floors ITSELF (no size lint exists): icons >= 60 on the base and F1, >= 56 pictures on F4, >= 52 on F2, >= 84 on F3; base bins 117 × 176 (210 at N = 2) with a pill >= 17 px; **R1 pill widths measured by `getBoundingClientRect` in every locale: <= 151 and adjacent half-sums <= 125** (FAIL names the locale + label); F1 key pills at 15 px under the same rule; F1 chip tiles >= 64 with a bin >= 44 wide and mark features >= 2 px; legend swatches 22 with words >= 17; bank one client rect; every `[data-lcs-answer]` absent; base: every tile's stamped bin present, each bin >= `perBinMin`, no bin's tiles adjacent, no `family` / `vocabKey` repeat; F1: one correct chip per row, order === `loc.bins`; F2: bank set === row word set, bank order != row order, no word text inside a row; F3: 3 + 1 per row, 4 distinct `vocabKey`s, equal icon sizes; F4: every part `fill` white, no mark, legend colours distinct, examples correctly binned; F5: 3 empty drawboxes + 6 empty ruling rows, starters (if any) <= 140 px; every `img` complete and `src` === `fileUri` of a bank item; everything above the footer. SWEEP: 20 seeds × d2 per face render distinct pages (base item sets vary and every 5-bin locale shows >= 3 different items per bin across the sweep except sv metall; F3 odd indices vary; F4 legend orders vary). Greyscale render of the base, F1 and F4 (`filter:grayscale(1)`) saved for the critic.

**Poison (each must FAIL for its OWN reason; the correct EN draft is the control):** P1 an item with `packaging` missing → rule 1. P2 `{theme:'kitchen tools', noun:'bottle', material:'glass', picOpened:true}` → rule 1 (in `_excluded`). P3 a second item with `vocabKey:'apple'` (`At the Supermarket/apple`) → rule 1. P4 de bins with 6 entries → rule 3. P5 de `route.toothbrush = {bin:'verpackung'}` (non-packaging into a `packagingOnly` bin) → rule 3. P6 sv `plast` with `materials:[]` → rule 4 (pool 0). P7 en `materialWords` all `'paper'` → rule 5 (1 distinct). P8 nl GFT and Glas both `codeGreen` with `colorConvention:'national'` → rule 6. P9 de `colorConvention:'classroom'` → rule 6. P10 fi base title `'Lajittelu'` → rule 7. P11 sv F1 title `'Sortera soporna'` → rule 7. P12 fr base instruction `'Trie en sciences les déchets.'` → rule 7. P13 F1 instruction `'Tick the bin it goes in.'` → rule 8. P14 es bins = 3 without `panelOverride` → rule 9. P15 F2 title `'Recycling Sort: Big Bins'` vs F4 `'Recycling Sort: Color the Bins'` → rule 10. **Render poisons:** PR1 base strip with the two Bio tiles adjacent → position leak. PR2 base with `envelope` + `letter` on one page → family. PR3 F1 with two consecutive rows answering the same bin → run. PR4 F2 row printing its word → "word printed". PR5 F3 row with 2 glass + 2 plastic → "not 3 + 1". PR6 F4 lid pre-filled `codeBlue` → "part not white". PR7 an `<img src>` from `pictureFor` (`At the Supermarket/apple` for `fruits/apple`) → "src not the opened picture". PR8 sv page with `saucepan` in the strip → "item excluded for locale". PR9 sv pill `Pappersförpackningar` (195.8) → R1 width. PR10 it pills `Indifferenziato` next to `Plastica` moved adjacent (70.2 + 42.2 = 112 passes) vs `Indifferenziato` next to a synthetic `Verpackungen` (70.2 + 67.1 = 137) → R1 adjacency. PR11 base d2 rendered with `perBinMax` read off the level index → the config guard fires before render. PR12 F1 rendered at `minmax(90px,1fr)` under the 677 stack → footer intrusion (702 > 677). PR13 F4 with a mark drawn on a `fill:'none'` bin → "mark on colour face". PR14 F5 starter "Jeg resirkulerer" at the metric size → > 140 px. PR15 F3 with the odd tile rendered at 92 → "unequal icon size".

**Page reads:** `RECYCLING`, `RECYCLING_LOC[loc]`, `fileUri`, tokens, `primitives/bin.js`; never `image-vocabulary.js`, `objForms`, `COLOR_WORDS` (the colour words are panel literals because grey is missing) or `approved-words-*.json` at render.

## 6 SEO plan

| face | title pattern (Germanic / Romance / Nordic + fi) | meta MIDDLE (whole 120-170; the child's instruction) | coordinate |
|---|---|---|---|
| base | Recycling Sort · Mülltrennung · Afval scheiden / Separación de basura · Coleta seletiva · Le tri des déchets · Raccolta differenziata / Källsortering · Affaldssortering · Kildesortering · Kierrätys | Each thing is empty or finished: draw a line from it to the bin it goes in (77) | `{type:'recycling', mode:'base', theme:'', level:K}` |
| F1 | Recycling Sort: Which Bin? · Mülltrennung: Welche Tonne? · Afval scheiden: welke bak? / Separación de basura: ¿en qué bote va? · Coleta seletiva: qual lixeira? · Le tri des déchets : quelle poubelle ? · Raccolta differenziata: in quale bidone? / Källsortering: vilket kärl? · Affaldssortering: hvilken skraldespand? · Kildesortering: hva skal hvor? · Kierrätys: mihin astiaan? | Look at each thing and circle the bin it goes in when it is empty or finished (74) | `mode:'which', level:K` |
| F2 | Recycling Sort: What Is It Made Of? · Mülltrennung: Woraus ist es? · Afval scheiden: waar is het van gemaakt? / Separación de basura: ¿reciclable u orgánico? · Coleta seletiva: de que é feito? · Le tri des déchets : en quelle matière ? · Raccolta differenziata: di che materiale è? / Källsortering: vilket material? · Affaldssortering: hvad er det lavet af? · Kildesortering: hva er det laget av? · Kierrätys: mitä materiaalia? | What is each thing made of? Write the word from the bank on the line (68) | `mode:'write', level:G1` |
| F3 | Recycling Sort: Odd One Out · Mülltrennung: Was passt nicht? · Afval scheiden: welke hoort er niet bij? / Separación de basura: ¿cuál no va? · Coleta seletiva: qual é o diferente? · Le tri des déchets : l'intrus · Raccolta differenziata: l'intruso / Källsortering: vilken passar inte? · Affaldssortering: hvad passer ikke? · Kildesortering: hva passer ikke? · Kierrätys: mikä ei kuulu joukkoon? | In each row three things are made of the same material: circle the one that is different (89) | `mode:'odd', level:G2` |
| F4 | Recycling Sort: Color the Bins (en REFUSED, no landing) · Mülltrennung: Tonnen ausmalen · Afval scheiden: kleur de bakken / Separación de basura: colorea los botes · Coleta seletiva: pinte as lixeiras · Le tri des déchets : colorie les poubelles · Raccolta differenziata: colora i bidoni / Källsortering: färglägg kärlen · Affaldssortering: farvelæg spandene · Kildesortering: fargelegg dunkene · Kierrätys: väritä astiat | Read the name on each bin and color it in the color the legend shows (66) | `mode:'color', level:K` |
| F5 | Recycling Sort: Three Things I Recycle · Mülltrennung: Das trenne ich · Afval scheiden: drie dingen die ik scheid / Separación de basura: tres cosas que reciclo · Coleta seletiva: três coisas que eu reciclo · Le tri des déchets : je trie · Raccolta differenziata: tre cose che riciclo / Källsortering: tre saker hemma · Affaldssortering: tre ting jeg sorterer · Kildesortering: tre ting jeg sorterer · Kierrätys: kolme asiaa, jotka kierrätän | Draw three things you recycle at home and write what each one is (62) | `mode:'open', level:G1` |

All titles are candidates the native panels rewrite (<= 70, no worksheet word; base = the bare head; each face adds one NOUN / verb, never an adjective). `coordinate.mode` is ALWAYS the face's mode string (base = `'base'`), `coordinate.theme:''`, level = the locale's band key of §1 (K for base / F1 / F4, G1 for F2 / F5, G2 for F3). `<title>` = `l.title || l.h1`; h1 = the title; JSON-LD `LearningResource` with NO `educationalAlignment` on any face (`teaches` = "Recycling sort (readiness)" localized); en prose names NGSS K-ESS3-3 on base / F1 / F5; every non-EN landing names the framework + the strand literal of §1 (the build adds the Science row to `strand-names.ts`, never the English fallback); pt cites EF01CI01 in prose; es states the SEP 2-bin model; every sv landing says the plastic and metal bins take packaging; every F4 landing names the national colour standard it follows (de Landkreis note, it UNI 11686 + "colours vary by comune", nl / sv / da / no the pictogram system). Meta lead = `seo.words.free_printable` (metadata ONLY; never a visible field; the visible free-claim lint runs on title / instruction / h1 / eyebrow / body / hub cards per locale). `topicMeta.recycling` >= 50 chars ×11 + `skill-sentences.<loc>.json.recycling` via the `register-b4-en-content.js` clone; the per-locale MIDDLE window measured with `tools/measure-instruction-window.js` on the staged ZIPs.

**Non-cannibalisation** (whole-landing 3-gram Jaccard, `scripts/seo-landing/gate.js` FAIL >= 0.80 / WARN 0.65; *est.*, the gate measures):

| pair | what the copy says differently | est. |
|---|---|---|
| base vs K-214 / K-235 / K-207 / K-211 (science-sort, "draw a line from each picture to the group") | material + the locale's named bins + "empty or finished" vs a concept pair; the base p1 names the bins by word and never says "the group it belongs to" | 0.30 |
| base vs K-042 / G1-160 (`sorting-categories`, icon-labelled bins) | word-labelled drawn bins, different objects, a material key vs identical icons | 0.15 |
| base vs F1 | a line to a bin vs a mini bin per row, one decision per row; the F1 copy leads with "which bin" | 0.35 |
| F1 vs F4 | both name the bins; F1 is a decision per item, F4 is colouring by a legend with example pictures | 0.25 |
| F2 vs G1-244 (write the picture's NAME) | "made of" + material bank vs the picture's name; F2 never says "name of the picture" and has no letter boxes | 0.20 |
| F3 vs K-043 (odd one out by category, one theme) | four different objects, an invisible property (material), grade 2 vs identical pictures at K | 0.20 |
| F3 vs K-039 / K-044 (same-different, size) | material vs size / identity | 0.10 |
| F4 vs K-241 / K-266 / G1-242 (colour by code / number / sentence) | the code is a bin WORD in the locale's real, named colour standard; the shapes are bins with example pictures | 0.15 |
| F5 vs G1-342 (read and draw) / K-332 (draw the feeling face) | no sentence to read; "at home", "recycle"; three own objects | 0.15 |
| any face vs K-212 wants / needs, K-201 living, K-214 (shared `water`, `bread`, `apple`, `spoon`, `cup`) | the question is "where does it go"; the boundary sentence names it | 0.10 |
| any face vs the `science-sort` hub heads (fr "Tri en sciences", it "Classifichiamo le scienze", sv "Sortera i naturen", fi "Luonnontieteen lajittelu") | the head is the national waste-sorting term; no title or instruction echoes the hub head (rule 7) | 0.10 |

Boundary sentence on every landing: "The child sorts everyday things by what they are made of into the bins used where you live: paper, glass, plastic, metal and food. Sorting living things, natural things or needs is on the science-sort pages."

## 7 Hub visibility contract

A face appears under `recycling` on `/[locale]/worksheets` IFF all four hold (brief): (1) `apps.recycling` exists in `frontend/config/topics-taxonomy.json` (ABSENT today, m; missing = rendered NOWHERE in the rail) with `default_subject:'science'`; (2) `axes['exercise-type'].recycling` has `slug` + `name` in all 11 locales (§1 table B; the 11 slugs measured collision-free; fi name "Kierrätys", never "Lajittelu"); (3) exactly one landing per face per locale with `coordinate.type === 'recycling'` verbatim, `mode` per §6, `theme:''`, the level key of §1 (K for base / F1 / F4, G1 for F2 / F5, G2 for F3), a unique slug and `canonicalDeckSlug` = the published deck; a REFUSED face has NO landing and the expectation is lowered explicitly, never padded; (4) the landing JSON committed AND deployed (per-process cache).

Gate: `node scripts/verify-hub-type-rows.js --keys=recycling` (script EXISTS since nt20-C, m; expectations from `hub-expectations.json`; poison = drop one landing / drop `apps.recycling` / a wrong `coordinate.type` must FAIL).

**Expected rows per locale:** en **5** (F4 REFUSED: no national colour convention) · de 6 · es 6 · pt 6 · fr 6 · it 6 · nl 6 · sv 6 · da 6 · no 6 · fi 6 = **65** (ceiling 66). Confirmed at design time: de / es / pt / fr / fi F4 on panel-confirmed national colours; every other face in every locale on measured pools (base and F1 need only the bin set; F2 pools >= 24 over >= 20 families; F3 locale-free; F5 open). Contingent, each recorded in the draft before the wave: F4 in **it** (UNI 11686 set confirmed `[NSR]`), **nl** (a distinct `glas` token, OPEN 5), **sv / da / no** (the Nordic pictogram colours mapped to distinct tokens `[NSR]`) → any of the five withheld lowers that locale to 5 (**floor 60**); en rises to 6 only if the operator overrules the classroom-key ban (recorded, not expected). Not a refusal: sv metall pool 1 (a WARN), da pap / fi kartonki merges (OPEN 7; bank rows), fr 3 vs 4 bins (OPEN 4; N is data), no `[NSR-FLAG]` glass og metall (OPEN 9; the literals, not the face).
