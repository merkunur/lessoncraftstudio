# GAME 78 — "THE LANTERN FERRY" · 2.MD.D.10 read a picture GRAPH (data-extraction) + compare · EN pilot · NEW FAMILY (categorical data interpretation) · Grade-2 · MID-SIZE · RE-CENTERED on DATA-EXTRACTION + symbol-first comparison

## Status: DESIGN COMPLETE — 6-expert ensemble closed (R1 4 agents → R2 CC synthesis → R3 adversarial critic NEEDS-REWORK→RE-CENTER → all fixes baked; NO operator fork — the re-center is a correctness ruling + policy-aligned [symbol-first house pattern]). Catalog source `gapfill-21`. Signature engine `engine-graph.js` + `graph-core.js` (0 protected-core lines). EN pilot, non-EN 404-by-design. **The DOUBLE-road guards were right to flag, but 2.MD.D.10's COMPARE-operation is IRREDUCIBLY keyword/template-signaled [the standard's own natural language] AND the standard is honestly THIN → the genuinely-NEW cognition is DATA-EXTRACTION; the compare is OWNED arithmetic → symbol-first it. The beautiful pairing-model survives as a SCAFFOLD, not the gate.**

## Context — why this game, why now
ELA→math balance is healthy; 2.MD.D.10 is a fresh DATA strand (distinct from Game-71 line-plot). Genuinely UNOWNED by live (chart-count is a worksheet-gen PDF; sort-and-count is K.MD.B.3 classify-a-pile; line-plot is 2.MD.D.9 measurement). Grade 2. Per the standing policy this was flagged a DOUBLE cheat-road (read = concrete-model Game-74/76; operation = classify-from-language Game-75) and guarded from R1 — but the critic showed the OPERATION road can't be keyword-stripped without DOWNGRADING the standard (one-to-one correspondence = K.CC), so the honest path is the data-extraction re-center + symbol-first comparison.

## §1 — Concept / world
**The Lantern Ferry.** Mossglow Harbor; Pim the lighthouse-moth. Each creature-kind waits in its own labeled LANE on the dock — a row of discrete, countable creature-icons (each icon = 1; the picture graph). Pim asks the keeper to read who came home + settle gentle compares. The signature **pairing manipulative** (clip a rope between two lanes; the leftover unmatched creatures = the difference) survives as a beloved SCAFFOLD/manipulative the child can use to SEE a difference — but it is NOT the graded gate (per the rework). Non-numeric abundance vessel: a far shore softly accumulates window-lights as creatures cross home (never a count). `ctx.correct()` auto-advances.

## §2 — The standard + the load-bearing cognition
**2.MD.D.10 (Grade 2, SOLE primary)** — read a picture/bar graph (up to 4 categories, SINGLE-UNIT scale) + solve put-together/take-apart/compare problems using the data. **Cognition (pedagogy + critic, AS RE-CENTERED):** the genuinely-NEW graph-specific cognition is **DATA-EXTRACTION — locate the right 2-of-4 NAMED categories + read their DISCRETE counts off the display** (the compare-arithmetic [difference/sum] is OWNED — counting from K, within-20 from 1.OA/2.OA.B.2 — and is IRREDUCIBLY keyword/template-signaled by the standard's own language, so it is posed SYMBOL-FIRST [given, not keyword-chosen], not assessed as an operation-choice). **Representation:** DISCRETE picture graph (icon=1); segmented single-unit BAR (stacked unit-cells + gridlines) OK as a later rung; SMOOTH continuous bar BANNED; SCALED pictograph (each=2/5) OUT (3.MD.B.3, defer). **Counts: ≥6 with ≥1 value ≥10 per round-set (PAST the subitizing ceiling — small counts are subitizable [read without counting]).** **Question weighting:** difference + sum (values load-bearing) over extremum (most/fewest = tallest-column shortcut → scaffold only, except "how many more does the most have than the fewest" which forces both reads). **FENCES:** K.MD.B.3 sort-and-count (single-category-count, no display), 2.MD.D.9 line-plot (measurement number-line), bare whole-number compare (E4 — here the numbers come FROM the graph; if the child can answer without reading the display it's collapsed to E4), scaled pictograph (3.MD.B.3), smooth bar. Numbers/labels = real DOM/SVG `<text>`.

## §3 — THE ARCHITECTURE REWORK (the heart; the deepest flaw + fix — a RE-CENTER onto data-extraction)
**The critic's catch — the double-road guards (discrete-icons beat eyeball; pairing-model beats keyword) are well-built, but BELOW them: (B) the compare-operation is IRREDUCIBLY keyword-signaled, the standard is THIN, and the pairing is handed/ritual:**
- **(B) COUNT-BOTH-THEN-KEYWORD-SUBTRACT (FATAL to the compare-is-the-new-cognition claim).** "How many MORE X than Y" IRREDUCIBLY signals difference — that's English, not keyword-stuffing; a (template + answer-type) solver recovers the operation with ~prob 1 ("more"+"how many"→difference; "more"+"which"→argmax). A value-reading child counts A=5, B=3 (passes ROAD 1 honestly) + template→subtract → NEVER pairs. The decorrelation does NOT hold for natural Grade-2 questions; the keyword-FREE pairing framing ("pair them — how many left over?") only becomes load-bearing by QUIETLY RE-CENTERING the standard DOWN to one-to-one correspondence (K.CC.C.6 — a DOWNGRADE; the operation-from-phrasing IS part of 2.MD.D.10's intended demand, so stripping it DELETES a cognition the standard wants).
- **(A) AUTO-PAIR / RITUAL-PAIR (the pairing is handed or a crank).** Auto-pairing + glowing the leftovers HANDS the modeling (the child just counts — Game-76); manual pairing collapses to a MOTOR-RITUAL (pair everything, count strays — mechanically correct, zero comprehension) + a pre-spanned rope bypasses the data-extraction.
- **(c/e) THE STANDARD IS THIN.** The read is a pre-K count; the compare is owned arithmetic → the only genuinely-NEW cognition is DATA-EXTRACTION (locate the right 2-of-4 named columns + read discrete counts).
- **(deepest) SUBITIZE-AND-ICON-MATCH.** With ≤6 icons + the prompt carrying the category's ICON, the child SUBITIZES the column counts + matches prompt-icon→column-icon VISUALLY — never reading the label, never pairing. The scrambled-lane guard kills HEIGHT-eyeball but NOT COUNT-subitizing; the 4-category guard is defeated if the prompt shows the icon.

**THE FIX — RE-CENTER the grade onto DATA-EXTRACTION + SYMBOL-FIRST the owned comparison (the house pattern; the pairing demoted to scaffold; MID-SIZE):**
- **GRADED CORE 1 — DATA-EXTRACTION (the honest new cognition).** 4 categories; the question names the pair by **WORD ONLY (NO icon in the prompt)** so the child must READ the label → locate the column → read its count; the asked-pair ROTATES; a close-value distractor neighbor. `WRONG_CATEGORY_SOLVER` + `SUBITIZE_ICON_MATCH_SOLVER` must FAIL.
- **GRADED CORE 2 — read DISCRETE counts PAST the subitizing ceiling.** Counts ≥6, ≥1 value ≥10 per round-set, so reading is a deliberate COUNT not a subitized glance; scrambled lanes retained (no height-eyeball); `EYEBALL_SOLVER` + `SUBITIZE_SOLVER` must FAIL.
- **SYMBOL-FIRST comparison (the owned op, posed not assessed).** After extraction, the comparison is a GIVEN number sentence the child completes (read 8 and 5 → complete `8 − 5 = ☐`) — the operation is GIVEN, not keyword-chosen (honest: the standard's arithmetic-on-graph-values is asserted, the un-decorrelatable operation-CHOICE is NOT claimed as the assessment). `TEMPLATE_OPERATION_SOLVER` must FAIL on the graded surface (the graded answer needs the EXTRACTED values, not a template→op map).
- **PAIRING = optional SCAFFOLD/manipulative (its honest best role), NOT the gate.** The child MAY use the rope to SEE a difference; if any pairing round is graded it must be child-column-selected + child-driven (`AUTO_PAIR_SOLVER` + `RITUAL_PAIR_SOLVER` must fail). **BUILD-THE-GRAPH:** dropped from graded OR inverted to a COMPARE-CONSTRAINT construction ("build a graph where A is 3 more than B"; `TRANSCRIBE_SOLVER` must fail) — bare transcription is pre-K.

## §4 — Anti-cheat architecture (the measured gate)
NEW `verify-graph-core.js` (window-shim; descriptor `{categories:[{key,count}], askedCategories, questionType, scale:1, promptMode: word-only}` — NO stored answer, NO continuous-height field, NO icon in the prompt; the answer DERIVED from the extracted counts at grade-time). Solvers — ALL must FAIL:
- **`SUBITIZE_ICON_MATCH_SOLVER`** (NEW — the deepest) — match prompt-image→column-image + subitize counts, never read the label → FAILS via WORD-ONLY prompts (no icon) + counts past subitizing.
- **`SUBITIZE_SOLVER`** (NEW) — subitize column counts ≤5 → FAILS via counts ≥6 + ≥1 ≥10.
- **`TEMPLATE_OPERATION_SOLVER`** (NEW — the (template+answer-type) keyword cheat) → FAILS because the comparison is SYMBOL-FIRST (the op is given; the graded answer needs the extracted VALUES, not a template→op map).
- **`WRONG_CATEGORY_SOLVER`** (the asked-pair rotates; word-only label is the SOLE category signal) + **`EYEBALL_SOLVER`** (no continuous field) + **`AUTO_PAIR_SOLVER`** + **`RITUAL_PAIR_SOLVER`** (if pairing graded: child-selected + child-driven) + **`TRANSCRIBE_SOLVER`** (build-graph compare-constraint) + **RANDOM** — all FAIL.
- Structural asserts: `DATA_EXTRACTION_LOAD_BEARING` (word-only prompt, the label is the sole signal, asked-pair varies), `PAST_SUBITIZING` (counts ≥6, ≥1 ≥10), `DISCRETE_READ_NOT_CONTINUOUS`, `OPERATION_SYMBOL_FIRST_NOT_CHOSEN` (the op is given; no template→op map recovers the graded answer), `EXTREMUM_FAILS_ON_DIFFERENCE`, `VALUES_LOAD_BEARING` (the answer needs the extracted values), `ANSWER_DERIVED_NOT_STORED`, VARIETY ≥7.
- **Wrong-answer re-show (no-reveal):** re-show the LABEL→column mapping ("the question asked about the **grapes** — this column") + re-count the column's discrete icons one-by-one; the pairing manipulative re-animates ONLY as an optional "see the difference" scaffold; never the bare answer.

## §5 — §A.13.60 distinctness + scope
Honest **~2 graded cognitions** — **DATA-EXTRACTION** (locate the right 2-of-4 named columns + read their discrete counts, past subitizing) + the **symbol-first comparison** (complete the given number sentence on the extracted values) — + the **content-variety waiver** via ≥7 distinct items by (graph content × asked-pair × question-type {difference/sum} × counts) + the order-only `nextTask` reshuffle. **MID-SIZE (pedagogy + critic):** NOT a "compare-cognition" game (the compare op is owned + keyword-intrinsic; the pairing is handed/ritual/re-center-to-K) — a clean, genuinely-premium **DATA-EXTRACTION mid-size** (locate-named-columns-among-4 + discrete-count-past-subitizing + symbol-first comparison), with the pairing a beautiful optional manipulative. Distinctness via ARTIFACT (a multi-category data DISPLAY — no other activity has one) + STANDARD-FACET (read-a-display vs sort-and-count's single-category / E4's bare-number-compare). Scaffold ladder: read-one-value → read-two-values → locate-the-named-pair-among-4 → symbol-first difference/sum (the graded core); pairing-manipulative + build-graph = scaffold.

## §6 — Visual / interaction / a11y
Direction-A (cream #FBF3E4, teal #146B5E, coral #F2784B; Baloo 2 + Nunito; dual-shadow card ~720px). Pim + creature-lanes (discrete icons in stalls, scrambled positions; CSS/SVG; stub = colored icon-tiles + `<text>` labels). Stub-first (→ CA5). **Tap-first:** read = tap-count a column's icons (each lights, discrete) → the question (WORD-ONLY, no icon) names the pair → tap the column matching the LABEL → complete the given number sentence (tap the answer-value); the pairing rope = an optional manipulative the child can invoke. ≥44px. **Screen-reader FIRST-CLASS:** each lane an ARIA group ("Frogs lane, tap to count"); each icon focusable ("Frog, 3 of the column"); the question read in full (WORD label, no icon — the SR child reads the label → counts the named column); the symbol-first sentence read ("8 minus 5 equals blank"); the SR child reads the data + completes the sentence, never a height, never an icon-match. 280px §A.13.55 (`vw` not `vmin`/`vh`); reduced-motion (icons appear, no fly-in); mute-safe (ARIA + visible labels carry all info).

## §7 — Per-locale
**EN pilot; non-EN 404-by-design** (resolveActivitySlug→null). **MODERATE-LOW per-locale cost** — numbers + graph structure + the symbol-first number-sentence are locale-neutral; only the CATEGORY nouns (from `image-vocabulary.js` — plural + gender per §A.13.58 per-locale-gender-authority, never cross-apply a code) + the question/prompt chrome localize. **TRAP (native-ensemble fork, NOT chrome):** the comparison FRAME varies structurally per language (fi case/partitive "kuinka monta kissaa enemmän kuin koiria"; de word-order "wie viele Katzen mehr als Hunde"; Romance "más…que" agreement) — author the full frame per locale, NEVER a fixed nominative-noun-slot template (the §A.13.56 case-trap); the word-only-prompt re-center actually HELPS (the label must be read in-language, reinforcing the per-locale noun). §A.13.48 native ensemble per locale (the comparison frame + category plural/gender the explicit forks); §20.10 framework-name localization; Nordic `[NSR-FLAG]`. The math/graph/extraction core is 0-lines-per-locale (activity-layer fan-out, §A.13.54).

## §8 — Build artifacts / signature architecture
- **NEW** `mini tools/engine-graph.js` + `mini tools/graph-core.js` (0 lines to any of the 5 protected cores; `array-core.js`/`verify-array-core.js` window-shim the template; premium scaffold `game-shell.js`+`game-collection.js` exist; DESIGN-ONLY). Descriptor `{categories:[{key,count}], askedCategories, questionType: difference|sum, scale:1, promptMode: word-only, numberSentence}` — integer-only, NO stored answer / NO continuous-height field / NO icon-in-prompt; `deriveAnswer` from the extracted counts ONLY at grade-time; the comparison posed symbol-first.
- **NEW** `scripts/verify-graph-core.js` (the §4 MEASURED gate; window-shim; the solvers all FAIL [SUBITIZE_ICON_MATCH / SUBITIZE / TEMPLATE_OPERATION / WRONG_CATEGORY / EYEBALL / AUTO_PAIR / RITUAL_PAIR / TRANSCRIBE / RANDOM] + all structural asserts incl. `DATA_EXTRACTION_LOAD_BEARING` + `PAST_SUBITIZING` + `OPERATION_SYMBOL_FIRST_NOT_CHOSEN`).
- **NEW** `scripts/local-test-graph.js` (EN render/interaction/280-768 mobile + the solver-fail check + `audit-activity-variety.js` 2-pass ≥7 + the word-only-prompt + past-subitizing-counts + SR label-read checks).
- Wrapper: Pim + the lanes + the word-only question + the symbol-first number-sentence + the optional pairing manipulative + the label→column re-show; `nextTask` order-only reshuffle (NOT the protected `lcs-shell.js`); `poolSize` counts the round pool; mastery = the data-extraction + symbol-first comparison (pairing + build-graph scaffold).
- Reuses shared `game-shell.js` (`ctx.correct()` auto-advance; `GameCollection` non-numeric vessel).
- Manifest row in `engine-graph`'s `*-activities.json`; register in `frontend/lib/activities.ts MANIFEST_FILES` (the BUILD-FAILING preflight gate, `preflight-activity-routes.js`); EN-only, non-EN 404-by-design.
- JSON-LD `educationalAlignment` = `{'@type':'AlignmentObject', alignmentType:'educationalSubject', targetName:'CCSS.MATH.CONTENT.2.MD.D.10', educationalFramework:'Common Core State Standards'}` — NO `targetUrl` (§22.1).
- **DESIGN-ONLY for now** — no code; everything stays LOCAL until the ~300-game set is ready. Persist this spec to `game-designs/graph-01.md` at closeout.

## Cohort doctrine this game adds
**The THIN-STANDARD / HEADLINE-OP-IS-OWNED lesson — PROGRAM-WIDE.** A standard can SOUND rich ("read a graph AND COMPARE") but its genuinely-NEW assessable cognition is NARROW — here just the **DATA-EXTRACTION** (locate the named categories + read the display) — while the HEADLINE operation (the compare/difference) is OWNED arithmetic (counting + within-20) that is **IRREDUCIBLY keyword/template-signaled by the standard's OWN natural language**: "how many more X than Y" *is* difference (English, not keyword-stuffing), and a (template + answer-type) solver recovers it with ~prob 1. You CANNOT decorrelate the operation from the phrasing without DELETING a cognition the standard intends — keyword-stripping here is a DOWNGRADE (to K.CC one-to-one correspondence), NOT a hardening. A beautiful CONCRETE MODEL of the owned operation (the pairing-to-find-the-unmatched) is a SCAFFOLD, not the assessment — it is handed (auto-pair = Game-76 affordance-leak), or a motor-ritual (crank-and-count), or it only "works" by re-centering the standard down to K-level. **THE FIX: (1) IDENTIFY the genuinely-NEW cognition (the read/extraction) and CENTER the grade THERE; (2) pose the OWNED operation SYMBOL-FIRST (a GIVEN number sentence the child completes, NOT a keyword-chosen op — the house pattern; assert the arithmetic-on-extracted-values, don't claim the un-decorrelatable operation-CHOICE); (3) DEMOTE the concrete model to an optional scaffold/manipulative.** AND the SUBITIZE corollary — a small-count discrete display is SUBITIZABLE (read WITHOUT counting) and an ICON-prompt is VISUALLY MATCHABLE (read WITHOUT the label): force counts PAST the subitizing ceiling (≥6, some ≥10) + WORD-ONLY prompts (no icon) so the read is a deliberate count of the LABELED column; a `SUBITIZE_ICON_MATCH_SOLVER` must fail. Generalizes to EVERY "read-X-and-do-owned-arithmetic" standard (graph+compare, clock+elapsed, money+change, measure+difference): the EXTRACTION/READ is the new cognition (center the grade there, past any subitize/eyeball shortcut, with the identifying label load-bearing); the arithmetic is OWNED + often phrasing-intrinsic → SYMBOL-FIRST it, don't sell the manipulative as the assessed cognition. (Lineage: the re-center family — Game-74 continuous-unassessable / Game-75 lexical-tell / Game-76 affordance-leak / 78 thin-standard-owned-op; the symbol-first house pattern [STANDING POLICY 2026-06-22] applied to the owned operation; distinct addition = "the standard sounds richer than its new cognition; find the narrow new surface and grade THAT.")
