# K-357 `recycling` : EN BASE build record (2026-09-21)

Built from `K-357-recycling.md` (FINAL) §2 base + §5 data + gates under `_BUILD-BRIEF.md`, `_SUBSTRATE.md` and the README cross-type rulings; every ruling of `_work/K-357-critic.md` applied (ONE `primitives/bin.js` with the design's geometry + fill modes + position-keyed marks, the pedagogy's composer instead of a `quota`, the 37-item bank, `layout` knob, `data-lcs-*` stamps, 117 x 270 bins (the reviewer re-sized the design's 176) with the measured pill budget). No em-dashes. Nothing shared edited; the faces (K-366 / G1-364 / G2-348 / K-367 / G1-365) are NOT emitted (Phase 2). Never `git add` / `commit` (the reviewer commits; `data/` is gitignored, force-add the two bank files).

## Files (all type-scoped)
| file | what |
|---|---|
| `scripts/worksheet-gen/types/k/K-357-recycling.js` | the spec: `exerciseType:'recycling'`, themeless, no unitAxis (RULED), `difficulty {1,2,3}` = `{items, perBinMin, perBinMax, tile, iconPx, binH, strips, twinsAllowed}` exactly the design ladder re-sized by the reviewer rulings (d1 6 / 1-2 / 92-78 / binH 270 / zone 260 · d2 8 / 1-2 / 73-61 / binH 270 / zone 260 · d3 10 / 2-2 / 72-60 two strips / binH 240 / zone 220); `build()` reads ONLY `bank('recycling', loc)` + the GLOBAL json + `fileUri`; `_buildWith({global, block})` is the gate's poison seam; `_bin / _poolFor / _materialPool / _trioClasses / binWidth` = the §5 helper contract (`_bin`: excludeItems → null, `route` > first bin in page order whose materials include the material and (packaging or not packagingOnly), else null = excluded for the locale); every guard keys on the RESOLVED config (a missing key throws "the guard needs the resolved config, never the level index"); `_buildFace` = the additive `layout` knob (undefined = base path byte-identical; 'which' / 'write' / 'odd' / 'color' / 'open' refuse by name until Phase 2); root `data-lcs-layout` ABSENT on the base. `verify(page)` re-derives everything from the stamps (root `data-lcs-bins` ordered keys + n / items / per-bin-min / per-bin-max / tile / icon-px / bin-w / twins; tile `data-lcs-item / material / bin / family / vocab / theme / noun`; slot `data-lcs-bin / materials / color / mark / label`): N slots 2..5 in the stamped order, mark === position (the drawn `[data-lcs-bin-mark]` too), fill mode lid + white body, four parts, one pill per bin === its label in one client rect at >= 17 px, pills never touch, every picture complete and `<dir>/<file>` === the stamped theme/noun (a `pictureFor` candidate fails), icons >= max(56, config), tiles === the config outer size, stamped bin on the page + its material in that bin's materials, per-bin counts within min..max, no family / vocab twice, no bin's tiles all adjacent (leak), paper + letter never both / never next to package, no text outside a pill, no `[data-lcs-answer]`, stage inside the body / above the footer, line zone >= 120. |
| `scripts/worksheet-gen/primitives/bin.js` (NEW) | `bin({w, h, colour, mark, fill, strokePx, data}) → {svg, w, h, colourHex, strokePx}`: px geometry from (w, h), no viewBox scaling (handle 0.38w..0.62w × 0.07h..0.14h r 0.04w · lid 0.06w..0.94w × 0.13h..0.28h r 0.07w · collar 0.14w..0.86w × 0.27h..0.37h · body 0.14w..0.86w × 0.27h..0.97h, square top, bottom r 0.18w), each part `<g data-lcs-bin-part>`, all four under `<g data-lcs-fill="<key>" data-lcs-bin-fillgroup data-lcs-fill-mode>`; stroke teal 3, `max(2, 3w/117)` under 64; `fill:'lid' | 'all' | 'none'` (none = every part white, NO mark); white marks 0 solid · 1 three dots r 0.16 lidH · 2 three bars 0.06w × 0.6 lidH · 3 rounded bar 0.5w × 0.24 lidH · 4 zigzag 5 points stroke 0.14 lidH, every feature clamped >= 2 px; colour keys `codeColors.*` / `inkSoft` / `teal` (additive, see deviations), anything else THROWS. |
| `scripts/worksheet-gen/qa/verify-b4-bin-primitive.js` (NEW) | the primitive's node gate: 5 sizes (44×66, 56×84, 117×176, 117×260, 210×176) × 10 colours × 5 marks × 3 fills = parts + draw order, one fill group naming the KEY, token hex, per-mode fills, stroke clamp, mark index + >= 2 px features at w 44, geometry in px (the 210 bin is 176 tall); throws on an unknown key / mark 5 / fill "body"; 5 poisons vs their controls (a leaked codeBlue lid on fill:none, a mark on fill:none, dots scaled to 1.58 at 44, unknown key, mark 7). → **PASS (27,056 assertions), 5/5 killed.** |
| `scripts/worksheet-gen/data/b4/recycling.json` (NEW, gitignored) | the GLOBAL bank: `materials` (the six), 37 `items {id, theme, noun, vocabKey (from cache/manifest.json), material, packaging, family, picOpened:true, note}` (paper 4 + cardboard 2 / glass 6 / plastic 8 / metal 4 / organic 13), `pageRules {neverBoth:[[paper, letter]], neverAdjacent:[[paper, package], [letter, package]]}`, `_excluded` with every reason of the design (AMBIGUOUS incl. `kitchen tools/bottle`, COMPOSITE, CERAMIC, NOT_WASTE, VOCAB_TRAP `tools/nail`). |
| `scripts/worksheet-gen/data/b4/recycling.js` (NEW, gitignored) | `{ RECYCLING: { en: {bins, route, excludeItems, materialWords, colorConvention, refuse, starters, scheme, strings, strand} } }` — en 5 bins paper (+cardboard) · plastic · glass · metal · compost, every `color:null` (no national convention), `packagingOnly:false` throughout, `route {}` / `excludeItems {}`, `materialWords` 5, `refuse:['color']`, the base + four face strings (K-366 / G1-364 / G2-348 / G1-365 = the §6 EN candidates; K-367 absent = refused), `strand:'Science (readiness)'`. NO `quota` (critic #5: the composer replaces it). |
| `scripts/worksheet-gen/templates/components-b4/recycling.js` (NEW) | `RC_CSS`, `NEUTRAL_LID`, `lidColourKey`, `binRow`, `sortStrip`, `keyBins` (+ `legendBins` = the same function under the build brief's name), `binChips`, `whichBinRow`, `materialRow`, `oddRow`, `legendChips`, `exampleShelf`, `recycleLane` — the §2 geometry verbatim (bins row padding-top 30, pill top -26 padding 5 10 border 2.5 Baloo 2 700 17, key slots 117 / bins 56×84 / pills 15, chips 64×80 holding 44×66 lid-filled bins (r 12 squares, never rings), which-row grid `30px 72px 1fr 330px`, material-row `30px 64px 1fr` + `writingRow 400×58 glyphH 30`, odd boxes 120 / icons 84, legend swatch 22 + colour word Nunito 800 17 + bin word Baloo 700 17 (colour keys codeColors ∪ inkSoft, the neutral `teal` THROWS), shelf 117×64 for two 56 px pictures, lane `30px 170px 1fr` + `drawBox 170×150` + `rulingBlock rows 2 h 56 glyphH 36 gap 8`). The barrel merges it (0 duplicate names). Only `RC_CSS` + `sortStrip` + `binRow` are consumed by the base; the eight face components render nothing until Phase 2 (each smoke-built once in node). |
| `scripts/worksheet-gen/qa/verify-b4-recycling.js` (NEW) | the gate (exports `validateBank(block, loc)` for `tools/b4-probe-child.js`, `validateGlobal`, `bankWarnings`, `measurePills`). |

## Measured (real render, shell woff2 from file://; `out/dev/K-357-gate-*.png`)
- **Stack (TWO REVIEWER RULINGS 2026-09-21, superseding design §2 "slack opens the line zone; strip and bins never move" and its 176 px bin):** ruling 1: the first d2 render read SPARSE (~430 px of blank between a 72 px strip and the bins under the one-line chrome) → the line zone became a FIXED `.rc-zone` block with the stage at the TOP of the body and the slack below; ruling 2: the blank then sat under the bins (281 px), so the lever is SIZE — the bin is the apparatus that fills a K page. Built: **d2 = strip 85 (4 + 73 + 8) + zone 260 (the ceiling) + bins 300 (30 padding + binH 270) = 645**, measured strip-top → bins-bottom **641** (the strip block's 4 px top padding sits above the first tile); slack under the bins 166 at the one-line en chrome (811), 77 at 722, 32 at 677; the visible bin = 26 px pill overhang + 270 = 296 (pill + lid + body ~300). d1 = 104 + 260 + 300 = 664 (measured 660; slack 147 / 58 / 13); d3 (two strips of 5, unpublished) = 168 (4 + 72 + 12 + 72 + 8) + 220 + 270 (binH 240) = 658 (measured 654; slack 153 / 64 / 19). Configs: d1 92 / 78 / binH 270 / zone 260 · **d2 73 / 61 / binH 270 / zone 260** · d3 72 / 60 / binH 240 / zone 220; guards `zone` 220..260 and `binH` 240..300 on the resolved config. The strip is at its one-row maximum for 8 (8 x 73 + 7 x 12 = 668 <= 675; icon 61 = 73 − 4 − 2 x 4). The 270-tall bin keeps `primitives/bin.js`'s proportional geometry (lid 0.15h = 40.5 px, collar 27, body 189; the design's F4 bin is 260 tall by the same rule) — no special-casing, the primitive gate now covers 117x270 and 210x270 (7 sizes, 37,876 assertions). Dot-to-pill zone measured **265** at d1 / d2 (= 260 + 8 + 4 − 7) and 225 at d3, identical under one-line, long chrome, the 722 pin and the 677 pin (fixed, nothing moves). The minimum a K line needs: 8 dots 85 px apart converge on 5 pill tops 131 px apart; the shallowest line (leftmost dot → rightmost pill, ~590 px across) rises 265 px, slope 0.45 (24°), every line a clear diagonal; 220 is the floor the gate enforces, 260 the ceiling above which the blank reads as paper. Icons 84 at d2 do not fit one row (8 x 92 + 84 = 820 > 675).
- d2 en: 8 tiles 73 outer / icons 61 (floor 56), strip 668 <= 675; 5 bins **117 x 270** (`binWidth(5)` = 117), row margins 17 / 17; pills 68.6 / 75.3 / 65.3 / 67.0 / 92.1 px (all <= 151; adjacent half-sums <= 84); fits 722 AND 677; verify 0 / lints 0.
- d1: 6 tiles 92 / icons 78; d3: two strips of 5 at 72 / 60. All verify 0 / lints 0.
- Long chrome (70-char 3-line de title + 150-char instruction): body **733** (the K-355 finding: the tallest chrome the 70 / 150 limits produce), zone 265 (d2), the stage directly under the instruction; the fi four-line 677 budget is checked by PINNING the body to 677 (and 722) in the live page: the strip inside the top, the bins inside the bottom (32 px of slack at 677), the zone unchanged at 265 (fixed, so nothing moves).
- Greyscale d2 (`K-357-gate-d2-en-grey.png`, `filter:grayscale(1)`): five identical dark-grey lids separated by the white mark (solid / dots / bars / bar / zigzag) + the pill word; the coral dots and pale tiles survive as mid greys; the lined `classroom/paper` reads as a pale rectangle (the composer keeps it off `letter` and away from `package`).
- Bank: 37 / 37 resolve via fileUri + entriesFor (singular) + hasPicture in all 11 locales, 0 B2_EXCLUDEd, 0 in `_excluded`, 0 duplicate vocab keys; per class families paper+cardboard 5 / glass 6 / plastic 8 / metal 4 / organic 13 (all >= 3, the F3 floor), a packaging item in each recyclable class. **Every picture OPENED on a 200 px contact sheet** (scratchpad `K-357-bank-sheet.png`): all 37 read as the bank notes say; `pepper` = a red bell pepper; `saucepan` has a DARK-BROWN handle that could read as wood at 60 px (the critic's "all-metal" ruling kept, noted on the item); `notebook` carries coloured tabs (paper, fine); `milk` = a glass bottle with a red cap (glass, per design).
- en pools: paper 6 / plastic 8 / glass 6 / metal 4 / compost 13. Sweep: 20 seeds x d2 → 20 distinct item sets; per bin distinct items across the sweep paper 6 · plastic 8 · glass 6 · metal 4 · compost 12 (>= 3 each); every seed passes the family / twin / leak / pale-item fences in node.
- R1 measured with the shell fonts (the design's numbers reproduced): `Indifferenziato` 140.3 (17 px) / `Pappersförpackningar` 195.8 (REFUSED > 151) / `Verpackungen` 134.1 (a synthetic neighbour: 70.2 + 67.1 = 137 > 125 fires; `Plastica` beside `Indifferenziato` passes).

## Gate output
`node qa/verify-b4-recycling.js` → **PASS (1109 assertions, 41 poison verdicts clean)**; `--quick` → PASS (543 assertions). The gate asserts ITSELF the fixed zone and the size floors: the `.rc-zone` block === the config within 220..260, the measured dot-to-pill zone <= 265.6 (above = "the page reads SPARSE") and >= 219.4, the stage top <= 8 px under the body top, every bin drawn === `binH` (>= 240), **the d2 stage (strip top → bins bottom) >= 630** and **the slack under the bins <= 180 at the ONE-LINE chrome**, under the live chrome AND both pins. Poisons killed 30 / 30 + the it fixture control: **PR-SHORT** (the previous 530 stack — tile 72 / binH 176 / zone 240 — the spec guard refuses `binH 176 outside 240..300`; rendered past the guard the gate fires "stage 526 px < 630 at d2: the apparatus does not fill the page (sparse)" AND "281 px of slack under the bins at the one-line chrome > 180 (sparse)", verify() fires on binH) · **PR-SPARSE** (the zone block stretched back to the design's 425 → measured 430: the gate fires "line zone 430 px > 266: the page reads SPARSE" AND verify() fires "the line zone block is 425, not the stamped 260" / "outside 200..300"), each for its own reason, the EN bank the control: P1 packaging missing · P2 `kitchen tools/bottle` (in `_excluded`) · P3 a second `apple` vocab key · P4 de 6 bins · P5 de `route.toothbrush → verpackung` (packagingOnly) · P6 sv `plast` materials [] (pool 0) · P7 en materialWords all "paper" · P8 nl GFT + Glas both codeGreen under national · P9 de classroom · P10 fi "Lajittelu" · P11 sv "Sortera soporna" · P12 fr "Trie en sciences …" · P13 en F1 "Tick the bin …" (rule 8 lexicon) · P14 es 3 bins without `panelOverride` FIRES / with a reason PASSES · P15 "Big Bins" vs "Color the Bins" (adjective-only) · P16 an unauthored locale → the bank REFUSES (never en) · P17 a bin pool emptied → the spec REFUSES (no filler) · PR11 `perBinMax` undefined in the resolved config → the guard · PR1 two compost tiles adjacent → verify() leak · PR2 envelope + letter → verify() family · PR7 `At the Supermarket/apple` src under a `fruits/apple` stamp → verify() "src is not the opened picture" · PR8 sv page with `saucepan` → node gate "excluded for sv" (verify PASSES: the picture is consistent, only the bank knows) · PR9 sv `Pappersförpackningar` 195.8 > 151 / `Papper` PASSES · PR10 adjacency 137 > 125 / `Plastica` PASSES. The sv fixture surfaces the `metall: pool of 1` as a WARN (asserted present), not a refusal.
`node qa/verify-b4-bin-primitive.js` → PASS (27,056 assertions, 5/5 poisons). `node i18n/build-en.js` → `606 types (title lint clean)` (the rebuilt `strings.en.json` also carries the sibling K-353..K-356 entries). `node tools/b4-probe-child.js en` builds K-357 with 0 recycling validator findings.

## Baseline
`node tools/b3-baseline.js --check --quick` → **`checked build 3428 + enum 255 in 16s: 0 drifted (0 expected), 0 missing` / PASS** (re-run after the size fix: `checked build 3428 + enum 255 in 17s: 0 drifted`) — nothing shared drifted (the additive `primitives/bin.js` and `components-b4/recycling.js` have no live consumer).

## Deviations from the design file / the build prompt (each measured)
1. **en N = 5, not 3.** The build prompt said "en N=3 per the en bin table"; the design's table B (and the pedagogy, the critic and the README's hub matrix "en F4 refused, 5 rows") say en = FIVE bins paper (+cardboard) · plastic · glass · metal · compost. Built from the artefact: N = 5, `binW` 117. (N = 3 would be 204-wide bins; the formula handles it, nothing else changes.)
2. **`'teal'` accepted as the NEUTRAL lid colour key** (additive to the design's "a codeColors key or 'inkSoft'; any other key THROWS"): the en bins carry `color:null` (no national convention, F4 refused) but the base still draws lidded bins; `binRow` maps null → the house teal and stamps `data-lcs-color="none"`. `legendChips` (F4) does NOT accept it (the neutral is not a legend colour). The primitive gate covers the key.
3. **Strip tile padding 4, not 6:** the design's "tile 72 … padding 6, icon 60" makes a 76 px outer tile (8 x 76 + 84 = 692 > 675); the tile is `tile` OUTER (box-sizing border-box) and `padding = (tile - 4 - icon) / 2` = 4 at 72 / 60, so 8 x 72 + 7 x 12 = 660 holds. Measured 72 x 72.
4. **Mark 2 bars centred on the lid** (left edges 0.31 / 0.47 / 0.63 w): the design's 0.28 / 0.44 / 0.60 centre the group at 0.47w, 3.5 px left of centre at w 117.
5. **Fixed 260 px line zone, 270 px bins, stage at the body top (the two reviewer rulings 2026-09-21; supersede design §2's "slack opens the line zone; strip and bins never move", its 425 / 380 zone and its 176 px bin):** the design's 425 was a maximum; the first render carried ~430 px of blank between strip and bins, the second 281 px under the bins — the K page is filled by the apparatus, so the bin grew to 270 (pill + lid + body ~300) and the zone sits at its 260 ceiling: 85 + 260 + 300 = 645 at every chrome; `zone` and `binH` are guarded configs; verify() and the gate measure them (see Measured).
6. **Rule 10 is direction-aware:** the base is the bare head and each face ADDS a noun / verb (one direction); two FACES must each carry a content token the other lacks. The first run accused the correct EN strings (base vs every face) before this was encoded, the P15 adjective-only case still fires.
7. **Rule 4's "every bin's pool >= 2 distinct families (F4 examples)" applies only where F4 ships** (`refuse` without `color`): sv `metall` = `can` alone is the design's recorded WARN, not a refusal, and sv refuses F4 anyway.
8. **Rule 8 lexicon is EN-only** (need + ban per face); the per-locale apparatus lexicon is `tools/validate-b4-draft.js`'s job when the panels author (Phase 2). Non-EN blocks get the length / brace / free-claim / answer-key checks.
9. `keyBins` is the design's name; **`legendBins`** (the build prompt's name) is exported as an alias of the same function. `exampleShelf` (design §2, not in the prompt's list) is exported too (F4 needs it).
10. The `strings` block carries NO `K-367` entry (F4 refused): rule 10 accepts a missing face id named in `refuse`.
11. `pageRules` live in the json AND as constants inside `verify()` (page.evaluate cannot require); the gate asserts the two copies agree (parsed off the spec source).
12. The 677 check is by body PINNING (the K-355 precedent): the en chrome cannot reach 677 under the 70 / 150 limits (long chrome measures 733).

## PNGs for the reviewer
`out/dev/K-357-null-d1-en.png` · `K-357-null-d2-en.png` · `K-357-null-d3-en.png` (re-rendered after the size fix and LOOKED at all three: eight / six / ten bright product tiles under the instruction, one coral dot each, a 260 / 220 px line zone, five tall teal-lidded bins (270 / 240) with the word pills and the five distinct white marks filling the page to ~166 px above the footer; nothing printed but the pills and the chrome) · `K-357-gate-d2-en-grey.png` (greyscale: bins separated by mark + word) · `K-357-gate-d2-en-longchrome.png` (3-line title + 2-line instruction: strip and bins hold, zone 448) · `K-357-gate-poison-PR1/PR2/PR7/PR8/SPARSE/SHORT.png`. Contact sheet of the 37 bank pictures: scratchpad `K-357-bank-sheet.png`.

## Open items for the faces / panels
- Phase 2 builds `_buildFace` for the five layouts on the components already shipped (`keyBins` + `binChips` + `whichBinRow` for F1 rows `minmax(85px,1fr)`; `materialRow` + `wordBank` F2; `oddRow` in `cardGrid` F3; `legendChips` + `binRow({fill:'none', binH:260})` + `exampleShelf` F4; `recycleLane` F5), adds the face render sections + PR3-PR6 / PR12-PR15 to the gate and the per-locale rule-8 lexicon to `validate-b4-draft.js`.
- Panels (design §4): the bin literals (label <= 15 chars, measured <= 151 / adjacent <= 125 at render, it "Secco" not "Indifferenziato" beside "Umido"), `route` / `excludeItems` with reasons (de Wertstoffhof metal; sv / fi non-packaging plastic + metal; fr OPEN 4 three vs four bins; da pap / fi kartonki merges OPEN 7; no `[NSR-FLAG]` glass og metall OPEN 9; `bucket` OPEN 2), `materialWords` (organic single token OPEN 3), F4 `colorConvention:'national'` + distinct tokens + colour words or `refuse:['color']` (it UNI 11686 / nl glas token / sv da no pictogram colours, each `[NSR]`), `starters` only if <= 140 px, `scheme`, `enAudit`, the six strings (fi never "Lajittelu"; sv never "Sortera …"; es "basura" / "botes"; pt "coleta seletiva").
- `saucepan`'s dark-brown handle (opened): a panel that reads it as wood may `excludeItems` it with a reason; the bank keeps the critic's metal ruling.
- The one-line en chrome (body 811) leaves 166 px under the bins at d2 (the chrome's slack, ruled acceptable; the gate caps it at 180). At 677 (fi four-line title) d2 keeps 32 px, d1 13 px: a panel title that wraps to FIVE lines has no room — the fi validator check "base title <= 4 lines" is belt and braces here.
