# K-357 `recycling` : the FIVE variation faces (Phase 2, 2026-09-21)

Built from `K-357-recycling.md` §3 (+ §1 / §4 / §5 for the rulings, the bank and the gates) under `_FACE-BRIEF.md` + the README cross-type rulings, on the base substrate of `_work/K-357-build.md` (its "open items for the faces" closed here: `_buildFace` for the five layouts on the components the base shipped, the face render sections + PR3-PR6 / PR12-PR15 / the per-face SPARSE poisons in the gate; the per-locale rule-8 lexicon stays `validate-b4-draft.js`'s job when the panels author). Ids from `_records/b4var-id-allocation.json` (K-366 K · G1-364 G1 · G2-348 G2 · K-367 K · G1-365 G1). Nothing shared was edited (`primitives/bin.js` untouched: `git diff` empty; `qa/verify-b4-bin-primitive.js` re-run PASS 37,876); nothing committed. Every number below is measured (node against the real files, or puppeteer through `render/render-instance.js` with the shell's `file://` fonts). No em-dashes.

## Files (all family-scoped)
| file | what |
|---|---|
| `scripts/worksheet-gen/tools/b4var-rows/recycling.js` | NEW: 5 rows (K-366 · G1-364 · G2-348 · K-367 · G1-365), `HANDWRITTEN` empty; every row spreads the base's d2 and sets the additive `layout` knob + its face keys; G1 / G2 faces carry `extra {gradeBand}`; the only file the emitter reads. The K-367 row carries the §6 EN candidate (en REFUSES the face; see below) |
| `scripts/worksheet-gen/types/k/K-357-recycling.js` | the Phase 2 stub replaced: `_buildFace` dispatches on `d.layout` to `_buildWhich / _buildWrite / _buildOdd / _buildColor / _buildOpen` (every guard on the RESOLVED config, never the level index; `block.refuse` names a face → REFUSE), shared page-fence samplers `_fence / _admits / _take / _tile` (no family twice, no twin, the pale pair never both), `_binsOf`, `_guardInt`; `verify()` reads the root's `data-lcs-layout` first and hands a face to `_verifyFace` (a second `page.evaluate`); the base's own evaluate, the base path of `_buildWith`, the configs and the base components are byte-untouched (baseline PASS). Two new requires: `C2` (components-b2 `wordBank`) and the constants `G1_ICON_FLOOR` 44 / `STAGE_MIN` 630 |
| `scripts/worksheet-gen/templates/components-b4/recycling.js` | `RC_CSS` / `sortStrip` / `binRow` (the base's consumers) byte-untouched. Additive: `whichBinRow` gains `chipsW` (the design's 330 chip column was a MISCOUNT: 5 x 64 + 4 x 10 = 360; the column is N x chipTile + (N - 1) x 10, 510 <= 675 holds); `oddRow` gains `boxMax` (default = box = the fixed design geometry; above it the boxes GROW to `min(boxMax, 100%)`, square, picture iconPx / box) + a `data-lcs-class` stamp; `exampleShelf` gains `dir:'column'` (pictures stacked under the bin, padding 6 gap 8); `legendChips` gains `rows` (2 = a forced break after ceil(n / 2), row-gap 10, `data-lcs-legend-rows`); `recycleLane` gains `grow` (the draw box flexes to the lane, `draw.h` its floor); NEW `RC_FACE_CSS` + `faceRoot` (the stamped face root: `.rc-face--fill` = `flex:1 1 auto`, `.rc-face--fixed`), `faceRows` (`repeat(n, minmax(min, 1fr))`), `oddGrid` (the card-grid markup with the trio class stamped on the CARD), `shelfRow`. The barrel accepted every name (0 duplicates) |
| `scripts/worksheet-gen/types/k/K-366-recycling-which-bin.js` · `types/g1/G1-364-recycling-what-is-it-made-of.js` · `types/g2/G2-348-recycling-odd-one-out-by-material.js` · `types/k/K-367-recycling-color-the-bins.js` · `types/g1/G1-365-recycling-three-things-i-recycle.js` | emitted by `node tools/gen-b4var-specs.js` (`emitted 30 variation specs + 0 handwritten`, 6 of 10 families on disk at the time) |
| `scripts/worksheet-gen/data/b4/recycling.js` · `recycling.json` | UNCHANGED: the faces print only bin labels, the 5 `materialWords` (F2 bank) and colour words (F4 legend, non-EN); the en `strings['K-366' / 'G1-364' / 'G2-348' / 'G1-365']` the base authored are the rows' titles + instructions verbatim (the gate asserts the pair is ONE source); K-367 has no en string (rule 10, `refuse:['color']`) |
| `scripts/worksheet-gen/qa/verify-b4-recycling.js` | the gate grows: sections 5-6 (rows ≡ spec ≡ strings incl. the en K-367 refusal; every face at d2 through the real pipeline + the 70 / 150 long face chrome + the body PINNED to 722 / 677 with `verify()` re-run on the re-flowed layout; the es N = 2 / fr N = 4 / it bin sets on F1, es + sv on F2, es + nl (+ de) on F4 in the full run; floors; node cross-checks with the bank; every picture on disk as `@2x.webp`; a 20-seed sweep per face; 25 face poisons). Final line `PASS (N assertions, M/M poisons killed, …)` |
| `scripts/worksheet-gen/i18n/strings.en.json` | regenerated by `node i18n/build-en.js` (`643 types … title lint clean`; left as generated, it also carries the sibling builders' faces; it now carries K-367's EN candidate, which the EN wave must not list) |
| `docs/worksheet-gen/b4-designs/_work/K-357-faces.md` | this record |

## The five faces

### K-366 : Recycling Sort: Which Bin? : CODE `layout:'which'` (F1, K)
- **Row:** `{...base.d2, layout:'which', rows:6, tile:72, iconPx:60, chipTile:64, chipBin:44, keyPx:15, shuffleChips:false}`.
- **Child:** a key strip of the N bins (56 x 84 lid-filled bins under their word pills at 15 px, the same centres as the base's bin row) then six numbered rows, each one product (tile 72 / picture 60) + the N mini bins (64 x 80 white rounded-square chips holding 44 x 66 lid-filled bins, in the FIXED locale order on every row); the child circles ONE bin per row. Six independent decisions with the full bin set; the lid MARK + the key word carry the bins in mono (greyscale crop read: solid / dots / bars / bar / zigzag legible at 44).
- **Build:** every bin one row, the extra rows round-robin over a shuffled bin order but only to a bin whose pool can carry them (sv `metall` = 1 item never doubles: without the rule a doubled metall row would refuse whenever the shuffle put metall first among the extras; the 200-seed fixture probe runs 200 / 200 with it); items per bin under the page fences; rows re-drawn until no two consecutive rows answer one bin (N = 2: strict alternation, the design's contract). Chip column = N x 64 + (N - 1) x 10 (360 at N = 5).
- **Stamps:** root `data-lcs-layout="which" data-lcs-bins data-lcs-n data-lcs-rows data-lcs-tile data-lcs-icon-px data-lcs-chip-tile data-lcs-chip-bin data-lcs-key-px data-lcs-shuffle-chips data-lcs-row-min data-lcs-bin-w`; key `[data-lcs-key]` slots `data-lcs-key-bin data-lcs-mark data-lcs-label`; rows `data-lcs-row data-lcs-n data-lcs-item data-lcs-material data-lcs-bin data-lcs-chip-order`; chips `data-lcs-chip data-lcs-mark`.
- **verify():** N key slots in the bin order, mark === position (the drawn mark too), pill === label in one rect at >= keyPx, key bins 56 x 84 lid-filled, pills never touch; `rows` rows, badges 1..n; per row N chips in the stamped order (=== the bin order at `shuffleChips:false`), exactly one chip whose key === the row's bin, **no pre-marked chip** (border / background / outline / shadow / radius / child count / opacity identical across the strip), chip tile >= 64 with a bin >= 44 whose mark index === the bin's position, fill mode lid; the product tile === 72 with the picture >= 60; **no run of 2 equal answers**; row >= rowMin (85) and **the chip tile >= 0.7 of the row** (sparse); **the rows grid fills the body** (root bottom = body bottom, the last row at the grid bottom); the stage at the body top; no text but the key pills + badges. **Node gate:** every row a bank item, its stamps === the bank, `bin(item, block)` === the stamped bin (an excluded item fails: PF4), picture === `fileUri`, chip order === the block's bins, >= min(N, 4) distinct answer bins, key pills under R1 at 15 px, mark features >= 2 px on every chip and key bin, the pale pair never both.
- **Poisons owned:** PR3 (two consecutive rows on one bin), PR12 (rows `minmax(90px,1fr)` → 702 px overflow the 677 pin), PS-F1 (rows fixed at 85 + the stage centred → floats + the stage ends above the body bottom), PF3 (`shuffleChips:'yes'` → the guard), PF4 (sv page with `saucepan` → the node gate; the glass pool emptied → the spec refuses).
- **PNG:** `scripts/worksheet-gen/out/dev/K-366-null-d2-en.png` (gate: `K-366-gate-d2-en.png`, `-en-longchrome.png`, `-es-fixture.png`, `-fr-fixture.png`, `-it-fixture.png`; poisons `K-357-gate-poison-PR3 / PR12 / PS-F1 / PF4.png`). **Read:** key Paper · Plastic · Glass · Metal · Compost; rows honey jar / envelope / orange / shampoo / can / package → glass, paper, compost, plastic, metal, paper (5 distinct bins, no run); every row five identical white chips with the five lid marks; nothing clipped, the sixth row ends on the footer line. Measured: rows 108 (en one-line chrome, body 811) / 95 (the 70 / 150 long chrome, 733) / 85 at the 677 pin (stack 672); chip tile 84 = 0.78 / 0.88 / 0.99 of the row; es fixture (N = 2) rows alternate inorganico / organico; fr (N = 4) and it render clean; pins 722 / 677 ok.

### G1-364 : Recycling Sort: What Is It Made Of? : CODE `layout:'write'` (F2, G1)
- **Row:** `{...base.d2, layout:'write', rows:8, tile:64, iconPx:52, rulingW:400, glyphH:30, bankPx:18, bank:true}`, `extra {gradeBand:'G1'}`.
- **Child:** a one-row word bank of the locale's distinct material words (18 px pills) then eight numbered rows, each a product tile 64 (picture 52) + a school-line writing row 400 x 58 (glyphH 30, x-height band) right-aligned; the child WRITES the material (never the object's name). No letter boxes (the `write-the-word` look).
- **Build:** pool = items whose material has a `materialWords` literal (en 24: paper 4 + cardboard 2 + glass 6 + plastic 8 + metal 4); every distinct word >= 1 and <= `perWordMax` = max(3, ceil(rows / words)) (3 at 5 words; 4 for es's 2-word bank: 8 rows over 2 words cannot hold a cap of 3); items per word under the page fences; rows shuffled; the bank = the words shuffled until it differs from the order the words FIRST appear down the page and (with >= 3 words) from its reverse.
- **Stamps:** root `data-lcs-layout="write" data-lcs-rows data-lcs-tile data-lcs-icon-px data-lcs-ruling-w data-lcs-glyph-h data-lcs-bank-px data-lcs-bank data-lcs-words data-lcs-per-word-max data-lcs-row-min`; rows `data-lcs-row data-lcs-n data-lcs-item data-lcs-material data-lcs-word` (never printed); bank words `data-lcs-bank-word` (the wordBank stamp).
- **verify():** 8 rows, badges; per row a single-token word of <= 12 graphemes on the stamp, **the row's text === its badge digit** (a printed word fails: PR4), one writing row 400 wide with its school lines, picture >= 52, row >= rowMin (68 = the 64 tile + the border, above the design's 62) and **the writing row (+ border) >= 0.7 of the row** (sparse); every word used <= perWordMax; the bank at the stage top, `words` distinct pills at >= 18 px on ONE row, its set === the set of row words, every bank word used, **not the first-appearance order (nor its reverse)**; the grid fills the body; no text but the bank + badges. **Node gate:** every row a bank item, `data-lcs-word` === `materialWords[item.material]`, picture === `fileUri`, every word 1..perWordMax, the bank === the locale's material word set, the bank pills' widths sum <= 663.
- **Poisons owned:** PR4 (a row printing its word), PS-F2 (rows fixed at 68 → the last row ends above the grid bottom), PF2 (a 1-word bank → refuse; es 2 words builds at <= 4 per word: control).
- **PNG:** `out/dev/G1-364-null-d2-en.png` (gate `G1-364-gate-d2-en{,-en-longchrome,-es-fixture,-sv-fixture}.png`, poisons `-PR4 / -PS-F2.png`). **Read:** bank plastic · paper · cardboard · glass · metal; rows bolt / spoon / notebook / sauce bottle / nut / lined paper / package / cereal box → metal, plastic, paper, glass, metal, paper, cardboard, cardboard (every word >= 1, <= 3; the bank is not the row order); eight clean three-line rulings; the eighth row ends on the footer line. Measured: rows 82 (en 2-line title, body 778) / 76 (733) / 68 at the 677 pin (stack 669); writing row 62 = 0.76 / 0.82 / 0.91 of the row; es fixture: reciclable / orgánico at 4 + 4; sv fixture: 5 words incl. `kartong`; pins ok.

### G2-348 : Recycling Sort: Odd One Out : CODE `layout:'odd'` (F3, G2)
- **Row:** `{...base.d2, layout:'odd', rows:4, items:4, box:120, iconPx:84, boxMax:150, organicOdd:true}`, `extra {gradeBand:'G2'}`.
- **Child:** four numbered cards, four DIFFERENT products a row in white boxes; three share a material CLASS (paper+cardboard / glass / plastic / metal / organic; unstated) and one does not; circle it. Never the K-043 identical-icon shape; the shared property is invisible.
- **Build:** trio classes = a shuffled 4 of the 5 classes (distinct across rows); per row 3 items of the class + 1 odd item of another class (organic allowed at d2), all under the page fences; **the odd class of an early row may not starve a LATER trio class** (metal = 4 items over 4 families: two metal odds leave 2 for a metal trio — the 20-seed sweep found the refusal), so an odd class still to come as a trio must keep >= 4 admissible items, and a dead-ended composition re-rolls the trio order (bounded, 40 attempts; 200 seeds: 200 / 200, 104 distinct trio orders); odd index re-drawn while equal to the previous row's; the pale pair (`paper` / `letter` beside `package`) never adjacent in a row.
- **Stamps:** root `data-lcs-layout="odd" data-lcs-rows data-lcs-items data-lcs-box data-lcs-icon-px data-lcs-box-max data-lcs-organic-odd data-lcs-row-min data-lcs-trio`; cards `data-lcs-card data-lcs-trio`; boxes `data-lcs-item data-lcs-material data-lcs-vocab data-lcs-class` (+ `data-lcs-odd="1"`).
- **verify():** 4 cards, badges, the trio stamp === the cards' classes (distinct); per card 4 boxes, class === CLS(material) (cardboard → paper), **exactly 3 of the trio class + 1 other** (PR5), the odd stamp on that one only, organic odd only when configured, 4 distinct vocab keys, **all four pictures ONE rendered size >= 84** (the odd one is never the odd SIZE: PR15), boxes square in [120, 150] and **>= min(150, the stage) AND >= 0.85 of the stage** (the boxes GROW: PS-F3), the odd column never repeats the row above, no text inside a stage, the grid fills the body. **Node gate:** every box a bank item with its stamps, picture === `fileUri`, the page fences.
- **Poisons owned:** PR5 (a 2 + 2 row), PR15 (the odd picture at 60 %), PS-F3 (the design's fixed 120 boxes: "box 120 in a 149 px stage does not grow", 0.81 < 0.85), PF3 (`boxMax` undefined → the guard).
- **PNG:** `out/dev/G2-348-null-d2-en.png` (gate `G2-348-gate-d2-en{,-en-longchrome}.png`, poisons `-PR5 / -PR15 / -PS-F3.png`). **Read:** jam · honey · milk bottle · purple spoon (glass, odd 4) / glue · toothbrush · leaf · comb (plastic, odd 3) / carrot · saucepan · banana · bell pepper (organic, odd 2) / sunscreen · nut · bolt · can (metal, odd 1); every picture what its name says; the boxes grown to 149 with 104 px pictures at the en chrome (body 799), 133 / 93 at 733, 120 / 84 at the 677 pin; nothing clipped. Note for the panels: the saucepan's dark-brown handle can read as wood at 100 px (the base record's note; the critic's metal ruling kept).

### K-367 : Recycling Sort: Color the Bins : CODE `layout:'color'` (F4, K; the CONTINGENT face)
- **Row:** `{...base.d2, layout:'color', bins:'locale', binH:330, examples:2, pic:84, legendPx:17, legendOrder:'shuffled'}`.
- **en: REFUSED.** `build()` throws `K-357 en: the en panel REFUSES the "color" face (refuse: color)` on the en block (`render/one.js K-367 null 2 en` prints exactly that; the gate asserts it). The en bank carries no K-367 string; the row's EN pair is the §6 candidate the non-EN panels rewrite (the fixtures carry the same pair). **The EN wave must not list K-367; `hub-expectations` en = 5.** The spec exists for the locales that keep the face (de / es / pt / fr / fi confirmed; it / nl / sv / da / no contingent on national colour tokens `[NSR]`).
- **Child (non-EN):** a legend in TWO balanced rows (colour chip 22 + colour word Nunito 800 17 + bin word Baloo 700 17; shuffled off the bin order in >= min(3, N) positions) over the N WHITE bins (117 x 330 at N = 5; 210 x 330 at es's N = 2; `fill:'none'`: every part white, NO lid mark, the word pill above) and under each bin a cream shelf of two correctly sorted example pictures at 84 px STACKED (117 x 188); the child reads the bin word, finds it in the legend and colours the bin in the locale's colour. The finished page is the class's own bin sign.
- **Build:** REFUSES unless `colorConvention === 'national'` and every bin carries a colour token + colour word with distinct tokens (a classroom key = K-241, never accepted); legend order re-drawn until it differs from the bin order in >= min(3, N) positions; per bin `examples` pictures of its resolved pool from distinct families page-wide, nothing twice, the pale pair never both; the FIXED stack legend 90 + 10 + pills 30 + binH 330 + 8 + shelf 188 = **656** (a guard: <= 677 and >= 630).
- **Stamps:** root `data-lcs-layout="color" data-lcs-bins data-lcs-n data-lcs-bin-w data-lcs-bin-h data-lcs-examples data-lcs-pic data-lcs-legend-px data-lcs-legend-order data-lcs-legend-rows data-lcs-legend data-lcs-stack`; legend `[data-lcs-legend-banner data-lcs-legend-rows]` entries `data-lcs-legend data-lcs-color`; bin slots as the base (`data-lcs-bin data-lcs-color data-lcs-mark data-lcs-label`) with the fill group's `data-lcs-fill` = the intended colour key; shelves `[data-lcs-shelf data-lcs-shelf-dir data-lcs-pic]` pictures `data-lcs-example data-lcs-bin`.
- **verify():** N in 2..5, binH 300..400; the legend at the stage top with N entries === the stamped order === the bin set, on exactly the stamped number of rows (an unplanned wrap fails), off the bin order in >= min(3, N) positions, every swatch a non-white hex distinct within the page, colour word + bin word at >= 17; N slots in the stamped order, pill === label at >= 17 in one rect, fill mode `none`, the fill group naming the slot's colour (never `none`), **every part white** (PR6), **no lid mark** (PR13), bin === binW x binH, pills never touch; N shelves each under its bin (left within 1 px, below the bin), `examples` pictures for that bin, no example twice, pictures >= 84; **the stack legend top → shelf bottom in [630, 677]** (PS-F4); no text but the pills + legend. **Node gate:** legend entries === the block's (colour token, colour word, label; the swatch hex === the token), every example a bank item whose `bin(item, block)` === its shelf's bin, distinct families per shelf, picture === `fileUri`, pills under R1, **the one-line slack under the shelves <= 180**.
- **Poisons owned:** PR6 (a lid pre-filled codeBlue), PR13 (a mark injected on a white bin), PS-F4 (the design's 260 bin + 64 horizontal shelf of 56 px pictures = stack 431: the spec guard refuses binH 260; past it the render fires "stack 462 < 630" + "316 px of slack > 180"), PF1 (the en block refuses; two bins sharing a token refuse; `colorConvention:null` refuses; the de fixture builds: control).
- **PNG:** `out/dev/K-367-gate-d2-de-fixture.png` (the de fixture: 5 bins Papier · Verpackung · Bio · Glas · Rest on blau / gelb / braun / grün / grau = inkSoft), `-de-fixture-longchrome.png`, `-es-fixture.png` (N = 2: Orgánico verde / Inorgánico gris, 210-wide bins and shelves), `-nl-fixture.png`; poisons `K-357-gate-poison-PR6 / PR13 / PS-F4.png`. `out/dev/K-367-null-d2-en.png` does NOT exist: the en render is the refusal. **Read (de):** legend row 1 blau Papier · gelb Verpackung · grün Glas, row 2 grau Rest · braun Bio (off the bin order); five white bins with the word pills, no marks; shelves package + cereal box / sunscreen + shampoo / corn + bell pepper / jam + jar / comb + bucket (every pair in its bin: cardboard → Papier, packaging plastic → Verpackung, organic → Bio, glass → Glas, non-packaging plastic → Rest via `route`); nothing clipped. Measured: stack 656 everywhere; slack under the shelves 122 (the fixture's 2-line title, A4 body 778) / 44 (the 70 / 150 long chrome on A4, body 700) / 21 at the 677 pin; pins ok.

### G1-365 : Recycling Sort: Three Things I Recycle : CODE `layout:'open'` (F5, G1, OPEN-ENDED)
- **Row:** `{...base.d2, layout:'open', lanes:3, draw:{w:170, h:150}, rows:2, rulingW:400, glyphH:36, starters:false}`, `extra {gradeBand:'G1'}`.
- **Child:** three numbered lanes (`.ws-lane`, inner 651), each a dashed coral draw box 170 wide that GROWS with the lane (150 the floor) + a `rulingBlock` of two 400 x 56 school-line rows (glyphH 36); draw a thing you recycle at home, write what it is. No starter at d2 (the design's ruling: the measured stems run 137-256 px against the 140 px budget in five locales).
- **Build:** nothing sampled (seed-invariant, asserted in the sweep); `starters:true` (d3) prints `block.starters[0]` on the first row of every lane and refuses when the panel authored none; lane min 196 (3 x 196 + 24 = 612 <= 677).
- **Stamps:** root `data-lcs-layout="open" data-lcs-lanes data-lcs-rows data-lcs-draw-w data-lcs-draw-h data-lcs-ruling-w data-lcs-glyph-h data-lcs-starters data-lcs-row-min`; lanes `data-lcs-lane data-lcs-n data-lcs-rows data-lcs-grow`; the draw box `data-lcs-drawbox data-lcs-draw-min`; ruling rows `data-lcs-ruling-row` (+ `data-lcs-starter data-lcs-starter-px` when authored).
- **verify() = structure only (open, no grade):** 3 lanes, badges; per lane ONE empty draw box 170 wide, >= 150 and **>= 0.8 of the lane's inner height** (PS-F5), `rows` ruling rows each one 400-wide writing row, no starter at `starters:false` (one per lane on the first row at `true`, carrying its metric size, **width <= 0.35 x 400** = 140: PR14), lane >= 196, the grid fills the body, no text but the badges (+ starters).
- **Poisons owned:** PR14 ("Jeg resirkulerer" at the metric size 41 px → 294 px > 140; "Jeg" PASSES: control), PS-F5 (the design's fixed 150 box in a 251 px lane), PF3 (`lanes:5` → the guard).
- **PNG:** `out/dev/G1-365-null-d2-en.png` (gate `G1-365-gate-d2-en{,-en-longchrome}.png`, poisons `-PR14 / -PR14-control / -PS-F5.png`). **Read:** three cream lanes, each a tall dashed draw box (170 x 227 at the en 2-line title, body 778) beside two clean three-line rulings; nothing else on the page; the third lane ends on the footer line. Measured: lanes 251 / draw 227 (778) · 236 / 212 (733) · 196 / 172 at the 677 pin; pins ok.

## Gate output
```
node scripts/worksheet-gen/qa/verify-b4-recycling.js
global: 37 items; per class paper 6 · glass 6 · plastic 8 · metal 4 · organic 13
… (base sections 1-4 unchanged: d1 / d2 / d3 + long chrome, widths, the 20-seed base sweep)
render K-366 which en: verify 0 lints 0 body 811 stage 811 lowest 921 vs foot 921 slack 0 rows 108 x6 answers glass,paper,compost,plastic,metal,paper (pins 722 ok / 677 ok)   · long chrome: body 733 rows 95   · es / fr / it fixtures clean
render G1-364 write en: verify 0 lints 0 body 778 stage 778 slack 0 rows 82 x8 words metal,plastic,paper,glass,metal,paper,cardboard,cardboard bank plastic/paper/cardboard/glass/metal   · long chrome: body 733 rows 76   · es (reciclable/orgánico 4 + 4) / sv fixtures clean
render G2-348 odd en: verify 0 lints 0 body 799 stage 799 slack 0 cards 189 x4 boxes 149 pics 104 trio glass,plastic,organic,metal odd cols 4321   · long chrome: body 733 cards 173 boxes 133 pics 93
render K-367 color en: REFUSED by construction — K-357 en: the en panel REFUSES the "color" face (refuse: color)
render K-367 color de-fixture: verify 0 lints 0 body 778 stage 656 lowest 799 vs foot 921 slack 122 stack 656 legend papier,verpackung,glas,rest,bio examples package+cereal sunscreen+shampoo corn+pepper jam+jar comb+bucket (pins 722 ok / 677 ok)   · long chrome: body 700 slack 44   · es (N = 2) / nl fixtures clean
render G1-365 open en: verify 0 lints 0 body 778 stage 778 slack 0 lanes 251 x3 draw 227 x3 (pins 722 ok / 677 ok)   · long chrome: body 733 lanes 236 draw 212
pictures on disk: 37 refs (37 bank items + every face picture) checked as <theme>/<noun>@2x.webp under image-library-webp/themes, 0 missing
sweep faces: which 20 item sets / 19 answer sequences · write 20 item sets / 18 bank orders · odd 18 odd vectors / 19 trio orders · color 19 legend orders / 20 example sets · open seed-invariant
poisons: PR3 PR4 PR5 PR6 PR13 PR12 PR14(+control) PR15 PS-F1(floats+fills) PS-F2 PS-F3 PS-F4(guard+verify+slack) PS-F5 PF1(x3 + control) PF2(+control) PF3(x3) PF4(x2) KILLED · the 31 base verdicts unchanged
PASS (2896 assertions, 56/56 poisons killed, 69 poison verdicts clean)          --quick: PASS (1630 assertions, 56/56 poisons killed, 69 poison verdicts clean, --quick: sweeps + fixture-locale renders skipped)
```
`node tools/gate-variation-distinct.js --batch=b4 --diffs=2 --family=recycling` → `[b4:recycling] compared 15 pairs over 5 faces against their bases + pairwise within family` / **`every variation differs from the deck its base publishes and from its siblings`**.
`node tools/b3-baseline.js --check --quick` → **`checked build 3572 + enum 255 in 18s: 0 drifted (0 expected), 0 missing` / `PASS`** (the base's d1 / d2 / d3 output is byte-identical: the base path of `_buildWith`, `RC_CSS`, `sortStrip`, `binRow` untouched).
`node i18n/build-en.js` → `643 types -> i18n/strings.en.json (title lint clean)` (left as generated). `node qa/verify-b4-bin-primitive.js` → `PASS (37876 assertions …)` (bin.js untouched). `node qa/verify-ruling-starters.js --quick --types=G1-364,G1-365` → `discovered 0 starter coordinates` (correct: neither face prints a starter at d2; nothing to add to its MUST_HAVE list).
200-seed probe per face over the en block and the de / sv / it / nl / fi / fr / es fixture blocks: every build succeeds, except K-367 on the blocks that `refuse:['color']` (sv / it / fi / fr fixtures = the fixtures' own refusal), which REFUSE by name on every seed.

## Deviations from §3 (each with the measurement)
1. **F1 chip column 360, not 330.** §3 / §2 say "width 5 x 64 + 4 x 10 = 330": the arithmetic is 360. `whichBinRow` takes `chipsW = N x chipTile + (N - 1) x 10`; the row is 30 + 12 + 72 + 12 + 360 + 24 = 510 <= 675. The guard refuses a chip set the row cannot hold.
2. **F1 rows `minmax(85px, 1fr)` FILL the body (108 at the one-line chrome), asserted by ratio, not capped.** The chip tile (84 with the border) is 0.78 of a 108 row (0.99 at the 677 pin): the K-355 F2 threshold (content >= 0.7 of the row) holds without a cap, so the design's `1fr` stands and the slack opens INSIDE the white cards rather than under them; the gate fires on a fixed-row / centred stage (PS-F1).
3. **F2 row minimum 68, not 62.** A 64 px tile in a 2 px-bordered card needs 68; 8 x 68 + 56 + 69 = 669 <= 677 still holds (62 was the writing row + border only). The design's "at 722 rows open to 74" becomes 76 at the 733 long chrome, 82 at the en 2-line title.
4. **F2 `perWordMax` = max(3, ceil(rows / words))** (4 for es's 2-word bank): "each distinct bank word >= 1 and <= 3 times" cannot fill 8 rows from 2 words. Non-es banks keep 3. The bank-order rule is applied to the FIRST-APPEARANCE order of the words down the page (8 rows carry repeats; a 5-word bank cannot equal an 8-row sequence), and the "nor its reverse" half only at >= 3 words (with 2 words every order is one or the other).
5. **F3 boxes GROW with the card** (`boxMax:150`, picture 70 % of the box) instead of a fixed 120 / 84: a fixed 120 box in the 189 px card of the en chrome measured 0.77 of its stage (PS-F3 fires at 0.85); at the 677 pin the boxes ARE the design's 120 / 84. `box` / `iconPx` stay the floors.
6. **F3 composer protects later trios** (deviation from the plain "trio class distinct across rows; odd class != trio class"): with metal at 4 items the first 20-seed sweep hit "class metal cannot give 3 items" once; an odd class still to come as a trio keeps >= 4 admissible items and a dead end re-rolls the trio order (200 / 200 seeds).
7. **F4 is a FIXED 656 px stack, top-anchored, not the design's 431 centred with "slack above and below".** The design's stack (legend 59 + bins 260 + shelf 64) floated 246 px of blank paper at the fi pin and 380 at the one-line chrome (the nt10-D SPARSE ruling; PS-F4 reproduces it: stack 462, slack 316). Built: legend in two balanced rows (90) + 10 + pills 30 + **binH 330** (config; guard 300..400) + 8 + a **stacked shelf of two 84 px pictures (117 x 188)** = 656: >= 630 (the reviewer's d2 stage floor, reused) and <= 677, one-line slack 811 - 656 = 155 <= 180. The crayon area grew (bin 117 x 330: lid 50, body 84 x 231) and the examples grew 56 → 84 (the bin body's own width) rather than a wider bin or a second bin row (§ Alternatives still hold: 5 x 117 is the width).
8. **F4 legend ALWAYS in two balanced rows** (a forced break after ceil(N / 2), row-gap 10; `data-lcs-legend-rows="2"`): five German entries measure ~745 px against the 663 px banner, and the browser's own wrap gave 4 + 1 at a 22 px row gap (banner 92, stack 678 > 677). One rule for every N keeps the stack identical in every locale (es: one entry per row, reads as a list). verify() fails a legend on any other number of rows.
9. **F5 lanes `minmax(196px, 1fr)` with a GROWING draw box** (`grow:true`: `flex:1 1 150px; min-height:150px`): the design's fixed 170 x 150 box in a 251 px lane read as a small box floating in a tall lane (PS-F5); the drawing space is the child's, so it takes the lane (227 at the en chrome, 172 at the 677 pin); the two ruling rows stay 56 (fixed glyph geometry) and centre.
10. **K-367 has NO en render and NO en string**: `render/one.js K-367 null 2 en` refuses with the panel's own refusal message (en `refuse:['color']`); the reviewable page is the gate's de / es / nl fixture render. The EN pair in the rows module is the §6 candidate for the locales that keep the face; `build-en.js` puts it into `strings.en.json` (a spec exists), which the EN wave must ignore.
11. **F5 starters at d2 are OFF for every locale by the shared config** (`starters:false`): the design's "a panel MAY author `starters` … only if the gate measures it <= 140 px" can only take effect at d3 (`starters:true`, unpublished) with the present one-config-per-face emitter; a per-locale opt-in would need `starters:'auto'` (print when authored), NOT built — the design's own default ("empty is the default and the honest one") is what ships.
12. **d1 / d3 of every face are supported by the builders (F1 `rows:4 / 8`, `shuffleChips:true`; F2 `rows:6`, `bank:false`; F3 `rows:3 / 5`, `organicOdd:false`; F4 `examples:1`; F5 `rows:1 / 3`, `starters:true`) but have no row**: the emitter ships ONE config for all three levels (README ruling).

## Per-locale refusals visible from the bank shape (Phase 4 `hub-expectations.json`)
- **en F4 REFUSED** (recorded: `refuse:['color']`, no K-367 string, `build()` refuses, no landing): en = **5**. Every other row of the README matrix stands: **65 (floor 60 if it / nl / sv / da / no withhold their colour tokens)**.
- **No other face refuses on the bank shape.** The 37 bank items resolve x11 and exist on disk as `@2x.webp` (37 / 37); F1 / F4 need only the bin set (N = 2..5 all render: es 2, fr 4, de / it / nl / sv / fi 5); F2 pools >= 24 over >= 20 families (es 37 with a 2-word bank at 4 + 4); F3 is locale-free; F5 is open.
- **sv `metall` pool of 1** (`can`): F1 never doubles the metall row (the composer's pool rule), so F1 ships; F4 is refused by sv's own `refuse:['color']` (the fixture) and by validator rule 4 only where F4 ships.
- **es N = 2:** F1 rows alternate the two bins strictly (the design's "no run of 2 equal answers" at N = 2 forces it) — a position pattern a child can notice; the design keeps it as a task ("copy says two bins"). The es panel may prefer a relaxed run rule at N = 2 (a config change, not a refusal).
- **Authoring that REFUSES if missing (never a filler):** a locale block needs `bins`, `materialWords` (>= 2 distinct), the face strings (one per face it keeps), and for F4 `colorConvention:'national'` + a token + colour word per bin with distinct tokens; an unauthored locale throws on every face.

## Open items for the panels / Phase 4
- **F4 legend words:** two rows of `colour word + bin word` at 17 px; a row of three entries wider than the 663 px banner would wrap a third row and fail verify() (the de fixture's three-entry row fits; the panels' literals are measured at render, never estimated).
- **F3 saucepan:** the dark-brown handle can read as wood at 100 px; a panel may `excludeItems` it with a reason (the bank keeps the critic's metal ruling).
- **F2 bank words:** de "Kunststoff" (10) fits the 400 ruling at glyphH 30; the design prefers "Plastik"; the panel decides the K-1 capitalisation of the German bank.
- **F1 at N = 2 (es):** the strict alternation (above); the es panel rules whether the run rule relaxes at N = 2.
- **Registration (not this build):** `coordinate.mode` = `which | write | odd | color | open`, levels K / G1 / G2 / K / G1; `apps.recycling` + the slug / name x11 per §1; the 11-locale Science strand row (`strand-names.ts`); `hub-expectations` en 5, others 6.
- **Reviewer:** `qa/verify-ruling-starters.js` needs no MUST_HAVE entry for this family (no starter surface at d2); if a locale ever ships F5 with `starters:true`, add `G1-365` then.
