---
name: project-pair-gate-tool
description: "Tool #53 The Pair Gate — the 2026-08-11 operator-ordered full transformation (pedagogy + design + art), what the panels found, and the standing lessons it bought"
metadata: 
  node_type: memory
  type: project
  originSessionId: f666d094-0b99-46ab-acfe-859fd297d219
  modified: 2026-08-11T08:48:54.602Z
---

# Tool #53 — The Pair Gate (rebuilt 2026-08-11)

Operator verdict on the original: "doesn't solve any teaching problem in an efficient way… no professional child-friendly visual design… not creative." Full transformation executed via three expert panels (pedagogy / interaction design / art) + ten native locale panels, commissioned goal-locked ("employ expert agents, don't ask, finish"). Commit `4121ce81`, wrapper 7.93→7.94, `pair-gate.js?v=2`.

## The two deep pedagogy defects (the rebuild's core)
- **The parade size was RANDOM** — the class chose nothing, which was literally the recorded GATE-4 objection in `premium-tools-v4.md:678` ("nothing the child chooses changes the answer"). Now: a 1–20 numeral strip; the prediction is a committed NUMERAL 0..k−1 (how many left standing) — a falsifiable computed claim; the inverse challenge ("find a parade that leaves exactly 2 at three abreast") exists.
- **⭐⭐ The second parade was RIGGED** — `_second()` silently nudged away multiples of k, making odd+odd=even UNFALSIFIABLE. Now any size is choosable, evens included; the fizzle (second parade clears → no one new for the sill) is a reachable, honestly-announced outcome. `bringSecond`/`setWidth` are dead and verify asserts they stay dead.
- Grammar: choose → commit → reveal, ×3 (parade, second parade, sill commit). Moat sentence: ten-frame/K-016/lids display a pairing after the fact; only here does the class commit a numeral claim about the remainder BEFORE the structure reveals it.

## Locked ethics that survived verbatim
Empty-seat-not-loner (leftover marcher byte-identical; only the dashed #7A6A55 seat is drawn); predict-before-the-bar-lifts; no red/green; no drag; part nouns ARCHWAY/PARADE/MARCHERS/SILL (never "pair"/"gate" — fr `pair` = EVEN); `strings.title.en === 'The Pair Gate'`; T_SILL never through `_dur()`; doubled focus ring.

## Panel-conflict adjudications (recorded)
- Numerals on stage: pedagogy won on load-bearing numerals (keystone k, chosen N, running through-count, per-file counts + standing count at standstill only, sill a+b), art won styling. Reveal discipline: givens when chosen, structure only after the archway has worked.
- Predict model: design's shape (`predict(st,n)` numeral, boolean REFUSED) — no fossil functions kept to appease old gates; the gates were rewritten to the new truth.
- Refusal: art's base (wall thud + coral jambs) + design's per-cause layers (bar flashes when the bar stops you, seats flash at the standstill).

## Standing lessons bought here
- ⭐⭐ **The native panels re-audited the SHIPPED strings, not just the new ones, and found systematic boundary-value grammar breaks at n=1/s=1/r=1 in SEVEN locales** (es "1 se quedan", nl "1 blijven staan", sv/no/da "1 gånger/ganger/gange", it/fr plural verbs, fi partitive-at-1). The trigger: the redesign made n=1 REACHABLE (chosen, not random ≥k+1). **When a rebuild widens a value range, every shipped {token} string must be re-audited at the new boundary values by natives.**
- ⭐⭐ **The docblock's own claim about Finnish ("partitive singular is right at 1") was REFUTED by the Finnish panel** — after numeral 1 Finnish takes the nominative. A grammar claim about a locale is a measurement only that locale's native can take.
- ⭐ **Two panels caught the same semantic lie in a "kept" string**: saidSillShort's "does not fill" was FALSE at the reachable overshoot (k=3, 2+2=4 — the plate holds MORE than a rank); the claim is exactness. And saidSecondClear's "nothing to bring to the sill" erased the first parade's still-standing child (3 panels); fixed to "no one new arrives".
- ⭐ **The da/no divisor idiom runs divisor-first** ("2 går op i 13", never "13 går op i 2") — the shipped saidStand literally said 13 doesn't divide 2 in two locales.
- ⭐ **The gate-fix chain fired twice on MY OWN gates before they caught anything real**: the seat regex condemned correct code (dash law lives in the seatURI builder, not the rule literal); the layout gate's page-overflow assertion was unmeasurable (the SHELL clips at `.lcs-app.embed{overflow-x:hidden}` — measured, then the assertion was deleted as measuring a thing that cannot exist); token parity had to be a SET not a multiset (es `de {k} en {k}` is legit — this same bug appeared independently in my fold script, the shipped verify-strings harness AND the shipped apply harness).
- ⭐ **Read-the-render findings that no gate saw**: the boom rendered as a 7px sliver that read as wall coursing (fixed: chest height, 0.46m thick); the keystone numeral measured 9px (floored at 12px, same later for the file numerals); a 5px cream gap between courtyard and wall (the negative-margin tuck was re-absorbed by the wallrow's own top margin); the road was a 10px stub at rest.
- **Row-word fence scope**: the fence (Reihe/fila/række/rad…) governs TOOL STRINGS only, not landing prose — the shipped landing copy uses them as ordinary words; two panels confirmed by precedent.
- `scripts/_pair-gate-strings.js` is now a REGENERATED MIRROR of the tool file (SoT = the tool; regenerate with `scripts/_pgt-regen-strings-data.js`; never hand-edit a locale there).

## Gate suite (all green at ship)
verify 23,781 asserts (exhaustive model incl. fizzle + evens-accepted + refusal lattice + CSS-source laws) · mutate 56/56 killed 0 faults (control-first, self-anchored EN needles adapted to the multi-line strings block) · probe 32 render checks ×4 viewports (keystone, reveal discipline, conservation, |sillW−archW|≤14, union tableau) · smoke 60 checks ×11 locales (strings-Proxy reachability, all 49 keys) · locale-layout 396 cells (11×6×k∈{2,5}, poison-armed) · liveness 87/0 across 3 entitlement states · print-sheets contract · preflight 61 tools 9 checks.

## Deferred / residual
- The pred-chip group re-arms for three different commits (parade 1, parade 2, sill) under one legend — watch first classroom feedback for confusion.
- During the sill's 800ms hold the act strip already rails "A new parade" (paint derives strictly from moves); trivially premature, accepted.
- es landing prose uses "fila" as ordinary prose (shipped precedent); the es panel offered alternates if the fence ever widens.
