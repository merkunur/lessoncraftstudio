# K-355 `five-senses` (K/G1) : FINAL design (editor merge, 2026-09-21)

Merged from `_work/K-355-pedagogy.md` + `_work/K-355-design.md` (record of every ruling: `_work/K-355-critic.md`). (m) = re-measured 2026-09-21 by read-only node or puppeteer with the shell's Baloo 2 / Nunito woff2 loaded from a `file://` origin (control `Wednesday` Baloo 2 700 26 = 135.1, the real-font figure; scratch `K-355-critic-measure.js`, `K-355-pool.js`, `K-355-sheet2.js`) or by OPENING the picture (the design agent's `K-355-sheet.png` 60 pictures + `K-355-chips.png` organ chips at 44 / 40 / 34, re-read by the editor; the editor's own `K-355-sheet2.png` for the 18 pictures neither sheet showed). *est.* = the engineer measures in the real render. No em-dashes.

**Boundary (load-bearing).** This family owns exactly one thing nobody has: **object to PRIMARY SENSE, anchored on the five fixed organ pictures `body parts/{eye, ear, nose, tongue, hand}`** as the constant answer key (`ORGAN_OF = {hear:ear, see:eye, smell:nose, taste:tongue, touch:hand}`; the sense is a property of the OBJECT, the organ is the answer). It is NOT K-211 `hot-and-cold` (`science-category-sort`, 14 items incl. `weather/sun`, `summer/campfire`, `winter/ice`, `winter/icicle`, `weather/snowflake`, `desserts and sweets/popsicle`: temperature is theirs), NOT G1-207 `food-groups` (24 items incl. `At the Supermarket/cheese`, `fruits/strawberry`, `At the Supermarket/bread`: food classification is theirs), NOT K-207 clothes, NOT K-344 `all-about-me-label-the-face` (LIVE: eye / ear / nose / mouth / hair copied as NOUNS onto lanes over a face picture) and NOT K-354 `human-body` (the drawn figure + body-part NOUNS; by its own Boundary "no face names a sense or uses tongue as an organ"), NOT K-225 (word to picture lines), NOT K-043 / K-044 / K-039 (`odd-one-out.js`: N of noun A + 1 of noun B on ONE theme, identity-based), NOT K-235 / the 21 `data/science/*.json` sorts (2-4 WORD-labelled bins), NOT K-213 helper-tool pairs. Structural fences the validator enforces (never taste): no item may be in `hot-vs-cold.json`, `food-groups.json` or `summer-vs-winter-clothes.json`; no item is a `body parts` noun; every printed word on the type is a sense VERB or an F5 sentence starter (never a body-part noun, never an object noun: objects are pictures only); the organ pictures appear ONLY as sense anchors. Odd-one-out here is BY CONCEPT across themes (four DIFFERENT pictures, three sharing a sense), never the K-043 identical-icon shape. Visual signature: the five pink organ pictures as a fixed column / strip / bin head, objects from many themes, one pencil mark per object.

## 1 Identity

| field | value |
|---|---|
| id / key / bands | `K-355` / `five-senses` / base **K**; F1 F2 F3 F4 F5 **G1** (`G1-354+ TBD by the emitter`; a locale may publish F3 under its G2 `level` key, §3). `default_subject: science` (7th science family after science-sort, science-sequence, science-match, seasons, animal-fact-file + human-body of this batch, m), `default_age_range: 5-7`, `assetClass: icon-placement`, `exerciseType: five-senses`. `apps['five-senses']` + `axes['exercise-type']['five-senses']` ABSENT (m); register with the `tools/register-b4-taxonomy.js` shape + slug/name x11. The 11 slugs below plus `sinnesorgane senses sinne zintuigen aistit sentidos sens sensi` grepped against every `axes.*` slug in every locale: **0 collisions** (m). |
| theme axis | **THEMELESS**: `themeAxis:{applicable:false}`; items are curated cross-theme `(theme, noun)` refs exactly like the 21 science banks, drawn from `data/science/five-senses.json` (§5); landings carry `coordinate.theme:''`. The five organ anchors are `body parts` pictures (34 files / 34 nouns with vocab x11, m; the five present x11 with singular + plural, m). **No `unitAxis`** (nothing fans on a non-theme unit). |
| CCSS (en, honest) | no K-2 CCSS code; NGSS 1-LS1-1 is animal structures and 4-LS1-2 is Grade 4, neither is claimed. **All six faces = readiness:** NO `educationalAlignment`; `teaches` = "The five senses (readiness)". Non-EN names the national framework + the science strand literal of table B (`strand-names.ts` has 0 Science rows, m: the build adds the 11-locale row from table B, the same row K-354 authors; never the English fallback). |
| new primitives / data | **No new primitive** (every drawing is a library picture, a page.css box, `writingRow` or `rulingBlock`). `templates/components-b4/five-senses.js` (8 components, §2), `data/science/five-senses.json` (GLOBAL concept bank), `data/b4/five-senses.js` + `data/b4/locales/five-senses.<loc>.json` (§5), `lib/b4-common.js bank()` over `data/b4/` (README open item 3; ABSENT, m). `templates/components-b4.js` barrel, `tools/apply-b4-locale.js`, `tools/validate-b4-draft.js` ABSENT (m): the build creates them. |

| loc | genre head (the panels verbatim; title = head) | ASCII slug | level K / G1 (`LEVEL_KEYS`, m) | verbs hear see smell taste touch (panel literals; the ONLY words the code prints besides the F5 starters) | national strand literal (this table IS the strand row) |
|---|---|---|---|---|---|
| en | Five Senses ("5 senses" in the META only; "printable" beats "worksheet" on this K head) | `five-senses` | `kindergarten` / `grade-1` | hear see smell taste touch | Science (readiness) |
| de | Die fünf Sinne (article kept, "fünf" lowercase; F4 title head = "Sinnesorgane", the Klasse 2-3 word, the organ-label face only; both July §1 gaps) | `die-fuenf-sinne` | `vorschule` / `1-klasse` | hören sehen riechen schmecken fühlen (lowercase infinitives; never Gehör / Geruch / Gesicht) | Sachunterricht |
| es (MX) | Los cinco sentidos (SEP form; "5 sentidos" meta only) | `los-cinco-sentidos` | `preescolar` / `primer-grado` | oír ver oler probar tocar (never "sentir") | Conocimiento del Medio (the K-354 ruling, decided once for both science families; the preescolar campo may be named in landing prose only) |
| pt (BR) | Os cinco sentidos (BNCC form; "5 sentidos" meta only) | `os-cinco-sentidos` | `educacao-infantil` / `1o-ano` | ouvir ver cheirar provar tocar (never "sentir") | Ciências (no descriptor code for the senses at 1º / 2º) |
| fr | Les 5 sens (the digit leads 43 vs 30, so the TITLE keeps it; "cinq" in the meta) | `les-5-sens` | `maternelle` / `cp` | entendre voir sentir goûter **toucher** ("sentir" = smell AND feel; touch is never "sentir") | Questionner le monde (GS "Explorer le monde" in landing prose only, the K-354 ruling) |
| it | I cinque sensi (F5 copy may say "classe seconda": the measured "descrivere con i 5 sensi" demand) | `i-cinque-sensi` | `infanzia` / `classe-prima` | udire (or ascoltare) vedere annusare (or odorare) gustare (or assaggiare) **toccare** ("sentire" = hear AND feel AND smell: printed NOWHERE on the type) | Scienze |
| nl | De vijf zintuigen (never a numeral in a nl title; F3 title must contain "zintuig") | `de-vijf-zintuigen` | `kleuters` / `groep-3` | horen zien ruiken proeven voelen | Oriëntatie op jezelf en de wereld |
| sv | Våra sinnen ("de fem sinnena" in copy; UR's series is a ranking neighbour, not a collision) | `vara-sinnen` | `forskola` / `ak-1` | höra se lukta smaka känna (bare infinitives, no "att") [NSR] | Naturorienterande ämnen (Lgr22 NO åk 1-3 "upplevelser av ljus, ljud, temperatur, smak och doft med hjälp av olika sinnen") |
| da | Sanserne ("De fem sanser" in copy) | `sanserne` | `boernehaveklasse` / `1-klasse` | høre se lugte smage føle [NSR] | Natur/teknologi |
| no | Sansene våre ("de fem sansene" in copy; bokmål) | `sansene-vare` | `1-trinn` / `2-trinn` | høre se lukte smake **kjenne or føle** (the panel picks ONE, `[NSR-FLAG][no]`) | Naturfag |
| fi | Viisi aistia (partitive after the numeral; NEVER the bare plural "Aistit" as a title or seed: polluted by "aistitieto") | `viisi-aistia` | `esikoulu` / `1-luokka` | kuulla nähdä haistaa maistaa tuntea ("tuntea" also = to know; the picture + organ disambiguate, the panel confirms) [NSR] | Ympäristöoppi |

**The rule that locks the type.** Every item carries ONE primary sense fixed in `data/science/five-senses.json` (`confidence:'strong'` = single-sense by any child's reading; `'signed'` = a second sense exists and the locale panel signs it in via `signedOk`); the answer on every verified face is `ORGAN_OF[item.sense]`, a constant map, never a word; the only words the code ever prints are the locale's 5 sense VERBS (`verbs[loc][sense]`, whole literals: the F1 bin labels and the F4 bank) and the 5 F5 starters (whole literals sized from the MEASURED font metrics), so nothing inflects, no object noun is printed (no `Fløyte` whistle-or-flute, no `Sieni` sponge-or-mushroom trap can reach a page) and no body-part NOUN is ever printed (the K-344 / K-354 fence holds by construction: a panel writing "Ohr" under the ear FAILS validator rule 5).

## 2 The base page

**Concept.** "Five Senses: Match the Picture to the Sense." Two calm columns. Left, five objects with five DIFFERENT primary senses (a drum, a rose, a rainbow, a lemon, a feather); right, the five organ pictures, deranged so nothing sits straight across. The child draws one line per object to the body part that senses it; every organ is used exactly once (a K exclusion strategy that stays honest because each object is unambiguous). No words on the body. From across the room: five pink organs down the right edge, five coloured objects down the left, a wide empty line zone between. 92 px pictures on a K page (floor 56), a 203 px line zone, nothing printed but title + instruction.

**Chrome budget (README ruling).** Body **722** with 3-line title + 3-line instruction (814 one-line; **677** with a 4-line fi title). `.ws-page` inner 675 (`page.css:16`, m); `* {box-sizing:border-box}` (`page.css:8`, m). Every stack below is quoted at 722 and checked at 677; slack goes into the line zone (`justify-content:space-around` / `space-between`), never into the tiles.

**Why NOT `makeSciencePairMatch.build()` (measured, the key contradiction of the two inputs):** rendered at n = 5 inside a 722 body with the shell fonts (m): items 132 high, the CSS column gap is **12.4** (`.ws-match-col gap:12px`, `page.css:366`, not the factory's internal 14), last item bottom **715.8 <= 722**: the factory FITS the 722 stack (the design's 728 is wrong by the gap). But it sizes from a fixed 720 (`science-pair-match.js:40`) and at the **677** fi stack the body scrolls to **720** (m): OVERFLOW on the fi 4-line title. Its 200 px items leave a 143 px line zone, and the pedagogy's `samplePairs` hook would EDIT a shared factory, which is forbidden (the shared factories are never edited). Ruling: the base is its OWN component `senseMatch` on the same `.ws-match` classes, budgeted at 722 AND 677.

**Layout d2**
```
x 0    40                    210 236          439 465                    635  675
y 0    +--------------------+  o              o  +--------------------+          row 1  itemH 122
       | object 92 (cream)  |                    | organ 92 (white)   |
       +--------------------+                    +--------------------+
y 134  gap 12 ... rows 2..5 identical          5 x 122 + 4 x 12 = 658
y 658  end of columns; container padding 6 top + 6 bottom = 670 <= 677 <= 722 (slack 7 / 52 -> space-around)
```
- Container `<div class="ws-match" data-ws-content data-lcs-layout="base" style="padding:6px 40px">` (inner 595). Items `.ws-match-item` `width:170px;height:122px` (the factory's 200 narrowed to widen the line zone). Left column x 40..210, dot `--right` at right:-26 -> x 236; right column x 465..635, dot `--left` at x 439; **line zone 203 px** (the factory's shape gives 143). Width 40 + 170 + 203 + 26 + 170 + 40 = **675** (m by the page.css offsets).
- Icons `.ws-icon` **92 x 92** (`min(92, itemH - 26)`), objects from `cache/themes-512` via `fileUri` (`image-cache/resolve.js:33`, m), no rotation. Floors: K min element 56 <= 92; 5 pairs in [4, 8].
- Right column = the 5 organs deranged (no organ straight across its object; the factory's do/while, `science-pair-match.js:36-38`, cloned).
- Stamps: left `data-lcs-item="<theme>/<noun>" data-lcs-sense="hear"`; right `data-lcs-organ="ear" data-lcs-sense="hear"`. The answer is the sense key on both sides; nothing printed.
- One-line chrome (814): `space-around` opens the gaps to ~48; items unchanged.

**Ladder** (resolved config keys; guards key on THESE, never the level index):

| key | d1 | d2 (ships) | d3 |
|---|---|---|---|
| `pairs` | 4 (4 senses sampled of 5) | **5** (all five senses) | 5 |
| `itemH / iconPx` | 140 / 100 (4 x 140 + 3 x 12 + 12 = 584) | **122 / 92** (670) | 122 / 76 |
| `pool` | strong | **strong** | strong + this locale's `signedOk` |
| `maxPerSense` | 1 | **1** | 1 |
| `organWords` | false | **false** | true: the verb under the organ (Baloo 2 700 18 teal, one line; 76 + 6 + 24 = 106 <= 118 inner) |

**d3 ruling:** the same move with a word to read (G1 register); it is not published and no copy describes it.

**Composer.** `rng.sample` `pairs` senses of the 5 (d2 = all five), for each ONE item from `items` with `sense === s` and `confidence` admitted by `pool`, then the family fence (never two of `families[0]` on a page; at one item per sense it cannot fire, it is asserted anyway); pair = `{left:{theme, noun, sense}, right:{organ: ORGAN_OF[s]}}`; rows shuffled, right column deranged. Seed is locale-neutral: same items, same derangement in all 11.

**Answer-hiding + uniqueness.** Each object has a different sense, so every organ is used exactly once; a wrong line is visible (an organ with two lines or none). `verify(page)`: `[data-lcs-layout]` on the root (base = `"base"`); 5 left items with pairwise distinct `data-lcs-sense`; right column = the 5 organ files each once (`src` ends with `body parts/<organ>@3x.webp`), a permutation with no row straight across; every `img.complete && naturalWidth > 0`; icons >= 56; no text node inside `.ws-match`; no `[data-lcs-answer]`. Node cross-check (`page.evaluate` cannot require): every left `data-lcs-item` is a bank item whose `sense` equals the stamp and whose `confidence` is admitted by the resolved `pool`; every right organ === `ORGAN_OF[sense]`; the family fence and `maxPerSense` hold.

**Reused (exact).** `page/page.css`: `.ws-match .ws-match-col .ws-match-item .ws-match-item--plain .ws-match-dot--left/--right` (`:354-391`, m), `.ws-icon` (`:178`), `.ws-lane` (`:401`, padding 12 16, inner 639; inner **647** with the stated inline `padding:10px 12px` and 647 x (h - 20) with `padding:8px 12px`, m), `.ws-card .ws-card-badge .ws-card-stage` (`:126-162`) via `cardGrid({cards, cols:1, rows})` (`templates/layouts/card-grid.js`, rows `minmax(0,1fr)`, m), `.ws-scene-banner .ws-bankword` via `wordBank({words, wordPx:18})` (`components-b2.js:228`, m: banner **60** high + `margin-bottom:10`), `rulingBlock({rows, w, h, glyphH, starters, gap})` + `starterFontPx({h, glyphH})` (`components-b2.js:69-92`, m: `starterFontPx({h:46, glyphH:30}).px = 34.5`, MEASURED metrics from `primitives/font-metrics.json`, nunito-700 xHeight 0.500), `writingRow({w, h, glyphH, xHeight:true})` (`primitives/trace-path.js:681`, m: h 62 renders **66**), `drawBox({w:120, h:100})` (`components-b3/read-and-do.js`, d1 of F5 only), `fileUri` (`image-cache/resolve.js`), `entriesFor` + `excluded` (`lib/b2-common.js`, validate-time), `hasPicture` (`lib/b3-picture-index.js`, validate-time), `lib/rng.js` `sample shuffle int`, `lib/b3-instructions.js fillSlots` (no slot exists on the type; imported for the K-333 refusal path), `bank(name, loc)` via `lib/b4-common.js` (an unauthored locale REFUSES, never falls back to en). The LOOK of `science-category-sort.js SCOPED_CSS` (`.sci-item .sci-dot .sci-bins .sci-bin .sci-bin-label`, `:26-34`) is cloned into the b4 component as `.fs-*` with the numeric changes of §3 F1.

**NOT used.** `makeSciencePairMatch.build()` (above) · `makeScienceCategorySort.build()` with `maxBins:5` (m by its own formula: 10 items -> `itemPx = min(78, floor(648/10) - 12) = 52`, box 52 + 12 + 4 = 68, 10 x 68 + 9 x 12 = 788 > 675 -> `flex-wrap` gives an 8 + 2 strip; one row of 10 needs itemPx <= 42 < the G1 floor 44; and at 5 bins with gap 18 a bin is (675 - 72) / 5 = 120.6 while the de `schmecken` pill at padding 16 + border 2.5 is 85.9 + 37 = **122.9** (m) > 120.6) · `makeOddOneOutType` (identity-based, theme-bound; only the `.ws-card-stage` look is cloned) · `pillChoice`, `letterBoxes`, `lineUpStrip`, `answerBox` (no written choice, no letter boxes, no ordinal strip, no answer box on any face; the instruction must not name them) · `image-vocabulary.js`, `objForms`, `approved-words-*.json` at render (no cloze frame exists; `objForms` holds 0 of the 5 organs in 10 locales and 4 of 5 in fi, m by the pedagogy; nothing is read from it) · the neighbour banks at render (validate-time only).

**NEW `templates/components-b4/five-senses.js`** (behind the `templates/components-b4.js` barrel; scoped inline CSS on token hexes only; stamps carry the truth; nothing prints an answer):
- `ORGANS = [{sense:'see', noun:'eye'}, {sense:'hear', noun:'ear'}, {sense:'smell', noun:'nose'}, {sense:'taste', noun:'tongue'}, {sense:'touch', noun:'hand'}]` (theme `body parts`; this order = the chip strip order on every surface) + `ORGAN_OF`.
- `senseMatch({pairs, itemW=170, itemH=122, iconPx=92, organWords=null})` -> the block above (`organWords` = `{sense: verb}` literals, d3 only; byte-identical when null).
- `senseSortStage({items, perBin, labels, organInBin=true, iconPx=64, cols=5})` -> `<style>` (cloned `.sci-*` rules renamed `.fs-*`) + a 2 x 5 tile strip + five bins (§3 F1 numbers).
- `organChips({order=ORGANS, tile=60, iconPx=46, gap=10})` -> five rounded-SQUARE tiles (white, border 2 creamDeep, r 12; NOT rings: the child's pencil circle must be the only circle on the row) = 5 x 60 + 4 x 10 = 340; stamps `data-lcs-chip="<organ>"`.
- `whichSenseRow({n, item, chips})` -> white card r 12, border 2 creamDeep, `display:grid;grid-template-columns:30px 72px 1fr 340px;column-gap:12px;align-items:center;padding:0 12px` (30 + 72 + 340 + 36 + 24 = 502 <= 675, spacer 173): badge (30 px teal circle, Baloo 2 700 16 white), object tile 72 (cream r 10, icon 60), spacer, chips; stamps `data-lcs-row data-lcs-n data-lcs-item data-lcs-sense`.
- `senseOddRow({items, oddIdx, box=96, iconPx=72})` -> `<div class="ws-card-stage" style="justify-content:space-evenly;padding:0 8px" data-lcs-items="4">` with four `span` boxes 96 x 96 (no border; the K-043 look) holding `.ws-icon` 72; the odd one `data-lcs-odd="1"`, every icon `data-lcs-item-sense`.
- `organLabelRow({organ, laneW=440, laneH=62, glyphH=36})` -> `.ws-lane` (inline `padding:10px 12px`, inner 647 x (row - 24)) `display:grid;grid-template-columns:84px 440px;column-gap:16px;align-items:center`: organ tile 84 (white r 12, icon 72) + `writingRow({w:440, h:62, glyphH:36, xHeight:true})` (renders 66, m); stamps `data-lcs-organ` (the verb is NEVER stamped: verify derives it from the organ + the locale bank).
- `senseLane({organ, starter, rows=2, laneW=560, h=46, glyphH=30, draw=null})` -> `.ws-lane` (inline `padding:8px 12px`, stated: inner 647 wide, height = content + 16 + 4) `display:grid;grid-template-columns:64px 560px;column-gap:12px;align-items:center` (64 + 12 + 560 = 636 <= 647): organ tile 64 (icon 56) + `rulingBlock({rows:2, w:560, h:46, glyphH:30, starters:{0: starter}, gap:6})` (= **106** high, m: 2 x 46 + 6 + 8 wrapper). With `draw:{w:120,h:100}` (d1 only) the columns are `64px 430px 120px` (638 <= 647).

**Alternatives (rejected).** Alt A the sense wheel (organs in a ring, ten objects around, lines inward): lines converge on a 150 px hub and cross; a teacher cannot read a wrong line; a diagram nobody has drawn. Alt B cut-and-glue sort: `picture-word-cards` (K-324) owns cut-and-play; cut lines eat 40 px; the sort by line (F1) delivers the same classification in one pencil. Alt C the pedagogy's factory reuse with two additive hooks: forbidden (shared factories are never edited) and the pair-match overflows the 677 stack (m). Alt D the pedagogy's F2 as a 2 x 4 card grid (card 330 x 171, chip strip under a 72 px picture): eight strips scattered over two columns read as eight keys; the design's eight ROWS with the strip in one fixed column read as ONE key, which is the teaching. Chosen: the two-column match + five distinct-act faces.

**Risks -> mitigations.** **The nose picture is the weak organ** (OPENED by all three: a red cone at 44 in a ring, a drop at 34, a nose at 56+): chips at 46 in the FIXED order (position + shape carry it), the bin icon at 44 only under its verb pill, everything else >= 56; never below 44. There is no other nose picture; the recorded fallback if a panel finds it unreadable in a real print is `body parts/mouth` for TASTE only (opened: an open mouth with teeth and tongue, clear at 44), the nose stays. **The tongue picture** shows lips + teeth + a tongue stuck out (opened x3): reads "tongue" at 56 and 44; a locale wanting "Mund / bouche" as the K taste organ swaps to `mouth` in its `organOf` override (data, no code) and says so. **Greyscale:** all five organs print as mid-grey; the eye (dark iris) and hand (fingers) stay distinct, the ear its inner curve, the tongue its dark mouth, the nose becomes a grey drop; teal pills and coral dots print as two clearly different greys; the engineer prints one d2 of F1 and F2 on a mono laser at 100 % and rings a chip in HB. **Long locales:** no wrapping text exists: pills are nowrap and measured (§3 F1), the bank is one measured row (§3 F4), starters are measured against the gate (§3 F5); a 3-line title is budgeted (722) and the 677 fi stack holds on EVERY face (base 670 · F1 405 · F2 664 · F3 fractional · F4 670 · F5 662). **Pencil space:** base zone 203, F1 zone 317, F4 writing row 440 x 62 (glyphH 36), F5 rows 560 x 46 x 2. **No cut lines.** **Palette:** cream, creamDeep `#F5E9D2`, white, teal, coral, ink, inkSoft, grid + the cloned `.fs-*` hexes `#F0E4CB` (the `.ws-match-item` border, already in page.css) and `#FBF3E4`; nothing else. **Font floor:** smallest text 16 (badge numerals) vs the 9 px lint (`qa/lints.js:86`); pills 17, bank 18, starters 34.5. Every stage stamps `[data-ws-content]`. What only a human eye catches: the nose at 44 in a real print, a panel verb that is a noun (`Geruch` vs `riechen`: the validator only catches the organ nouns), an object whose primary sense a national curriculum assigns differently (the panel's `signedOk` list).

## 3 Faces 2-6

All five are CODE faces on ONE additive `layout` knob (`d.layout` undefined = the base path, byte-identical; a face stamps `data-lcs-layout` only when declared; the K-354 sibling's shape), each with its own `verify()` branch; guards key on the config (`d.perBin`, `d.items`, `d.majorityRows`, `d.bank`, `d.rowsPerSense`, `d.pool`, `d.maxPerSense`), never on the level index. Resolved d2 configs are pairwise distinct (`tools/gate-variation-distinct.js --batch=b4`). None is PARAM: each changes the apparatus a teacher sees (columns / strip + bins / chip rows / odd rows / bank + lanes / ruled lanes). Density G1: pictures >= 44 (F1 64, F2 60 + chips 46, F3 72, F4 organs 72, F5 organs 56); items in [6, 12] (F1 10, F2 8; F3 20 pictures over 5 decisions; F4 / F5 five organs each, the type's constant). **Page rule on EVERY face** (config `maxPerSense`, default 2): no page carries more than 2 items of one sense as objects (the lock's "<= 2 smell items per page", generalised) and never two of one visual family (`families`); on F3 the rule applies to the ODD items only (a majority row is 3 of one sense by construction). Every face is buildable x11 on measured pools (§5: 38 strong items, the language is 10 literals per locale): **no face refusal in any locale.**

### F1 : Sort by Sense (G1, CODE `layout:'sort'`)
**Move:** SORT ten different pictures into five sense bins, two per sense; the bins carry the sense VERB and the organ picture. Nothing is one-to-one any more: the child holds five categories and places ten members. **Layout:** `senseSortStage`: strip = 2 rows of 5 tiles `.fs-item` (cream, border 2 `#F0E4CB`, r 14, padding 8, icon **64** -> tile 84; gap 12; row 5 x 84 + 4 x 12 = 468 centred; 2 rows + 12 = 180; strip padding 6 / 8 -> **194**), coral dot 11 with a 2 px white ring on each tile's bottom edge (the `.sci-dot` idiom). Bins `padding-top:26`, `.fs-bins{display:flex;justify-content:center;gap:14px}`, `.fs-bin{flex:0 0 123px;height:185px;background:#FFFFFF;border:3px solid #146B5E;border-top:3px dashed #146B5E;border-radius:0 0 18px 18px;position:relative}` (5 x 123 + 4 x 14 = **671 <= 675**); label pill `.fs-bin-label{position:absolute;top:-26px;left:50%;transform:translateX(-50%);white-space:nowrap;background:#FBF3E4;border:2.5px solid #146B5E;border-radius:999px;padding:6px 12px;font:700 17px 'Baloo 2';color:#146B5E}`; **widest pill (m, Baloo 2 700 17): de `schmecken` 85.9 -> 85.9 + 24 + 5 = 114.9 <= 123; it `assaggiare` 83.1 -> 112.1; `ascoltare` 71.4 / `annusare` 71.6; fr `entendre` 70.1; nl `proeven` 63.3; fi `maistaa` 60.9; pt `cheirar` 54.5**; pill centres 137 apart, so two neighbours never touch (114.9 / 2 + 86.8 / 2 = 101 < 137). `organInBin:true` = the bin's organ `.ws-icon` **44** centred at y 14 inside the bin under its pill. Stack **194 + 26 + 185 = 405**; line zone 317 at 722, 272 at 677 (`justify-content:space-between`). **Config d2** `{layout:'sort', perBin:2, bins:5, cols:5, iconPx:64, maxPerSense:2, pool:'strong', organInBin:true}`; d1 `{perBin:1, iconPx:76}` (5 items, one row); d3 `{perBin:3, iconPx:56, pool:'strong+signed'}` (14 items in rows of 5-5-4: smell holds `min(perBin, 2)` = 2 because its co-occurrable pool is 3 and the family fence leaves exactly one triple; stack 248 + 14 + 211 = 473). **Composer:** per sense `rng.sample` `perBin` items under the family fence (smell at d2 = one flower-family item + garlic or onion, or garlic + onion: 5 x 2 + 1 = 11 legal pairs, so the sweep must vary it); strip `rng.shuffle`; bins in a shuffled order that is NOT the canonical `ORGANS` order. **Verify:** stamps `data-lcs-tile="<sense>"` on tiles, `data-lcs-bin="<sense>"` on bins, `data-lcs-bin-label` on pills. Re-derived: bins === the 5 senses in a non-canonical order, exactly `perBin` tiles per sense, every tile's picture is a bank item whose `sense` === the stamp (node), pill text === `verbs[loc][sense]` (node), bin icon `src` === `fileUri('body parts', ORGAN_OF[sense])`, no picture repeated, family fence + `maxPerSense` held, pills one line (one `getClientRects()` box each), no tile row wraps (all tiles of a row share one `top`), icons >= 44. **Refusals:** none. **Query face:** en "five senses sort" (WINNABLE verbatim) · de "die fünf Sinne zuordnen / sortieren" · nl "zintuigen sorteren groep 3" · sv "sortera efter sinne" · fi "lajittele aistin mukaan".

### F2 : Which Sense? (G1, CODE `layout:'which'`)
**Move:** DECIDE per item: one picture, the five organ chips beside it in the FIXED order eye ear nose tongue hand on every row; circle the ONE organ you use (the "which sense" query verbatim); 8 independent decisions with the full choice set every time, never "circle all that apply" (lock). The constant strip is the teaching: the page reads as one key. **Layout:** 8 `whichSenseRow`s in `display:grid;grid-template-rows:repeat(8, minmax(76px, 1fr));gap:8px;flex:1 1 auto;min-height:0`: **8 x 76 + 7 x 8 = 664 <= 677 <= 722** (rows open to 83 at 722); card border 2 -> inner 72 = the object tile 72 (icon 60); chips 60 (icon 46). **Config d2** `{layout:'which', items:8, chipOrder:'fixed', tile:60, chipPx:46, pic:60, pool:'strong', maxPerSense:2, minPerSense:1}`; d1 `{items:6, tile:72, chipPx:56, pic:72}` (6 x 100 + 40 = 640); d3 `{items:8, chipOrder:'shuffled', pool:'strong+signed'}` (a different permutation per row, no two rows equal). **Composer:** 8 items over the 5 senses as {2, 2, 2, 1, 1} (every organ is the answer at least once, none more than twice), family fence, no picture twice; rows shuffled. **Verify:** stamps `data-lcs-sense` on the row, `data-lcs-chip=<organ>` per chip, `data-lcs-chip-order=fixed|shuffled` on the list. Re-derived: 8 rows; exactly 5 chips per row, the 5 organ files each once, equal size >= 44, no chip pre-marked (no ring, no fill), at `fixed` every strip in `ORGANS` order; exactly one chip per row with `organ === ORGAN_OF[sense]`; the row's picture is a bank item of that sense with an admitted `confidence` (node); sense counts {2,2,2,1,1}; family fence; no printed text but badges. **Refusals:** none (no text on the rows). **Query face:** en "which sense worksheet kindergarten / grade 1" (WINNABLE verbatim) · de "welcher Sinn?" · es "¿qué sentido usas?" · fr "quel sens ?" · it "quale senso?" · nl "welk zintuig?" · fi "millä aistilla?".

### F3 : Which One Does Not Belong? (G1, CODE `layout:'odd'`; a locale may publish it at its G2 `level` key, a landing-file decision)
**Move:** REASON about a group: four DIFFERENT pictures in a row, three share a sense (unstated), one does not; cross out the intruder. The K-043 verb on a CONCEPT instead of an identical icon. **Layout:** `cardGrid({cols:1, rows:5})` (rows `minmax(0,1fr)`: fractional, the grid always fits the body): row (722 - 4 x 14) / 5 = 133.2 (at 677: 124.2); card padding 12 + border 2 -> inner 105 (96 at 677); stage `padding:0 8px` -> four boxes **96 x 96** with icons **72** (>= 44), `space-evenly` over the 631 px inner width. **Config d2** `{layout:'odd', rows:5, items:4, box:96, pic:72, majorityRows:{hear:2, taste:2, see:1}, pool:'strong', maxPerSense:2}` (`maxPerSense` counts the ODD items; smell and touch are never a majority: smell has one co-occurrable triple, touch 5 strong; either may be the odd item); d1 `{rows:4, majorityRows:{hear:2, taste:2}, box:120, pic:84}` (the odd sense far from the trio: taste vs hear, never touch vs taste); d3 `{rows:5, items:5, box:84, pic:64, pool:'strong+signed'}`. **Composer:** row senses = the multiset of `majorityRows` in a shuffled order; per row 3 distinct items of the row sense + 1 odd item of another sense; odd column `rng.int(0,3)`; all 20 pictures distinct on the page; family fence (a flower-family item at most once per page); odd items obey `maxPerSense`; the odd column is not identical on 3 consecutive rows and never the same in more than `ceil(rows/2)` rows. **Verify:** stamps `data-lcs-row-sense` on the card, `data-lcs-odd="1"` on the intruder, `data-lcs-item-sense` on every picture. Re-derived: 4 pictures per row, exactly one odd, its sense !== the row sense, the other three === the row sense, row-sense multiset === `majorityRows`, all pictures distinct, family fence, `maxPerSense` on odds, odd-column rule, no picture crossed out or greyed at render, icons >= 44. **Refusals:** none. nl title must contain "zintuig" (the nl `odd-one-out` family head is "Welke Hoort Er Niet Thuis", m; validator rule 9). **Query face:** en "five senses odd one out" · de "Was passt nicht? Die fünf Sinne" · nl "welk zintuig hoort er niet bij" · sv "vad passar inte in? sinnen" · fi "mikä ei kuulu joukkoon? aistit".

### F4 : Sense Organs: Write the Sense Word (G1, CODE `layout:'label'`; de title from the "Sinnesorgane" head)
**Move:** NAME the sense of each organ: the five organ pictures each with a school-lined writing row; a 5-word bank of the sense VERBS on top; copy the right verb beside the organ that does it. Differs from K-344 / K-354 because the word is a VERB of what the organ DOES, never the organ's name. **Layout:** `wordBank({words: shuffled verbs, wordPx:18})` **60** + margin 10 (m; one row everywhere: de 509 · it (assaggiare) 567 · fi 490 <= 647, m) then 5 `organLabelRow`s in `repeat(5, minmax(112px, 1fr))` gap 10: **70 + 5 x 112 + 4 x 10 = 670 <= 677 <= 722** (rows open to 122 at 722); row = `.ws-lane` `padding:10px 12px` (inner 647 x 88) `[organ tile 84, icon 72][16][writingRow 440 x 62, glyphH 36 (renders 66 <= 88)]` = 540 <= 647. **Config d2** `{layout:'label', organs:5, bank:true, starter:false, laneW:440, laneH:62, glyphH:36, organPx:72}`; d1 `{organs:3, bank:true, starter:true, glyphH:40, rows 3 x 190}` (eye ear hand; the starter = the verb's first letter on the row via `rulingBlock` starters, sized by `starterFontPx`); d3 `{organs:5, bank:false}` (retrieve the verb: a G2 move, unpublished). **Composer:** organ rows in a shuffled order; bank = `rng.shuffle(verbs)` redrawn until it differs from the row order in >= 3 positions (position leak). **Verify:** stamps `data-lcs-organ` per row, `data-lcs-bank-word` per bank word (the `wordBank` stamp), NO verb stamp on the row. Re-derived: bank word set === `{verbs[loc][s]}` (5, distinct, case-sensitive; node), organ rows === the 5 organ files each once, bank order !== row order in >= 3 positions, lanes carry no text at d2 (no `[data-lcs-starter]`; at d1 exactly one starter glyph per row = the first letter of `verbs[loc][SENSE_OF[organ]]`), `writingRow` present with the 3 rules, `glyphH >= 26`, no verb printed outside the bank, no verb equals the vocab singular or plural of any organ in that locale (rule 5). **Refusals:** none. fr touch = "toucher"; it hear = "udire" or "ascoltare", touch = "toccare"; pt smell = "cheirar"; no touch = "kjenne" or "føle" (ONE); de lowercase infinitives. **Query face:** de "Sinnesorgane beschriften" (its own head, a July §1 gap) · en "sense organs worksheet" ("5 sense organs" tail) · es "los cinco sentidos y sus órganos" · pt "órgãos dos sentidos" · fr "les organes des sens" · it "gli organi di senso" · nl "zintuigen benoemen" · sv "sinnesorgan" · fi "aistielimet".

### F5 : I Can Hear ... (G1, CODE `layout:'write'`, OPEN-ENDED: no verify)
**Move:** WRITE from experience: five lanes, each opened by an organ picture and a sentence starter printed on the first ruling row, a second row to finish; the it "descrivere con i 5 sensi" demand (measured x3, classe seconda writing) and the en "five senses writing" tail. **Layout (the measured ruling):** 5 `senseLane`s in `repeat(5, minmax(126px, 1fr))` gap 8: **5 x 126 + 4 x 8 = 662 <= 677 <= 722**; lane `padding:8px 12px` (stated; inner 647 wide), height 106 + 16 + 4 = 126; grid `64px 560px` gap 12 (636 <= 647): organ tile 64 (icon 56) + `rulingBlock({rows:2, w:560, h:46, glyphH:30, starters:{0: starters[loc][sense]}, gap:6})` = 106 (m). **The starter is sized by `starterFontPx({h:46, glyphH:30}) = 34.5 px** (m) and gate D of `qa/verify-ruling-starters.js` (`:183`, m) refuses a starter wider than **0.5 x the row = 280 px**. Measured at 34.5 px Nunito 700 (m): en "I can hear ..." 190 · fi "Maistan ..." 162 · sv "Jag smakar ..." 213 · de "Ich kann hören: " 243 but "Ich kann schmecken: " **327** · fr "Je peux entendre ..." **306** · it "Posso assaggiare ..." **312** · pt "Eu posso cheirar ..." **298** · es "Puedo saborear ..." **287** · nl "Ik kan proeven ..." 268 · da "Jeg kan smage ..." 268 · no "Jeg kan kjenne ..." 268. Therefore: (a) the d2 row is **560 wide with NO draw box** (the design's 430 + draw box leaves a 215 limit that even "Ich höre ..." barely clears; the draw box is d1's); (b) **the "I can" ability frame is a panel choice ONLY where its longest starter measures <= 280** (nl / da / no clear it by 12 px; de / fr / es / pt / it must write the bare present: "Ich schmecke ...", "J'entends ...", "Pruebo ...", "Eu cheiro ...", "Assaggio ..."); validator rule 7 measures every starter with the real font and refuses. **Config d2** `{layout:'write', rowsPerSense:2, rowH:46, glyphH:30, laneW:560, organPx:56, draw:null}`; d1 `{rowsPerSense:1, rowH:52, glyphH:34, laneW:430, draw:{w:120, h:100}}` (draw-led; starters measured against 215); d3 `{rowsPerSense:3, rowH:40, glyphH:26}` (5 x (3 x 40 + 12 + 8 + 20) + 32 = 732 > 722: d3 is legal only at `rowH:38`, re-measured; unpublished, say so). **Verify:** none (open). Layout lints + `verify-ruling-starters.js` (a NEW `MUST_HAVE` entry for K-355's F5 id; gates A-E on every starter, discovered by `build()`) + the gate's structural checks: 5 lanes, each organ once, each lane's starter literal === `starters[loc][SENSE_OF[organ]]` (node), `rowsPerSense` rows per lane, every starter `<= 0.5 x rowW`, `glyphH >= 26`, nothing below the footer. **Refusals:** none (a frame that overflows is rewritten, not refused). **Query face:** it "descrivere con i 5 sensi" (verbatim) · en "five senses writing worksheet" · de "die fünf Sinne Sätze schreiben" · fr "les 5 sens : j'entends, je vois" · pt "os cinco sentidos frases".

**Rejected non-moves.** "Circle all the senses you use" (lock: never multi-answer) · a NOUN-of-sense label face (de Gehör / Geruch, es el gusto / el tacto: G3 register, and it collides with the organ-noun fence where the noun is the organ, es "el oído") · a theme swap (themeless) · "hot or cold by touch" (K-211) · "which food do you taste" (G1-207) · "label the eye / ear / nose" (K-344 / K-354) · relabelling d1 or d3 of the base as a face (same move; fails `gate-variation-distinct`) · "count the senses / organs" (a counting page) · an identical-icon or own-shadow odd-one-out (K-039 / K-043) · a "colour the organs" face (colour-by-legend is K-354 F2 / G1-242) · the design's 4-row F3 at 120 px boxes (a K page count on a G1 face; ruled to 5 rows).

## 4 Native rebuild plan x11

Frames NEVER inflect: this type has NO cloze frame, NO printed noun and reads NO `objForms`. Per locale the panel authors: `strings` (6 titles + 6 instructions), `verbs` (5 whole infinitive literals: the F1 bin labels and the F4 bank), `starters` (5 whole first-person literals for F5, each measured <= 280 px at 34.5 px; the ability frame only where it fits), `signedOk` (which `signed` items this locale admits; default = the EN set), an optional `organOf` override (`taste:'mouth'` only), `refuse` (per face; none expected), and audits the EN as a SOURCE. = 22 literals + the signature. Instructions name only apparatus on the page (pictures, the body parts, boxes, the word bank, lines) and at K are one imperative sentence. Titles: base = the bare head; each face adds one NOUN / verb; F4 is the ORGAN head in every locale.

| loc | literals (counts) | forms / slots and where they exist | refusal / re-target | traps |
|---|---|---|---|---|
| en | 12 strings · 5 verbs · 5 starters ("I can hear ..." 190 px, fits) = 22 | none | none | US spelling; "Five Senses" title, "5 senses" meta only; "touch" is the K word (skin is G3+); "printable" in the meta |
| de | 12 · 5 (hören sehen riechen schmecken fühlen) · 5 (bare present: "Ich schmecke ...", "Ich rieche ..."; "Ich kann schmecken:" = 327 > 280, m) = 22 | none | none | never Gehör / Geruch / Gesicht; "Die fünf Sinne" keeps the article, "fünf" lowercase; F4 head "Sinnesorgane" (105 px at Baloo 17, m); engine appends "Arbeitsblatt"; the K-344 / K-354 heads (Gesicht / Körper) in no K-355 title |
| es (MX) | 12 · 5 (oír ver oler probar tocar, accents) · 5 (bare present "Oigo ...", "Pruebo ..."; "Puedo saborear ..." 287 > 280, m) = 22 | none | none | MX register; never "sentir" for touch; "el oído" (sense) vs "la oreja" (organ) is why only VERBS print; "5 sentidos" meta only; strand literal fixed with K-354 |
| pt (BR) | 12 · 5 (ouvir ver cheirar provar tocar) · 5 (bare present "Eu ouço ...", "Eu cheiro ..."; "Eu posso cheirar ..." 298, m) = 22 | none | none | never "sentir"; "paladar" / "olfato" are nouns, not printed; "atividade" never in a title; "5 sentidos" meta only |
| fr | 12 · 5 (entendre voir sentir goûter **toucher**) · 5 (bare present "J'entends ...", "Je touche ..."; "Je peux entendre ..." 306, m) = 22 | none | none | **touch is never "sentir"** (rule 6, poisoned both ways); title "Les 5 sens" keeps the digit, meta "cinq"; NBSP before `?` / `:`; "fiche" never in a title; elision is the panel's |
| it | 12 · 5 (udire / ascoltare, vedere, annusare / odorare, gustare / assaggiare, **toccare**) · 5 (bare present "Ascolto ...", "Assaggio ..."; "Sento ..." FORBIDDEN; "Posso assaggiare ..." 312, m) = 22 | none | none | **"sentire" printed nowhere on the type** (rule 6); F5 owns "descrivere con i 5 sensi" (classe seconda copy allowed); "scheda" never in a title; `assaggiare` pill 112.1 fits (m) |
| nl | 12 · 5 (horen zien ruiken proeven voelen) · 5 ("Ik kan proeven ..." 268 <= 280 fits, m; or "Ik proef ...") = 22 | none | none | "het zintuig / de zintuigen"; never a numeral in a nl title; F3 title contains "zintuig"; engine appends "werkblad" |
| sv | 12 · 5 (höra se lukta smaka känna, no "att") · 5 (bare present "Jag hör ...", "Jag smakar ..." 213, m) = 22 | none | none | sense nouns syn / hörsel / känsel / smak / lukt are NOT printed; title "Våra sinnen"; engine appends "arbetsblad"; never «grupp» [NSR] |
| da | 12 · 5 (høre se lugte smage føle) · 5 ("Jeg kan smage ..." 268 fits, m; or "Jeg smager ...") = 22 | none | none | "følesans" / "lugtesans" not printed; title "Sanserne"; engine appends "opgaver" [NSR] |
| no | 12 · 5 (høre se lukte smake **kjenne or føle**, ONE, with the reason recorded) · 5 ("Jeg kan kjenne ..." 268 fits, m) = 22 | none | none | bokmål; "berøringssans" not printed; title "Sansene våre"; `[NSR-FLAG][no]` on the touch verb |
| fi | 12 · 5 (kuulla nähdä haistaa maistaa tuntea) · 5 first-person literals ("Kuulen ...", "Näen ...", "Haistan ...", "Maistan ..." 162, "Tunnen ...") = 22 | none (fi `part` / `gen` forms exist for 4 organs; no frame uses them) | none | title "Viisi aistia", NEVER "Aistit"; cases are the panel's ("Yhdistä esine oikeaan aistiin." illative, "Ympyröi aisti, jolla huomaat sen."); "tuntea" also = to know (picture + organ disambiguate); every stack holds at the 677 fi budget (m), so no fi title-length rule is needed [NSR] |

Every panel OPENS every picture its faces keep (the 45 bank items + the 5 organs; the sv #35 `fruits/plum` precedent), signs `items[].sense` for the `signed` items it admits, and reads one render of every face in its locale.

## 5 Data + gates

`data/science/five-senses.json` (GLOBAL concept bank, the same directory + shape family as the 21 live science banks; locale-neutral) + `data/b4/five-senses.js` (EN `verbs` / `starters` / `strings`) + `data/b4/locales/five-senses.<loc>.json` GENERATED from `i18n/.draft-b4-<loc>.json` by `tools/apply-b4-locale.js` after `tools/validate-b4-draft.js` (b4 clones of the b3 pair; `data/` gitignored, `git add -f`); read through `lib/b4-common.js bank('five-senses', loc)`.
```
FIVE_SENSES (data/science/five-senses.json, GLOBAL; every item OPENED: (p) pedagogy sheet, (d) design sheet, (e) editor sheet2):
  senses:  ['hear','see','smell','taste','touch']
  organOf: { hear:'ear', see:'eye', smell:'nose', taste:'tongue', touch:'hand' }      // theme 'body parts'; all 5 opened (p d e); 'mouth' opened as the recorded taste fallback
  items: [ // ONE primary sense each; 38 strong + 7 signed = 45; every item resolves via fileUri + entriesFor(theme, loc) in all 11 locales (m)
    // hear: 10 strong + 2 signed
    {theme:'music', noun:'drum', sense:'hear', confidence:'strong', picOpened:true}, guitar, trumpet, piano, violin, xylophone, saxophone (music, strong; saxophone opened p e),
    {theme:'christmas', noun:'bell'}, {theme:'accessories', noun:'whistle'}, {theme:'around the house', noun:'telephone'} (strong),
    {theme:'music', noun:'flute', confidence:'signed', note:'an orange tube with holes; reads as a recorder (still heard)'},
    {theme:'weather', noun:'thunderstorm', confidence:'signed', note:'a cloud + a LIGHTNING bolt (a see cue); admit only where the panel says thunder'},
    // see: 7 strong + 4 signed
    rainbow (weather), moon (camping), telescope (space), binoculars (camping), star (christmas: a gold 3D star; NOT shapes/star), flashlight (camping), lights (christmas: a string of coloured bulbs, opened d e) = strong,
    television (around the house, signed: also hear), book (classroom, signed: read), lamp (around the house, signed: a teal lamp with a red shade), lighthouse (beach, signed: a building; "see its light" is a convention),
    // smell: 7 strong, family-fenced
    flower (spring: an orange daisy), rose (flowers), lavender (flowers), lilac (flowers: 3 purple blooms), jasmine (flowers: a PINK 5-petal flower, opened p e),
    garlic (vegetables: a purple-striped bulb, opened p e; NOT At the Supermarket/garlic, a white bulb, a different file, m), onion (vegetables: a brown onion with a sprout),
    // taste: 9 strong + 1 signed
    lemon (fruits), honey (breakfast: a jar with a blank label), candy (At the Supermarket: a wrapped sweet), lollipop, cookie, cake (desserts and sweets: a slice with a strawberry; NOT bakery/cake, a different file, m), cupcake (desserts and sweets: with a cherry, opened d e),
    popcorn (At the Supermarket: a striped box), chocolate (easter: a bar) = strong; pizza (At the Supermarket, signed: also smell),
    // touch: 5 strong
    feather (easter), sand (summer: a pile), sponge (kitchen tools: a yellow rectangular slab with holes; the cheese wedge it resembles is fenced OUT of the type, so nothing on a page competes), pillow (around the house), rock (camping: a grey stone; NOT beach/rock, a purple crystal) ]
  families: [['flower','rose','lavender','lilac','jasmine']]                // never two of one family on a page
  pageRules: { maxPerSense:2, majorityRows:{hear:2, taste:2, see:1} }     // defaults; every face's config restates what it uses
  excludedByRule (recorded, never items): sun · campfire · ice · icicle · snowflake · popsicle (hot-vs-cold.json) · cheese · strawberry · bread (food-groups.json) · sweater · scarf · mittens (K-207) ·
    alarm_clock · picture_frame · ice_cream · chocolate_bar · teddy_bear (NO vocab key, m: rule 2 cannot pass) · mirror (opened: an arched frame, reads window) · candle (see / smell) · shampoo (touch / smell) ·
    cushion (the pillow's twin at 56, opened e) · ball (see / touch) · hedgehog (an animal: the library draws products, and a child says "see") · coral · eraser (ambiguous, d) · beach/rock · shapes/star · At the Supermarket/garlic · bakery/cake (weaker twins) · glue · rope · seashell (p)
FIVE_SENSES_L10N[loc] = {
  verbs:    { hear, see, smell, taste, touch },      // whole infinitive literals; the F1 pills + the F4 bank
  starters: { hear, see, smell, taste, touch },      // whole first-person literals for F5, each <= 280 px at 34.5 px (m)
  signedOk: ['pizza','television','book','lamp','lighthouse','flute','thunderstorm'] ⊆ the signed nouns,   // per locale; default = EN
  organOf:  {} | { taste:'mouth' },                   // the only override allowed
  refuse:   [],                                       // 'sort'|'which'|'odd'|'label'|'write'
  strings:  { 'K-355':{title, instruction}, <F1..F5 ids>:{title, instruction} } }
```
Helper contract: `bank('five-senses', loc)` returns the merged object; `verb(loc, sense)` and `starter(loc, sense)` throw on a missing literal (never fall back to en); `organOf(loc, sense)` applies the override; `itemsFor(sense, pool, loc)` = strong items + this locale's `signedOk` when the pool admits signed.

**`tools/validate-b4-draft.js` (five-senses block; every rule runs, exit 1 on any):**
1. `senses` === the 5 keys; `organOf` maps each to one of `eye ear nose tongue hand` (a locale override may set `taste:'mouth'` only), each organ once; every organ resolves via `fileUri('body parts', organ)` and has a vocab singular + plural x11 (m: all present).
2. Every item `{theme, noun}` resolves via `fileUri` (throws on a missing cache entry), its theme dir carries no B&W marker (`\b(bw|sw|bn|nb|zw|sh|pb|mv|sv)$`), `picOpened:true`, a non-null `vocabKey` in `cache/manifest.json`, is present in `entriesFor(theme, loc)` in ALL 11 locales and `hasPicture(vocabKey, loc)` x11, `vocabKey ∉ excluded` (the exported set of `lib/b2-common.js`; the internal name is `B2_EXCLUDE`, m), `sense` ∈ senses, `confidence` ∈ {strong, signed}; a `signed` item carries a non-empty `note`.
3. **Boundary fences (read the neighbour banks at validate time, never on the page):** no item `theme/noun` or `vocabKey` appears in `hot-vs-cold.json`, `food-groups.json` or `summer-vs-winter-clothes.json` items; no item noun is one of the 5 organs or any `body parts` noun; no noun in two items.
4. Per sense: strong items >= 5 AND co-occurrable strong items (after collapsing each family to one) >= 3; every `majorityRows` sense has >= 6 co-occurrable strong items (two disjoint triples) and `majorityRows[s] <= floor(coOccurrable_s / 3)`; every family is a subset of one sense.
5. `verbs[loc]`: 5 non-empty distinct literals, no `{`, no digit, <= 14 chars, none equal (`toLocaleLowerCase(loc)`) to the vocab singular OR plural of any organ or of any item noun in that locale (the organ-noun fence: "Ohr" under the ear FAILS); the widest verb at Baloo 2 700 17 + 29 <= 123 (rendered, the K-320 rule-5 precedent).
6. Locale bans, each poison-tested in BOTH directions: fr `verbs.touch !== 'sentir'` and no touch starter contains `sens`; it: `sentir` / `sento` / `senti` in NO literal of the type; pt `verbs.smell !== 'sentir'`; de: no `Gehör` / `Geruch` / `Gesicht`; MUST_PASS controls fr "toucher", it "ascolto", de "hören", pt "cheirar".
7. `starters[loc]`: 5 non-empty literals ending in "..." or ":", no `{slot}`, rendered width at `starterFontPx({h:46, glyphH:30}).px` (Nunito 700, real font) **<= 280** (0.5 x 560, gate D) and, when a d1 draw box is declared, <= 215.
8. `signedOk` ⊆ the signed nouns; every face's d2 pool reaches its config (base 5 senses x 1; sort 5 x 2 with smell co-occurrable >= 2; which {2,2,2,1,1}; odd `majorityRows` + 5 odds under `maxPerSense`; label 5; write 5) else `refuse` names the face.
9. Titles <= 70 chars, no worksheet-word (`WORKSHEET_WORD` regex of the b3 gate), unique in the band, never equal to a neighbour family's `axes['exercise-type'].*.name` (science-sort, visual-discrimination, odd-one-out, human-body) nor to any `axes.theme.*.slug/name` in that locale; nl F3 title contains "zintuig"; fi titles never carry the bare token "Aistit" as the head; nl titles carry no digit; fr base title may carry "5"; instructions <= 150; the six titles differ pairwise by >= 1 noun or verb token; no visible string contains the locale's free-word.
10. `strings` ids === {base + the 5 allocated face ids}; every face id present in every locale or named in `refuse`.

**`qa/verify-b4-five-senses.js`** (render gate; clone the b3 shape): renders base + 5 faces x 11 locales x d2 through `render/render-instance.js` (file:// fonts) under the 722 stack (3-line de title + 150-char instruction) AND the 677 stack (4-line fi title); asserts `verify()` empty and `qa/lints.js` clean; then ITSELF (no size lint exists): base pictures >= 92 (K floor 56), F1 tiles >= 64 and the bin organ >= 44, F2 pictures >= 60 and chips >= 44 equal, F3 pictures >= 72, F4 organs >= 72 with `glyphH >= 36` and empty lanes, F5 organs >= 56 and every starter at `data-lcs-starter-px === 34.5` with width <= 0.5 x row (`verify-ruling-starters.js` discovers the surfaces), pills one `getClientRects()` box, strip rows share one `top`, the answer organ never printed as a word, no `[data-lcs-answer]` anywhere, no numeral glyph outside badges, everything above the footer. NODE cross-checks per face: base pairs (sense -> organ file, distinct senses), F1 (pill === verb, icon === organ, tiles' sense === bin), F2 (one chip === `organOf[sense]`, canonical strip at `fixed`, {2,2,2,1,1}), F3 (row-sense multiset === `majorityRows`, one odd, odd column varies), F4 (bank set === verbs, no verb stamp, bank order !== row order), F5 (starter literal per organ); page rules (`maxPerSense`, family fence, no picture twice) on every face. SWEEP: 20 seeds x d2 per face render distinct pages (base item SET varies; the F1 smell bin varies over its 11 pairs; F2 answer positions vary; F3 odd columns and majority ROW ORDER vary; F4 bank order varies).
**Poison (each must FAIL for its OWN reason; the correct EN draft is the control):** P1 item `{theme:'weather', noun:'sun', sense:'see'}` -> rule 3 (K-211). P2 `{theme:'At the Supermarket', noun:'cheese', sense:'smell'}` -> rule 3 (G1-207). P3 `{theme:'toys', noun:'teddy_bear', sense:'touch'}` -> rule 2 (vocabKey null). P4 `organOf.touch = 'skin'` -> rule 1 (no picture). P5 `oddMajority += 'smell'` -> rule 4 (co-occurrable 3 < 6). P6 de `verbs.hear = 'Ohr'` -> rule 5. P7 fr `verbs.touch = 'sentir'` -> rule 6 MUST_FIRE; fr `'toucher'` PASSES (control). P8 it `starters.hear = 'Sento ...'` -> rule 6. P9 de `starters.taste = 'Ich kann schmecken: '` -> rule 7 (327 > 280, m); de `'Ich schmecke ...'` PASSES (control). P10 fi `starters.hear = 'Kuulen {noun}'` -> rule 7 (slot). P11 nl F3 title `'Wat hoort er niet bij?'` -> rule 9. P12 fi base title `'Aistit'` -> rule 9. P13 `strings` missing the F4 id in sv without `refuse:['label']` -> rule 10. P14 two items with noun `rock` (camping + beach) -> rule 3. **Render poisons:** PR1 base right column with an organ straight across -> derangement. PR2 base with two hear items -> "senses not distinct". PR3 F1 a pill labelled with the organ NOUN ("Nase") -> "pill !== verb". PR4 F1 two flower-family tiles -> family fence. PR5 F2 a row with 4 chips -> "5 chips". PR6 F2 the ear chip pre-ringed -> "chip pre-marked". PR7 F3 a row of 3 smell + 1 hear -> "majority ∉ config". PR8 F3 every odd in column 4 -> position leak. PR9 F4 a bank in the row order -> order leak. PR10 F5 a starter at 0.9 x glyphH (27 px) instead of `starterFontPx` -> the starters gate (the G1-306 defect class). PR11 F2 `chipOrder` resolved from a level-index guard with d2 re-pointed to d3's config -> the config guard fires before render. PR12 F1 the factory's 8 + 2 wrapped strip -> "strip row wraps". PR13 the base built with `makeSciencePairMatch` under the 677 stack -> footer lint (720 > 677, m). PR14 F5 with the design's 430 row + draw box and the de frame -> gate D.
**Page reads:** `FIVE_SENSES`, `FIVE_SENSES_L10N[loc]`, `fileUri`; never `image-vocabulary.js`, `objForms`, `approved-words-*.json` or the neighbour banks at render (validate-time only).

## 6 SEO plan

| face | title pattern (Germanic / Romance / Nordic + fi) | meta MIDDLE (whole 120-170; the child's instruction, en) | coordinate |
|---|---|---|---|
| base | Five Senses · Die fünf Sinne · De vijf zintuigen / Los cinco sentidos · Os cinco sentidos · Les 5 sens · I cinque sensi / Våra sinnen · Sanserne · Sansene våre · Viisi aistia | Draw a line from each picture to the body part you use for it: eye, ear, nose, tongue or hand | `{type:'five-senses', mode:'base', theme:'', level:<K key>}` |
| F1 | Five Senses: Sort by Sense · Die fünf Sinne: nach Sinn sortieren · De vijf zintuigen: sorteer op zintuig / Los cinco sentidos: clasifica por sentido · Os cinco sentidos: separe por sentido · Les 5 sens : trie par sens · I cinque sensi: classifica per senso / Våra sinnen: sortera efter sinne · Sanserne: sortér efter sans · Sansene våre: sorter etter sans · Viisi aistia: lajittele aistin mukaan | Look at each picture and draw a line from its dot to the box of the sense you use for it | `mode:'sort'`, G1 |
| F2 | Five Senses: Which Sense? · Die fünf Sinne: Welcher Sinn? · De vijf zintuigen: welk zintuig? / Los cinco sentidos: ¿qué sentido usas? · Os cinco sentidos: qual sentido? · Les 5 sens : quel sens ? · I cinque sensi: quale senso? / Våra sinnen: vilket sinne? · Sanserne: hvilken sans? · Sansene våre: hvilken sans? · Viisi aistia: millä aistilla? | Which sense do you use for each picture? Circle the eye, ear, nose, tongue or hand | `mode:'which'`, G1 |
| F3 | Five Senses: Which One Does Not Belong? · Die fünf Sinne: Was passt nicht dazu? · De vijf zintuigen: welk zintuig hoort er niet bij? / Los cinco sentidos: ¿cuál no pertenece? · Os cinco sentidos: qual não combina? · Les 5 sens : l'intrus · I cinque sensi: l'intruso / Våra sinnen: vad passar inte in? · Sanserne: hvad passer ikke? · Sansene våre: hva passer ikke? · Viisi aistia: mikä ei kuulu joukkoon? | In each row three pictures go with the same sense: cross out the one that does not | `mode:'odd'`, G1 (G2 key per locale allowed) |
| F4 | Sense Organs: Write the Sense Word · Sinnesorgane beschriften · Zintuigen benoemen / Los cinco sentidos y sus órganos · Os órgãos dos sentidos · Les organes des sens · Gli organi di senso / Sinnesorgan: skriv rätt ord · Sanseorganer: skriv ordet · Sanseorganer: skriv ordet · Aistielimet: kirjoita sana | Copy each sense word from the bank onto the line next to the body part that does it | `mode:'label'`, G1 |
| F5 | Five Senses: I Can Hear ... · Die fünf Sinne: Ich höre ... · De vijf zintuigen: ik kan horen ... / Los cinco sentidos: oigo ... · Os cinco sentidos: eu ouço ... · Les 5 sens : j'entends ... · Descrivere con i cinque sensi / Våra sinnen: jag hör ... · Sanserne: jeg kan høre ... · Sansene våre: jeg kan høre ... · Viisi aistia: kuulen ... | Finish each sentence: write what you can see, hear, smell, taste and touch | `mode:'write'`, G1 |

All titles are candidates the native panels rewrite (<= 70, no worksheet-word, base = the bare head, each face adds one NOUN / verb, never an adjective; fr keeps the digit, nl never a digit; F4's head is the ORGAN head in every locale, the de July §1 gap). `coordinate.mode` is ALWAYS the face's mode string (base `'base'`; README ruling), `coordinate.theme:''` (themeless), level = the K key for the base and the G1 key for F1-F5 (a locale publishing F3 at G2 swaps the key in its landing file only). h1 = title; eyebrow = level label; strand chip = the table-B literal (framework NAME only; the build adds the 11-locale Science row to `strand-names.ts`, never the English fallback). JSON-LD `LearningResource` with NO `educationalAlignment` on any face. Meta lead inherits `seo.words.free_printable` (metadata only; no visible copy claims "free"; the MIDDLE is the instruction; the per-locale window is measured with `tools/measure-instruction-window.js`; the en middles above sit at 60-91 chars, *est.* inside a ~60-105 window). `topicMeta['five-senses']` >= 50 x11 + `skill-sentences.<loc>.json['five-senses']` via the `register-b4-en-content.js` clone.

**Non-cannibalisation** (whole-landing 3-gram Jaccard, `scripts/seo-landing/gate.js` FAIL >= 0.80 / WARN 0.65; *est.*, the gate measures):

| pair | what the copy says differently | est. |
|---|---|---|
| base vs F1 | five lines, one object per organ vs ten pictures into five labelled boxes, two per sense; K vs grade 1 | 0.35 |
| base vs F2 | lines to organs vs circle one of five chips beside each picture; the "which sense" query | 0.30 |
| F1 vs F3 | put every picture in its box vs find the shared sense of a row and cross out the intruder | 0.25 |
| F2 vs F3 | one picture, five chips vs four pictures, one intruder | 0.25 |
| F4 vs base / F1 / F2 | the child WRITES the sense word beside the organ; no object pictures at all on F4 | 0.20 |
| F5 vs any | open writing on ruled rows with a starter; no line, no circle, no bank | 0.10 |
| F4 vs K-344 (label the face) / K-354 base | a VERB of sense from a bank beside an organ picture vs a body-part NOUN onto a lane pointing at a face / figure; F4 copy never names eye / ear / nose as targets, it names hear / see / smell | 0.25 |
| any vs K-354 (human-body) | no body figure, no body fact, no body-part word; the boundary sentence names it both ways | 0.10 |
| F1 vs K-211 / K-201 / the 19 other science sorts | `science-sort` has 0 landings in all 11 corpora (m by the seasons file); the deck boundary is structural (hot-vs-cold items excluded); five bins, not two | n/a |
| F3 vs K-043 / K-061..063 (visual-discrimination) | four different pictures by concept vs identical icons and one different noun / a size difference; the copy says "sense" in every sentence | 0.15 |
| F1 vs G1-207 (food groups) | senses, not food groups; no food-groups item can be an item here (rule 3) | 0.10 |
| F2 vs K-319 feelings "which face" | an organ chip strip vs a face; no emotion word | 0.05 |

Boundary sentence on every landing: "The child matches, sorts and names by the five SENSES, with the eye, ear, nose, tongue and hand as the answer key. Hot and cold, the food groups and the parts of the body are their own worksheet types."

## 7 Hub visibility contract

A face appears under `five-senses` on `/[locale]/worksheets` IFF all four hold (brief): (1) `apps['five-senses']` exists in `frontend/config/topics-taxonomy.json` (ABSENT today, m; missing = rendered NOWHERE in the rail) with `default_subject:'science'`; (2) `axes['exercise-type']['five-senses']` has `slug` + `name` in all 11 locales (§1 table B; the 11 slugs measured collision-free); (3) exactly one landing per face per locale with `coordinate.type === 'five-senses'` verbatim, `mode` per §6, `theme:''`, the level key of §1 (K for the base, G1 for F1-F5, or the locale's G2 key on F3), a unique slug and `canonicalDeckSlug` = the published deck; a REFUSED face has NO landing and the expectation is lowered explicitly, never padded; (4) the landing JSON committed AND deployed (per-process cache).

Gate: `node scripts/verify-hub-type-rows.js --keys=five-senses` (script EXISTS since nt20-C, m; expectations from `hub-expectations.json`; poison = drop one landing / drop `apps['five-senses']` must FAIL).

**Expected rows per locale:** en 6 · de 6 · es 6 · pt 6 · fr 6 · it 6 · nl 6 · sv 6 · da 6 · no 6 · fi 6 = **66**. No face is refused in any locale: the items are locale-neutral pictures (45 resolve x11, m), the language is 5 verbs + 5 starters per locale, every d2 pool clears its config from the strong items alone (hear 10 · see 7 · smell 7 with 3 co-occurrable · taste 9 · touch 5), every stack holds at the 677 fi budget (m), and the one measured refusal class (an "I can" starter over 280 px in de / fr / es / pt / it) is a frame the panel rewrites, not a face it loses. Contingencies, each recorded in the draft before the wave, none expected to fire: a locale panel refusing the tongue as the K taste organ (swaps to `mouth`, 0 rows lost); a panel finding NO touch verb that clears rule 5 / rule 6 (none anticipated: every locale's K verb is measured and distinct from its organ nouns).
