---
name: project-seo-rescue-program
description: Multi-part SEO rescue program (2026-06-14); GSC ground-truth + the maker-tools resurrection ruling
metadata: 
  node_type: memory
  type: project
  originSessionId: a6cb9111-c6ad-4379-9338-9b1ec16f8ff1
---

SEO rescue program launched 2026-06-14 (operator: site earns tiny organic traffic despite huge indexed footprint). Structured as an exhaustive audit + digestible remediation Parts, **each Part run in its own plan-mode session with expert agents**; operator approves + switches to plan mode before each Part. **SoT plan file: `C:\Users\rkgen\.claude\plans\the-seo-of-the-mossy-hippo.md`** (read it first when resuming).

**GSC ground-truth (3-mo, pulled 2026-06-14):** 217K impr / 2.88K clicks / 1.3% CTR / avg pos 9.8; 39.2K indexed / 57.4K not-indexed of ~96.6K known. Not-indexed buckets: Discovered-not-indexed 21,398 + Crawled-not-indexed 8,447 (= ~30K thin-content-suppressed K-3 catalog pages); Not-found-404 12,575; redirect 6,341; noindex 3,819; duplicate-no-canonical 3,388.

**THE decisive finding:** the real organic traffic came from **worksheet-MAKER tool pages** (`/tools/*-maker`, `/apps/*-worksheets` — cryptogram maker, sudoku maker, i-spy/find-objects generator, word search) across en/es/de/it, and **the pivot 410'd nearly all of them** (verified 410: cryptogram-maker, generador-criptogramas, cryptogram-worksheets, buchstabensalat-arbeitsblaetter, sudoku-maker). The huge K-3 catalog (decks/landings/topics) earns almost nothing. Locale CTR proves the non-English strategy: US/English 0.46% CTR (90K impr, out-competed) vs Sweden 5.9%, Italy 3.0%, Netherlands 3.2%, Germany 2.6%.

**Operator rulings (locked):** (1) quality-over-quantity — de-indexing/consolidating thin pages OK (indexed count will drop); (2) low-competition locales first (de/nl/sv/it/Nordic), English deprioritized; (3) **resurrect the maker tool pages** as a teacher-framed indexable SEO channel (no Etsy/KDP/"sell printables" copy) — CONFLICTS with CLAUDE.md §3.3 "apps are private; catalog is the product", **§3.3 to be amended**; (4) recover deleted-page equity via 301 (not 410) where a live equivalent exists.

**Roadmap (re-prioritized by GSC):** Part 1 = resurrect+optimize maker tool pages + 301 equity recovery (FASTEST WIN); Part 2 = K-3 catalog thin-content consolidation + `/decks/` vs `/worksheets/` dedup; Part 3 = index hygiene (404/redirect/dup cleanup) + purge seller-era contamination + single identity; Part 4 = low-competition locale keyword alignment (reuse §22.4 rekey pattern + §A.13.48 ensembles); Part 5 = authority/off-page/E-E-A-T + monitoring.

**PART 1 Phase 1a SHIPPED+LIVE 2026-06-14 (commit `32896d0d`).** 6 makers (cryptogram, wordsearch, sudoku, crossword, find-objects, word-guess) × 6 locales (en/de/es/it/nl/sv) = 36 landings rebuilt at their ORIGINAL ranked `/tools/<native-slug>` URLs; `/apps/<x>-worksheets` 301→`/tools/<maker-slug>` (incl. legacy de `buchstabensalat-arbeitsblaetter`). Pattern: cloned tool-content infra → `frontend/lib/seo/maker-content.ts` + `messages/maker-content/<loc>.json` (native ensemble copy, recovered slugs verbatim); `/tools/[tool]` route branches to `components/makers/MakerLanding.tsx` (launch-button to `/worksheet-generators/<app>.html`, WebApplication JSON-LD); `config/live-tool-slugs.ts` carve-out extended; `config/maker-redirects.ts` + middleware 301 (pre-410); `worksheet-makers` hub flipped indexable + links to landings; sitemap shard 3; `scripts/audit-maker-pages.js` (36/36 pass live). **Watermark = the §14.3 attribution footer — operator: intentional promotion, DO NOT remove.** **§3.3/§2 "apps private" needs operator amendment** (per §12).

**PART 1 Phase 1b SHIPPED+LIVE 2026-06-14 (commits `836ab0b8`+`636b8246`+`89be8883`) — operator said "don't wait".** ALL **33 generators × 11 locales = 363 maker landings** live at `/tools/<native-slug>`; `audit-maker-pages.js` 363/363; deploy smoke ALL PASS. Machinery: `maker-content.ts` MAKER_KEYS=33 + LOADERS=11; `maker-content/<11 locales>.json` (each 33 makers + labels; native ensembles, sv/da/no/fi NSR-flag; pilot-6 preserved); `live-tool-slugs.ts` carve-out=33×11; `config/maker-redirects.ts` = 363 `/apps/`→`/tools/` 301s AUTO-GENERATED by `scripts/seo-maker/build-maker-assets.js` (reads pre-pivot slug configs from git `49b501b0~1`; **fixed pilot's de buchstabensalat→word-scramble mis-map**); `scripts/seo-maker/install-locale-content.js` (merge+validate); slug SoT `scripts/seo-maker/maker-slugs.json`. Smoke `post-deploy-smoke.sh` updated: `/apps/` 410-sentinel swapped to a non-maker slug + new Test 8d asserts maker 301s. **GOTCHA: Node `execSync` on Windows uses cmd.exe which eats `^` in git refs — use `~1` not `^`.** **Part 1 COMPLETE.**

**PART 2 (catalog thin-content consolidation) — IN PROGRESS 2026-06-14.** GSC ground-truth (Crawled-not-indexed bucket, ~50 examples): **~78% are `/decks/` pages** (near-duplicate image-only thin assets), NOT landings (~4%). Also found cross-locale contamination (sv-slug decks under `/es/`). Operator ruled: **enrich landings, don't prune** (Wave B). **Wave A Step 1 SHIPPED (commit `eb6b43cc`):** deck pages de-promoted from sitemap (index `[0,1,2,3,4]`→`[2,3,4]`; shards 0/1 empty behind `EMIT_DECK_SITEMAP=false`, reversible). Kept: 13,445 landings + **17,670 intersections** (bigger thin surface than expected) + 2,337 topics. **SHIPPED since:** **W1 intersection-noindex** (commit `a60594ca`): `intersectionIsAuthored()` (`lib/seo/intersection-authored.ts`) gates the `[secondary]` route robots-noindex + sitemap shard-2 → shard 2 **17,670→698** authored-only (live-verified). **Discovered-not-indexed bucket (21K) verified ~96% `/decks/`** → Wave A sitemap-removal is the drain; **the big deck-noindex was deemed REDUNDANT (decks already canonical→landing; skipped).** **EXHAUSTIVE 3-agent live audit done:** technical/canonical/hreflang/PDF-noindex/schema/security all PASS (operator's external checklist ~all already handled — PDF noindex ✓, hreflang ✓ [the "missing" claim was a case-sensitive-grep false positive], Org schema ✓, alt-text ✓; the "Count/add/subtract" vague-meta is a stale cache, not in code). **A1 (commit `45ec468d`):** keyword-rich EN hub titles (activities/tools/about were generic). **A2:** Org schema brand-disambiguation (alternateName/slogan/knowsAbout vs LessonCraft Pro/AI; sameAs omit-on-empty via `ORGANIZATION_SAME_AS`). All live-verified.

**REMAINING (large, ongoing):** **W2** maker+topic keyword alignment per converting locale (de/sv/nl/it…; the maker pages `messages/maker-content/<loc>.json` are the catalog-independent money surface — flagged miss; needs ~20-min per-locale autocomplete harvests); **W3** landing enrichment (13,445; data-grounded hybrid: `extract-deck-vocab.js`+`lib/vocab.js` from image-vocabulary.js → vocab/workedExample/faq fields, gate.js FAIL→0.55, pilot es×chart-count → scale; + Italy landings); **W4** 404/redirect triage + GSC seller-removals; **W5** hub↔maker internal links; **W6** embed-virality backlink flywheel; **W7** cross-locale contamination audit (`audit-cross-locale-contamination.js`). **OPERATOR-DEPENDENT:** backlink outreach (the remaining un-automatable item).

**W3a SHIPPED (commit `93a906ff`):** operator gave YouTube `https://www.youtube.com/@LessonCraftStudioApps` → wired to `ORGANIZATION_SAME_AS` (live `sameAs`); About "How it's made" methodology blurb (native-review + curriculum-alignment, no fabricated person) + YouTube tutorials link (`t.has`-guarded for non-EN). **W3 PILOT REFRAMED (key discovery):** **es is ALREADY the enriched "per-deck gold" MODEL locale** (es.json header + real themed nouns in addition p2) — NOT thin. The thin landings are the OTHER locales (en/de/nl/sv/da skeleton builds, Part-0 0.78 Jaccard) + short-word-count es families (pattern-train 147–195<200). So W3's real job = **propagate the proven es enrichment model (gen-es-chartcount.js + `gen-cc-facts.js` facts-dump + per-deck-gold titles) to the thin locales (en first)** + a short-wordcount fix pass. Large per-locale build, pattern proven → mechanical-ish per locale; best as a focused fresh effort. **SHIPPED this session total:** Part 1 (363 makers) + Part 2 W1 (intersections 17,670→698) + Wave A (54K deck sitemap removal) + audit A1/A2 + W3a. Plan `~/.claude/plans/the-seo-of-the-mossy-hippo.md` = full SoT.

**W3 CLOSED + PART W2 SHIPPED (2026-06-14).** Pre-build re-measurement KILLED the W3 landing-enrichment premise: en.json passes the dup-gate clean (0 FAIL@0.80, ~0 WARN@0.65, max-pair ~0.5 — the Part-0 "0.78" was STALE post the wave rebuilds + §22.4 rekey) AND the converting locales (sv/nl/da/de) ALREADY inject real per-deck vocab. Landing-enrichment is substantially done by the landing program; re-enriching en (deprioritized, non-thin) = low-value. (`gen-es-addition.js` never existed; es addition was hand-authored.) So pivoted to the real #1 lever: **W2 maker-title demand alignment** — commit `00716081`, 12 generator-intent maker titles re-keyed in de/sv/nl/it to harvested head terms (4 expert agents w/ regional WebSearch; data-only metaTitle/meta/name/tagline; body/slug/route/schema untouched). Live-verified (de Geheimschrift/Labyrinth, it Crucipuzzle/Tombola, nl Doolhof, sv Ordsök), 301s intact, audit 132/132, smoke PASS. **PART W4 SHIPPED (commit `905c4374`)** — index-hygiene found the heavy infra already clean (410s/redirects/canonical/PDF-noindex), so the autonomous surface was small + surgical: (1) de.json `nav.resourcesDesc` guides/ideas/start re-keyed seller→teacher (de = the ONE locale the pivot missed; the seller text was shipping in German RSC flight-data per §20.10 even though unrendered — fix confirmed 0 seller strings live); (2) dropped dead `faq` from catch-all hreflangPages; (3) neutralized member-dashboard "commercial license" string (admin/noindexed). The real 404/redirect-bucket triage is GSC-console-only → handed operator a runbook (Validate-Fix maker URLs, Removals for seller prefixes, resubmit sitemap, Bing). **PART W6 SHIPPED (commit `ef7a8a2f`)** — embed-backlink flywheel. The embed snippet was already perfect (2 crawlable `<a>` outside the iframe + resize handshake, `catalog-export.js buildEmbedAffordance`) but only reachable inside the standalone deck.html; teachers arrive from Google on `/[locale]/worksheets/[slug]` and couldn't copy it → flywheel broken at the arrival surface. Fixed: NEW `lib/seo/embed-snippet.ts` (server-safe `buildEmbedSnippet`) + NEW client `components/worksheets/EmbedWorksheet.tsx` (toggle + copy) + the landing route renders it; caption strings added to the route's inline `UI_STRINGS` (6 landing locales en/de/es/sv/nl/da — the route uses inline UI_STRINGS not next-intl; native values reused verbatim from `translations-shared.js embedAttribution*`; sv/da NSR). Live-verified backlinks (brand→canonical, keyword→homepage) + de native caption. **PART W7 RAN — CLEAN, NO FIX NEEDED (audit commit `d49453f9`).** New read-only `scripts/publish-cli/audit-cross-locale-contamination.js` (models audit-slug-fs-db-consistency.js): per deck compares path-locale (Deck.language) vs content lang (manifest.language + deck.html `<html lang>`). Ran on Hetzner across 41,820 published decks → **0 CONTENT_LANGUAGE_MISMATCH**. 708 SLUG_LANGUAGE_SUSPECT = INFO-only classifier noise (da/no/sv cognate overlap; all on content-correct decks). Original GSC contamination examples now 404 (already resolved). So the suspected sv-under-/es/ contamination is NOT a live problem; the approval-gated fix part is unnecessary. Script kept as a standing guard.

**PART IT (Italian landing fan, §22.5 it step) — Phase-A FOUNDATION SHIPPED (commit `2b4a4003`, inert/not-deployed).** Italy = 3.0% CTR, ~3,743 decks, 0 landings → biggest remaining lever, but a ~2,000-page native-Italian build (multi-session). Ledger ALREADY locked (SoT `~/.claude/plans/commission-to-cc-swirling-bengio-agent-a75243ee4844c7f54.md`: framework Indicazioni nazionali; 3-band infanzia[EN preschool+K]/classe-prima/classe-seconda, terza OUT; range-led keyword "Schede di … entro il N … da stampare PDF"; gender via stored forms; readiness strand "La conoscenza del mondo"; calque traps). Shipped inert scaffolding: it-render.js (pure-substitution engine, sv model + assertThemeTable), worksheets/[slug]/page.tsx it maps (FRAMEWORK/LEVELS/UI_STRINGS), landing-content FILES += it, empty it.json (→ 0 routes, tsc-clean, zero runtime effect). **MISTAKE-FREE methodology (operator-commissioned 2026-06-14, "craft without mistakes + native expert linguists"):** ~2,000 it landings come from a SMALL finite set (per type ~14 frames × ONE 48-row theme table) → exhaustive native review of those small sets = mistake-free at scale. 3 pillars: derive-don't-invent (nPl from image-vocabulary.js native plurals; h1 from taxonomy name.it), agreement-safe frame design ({N_PL} only in article-free slots; stored "{GEN_ART} {GEN}" pair), adversarial author≠reviewer native-agent loop + deterministic lints (assertThemeTable, NEW it-agreement-lint.js, gate.js). Digestible parts IT-A(addition proof)/IT-B(readiness 24)/IT-C(picture-sort+chart-count). 
**IT-A PROGRESS: ✅ it-render.js + scaffolding (`2b4a4003`); ✅ it-themes.js DONE (`d9b787a4`)** — 49 themes native-ensemble-authored + INDEPENDENT native-proofreader reviewed (all genArt+plurals verified, 1 fix: emotions arrabbiature→rabbie) + assertThemeTable PASS + render smoke correct ("con gli animali"/"con i veicoli"/"con le emozioni"). **IT-A COMPLETE + LIVE (commit `948d7de0`): 162 mistake-free Italian addition landings.** Pipeline PROVEN end-to-end: enum (196 coords/4 modes: image-image/image-number/mixed→classe-prima entro il 20, find-addend→infanzia entro il 10) → native ensemble authored frames → INDEPENDENT native proofreader fixed 33 agreement violations ({N_PL}→"immagini di" slot; "the things"=stored {GEN_ART} {GEN}) → expand to ≥200w → gen-it-addition.js → it-agreement-lint.js CLEAN + gate.js 0 wordcount/0 similarity FAIL → deploy → 162 live (200), keyword titles, no de/en regression → FINAL independent native review of 4 gender-stratified LIVE pages = SHIP 0 issues (incl. s-impura "gli strumenti musicali") → 162 deck canonicals repointed→landings. **The "without mistakes" methodology (author≠reviewer adversarial loop + derive-plurals-from-image-vocabulary + agreement-safe frame design where {N_PL} only follows a preposition + deterministic it-agreement-lint backstop) is VALIDATED.** New files: it-render.js, it-themes.js (49, reviewed), gen-it-addition.js, it-agreement-lint.js, it-addition-coordinates.json, it.json(162). **IT-B KICKOFF DONE + LIVE (commit `5ce437e8`): gen-it-readiness engine + 164 Italian subtraction landings.** Built `gen-it-readiness.js` (config-driven engine, clone of gen-de-readiness, adapted to it-render/it-themes — unblocks all 22 remaining types via per-type it-readiness-<type>.js configs). Subtraction (4 modes cross-out/find-subtrahend/image-number/mixed → classe-prima entro il 20, 1.OA.D.8, Numeri): native ensemble → independent reviewer-assembler (0 agreement fixes — author internalized the rule) → lint CLEAN + gate 0-FAIL → deploy → 164 repointed → final live native review SHIP 0 issues. **Engine upgrade (reusable): it-render.js gained preposition-contraction placeholders {A_GEN}/{DI_GEN}/{DA_GEN}/{IN_GEN}/{SU_GEN}** (a+gli=agli…) after the lint caught 60 "a le cose" uncontracted-prep hits → 0. it.json = 326 (162 add + 164 sub). **code-addition DONE + LIVE (commit `92e0ce33`):** 96 landings (null 49 + secret-word 47) via gen-it-readiness; readiness logic (NO standard, classe-seconda, strand "Logica e ragionamento" — per de ruling: decode is key-lookup, not a CCSS skill). Independent reviewer-assembler 6 fixes (intorno-a→{A_GEN}, di-{GEN_ART}→{DI_GEN}, length) → lint CLEAN + gate 0-FAIL/0-WARN → live-verified. **it.json = 422 (add 162 + sub 164 + code-add 96).** Italian fan running smoothly via the proven engine+loop (3 types live: addition/subtraction/code-addition).
**math-puzzle DONE + LIVE (commits `db990147`+fix):** 117 landings, TWO-BAND (86 classe-prima/1.OA.C.6 + 31 classe-seconda/2.NBT.B.5 via check-sum-ceiling per-coordinate split — read each deck's manifest operations max), 3 mode-TRUE modes (addition=comporre/build, subtraction=scoprire/reveal, mixed=scegliere/decide → 0 FAIL/0 WARN). Engine upgrades: gen-it-readiness P3 string|function + level-aware h1/standard via config fns. Lint fixed 29 "di {GEN_ART}"→{DI_GEN}; **final live native review caught 1 subject-verb agreement ("scoprire {GEN_ART} {GEN} che si cela"→"che si celano" — {GEN} always plural; OUTSIDE the lint's prep/article scope) → fixed.** **it.json = 539 (add 162 + sub 164 + code-add 96 + math-puzzle 117); 4 arith/logic types live.** LESSON: lint = prep/article/placeholder; prose subject-verb agreement w/ the plural {GEN} needs the live native review — keep it a mandatory per-type gate.
**READINESS BATCH 1 DONE + LIVE (commit `32c8d893`): grid-match (48) + missing-pieces (92) = 140 landings.** No standard, infanzia; strands "Percezione visiva"/"Completamento visivo". Native→reviewer-assembler (14 di→{DI_GEN})→lint CLEAN→final live review SHIP. **it.json = 679 (6 types live: addition/subtraction/code-addition/math-puzzle/grid-match/missing-pieces).** **KEY LESSON (reusable): readiness frames (uniform single-mechanic) are prone to high within-type similarity** — missing-pieces first hit 35+64 WARN @0.71-0.74 → a §22.1 diversity-sharpening pass (vary openings/verbs/metaphors/reassurance) dropped to 0 WARN @0.61. Arithmetic types had varied content + 0 WARN; **budget a sharpening pass for every readiness type.** Also: 3 of the original 4 "single-mode" readiness types are actually MULTI-mode (missing-pieces one/two-missing; shadow-match find-shadow/make-whole; picture-path classic-maze/choose-path) — enum to confirm modes before authoring.
**READINESS BATCH 2 DONE + LIVE (commit `353de364`): shadow-match (93: find-shadow 45 + make-whole 48) + picture-path choose-path (48) = 141 landings.** No standard, infanzia. **Per-mode strand** (engine supports `strand:(mk)=>…`; distinct labels reduce cannibalization): find-shadow=Discriminazione visiva, make-whole=Percezione visiva; picture-path=Orientamento spaziale. **picture-path classic-maze (38) + stray pathway DEFERRED** — inherited de precedent (maze "image=count" legend = counting activity, band/standard unruled; the config defines only choose-path so the engine auto-skips the other modes' coords). Native ensemble → **independent reviewer caught 1 elision defect** (`una abilità`×2→`un'abilità`; comprehensive una+vowel/prep-article/lo-la+vowel sweep ruled the rest clean incl. `ciascuna immagine` false-positives + facultative clitic-elision) → fixed + re-verified live. **All gates green, NO sharpening pass needed** (find-shadow 0.608/make-whole 0.634/choose-path 0.584, all 0 FAIL/0 WARN — the author's built-in opening/verb/metaphor diversity cleared the readiness-similarity trap on the first pass). Lint CLEAN, all ≥200w. Live-verified: 200, native mode-true titles, level "Scuola dell'infanzia", per-mode strands, full 8-entry hreflang chain, dashed no-standard chip correct. **it.json = 820 (8 types live: addition 162 + subtraction 164 + code-addition 96 + math-puzzle 117 + grid-match 48 + missing-pieces 92 + shadow-match 93 + picture-path 48).** LESSON confirmed: a strong author ensemble briefed on the diversity requirement can clear the readiness-similarity trap up-front (no remedial sharpening) — but keep the per-mode-distinct-strand call + the final live native review (the elision was below the lint's scope).
**READINESS BATCH 3 DONE + LIVE (commit `090c9e02`): big-small (78: findBig 36 + orderAsc 42) + more-less image-image (41) = 119 landings.** The Comparison cluster, all infanzia NO standard. **Per-mode strand for big-small** (Confronto di grandezze / Ordinamento per grandezza); more-less = Confronto di quantità. **more-less image-number (49) + check-cross (50) DEFERRED** — inherited de precedent (image-number is numeric/compares-to-a-numeral → possible K.CC.C.6 standard, unruled; check-cross unverified; config defines only image-image → engine auto-skips). Validity gate dropped abstract/non-orderable themes (colors/emotions/seasons/weather/body_parts → 78+41 not 94+51). **Two deterministic catches + 1 reviewer catch:** (a) `{GEN}`-gender participle trap — "gli animali divise" (fem participle on masc collective) → re-anchored to feminine "le immagini di {N_PL} divise/disposte"; (b) doubled-prep "di di più" → "di dove ce n'è di più"; (c) independent reviewer caught apocope "Quale è"→"Qual è" ×4. All fixed + live-verified (badQualeE=0). **Gates green:** lint CLEAN; gate findBig 0.643/orderAsc 0.629/more-less 0.626, all 0 FAIL/0 WARN, NO sharpening; **big-small↔more-less did NOT cannibalize** (§22.1 doctrine-flagged comparison pair — distinct grandezza/quantità lexicons held it; all-pairs max 0.680 = pre-existing subtraction). 119 repointed (952 replacements). more-less prose 0 counting-language ("quante" on page = related-carousel chrome). **it.json = 939 (10 types live).** **KEY LESSON (reusable): a participle/adjective directly modifying `{GEN}` is the SAME unsafe class as adjective-on-`{N_PL}`** (gender varies per theme) → anchor it to a fixed-gender noun ("le immagini … divise"); the deterministic `grep "{GEN} <word>"` sweep catches it before the reviewer.
**READINESS BATCH 4 DONE + LIVE (commit `c510fcec`): sudoku (96: easy 48 + medium 48) + odd-one-out (89: same-theme 47 + cross-theme 42) = 185 landings.** The Logic pair, all infanzia NO standard. sudoku = **Logica e ragionamento** (picture-sudoku, digit-free; stray null auto-skipped); odd-one-out = **Logica – classificazione** ("L'intruso"). **odd-one-out ships BOTH modes — reasoned DIVERGENCE from de** (de shipped same-theme only because ITS cross-theme was a 1-coord singleton = data-volume fold, NOT pedagogy; IT cross-theme = 43 coords). cross-theme mechanic VERIFIED from a live deck (§A.13.31): 3 theme images + 1 intruder from another category. **Defect chain:** deterministic `{N_PL}`-sweep caught a gender trap ("dal mondo delle {N_PL}"→safe "di {N_PL}") → gate flagged **sudoku templated** (easy 17 + medium 19 WARN @0.66) → §22.1 sharpening pass COLLAPSED it (→0.642/0.629, 0 WARN = prose-coupling, not a floor) → independent reviewer caught "la maggior parte…sono {N_PL}"→"è di {N_PL}" (singular + semantics + safe slot) + "due o tre"→"pochissime" (digit-free). All fixed. **Gates green:** lint CLEAN; 0 FAIL/0 WARN all 4 clusters; sudoku↔odd-one-out non-cannibalizing (distinct Logica labels, EN W5 0.108). 185 repointed (1478 repl). sudoku prose digit-free ("conta" on page = carousel chrome). **it.json = 1124 (12 types live).** LESSONS: short-body readiness can hit the WARN via templating (sharpening collapses it); verify a novel mode's mechanic from a LIVE deck before authoring; the de "singleton-fold" deferral is data-volume not pedagogy → re-include a well-populated mode in a richer locale.
**READINESS BATCH 5 DONE + LIVE (commit `aec9b7c9`): find-objects find-odd 46 (Discriminazione visiva) + pattern-worksheet 48 (Riconoscere e continuare gli schemi) + alphabet-train 28 (Conoscere le lettere) = 122 landings.** The LAST pure-readiness types, all infanzia NO standard. **find-objects ships find-odd ONLY — i-spy DEFERRED** (count-based: legend has child ENTER COUNTS, de-validated + live-deck-confirmed; mirrors find-and-count; joins more-less-image-number/picture-path-classic-maze deferred pile). find-odd = busy scene, pairs + a few unpaired → find the unpaired (verified live). alphabet-train = letter-recognition + ABC order ONLY (NOT phonics). pattern-worksheet = continue a started repeating pattern. **CLEANEST batch:** native ensemble applied §22.1 diversity PREVENTIVELY (sudoku lesson) → all 3 single-mode types cleared the gate FIRST-PASS, NO sharpening (find-odd 0.604/pattern 0.616/alphabet 0.616, 0 FAIL/0 WARN); deterministic sweeps clean (flags = doc-comment text + safe post-{GEN_ART}{GEN} conjunctions + idiom false-positives); **independent reviewer VERDICT SHIP, 0 defects** (first batch with no review-stage fix). find-odd↔shadow-match shared "Discriminazione visiva" did NOT cannibalize (distinct lexicons, EN W5 0.109). 122 repointed (976 repl). **it.json = 1246 (15 types live). ALL pure-readiness types DONE.** LESSON: prevent the §22.1 sharpening (brief the author on diversity up-front) rather than remediate — all 3 cleared first-pass vs sudoku's remedial pass.
**LITERACY BATCH 6 DONE + LIVE (commit `4db0bbe3`): crossword 41 (L.2.2.d, classe-seconda, Lessico e ortografia) + word-guess 90 (easy 46 + normal 44; L.K.2.d, classe-prima, Scrittura) = 131 landings.** First IT FRAMEWORK-BEARING literacy types (scuola primaria). crossword = spell words into a crossing grid from picture clues; word-guess = write/complete the pictured word letter-by-letter (h1 "Scrivi/Completa", NEVER "indovina"). **Standard-bearing shape** (the code-addition/math-puzzle path: `standard` field → framework chip "Indicazioni Nazionali" + classe band; CCSS code retained as machine anchor §20.10). Native ensemble (diversity up-front → all 3 clusters first-pass 0 FAIL/0 WARN: crossword 0.613/wg-easy 0.636/wg-normal 0.647; non-cannibalizing) → deterministic sweeps clean (the {GEN}+singular-verb cases safe — "Lavorare con {GEN_ART} {GEN} rende…", singular subject) → **independent reviewer caught 2: "sillabare"→"compitare" (syllabify vs spell-out) + calque "il bel sentimento di"→"la bella soddisfazione di".** Both fixed. lint CLEAN. 131 repointed (1048 repl). **Live-verified: chip "Indicazioni Nazionali" + CCSS anchor (L.2.2.d/L.K.2.d) + classe bands (Classe seconda/prima) + strands all render.** **it.json = 1377 (17 types live).** LESSON: my OWN lexicon guidance in the author brief introduced the "sillabare" defect — the independent reviewer is the backstop even for brief-sourced errors.
**LITERACY BATCH 7 DONE + LIVE (commit `dc48c108`): word-scramble 92 (easy 46 + normal 46; L.K.2.d classe-prima Scrittura — "Anagrammi", reorder scrambled letters to spell) + wordsearch 48 (RF.K.3.c classe-prima Lettura — "Cerca le parole", find+circle hidden picture-words in a grid) = 140 landings.** Framework-bearing. **The de configs were the gen-wave4-literacy engine shape (`h1:(T,i)`, `{TEMA}`) — ADAPTED to the proven gen-it-readiness standard-bearing shape** (modes + {N_PL}/{GEN_ART} + standard + level, like crossword/word-guess); no new generator. Native ensemble (diversity up-front) → deterministic sweeps clean (the {GEN}+singular-verb cases safe — "Cercare tra {GEN_ART} {GEN} rende…" singular subject; elision flags = "una alla volta" idiom) → **independent reviewer caught 1 calque** ("nel suo cuore più giocoso"→"nella sua forma più giocosa"). Fixed. lint CLEAN; **gate 0 FAIL** (wscramble-easy 0.614, wordsearch 0.600 clean; wscramble-normal 0.668 = **1 WARN ACCEPTED** per §22.1 thresholds — genuine near-floor for shared writing-task vocabulary, single near-line pair not a density signal; WARN ships, 0 FAIL). 140 repointed (1120 repl). **Live-verified: chip "Indicazioni Nazionali" + CCSS anchor (L.K.2.d/RF.K.3.c) + "Classe prima" + strands render.** **it.json = 1517 (19 types live). ALL pure-readiness + ALL 4 word/literacy types DONE.** LESSON: a de config in a different engine shape (wave4-literacy) is just a MECHANIC template — adapt to the proven gen-it-readiness standard-bearing shape, don't port the foreign engine; AND a lone similarity-WARN near 0.65 in a vocabulary-constrained task is shippable (0 FAIL = PASS) — full sharpening is disproportionate for 1 near-line pair.
**BATCH 8 DONE + LIVE (commit `f9fd2358`): matching/letter 46 (RF.K.3.a, classe-prima, Lettura — connect picture→initial letter; framework-bearing; 'name' stray dropped) + prepositions 98 (fillin 49: L.K.1.e classe-prima Riflessione linguistica, write the preposition + multiplechoice 49: infanzia NO-standard Concetti spaziali, pick by eye) = 144 landings.** prepositions is **mode-aware** (per-mode standard/strand fns + per-coordinate level fillin→classe-prima/mc→infanzia; FIRST IT mixed-level mode split). Native ensemble → **deterministic sweeps caught the prepositions contraction-bug TWICE** (compound prep + {GEN_ART}: "accanto a {GEN_ART}"+"intorno a {GEN_ART}" → "accanto {A_GEN}"/"intorno {A_GEN}") → gate flagged mc templated (3 WARN @0.672) → §22.1 sharpening COLLAPSED it (0.672→0.615, prose-coupling) → **independent reviewer caught 2: the shared-object coordination "su, sotto o accanto agli animali" is UNGRAMMATICAL** (one contraction can't serve su→sugli + sotto; my "acceptable colloquial" guess was WRONG — native reviewer overruled) → dropped the object ("su, sotto o accanto?"); + calque "bella sensazione"→"soddisfazione". Fixed. lint CLEAN; gate 0 FAIL/0 WARN all 3. 144 repointed (1152 repl). **Live-verified: matching+fillin = chip "Indicazioni Nazionali"+CCSS(RF.K.3.a/L.K.1.e)+Classe prima; mc = NO chip + Scuola dell'infanzia** (mode-split renders correctly). **it.json = 1661 (21 types live).** LESSONS: the prepositions topic inherently collides with the contraction system (compound preps + {A_GEN}; a shared-object preposition-list is ungrammatical even with one correct contraction → DROP the object); my own naturalness guesses can be wrong → the live native review is the authority.
**BATCH 9 DONE + LIVE (commit `f58b7d65`): find-and-count/letter-spotting 43 (readiness infanzia NO-standard, Consapevolezza fonologica — initial-sound spotting) + pattern-train 245 (5 modes AB/AAB/ABB/AABB/ABC; readiness infanzia, Sequenze e ritmi — "Il treno dei modelli") = 288 landings. ALL non-deck types DONE.** find-and-count/hidden-object DEFERRED (count-based: live h1 "Vedo Vedo", "conta"×32, count legend — i-spy pile). pattern-train de used its OWN gen-de-patterntrain.js → adapted to gen-it-readiness (5-mode config); strand "Sequenze e ritmi" + modello/sequenza/ritmo/treno lexicon DISTINCT from pattern-worksheet (0 "schema" in prose). 2 parallel author ensembles (mode-true per pattern type) → deterministic sweeps (author self-caught a {GEN} participle; I caught 2 "quante uguali"→"quali" per no-counting discipline) → **independent reviewer VERDICT SHIP 0 defects**. lint CLEAN; **gate 0 FAIL/0 WARN ALL 6 clusters** (the 5 parameter-twin pattern-train modes cleared FIRST-PASS via mode-true authoring — NO §22.1 sharpening needed: AB 0.644/AAB 0.633/ABB 0.624/AABB 0.633/ABC 0.631). 288 repointed (2304 repl). Live-verified: find-and-count "Trova le lettere iniziali" + pattern-train mode-aware h1 "Treno dei modelli AB/ABC", Scuola dell'infanzia + correct strands. **it.json = 1949 (23 types live).** LESSON: strong mode-true authoring up-front clears even 5 parameter-twin modes first-pass; pre-empt counting-framing ("quante"→"quali") in no-counting types.
**IT-C1 DONE + LIVE (commit `5e99bab4`): picture-sort 71 `-vs-` pairs (readiness infanzia NO-standard, "Raggruppare e classificare" — sort two themes' mixed pictures into 2 groups).** FIRST IT two-theme generator `gen-it-picturesort.js`: cloned gen-sv-picturesort (render2/coprime/cellAssign/audit verbatim) + IT two-theme render2 ({A_NPL}/{B_NPL}=nPl, {A_GRP}/{B_GRP}=genArt+gen, {A_H1}/{B_H1}=h1, + contraction markers {A_TO}/{B_TO}/{A_FROM}/{B_FROM} via PREP_A/PREP_DA). 12×8=96 cells>71 pairs. CHART-COUNT FENCE (sort/group, 0 count-framing). Methodology: ensemble built gen+frames → **it-agreement-lint caught a contraction bug** ("appartiene a {A_GRP}"→"a gli alberi"; "a"+gli=agli — added {A_TO}/etc. + rewrote ~6 "a/da {GRP}" phrases) → **independent reviewer caught a period-before-placeholder lowercase** ("in due gruppi. {A_GRP}"→": {A_GRP}"). Both fixed. lint CLEAN; gate picture-sort 0.584 0 FAIL/0 WARN (no cannibalization w/ odd-one-out). 71 repointed (568 repl). **it.json = 2020 (24 types live).** LESSON: "a"+article DOES contract → a two-theme render needs contraction markers like single-theme it-render; the it-agreement-lint catches what an agent's own audit misses.
**IT-C2 DONE + LIVE (commit `0636f7be`): chart-count 40 honest-count landings (standard-bearing K.MD.B.3, classe prima, "Relazioni, dati e previsioni", chip "Indicazioni Nazionali").** NEW `gen-it-chartcount.js` + Hetzner facts dump `_it-cc-facts.json` (per-deck manifest counts) + native OVERRIDE_IT. Honest-count Quiz: p2 count list + practiceProblems answers BOTH from same facts (prose count=bar=answer; assertion CLEAN). CULLED farm_animals (dup-cat ambiguity) + ~8 non-countable themes. **TWO independent native reviews:** 1st caught a SYSTEMATIC feminine-article-on-masculine-noun bug the §22.1 sharpening introduced ("le carrelli/coni/divani", "delle occhiali") + 2 noun-data (paraorecchie invariable, aglio→agli) → fixed via gender-articled forms (the **gli-rule**: masc plural "gli" before vowel/s-impura/z, "i" else; degli/dei + dagli/dai contractions) + a `da` form + pomegranate→feminine override (facts locName "Melograno" was the masc TREE). gli-fix surfaced 5 mechanical lint hits → cleared. 2nd review of corrected 9-theme sample = VERDICT SHIP, 0 defects. lint CLEAN; gate 0 FAIL (48 WARN = chart-task lexicon floor, accepted, max 0.777). 40 repointed (320 repl). **LESSON: it-agreement-lint catches preposition/article-contraction + i-before-vowel, but a plain wrong-GENDER definite article ("le carrelli") is lint-INVISIBLE → the independent native review is the SOLE backstop for that class. Masculine plural ≠ always "i" (gli-rule mandatory).**
**🏁 THE IT FAN IS COMPLETE — it.json = 2060, 25 types, every type native-reviewed SHIP, deployed + repointed (full `repoint --locale=it`: 2060 slugs, 0 missing) + live-verified + `gate.js it.json` 0-FAIL across all 25 types.** `audit-deck-html --locales=it` = 0 FS errors (the 2060 `CANONICAL_NO_TRAILING_SLASH` = the established landing-canonical convention — deck.html canonical MATCHES the landing's own no-trailing-slash self-canonical; identical to de's 2112; NOT a defect, a `/decks/`-centric audit invariant). Italy (proven 3.0%-CTR) now has a full rankable `/it/worksheets/` layer. **DEFERRED-BY-RULING (stay on /decks/):** math-worksheet (per-deck re-grade), treasure-hunt (v32 app-bug thumbnail recovery), + deferred-mode pile (i-spy, find-objects/hidden-object, more-less image-number+check-cross, picture-path classic-maze+pathway). `verify-deorphan` deferred (Chrome absent on Hetzner; locale-agnostic + 6-locale-proven). `rekey-it-titles --all` = optional refinement. **NEXT (operator's call):** §22.5 sequence → `nl` (Dutch — SLO-kerndoelen, groep band system, `tot N`, kleuters, de/het gender) as its own plan-mode ledger-lock → autonomous fan, reusing the proven IT pipeline (it-render→nl-render, it-themes→nl-themes, gen-it-readiness/picturesort/chartcount engines, author≠reviewer + lint + final-live-native-review loop). **Operator-only GSC console actions still stand** (W4 runbook).

**SEO-RESCUE autonomous code-parts essentially COMPLETE:** Part 1 (363 makers) · Part 2 W1 (intersections 17,670→698) + Wave A (54K-deck sitemap removal) · audit A1/A2 · W3a (About+YouTube) · W2 (maker titles de/sv/nl/it) · W4 (index-hygiene+identity) · W6 (embed flywheel) · W7 (contamination — clean). **Italy landing fan = 🏁 COMPLETE (2060 landings, 25 types, commit `0636f7be`).** **REMAINING = large/operator-strategic (NOT auto-start):** §22.5 sequence next locale `nl` (then fr/pt/no/fi); thin-deck consolidation for any no-landing locales; operator-only GSC console actions (W4 runbook). Operator cadence LOCKED: I prioritize + implement one part at a time autonomously; ask operator to switch to plan mode before each part. seo-keyword-strategy.md memory rewritten off stale seller-era content + W2 harvest recorded.

Technical SEO infra is strong and NOT the problem (robots/sitemaps/canonical/hreflang/JSON-LD/speed all good). Cross-ref [[seo-landing-page-program]], [[project-seo-landing-page-program]], CLAUDE.md §17/§21/§22.

**INDEX-RECOVERY DIAGNOSIS (2026-06-18, plan `~/.claude/plans/the-seo-of-the-partitioned-abelson.md`).** Fresh 24h GSC: 3.84K impr / 32 clicks / **0.8% CTR / avg pos 11.1**; one query "camping homework" = **2,481 impr / 0 clicks** (≈64% of impr). Operator frustrated nothing improved. **Real cause = THREE binding constraints, none on-page:** (1) **Google's INDEX is STALE** — `site:lessoncraftstudio.com` STILL shows dead seller pages (profit-hub "Track Your Etsy & KDP Earnings", activity-book-planner) + Norwegian seller titles on /en/ URLs, even though LIVE site correctly 410s/301s them + serves English + emits correct per-locale hreflang/canonical (verified live: `/en/tools/profit-hub`→410, `/en/apps/crossword-worksheets`→301 to English maker, `/en/tools/treasure-hunt-maker`→English). So the prior "W4 clean / W7 contamination clean / single identity asserted" was about the LIVE site; it NEVER confirmed Google DROPPED the old index — pure un-recrawled cache. (2) **zero domain authority** = the real ceiling (pos ~11 = page 2 = ~1% CTR by physics vs TPT/Twinkl/Education.com; "camping homework" 2481/0 proves it) — Part 5, deferred. (3) **thin footprint self-suppresses** (~30K not-indexed + 0-CTR doom loop) — deferred. **Operator scoped this session to Cause 1 (Index Recovery + Identity) only.** **2 code defects found+fixed:** (a) `deploy.sh:468` Google sitemap-ping was DEAD CODE (Google retired `/ping?sitemap=` in 2023) → replaced with **IndexNow** (Bing/Yandex; Google ignores it) — new `scripts/indexnow-submit.js` + key file `frontend/public/f261bd8eb7ea657cdb8051d5d8e3bc4c.txt` + middleware matcher carve-out for that file; (b) lastmod — already fine (`deploy.sh:190` already sets `BUILD_DATE`; Explore agent wrongly claimed line 468 + missed BUILD_DATE — §A.13.14). **B3 hreflang/canonical verified CORRECT in code (maker route self-canonicals + `hreflangAlternatesForMaker`), no fix.** **The REAL Google fix is GSC-console-only (operator) → runbook written at `docs/seo/index-recovery-runbook.md`:** Removals tool for seller /apps/ + profit-hub/activity-book-planner; Request-Indexing the clean pages; resubmit sitemap (ping is dead); add Bing Webmaster. NOT YET DEPLOYED/COMMITTED (operator to review+deploy; IndexNow takes effect on the 2nd deploy after commit per bash self-pull). Do NOT re-judge before 3-4 weeks.
