---
name: project_games_build_walk
description: "The ACTIVE build loop — \"Good. Continue with the next activity\" builds the next un-built game-designs/INDEX.md game as a lcs-shell activity. Current head"
metadata: 
  node_type: memory
  type: project
  originSessionId: f2e74a05-4a11-4b23-a203-ac9e957448f2
---

# Games build-walk — the ACTIVE loop ("Good. Continue with the next activity")

> **🐛 BUGFIX 2026-06-27 — "Add the Magic e" gave away the answer (FIXED, batched-not-deployed).** `cvc-builder.build-magic-e.rf-1-3-silent-e` GILDED the correct `e` tile (gold) — no challenge. Removed the gild CSS rule from the PROTECTED `cvc-builder-core.js` (operator-AUTHORIZED protected-core touch, CSS-only, 0 logic; kept `.cvc-prefill` + the locked-slot dim); cache-buster `cvc-builder-core.js?v=3→4`; added a no-gild computed-style-equality regression guard to `local-test-cvc-builder-magice.js`. Verified 3 ways (guard + Read + visual-critic GILD:CLEAN) + blend/vowel-team/endings regressions + preflight. NOT deployed (joins the un-deployed batch). **Lesson: never gild/highlight the answer tile — it defeats the activity (§A.13.60).** **FOLLOW-UP DONE 2026-06-27 — cvc-builder family desktop+phone fold FIXED** (operator: "take it as a follow-up"). Root cause: `.cvc-slot`/`.cvc-letter` are `aspect-ratio:1` (height = container-width÷N) + the **palette WRAPPED to 2 rows** for build-cvc's 7 tiles (130px hog) + v7.4 desktop subject-img 180px → stack >900. Fixed in `cvc-builder-activity.html` `@media` ONLY (no protected-core touch): capped subject-img + `.cvc-slots`/`.cvc-palette` max-width + forced palette to ONE ROW (`grid-template-columns:repeat(auto-fit,minmax(46px,56px))!important;justify-content:center`) + trimmed gaps; phone block trimmed for the 320×640 floor. All 3 cvc-builder activities (build-cvc/blend/magic-e) now visual-qa GREEN at all 6 viewports; magic-e gild fix + verify/local-tests intact; visual-critic FIT:CLEAN. Batched-not-deployed. Lesson: aspect-ratio:1 tiles balloon height at desktop + a 2-row palette is the silent fold hog — cap tile size (minmax not 1fr) + one-row, don't just scale fonts. **FOLLOW-UP 2 DONE 2026-06-27 — word-builder/syllable-builder family fold FIXED** (operator: "take that as the next follow-up"). All 13 activities sharing `syllable-builder-activity.html` (EN vowel-teams/endings/build-words + es/fi/pt/fr/it/de/nl/sv/no/da syllable+sound-chunk) now visual-qa GREEN at all 6 viewports. Fix = `syllable-builder-activity.html` `@media` ONLY (no protected `word-builder-core.js` touch): capped subject-img + wb-subject-text (was 72px → wrapped long bases) + wb-slot min-height + wb-tile (one-row palette, the flex-wrap 2-row was the hog) + gaps; phone block trimmed for 640. word-builder verify/local-tests intact; cvc unaffected; chunks unclipped (visual-critic CLEAN). Batched-not-deployed. **New lessons: flex-wrap wide-tile palette = same 2-row fold hog (cap min-width + one-row max-width); a 72px word-only text subject wraps long words (cap it); VERBOSE-LOCALE prompts (es/de/it/sv) wrap to 3-4 lines in the protected shell (~55px more chrome) → trim content to fit UNDER the longest locale prompt, dial off a verbose locale not EN.** **BOTH phonics families (cvc-builder + word-builder) are now §A.13.62-fold-clean across all viewports + locales; both join the un-deployed batch.**

> **🏁 TIME-ACTIVITIES EXPANSION — COMPLETE (2026-06-27). 10 new time activities BUILT (Sprocket the rooster #76, all EN-only pilots, NOT deployed).** #1-5 reading ladder hour→half→quarter→five→minute (`clock-digital`) · #6 analog↔digital match (`clock-digital` reverse-direction render-branch) · #7 elapsed "what time will it be" + #8 add/subtract (`clock-elapsed`, sign-safe `endTime`) · #9 12↔24 conversion (`clock-convert`, digital-only, 2.MD.C.7 a.m./p.m. extended — operator-approved) · #10 a.m./p.m. (`clock-ampm`, 2-choice, activity-is-the-cue). The screenshot spec is fully covered. The override is CLOSED. **NEXT is the OPERATOR'S call (do NOT auto-start): (a) DEPLOY the batch — all 10 EN activities are built + gated but NOT live; one chain `git pull → cp 'mini tools/clock-*' to /var/www/lcs-media/mini-tools/ → bash deploy.sh` (the §20.4 cp-before-build race rule), then verify live + the activities-index picks them up; (b) 11-LOCALE FAN-OUT of the EN pilots — cheap: digital times are locale-neutral, only the prompts/win-notes/page-intros localize (clock-digital STRINGS-bake pattern; clock-ampm activity sentences need per-locale authoring); (c) a FRESH commission.** Plan/build record: `C:\Users\rkgen\.claude\plans\continue-with-the-next-shimmering-raccoon.md` (all 10 detailed) + char-spec #76 addenda 1-9. The INDEX games-build-walk below resumes only if the operator says "Good. Continue with the next activity."
>
> **(prior, now closed) ⏰ ACTIVE COMMISSION OVERRIDE (2026-06-27) — TIME-ACTIVITIES EXPANSION.** The operator interrupted the INDEX walk to commission **≥10 NEW time activities** (screenshot spec: reading granularities Hour/Half/Quarter/Five-min/Minute + operations Elapsed/12↔24-Convert/Add-Subtract + Analog/Digital). **Cadence is operator-locked + DIFFERENT from the INDEX loop: "You should ask me to switch to the plan mode before you start building each activity" → ONE time activity per plan-mode session; after each ships I PROMPT the operator to switch to plan mode + say continue for the next.** Plan/queue SoT = `C:\Users\rkgen\.claude\plans\continue-with-the-next-shimmering-raccoon.md` (11-activity queue + build order). **Engine:** a SINGLE new clean-sibling `clock-digital` engine (`mini tools/clock-digital-core.js` + `-activity.js`, `handAngles` re-implemented — 0 lines to the protected `clock-core`) serves the reading + matching variants #1-7,11 as config; operations #8-10 get their own small engines (`clock-elapsed`/`clock-convert`/`clock-arith`). Code-duplication across time standards is EXPECTED here (granularity × representation × operation breadth IS the product) → the owned-code gate does NOT block. EN-only pilots (digital times locale-neutral; 11-locale fan-out is a cheap follow-on). NO deploy (operator batches). **STATUS: 9 time activities BUILT (Sprocket the rooster #76) + full §A.13.62 DoD passed 2026-06-27, NOT deployed.** (#9 "12↔24 Conversion" `clock-convert.12-24.2-md-c-7` = a NEW digital-only `clock-convert` engine — notation conversion 12h↔24h, MIXED both dirs, "forgot +12"/wrong-meridiem trap; aligned 2.MD.C.7 [operator-approved via AskUserQuestion — 24h isn't clean US-CCSS but 2.MD.C.7 owns a.m./p.m., extended to 24h for the European audience]; ONE tracked edit = activities.ts registration. Lessons: FLAG no-clean-CCSS skills [don't force-fit/invent]; store format-agnostic value [h24] + dir flag, render per-direction, derived answer option===h24. verify+visual-qa+probe+local-test+preflight 88/159+variety+clock-elapsed/clock-digital regression+personal Read both dirs+visual-critic CLEAN.) Earlier: Reading ladder COMPLETE — #1 Hour + #2 Half (1.MD.B.3) + #3 Quarter + #4 Five-min (2.MD.C.7) + #5 Minute (3.MD.A.1). #6 "Match the Clocks" (`clock-digital.match-clocks.2-md-c-7`, digital→analog direction-branch). **#7 "Elapsed Time" (`clock-elapsed.what-time.3-md-a-1`) = first OPERATION, NEW `clock-elapsed` engine** (read START clock + "in N min" → computed END, 12h rollover, derived). **#8 "Time Addition & Subtraction" (`clock-elapsed.add-subtract.3-md-a-1`, 3.MD.A.1) = the clock-elapsed SUBTRACT mode — MIXED add+subtract** (4 add + 5 subtract, "what time was it N min ago?", back-past-12 rollover). **ZERO new tracked edits + ZERO core change** — `endTime`'s `((total%720)+720)%720` wrap (written sign-safe in #7) already handled negative delta; #8 = a manifest row (already-registered manifest) + sign-aware copy (`qBack`/`winBack`/`hintBack`, branch on `deltaMin<0`). Lesson: a sign-safe engine pays off — the whole subtraction activity needed ZERO core change. DoD: verify 18 rounds + 4 negative ground-truth + visual-qa 54/54 + probe/local-test + #7 add-path regression + preflight 87/158 + variety + personal Read (subtract back-rollover) + visual-critic CLEAN (verified all 9 incl. both rollovers). **#6 "Match the Clocks" (`clock-digital.match-clocks.2-md-c-7`, 2.MD.C.7) = the first MATCH variant — the REVERSE direction (`params.direction:"digital-to-analog"`): big DIGITAL readout stimulus + 3 ANALOG-clock-face cards; the child taps the matching clock.** The engine stayed DIRECTION-AGNOSTIC — `render()` branches on `params.direction` (pure render; core/verify/oracle unchanged); `_makeTask`/`_nudge`/`_srMirror` direction-aware; `qMatch`/`hintMatch`; local-test made direction-aware. Visual-critic advisory (clocks small on desktop) acted on: `.cd-match` root modifier widened the row 460→560px + clock cards →165px (reading activities untouched), FITS held. Lesson: flip the stimulus/choice representation by branching ONLY the render; the cognition core stays direction-agnostic and verify/oracle/data-shape carry over free. DoD: verify 54 rounds (6 rows) + visual-qa 54/54 (analog cards pass not-sparse/tap) + probe/local-test (match + #1-forward + #5 regression) + variety (all 6 reshuffle) + preflight 156 + personal Read + visual-critic CLEAN + git-diff 0 new tracked. #5 = `clock-digital.read-minute.3-md-a-1` (`granularity:"minute"`, 3.MD.A.1 was UNOWNED): any non-5 minute, ±2/±3 traps. **Added a conditional MINUTE-TICK RING to the shared `clockSVG`** (`clockSVG(h,m,opts)` → 48 fine ticks when `opts.minuteTicks`; `render()` passes it only for granularity==='minute' → #1-4 untouched) — a to-the-minute read NEEDS minute marks. Added `hintMinute`. Lesson: desktop clock is FOLD-constrained (230px, looks small vs the 720px card but resolved @1024×900=890/896 → can't enlarge without busting FITS; never move the gate). DoD: verify 45 rounds (5 rows) + visual-qa 54/54 + probe/local-test (minute + #1-4 regression) + variety (all 5 reshuffle) + preflight 155 + personal Read + visual-critic CLEAN + git-diff 0 new tracked. #4 = `clock-digital.read-five-minute.2-md-c-7` (`granularity:"five"`): non-quarter 5-min targets, count-by-5s, adjacent ±5 minute-trap; 1 manifest row + 1 tiny tweak (added `hintFive`). **Shared hour-hand fix (improves ALL #1-4):** the visual-critic flagged the hour hand as nearly as long as the minute hand → shortened in the shared `clockSVG` (hour length 22→20 ≈ 55% of minute; minute 34→36) so the "short hand = hour" cue is unmistakable; re-critic CLEAN, all 4 re-ran visual-qa GREEN. Lesson: the short/long cue must be visually OBVIOUS (~55% ratio), not just present. DoD: verify 36 rounds (4 rows) + visual-qa 54/54 ×4 + probe/local-test (five + #1/#2/#3 regression) + variety (all 4 reshuffle) + preflight 154 + personal Read + visual-critic CLEAN + git-diff 0 new tracked. #3 = the cleanest variant yet: ONE manifest row (`granularity:"quarter"`, slug `read-clock-to-the-quarter-hour`), ZERO code changes (engine/`spoken()`/`hintMin`/clock-size/verify/probe all already handle it). Targets 5×:15 + 4×:45; two load-bearing reads — minute-position (3 vs 9, via same-hour minute-trap) + quarter-to hour read (3:45 vs the 4:45 next-hour trap). 2.MD.C.7 owned only by clock-core SET-the-hands → the READ-digital dup is intended breadth. **Lesson: never fudge correct clock geometry for a small-render perception** (11:15 hour hand = 337.5° = 7.5° past 11, geometrically right; visual-critic's "looks near 12" was a thumbnail artifact, confirmed via full-res zoom). DoD: verify 27 rounds + visual-qa 54/54 + probe/local-test (quarter + #1/#2 regression) + variety (all 3 reshuffle) + preflight 153 coords + personal Read + visual-critic CLEAN + git-diff 0 new tracked. #2 = the first `clock-digital` granularity variant (`granularity:"half"`, slug `read-clock-to-the-half-hour`): a SECOND row in the already-registered `clock-digital-activities.json` (→ **ZERO new tracked edits** — `activities.ts` unchanged since #1) + 9 rounds each carrying a **same-hour minute-trap** (3:30 target / 3:00 distractor → minute hand is load-bearing, the distinctness vs #1's hour-only). Core untouched (`GRAN.half` + `spoken()` "half past" already shipped); 2 tiny activity-layer tweaks (granularity-aware hint `hintMin`; desktop clock enlarged `min(52vw,180px)→min(58vw,230px)` per the visual-critic — the half-past dip must be legible at desktop). Probe + local-test GENERALIZED to `--activity=<id>` + force-by-pool-index (so #3-7,11 reuse them). DoD: verify 18 rounds + visual-qa 54/54 both ids + probe/local-test both + #1 regression + variety both reshuffle + preflight + personal Read 360/768/1024 (dip clear at desktop) + visual-critic CLEAN + git-diff 0 new tracked / 0 protected-core. **NEXT on "switch to plan mode + continue" = #10 (FINAL, bonus) "A.M. / P.M."** — cleanly 2.MD.C.7 ("using a.m. and p.m."; no stretch). Mechanic: show a time + a day-context cue (e.g. "You eat breakfast" / a sun-vs-moon, or a clock + activity) → tap A.M. (morning) or P.M. (afternoon/evening). Could be a `clock-digital`-family variant OR a tiny new shape (it's a 2-choice tap, simpler than 3-card). Reuse Sprocket #76. This COMPLETES the commission (≥10 time activities: 9 built + #10). After #10, the time-expansion is DONE — surface to the operator (the next thing is their call: 11-locale fan-out of these EN-only pilots, deploy the batch, or a fresh commission). When all ≥10 ship, this override closes and the INDEX walk resumes on "Good. Continue with the next activity."

**This is the loop a fresh session resumes on the operator phrase "Good. Continue with the next activity."** Do NOT confuse it with the premium-games program (its trigger is "let's start building the games") or the SEO-recovery program (its own plan-mode parts). Each "continue" trigger = ONE more game, built start-to-finish.

## The trigger → loop
1. Operator says **"Good. Continue with the next activity."**
2. **EnterPlanMode.** Read `C:\Users\rkgen\.claude\plans\game-designs\INDEX.md`; take the **LOWEST-numbered un-built "approved" entry** (the status cell says **approved**, not **BUILT**). Re-derive from the INDEX every time — the pointer self-corrects as entries get marked BUILT.
3. **Gate-check** the entry's CCSS code is UNOWNED: grep `mini tools/*-activities.json` for the `alignment.code`. If a built activity already owns it → surface via AskUserQuestion (the operator decides build-anyway vs skip). The spec usually pre-states the gate-check; still verify.
4. Read the entry's design spec `game-designs/<spec>.md` (e.g. `area-01.md`). It is APPROVED + all critic fixes baked — do NOT re-design.
5. Write the build plan → **ExitPlanMode** for approval → BUILD.

## Build = a lcs-shell FREE activity (the proven pattern, Games 47-53)
NOT the premium `game-shell.js` scaffold. Mirror the latest builds (`opposites-*`, `compound-meaning-*`):
- `mini tools/<name>-core.js` → `window.<Name>Core`: pure cognition, the answer is **DERIVED (oracle), never a stored isCorrect/index**; `snapshot()` strips gate-only fields; `facts()`/`deckFacts()`/`audit()`.
- `mini tools/<name>-activity.js` → `window.<Name>Activity`: `LCS.mount`, `answerType:'state'`, `nextTask` order-only reshuffle, an **abundance vessel** SVG (Mirror-Book / Build-Board / gumball-globe — never a count/score), warm scaffolded-wrong (DIM + no advance, no red-X), shell Check hidden until resolved via `.lcs-app.<x>-resolved`, per-round `_token` guard.
- `mini tools/<name>-activity.html`: `lcs-shell.js?v=3` + `<name>-core.js?v=1` + `<name>-activity.js?v=1` + `LCS.mount`.
- `mini tools/<name>-activities.json`: manifest, `alignment{framework:CCSS,code,grade,strand}`, `slug:{en:…}` ONLY (**EN-only pilot → 404 non-EN**), `page_title`/`page_intro`/`kid_prompt_template`.
- **The ONLY tracked edit:** add `'<name>-activities.json'` to `frontend/lib/activities.ts MANIFEST_FILES` (the BUILD-FAILING preflight gate).
- Gates: `scripts/verify-<name>-core.js` (MEASURED — `new Function` shim, oracle 100% + adversary solvers ≤chance) + `scripts/_<char>-probe.js` (UNIQUE name — grep first) + `scripts/local-test-<name>.js`.
- Library-image games consume `/image-library-webp/themes/<themeDir>/<noun>@2x.webp` (COLOR only §20.5; part-noun ∈ image-vocabulary; phonics games also ∩ approved-words). Code-drawn-glyph games use no library.

## DoD = §A.13.62 (operator-locked standing rule) — NOT done until all hold
`verify-<name>-core` PASS · `node scripts/visual-qa-activity.js --activity=<id>` GREEN 320·360·412·768·1024·1366 × every round (FITS at desktop 768+1024 + no-overflow + not-tiny + not-sparse + tap≥44) · `_<char>-probe` · `local-test` · `preflight-activity-routes.js` · `audit-activity-variety.js` (≥7 + reshuffle) · **I personally Read 360+768+1024** · **visual-critic agent CLEAN** · `git diff` = **0 lcs-shell / 0 shared-protected-core** (only the activities.ts row tracked). **NEVER move a gate threshold to pass — fix the layout.** **No deploy** (operator batches; `audit-activity-mobile.js` runs live post-deploy). Close out: append the character to `docs/character-art-spec.md` (Write temp + `cat >>`, NOT a heredoc) + mark the entry **BUILT** in `game-designs/INDEX.md`.

## ARCHITECTURE NOTE — the key de-confuser
The `game-designs/*.md` specs are written in the premium `game-shell.js` / "DESIGN-ONLY — no code … until the ~300-game set is ready" idiom. **IGNORE that framing on this loop** — the operator has been having each one BUILT LIVE as a lcs-shell free `/activities/` build. So on the trigger, **BUILD it** (the spec is the design source; the build target is a lcs-shell activity). Whether these live free activities also "count toward" the paid premium-games program is an OPEN operator classification — flag if asked, never block the build on it.

## ⚠️ POLICY CHANGE (operator, 2026-06-26) — NO SKIPS, REDESIGN ALL, TARGET 80
The operator ruled: **"Instead of skipping the activities you should redesign them. Don't skip any activity. There should be 80 activities in the end."** This SUPERSEDES the prior defer-the-abstract-RL/POV behavior. **NEW rule: build EVERY un-built INDEX entry — including the previously-deferred abstract RL/story/POV ones (#58✓/#62/#66/#68/#69/#72) — by REDESIGNING each clarity-first** (a comprehension standard's "no-text-support / wordless / pure-anti-cheat" purity is the very thing that broke clarity → redesign to a clear, readable form that still instantiates the standard; e.g. #58 wordless-story → a CAPTIONED picture-story). The duplicates/owned (#59 coin-stall, #60 wake-up-pip, #63/#65 choice-board/number-bond) are NOT skips — the activity exists; they count toward 80. INDEX = 87 rows; minus superseded (#55/#57) + the 4 owned-dups ≈ **80 distinct buildable**. **Do NOT AskUserQuestion about which to build / whether to defer — just build the lowest un-built non-duplicate, redesigned clarity-first, one per "continue."** (Strategic flags like the math-white-space note are still worth a one-line mention, but don't block the build.)

## Current head (2026-06-26)
- **STORY COMPREHENSION (RL.K.1) BUILT as Game #58 "Fable's Picture Stories"** — the lcs-shell free activity `picture-story` (Fable the fox = char #60). The FIRST build under the NO-SKIP/REDESIGN policy (#58 was the most-deferred — wordless + entity-registry + blind-annotator + HUMAN bypass-pilot + Pip/lantern). REDESIGNED clarity-first as a CAPTIONED 3-panel picture-story (simple SVG scene-glyphs + ≤6-word captions + a Hear-it button) + key-detail questions (who/what/where/order/why/feeling) → tap a text card; answer matched (response===answer, no index) + grounded in the captions; the wordless/registry/HITL rigor set aside. **Build lessons:** (1) the wordless→captioned fix is the MODEL for redesigning every deferred RL/comprehension spec — make the "text" readable (captions + Hear-it) so it's clear AND still instantiates "recall key details." (2) **stub-glyph readability is load-bearing for a picture-story** — the critic rejected the first mitten (a red blob) + bird (a blue blob); a pre-reader needs the art to reinforce the caption, so each glyph must read as its noun (redrew mitten=thumb+cuff, bird=beak/wing/tail/legs; ≤2 glyphs/panel — 3 crowds at 360). Full §A.13.62 DoD green (verify-core MEASURED + visual-qa 66 + probe + local-test + variety + visual-critic PASS after the glyph redraw). 0 lcs-shell/core lines (only `activities.ts`; `lcs-shell.css` git diff PRE-EXISTING). NO deploy. char-spec tail → #60 Fable.
- **STORY-GRAMMAR (RL.K.3) BUILT as Game #62 "Dot's Story Spine"** — the lcs-shell free activity `story-spine` (Dot the dragon = char #61). 2nd build under the no-skip/redesign policy. Reuses the #58 captioned-strip + fixed glyphs but a DISTINCT mechanic: each round asks one narrative JOB ("which part is THE START / THE TROUBLE / THE FIX?") and the child taps the matching PANEL (the panels ARE the answer cards). Role derived from panel roles; panels SHUFFLED so position≠role. The spec's affect/elimination/swing-pair/decoy/blind-annotator/HITL rigor set aside. **Build lessons:** (1) when the PANELS are the answer cards, the not-sparse gate applies to them → glyphs must FILL the panel (bigger + taller scene) AND captions must be SHORT (a long caption stretches all panels via align-items:stretch → svg fraction drops → SPARSE). (2) a picture-story's beats must be VISUALLY DISTINCT for a pre-reader (reworked the carrot story to owner-mouse→thief-bird→helper-friend; the first all-mouse+carrot version failed the critic). (3) shuffle display order for an order-independent role-typing task (tap maps via data-pi, not the Nth button). Full §A.13.62 DoD green (verify + visual-qa 54 + probe + local-test + variety + visual-critic PASS after the carrot rework + snow-glyph + shuffle + glyph/caption sizing). 0 lcs-shell/core lines (only `activities.ts`; `lcs-shell.css` git diff PRE-EXISTING). NO deploy. char-spec tail → #61 Dot.
- **POINT-OF-VIEW (RL.1.6) BUILT as Game #66 "Lumen's Lighthouse Windows"** — the lcs-shell free activity `point-of-view` (Lumen the owl = char #62). 4th build under the no-skip/redesign policy; the FRESH RL "perspective-attribution" engine (NOT the #58/#62 story-graph). CLARITY-FIRST: a first-person line (shell prompt, "…" Who said it?) + a row of 3 tappable creature **height-windows** — each a LANDSCAPE SVG showing height THREE reinforcing ways (background TINT [pale sky #E4F1FB high / cream mid / deep teal water #8FC3CF low] + an up/down arrow + a short text label "Up high/Middle/Down low"), a big creature on a white halo (visible on any tint); the child reasons tiny/far→the high one, big/close→the low one. Heights VARIED per round (not a fixed cast → position must be read). Correct = the creature whose `pos`===round's `view`, DERIVED-not-stored. The spec's gate-consumes-render / position-vs-canSee split / keyword-lint / teller-shift / knowledge-tier / HUMAN bypass-pilot rigor SET ASIDE for clarity (high/low vantage only — the Grade-1-embodied heart). **3 build lessons:** (1) **a PORTRAIT shape (the lighthouse tower) can't be the answer-card content** — filling a wide card's width makes it too tall (FITS bust) but capping it small makes it a tiny card-fraction (SPARSE); fix = a LANDSCAPE height-window that fills the width without going tall, conveying "height" by TINT+ARROW+LABEL not vertical position. (2) the recurring narrow-width SPARSE/long-label tension — short labels ("Up high" not "High on the tower") + a taller window viewBox cleared the 320 sparse (0.30→pass). (3) the critic's color-reinforcement note: when a cue rides on COLOR, make the two poles clearly DIFFERENT hues (pale sky vs deep teal water) + a white halo behind the creature so it stays visible on the deep tint. Full §A.13.62 DoD green (verify-core MEASURED + visual-qa 54 + _lumen-probe + local-test + variety + visual-critic PASS). 0 lcs-shell/core lines (only `activities.ts`; `lcs-shell.css` git diff PRE-EXISTING, not mine). EN-only pilot (404-by-design non-EN). NO deploy. char-spec tail → #62 Lumen.
- **COMPARE-TWO-STORIES (RL.1.9) BUILT as Game #68 "Two Moons"** — the lcs-shell free activity `two-tales` (Mossbeard the tortoise = char #63). 5th build under the no-skip/redesign policy; the FRESH cross-text compare-mechanic completing the RL arc (comprehend #58 → retell #60 → role-type #62 → POV #66 → COMPARE #68). CLARITY-FIRST: two moon-tinted tales side-by-side (a small animal glyph + a short experience-summary each) + a shell prompt "What is the SAME in both stories?" (`mode:same`) or "What happened in ONLY ONE story?" (`mode:diff`) → tap one of 3 experience-statement cards. Correct DERIVED by a mode predicate over per-story CLOSED experience-tags (SAME = tag in A∩B / ONLY-ONE = tag in A△B), no stored answer; SAME distractors true-of-one, ONLY-ONE includes the shared true-of-both as the tempting distractor + a neither. 9 rounds (5 same/4 diff) over 6 stories in 7 distinct pairings. The spec's synonym-bank / render-consuming EXPERIENCE_WORD_MATCH gate / lexical-distance / NO_POLARITY / picture-MI / mute-QA / 2nd which-part-differs rigor SET ASIDE for clarity. EN-only pilot (spec FORK-A: the most language-bound game — entire stimulus is prose; non-EN a separate ~2× commission). **2 build lessons:** (1) **the tightest FITS budget yet** (two panels + say + Hear + 3 cards + the resolved shell Check ~+120px) — the phone STACK busted; fix = keep tales SIDE-BY-SIDE at all widths (one row-height) + shorter card phrases + HIDE the decorative glyph at ≤340 (text-first; summary+Hear carry it) to reclaim the resolved fold. (2) **the visual-critic caught a real PROSE↔TAG DRIFT** — my first verify gate proved exactly-one-correct over the TAGS, but several summaries didn't visibly evidence both their tags (duck "shared lunch" tagged helped; frog tagged scared but prose said only "felt safe"; fox tagged "kept trying" but prose didn't show it) → a child reasoning from the VISIBLE TEXT diverged from the tag-oracle, and a "true-of-both" distractor read as a 2nd valid answer. Fix = rewrite every summary to visibly show BOTH experiences + ADD a `PROSE_SUPPORTS_TAG` keyword build-gate (each summary must evidence each tag) so the drift class is STRUCTURALLY caught. **LESSON (program-wide): when an answer is a SET-EQUALITY/MEMBERSHIP over closed enums rendered AS TEXT, a tag-only gate is insufficient — also gate that the RENDERED TEXT supports each enum, or the oracle and the child diverge (the lightweight clarity-first form of the spec's "gate consumes the render").** Full §A.13.62 DoD green (verify-core MEASURED + visual-qa 54 + _mossbeard-probe + local-test + variety + visual-critic PASS after the prose↔tag fix). 0 lcs-shell/core lines (only `activities.ts`; `lcs-shell.css` git diff PRE-EXISTING, not mine). EN-only (404-by-design non-EN). NO deploy. char-spec tail → #63 Mossbeard.
- **ORDER/INDIRECT LENGTH-COMPARE (1.MD.A.1) BUILT as Game #69 "The Faraway Shelf"** — the lcs-shell free activity `seriation` (Tilly the shop cat = char #64, renamed from the spec's colliding "Pip"). 6th build under the no-skip/redesign policy; a CONCRETE-MATH build (answers the standing Gr1-3 math-white-space flag). CLARITY-FIRST: Tilly compares ribbons on far-apart shelves using a measuring CORD (the "third object") — a teal striped cord bar + a dashed cord-line, 3 color ribbons from a common left edge at HONEST lengths (each visibly past/short of the cord); shell prompt "Tap the LONGEST" / "SHORTEST" / "Which ribbon is as long as the cord?" → tap a color chip. Correct DERIVED per mode (max/min/==cordLen), no stored answer; 9 rounds (3 longest/3 shortest/3 samecord). The cord is the genuine indirect-comparison device (samecord requires comparing each ribbon to it) → instantiates 1.MD.A.1 "compare two indirectly via a third" + "order three by length". The spec's scale-inverted "lying pixels" / frozen cord-segment memory rail / REMEASURE_IMPOSSIBLE / role-frequency solver / 40%-genuine-transitive-ceiling rigor SET ASIDE for clarity (HONEST pixels — a 6yo must SEE the lengths). **Build lessons:** (1) honest horizontal bars are FITS-friendly + dodge not-sparse when the ANSWER surface is text color-chips (length-bearing ribbons live in the display scene, not answer cards); (2) the resolved-state 320×640 fold trimmed at ≤380 (track 24→20px + tighter gaps/padding); (3) the dashed cord-line aligns across the cord bar + all 3 shelves via shared left:0 + same track-% (no fragile overlay) — that alignment is what makes the indirect compare READ (visual-critic confirmed). Full §A.13.62 DoD green (verify-core MEASURED + visual-qa 54 + _tilly-probe + local-test + variety + visual-critic PASS first try). 0 lcs-shell/core lines (only `activities.ts`; `lcs-shell.css` git diff PRE-EXISTING, not mine). EN-only (404-by-design non-EN). NO deploy. char-spec tail → #64 Tilly.
- **VERB TENSE (L.1.1.e) BUILT as Game #70 "The Clock Tower"** — the lcs-shell free activity `tense` (Juniper the robin = char #65, renamed from the spec's colliding "Marigold"/lighthouse → a clock tower). 7th build under the no-skip/redesign policy; first build in the unowned L.1.1 grammar family. CLARITY-FIRST: a sentence with a clear TIME word + a blank (shell prompt), three time-windows (Before🌙/Now☀/Soon-clock) with the round's time highlighted → tap one of 3 verb-form chips (the same verb's present/past/future). Correct DERIVED (the chip whose tense===round.time; form=verb.forms[time]), no stored answer; 9 rounds (3/3/3), 9 verbs, 3 irregular (went/ran/rode); present rounds use PLURAL/"and" subjects → bare form (no 3rd-sing -s trap, the L.1.1.c fence); future="will + base". The spec's OPTION_SURFACE_NON_DIAGNOSTIC / cue-absent / future-will-must-fail / odd-form-out / morph-reveal / 4-mode rigor SET ASIDE for clarity — the time cue + visible "will" are GOOD Grade-1 pedagogy (recognizing "Yesterday→-ed", "Tomorrow→will" IS how L.1.1.e is learned), not cheats to defeat. **Build lessons:** (1) the resolved-state 320 fold trimmed at ≤380 (window glyphs 40→24px) leaving ~7px headroom (a 1px pass is too fragile before the real visual-qa run); (2) the Hear button must stay ≥44px in the ≤380 compaction block — that rule fires at 360 (a visual-qa shot width) and visual-qa's tap≥44 measures ALL interactive controls, not just answer chips; (3) the highlighted window + time word + prompt give 3 reinforcing time cues so the form↔time mapping is the only thing to reason. Full §A.13.62 DoD green (verify-core MEASURED + visual-qa 54 + _juniper-probe + local-test + variety + visual-critic PASS first try — confirmed -s trap avoided + window-highlight matches the time word in all 9). 0 lcs-shell/core lines (only `activities.ts`; `lcs-shell.css` git diff PRE-EXISTING, not mine). EN-only (404-by-design non-EN). NO deploy. char-spec tail → #65 Juniper.
- **AUTHOR'S PURPOSE (RI.2.6) BUILT as Game #72 "The Harbor Post"** — the lcs-shell free activity `author-purpose` (Marlow the pelican = char #66, renamed from the spec's colliding "Lighthouse Keeper" → a harbor postmaster). 8th build under the no-skip/redesign policy. CLARITY-FIRST: read ONE short note → tap the bin for WHY it was written ("To tell facts" / "To tell a fun story" / "To show how to do it"). Answer = the note's authored purpose (the ground truth for an authored-judgment standard), no stored index; 9 notes (3/3/3), all clearly single-purpose. The spec's machine swap-invariance / HUMAN BLIND-AGREEMENT panel / quarantine / same-topic-triad-only / which-of-3 rigor SET ASIDE (unrunnable in this loop), REPLACED by clear prototypes + a lightweight non-tautological `matchesPurpose` STRUCTURAL VALIDITY gate (reads each note's PROSE vs its purpose: instruct=≥2 first/next/then markers, entertain=playful "!", inform=facts). **Build lessons (2 important):** (1) **an SVG ICON inside an answer card TRIGGERS the visual-qa not-sparse gate** — the icon is measured as the answer-content (30px icon in a 200px bin → SPARSE 0.06); a CSS background swatch (#69) does NOT, but an `<svg>`/`<img>` does. Fix = TEXT-ONLY bins (the gate's "text-only buttons SKIPPED" rule). RULE: answer chips with an inline svg/img must fill ≥0.32 area/≥0.45 width, OR be text-only (or use a CSS-painted cue). (2) the text-heaviest activity busted FITS at 320 (a 28-word note wraps ~7 lines) → shorten notes to ~15-20 words + drop the Hear button at ≤340 (the note is on-screen text). Full §A.13.62 DoD green (verify-core MEASURED + visual-qa 54 + _marlow-probe + local-test + variety + visual-critic PASS first try — all 9 notes validity-clean, no contestable 50/50, author-move labels). 0 lcs-shell/core lines (only `activities.ts`; `lcs-shell.css` git diff PRE-EXISTING, not mine). EN-only (404-by-design non-EN). NO deploy. char-spec tail → #66 Marlow.
- **ADJECTIVE vs ADVERB (L.2.1.e) BUILT as Game #73 "Glim's Describing Words"** — the lcs-shell free activity `wordclass` (Glim the light-wisp = char #67, renamed from the spec's colliding "Pip"). 9th build under the no-skip/redesign policy. CLARITY-FIRST: choose adjective vs adverb by WHAT is modified — a sentence with a blank + a prompt naming the modified word ("describes the rabbit" → adjective / "tells how the rabbit hopped" → adverb) → tap one of 2 form chips (same base, e.g. quick/quickly); on correct the blank fills with the chosen word. Answer DERIVED (oracle = targetKind==='noun'?'adjective':'adverb'), no stored index; 9 rounds (4 adj / 5 adv), 2 dual-class bases (quick + loud used both ways — the spec's signature, kept). The spec's ADJACENCY-decorrelation / SEMANTIC_FIT solver / non-adjacency-mandate / ≥3-multi-candidate / point-at-the-word-in-the-sentence / mode-3 / corpus-collapse rigor SET ASIDE for clarity — the form-pick + the function-naming prompt carry it (the -ly contrast kept as good first-learning pedagogy, NOT distrusted as the spec does for assessment-purity). Light-content activity → FITS-comfortable everywhere (no narrow trimming; contrast the text-heavy #72). The fill-the-blank-on-correct is a clean resolve cue. Full §A.13.62 DoD green (verify-core MEASURED + visual-qa 54 + _glim-probe + local-test + variety + visual-critic PASS first try — prompt-target matches blank position + exactly one answer in all 9). 0 lcs-shell/core lines (only `activities.ts`; `lcs-shell.css` git diff PRE-EXISTING, not mine). EN-only (404-by-design non-EN). NO deploy. char-spec tail → #67 Glim.
- **CONTRACTIONS (L.2.2.c) BUILT as Game #77 "Nib's Apostrophe Seat"** — the lcs-shell free activity `contraction` (Nib the apostrophe-spirit = char #68, renamed from the spec's "Wisp" which collides with Glim #67). 10th build under the no-skip/redesign policy. **GATE-CHECK FOUND #74/#75/#76/#78 are ALL OWNED-DUPLICATES** (already live — #74 3.MD.A.2→`pond-juice`, #75 3.OA.A.2→`maple-bakery` #61, #76 3.NF.A.1→live, #78 2.MD.D.10→live; they count toward 80, like #59/#60 — NO rebuild). #77 L.2.2.c was the first genuinely-unowned. CLARITY-FIRST: read two words → tap the correctly-written contraction from 3 (correct / misplaced-apostrophe / no-apostrophe); on correct the contraction + a memorial note show. Answer DERIVED (insert "'" into the de-apostrophe'd `combined` at apostropheIndex), NO stored literal (verify deep-scans that no stored string contains an apostrophe — a clean derived-not-stored proof for a symbol-output game); 9 rounds across 5 families (n't/'s/'re/'ll/'m) + 2 irregulars (won't/can't as flagged wholes). The spec's PROCESS-axis rigor (build-by-dropping is brute-forceable → no-pre-commit-oracle / commit-cost / TRIAL_AND_ERROR & FAMILY_RULE solvers / cross-family-≥4 / discriminating-≥50%) SET ASIDE via a no-brute-force 3-way PICK (no letter-search, no hot/cold probing). **Build lessons:** (1) gate-check FORWARD through owned codes — `grep "code" mini tools/*-activities.json`; owned = skip-past (counts toward 80), land on the first unowned. (2) **the visual-critic's I/l-bar catch** — "I'll / Ill / Il'l" renders as near-identical vertical bars (capital-I and l are the same glyph), the hardest chip-set to read on the exact apostrophe-position skill → swapped that round to "you'll" (lowercase, distinct positions, no real-word distractor; kept I'm — the `m` keeps it readable). RULE: for apostrophe/letter-position activities, AVOID words whose letters render as identical bars. Full §A.13.62 DoD green (verify-core MEASURED + visual-qa 54 + _nib-probe + local-test + variety + visual-critic PASS, the I'll→you'll polish addressing its one note). 0 lcs-shell/core lines (only `activities.ts`; `lcs-shell.css` git diff PRE-EXISTING, not mine). EN-only-by-design (404 non-EN). NO deploy. char-spec tail → #68 Nib.
- **IRREGULAR PLURALS (L.2.1.b) BUILT as Game #79 "The Doubling Pond"** — the lcs-shell free activity `plural` (Pearl the pond-moon = char #69). 11th build under the no-skip/redesign policy. CLARITY-FIRST: read a singular → tap the correct irregular plural from 3 {correct / +s overregularization / unchanged-singular}; on correct a "foot → feet" reveal + note. Answer DERIVED (vowel-change=`replace`, suffix=`append`, no-change=identity), NO stored plural literal; 9 rounds (foot/tooth/goose oo→ee, man/woman a→e, mouse→mice, child→children, deer/sheep no-change). The spec's RE-CENTER onto constrained-build + MENU_GUESS/CUE_CLASSIFY/REGULAR_BIAS solvers + pre-highlight + no-pre-commit-oracle rigor SET ASIDE — a clean 3-PICK whose **unchanged-singular distractor blocks the eliminate-+s cheat** (drop-anything-with-s leaves {correct, singular} → must know it changes). **Build lessons:** (1) derived-not-stored for a NO-CHANGE word needs the leak-scan to EXCLUDE the singular stem (plural===singular; the stem is legitimately the displayed word). (2) **the critic's "fishes" catch** — "fishes" is a technically-valid plural (species) → swapped the fish round for `deer` (whose +es "deeres" is unambiguously non-word). RULE: when a generic distractor rule can produce a REAL alternate for one word, swap the WORD not the rule. Full §A.13.62 DoD green (verify-core MEASURED + visual-qa 54 + _pearl-probe + local-test + variety + visual-critic PASS, the deer-swap actioning its one note). 0 lcs-shell/core lines (only `activities.ts`; `lcs-shell.css` git diff PRE-EXISTING, not mine). EN-only-by-design (404 non-EN). NO deploy. char-spec tail → #69 Pearl.
- **PERSONAL PRONOUNS (L.1.1.d) BUILT as Game #84 "The Borrowed Hat"** — the lcs-shell free activity `pronoun` (Hattie the hare = char #70, renamed from the spec's colliding "Pim"). 12th build under the no-skip/redesign policy. (#80 3.MD.C.6 confirmed BUILT [mosaic-menders]; #81/#82/#83 owned-dups — all skipped; #84 was the first unowned.) CLARITY-FIRST: read a sentence with a blank → tap the right pronoun from 2 chips (the two case forms of ONE pronoun: He/Him, I/me, his/him); on correct the blank fills + a role note. Answer DERIVED from a CASE_TABLE by role, NO stored pronoun literal; 9 rounds (subject/object/possessive + compound-subject "Sam and I" + compound-object "Mom called Sam and me" + form-by-function their/them). **KEPT 2 clarity-aligned spec elements:** the TWO-TILE SAME-REFERENT board (CASE is the only axis → no gender clever-hans) + the form-by-function vein. SET ASIDE the grade-only-ear-resistant / role-cue-withhold-mechanics / ROLE_AFFORDANCE_READ_SOLVER / hard-discrete-commit / position-heuristic rigor (the clean sentence + 2-form pick shows no role cue anyway; simple cases kept as valid first-learning). **Build lesson:** the two-tile same-referent board IS the clean clarity-first answer to the gender-cheat (both chips = one pronoun's two cases → nothing to read but the forms → no role-cue-withhold machinery needed). Full §A.13.62 DoD green (verify-core MEASURED + visual-qa 54 + _hattie-probe + local-test + variety + visual-critic PASS — all 9 a clean single-pronoun case-contrast). 0 lcs-shell/core lines (only `activities.ts`; `lcs-shell.css` git diff PRE-EXISTING, not mine). EN-only-by-design (404 non-EN). NO deploy. char-spec tail → #70 Hattie.
- **INFORMATIVE WRITING / TOPIC FOCUS (W.K.2) BUILT as Game #85 "The Wobble Museum"** — the lcs-shell free activity `wobble-museum` (Bram the magpie = char #71, renamed from the spec's colliding "Pip"). 13th build under the no-skip/redesign policy; the program's FIRST writing-production game + FIRST text-sentence-tap answer surface. CLARITY-FIRST: a room-sign "This room is all about frogs." + 4 fact sentences (3 on-topic + 1 off-topic-but-TRUE) → tap the one that wandered in; on correct a "for-later" note. Answer DERIVED (the sentence where about !== topic), no stored correctIndex, re-pointable (change topic → drift moves); 8 rooms, drift at 4 positions, KEYWORD-DEFEAT. **KEPT the spec's catch-the-drift spine + keyword-defeat (a genuinely clear whole-text mechanic, NOT an anti-cheat contortion)**; SET ASIDE the commit-before-facts authorial flow / separate-graded-re-home / hard-lure-only / CATEGORIZATION_BIN-solver rigor. **3 build lessons:** (1) the shell PROMPT wrapping to 2 lines at 1024 is a real fold cost for a TALL (4+ row) activity → keep the prompt 1 line ("What wandered in?"); measure section heights (throwaway puppeteer boundingRect dump) to find the true tall culprit (it was the prompt, not the rows). (2) a 4-stacked-row answer surface → merge Bram+msg+Hear into ONE footer row to save a ~44px row. (3) **the visual-critic VALIDITY catch** — a shared-word drift must be a genuinely SEPARATE object/organism, NOT a product/function/sub-part of the topic ("Water puts out fires"=truck function / "Honey is sweet"=bee product → "Stop signs are red."/"Ants live in the dirt."). Full §A.13.62 DoD green (verify-core MEASURED + visual-qa 48 + _bram-probe + local-test + variety + visual-critic PASS after the 2-room validity fix). 0 lcs-shell/core lines (only `activities.ts`; `lcs-shell.css` git diff PRE-EXISTING, not mine). EN-only-by-design (404 non-EN). NO deploy. char-spec tail → #71 Bram.
- **EQUIVALENT FRACTIONS (3.NF.A.3) BUILT as Game #86 "Crumb's Same-Amount Bakery"** — the lcs-shell free activity `fraction-equiv` (Crumb the mouse = char #72). 14th build under the no-skip/redesign policy; **the LAST DESIGNED INDEX entry** (#87 is the "next un-designed catalog entry" placeholder → CATALOG-DEPLETION REACHED). Gate-check: 3.NF.A.1 owned (fraction-forge), A.3 (equivalence) distinct + UNOWNED. CLARITY-FIRST: a REFERENCE fraction (proportion-shaded bar + stacked numerals) + 3 candidate cards (shaded bar + numerals) → tap the SAME amount (the equivalent). The spec's comb-rake / child-glaze-proportional / "did-the-dough-move?" motion-judgment / 8-solver suite (KXK_PROCEDURE / ENGINE_AUTOSHADE / COMB_VOCABULARY_BRUTE_FORCE …) SET ASIDE — the standard's OWN visual-fraction-model carries it (proportion is scale-invariant so a wide ref bar + small candidate bars compare by fullness; numerals tie amount↔symbol). Clean sibling `fraction-equiv-core.js` (window-shim, integer cross-multiply `a.num*b.den===a.den*b.num`, oracle/derived-not-stored, re-pointable ref), distinct from the live `fractions-core` PARTITION engine (cut-one-shape vs recognize-equal-amounts). 8 rounds, denoms {2,3,4,6,8}, mixed direction, equiv at 3 distinct indices, display shuffled, STRADDLE distractors (one less / one more). **2 build lessons:** (1) **the RESOLVED state is a fold cost visual-qa never measures** — visual-qa never clicks Check, so the shell `.lcs-activity-check` (hidden until `.crumb-resolved`) is absent from its sweep; visual-qa passed but `_crumb-probe` (which resolves) caught a desktop cut-off at 1024/1366 h=900 (resolved ctrlBottom 927 > 896). FIX = cap the desktop card/ref sizes (`.fe-cand` max-width 160→118, `.fe-ref` 380→300) → 866. LESSON: ALWAYS run the probe's RESOLVED state at the 900-tall desktop viewports — the appearing Check is ~120px the layout must still fit; visual-qa alone is blind to it. (2) the visual-critic flagged both-distractors-same-side rounds as acceptable-but-weaker → STRADDLE them (one less, one more) reads clearer for "find the SAME amount" (cheap quality win; r3/r5 fixed). Full §A.13.62 DoD green (verify-core MEASURED + visual-qa 48 + _crumb-probe + local-test + variety + visual-critic CLEAN + personal Read 360/768/1024). 0 lcs-shell/game-shell/core lines (only `activities.ts`; `lcs-shell.css` git diff PRE-EXISTING, not mine). EN-only-by-design (404 non-EN; math locale-neutral → low-cost future fan-out). NO deploy. char-spec tail → #72 Crumb. **🏁 #86 was the last designed INDEX entry — I surfaced the strategic step-back; the operator chose option 3 = DEFERRED PRODUCTION-CORES (the loop is now OFF the INDEX-walk).**
- **DEFERRED PRODUCTION-CORE #1 (1.NBT.C.4 add-with-regrouping) BUILT as "Tuck's Ten Bundles"** — the lcs-shell free activity `place-value-regroup` (Tuck the hamster = char #73). The FIRST build of the deferred-core direction. Gate-check of the disciplined-set deferred-core backlog: K.CC.C.6 (compare-groups) + 2.MD.A.1 (drag-measure) are OWNED codes (E2 which-more / Tildy's Ruler Lab) → DROPPED; **1.NBT.C.4 + 2.NBT.B.7 (regrouping) UNOWNED** → operator chose place-value regrouping. Fills the explicit OPERATE gap in the live E12 place-value family (`place-value-core` only REPRESENTS N; never operates). Mechanic: base-ten blocks (tens-rods + ones-cubes), tap "Make a ten" to bundle 10 ones → 1 ten, then the shell keypad for the total. NEW clean-sibling `place-value-regroup-core.js` (array-core/place-value-core contract; pure helpers + `gradeAnswer(round,answer,onesCount)=answer===a+b && onesCount<10` → regroup load-bearing AND DOM-free testable) + `place-value-regroup-activity.js` (Object.assign + nextTask + keypad tasks). **CONFIG-READY** (operation/places) so the **2.NBT.B.7 fast-follow** (subtract/borrow + 3-digit) rides this SAME core. **Build lessons:** (1) a block-mat + the shell keypad (~190px) is the tallest surface yet → collapse mascot/caption into ONE action row, shrink blocks, tighten gaps. (2) the ones column wrapping to a 3rd row is a hidden +15px fold cost (12 ones=2 rows, 13-14=3) → 7 cubes/row at ≤380 + tens `flex-wrap:nowrap`. (3) **the shell `.lcs-activity-answer-display` BALLOONS ~70px when it shows the typed number** — a shell-keypad characteristic (array/ten-frame too), only in the transient post-success state; visual-qa never resolves → check FITS on the INTERACTIVE states + assert resolve FUNCTIONALLY (don't chase the shell-inherent post-success balloon by gutting the manipulative). (4) `answerType:'number'` has no answer cards → visual-qa not-sparse/tap skipped; the `_tuck-probe` owns the regroup-required + resolve-functions + interactive-fold. Full §A.13.62 DoD green (verify MEASURED + visual-qa 48 + _tuck-probe + local-test + variety + visual-critic PASS [+ bumped ten-rod notch visibility] + personal Read 360/768/1024). 0 lcs-shell/game-shell/protected-core (only `activities.ts`; `lcs-shell.css` git diff PRE-EXISTING, not mine). EN-only pilot. NO deploy. char-spec tail → #73 Tuck.
- **DEFERRED PRODUCTION-CORE #1b (2.NBT.B.7 subtract-decompose) BUILT as "Tuck Breaks a Ten"** — the operator-chosen fast-follow on the place-value-regroup core. Activity #2 `place-value-regroup.subtract-decompose.2-nbt-b-7` (Grade 2): a 3-COLUMN mat (hundreds-FLATS | ten-rods | unit-cubes) showing a 3-digit minuend (342 − 5); not enough ones → tap "🔁 Break a ten" (1 ten→10 ones, tens 4→3, ones 2→12), the last `b` ones get a dimmed ✕ "take away" mark (5 marked → 7 remain), type the difference (337) on the keypad. ONE clean cognition = **decompose a ten** (inverse of #1's compose); hundreds shown (3-digit) but NOT decomposed. The config-ready core paid off — lit up `subtract`/`places:3`: pure `diffOf`/`needsBorrow`/`gradeSubtract(round,ans,decomposed)` (borrow load-bearing + DOM-free testable) + `breakTen()` + `_flat()` (gridded-square hundred) + op-aware `setupTask`/`render`/`lockAndCaption` + a synchronous per-activity title swap (array-activity pattern → "Tuck Breaks a Ten"). ADD #1 stayed regression-green. **Zero new tracked edits** (subtract row → the already-registered `place-value-regroup-activities.json`); 0 lcs-shell/game-shell/protected-core. **Build lessons:** (1) a 3rd column adds WIDTH not height → no-overflow 280→768 is the binding gate; the ones column is widest (`flex:1.4`, holds 12 post-break); tens(a)≤5 + hundreds(a)≤4 so 3 columns fit 320. (2) **the action bar's `min-height:44` over-reserves in the button-ABSENT broken/resolved states** — the button keeps its own min-height:44 (tap), so dropping the bar reserve to 36 reclaims ~8px exactly where the post-break 12-ones busts the 320 fold (the broken-state fix). (3) the visual-critic's base-ten magnitude note (a hundred should read bigger than a ten) is fold-constrained at 320×3-col → addressed within budget (flat 28px desktop + stronger grid + bolder ✕), not full-rod-height; labels+color+grid carry the rest (critic PASS). Full §A.13.62 DoD green on BOTH activities (verify extended for both rows + visual-qa 48×2 + _tuck-subtract-probe + local-test-subtract + add regression probe/local-test + variety + visual-critic PASS + personal Read pre+post-break 360/768/1024). EN-only pilot. NO deploy. char-spec → #73 Tuck addendum.
- **DEFERRED PRODUCTION-CORE #1c+1d (2.NBT.B.7 compose-a-hundred + decompose-a-hundred) BUILT — operator "both"** — "Tuck Makes a Hundred" (`add-compose-hundred.2-nbt-b-7`) + "Tuck Breaks a Hundred" (`subtract-decompose-hundred.2-nbt-b-7`). **The place-value-regroup core is now COMPLETE for K-2 add/subtract regrouping — 4 activities** (compose-ten 1.NBT.C.4 + decompose-ten + compose-hundred + decompose-hundred). A: 11 tens → "🔁 Make a hundred" (10 tens→1 flat) → type total (e.g. 160+50=210, 340+70=410). B: borrow ACROSS ZERO — 0 tens → "🔁 Break a hundred" (1 hundred→10 tens) → "🔁 Break a ten" (1 ten→10 ones) → 5 ✕-take-away → type diff (302−5=297). Core deltas: `makeHundred`/`breakHundred`, op+state-aware button dispatch (Make/Break a hundred/ten), `gradeAddHundred` (tens<10) + REUSE `gradeSubtract` for B (final break-ten sets `_decomposed` → full cascade required, one break won't resolve), `_newTens` tint, **per-activity fat-column root class** (`fat-tens` for compose-a-hundred). All 4 regression-green; 0 protected-core/shell lines; ZERO new tracked edits (rows in the registered manifest). **Build lessons:** (1) **3 columns + a block-heavy column = the program's tightest fold** → fat-column (`flex:1.9`) + thin rods (≤380 width 7px, desktop height 36) keep ≤11 tens (compose) / 9 tens (decompose-post-cascade) ONE row; (2) the dump proved 11 rods fit / 13 wrap at 320 → cap compose rounds to tens(a)+b/10 ≤ 11; (3) cap decompose-hundred ones(a) ≤ 3 so post-cascade ones ≤ 13 (2 rows); (4) the 46px desktop rod made the 11-tens column overflow 900 at 1024 → 36px. Full §A.13.62 DoD (verify 4 rows + visual-qa 48×4 + `_tuck-hundred-probe` [make-a-hundred + the break-hundred→break-ten cascade both load-bearing + busy-state fold] + `local-test-place-value-regroup-hundred` + 2 prior probes/local-tests regression + variety + visual-critic PASS both + personal Read pre+post-regroup 360/768/1024). EN-only. NO deploy. char-spec → #73 Tuck addendum-2. **The place-value-regroup core is DONE (4 activities).**
- **DEFERRED PRODUCTION-CORE #2 (RF.K.2.d phoneme/Elkonin) BUILT as "Coco's Sound Boxes"** — the operator-chosen next backlog core. `sound-boxes.phoneme-position.rf-k-2-d` (Coco the koala = char #74). Target CVC picture + 3 Elkonin boxes (one highlighted = first/middle/last position) + "Hear it" (whole word) → tap the picture tile sharing that position's phoneme. RF.K.2.d's gradeable form (can't grade spoken sound; isolated-phoneme TTS unreliable) = picture-match by position-phoneme, Elkonin boxes the scaffold; medial-vowel + final are the unowned core vs the owned RF.K.3.a initial. NEW clean-sibling `sound-boxes-core.js` (window-shim pure cognition: `isAnswer(round,i)=options[i][position]===target[position]`, derived-not-stored) + `sound-boxes-activity.js` (picture-tile skin, replicates the choice-board img pattern). Phonemes curated from `scripts/worksheet-gen/data/literacy/{beginning,middle,ending}-sounds.json` + `vocabulary-phonics.json`; CVC pics from `/image-library-webp/themes/<theme>/<noun>@2x.webp`. 9 rounds (3 each position), 7 distinct targets. 0 protected-core/shell lines; only tracked edit = the `activities.ts` row; EN-only-by-design (phonemes language-specific → non-EN 404). **Build lessons:** (1) **picture RECOGNIZABILITY is a BLOCKING gate for a pre-reader picture-match activity** — the visual-critic caught 4 unrecognizable image-library assets (net=racket, mop=broom, fan=pinwheel, bun=loaf) on TARGETS/CORRECT-tiles → fatal; rebuilt with only obviously-recognizable concrete nouns (cat/cap/sun/dog/bat/bag/hat/pig/pot/van/jug/bed/can) + dropped the scarce/ambiguous medial-/i/ (used /a/,/u/,/o/). The image-library LABEL ≠ a recognizable picture — run the recognizability critic + prefer everyday concrete nouns. (2) the shell prompt (~200px desktop, wraps to 2 lines when long, the #85 lesson) + a picture stimulus + picture tiles overran the desktop fold → SHORT prompts ("Same MIDDLE sound as Cat?") + compact stimulus (Hear-it inline with the boxes, target pic 56px, tiles ≤104px). (3) whole-word audio only (no isolated-phoneme TTS in the grade path); the verify gate cross-checks every word clean-CVC+approved+color-imaged. Full §A.13.62 DoD (verify MEASURED + visual-qa 54 + _coco-probe + local-test [images-load-no-404] + variety + visual-critic PASS [after recognizability rebuild] + personal Read 360/768/1024). EN-only. NO deploy. char-spec → #74 Coco.
- **DEFERRED PRODUCTION-CORE #3 (L.1.1.j + L.K.1.b sentence-building) BUILT as "Wiggles' Sentence Builder" — the LAST backlog core** (`sentence-builder.build-a-sentence.l-1-1-j`, Wiggles the caterpillar = char #75). Arrange scrambled word-tiles into a complete declarative sentence; subject-noun picture + N empty slots + scrambled palette; tap-to-place (word-builder pattern, clean sibling) + the shell Check VISIBLE (arrange-then-check, unlike auto-resolve tap-cards). Grade = `gradeOrder(placed, canonical)` exact-order; the canonical sentence is the content (no stored correctIndex); 9 curated sentences. NEW `sentence-builder-core.js` (window-shim pure cognition) + `sentence-builder-activity.js`. 0 protected-core/shell lines; only tracked edit = the `activities.ts` row; EN-only-by-design (word order language-specific → non-EN 404). **Build lessons:** (1) **the determinism guard = bake the capital onto the determiner tile + the period onto the verb + an adjective-noun middle + intransitive verbs** → a SECOND valid sentence is structurally impossible (capital can only start, period only end, "big dog" never "dog big") → exact-order grade is safe; the visual-critic's #1 gate ("could the tiles form another valid sentence?") confirmed all 9 rigid. For any arrange-into-one-order activity, design the tile set so only ONE grammatical order exists. (2) variable-width WORD tiles in two rows (slots+palette) must flex-wrap + compact font to fit 320. (3) Hear-it reads the CURRENT arrangement (self-monitoring), not the target. Full §A.13.62 DoD (verify MEASURED [ordered-grade strict, derived, subjects color-imaged] + visual-qa 54 + _wiggles-probe + local-test + variety + visual-critic PASS [second-valid-order clean] + personal Read empty/built/resolved 360/768). EN-only. NO deploy. char-spec → #75 Wiggles.
- **🏁 THE DEFERRED PRODUCTION-CORE BACKLOG IS EXHAUSTED** — place-value-regroup ×4 (1.NBT.C.4 + 2.NBT.B.7 compose/decompose ten+hundred) + sound-boxes (RF.K.2.d) + sentence-builder (L.1.1.j) all built; owned-code cores K.CC.C.6/2.MD.A.1 dropped. **NEXT "Good. Continue…": do NOT auto-pick — SURFACE the strategic step-back (both the per-game catalog-walk AND the deferred-core backlog are now done). Options: 11-locale fan-out of the place-value-regroup activities (cheap) · theme-waves over existing engines · the premium-games PROGRAM thrust (separate trigger) · a fresh-design commission.**
- **NEXT = #86 3.NF.A.3 (equivalent fractions — gate-check: 3.NF.A.1 is OWNED [Pip's fraction forge] but A.3 "equivalent fractions / compare" is the distinct sub-standard; confirm unowned at the trigger).** #86 is the LAST real INDEX entry (#87 is empty "—"). **After #86, do a TARGET RECKONING:** count distinct built activities vs the 80 goal (the INDEX may be exhausted; remaining gap may be theme-waves / a fresh-design commission — surface to the operator). **GATE-CHECK each via `grep "code" mini tools/*-activities.json`.** Confirmed owned-dups: #74 3.MD.A.2, #75 3.OA.A.2, #76 3.NF.A.1, #78 2.MD.D.10, #80 3.MD.C.6, #81 3.NBT.A.1, #82 3.OA.A.1, #83 3.MD.D.8 (+ #59/#60/#63/#65). Build clarity-first, no skips, toward 80.
- **PERIMETER (3.MD.D.8) BUILT as Game #83 "The Mending Fences"** — the lcs-shell free activity `mending-fences` (Hazel the badger [RENAMED from the spec's Bramble = char #46 collision] + Clover the sheep). Gate-check fired: TWO approved 3.MD.D.8 designs (#55 "Woolly's Fence Line" dual-graph + #83 "The Mending Fences" doubling-inverse) BOTH pointed at `perimeter-01.md` which #83 had overwritten → operator chose build #83; #55 marked SUPERSEDED in the INDEX. New `mending-fences-core.js`+`-activity.js` (SVG polygons + DOM text labels; 5 acts mend-board/joint-ranking/around-vs-fill/roll-reach/same-area; OWNED_SUBTRACTION→present-and-wrong P−side foil, AREA_COVARIATION killed by opposite orderings, PERCEPTUAL_INVERSION via a compact-high-perimeter C-field, answer derived-not-stored). Full §A.13.62 DoD green (visual-critic CLEAN after adding side-labels-for-solvability + a clear coral "?" badge + landscape-orient-to-fit; gate-FITS via harness-recognized control classes). **Deviations flagged:** l-closure DROPPED (= owned subtraction P−Σ); the "gated advanced tier" ships as plain variety (no progress tracking). 0 lcs-shell/game-shell/core lines (only `activities.ts` row). char-spec tail → #53 Hazel. **POST-BUILD FIX (operator-caught): the coral "?" missing-board badge was CLIPPED in all mend rounds** — it's drawn ~0.84U OUTSIDE the rect but the field-SVG viewBox pad was only 0.5U → sheared. Fixed via a per-call `pad` opt (mend passes `pad:1.0`, +1 on `--mf-c` to keep field size). **LESSON (gate blind-spot): visual-qa + the probe measure element rects + `.lcs-app` overflow → they are BLIND to a glyph drawn outside its OWN svg viewBox (no app overflow). A decorative marker drawn OUTSIDE a shape but inside its SVG must fit the viewBox padding — verify such markers by READING the render, not just the gates** (I had Read the mend shots but fixated on "?"-vs-"7" legibility and missed the half-cut circle — the operator caught it).
- **ROUNDING (3.NBT.A.1) BUILT as Game #81 "The Halfway Harbors"** — the lcs-shell free activity `halfway-harbors` (Marina the lighthouse-cat). Gate-check FORK (like #54/#83): TWO approved designs for the unowned 3.NBT.A.1 — #57 "Rolo's Lantern Path" (nearness/number-line) + #81 "The Halfway Harbors" (inverse/boundary), separate spec files same slot → operator chose #81 (#57's own critique flagged nearness designs as digit-equivalent); #57 marked SUPERSEDED. 4 cogs (set-band / halfway-buoy / which-place / catch-false-flag); answer DERIVED from {target,place}. **HEADLINE = the SMART-adversary false-pass guard:** forward rounding is digit-equivalent (a SMART rote-rounder passes), so the grade re-centers onto the INVERSE the rounder can't produce (band edges + midpoint = SMART-proof) + place-undeclared (defeats NAIVE) + meaning-traps (truncation/always-up/already); the gate arms `SMART_PLACE_DIGIT_SOLVER` with `SMART_FAIL_REQUIRED` (a naive-only-fail is a FALSE PASS — SMART scores only the bounded catch minority ≤chance). Full §A.13.62 DoD green. **THEN REBUILT CLARITY-FIRST (operator-caught): the operator played it and said "I cannot understand the activity — the graphics don't make sense, the question isn't clear."** Root cause: the abstract grade-the-inverse design (band edges / which-place / catch-false-flag) had NO number-line + tiny decorative icons → incomprehensible. Operator chose "rebuild around a clear number-line." Rebuilt: cogs nearest / big-trail / halfway on a CLEAR to-scale NUMBER-LINE (labeled harbor ticks, boat at its position, dashed halfway-divide buoy, tall-lighthouse=hundreds vs short-lantern=tens, single-"?" halfway); id → `halfway-harbors.nearest.3-nbt-a-1`; the #81 SMART-solver suite RETIRED (clarity > anti-cheat purity for a free activity); lean verify. Comprehension-critic CLEAN (rubric expanded to "would a 7-year-old understand what to tap?"). **DOCTRINE (program-wide, NEW): the gates verify layout + anti-cheat but NOT comprehension — a rigor-maximizing design can ship gate-clean yet be unusable by a child; the operator-eyeball comprehension check is load-bearing; for free K-3 practice CLARITY > anti-cheat purity. Apply at DESIGN time: if a spec removes the standard's natural concrete model (a number-line for rounding) for anti-cheat reasons, flag it — comprehension first.** 0 lcs-shell/game-shell/core lines (only `activities.ts` row). char-spec tail → #55 Marina (+ rebuild note).
- **SHADES (L.1.5.d) BUILT as Game #56 "Pesto's Soup Stall"** — the lcs-shell free activity `shades` (Pesto the parrot). Clean build (UNOWNED, no fork; the catalog's 1.L.5.b mis-tag → L.1.5.d at build). A TEXT meaning-game: same-family word jars (UNIFORM size — NOT rank-sized, which would solve `order` by eye), 4 cogs (pick≥50% / order / bound / manner). **DUAL-RECALL gate (headline):** the same-set invariant kills plain-synonym; TWO separate measured solvers kill the two remaining recall routes — `FAMILIAR_RANK_SOLVER` (defeated by NON_CANONICAL_ORDER, order uses only rote:false sets) + `CONTEXT_KEYWORD_SOLVER` (defeated by CONTEXT_KEYWORD_AMBIGUOUS, every keyword→≥2 ranks); answer derived from rank, never stored (manner's authored answerId is the legit semantic pairing). Full §A.13.62 DoD green (visual-critic CLEAN first try — text game is layout-friendly; 2 optional polish notes only). 0 lcs-shell/game-shell/core lines (only `activities.ts` row). char-spec tail → #54 Pesto. Per-locale: keyword-ambiguity corpus re-authors per locale (most language-bound L.5 strand) — EN pilot only.
- **DIVISION (3.OA.A.2) BUILT as Game #61 "Maple's Bakery"** — the lcs-shell free activity `maple-bakery` (Maple the mouse = char #56). CLARITY-FIRST: the child SEES the division — cookies shared equally onto a row of plates → tap how many on EACH plate (partitive `share`); OR packed into boxes-of-N → tap how many BOXES (quotitive `pack`); answer = D÷divisor, DERIVED-not-stored; both quotient meanings mandatory. **The walk hit THREE duplicates/defers first** (gate-check is load-bearing): the operator's pick **#59 coins (2.MD.C.8) = DUPLICATE** of the live Game #38 "Pip's Market Stall" (`coin-stall`; the INDEX #38 row even says "Game 59 SUPERSEDED-by-overlap"); **#60 RL.K.2 retell = already BUILT** (Wake-Up-Pip, `retell-story-core`); **#58 "The Memory Lantern" (RL.K.1) DEFERRED** (stub-art wordless-story comprehension is art-dependent + mandates a HUMAN native bypass-pilot ship gate I can't run + reuses the Pip-firefly/lantern theme already shipped as Wake-Up-Pip) — all surfaced via AskUserQuestion. #61 division's spec (`divide-01.md`) is an ABSTRACT structural-placement/grammar-neutralized anti-cheat design → operator ruled **"Division, CLARITY-FIRST"** (same clarity > anti-cheat-purity trade as the #81 rounding rebuild; deal-and-count allowed for free practice; the KEYWORD/SCENE/structural solver suite NOT implemented). **Build lesson:** 2-row cookie stacking → tall+narrow vessels → shrunk to pass the resolved-fold FITS gate → visual-critic flagged DESKTOP SPARSE; the fix that satisfied BOTH = **a SINGLE wide row of cookies** (fills the card width + stays short) + bigger cards + a SHORTER prompt sentence (the long prompt wrapped extra lines, eating the fold budget). Full §A.13.62 DoD green (verify-core MEASURED + visual-qa 60 + probe + local-test + variety + visual-critic PASS). 0 lcs-shell/game-shell/core lines (only the `activities.ts` row; the `lcs-shell.css` diff in git is a PRE-EXISTING uncommitted change, not mine). NO deploy (operator batches). char-spec tail → #56 Maple.
- **SKIP-COUNTING (2.NBT.A.2) BUILT as Game #67 "Hopper's Lily Hops"** — the lcs-shell free activity `skipcount` (Hopper the frog = char #57). CLARITY-FIRST: a single wide row of numbered lily-pad stepping stones + "+5/+10/+100" hop arcs, one blank pad (coral "?"); cog `fill` = tap the missing number (fwd + backward + non-multiple-start + a 100-crossing), cog `whichstep` = labels hidden → tap the hop size; answer = start+k*step, DERIVED-not-stored + not-displayed. **Gate-check after #61 first cleared the defers/owned:** #62 RL.K.3 "Dot's Story Spine" = DEFER-class (authored-story RL, explicitly extends the deferred #58 engine — art-dependent/comprehension-risky); #63 (2.OA.C.3) + #65 (2.OA.A.1/B.2) already owned → the clear unowned options were #64 affix word-meaning (ELA text) + #67 skip-counting (math); operator chose **"#67, CLARITY-FIRST"** via AskUserQuestion. The `skipcount-01.md` spec is a friction-heavy anti-cheat design (produce place-value BUILDER + double-blank + forced un-skippable interstitial vs "builder search") → SET ASIDE for clarity (same trade as #81/#61; tap-the-number, not a builder). Spec's "Pim the otter" character COLLIDES (Pim = char #2 AND #27) → renamed Hopper; "stamp mill" re-themed to lily-pad hopping (hop = +step, the clearest skip-count metaphor). **Build lesson:** a wide-short scene that fills the card width leaves the SHORT answer-card row looking sparse on a tall desktop card → the critic flagged small/thin desktop cards; fix = size the answer cards generously (tall ~96px + bold), bounded only by the resolved-state FITS budget. Full §A.13.62 DoD green (verify-core MEASURED + visual-qa 66 + probe + local-test + variety + visual-critic PASS). 0 lcs-shell/game-shell/core lines (only the `activities.ts` row; the `lcs-shell.css` diff in git is PRE-EXISTING, not mine). NO deploy. char-spec tail → #57 Hopper.
- **AFFIX WORD-MEANING (L.2.4.b/c) BUILT as Game #64 "Marigold's Knowing Machine"** — the lcs-shell free activity `affix` (Marigold the mole = char #58). CLARITY-FIRST text game (the #53 compound companion): a root + an affix-cog (un-=NOT/re-=AGAIN/-ful=FULL OF/-less=WITHOUT) → read the new word + tap its meaning; cog `apply` = tap the meaning (correct + root-only + wrong-affix), cog `which` = tap the affix-cog for a target meaning; correct DERIVED by matching round.affix (no stored isCorrect/index). The spec's tuple/phrasings/PARAPHRASE_TEMPLATE_SOLVER rigor SET ASIDE for clarity (same ruling as #81/#61/#67); simple readable meanings. **Two build lessons:** (1) a text-meaning game's meaning cards STACKED vertically blow the resolved-FITS budget on a 900-tall desktop (apply hit 1039px) → use a ROW of wrapping cards + a shorter scene (like the math number-rows); (2) that row then read DESKTOP-SPARSE → enlarge the cards (taller + bigger font) + grow the scene to fill the band, bounded by the resolved-FITS headroom (the recurring wide-short-scene↔sparse tension; resolved landed at 893≤896). Also: the -less cup glyph was a bare "U" (ambiguous) → redrawn as a clear empty cup (outline+rim) paralleling -ful. Full §A.13.62 DoD green (verify-core MEASURED + visual-qa 66 + probe + local-test + variety + visual-critic PASS). 0 lcs-shell/game-shell/core lines (only the `activities.ts` row; the `lcs-shell.css` git diff is PRE-EXISTING, not mine). NO deploy. char-spec tail → #58 Marigold.
- **LINE PLOT (2.MD.D.9) BUILT as Game #71 "Shelly's Tide-Line"** — the lcs-shell free activity `line-plot` (Shelly the hermit crab = char #59). CLARITY-FIRST concrete data graph (operator chose it over the abstract #66 RL POV + #69 transitive-reasoning via AskUserQuestion): a numbered scale (1–8) + ✕'s stacked above each length; cog `plot` = measure a shell-bar (0→length) + tap its length to drop the X, cog `read` = a question → tap a number card (how-many-at-N / longer-than / most-common [mode non-extreme] / longest-minus-shortest / how-many-more-A-than-B). Answer DERIVED from the plot data; the genuine VALUE-reads KEPT (real line plot, not a bar graph); the spec's VALUE_PERMUTE_INVARIANCE/region-count tier suite SET ASIDE for clarity. **3 build lessons:** (1) the `plot` measure-bar makes the plot scene the FITS constraint (its resolved state) → cap the plot scene tight, let the read scene grow (per-cog scene cap); (2) **`vw` resolves to the IFRAME width (~400px) not the screenshot viewport** → a `clamp(…,Nvw,…)` font sits near its MIN in-iframe and renders small → use a bigger rem FLOOR + snugger cards (the recurring desktop-card lesson, vw-floor twist); (3) the derived-not-stored mutation test must assert the oracle RECOMPUTES correctly on changed data, NOT "the answer must change" (adding a mark at max changes neither mode nor max−min → false fail). Full §A.13.62 DoD green (verify-core MEASURED vs independent truth + visual-qa 66 + probe incl read-resolved + local-test + variety + visual-critic PASS). 0 lcs-shell/game-shell/core lines (only the `activities.ts` row; `lcs-shell.css` git diff is PRE-EXISTING, not mine). NO deploy. char-spec tail → #59 Shelly.
- **⚠️ STRATEGIC FLAG (operator checkpoint): Grade 2-3 MATH white-space is now THIN.** The near-term un-built INDEX entries skew ELA/RL — #66 RL.1.6 (POV), #68 RL.1.9 (cross-text compare), #70 L.1.1.e (verb tense — ELA grammar, concrete-ish), #72 RI.2.6 (author's purpose). The clean CONCRETE-math entries are largely exhausted. At the next "continue," consider surfacing to the operator: (a) build the concrete-ish ELA #70 verb-tense next; (b) take on a deferred abstract-RL/author's-purpose entry with a comprehension-careful clarity-first design; or (c) a strategy shift. **NEXT = re-derive from the INDEX (lowest un-built unowned after #67); #66/#68/#69 are deferred-class, so #70 L.1.1.e is the likely next concrete-ish — but surface the math-white-space flag.** (#57 Rolo SUPERSEDED — 3.NBT.A.1 built as #81.)
- **#47–#54 BUILT.** #51 Quill's Mirror Market (K.L.5.b opposites) · #52 The Echo Grove (3.OA.A.1 — re-centered from "Pippa's Crate Cart"; the Echo Grove + Mochi §A.13.62 fix-backlog was VERIFIED CLEARED) · #53 Skip's Word-Welding Yard (L.2.4.d compound-word meaning) · **#54 Sprout's Patchwork Meadow (3.MD.C.6 area)** — gate-check fired (3.MD.C.6 RECOGNITION owned by the live `mosaic-menders`/Game-80) → operator ruled "build the active PRODUCE game, DROP `conserve`"; new `patchwork-meadow-core.js`+`-activity.js` (SVG tap-to-place covering, 5 cogs tile/repair/estimate/finish/build × 11 irregular regions; `COUNT_VISIBLE_OBJECTS_SOLVER` killed via empty-at-predict + knit-before-report); full §A.13.62 DoD green (visual-critic CLEAN after a desktop board scale-up `--pm-cell` 60→clamp 88 + the FITS-via-`.pm-cand`-recognized-control fix), 0 lcs-shell/shared-core. **#80 `mosaic-menders` status corrected approved→BUILT/SHIPPED** in the INDEX (it's the RECOGNITION slice of the same standard).

## Cohort anti-cheat tech-debt (the MEASURED-gate lineage — keep arming the right solver)
Each game's gate must defeat the cheapest cheat at the granularity the child plays: Game-47 STATEFUL memorize-exceptions · 48 BRUTE/enumerator · 49 WORLD-KNOWLEDGE · 50 per-ROUND print-match · 51 DIMENSION-MATCH (a same-dimension distractor makes opposition load-bearing; association-only gates are blind to it) · 53 FAMILIAR-RETRIEVAL ("derived-not-stored proves the SCORING composes, NOT that the CHILD did — gate the retrieval/shortcut route: novel + no-whole-picture"). Generalizes: for an interpret-the-notation standard adjacent to a produce standard, grade the INVERSE + DISCRIMINATION, and test the ACT not just the answer.

Related: [[project_activities_live_inventory]] · the §A.13.62 standing rule + [[feedback-visual-qa-container-containment]] · [[project_premium_games_program]] (the SEPARATE paid thrust).

## 2026-06-27 — BATCH FOLD FIX (5 activities + geometry) + authorized lcs-shell trim (BATCHED, not deployed)
Operator: 5 LIVE activities "cut off at bottom, Check not visible in some exercises" + a geometry bug. FIXED all 6.
- **A array** (build-array+equal-groups), **B fractions make-and-count-squares**, **C choice-board fraction-noncongruent** (+ Part F geometry): GREEN all 6 viewports + visual-critic CLEAN.
- **D ten-frame solve-the-story**, **E number-bond story-problem**: GREEN at 768/1024/1366/360/412 + critic CLEAN; fail ONLY 320×640 (Galaxy-Fold cover — a word-problem + manipulative + keypad physically > 640 at legibility floor; even zero manipulative → story+keypad>640; real phones scroll via growing iframe, no real cut-off). DOCUMENTED WALL.
- **Two root causes:** (1) oversized manipulatives → per-activity html `@media` caps (array cells→fixed squares so height tracks rows; fractions `.frac-wrap .frac-svg` HIGHER-specificity cap; choice-board fraction tiles + `:has(.cb-frac-board)` prompt cap; ten-frame/bond manipulative + prompt). (2) systemic keypad-family floor (~30-50px over strict 900) — **operator AUTHORIZED an lcs-shell.css trim**: dead outer `padding-block:10vh`→`clamp(16px,4vh,44px)` + universal gap 28→22 + mobile padding/gap; recovered ~80px desktop; `?v=17→18` across 92 htmls; broad regression sample clean (only reduces dead space; improved whole keypad family — place-value 1002→926, ten-frame how-many now passes).
- **Geometry F:** FRACTION_FIGURES diag lines drew to SHARP corners of rx=6 rounded rects (tip poked out) → inset endpoints (rect 13,31→87,69; sq 15,15→85,85) + share-path apexes; high-res render + critic confirm inside-shape.
- **0 protected-CORE lines** (choice-board-core/ten-frame-core untouched; only activity-layer choice-board-activity.js + authorized lcs-shell.css). All verify/local-test/preflight PASS.
- **KEY LESSONS:** image-built manipulatives (ten-frame counters) can't shrink for fold (14px TINY floor) — only text/line figures do; a WIDE figure in a SQUARE viewBox reads sparse → negative `margin-block` absorbs empty bands (scope via `:has()`); shell MOBILE prompt rule (!important,0,2,0) shadows wrapper `.lcs-activity-prompt`(!important,0,1,0) → phone prompt caps need ≥0,2,0; story-in-prompt is the tallest verbose case (drive dial-in off longest-story locale de); word-problem+keypad unfittable at 320px (real wall, phones scroll).
- **PRE-EXISTING family near-misses found during regression (NOT in batch, my trim IMPROVED their desktop):** place-value 3 (926 @1024, passes 768), write-numeral (320/360 TINY counters + 1024 now fixed), add-three (320), match-pairs (320). A follow-up could sweep these to full-green with the same manipulative-cap pattern.

## 2026-06-27/28 — FULL-LIBRARY fold SWEEP ("sweep those too") — BATCHED, not deployed
Ran §A.13.62 gate across ALL 160 activities → 13 failers (147 already green). Fixed all desktop+360/412 failures (html @media only, 0 protected-core; 5 wrapper htmls: place-value/match-pairs/choice-board/sort-bins/ten-frame).
- **8 fully green (all 6 viewports):** choice-board which-more + flat-solid; match-pairs make-the-number + addition-subtraction-pairs + find-the-missing-number; place-value tens-and-ones + expanded-form; sort-bins defining-attributes.
- **5 green at desktop+360/412, 320×640 (Galaxy-Fold) wall (documented; real phones scroll):** match-pairs three-ways (6 number-word cards), ten-frame write-numeral (20-counter density) + solve-the-story (word problem), number-bond story-problem (word problem), sort-bins sort-by-sides (multi-bin).
- **place-value-regroup (4 keypad activities) was NEVER in my named list** — the full-gate sweep is what caught it (it passed, but the sweep is why I know). LESSON: don't trust a partial-regression named list; run the full gate to find every failer.
- KEY LESSONS: (1) keep `</style>` when replacing an inline `<style>` block (dropped 3×→ scripts swallowed → ".lcs-app never mounts" gate ERROR). (2) `.tf-double` is compound on `.tf-wrap` → use `.tf-wrap.tf-double .tf-cell`, not descendant. (3) phone prompt cap needs `.lcs-app.activity .lcs-activity-prompt` (≥0,2,0 + !important) to beat the shell mobile rule; desktop works at 0,1,0. (4) generic phone card shrink BACKFIRES on heterogeneous-card activities (three-ways) → scope to the tall card type. (5) `:has(.cb-group)` / `:has(.lcs-activity-keypad)` are the clean shared-html scopers. (6) a keypad activity's manipulative is FOLD-BOUND by the ~828px chrome+keypad+Check fixed cost at vh=900 → write-numeral desktop counters capped ~40px/24px (gate not-tiny passes, but a visual-critic wants bigger; enlarging needs a protected-shell keypad change — flagged, not done).
- **OPEN (flagged to operator):** write-numeral desktop ten-frame counters are at the small-but-countable end (24px), bounded by the keypad fold; making them bigger needs a protected-shell keypad-compaction decision.

## 2026-06-28 — Bundle Bot: ones made controllable (one-by-one + tap-to-remove) — BATCHED, not deployed
Operator bug: ones couldn't increase one-by-one (forced cycling 1-3 clump via `Core.nextClump`) and couldn't be deleted (no remove path). FIX (operator chose one-by-one + tap-to-remove): added `removeOne(s)` to bundle-machine-core (non-protected); feeder → one-per-tap (`feed(cstate,1)`, "Drop a one"); each loose `.bb-cube` → button calling `_removeOne` (re-checks isSolved→win since removing an overshoot can land the target, 24→23); cubes enlarged clamp(22,5vw,28) + hover/focus coral × "removable" badge; instruction updated; `?v` core 1→2, activity 2→3; local-test extended (one-per-tap +1, tap-remove −1, remove-to-solve). The clump-feed was NOT gate-enforced (`feedIsClumpNotBlindTap` is facts-only; verify uses feed(s,1)) → core cognition (count→judge-ten→lever, refuse<10, no auto-bundle) untouched. All gates PASS (verify-core + local-test + visual-qa 54/54 + preflight + personal Read + visual-critic CLEAN). 0 protected-core (bundle files are untracked — part of the un-deployed batch). LESSONS: manipulative tap targets (≤12 tray cubes) can't hit the 44px card standard (gate exempts non-cards) → ~22-28px + visible affordance; removeOne must re-check isSolved; `filter:scale()` is invalid (scale is a transform).

## 2026-06-28 — Make Thirds: dotted cut-line outside the rectangle — FIXED (batched, not deployed)
Operator (screenshot): rect round of `fractions.partition-thirds.2-g-a-3` had a dashed ghost line above the top edge (outside). Root cause `fractions-core.js _lines` `n===3&&cut==='h'`: rect box y28..72 (h=44), distractor `_lineH(box,h1-16)`=26.67 = 1.33u above top(28). A parallel horizontal distractor can't fit ≥14u from both thirds-lines inside a 44u rect (gate's tap-separation) → it fell outside. FIX: orthogonal vertical centre distractor `_lineV(box,midX)` (inside; foil=4 unequal pieces ratio~2.0; orthogonal pair exempt from separation; correct set untouched). Only rect+h used this branch; other 6 thirds rounds were inside. GATE HARDENED: verify-fractions-core now asserts IN-BOUNDS (interior samples t∈{.25,.5,.75} must be pointInShape) — it previously checked equal-area+foil+separation but NOT in-bounds (why it shipped). `?v` core 7→8. All gates PASS (verify-core 15 rounds + gridcount + visual-qa thirds & equal-shares + preflight + personal Read [2 horiz + 1 vert, all inside] + visual-critic CLEAN). 0 protected-core (fractions-core non-protected). LESSON: an equal-area/foil/separation content gate can still miss "line drawn outside the figure" → add a point-in-shape in-bounds assertion; a too-short shape can't host a parallel distractor with tap-separation → use an orthogonal foil.

## 2026-06-28 — Mama's Roll Call: trace numerals made professional/traceable — FIXED (batched, not deployed)
Operator: numeral-trace numbers "not professional / not traceable." Root cause: `numeral-trace-core.js GLYPHS` were coarse checkpoint POLYLINES rendered raw as `<polyline>` (0=hexagon, 8=diamond-stack knot, 3/5/6/9 zigzags); same array = trace-validation checkpoints. FIX: (1) re-authored GLYPHS 0-9 to smooth K-print forms (points on the numeral curve, correct stroke order, 0 oval/4 two-stroke/8 true figure-eight/6&9 loops) keeping the gate-required structure (array-of-arrays len≥2, 4=2 strokes); (2) render each stroke as a Catmull-Rom→Bézier SPLINE `<path>` (interpolates the checkpoints → smooth guide that still hits every validation point). `?v` core 1→2, activity 3→4. Gate-safe: verify-numeral-trace-core pins structure+4=2-strokes+solvers (idealPath=checkpoints, TOL=20) → all pass. All gates PASS (verify-core + local-test + visual-qa 54/54 + preflight + before/after glyph sheets + LIVE sign-stage 8 personally Read + visual-critic CLEAN). 0 protected-core (numeral-trace-core non-protected; the only numeral-TRACING activity — Rivets shows plain-text numerals). LESSON: when ordered checkpoints double as validation path AND visible guide, render a spline-THROUGH-the-points (Catmull-Rom interpolates), not a raw polyline; change positions+render, keep the structure the gate/solvers expect. Per-locale 1/4/7/9 variants = fast-follow.

## 2026-06-28 — Mama's Roll Call: numerals look right but WON'T TRACE — FIXED (batched, not deployed)
Operator follow-up: "look much better but I still can't trace them." Root cause = trace ACCEPTANCE: `numeral-trace-core.js traceScore` was point-to-POINT advancing ≤1 checkpoint per pointer sample (TOL=20); the prior glyph rewrite made digits 11-13 checkpoints, so a real drag (fewer samples than checkpoints / skips one between samples) never advanced all → always 'off-path' → silent reject. idealPath passed only because it feeds exact-on-checkpoint points. FIX: rewrote traceScore to point-to-SEGMENT (a checkpoint counts if any drawn segment passes within TOL; inner while → one segment covers several checkpoints) + `segDist` helper; order/direction strict, scribble still fails. `?v` core 2→3 (activity unchanged). GATE #11 added: coarse on-curve path with FEWER pts than checkpoints must traceScore===true (+ off-shape false) — old code fails it, locks the fix. PROVEN: verify-core PASS incl #11 (all 10 digits) + local-test + visual-qa 54/54 + preflight + LIVE real-PointerEvent coarse drag advanced strokesDone 0→1 + full count→trace→seal SEALED+solved + sealed render Read. 0 protected-core. LESSONS: (1) trace validators MUST be point-to-SEGMENT not point-to-point-per-sample (pointer sample count is variable + usually < checkpoint count). (2) puppeteer page.mouse may not fire pointer* listeners headless — dispatch real PointerEvents in-page to prove a drag. (3) "looks fixed" can hide a broken INTERACTION — prove the actual gesture.

## 2026-06-28 — Mama's Roll Call trace: exhaustive re-analysis (vs working snippy) — CANNOT REPRODUCE; applied preventDefault parity
Operator: trace STILL fails, compare to snippy (works). FINDINGS: (1) snippy's traceScore = IDENTICAL point-to-point → my point-to-segment "fix" was a red herring. (2) Instrumented real CDP mouse+touch wobble A/B with correct count: mama's _traceDown fires, _formStroke gets the path, traceScore=TRUE, round SEALS (mouse+touch, even digit 8/13-checkpoints) → the trace WORKS in headless; my earlier "0→0" probe failures were COUNT ARTIFACTS (over-sent ducks → commit-mismatch → state reset), not trace failures. (3) The ONE code diff from working snippy = `ev.preventDefault()` on pointerdown (mama lacked it; real browsers can hijack a drag with selection/callout/scroll — headless doesn't). APPLIED: preventDefault in _traceDown+_traceMove (e.cancelable-guarded) + user-select/touch-callout:none on .rc-glyph; ?v activity 4→5. Verified mouse+touch seal + all gates PASS, no regression. **HONEST: cannot reproduce the failure → preventDefault is correct parity but UNCONFIRMED as the cause.** Asked operator for: preview-vs-LIVE (everything batched-NOT-deployed → live = old code!), device/touch-vs-mouse, exact symptom. LESSON: tests-pass-but-operator-fails → bug is in the untested layer (real-browser input hijack / device / STALENESS) — gather repro, don't guess again; isolate trace from commit when probing (count artifacts mask trace success).

## 2026-06-28 — Mama's Roll Call trace: REAL CAUSE = NO LIVE-INK feedback (FIXED, batched)
After 3 wrong diagnoses (glyph look, point-to-segment, checkpoint density), the operator's clues cracked it: LOCAL PREVIEW + MOUSE + "nothing draws at all." Validation was NEVER the problem (sealed in every headless test). Bug: `_traceMove` accumulated points but NEVER painted → no ink while dragging → "can't trace." Snippy works because it `_paintCur`s on every pointermove. FIX: added `_paintCur` to Mama's (one coral `.rc-curink` path of in-drag pts, no full re-render) + called from `_traceMove`; kept this session's snippy-parity guards (preventDefault + user-select/callout none). `?v` activity →5; numeral-trace-core UNCHANGED (the approved plan's sparse-checkpoint/TOL changes were correctly skipped — diagnostic proved validation fine). PROVEN: mid-drag coral ink trail renders (screenshot Read) + still seals; all gates PASS. 0 protected-core. META-LESSON: "tests pass but operator fails" → get exact symptom+environment FIRST (AskUserQuestion), don't guess code. "Nothing draws" = feedback/render bug not logic; headless seal-tests masked it (never needed to SEE ink).

## 2026-06-28 — Mim's Glow Workshop (shapeforge) "can't fill any shape" — var-closure-in-loop bug FIXED (batched)
Operator: pieces place/rotate but no shape fills (preview, all shapes). Logic was correct (UI-path probe: every round solvable via offered anchors+rotate). REAL CAUSE (reproduced in DOM): anchor-dot click handler closed over `for(var o…)` loop var → at click `o===orientCount` (invalid) → `_placeAt(invalid orient)` → cellsOf(orients[invalid]) covers nothing → never completes; hit every piece/round. FIX: `var oFix=o` captured per-dot, used in click/keydown + cells/legalAnchors. `?v` activity 2→3; core unchanged. PROVEN: real dot click → orient 0 → onramp SOLVES; all 11 rounds complete via real flow; all gates PASS; solved render Read ("You forged it!"). 0 protected-core. LESSON: classic var-in-loop closure; the solvability gate tested the `solve` ORACLE not the UI dot-click → green while every click broke. Exercise the REAL DOM click, not just the model (trace-saga lesson, applied right this time — UI-path probe + dot-click test nailed it fast).

## 🐛 BUGFIX 2026-06-28 — Winter Piles (winter-piles.draw-partition.2-oa-c-4) "grid corners almost invisible" FIXED. Cause: `.wp-field` SVG `background:#5A4632`+CSS `border-radius:18px` → border-radius clips the brown bg at corners to transparent → corners show the cream card behind (cream-on-cream); only an `inset 0 0 0 4px rgba(0,0,0,.12)` defined them (invisible). Fix (winter-piles-activity.js only, 2 edits): `.wp-field` box-shadow → OUTER brown ring `0 0 0 3px #5A4632`+stronger inset+soft drop (outer box-shadow follows border-radius → all 4 corners crisp vs card); band rect `rx:26→14` (concentric w/ 18px frame → uniform frame width). `?v=8→9`; draw-partition-core UNCHANGED; 0 protected-core. Full §A.13.62 DoD green (verify-draw-partition-core + local-test + preflight + visual-qa 54/54 + personal Read 360/768/1024 un-cut+multi-band + visual-critic CORNERS/OVERALL CLEAN). Batched — NOT deployed. LESSON: a rounded box whose corner fill is its CSS `background` loses its corners when border-radius clips that bg vs a same-family bg — define the corner with an OUTER ring/border (outer box-shadow follows border-radius), never rely on an inset shadow + the bg.

## 🐛 BUGFIX 2026-06-28 — Tildy's Tailor Shop (tildy.measure-ruler.2-md-a-1) "can't slide the ruler" FIXED. Cause: classic drag bug — `_setRulerZero`'s drag branch did `_repaintRuler()`→full `this.render()`→`stage.innerHTML=''` which detaches the pointer-captured ruler `<g>` mid-drag → capture lost → ruler moves one tick then sticks (also detaches the pointermove closure's `sv`→getBoundingClientRect zeros→toCm garbage). Fix (tildy-activity.js only): store `_rulerG`+`_rulerBaseZero` at render, SLIDE the existing group via `transform:translate((rulerZero-base)*CMW,0)` during drag — NO re-render; render only to settle on pointerup/pointercancel (the winter-piles "no full render during drag" pattern). `?v=5→6`; tildy-core UNCHANGED; 0 protected-core. PROVEN: full §A.13.62 DoD green (verify-tildy-core + local-test + _tildy-probe + visual-qa 54/54 + preflight + personal Read 360/768/1024 + visual-critic CLEAN) PLUS new `scripts/_tildy-drag-probe.js` = a REAL mouse-drag test (tracks ≥3 moves incl. vertical drift off the band = capture-survival; clamps at 0 + right edge); PROVEN to FAIL on the buggy build (stuck track [2,2,2,2,2,2,2,2]) and PASS on the fix ([2,1,1,0,0,0,0,0]). Batched — NOT deployed. LESSON: a drag/slide gesture must NEVER full-re-render mid-gesture (detaches the captured element + the move-closure's element refs); mutate the existing element's transform/attrs during drag, render only on release; and verify drags with a REAL multi-move pointer sequence WITH off-target drift — the old _tildy-probe drove `_setRulerZero` directly so it never exercised the gesture (same lesson as trace/shapeforge: test the real DOM interaction).

## 🐛 BUGFIX 2026-06-28 — Tuck Makes a Hundred (place-value-regroup.add-compose-hundred.2-nbt-b-7) "hundred-flats overflow the column when hundreds>3" FIXED. Cause: `.pvr-col-hundreds .pvr-blocks{flex-wrap:nowrap}` forced 4-5 hundred-flats onto one row, but compose-hundred's `fat-tens` mode (tens flex:1.9) squeezes the hundreds column to ~90-113px → flats (28px each, 4=121px/5=152px) spilled OUTSIDE the column border. An in-column horizontal spill that page-overflow/fold gates (visual-qa, _tuck-hundred-probe) never caught (stays within page width) → shipped "green." Fix (place-value-regroup-core.js only, 1 CSS rule): nowrap→WRAP on the hundreds blocks → wrap to centered rows inside the box (5=3+2 desktop/4+1 phone); fold unaffected. `?v=16→17`; activity.js + protected place-value-core.js UNTOUCHED; 0 protected-core. PROVEN: full DoD green (verify + 3 local-tests + _tuck-hundred-probe + visual-qa 48×3 + preflight + personal Read 5-flat 360/768/1024 + visual-critic CONTAINMENT/OVERALL CLEAN) PLUS new `scripts/_tuck-hundred-containment-probe.js` (asserts every .pvr-flat bbox ⊆ .pvr-col-hundreds box across 6 viewports; PROVEN to FAIL on nowrap [spills 2-4] and PASS on wrap). Batched — NOT deployed. LESSON: a flex-wrap:nowrap row of FIXED-size blocks inside a FLEX-sized column overflows once count exceeds the variable width — wrap it; and add a CONTAINMENT assertion (child bbox ⊆ parent bbox) — page-overflow/fold gates miss a child spilling its OWN box while inside the page (same family as the §A.13.62 desktop-cut-off gap).

## 🆕 BUILD 2026-06-28 — expanded "Wake Up, Pip!" (RL.K.2 retell) into a K reading-comprehension TRIAD with 2 NEW activities (operator: "build two more with original design + context to expand it"). EN-only pilots, batched NOT deployed. **#1 "Otto's Picture Book" (RL.K.7, char Otto the owl)** — tap WHICH of 4 emoji-stub illustrations (a picture-book spread) shows the part Otto reads; foils = same-story other moments; 8 original mini-stories, targets balanced across positions. New clean-sibling `picture-moment-core.js` + `otto-picture-book-activity.{js,html}` + `-activities.json`; id `otto-picture-book.which-picture.rl-k-7` slug `ottos-picture-book`. **#2 "Willow's Story Corner" (RL.K.9, char Willow the tortoise)** — read two parallel tales, tap WHICH character had a named experience → Fox/Bunny/Both/Neither (CHARACTER-centric, deliberately distinct from existing `two-tales` RL.1.9 which taps an experience STATEMENT); 6 friend-tales × boolean fact-keys, 10 Qs, answer DERIVED, all 4 options load-bearing. New clean-sibling `compare-tales-core.js` + `willow-story-corner-activity.{js,html}` + `-activities.json`; id `willow-story-corner.compare-tales.rl-k-9` slug `willows-story-corner`. Both: answerType:'state' (in-stage tap → shell Check), diffuse no-leak feedback, ≥7 + reshuffle, emoji stubs (CA5 later), 0 protected-core/lcs-shell; the ONLY tracked edit = 2 rows in `frontend/lib/activities.ts MANIFEST_FILES`. **Lean DoD** (per the new lean-QA pref): verify-core (oracle 100% / random≈chance / derived / no-leak) + local-test + preflight + scoped `visual-qa-activity.js` (Otto 48/48, Willow 60/60 — both caught + fixed a real cut-off) + personal eyeball phone+desktop. Both unowned codes confirmed (RL.K.1/2/3 owned). → [Lean activity QA](feedback-lean-activity-qa.md).

## 🆕 BUILD 2026-06-28 — expanded "Vet's Diagnosis Window" (1.OA.A.1 bar-model) into a word-problem-MODELING set with 2 NEW Grade-2 activities (operator: "build two more with original design + context"). EN-only pilots, batched NOT deployed. **#1 "Bram's Board Shop" (2.MD.B.5, char Bram beaver)** — LENGTH word problems modeled on a proportional TAPE/ruler (place length-chips in segments + mark unknown + dial cm); NEW clean-sibling `length-tape-core.js` (adapts vet role-math, within-100, magnitude-decoy). **#2 "Hopper's Number Line" (2.MD.B.6, char Hopper frog)** — sums/differences modeled as a HOP on a 0..N number line (tap start tick + forward/back + size chip[+decoy] → span/arrow/landing-lily "?" → dial landing); NEW clean-sibling `numberline-jump-core.js` (landing=start±size derived). Both: drawn models (no assets), answerType:'state'+own commit+shell Check, derived answer, ≥7+reshuffle, 0 protected-core/lcs-shell; only tracked edit = 2 rows in `activities.ts MANIFEST_FILES`. **Lean DoD**: verify-core (oracle 100% / all non-modeling cheats 0% / decoy-biggest [Bram] / dial-grab+wrong-size+wrong-dir+wrong-start=0 [Hopper]) + local-test + preflight + scoped visual-qa (Bram 54/54, Hopper 54/54 model phase) + eyeball. **KEY LESSON: scoped visual-qa only drives FIRST-PAINT — a multi-phase modeler's later (dial) phase is taller and UNTESTED by it; Hopper's dial phase cut off at 320×640/360×740/AND desktop 1024×900 while first-paint passed → caught only by a manual dial-phase capture+measure, fixed by collapsing the modeling UI in the dial phase. For any phase the scoped gate can't reach, capture+measure that phase's fold by hand.** Modeling set: Vet 1.OA.A.1 → Bram 2.MD.B.5 → Hopper 2.MD.B.6. → [Lean activity QA](feedback-lean-activity-qa.md).

## 🆕 BUILD 2026-06-28 — expanded "Two Moons" (RL.1.9 compare two stories) into a Grade-1 story-comprehension set with 2 NEW activities (operator: "build two more with original design + context"). EN-only pilots, batched NOT deployed. **#1 "Juniper's Story Lantern" (RL.1.2, char Juniper hedgehog)** — read a fable, tap the card that states its LESSON; 3 cards = moral + true-detail-foil(quotes story) + wrong-moral-foil; NEW clean-sibling `central-message-core.js`. **#2 "Marlo's Magnifier" (RL.1.3, char Marlo raccoon detective)** — story + a trait clue ("Mira is brave"), tap the detail that PROVES it; foils = true details that don't show the trait; evidence never restates the trait word; NEW clean-sibling `trait-evidence-core.js`. Both: text+emoji stubs (no assets), answerType:'state' tap-a-card+shell Check, diffuse no-leak feedback, ≥7+reshuffle, 0 protected-core/lcs-shell; only tracked edit = 2 rows in `activities.ts MANIFEST_FILES`. **ANTI-CHEAT design: the obvious-looking foil is WRONG** — Juniper's detail-foil has the highest story word-overlap (so "pick the most story-like card" misses the abstract moral, overlap-bot 0%); Marlo's evidence never contains the trait word (trait-word-bot 0%). Verify gates MEASURE position/longest/shortest/overlap/trait-word bots ≤ chance + oracle 100%. **Lean DoD** green both (verify + local-test + preflight + scoped visual-qa 48/48 + eyeball). LESSON: text-heavy reading activities (story + multi-line cards) cut off at BOTH 320×640 AND 900-tall desktop; Marlo needed a dedicated `@media(max-height:920px)` block (the ≤700 blocks never reach the 900-tall desktop) + dropping the mascot row + auto-narrating Read button at ≤640; 3 cards×44px + shell Check = the irreducible height floor. Set: RL.1.9→RL.1.2→RL.1.3. → [Lean activity QA](feedback-lean-activity-qa.md).

## 🆕 BUILD 2026-06-28 — expanded "The Harbor Post" (RI.2.6 author's purpose) into a Grade-2 "read the news" set with 2 NEW activities (operator: "build two more with original design + context"). EN-only pilots, batched NOT deployed. **#1 "Pearl's Opinion Page" (RI.2.8, char Pearl seal)** — a POINT/claim banner, tap the card that BACKS IT UP; 3 cards = reason + circular-restate-foil(echoes the claim) + offtopic; NEW clean-sibling `reason-support-core.js`. **#2 "Marina's Headline Desk" (RI.2.2, char Marina otter)** — read an article, tap the MAIN TOPIC; 3 cards = topic(broad) + detail-foil(quotes the article) + offtopic; NEW clean-sibling `main-topic-core.js`. Both: text+SVG stubs (no assets), answerType:'state' tap-a-card+shell Check, diffuse no-leak feedback, ≥7+reshuffle, 0 protected-core/lcs-shell; only tracked edit = 2 rows in `activities.ts MANIFEST_FILES`. **ANTI-CHEAT (same family as Juniper/Marlo): the obvious-looking foil is the highest-overlap WRONG answer** — Pearl's restate echoes the claim (point-overlap bot → restate, 0% on reason); Marina's detail quotes the article (article-overlap bot → detail, 0% on topic). Verify gates MEASURE position/longest/shortest/overlap ≤ chance + oracle 100%. LENGTH-BALANCING iteration: Pearl lengthened the restate foils (reason not longest); Marina SHORTENED the offtopic foils (the broad topic must not be the shortest — shortest-bot was 100% until offtopics went shortest). Lean DoD green both (verify + local-test + preflight + scoped visual-qa 48/48 + eyeball). FOLD: Pearl (point banner, no story) 48/48 first try; Marina (3-line article) shipped with the Juniper/Marlo fold playbook pre-applied (general trims + max-height:920 desktop block + drop mascot/Read ≤640) → 48/48 first try. Triad: RI.2.6→RI.2.2→RI.2.8. → [Lean activity QA](feedback-lean-activity-qa.md).

## 🆕 BUILD 2026-06-28 — expanded "The Borrowed Hat" (L.1.1.d personal pronouns) into a Grade-1/2 grammar set with 2 NEW activities (operator: "build two more with original design + context"). EN-only pilots, batched NOT deployed. **#1 "Robin's Mirror" (L.2.1.c reflexive pronouns, char Robin robin)** — sentence with "___", tap the reflexive (3 chips) matching the subject; derived from REFLEXIVE_TABLE; NEW clean-sibling `reflexive-pronoun-core.js`. **#2 "Hazel's Word Bridge" (L.1.1.g conjunctions, char Hazel heron)** — two-clause sentence with "___", tap the joining word (4 fixed chips and/but/because/so) fitting the RELATION; derived from RELATION_CONJ; NEW clean-sibling `conjunction-core.js`. Both: text+SVG stubs (no assets), answerType:'state' tap-a-chip+shell Check, diffuse no-leak feedback, chips shuffle per render (no position cue), ≥7+reshuffle, 0 protected-core/lcs-shell; only tracked edit = 2 rows in `activities.ts MANIFEST_FILES`. **ANTI-CHEAT (derived-from-table, like The Borrowed Hat): answer DERIVED from a table (never stored), no-answer-leak (correct word not in the sentence), chips shuffle.** Verify gates MEASURE oracle 100% + longest/shortest/fixed-guess ≤ chance + coverage (≥4 referents / all 4 relations). LENGTH-BALANCING (Robin): correct reflexive mustn't be systematically the shortest chip → swapped 2 wrong-referents to "i"(myself shortest) so a foil is shortest (shortest-bot 50%→25%); Hazel balanced first try (2/relation → each conj 25%). Lean DoD green both (verify + local-test + preflight + scoped visual-qa 48/48 + eyeball); both short single-sentence → 48/48 first try incl. desktop. Set: L.1.1.d→L.2.1.c→L.1.1.g. → [Lean activity QA](feedback-lean-activity-qa.md).

## 🆕 BUILD 2026-06-28 — expanded "Sunny-Side Diner" (L.K.1.f say the whole order) into a Kindergarten language set with 2 NEW activities (operator: "build two more with original design + context"). EN-only pilots, batched NOT deployed. **#1 "Wren's Question Window" (L.K.1.d question words, char Wren wren)** — a question with "___" ("___ would you like to eat?"), tap the wh-word among 6 FIXED chips (who/what/where/when/why/how); derived from QWORD_TABLE (person→who…manner→how); round stores {sentence, asks} (answer-TYPE enum) never the wh-word; NEW clean-sibling `question-word-core.js`. **#2 "Daisy's Plate Stack" (L.K.1.c regular plural nouns, char Daisy duck)** — request for more-than-one ("Wash all six ___."), tap the correct plural among 3 chips; derived from the /s/-or-/es/ RULE (pluralize: +es after s/x/z/ch/sh else +s); round stores {noun, sentence}; the 3 chips DERIVED [plural, singular, wrongPlural] where wrongPlural uses the OTHER suffix (cup→"cupes", dish→"dishs") so the /s/-vs-/es/ choice is load-bearing; NEW clean-sibling `plural-noun-core.js` (DISTINCT from the existing `plural` engine = L.2.1.b IRREGULAR). Both: text+SVG stubs (no assets), answerType:'state' tap-a-chip+shell Check, diffuse no-leak feedback, chips shuffle per render (no position cue), ≥7+reshuffle, 0 protected-core/lcs-shell; only tracked edit = 2 rows in `activities.ts MANIFEST_FILES`. **ANTI-CHEAT (derived-from-table/rule, like Robin/Hazel): answer DERIVED (never stored), no-answer-leak, chips shuffle.** Verify gates MEASURE oracle 100% + longest/shortest/fixed-guess ≤ chance + coverage (all 6 question-types / ≥2 /es/ nouns) + ≥7; Daisy's gate ALSO asserts pluralize(noun)===HARDCODED ground-truth plural for every noun incl. all /es/ (ground truth in the gate file, not the round → round stays answer-free). LENGTH-BALANCING (Daisy): singular is ALWAYS shortest (base form, never the answer → shortest-bot 0%); +es plural ALWAYS longest → capped /es/-class to 3/9 (33%) so longest-bot ≤ chance. Lean DoD green both (verify + local-test + preflight + scoped visual-qa: Wren 48/48, Daisy 54/54 + eyeball incl. desktop 768/1024); both short single-sentence → first try. Set: L.K.1.f→L.K.1.d→L.K.1.c. → [Lean activity QA](feedback-lean-activity-qa.md).

## 🆕 BUILD 2026-06-28 — expanded "Story Problems … Within 100 as a Bond" (2.OA.A.1, the 6th `number-bond-activities.json` entry) into a Grade-2 within-100 number-sense set with 2 NEW activities (operator: "build two more with original design + context"). EN-only pilots, batched NOT deployed. The within-100 word-problem code (2.OA.A.1) + money (2.MD.C.8) + length (2.MD.B.5, Bram) are all OWNED → the two new ones take UNOWNED Grade-2 codes (operator-confirmed): 2.NBT.B.8 + 2.NBT.B.6. **#1 "Skip the Kangaroo — Ten More, Ten Less" (2.NBT.B.8 mental ±10/±100, char Skip kangaroo)** — a 3-digit START + a hop card (10/100 more/less, shown ↗+10 / ↘−100), compute & type the landing number on the shell **keypad**; derived from start+delta; defining invariant = the ONES digit never changes (place-value hop); NEW clean-sibling `jump-tens-core.js`. **#2 "Tally the Squirrel — Add the Baskets" (2.NBT.B.6 add up to four two-digit numbers, char Tally squirrel)** — 2–4 basket cards joined by "+", add them & type the total on the keypad; derived = sum(addends); 3 rounds each of 2/3/4 addends (instantiates "up to four"); NEW clean-sibling `add-stack-core.js`. Both: shell **keypad** (`answerType:'number'`, stage display-only — modeled on the named word-problem render + array/place-value keypad activities); text+SVG stubs (no assets); ≥9 rounds + reshuffle; 0 protected-core/lcs-shell; only tracked edit = 2 rows in `activities.ts MANIFEST_FILES`. **ANTI-CHEAT (derived-from-operands, keypad): answer DERIVED from the operands (never stored); the stage NEVER shows the result/total (no-leak check in both local-tests).** Verify gates MEASURE oracle 100% + keypad bots ≤ chance (Skip: copy-start 0% + fixed-guess 11.1%; Tally: copy-largest-addend 0% + copy-first-addend 0% + fixed-guess 11.1%) + standard invariants (Skip: ones-digit-unchanged every round + all four deltas + result∈[100,900]; Tally: every addend two-digit + ≥1 four-addend + ≥1 three-addend + total≤140). Lean DoD green both (verify + local-test + preflight + scoped visual-qa 54/54 + eyeball desktop 768/1024 + phone 360). FOLD: keypad activities run tall but passed 54/54 first try with the `@media(max-height:920/700/640)` blocks pre-applied (drop mascot ≤640); keypad height doesn't grow on resolve so §A.13.62 resolved-state risk is bounded by the verified first-paint. GOTCHA: the EN-only `page_intro` inline object needs its closing `}` (`"..." },` not `"...",`) — a missing brace fails the whole manifest JSON.parse; a depth-audit pinpointed the unclosed `{`. Set: 2.OA.A.1 story-bond → 2.NBT.B.8 ±10/±100 hops → 2.NBT.B.6 add-four. → [Lean activity QA](feedback-lean-activity-qa.md).

## 🔧 FIX 2026-06-28 — the Story-Problems expansion (2.NBT.B.8 + 2.NBT.B.6) shipped UNFINDABLE in the preview index. I wrongly blamed browser cache twice; REAL cause was my naming: (a) page_title didn't LEAD with the character name (every sibling does → a name-search in the 5050 index found nothing); (b) title/character COLLISIONS — my 2.NBT.B.8 "Ten More, Ten Less" duplicated the EXISTING 1.NBT.C.5 "Ten More, Ten Less" (choice-board), and chars "Skip" (Skip's Word-Welding Yard) + "Squirrel" (Squirrel's Fair Winter Piles) were already taken (Hopper owns hop/skip-count). FIX (operator: keep 2.NBT.B.8, differentiate): re-charactered **Skip the Kangaroo → Comet the Kangaroo** ("big leaps", id `comet-kangaroo.tens-hundreds.2-nbt-b-8`, page_title **"Comet's Big Leaps — 10 & 100 More or Less (Grade 2)"**, intro stresses 100-more/less + three-digit = the Grade-2 extension of the Grade-1 ±10 1.NBT.C.5); #2 kept "Tally the Squirrel" (name "Tally" unused) → page_title **"Tally's Acorn Baskets — Add Up to Four Two-Digit Numbers (Grade 2)"**. Renamed files/scripts skip-kangaroo→comet-kangaroo; cores byte-unchanged; activities.ts row swapped. All gates re-green (verify ×2 + local-test ×2 + preflight 103 + visual-qa 54/54 ×2 + eyeball); curl-confirmed the index now leads with Comet's/Tally's, no orphan, one "Ten More, Ten Less" left (the existing). Also patched `_preview-server.js` to send `Cache-Control: no-store` (prevents future stale-index confusion). NEW STANDING DESIGN GATE: page_title MUST lead with the character name + grep existing page_titles for NAME/TITLE/skill collisions at design time (not just the CCSS owned-code check). → [Lean activity QA](feedback-lean-activity-qa.md).

## 🆕 BUILD 2026-06-28 — expanded "Skip's Word-Welding Yard" (L.2.4.d compounds) into the Grade-2 word-meaning toolkit with 2 NEW activities. EN-only pilots, batched NOT deployed. COLLISION CHECK done FIRST this time (post Comet/Tally failure): cluster owns L.2.4.b (Marigold prefixes/suffixes) + L.1.5.d (Pesto shades) → L.2.4.c + L.2.5.b ruled OUT; operator-confirmed unowned+non-colliding = L.2.4.a + L.2.4.e. **#1 "Fern's Clue Garden" (L.2.4.a context clues, char Fern fawn)** — read a sentence (target word highlighted), tap the meaning the context points to; 3 meaning cards, answerType:'state' tap-a-card+Check; NEW clean-sibling `context-clue-core.js`. ANTI-CHEAT (obvious-foil): cards `correct`/`no-context`(the word's other sense=trap)/`off`; gate MEASURES position/longest/shortest + OVERLAP bot ≤ chance (correct meanings paraphrase, not echo → overlap-bot 0%). **#2 "Booker's Glossary Desk" (L.2.4.e guide words, char Booker bear)** — glossary page shows guide words (cap•••cot), tap the word that belongs between them in ABC order; 3 short word cards; NEW clean-sibling `glossary-guide-core.js`. ANTI-CHEAT (DERIVED/alphabetical, strongest): round {g1,g2,words}, oracle=the word with lower(g1)≤w≤lower(g2); each round has a same-first-letter out-of-range foil (cap..cot→car/can/cup all "c") forcing 2nd/3rd-letter reasoning; gate MEASURES exactly-one-in-range + same-letter-foil + position/longest/shortest/fixed-guess ≤ chance. Both ≥8(=9) rounds + reshuffle, cards shuffle, EN-only, 0 protected-core/lcs-shell; only tracked edit = 2 rows in `activities.ts MANIFEST_FILES`. Lean DoD green both (verify + local-test + preflight 105 + scoped visual-qa 54/54 + eyeball desktop/phone). LESSONS APPLIED: collision-grep before locking codes; page_title LEADS with character name (curl-confirmed the live index lists "Fern's Clue Garden"+"Booker's Glossary Desk"); childView-leak gate must check the `.kind` PROPERTY not the substring "kind" (the "a kind of juicy fruit" card tripped it — the Juniper trap). Set: L.2.4.b→L.2.4.d→L.2.4.a→L.2.4.e. → [Lean activity QA](feedback-lean-activity-qa.md).

## 🆕 BUILD 2026-06-28 — expanded "Rosa Raccoon's Rhyme Wagon" (RF.K.2.a rhyme) into the phonological-awareness LISTENING cluster with 2 NEW activities. EN-only pilots, batched NOT deployed. Collision-check first: RF.K.2 .b/.c/.d + RF.K.3.a/.c owned → only RF.K.2.e free; operator pair = RF.K.2.e + RF.1.2.a. DEPARTURE from text-stub pilots: these MATCH the named = image-library pictures + tap-to-hear TTS (pre-reader phonemic awareness). Reuse rhyme-shop/Coco helpers (`imgUrl /image-library-webp/themes/<themeDir>/<noun>@2x.webp` onerror-hide + `LCSAudio.speak` whole-word; verified CVC `{noun,themeDir,b,m,e}` pool). **#1 "Pepper's Sound Swap" (RF.K.2.e, char Pepper parrot)** — hear cat, see letter cue "change FIRST sound to H", tap the picture of hat; NEW `phoneme-swap-core.js`. ANTI-CHEAT (DERIVED phoneme match): oracle=choice whose {b,m,e}==target with [position]→phoneme; near-miss = same-rime different-onset (2-of-3); letter is display cue not answer. **#2 "Stretch the Giraffe" (RF.1.2.a, char Stretch giraffe)** — hear words, tap the LONG (or SHORT) vowel one; ask ALTERNATES; NEW `vowel-length-core.js`. ANTI-CHEAT: oracle=choice whose vowel==ask; long/short axis is the SKILL (not a gated bot, like Booker's alphabetical axis), mitigated by ASK-BALANCE; gate the 4 FORM bots ≤chance. Both gates ALSO MEASURE image-existence (`<themeDir>/<noun>@2x.webp` exists) — no broken pictures. KEY FIX: long-vowel words skew 4-letter/short 3-letter → balanced lengths (long-3 bee/pie + short-4 fish/duck) so shortest-bot ≤chance. Both ≥8 rounds + reshuffle, cards shuffle, EN-only, 0 protected-core/lcs-shell; only tracked edit = 2 rows in `activities.ts MANIFEST_FILES`. Lean DoD green both (verify+image-existence + local-test[serves image-library-webp, naturalWidth>0 broken-image catch] + preflight 107 + scoped visual-qa 48/48 + eyeball desktop/phone, pictures recognizable). LESSONS: collision-grep before codes; page_title LEADS with name (curl-confirmed index); local-test isNoise must include "Failed to load resource"/"inventory.json" (LCSAudio /audio/inventory.json 404 harmless; broken images caught by naturalWidth not console). Cluster: RF.K.2.a→RF.K.2.e→RF.1.2.a. → [Lean activity QA](feedback-lean-activity-qa.md).

## 🆕 BUILD 2026-06-28 — expanded "Read the Word, Tap the Picture" (RF.K.3 decode CVC) into the decoding cluster with 2 NEW activities. EN-only pilots, batched NOT deployed. Collision-check first: RF.K.3.a/.c + RF.K.3 + RF.1.3 builds owned; RF.K.3.b overlaps just-built Stretch; RF.1.3.a/.c overlap digraph/magic-e builds → operator pair = RF.K.3.d + RF.1.3.e. Both reuse image-library pictures + TTS (Pepper/Stretch pattern). **#1 "Bingo's Word Hunt" (RF.K.3.d, char Bingo beagle)** — INVERTS the named: PICTURE + 3 printed WORD cards (cat/cot/cut vowel-minimal-pairs), tap the word that names the picture; word cards silent (decode them), picture has 🔊; NEW `word-match-core.js`. ANTI-CHEAT (DERIVED): oracle=choice word===picture noun; gate asserts every foil differs by EXACTLY ONE letter + bots ≤ chance + image-existence (only target needs a pic; foils are text). **#2 "Domino's Two-Part Words" (RF.1.3.e, char Domino penguin)** — read a 2-syllable word shown SPLIT (rab·bit), tap the picture; 3 confusable 2-syllable picture choices (rabbit/ribbon/robot); NEW `read-bisyllable-core.js`. ANTI-CHEAT (DERIVED): oracle=choice noun===printed word; gate asserts syl.join('')===word + 2-syllables + bots ≤ chance + image-existence (all choices). Both ≥8 rounds + reshuffle, cards shuffle, EN-only, 0 protected-core/lcs-shell; only tracked edit = 2 rows in `activities.ts MANIFEST_FILES`. Lean DoD green both (verify+image-existence + local-test[serves image-library-webp, naturalWidth>0] + preflight 109 + scoped visual-qa 48/48 + eyeball desktop/phone, pictures recognizable). LESSONS: collision-grep before codes; page_title LEADS with name (curl-confirmed index); local-test isNoise must include "Failed to load resource"/"inventory.json". Cluster: RF.K.3→RF.K.3.d→RF.1.3.e. → [Lean activity QA](feedback-lean-activity-qa.md).

## 🆕 BUILD 2026-06-28 — expanded "Quill's Mirror Market" (L.K.5.b antonyms) into the word-relationship cluster with 2 NEW activities. EN-only pilots, batched NOT deployed. Collision-check first: L.5 cluster owns categories (Mim L.K.5.a) + shades (Pesto L.1.5.d) → operator pair = L.1.5.b + L.1.5.c. Kept the two mechanically DISTINCT: Olive=picture+category-WORD cards, Jasper=situation-text+picture cards. **#1 "Olive's Kind-Of Tree" (L.1.5.b, char Olive owl)** — picture + attribute clue ("it flies and has feathers"), tap the category word (bird/fish/food); NEW `category-define-core.js`. ANTI-CHEAT (DERIVED): oracle=choice word===category; clearly-different-category foils; balanced category-word lengths so length bots ≤ chance + image-existence (target only). **#2 "Jasper's Just-Right Bag" (L.1.5.c, char Jasper jay)** — situation ("it is raining — what do you use?"), tap the PICTURE (umbrella); NEW `real-life-use-core.js`. ANTI-CHEAT (DERIVED): oracle=choice noun===correctNoun; foils don't fit + each object is correct in one round/foil in another ("always pick X" fails); balanced label lengths + image-existence (all choices). Both ≥8 rounds + reshuffle, cards shuffle, image-library pictures + TTS, EN-only, 0 protected-core/lcs-shell; only tracked edit = 2 rows in `activities.ts MANIFEST_FILES`. Lean DoD green both (verify+image-existence + local-test[serves image-library-webp, naturalWidth>0] + preflight 111 + scoped visual-qa 48/48 + eyeball desktop/phone, pictures recognizable). LESSONS: collision-grep before codes; page_title LEADS with name (curl-confirmed index); isNoise includes "Failed to load resource"/"inventory.json". Cluster: L.K.5.a/L.K.5.b/L.1.5.d → L.1.5.b + L.1.5.c. → [Lean activity QA](feedback-lean-activity-qa.md).

## 🆕 BUILD 2026-06-28 — expanded "Pesto's Soup Stall" (L.1.5.d shades) into the word-nuance cluster with 2 NEW activities. EN-only pilots, batched NOT deployed. Collision-check first: L.5 cluster fully built (Mim/Quill/Olive/Jasper/Pesto) → operator pair = L.2.5.b (shades grade-up) + L.3.5.a (idioms, new skill). Both text-based/self-contained (no images/audio). **#1 "Roary's Roar Meter" (L.2.5.b shades G2, char Roary lion)** — NEW mechanic: 3 graded words (talk/shout/scream), tap the STRONGEST/WEAKEST (ask alternates); NEW `word-intensity-core.js`. ANTI-CHEAT (DERIVED rank, ask-flips like Stretch): oracle=max/min rank; MIDDLE never the answer (gate asserts=0); ask balanced; form bots ≤ chance. LESSON: intensity↑≈length↑ → curate sets so the extreme answer isn't the length-extreme (big/large/HUGE; longest 37.5%/shortest 25%). **#2 "Gabby's Funny Sayings" (L.3.5.a idioms, char Gabby goose)** — read an idiom ("raining cats and dogs"), tap the REAL meaning; NEW `idiom-meaning-core.js`. ANTI-CHEAT (obvious-foil like Fern): kinds correct/literal(trap, echoes idiom)/off; oracle=correct; gate asserts literal-trap present + OVERLAP bot ≤ chance (literal=high-overlap → correct paraphrases → overlap 0%). Both ≥8 rounds + reshuffle, cards shuffle, text+SVG stubs, EN-only, 0 protected-core/lcs-shell; only tracked edit = 2 rows in `activities.ts MANIFEST_FILES`. Lean DoD green both (verify + local-test + preflight 113 + scoped visual-qa 48/48 + eyeball desktop/phone). LESSONS: collision-grep before codes; page_title LEADS with name (curl-confirmed index); childView-leak gate checks the PROPERTY not the substring. Cluster: L.1.5.d → L.2.5.b → L.3.5.a. → [Lean activity QA](feedback-lean-activity-qa.md).

## 🆕 BUILD 2026-06-28 — expanded "Mim's Memory Baskets" (L.K.5.a sort into category baskets) into the groups cluster with 2 NEW activities. EN-only pilots, batched NOT deployed. Collision-check first: L.5 word-relationship cluster fully built → operator pair = L.1.5.a + L.2.1.a (both still about "groups"). **#1 "Ziggy's Odd One Out" (L.1.5.a, char Ziggy zebra)** — 4 pictures, 3 of one category + 1 outsider, tap the one that DOESN'T belong (flip side of Mim's sort); NEW `odd-one-out-core.js`. ANTI-CHEAT (DERIVED by category): oracle=minority-category item; gate asserts clean 3-1 split + bots ≤ chance + image-existence. **#2 "Mango's Animal Groups" (L.2.1.a collective nouns, char Mango monkey)** — pictured animal + "a group of cows is a ___?", tap the group WORD (herd/flock/pack); NEW `collective-noun-core.js`. ANTI-CHEAT (DERIVED noun match): oracle=choice word===correct; subjects with unambiguous single collective noun (cows→herd, fish→school, wolves→pack, bees→swarm, birds/sheep→flock, lions→pride, grapes→bunch); bots ≤ chance + image-existence (subject only). Both ≥8 rounds + reshuffle, cards shuffle, image-library pictures + TTS, EN-only, 0 protected-core/lcs-shell; only tracked edit = 2 rows in `activities.ts MANIFEST_FILES`. Lean DoD green both (verify+image-existence + local-test[serves image-library-webp, naturalWidth>0] + preflight 115 + scoped visual-qa 48/48 + eyeball desktop/phone, pictures recognizable). LESSONS: collision-grep before codes; page_title LEADS with name (curl-confirmed index); isNoise includes "Failed to load resource"/"inventory.json". Cluster: L.K.5.a → L.1.5.a → L.2.1.a. → [Lean activity QA](feedback-lean-activity-qa.md).

## 🆕 BUILD 2026-06-28 — expanded "Marigold's Knowing Machine" (L.2.4.b prefixes/suffixes) into the word-parts/transform cluster with 2 NEW activities. EN-only pilots, batched NOT deployed. Collision-check first: affix/word-meaning cluster built (Marigold/Skip/Fern/Booker) → operator pair = L.2.4.c + L.2.1.d. Both text-based/self-contained (like Marigold). **#1 "Sage's Root Garden" (L.2.4.c root words, char Sage tortoise)** — a root (farm) + 3 words, tap the family member (farmer); foils similar-spelled diff-root (former/forest); NEW `root-word-core.js`. ANTI-CHEAT (DERIVED): oracle=choice===correct; gate asserts correct.startsWith(root) + bots ≤ chance (length-balanced). **#2 "Rusty's Yesterday Machine" (L.2.1.d irregular past, char Rusty robot)** — "Today I go. Yesterday I ___?", tap went; foils = base verb (go) + over-regularized (goed), the classic errors; NEW `irregular-past-core.js`. ANTI-CHEAT (DERIVED): oracle=choice===correct; gate asserts correct≠present + bots ≤ chance. KEY: short irregular pasts → the base-verb foil (= present length) balances length so the past isn't systematically shortest (length bots 0%). Both ≥8 rounds + reshuffle, cards shuffle, text+SVG stubs, EN-only, 0 protected-core/lcs-shell; only tracked edit = 2 rows in `activities.ts MANIFEST_FILES`. Lean DoD green both (verify + local-test + preflight 117 + scoped visual-qa 48/48 + eyeball desktop/phone). LESSONS: collision-grep before codes (caught Clock Tower verb-tense adjacency → AskUserQuestion); page_title LEADS with name (curl-confirmed index); base-verb-foil length-balancing for the irregular-past confound. Cluster: L.2.4.b → L.2.4.c → L.2.1.d. → [Lean activity QA](feedback-lean-activity-qa.md).

## 🆕 BUILD 2026-06-28 — expanded "Hopper's Lily Hops" (2.NBT.A.2 skip-counting) into the number-system arc with 2 NEW activities. EN-only pilots, batched NOT deployed. Collision-check first: add/sub-by-hopping OWNED (2.MD.B.6 "Hopper's Number Line") + counting-on OWNED (K.CC.A.2) → AskUserQuestion picked K.CC.A.1 + 2.NBT.B.5. #1 Posy the hen "Posy's Egg Cartons" (K.CC.A.1, count by tens) = `tens` cartons-of-ten + 3 number cards, tap the total; NEW `count-tens-core.js`, DERIVED (answer=10×tens; foils=tens by-ones-error + 10×tens+10; correct is middle value so length bots miss; correct at canonical idx tens%3 over a balanced tens set keeps positionBot 0.375 — the canonical-sorted-order=always-middle=100%-position trap). #2 Gus the gopher "Gus's Snack Cart" (2.NBT.B.5, add/sub within 100) = restock/sell story + big number sentence + shell KEYPAD, type the answer (? reveals on correct); NEW `add-sub-100-core.js`, DERIVED (a±b; keypad bots fixed-guess/echo-a/echo-b ≤ chance via b≥1 + no a===2b + distinct answers; op-MIX 4+/4−). Both 0 protected-core/lcs-shell, 8 rounds + reshuffle. KEYPAD-FOLD lesson: keypad+stage overflowed 320×640 & 1024×900 → hid hen row at ≤920px height + tightened equation (never moved the threshold); visual-qa 48/48 each. Footprint = 8 new mini-tools files + 4 scripts + 1 activities.ts line. The arc: 2.NBT.A.2 skip-count (Hopper) → K.CC.A.1 count-by-tens (Posy) → 2.NBT.B.5 add/sub-within-100 (Gus). Preview: /posy-egg-cartons-activity.html?embed=1&activity=posy-egg-cartons.count-tens.k-cc-a-1 + /gus-snack-cart-activity.html?embed=1&activity=gus-snack-cart.within-100.2-nbt-b-5

## 🆕 BUILD 2026-06-28 — expanded "Fable's Picture Stories" (RL.K.1 story key details) into the understanding-books cluster with 2 NEW activities. EN-only pilots, batched NOT deployed. Collision-check first: RL densely owned (RL.K.1/2/3/7/9 + RL.1.2/3/6/9) → AskUserQuestion picked RL.K.6 + RL.1.5 (avoided the near-reskin RL.1.1). #1 Inky the octopus "Inky's Book Workshop" (RL.K.6 author/illustrator) = book-page job art + 3 role cards Author/Illustrator/Reader, tap whose job; NEW `author-illustrator-core.js`, DERIVED (role mapped from job wrote/drew/read). BINARY-FLOOR lesson: a 2-role concept = 50% floor → added a REAL third option (Reader, 2 of 8) for a true 3-way (chance 0.33, fixedGuess 0.375) while keeping author/illustrator the majority (6/8) so it still instantiates the standard. #2 Bea the bookworm "Bea's Two Bookshelves" (RL.1.5 story vs fact book) = 3 book covers (title+blurb, NO type shown), ask-flip "STORY?"/"FACTS?", tap the kind; NEW `story-fact-core.js`, DERIVED type-match, childView hides `type`, ask-balanced 4/4. LENGTH-BALANCE lesson: titles all ~17-19 chars → longest/shortest bots 0% (a story-titles-longer draft hit longestBot 0.625; never move the threshold — balance the content). CARD-FOLD: tall book cards overflowed 320×640 → shrank ≤640 card min-height+fonts. Both 0 protected-core/lcs-shell, ≥8 rounds + reshuffle, visual-qa 48/48 each. Footprint = 8 new mini-tools files + 4 scripts + 1 activities.ts line. The cluster: RL.K.1 key details (Fable) → RL.K.6 author/illustrator (Inky) → RL.1.5 story-vs-fact (Bea). Preview: /inky-book-workshop-activity.html?embed=1&activity=inky-book-workshop.author-illustrator.rl-k-6 + /bea-two-bookshelves-activity.html?embed=1&activity=bea-two-bookshelves.story-or-fact.rl-1-5

## 🆕 BUILD 2026-06-28 — expanded "Dr. Plume's Sentence Clinic" (L.2.1 fix-the-sentence) into the conventions cluster with 2 NEW activities. EN-only pilots, batched NOT deployed. Collision-check first: L.2.1.* sub-codes owned (a-e) + L.2.2.c contractions (Nib) → AskUserQuestion picked L.2.2.a + L.2.2.b. #1 Wally the walrus "Wally's Capital Crane" (L.2.2.a capitalize holidays/places/products) = sentence word-chips, tap the lowercase special name → it "lifts" + shows its Capital; NEW `capital-name-core.js`, DERIVED oracle=tokens.indexOf(proper). Chips in SENTENCE ORDER (not shuffled) → anti-cheat on target POSITION varying (balanced ≤3/idx) + token-length balance (pair long proper nouns halloween/christmas with a comparably-long common word so the special name isn't the unique longest). #2 Pim the pigeon "Pim's Comma Mail" (L.2.2.b commas in greetings/closings) = 3 forms (Dear Sam, / Dear Sam / Dear, Sam), tap the right comma; NEW `letter-comma-core.js`, DERIVED ok-flag, childView hides `ok`, kind-mix 4/4. LENGTH-MATCH lesson: the misplaced-comma foil matches the correct's char length (both have a comma) → correct never unique-longest → longest/shortest bots 0%; single-word closings (Love,) use a period-foil (Love.) for the length tie. Both 0 protected-core/lcs-shell, ≥8 rounds + reshuffle, visual-qa 48/48 first pass (no fold rescue). Footprint = 8 new mini-tools files + 4 scripts + 1 activities.ts line. The cluster: L.2.1 fix-it (Dr. Plume) → L.2.2.a capitalize-special-names (Wally) → L.2.2.b letter-commas (Pim). Preview: /wally-capital-crane-activity.html?embed=1&activity=wally-capital-crane.special-names.l-2-2-a + /pim-comma-mail-activity.html?embed=1&activity=pim-comma-mail.letter-commas.l-2-2-b

## 🆕 BUILD 2026-06-28 — expanded "Dot's Story Spine" (RL.K.3 story parts) into the informational-text (RI) cluster with 2 NEW activities. EN-only pilots, batched NOT deployed. Collision-check first: RL densely owned (RL.K.1/2/3/6/7/9 + RL.1.2/3/5/6/9) → AskUserQuestion picked RI.K.1 + RI.K.3, OPENING the under-built RI strand (only Nila RI.K.2 existed). #1 Atlas the armadillo "Atlas's Fact Files" (RI.K.1 key details in a fact text) = 2-sentence real-fact blurb + question + 3 answer cards; NEW `fact-detail-core.js`, DERIVED oracle=options.indexOf(answer). NEW INVARIANT: the gate asserts the answer is a SUBSTRING of the fact text → real "find it in the text" comprehension, not world-knowledge trivia (instantiates RI.K.1). #2 Linc the lizard "Linc's Fact Chain" (RI.K.3 how two facts connect) = stem fact + sequence ("…NEXT?") or cause ("…So?") + 3 outcome cards; NEW `fact-connect-core.js`, DERIVED, mode-mix 4 seq/4 cause asserted (child must read, not pattern-match one connector); near-miss foils ("snow melts" vs "snow falls"). Both 0 protected-core/lcs-shell, ≥8 rounds + reshuffle, length-balanced options → longest/shortest bots 0%, visual-qa 48/48 first pass (no fold rescue). Per-round question renders on the stage (generic shell prompt). Footprint = 8 new mini-tools files + 4 scripts + 1 activities.ts line. The cluster: RL.K.3 story parts (Dot) → RI.K.1 fact key-details (Atlas) → RI.K.3 fact connections (Linc). Opens RI for future expansion (RI.K.4/6/7, RI.1.*). Preview: /atlas-fact-files-activity.html?embed=1&activity=atlas-fact-files.key-detail.ri-k-1 + /linc-fact-chain-activity.html?embed=1&activity=linc-fact-chain.connect.ri-k-3

## 🆕 BUILD 2026-06-28 — expanded "Describe Measurable Attributes" (K.MD.A.1) into the measurement-reasoning ladder with 2 NEW activities. EN-only pilots, batched NOT deployed. Collision-check first: Measurement & Data densely owned (nearly every K-3 MD code) → AskUserQuestion picked 2.MD.A.3 + 2.MD.A.4. #1 Gauge the goat "Gauge's Good Guess" (2.MD.A.3 estimate lengths) = object + "About how long is a [thing]?" + 3 cm/m estimate cards; NEW `estimate-length-core.js`, DERIVED oracle=options.indexOf(answer). NEW ANTI-CHEAT CLASS (estimation/magnitude): distribute the correct option's MAGNITUDE across the bank (smallest 3/middle 3/largest 2) + measure smallest/middle/largest bots (all ≤0.375) → kills the "always pick the middle number" exploit (fixed-magnitude correct teaches number-picking not scale-sense, §20.8). #2 Span the inchworm "Span's Length Gap" (2.MD.A.4 how much longer) = two proportional drawn bars (ribbon 8cm/string 5cm) + KEYPAD, type the gap (reveals on correct); NEW `length-gap-core.js`, DERIVED answer=aLen−bLen, childView shows lengths-as-data not answer, keypad bots fixed/echo-a/echo-b ≤ chance (no diff===operand). Both 0 protected-core/lcs-shell, ≥8 rounds + reshuffle, visual-qa 48/48 first pass (Span pre-emptively hides the intro row at ≤920px for the keypad fold). Footprint = 8 new mini-tools files + 4 scripts + 1 activities.ts line. The ladder: K.MD.A.1 describe-attribute → 2.MD.A.3 estimate (Gauge) → 2.MD.A.4 how-much-longer (Span). Preview: /gauge-good-guess-activity.html?embed=1&activity=gauge-good-guess.estimate.2-md-a-3 + /span-length-gap-activity.html?embed=1&activity=span-length-gap.how-much-longer.2-md-a-4

## 🆕 BUILD 2026-06-28 — expanded "The Doubling Pond" (L.2.1.b irregular plural nouns) into the noun-grammar cluster with 2 NEW activities. EN-only pilots, batched NOT deployed. Collision-check first: L.2.1.* sub-codes (a/c/d/e) + L.2.2.a/b/c owned → AskUserQuestion picked L.1.1.c + L.1.1.b. #1 Vera the vole "Vera's Verb Match" (L.1.1.c subject-verb agreement) = sentence-with-blank + 3 verb cards, tap the agreeing one; NEW `be-agreement-core.js`. KEY RULING: a two-form agreement (base vs base+s) is an inherent BINARY (50% floor) AND leaks "correct is always the lone odd-number card" → recast as be-verbs AM/IS/ARE, a genuine 3-way (correct distributed 2/3/3) so no single-form bot >0.375 and no odd-one-out; form-mix asserted, am&is both len-2 (no unique shortest). #2 Hattie the hedgehog "Hattie's Whose-Is-It" (L.1.1.b possessive nouns) = "belongs to the dog → whose?" + dog's/dogs/dogs', tap singular possessive; NEW `possessive-noun-core.js`, DERIVED from `noun` (answer=noun+"'s", foils plain-plural + plural-possessive); the plural-possessive foil (dogs') length-matches the answer (dog's) → longest/shortest 0%; per-round `slot` balances canonical position (the Posy canonical-order trap). Both 0 protected-core/lcs-shell, ≥8 rounds + reshuffle, visual-qa 48/48 first pass. NEW GENERAL LESSON: when a grammar concept is fundamentally two-form (binary 50% floor + odd-one-out leak), recast it as a genuine 3-way with three real correct categories — cleaner than gating around the binary (the Inky author/illustrator/reader pattern, applied to agreement via am/is/are). Footprint = 8 new mini-tools files + 4 scripts + 1 activities.ts line. The cluster: L.2.1.b irregular plurals (Doubling Pond) → L.1.1.c agreement (Vera) → L.1.1.b possessives (Hattie). Preview: /vera-verb-match-activity.html?embed=1&activity=vera-verb-match.be-agreement.l-1-1-c + /hattie-whose-is-it-activity.html?embed=1&activity=hattie-whose-is-it.possessive.l-1-1-b

## 🆕 BUILD 2026-06-28 — expanded "The Clock Tower" (L.1.1.e verb tense) into the time/conventions cluster with 2 NEW activities. EN-only pilots, batched NOT deployed. Collision-check first: verb codes all owned (tense/agreement-Vera/irregular-past-Rusty/adj-adv-Glim), L strand dense → AskUserQuestion picked L.1.1.i + L.1.2.b. #1 Tempo the turtle "Tempo's Day Plan" (L.1.1.i prepositions, built as TIME prepositions) = sentence-with-blank + 3 constant cards before/during/after, tap the fit; NEW `time-preposition-core.js`, DERIVED genuine 3-way (correct distributed 3/3/2, no single-form bot >0.375, form-mix asserted, before/during len-6 no-unique-longest). RULING: built L.1.1.i as TIME prepositions (before/during/after+noun) → ties Clock Tower's time theme + dodges Bramble spatial position-words (K.G.A.1); "during" is a CCSS L.1.1.i example; noun-object asserted (preposition not clause). #2 Cleo the chameleon "Cleo's Packing List" (L.1.2.b commas in a series) = 3 list sentences (a,b,and c / no-comma / misplaced "a, b and, c"), tap correct; NEW `series-comma-core.js`, DERIVED from items, misplaced-comma foil length-matched to correct (both 2 commas) → longest/shortest 0%, per-round slot balances canonical position. Distinct comma facet from Pim's letter commas. Both 0 protected-core/lcs-shell, ≥8 rounds + reshuffle, visual-qa 48/48 first pass. Reused patterns: be-agreement constant-3-card 3-way (Tempo), Pim length-matched-foil + Posy slot-balance (Cleo). Footprint = 8 new mini-tools files + 4 scripts + 1 activities.ts line. The cluster: L.1.1.e tense (Clock Tower) → L.1.1.i time prepositions (Tempo) → L.1.2.b series commas (Cleo). Preview: /tempo-day-plan-activity.html?embed=1&activity=tempo-day-plan.time-prepositions.l-1-1-i + /cleo-packing-list-activity.html?embed=1&activity=cleo-packing-list.series-commas.l-1-2-b

## 🆕 BUILD 2026-06-28 — FIX Snippy's letter glyphs + NEW full-alphabet trace (Penny). Batched NOT deployed. Operator: Snippy's letters are ugly — fix like the Mama's-Roll-Call NUMBER fix; + build one activity tracing the COMPLETE alphabet (caps+lowercase). ROOT CAUSE: snippy-activity.js rendered the guide as STRAIGHT `L` polyline over 6-7 sparse pts → jagged; the number fix used DENSE on-curve pts + a Catmull-Rom→cubic-Bézier `splinePath` (smooth). PART 1: redrew snippy-core.js's 12 glyphs as dense on-curve pts KEEPING each stroke start/order/count (so verify-snippy-core's homer/dot-follower anti-cheat skew is unchanged — stayed GREEN: oracle 100%, homer 40% defeated-on-60%), switched snippy-activity.js guide render to splinePath; bumped ?v. PART 2 "Penny's Alphabet Trace" (L.K.1.a, Penny pencil, operator-directed 2nd L.K.1.a): NEW alphabet-trace-core.js with all 52 glyphs built from TRUE ELLIPSE ARCS (`arc`) + sampled segments (`line`) → guaranteed-smooth curves, rendered via spline; strict-order on-path trace + SOLVER gauntlet (oracle 52/52, scribble+out-of-order fail). penny-alphabet-trace-activity always-guided spline-trace w/ baseline+midline writing guides + CAPITAL/lowercase badge + next-stroke start dot; 52-round manifest (A-Z band1, a-z band2). KEY TECHNIQUE: parametric arc/line glyph composition (NOT hand-eyeballed pts) = professional consistent letters; reviewed all 52 via a contact-sheet probe (removed). Verified verify-core 52/52 + local-test + visual-qa (snippy 60/60, penny 240/240) + preflight 132 + findability + eyeball 360/768/1024. 0 protected-core/lcs-shell. NOTE Snippy files were already untracked (prior batched session) so edits fold in. Preview: /penny-alphabet-trace-activity.html?embed=1&activity=penny-alphabet-trace.letter-formation.l-k-1-a + /snippy-activity.html?embed=1&activity=snippy.letter-formation.l-k-1-a

## 🆕 BUILD 2026-06-28 — NEW Digby's Number Trace (full 0–9 numeral tracing). Batched NOT deployed. Operator: analyze Mama's Roll Call, build one activity tracing numbers 0-9 (digit parallel of Penny's Alphabet Trace). Digby's Number Trace (K.CC.A.3, Digby the dog) = always-guided spline-trace of 0–9; NEW `number-trace-core.js` = the proven Penny trace engine (strict-order on-path attemptStroke/traceScore + oracle/scribble/out-of-order SOLVER gate) with GLYPHS = the 10 digit glyphs COPIED VERBATIM from the operator-approved numeral-trace-core.js (REUSE, no re-authoring → quality guaranteed; copied not imported for verify isolation; numeral-trace-core.js untouched). digby-number-trace-activity clones penny's (no case badge; dog SVG; "Trace N"). 10-round manifest. 2nd K.CC.A.3 is operator-directed (Rivet/Pip own it). Verified verify-core 10/10 + local-test + visual-qa 60/60 + preflight 133 + findability + eyeball all 10 digits (contact sheet, removed) + 360/768. 0 protected-core/lcs-shell. The trace family: letters (Penny) + numbers (Digby), both dense-on-curve+Catmull-Rom-spline (the Mama numeral-fix approach). Preview: /digby-number-trace-activity.html?embed=1&activity=digby-number-trace.numeral-formation.k-cc-a-3
