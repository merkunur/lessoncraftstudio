---
name: project-longtail-seo-topic-overrides
description: "The 2026-07-06 exhaustive per-language long-tail SEO program (/goal 1000 clicks/day): demand-keyed topic-page title/H1/meta overrides + intersection flips + deck-title tails, 11-locale autocomplete-harvested. Substrate + en/de/es/fr/pt LIVE; nl/sv/da/no/fi/it in progress."
metadata:
  node_type: memory
  type: project
  originSessionId: 9cff80f8-82e8-41c2-ac5a-b6a88c8ed77f
---

Operator /goal 2026-07-06 (Stop-hook): "thin pages remain; titles/subtitles/descriptions/meta/PDF descriptions not improved from thorough per-language long-tail keyword research; hit 1000 daily clicks; no shortcuts, no generic templates." This is the successor push to [[project-seo-real-cause-and-hub-program]] (hubs) — it targets the surfaces the hub program didn't: single-axis topic pages, intersections, landing-less deck titles. Plan file `C:\Users\rkgen\.claude\plans\luminous-wibbling-bonbon.md`.

**THE GAP (exploration-verified):** single-axis topic pages (`/[locale]/topic/<slug>`) had **100% generic-template titles+H1s** (`"{topic} worksheets"` via renderTopicTitle) + template meta beyond the ~156/locale authored `topicMeta`; ~17,140 of 17,670 intersections **noindex** (the dominant K-3 "type+grade" query class had NO indexable page); landing-less deck titles **bare** (no free/print/PDF qualifiers). Landing titles were ALREADY demand-keyed all 11 locales (rekey-*-titles.js) EXCEPT fi (lean) + sv (240 pattern-train un-run). Demand maps existed only for de/es/fr/nl (subject×grade only).

**ARCHITECTURE (shipped, code commit `443f4f7d`):** new `frontend/lib/seo/topic-overrides.ts` — fs-loaded (3-dir incl. `/opt/...` absolute fallback for the release-model trap), fails-closed, per-locale content JSON `frontend/content/topic-seo-overrides/<locale>.json` `{singleAxis:{<axisKey>:{title,h1,metaDescription,prose}}, intersection:{<a__b sorted>:{...}}}`. NOT messages (layout.tsx serializes whole locale msgs into every page's RSC flight payload → bloat). Hooks: `topic/[slug]/page.tsx` (title ~246 / getTopicMeta ~215 / getTopicProse ~195 / H1 ~712, precedence seasonal>override>template); `[secondary]/page.tsx` (resolvers + title ~530 + H1); `intersection-authored.ts` one-liner `intersectionOverrideAuthored()` → flips indexing+sitemap-shard-2+hreflang (SoT); `TopicProseContainer.tsx`. Validator `scripts/seo/validate-topic-overrides.js` (prose≥200w for flips, meta 120-170 rendered, unique titles/metas, seasonal-collision guard). Converter `scripts/seo/build-topic-overrides.js` (demand-terms-<loc>.json → override file). **Author into the content JSON, run validator, deploy, pm2 restart (module cache is process-life).** SEASONAL KEYS (christmas/easter/winter/spring/summer + en 4th_of_july/thanksgivinng) must NOT be in singleAxis (seasonal-hub owns them) — the converter merges them, strip after.

**RESEARCH ENGINE:** `scripts/seo-research/harvest-suggest.js` — Google suggest `hl/gl` per market, taxonomy-seeded, → `docs/SEO/harvests/<locale>.json`. All 11 harvested (google rate-limits ~650 req; re-harvest at delay=600 after ~1h wait). Per locale: native 3-agent-equivalent general-purpose agent reads harvest + SERP-samples winnability → `docs/SEO/demand-map-<locale>.md` + `docs/SEO/demand-terms-<locale>.json` (singleAxis + intersections w/ verbatim primaryQuery). Deck inventory: `docs/SEO/deck-inventory-{type-level,themes}.json` (prod DB groupBy). **Winnability prior: EN head + de/pt/it/es print-head top-3 UNWINNABLE; winnable = LCS-distinctive mechanics (sudoku/cryptogram/odd-one-out/shadow-match/find-objects), online/interactive, skill+seasonal long-tail, type×grade intersections.**

**SHIPPED LIVE (verified curl):** single-axis overrides **en 94 / de 92 / es 84 / fr 83 / pt 77** (~430 topic pages, demand titles like de "Suchsel – Wortgitter kostenlos ausdrucken", fr "Suites logiques maternelle", es "Laberintos de imágenes"); **de 26 intersection flips** (200-260w native prose, e.g. `/de/topic/1-klasse/uhrzeit` now index,follow + in sitemap-2); **9,748 landing-less deck titles** get per-locale demand tails via `scripts/publish-cli/append-demand-title-tail.js` (canonical-URL discriminator: `/decks/<slug>/`=eligible, `/worksheets/`=skip; titleHash-gated; idempotent; `demand-title-tails.json`; budget 80c; re-run after publish waves); **sv 240 pattern-train landings** rekeyed; **fi 2714 landing titles** enriched with "ilmainen/ilmaisia" free-signal (harvest+native confirmed, [NSR][fi] cleared); **Nordic PDF /Info** native-reviewed (fi plural/case/compound, da/no closed compounds, sv word-order) → 15,699 PDFs regenerated via `regenerate-pdf-metadata.js --locales=sv,da,no,fi`.

**COMPLETE + LIVE (all 11 locales deployed + verified):** singleAxis en94/de92/es84/fr83/pt77/it89/nl87/sv90/da87/no82/fi90 = **955 demand-keyed topic pages** + intersections en38/de26/es39/fr28/pt27/it45/nl33/sv41/da40/no40/fi44 = **401 type×grade flips** (noindex→indexed, in sitemap shard 2, hreflang-meshed). Verified live e.g. de "Suchsel – Wortgitter kostenlos ausdrucken", no "Hvem skal ut? Logikkoppgaver med bilder", fi `/fi/topic/esiopetus/kuvasudoku` index,follow. Fail-closed confirmed (non-overridden pages byte-unchanged: `/de/topic/4-juli` still template). All 11 demand maps + terms committed.

**PHASE 6 (activity prose) = 0 TRUE GAP — a non-issue.** An activity is live in a locale iff `manifest.slug[locale]` exists (`frontend/lib/activities.ts:238/334`). Computed: for EVERY locale, live-activity count === prose-coverage count (en 195/195, de 175/175, es/it/pt/fr/no/sv 57/57, nl/da 58/58, fi 55/55) → **0 live activity pages lack prose.** The naive "139 missing vs en" was entirely EN-only English-literacy activities (cvc/magic-e/syllable/wordclass) that have NO non-English page. The §20 activity fan-out authored prose as activities were localized, so coverage == liveness by construction. Nothing to author. Nordic PDF /Info regen (15,699) also done. **PHASE 6 CLOSED.**

**INDEXNOW FIXED 2026-07-06 (root cause was NOT "Bing key revalidation lag" — that prior-session theory was WRONG).** The real cause: the hosted key `f261bd8eb7ea657cdb8051d5d8e3bc4c` was **self-generated and never issued/recognized by Bing** → every submission 403'd with `UserForbiddedToAccessSite` / "key not valid" even though the .txt served 200. The site IS verified in Bing Webmaster Tools (real data visible: 782 clicks / 39.6K impressions over ~10wk — Bing, not Google). Fix (commit after fi map): swapped to the **Bing-issued key `f1bea8a4e58d43eda62b09b891f305a4`** in 3 git-tracked spots — new key file `frontend/public/f1bea8a4….txt`, `scripts/indexnow-submit.js` KEY default, and the `frontend/middleware.ts` matcher carve-out (added the new .txt alongside the old). Deployed → new key file serves 200 → **live submission returns HTTP 200** (verified: `indexnow-submit.js` 22 URLs → 200). Forward: `INDEXNOW_KEY` env overrides the default. **Bing Webmaster Tools IS accessible** — its Search Performance has real query data + recommendations ("meta descriptions too short", "URLs noindex'd by robots") = a usable measurement surface going forward, and it's where the key came from (left sidebar → IndexNow → Generate API Key).

**REMAINING:** (1) commit nl/sv/da/no/fi intersection prose when the 5 agents finish → FINAL deploy.sh (gets da/no/fi single-axis + nl/sv/da/no/fi intersections live) → verify curls + sitemap-2. (2) **Phase 6 activity prose — RE-SCOPED, NOT a blind fill.** The naive gap (de missing 21, other 9 locales missing ~139 vs en's 195 prose keys) is MISLEADING: the "missing" set is dominated by EN-only English-literacy activities (cvc-builder/magic-e/syllable-builder/word-class/compound-words/RF.K phonics/storybook) that legitimately have NO non-English page. Real Phase 6 = per-locale: cross-ref the LIVE activity manifest (which activities render for which locale — many are locale-scoped EN-only pilots per §20.2) against prose coverage, author prose ONLY for live+language-appropriate activities. Needs liveness scoping first. (3) IndexNow still 403 (Bing key lag). **Deploy = deploy.sh (build+pm2) for topic overrides; deck-title tails + PDF regen + landing renders are Hetzner node scripts (no build).**

**OPERATOR FORK surfaced (de research):** all de addition/subtraction decks tagged Kindergarten(5-7) but German demand is "…klasse 1" — a level-retag or Klasse-1 wave unlocks the 2 largest blocked de intersections. Also deck-production gaps w/ proven demand: tracing/grafomotricidad/graphisme/pregrafismo/schwungübungen (3+ locales), zahlenstrahl, anlaute, tabla-del-100.
