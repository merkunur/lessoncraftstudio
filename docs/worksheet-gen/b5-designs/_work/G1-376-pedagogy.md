# G1-376 `plants` : PEDAGOGY + CONTENT (nt10-E, 2026-09-23)

Read: `_ROLE-PEDAGOGY.md`, `_STUDIO-BRIEF.md`, `_SUBSTRATE.md`, `_PANEL-FINDINGS.md` (lock row 4 + the `plants` ruling), the `plants` sections of the four `_work/_selection-*.md`, `../b4-designs/K-354-human-body.md` + `_work/K-354-pedagogy.md` (the label-a-drawn-figure precedent), `types/g1/G1-203-chicken-life-cycle.js` + `types/_shared/science-sequence.js`, `types/_shared/science-pair-match.js`, `data/science/{fruits-vs-vegetables,living-vs-nonliving,natural-vs-manmade,chicken-life-cycle}.json`, `templates/components-b4/human-body.js` (export list), `primitives/body-figure.js` (header), `frontend/lib/seo/strand-names.ts` (`Science` row), `frontend/config/topics-taxonomy.json`, `scripts/seo-landing/gen-b3-landings.js LEVEL_KEYS`. (m) = measured 2026-09-23 by read-only node (`lib/b2-common.js vocab()/entriesFor`, `cache/manifest.json`) or by OPENING the picture (contact sheets `scratchpad/G1-376-{veg1,veg2,spring,flow,non}.png`, every named picture read with the Read tool). *est.* = the engineer measures in the real render (`render/one.js`, shell woff2 from `file://`). No em-dashes.

**Boundary (load-bearing).** K-202 `fruits-vegetables` sorts FOOD CLASS (its bank bins `tomato`/`cucumber`/`corn`/`potato`/`onion` as *vegetables*, m); K-201 living/non-living and K-214 natural/man-made each carry `spring/flower` as one item (m); K-322 seasons + K-338..341 own spring and "draw the tree"; G1-203 `science-sequence` owns ordering LIBRARY pictures in a LINE ("Write 1, 2, 3", chicken, m); G1-377 `animal-life-cycles` (this batch) owns metamorphosis (butterfly, frog); K-354 owns copying words onto leader lanes of the BODY figure; K-211 hot/cold carries `weather/sun`. Therefore `plants` owns exactly four things nobody has: (1) the drawn PLANT (NEW `primitives/plant-figure.js`: roots under a soil line, stem, leaves, one flower, one opened pod showing its seeds, plus five growth-stage variants and a separate cut-open FLOWER view), (2) the PLANT-PART vocabulary as a closed, panel-authored literal set (root/stem/leaf/flower/fruit/seed; flower organs petal/sepal/stamen/pistil), (3) what a plant NEEDS judged from given conditions, (4) the seed-to-plant CYCLE closed back to the seed. No face ever asks fruit-or-vegetable (the eat face asks WHICH PART, and no chip says "fruit or vegetable"); no face orders library pictures in a line; no face uses an animal; no face is titled by bare "Life Cycle(s)", bare "Flowers" or bare "Trees" (theme slugs `flowers` `trees` `vegetables` `spring` `fruits`, m).

## A. Identity

| loc | genre head (panels; never the bare theme word) | school year (base G1) | national strand (framework NAME; `Science` row of `strand-names.ts` exists, m) | CCSS (en only, honest) |
|---|---|---|---|---|
| en | **Parts of a Plant** (Tier A, net 131 of 133, head HARD; WINNABLE "parts of a plant worksheet kindergarten pdf", "… and their functions worksheet grade 2", "parts of a flower worksheet") | grade 1 | Science | none (science). NGSS: base 1-LS1-1 (external parts); needs face K-LS1-1; functions face 1-LS1-1 / 2-LS2-1; cycle face readiness for 3-LS1-1 (G3); flower face readiness for 4-LS1-1 (G4, landing says so) |
| de | **Teile der Pflanze** (Tier B, net 37; "teile der pflanze grundschule", "welche teile der pflanze essen wir"; re-probe "aufbau einer pflanze") | 1. Klasse | Sachunterricht | n/a (Lehrplan Sachunterricht, Perspektive Natur) |
| es | **Partes de la planta** (Tier A, net ≈42; "partes de la planta para completar primer grado" = the base verbatim) | primer grado | Conocimiento del Medio | n/a (SEP/NEM, "Exploración de la naturaleza") |
| pt | **Partes da planta** (Tier A-, net ≈22; "partes da planta 1 ano / 2 ano") | 1º ano | Ciências | n/a; BNCC EF02CI06 verbatim (partes + função) on base, eat, functions; EF02CI05 (água e luz) on the needs face |
| fr | **Les parties de la plante** (Tier B, net ≈11, re-probe "légender une plante") | CP | Questionner le monde | n/a (programmes officiels cycle 2) |
| it | **Le parti della pianta** (Tier A-, net ≈30; "classe seconda schede / verifica") | classe prima | Scienze | n/a (Indicazioni nazionali) |
| nl | **Delen van een plant** (Tier C, net 8; "delen van een plant benoemen", "eetbare delen van een plant") | groep 3 | Oriëntatie op jezelf en de wereld | n/a (SLO kerndoel 40-41) |
| sv | **Växtens delar** (Tier B; "växtens delar arbetsblad", "blommans delar åk 1") | åk 1 | Naturorienterande ämnen | n/a (Lgr22 NO åk 1-3; never quoted in metadata) |
| da | **Plantens dele** (Tier C; "blomstens dele", "fra frø til plante") | 1. klasse | Natur/teknologi | n/a (Fælles Mål) |
| no | **Plantens deler** (Tier C; "fra frø til plante barnehage") | 2. trinn | Naturfag | n/a (LK20) |
| fi | **Kasvin osat** (Tier B-; "nimeä kasvin osat", "kukan osat", "kasvin osat ja niiden tehtävät") | 1. luokka | Ympäristöoppi | n/a (OPS 2014) |

Measured: `apps.plants` and `axes['exercise-type'].plants` ABSENT; no `axes.*` slug in any locale matches /plant|pflanz|planta|plante|pianta|växt|kasvi/ (m). Register `default_subject:'science'`, `default_age_range:'6-8'`.

**Theme axis: OFF** (`themeAxis:{applicable:false}`; landings `coordinate.theme:''`). The apparatus is drawn; the few library pictures (eat face, needs face) are FIXED (theme, noun) pairs listed in the bank with `picOpened:true`, not a fanned theme. **No `unitAxis`.**

**Vocabulary facts that force the design (m):** `root`, `stem`, `seed`, `sprout`, `soil`, `air` have NO vocab entry; `leaf` (en "Leaf", de "Blatt") and `flower` (de **"Blume"**, nl "Bloem", sv "Blomma") exist, but the plant-PART term differs from the everyday noun (de part = **Blüte**, never Blume). **The one rule that locks the type: every part word on every page is a panel literal from `plants.<loc>.json partWords` / `flowerWords`, never a vocab lookup, and every answer is a FACT of the drawn figure or a bank field (`eat[i].part`, `needs`, stage index), never a word the code forms.**

## B. The six faces

Common: `templates/components-b5/plants.js` + NEW `primitives/plant-figure.js` (`plantFigure({h, stage:'seed'|'germinating'|'seedling'|'young'|'flowering'|'fruiting'|'full', highlight:[partId], id}) -> {svg, width, height, anchors:{root,stem,leaf,flower,fruit,seed:{x,y}}, obstacles}`; one style for all stages: teal 3 px outline, cream fills, soil line + soil band `creamDeep`, roots teal 2 px; the `full` stage = the labelled plant: 3-5 branching roots under the soil line, one upright stem, 2 opposite leaves + 1 higher leaf, one open 5-petal flower at the top, one OPENED POD hanging at mid-stem with 3 visible seeds; `highlight` fills that part `coralSoft` with a coral outline) + NEW `flowerFigure({h})` (one open flower, cut-away: 5 petals, 3 sepals below, 5 stamens = filament + anther, central pistil = stigma + style + ovary, a stem stub with no root and no leaf) in the same file. Pedagogy rules for the art (the critic checks them on the render): roots are BELOW the soil line and nothing else is; the pod visibly CONTAINS seeds (the link fruit→seed→cycle); a leaf anchor sits inside a leaf, never on the vein; the flower is not a tulip clip-art (5 separable petals, a visible centre); leaders never cross (the K-354 `bodyLeaderSweep` pattern). Every face is a CODE face on one additive `layout` knob (base = `layout` undefined, byte-identical; faces stamp `data-lcs-layout`); guards key on the CONFIG (`d.layout`, `d.labels`, `d.mix`), never on the level index. Face ids `TBD by the emitter` in the band stated.

### Base: Parts of a Plant (G1, `G1-376`, layout undefined)
**Move:** LOCATE each part on the whole plant and WRITE its name on the lane whose leader points at it, choosing from a word bank (every word used once).
**Child:** "Which part is it? Write its name on the line. Use every word in the word bank once." (86)
**Params:** d1 `{labels:4, parts:['root','stem','leaf','flower'], perSide:2, figureH:560, laneW:210, laneH:64, glyphH:34, bank:true}` · **d2 (ships)** `{labels:5, parts:['root','stem','leaf','flower','fruit'], perSide:[3,2], figureH:560, laneW:200, laneH:64, glyphH:32, bank:true, bankPx:18}` · d3 `{labels:6, parts:+'seed', perSide:3, laneH:56, glyphH:28, bank:false}` (d3 = recall without bank; unpublished). Stack d2 *est.*: bank 59 + 10 + figure 560 = 629 ≤ 722.
**verify():** stamps `data-lcs-figure="plant-full"`, `data-lcs-anchors` (JSON px), `data-lcs-label=<part>` per lane, `data-lcs-bank=<part>` per bank word. Re-derived: bank set === lane set === `d.parts`; each leader end within 10 px of its anchor and inside the part's own shape; no two leaders intersect; lanes empty (no text, no `<text>`); the root leader end is BELOW the soil line y and every other end ABOVE it; bank order ≠ lane top-to-bottom order AND ≠ its reverse (position leak, both directions); bank words === `partWords[loc][part]`.
**Refusals:** none (6 literals authorable ×11). Figure is locale-neutral.
**Query face:** the bare head: en "parts of a plant" · de "Teile der Pflanze" · es "partes de la planta para completar" · fi "nimeä kasvin osat".

### F1: What Plants Need to Grow (K, K-3xx TBD, CODE `layout:'needs'`)
**Move:** JUDGE conditions: each row shows two identical seedlings in pots, each pot "given" two pictures; the child circles the ONE pot that gets both things a plant needs (light and water). Science knowledge is load-bearing: the non-needs are ordinary objects, never crossed-out icons, so "pick the one without an X" cannot solve it.
**Child:** "Look at what each plant gets. Circle the plant that has what it needs to grow." (79)
**Params:** d1 `{layout:'needs', rows:4, pots:2, mix:{noWater:2,noLight:2}, gift:64, potH:110}` · **d2** `{layout:'needs', rows:5, pots:2, mix:{noWater:2,noLight:2,neither:1}, gift:64, potH:110, rowGap:14}` · d3 `{layout:'needs', rows:5, pots:3, gift:56, potH:96}`. Needs pictures: `weather/sun` (light; smiling sun, opened) and `weather/raindrop` (water; opened); `space/sun` stays BLOCKED. Non-needs pool (all opened, vocab ×11 m): `toys/ball toys/blocks toys/car toys/dice toys/train toys/robot clothing/shoe clothing/glove music/drum music/guitar classroom/pencil classroom/book` (12). **Forbidden as non-needs** (each is arguably a need or a need-cue): kite (air/wind), hat, cap (shade), lamp (light), moon, watering can, any drink or food, `weather/cloud`/`rainy`/`hot`/`puddle`. Every pot shows soil (the pot) and air is everywhere: neither is ever the discriminator (K-LS1-1 hydroponics caveat). Row *est.*: [gift 64][pot 110][gift 64] ≈ 240 wide, 2 per row + gaps in 639; 5 rows × 120 + 4 × 14 = 656 ≤ 722.
**verify():** stamps per pot `data-lcs-gifts="sun,ball"` and `data-lcs-grows="1|0"`. Re-derived: `grows === gifts ⊇ {sun, raindrop}` from the bank's `NEEDS` set; exactly one `grows=1` per row; the row multiset of missing-needs === `d.mix`; correct-pot position counts differ by ≤ 1 between left and right AND the left/right sequence is not strictly alternating nor constant (the nt10-D staircase trap, measured on the SHIPPED seed); all seedling drawings byte-identical (no health cue); no picture drawn wilted or crossed.
**Refusals:** none (no words on the body).
**Query face:** "what plants need to grow" · de "Was Pflanzen zum Wachsen brauchen" · pt "do que as plantas precisam educação infantil" · sv "vad behöver växten".

### F2: From Seed to Plant (G1, G1-3xx TBD, CODE `layout:'cycle'`)
**Move:** PLACE growth stages round a CYCLE: a wheel of 5 boxes joined by arrows, the seed printed in box 1; four stage drawings (A-D) in a shuffled bank below; the child writes each letter in the box where that stage comes, and the last arrow returns from the pod (whose seeds are drawn) to the seed. Distinct from G1-203 (a line of 3 library pictures numbered 1-3) in apparatus (wheel, return arrow), content (plant), and answer form (letter into a slot).
**Child:** "Start at the seed. Follow the arrows and write the letter of each picture in the next empty box." (97)
**Params:** d1 `{layout:'cycle', stages:['seed','germinating','seedling','flowering'], given:1, wheel:true}` · **d2** `{layout:'cycle', stages:['seed','germinating','seedling','flowering','fruiting'], given:1, wheel:true, slot:[120,130], bankCard:[130,140]}` · d3 `{layout:'cycle', stages:6 incl. 'young', given:0}`. Stack *est.*: wheel ≈ 550 + 16 + bank 140 = 706 ≤ 722 (engineer measures; if over, wheel slots 110).
**verify():** stamps `data-lcs-slot=<k> data-lcs-stage=<id>` per wheel box, `data-lcs-letter=<A..D> data-lcs-stage=<id>` per bank card; re-derived: slot k holds stage `d.stages[k-1]`; the expected letter per slot is the bank letter of that stage; bank order is a derangement of slot order and not its reverse; every stage drawing is `plantFigure({stage})` (`data-lcs-figure="plant-<stage>"`); the return arrow exists (`data-lcs-arrow="return"`); slots 2..n empty.
**Refusals:** none. Fence: titles never contain the bare "life cycle(s)" word alone (science-sequence + G1-377); where the locale's head IS "ciclo de vida de las plantas / das plantas / d'une plante / ciclo vitale della pianta", the plant is always in the title.
**Query face:** en "plant life cycle" · de "Von der Bohne zur Pflanze" · es "ciclo de vida de las plantas primer grado" · it "ciclo vitale della pianta classe prima" · nl/sv/da/no "van zaadje tot plant / från frö till växt / fra frø til plante" · fi "siemenestä kasviksi".

### F3: Parts of a Plant We Eat (G2, G2-3xx TBD, CODE `layout:'eat'`)
**Move:** CLASSIFY a food by the PLANT PART it is: 8 picture cards, each with three part chips; circle the part we eat. The chips are part words, never "fruit/vegetable" (K-202 owns food class).
**Child:** "Which part of the plant do we eat? Circle the right word under each food." (72)
**Params:** d1 `{layout:'eat', cards:6, cols:2, mix:{root:2,leaf:2,flower:1,fruit:1}, chips:3, pic:100}` · **d2** `{layout:'eat', cards:8, cols:2, rows:4, mix:{root:2,leaf:2,flower:1,stem:1,seed:1,fruit:1}, chips:3, pic:96, chipH:40, chipPx:17}` · d3 `{cards:10, mix:+fruit 1 +seed 1}`. Card *est.* 312 × 160 (picture 96 + a column of 3 chips 40 px, gap 8); 4 × 160 + 3 × 12 = 676 ≤ 722. **Pictures print WITHOUT a food name** (m, leaks: nl carrot "Wortel" = the root chip "wortel"; fi beetroot "Punajuuri" ⊃ "juuri", cauliflower "Kukkakaali" ⊃ "kukka"; nl "Bloemkool" ⊃ "bloem"; sv/da/no "Blomkål" ⊃ "blom-"). Picture-only is safe because every plausible mis-recognition among the opened pictures stays inside ONE part (radish/beetroot/parsnip/carrot = root; lettuce/cabbage/spinach = leaf; broccoli/cauliflower = flower; corn/peas = seed).
**Pool (all opened, m):** root `vegetables/carrot`, `vegetables/radish`, `vegetables/beetroot`, `vegetables/parsnip` (4) · leaf `vegetables/lettuce`, `vegetables/spinach` (one leaf, ideal), `vegetables/cabbage` (3) · flower `vegetables/broccoli`, `vegetables/cauliflower` (2) · stem `vegetables/celery` (1) · seed `vegetables/corn` (cob in husk), `At the Supermarket/peas` (open pod) (2) · fruit `vegetables/tomato`, `vegetables/cucumber`, `At the Supermarket/pepper` (red bell pepper) (3). **Excluded after opening or by botany:** `vegetables/asparagus` (red-tipped spear reads as a flower bud), `vegetables/potato` (tuber = stem, contradicts K-3 intuition), `onion`/`garlic`/`leek` (bulb), `mushroom` (not a plant), `vegetables/turnip` (= beetroot art), `green_beans` (pod: fruit or seed, contested), `bell_pepper`/`chilli_pepper`/`green_beans` (no vocab), `fruits/peach` (reads as an apple), all `fruits/*` (the fruit part is taught with the vegetable-shelf fruits tomato/cucumber/pepper; an apple makes "fruit" a food-class answer).
**Single answer by construction:** each item carries `allow` = the distractor parts that are UNARGUABLY wrong for that picture: root items `[flower,fruit,seed]` (the pictures show leafy tops, so leaf and stem are banned); leaf `[root,flower,fruit,seed]`; flower `[root,fruit,seed]` (stalk and leaves visible); celery `[root,flower,fruit,seed]` (botanically a leaf-stalk: leaf banned); corn `[root,stem,flower]` (a kernel is botanically a fruit; husk leaves visible); peas `[root,stem,flower,leaf]` (the pod is the fruit: fruit banned); tomato `[root,flower]` (calyx + stalk visible, seeds inside); cucumber, pepper `[root,flower,leaf]`. Chips = answer + 2 sampled from `allow`.
**verify():** stamps `data-lcs-food=<theme/noun> data-lcs-part=<answer>` per card and `data-lcs-chip=<part>` per chip; re-derived: answer === `EAT[item].part`; the two distractors ⊂ `EAT[item].allow`; the card-part multiset === `d.mix`; no chip repeated in a card; chip labels === `partWords[loc]`; the answer's chip position over 8 cards: each of 3 slots used 2-3 times, not a staircase (0,1,2,0,1,2…) and not constant; no food name printed; no two cards the same picture.
**Refusals:** none (no noun label; 6 part literals ×11). Landing (not the page) explains tomato: "a tomato is the FRUIT of the tomato plant, even if we call it a vegetable in the kitchen" (the K-202 bin).
**Query face:** "which part of the plant do we eat" · de "Welche Pflanzenteile essen wir" · nl "eetbare delen van een plant" · es "partes de la planta que comemos".

### F4: Parts of a Plant and Their Functions (G2, G2-3xx TBD, CODE `layout:'jobs'`)
**Move:** MATCH a part to its JOB: left column = 5 part cards (a small `plantFigure({stage:'full', highlight:[part]})` + the part word), right column = 5 job sentences (derangement); draw a line. Function, the EF02CI06 second clause and 1-LS1-1.
**Child:** "Draw a line from each part of the plant to the job it does." (58)
**Params:** d1 `{layout:'jobs', parts:['root','stem','leaf','flower'], figPx:90}` · **d2** `{layout:'jobs', parts:['root','stem','leaf','flower','seed'], figPx:96, rowH:110, jobPx:17}` · d3 `{parts:6 +fruit}` (fruit's job needs the word "seed": legal only because seed's job must then avoid "fruit"; validator decides). Stack *est.*: 5 × 110 + 4 × 14 = 606 ≤ 722; job sentence ≤ 2 lines in a ~330 px column.
**Jobs (en source; each is true, and TRUE OF ONE PART ONLY on the page):** root "takes in water from the soil" · stem "holds the plant up tall" · leaf "makes food for the plant from sunlight" · flower "calls bees with its bright colours" · seed "can grow into a new plant" · (d3) fruit "keeps the seeds safe inside". Stem's water transport and root's anchoring are deliberately NOT used (each would fit two parts). **Rule: a job sentence never contains the word (or panel-listed stem) of any part printed on the same page** (the flower "makes seeds" is out because seed is on the d2 page).
**verify():** stamps `data-lcs-part=<id>` left, `data-lcs-job=<id>` right; re-derived: the bijection part↔job by id; right order is a derangement and not the reverse; each right text === `jobs[loc][id]`; no right text matches any left part word (normalized, `(?<!\p{L})…(?!\p{L})`, plus `partStems[loc]`); each left figure highlights exactly its part.
**Refusals:** none expected; any locale whose panel cannot write a one-part-only job for a part swaps that part for `fruit` (data decision).
**Query face:** "parts of a plant and their functions" · de "Wozu braucht die Pflanze ihre Teile" · es/pt/it "y sus funciones / e suas funções / e le loro funzioni" · fi "kasvin osat ja niiden tehtävät".

### F5: Parts of a Flower (G3, G3-3xx TBD, CODE `layout:'flower'`)
**Move:** LABEL the cut-away FLOWER (a new diagram and new vocabulary: the flower is the part that makes seeds) from a bank that holds ONE word that is not a flower part (cross it out). Distinct from the base in object, vocabulary and the extra reject step.
**Child:** "Label the flower. Write each word on its line. One word in the word bank is not part of a flower: cross it out." (112)
**Params:** d1 `{layout:'flower', labels:['petal','stem','stamen','pistil'], decoy:0}` · **d2** `{layout:'flower', labels:['petal','sepal','stamen','pistil','stem'], decoy:1, decoyPool:['root'], figureH:540, laneW:200, laneH:60, glyphH:26}` · d3 `{labels:+'ovary', decoy:1}`. Stack *est.*: bank 59 + 10 + 540 = 609. The decoy is `root` because the flower view has NO root drawn (never `leaf`, `seed`, `fruit`: an ovary holds the future seed and fruit, so they are arguable).
**verify():** as base, plus: `data-lcs-figure="flower"`; bank = labels ∪ {decoy}; the decoy id is not an anchor of the figure; the decoy's word stamped `data-lcs-decoy`; decoy position in the bank is not first or last on the SHIPPED seed (a trivial tell), labels ↔ lanes bijection.
**Refusals:** none; **above band** in en (4-LS1-1), fr (cycle 3), es (3º-4º), it (terza-quarta), fi (3.-4. lk), landing says so per locale and the page is still built (brief CCSS-honesty rule). Band G3 because the content belongs there in 8 of 11 locales (the lock's "faces K/G2" was a guide; flag for the critic).
**Query face:** en "parts of a flower" · de "Aufbau der Blüte" · sv "blommans delar" (the strongest Nordic cell, R2 18 all on-topic) · fi "kukan osat" (R2 20) · da/no "blomstens dele/deler".

**Rejected non-moves.** colour the plant by legend (a theme-free colouring of the base figure: kept as the RESERVE face if a locale refuses one) · plant diary / grow a bean (open-ended; the classic Bohnentagebuch/carnet d'observation, reserve #2) · "circle what plants need among 6 chips" (silly distractors, the K child answers by common sense) · parts of a TREE (theme slug + the base move on another object) · fruit-or-vegetable (K-202) · living/non-living (K-201) · spring flowers (K-322).

## C. Native rebuild ×11

Per locale the panel authors: `partWords` 6 · `flowerWords` 4 (+ `stem` reused, + decoy = `partWords.root`) · `jobs` 6 · `partStems` (the job-leak list, 3-10) · 6 titles + 6 instructions = **~28 literals**. Frames never inflect; the part words are bare nouns in the locale's diagram register (sg or pl as the panel rules; the figure has several roots and 3 leaves, the leader points at one). No objForms needed (no noun enters a sentence).

| loc | part words the panel authors (proposal) | flower words | re-target / refusal | traps |
|---|---|---|---|---|
| en | roots, stem, leaf, flower, fruit, seed | petal, sepal, stamen, pistil | none | "fruit" on the pepper card is botanical; landing explains |
| de | Wurzel, Stängel, Blatt, **Blüte**, Frucht, Samen | Kronblatt (or Blütenblatt), Kelchblatt, Staubblatt, Stempel | none | Blüte ≠ Blume (vocab `flower` = "Blume", m); Stängel (post-1996) not Stengel, not Stamm; Frucht not Obst; capital kept |
| es | raíz, tallo, hoja, flor, fruto, semilla | pétalo, sépalo, estambre, pistilo | none | fruto not fruta; "planta del pie" noise: the head always "de la planta"; MX register |
| pt | raiz, caule, folha, flor, fruto, semente | pétala, sépala, estame, pistilo | none | caule not tronco; fruto not fruta (EF02CI06 wording) |
| fr | racines, tige, feuille, fleur, fruit, graine | pétale, sépale, étamine, pistil | none | graine not semence/pépin; "plante des pieds" noise |
| it | radici, fusto, foglia, fiore, frutto, seme | petalo, sepalo, stame, pistillo | none | fusto vs stelo: ONE word for both faces (panel); frutto not frutta; "ciclo vitale" |
| nl | wortel, stengel, blad, bloem, vrucht, zaad | kroonblad, kelkblad, meeldraad, stamper | none | vrucht not fruit; wortel = carrot (why the eat face prints no names) |
| sv | rot, stjälk, blad, blomma, frukt, frö | kronblad, foderblad, ståndare, pistill | none | chips indefinite (never roten/stjälken; `bana`→banan class); stam = trunk |
| da | rod, stængel, blad, blomst, frugt, frø | kronblad, bægerblad, støvdrager, støvvej | none | definite never on chips; "planter" retail noise in titles |
| no | rot, stilk, blad, blomst, frukt, frø | kronblad, begerblad, støvbærer, støvvei (or pistill; panel) | none | bokmål; stilk not stengel (panel); definite never on chips |
| fi | juuri, varsi, lehti, kukka, hedelmä, siemen | terälehti, verholehti, hede, emi | none | nominative chips only; every job sentence and instruction a whole literal (case); theme "Kukat" never in a title; "punajuuri"/"kukkakaali" leaks avoided by picture-only |

## D. Data + gates

```js
// data/b5/plants.js  (locale-neutral)
module.exports = {
  PARTS: ['root','stem','leaf','flower','fruit','seed'],
  FLOWER_PARTS: ['petal','sepal','stamen','pistil','stem','ovary'],
  NEEDS: ['sun','raindrop'],                                   // light, water
  NEED_PICS: { sun:{theme:'weather',noun:'sun',picOpened:true}, raindrop:{theme:'weather',noun:'raindrop',picOpened:true} },
  NON_NEEDS: [{theme:'toys',noun:'ball',picOpened:true}, /* blocks car dice train robot shoe glove drum guitar pencil book */],
  NON_NEED_FORBIDDEN: ['kite','hat','cap','lamp','moon','watering_can','cloud','rainy','hot','puddle'],
  EAT: [ {theme:'vegetables',noun:'carrot',part:'root',allow:['flower','fruit','seed'],picOpened:true}, /* 14 more, B F3 */ ],
  EAT_BLOCKED: ['asparagus','potato','onion','garlic','leek','mushroom','turnip','green_beans','peach'],
  STAGES: ['seed','germinating','seedling','young','flowering','fruiting'],
  JOB_IDS: ['root','stem','leaf','flower','seed','fruit'],
};
// data/b5/locales/plants.<loc>.json
{ "partWords":{"root":"","stem":"","leaf":"","flower":"","fruit":"","seed":""},
  "flowerWords":{"petal":"","sepal":"","stamen":"","pistil":"","ovary":""},
  "jobs":{"root":"","stem":"","leaf":"","flower":"","seed":"","fruit":""},
  "partStems":["…"], "forbidden":["fruta","frutta","Obst","Blume","Stengel","tronco","fruit(nl)"],
  "strings":{"base":{"title":"","instruction":""},"needs":{},"cycle":{},"eat":{},"jobs":{},"flower":{}} }
```
Reuses: none of the science JSON banks (fence); pictures via `lib/b3-picture-index.js hasPicture` (throws on a miss); loaded by `lib/b5-common.js bank('plants', loc)`.

**Validator (`tools/validate-b5-draft.js`, plants rules):** (1) every PARTS/FLOWER_PARTS/JOB_IDS key present, non-empty, trimmed; (2) all part + flower words pairwise distinct after NFC-lowercase; (3) no part word equals a `forbidden` literal of its locale; (4) every EAT item: `part ∈ PARTS`, `allow.length ≥ 2`, `part ∉ allow`, noun ∉ EAT_BLOCKED, `picOpened:true`, picture exists; (5) every mix bucket at the shipped level reachable from the pool (d2 stem 1 ≤ pool 1, seed 1 ≤ 2); (6) NON_NEEDS disjoint from NEEDS and from NON_NEED_FORBIDDEN, each `picOpened`; (7) every job sentence ≤ 90 chars and contains NO part word nor `partStems` entry of any part on the same face config (Unicode letter boundaries); (8) the flower decoy is not a FLOWER_PARTS anchor; (9) instructions ≤ 150 chars, contain no free-predication and no apparatus word the face lacks (per-face banned list: `tick`, `colour`, `letter boxes`, `cut`, `glue`); (10) non-de part words not capitalised (a vocab citation form leaking in).

**Poison cases (each must FAIL; the correct draft is the control):** P1 carrot `allow` contains `root` · P2 celery `allow:['root']` (1 distractor) · P3 `vegetables/asparagus` in EAT · P4 de `partWords.flower:"Blume"` · P5 es `partWords.fruit:"fruta"` · P6 sv `partWords.root` = `partWords.leaf` duplicate ("blad") · P7 en job flower "makes seeds" on the d2 page (seed present) · P8 fi job root "juuri ottaa vettä" (own part word) · P9 flower decoy `stem` · P10 NON_NEEDS contains `toys/kite` · P11 needs row rendered with two `grows=1` pots · P12 cycle bank order === slot order · P13 eat answer slot sequence 0,1,2,0,1,2,0,1 · P14 base instruction "Tick the parts" · P15 EAT item `picOpened:false`.

**`qa/verify-b5-plants.js` asserts on the render (all 6 faces × 11 locales):** body ≤ 722 at the fi 4-line-title stack (677 when the title wraps 4) with no overflow; floors: K needs gift pictures ≥ 56 px, pot ≥ 96 px; G1 base/cycle lane glyph ≥ 28 and stage drawings ≥ 90 px, bank words ≥ 17 px; G2 eat picture ≥ 90, chips ≥ 40 high, 17 px; G3 flower lanes glyph ≥ 24; figure measured: every anchor inside its own shape, roots only below the soil line, the pod contains ≥ 3 seed shapes, leaders pairwise non-intersecting and ≥ 12 px apart; every answer surface empty (lanes, wheel slots 2..n, no pre-circled chip/pot); no food name in the eat body; no numeral in the body except card badges and the given "1"-seed box; uniqueness of each re-derived answer (verify() above); position-tell measured on the SHIPPED instance (seed is locale-independent, so a tell ships to all 11 at once). Poison the render too: a highlighted pot drawn wilted, a bank word removed, a leader moved 20 px must each fail.

## E. SEO

**Titles (the native panels finalise; ≤ 70; never the worksheet word; never a bare theme word):**

| face | en | de | es | pt | fr | it | nl | sv | da | no | fi |
|---|---|---|---|---|---|---|---|---|---|---|---|
| base | Parts of a Plant | Teile der Pflanze | Partes de la planta | Partes da planta | Les parties de la plante | Le parti della pianta | Delen van een plant | Växtens delar | Plantens dele | Plantens deler | Kasvin osat |
| needs (K) | What Plants Need to Grow | Was Pflanzen zum Wachsen brauchen | ¿Qué necesitan las plantas para crecer? | Do que as plantas precisam para crescer | Les besoins des plantes | Di cosa ha bisogno una pianta | Wat heeft een plant nodig? | Vad behöver växten? | Hvad har en plante brug for? | Hva trenger en plante? | Mitä kasvi tarvitsee? |
| cycle (G1) | Plant Life Cycle: From Seed to Plant | Von der Bohne zur Pflanze | Ciclo de vida de las plantas | Ciclo de vida das plantas | Le cycle de vie d'une plante | Il ciclo vitale della pianta | Van zaadje tot plant | Från frö till växt | Fra frø til plante | Fra frø til plante | Siemenestä kasviksi |
| eat (G2) | Parts of a Plant We Eat | Welche Pflanzenteile essen wir? | Partes de la planta que comemos | Partes da planta que comemos | Les parties des plantes que l'on mange | Le parti della pianta che mangiamo | Eetbare delen van een plant | Vilka växtdelar äter vi? | Hvilke plantedele spiser vi? | Hvilke plantedeler spiser vi? | Mitä kasvin osia syömme? |
| jobs (G2) | Parts of a Plant and Their Functions | Wozu braucht die Pflanze ihre Teile? | Partes de la planta y sus funciones | Partes da planta e suas funções | Les parties de la plante et leurs fonctions | Le parti della pianta e le loro funzioni | Waarvoor dient elk deel van een plant? | Växtens delar och deras uppgifter | Plantens dele og deres opgaver | Plantens deler og hva de gjør | Kasvin osat ja niiden tehtävät |
| flower (G3) | Parts of a Flower | Aufbau der Blüte | Partes de la flor | Partes da flor | Les parties de la fleur | Le parti del fiore | De delen van een bloem | Blommans delar | Blomstens dele | Blomstens deler | Kukan osat |

⚠ da/no base and cycle both say "plante"; nl cycle "zaadje" and base "plant": each pair differs by head noun, not by an adjective. es/pt "flor" face is the only title without "planta": intended (the flower head).

**Meta MIDDLEs (en source, the child's instruction extended; whole meta 120-170; panels rebuild):**
- base: "Children find each part of the plant (roots, stem, leaf, flower and fruit) and write its name on the line, using every word in the word bank once" (≈143)
- needs: "Each plant gets two things: children circle the plant that has both light and water, the two things every plant needs to grow" (≈127)
- cycle: "Starting at the seed, children follow the arrows round the circle and write the letter of each growing stage in the right box" (≈126)
- eat: "Carrot, lettuce, broccoli, celery, corn or tomato: children circle which part of the plant we eat, root, leaf, flower, stem, seed or fruit" (≈139)
- jobs: "Children draw a line from each part of the plant to its job: taking in water, holding the plant up, making food, calling bees, growing a new plant" (≈146)
- flower: "Children label the petal, sepal, stamen, pistil and stem of a flower, and cross out the one word in the bank that is not part of a flower" (≈136)

**Coordinates:** `{type:'plants', mode:'base'|'what-plants-need'|'seed-to-plant'|'parts-we-eat'|'functions'|'parts-of-a-flower', level: LEVEL_KEYS[loc][band], theme:''}` (bands G1 · K · G1 · G2 · G2 · G3).

**Non-cannibalisation (estimated whole-page word-3-gram Jaccard, engineer measures):** base↔flower ≈ 0.20 (shared "write … on its line", "word bank"; different object + decoy) · base↔jobs ≈ 0.12 · base↔eat ≈ 0.08 · eat↔jobs ≈ 0.10 · needs↔any ≈ < 0.06 · cycle↔any ≈ < 0.08 · **base↔K-354 human-body base ≈ 0.15** (instruction deliberately rebuilt: K-354 = "Copy each word from the bank onto the line that points to that part of the body"; ours shares only "part of the" and "word bank") · cycle↔G1-203 ≈ < 0.10 (chicken, "Write 1, 2, 3") · cycle↔G1-377 base (metamorphosis) ≈ 0.20 if both use a wheel: titles never share "life cycle" alone; landings must differ by organism in every sentence · eat↔K-202 ≈ < 0.08 (no bin, no "vegetable" chip). Same-grade pairs to watch: eat↔jobs (both G2: different head nouns "we eat" / "functions").

## F. Open questions + summary

**Engineer must measure:** the cycle stack (wheel + bank ≤ 722 at the fi title); the eat card chip widths at the longest literal (de "Stängel", es "semilla", fi "hedelmä", *est.* ≤ 100 px at 17 px); the flower-figure legibility of stamen vs pistil at h 540 (anthers ≥ 10 px, stigma distinct); every anchor inside its shape; all Jaccards above against the published corpus; `fruits/peach`-class ambiguity is avoided because no fruit-theme picture is used.
**Only a native panel can rule:** sg/pl of each part word (en roots/leaf); it fusto vs stelo; no stilk vs stengel, støvvei vs pistill; da støvvej; whether the job sentences stay one-part-only in their language (rule 7); de "Kronblatt" vs "Blütenblatt"; whether fr/it/es/fi landings flag the flower face as above band.
**Critic decisions:** F5 band G3 (content home) vs the lock's K/G2 guidance; whether `cycleWheel` is shared with G1-377 (one component in `components-b5`, two families).

**Summary.** Six faces, all CODE on one `layout` knob over ONE new primitive (`plant-figure.js`, plant + 6 stages + cut-away flower): base Parts of a Plant (G1, label from a bank) · F1 What Plants Need to Grow (K, circle the pot given light AND water; ordinary objects as non-needs, no X shortcut) · F2 From Seed to Plant (G1, letters into a 5-box cycle wheel with a return arrow) · F3 Parts of a Plant We Eat (G2, 8 unnamed food pictures × 3 part chips, per-item unarguable distractors) · F4 Functions (G2, part to one-part-only job) · F5 Parts of a Flower (G3, label + cross out the decoy). Refusals: none in any locale (66/66 expected); flower face above band in en/fr/es/it/fi, stated on the landing.
