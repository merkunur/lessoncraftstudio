# K-368 `2d-shapes` — the five faces (Phase E, 2026-09-23)

Contract: `../K-368-2d-shapes.md` §3 (faces) + §5 (gates); rules `../_FACE-BRIEF.md` (incl. the nt10-E additions and the lead's FILL note). Ids fixed by `../_records/b5var-id-allocation.json`.

All five are **CODE** faces on the base's ONE additive knob `mode` (`types/k/K-368-2d-shapes.js`: `d.mode` undefined = the base path, byte-identical; a face dispatches to its own builder, stamps `data-lcs-mode` on the root, and keys every guard on its own resolved config). Rows: `scripts/worksheet-gen/tools/b5var-rows/2d-shapes.js` → `node tools/gen-b5var-specs.js`. The G1 faces carry `extra {gradeBand:'G1'}`. Titles + instructions are the bank's `SHAPES_2D.en.strings[mode]` verbatim (the gate asserts row === bank).

## Files touched (family-scoped only)
| file | change |
|---|---|
| `types/k/K-368-2d-shapes.js` | the mode dispatch (replaces the Phase-E refusal); five face builders + configs/guards; `faceVerifyInPage` (one in-page verify for all five, own geometry, never `data-lcs-kind`) |
| `templates/components-b5/2d-shapes.js` | `givenPill`, `realRow`, `shapeChoice` (+ `tileGlyph`, `cropSvg`), `objectCard`, `writeLane`, `riddleBubble` (the design's `bubble`, renamed: the shared namespace refuses duplicates and `bubble` is too likely to collide), `riddleCard`, `dotLattice`, `dotCard` |
| `tools/b5var-rows/2d-shapes.js` | NEW — the 5 rows |
| `types/{g1,k}/*` | EMITTED: `G1-381-2d-shapes-real-or-not.js`, `K-371-2d-shapes-around-us.js`, `G1-382-2d-shapes-write-the-names.js`, `G1-383-2d-shape-riddles.js`, `K-372-2d-shapes-draw-on-dots.js` |
| `qa/verify-b5-2d-shapes.js` | rule 6 grows the APPARATUS lint (en); PR11 redefined for the built faces; calls section 5 |
| `qa/b5-2d-shapes-faces.js` | NEW — the face gate (sweeps, renders, SPARSE / FILL / OVERLAP / floors, the face poisons) |
| `i18n/strings.en.json` | regenerated by `node i18n/build-en.js` (688 types, title lint clean) |
| `data/b5/2d-shapes.js` | unchanged (the face strings + the 9 objects were authored with the base) |

## The faces
### F1 · G1-381 · `2d-shapes-real-or-not` · CODE `mode:'real-or-not'` · G1
- Config `{rows:['triangle','rectangle','square'], perRow:4, trueMin:1, trueMax:3, lens:132, R:[33,60], nearR:[44,60], turnedMin:2, skinnyMin:1, floorW:30, floorE:45, colMaxShare:0.5}`.
- Child: reads the teal name at the head of each lane, circles EVERY figure in the row that truly is that shape (near-misses: gap, curved side, rounded corners, kite / rhombus / parallelogram / trapezoid, a rectangle in the square row, never a square in the rectangle row).
- Owns: per row trueCount 1..3 (rows never all equal); ≥ 2 turned + ≥ 1 skinny TRUE figures measured from the drawn path; every near-miss measurable (gap ≥ 10, sagitta ≥ 15 %, fillet ≥ 20 % of the shortest side rebuilt from the path's own straight runs, ellipse ≥ 1.4, chord cap ≥ 0.2 r, quadrilateral ≥ 10° off right); per-PAGE column tell (≥ 3 columns used, no column over ⌈total/2⌉ and no column carrying a true figure in every row) + pooled ≤ 40 % (measured 0.23-0.28). Poisons PR2, PR3F (stays green), PR8, SP1, OV1, FL1, AT1, AP1.
- PNG `scripts/worksheet-gen/out/dev/G1-381-null-d2-en.png` — read: three lanes, pill over four lenses, near-misses read as what they are, trues turned / skinny / small, no clip.
- Deviations (measured): (1) the triangle row's `round` near-miss is REPLACED by a curved hypotenuse on a right triangle — the primitive's fillet (r = 0.25 × shortest side) trims 43 % of every side of an equilateral triangle and drew a pebble (~10 px of straight side at R 44), read as "a blob", not "a triangle with round corners". (2) Lanes grow to fill the body with `justify-content:space-evenly` (the design's stack is 648 of 677; at the one-line chrome a fixed stack left 166 px blank). (3) Every "level" figure is EXACTLY level or clearly turned (16-74°): a 1-8° tilt (the design's "square rot 0..12", "right ±8") read as a drawing slip in the render. (4) Lens clearance is 3 px (R 60 + 1.5 in a 66 radius), per the design's own arithmetic, not the base's 7.5.
- Refusals: none visible (names only; every locale's 4 names already pass the base's synthetic-block check).

### F2 · K-371 · `2d-shapes-around-us` · CODE `mode:'around-us'` · K
- Config `{items:8, split:{circle:4, rectangle:4}, pic:110, tile:[176,56], tilePx:16, cols:2, rows:4, runMax:3}`.
- Child: looks at each opened library picture and COLOURS its shape tile (circle over rectangle, fixed legend order).
- Owns: split from the BANK's shape (not the stamp); every `img src === fileUri(theme, noun)` of its bank entry; no object twice; each grid column holds both shapes and no run > 3 (per-page tell); tiles exactly circle,rectangle; picture ≥ 110, tile ≥ 56, tile text unclipped (fi `suorakulmio` at 16 px fits). Poisons PR6, SP2, AT2, AP2.
- PNG `scripts/worksheet-gen/out/dev/K-371-null-d2-en.png` (+ `K-371-gate-fi677.png`) — read: envelope, plate, lollipop, door, clock, tablet, whiteboard, pizza; every picture reads as its shape in colour and in grey.
- Deviations: none. d1 / d3 of §3 are not faces (the emitter ships one config at every level).
- Refusals: none (pictures are locale-neutral). Panels still rule on the lollipop and the tablet (design §4).

### F3 · G1-382 · `2d-shapes-write-the-names` · CODE `mode:'write-name'` · G1
- Config `{lanes:6, bank:true, turnedMin:2, elongated:1, lens:84, R:[33,36], rowW:541, rowH:70, glyphH:32, bankPx:18, floorW:30, floorE:45}`.
- Child: writes each shape's name on the empty writing line, spelling from the word bank (no letter boxes, no starter).
- Owns: answers (own classify) = the 4 core kinds, none > 2, never the same kind in adjacent lanes; bank = the 4 core literals in an order ≠ the first-appearance order of the answers (per-page tell); every ruling row present and EMPTY; no text in any lane; ≥ 2 turned; G1 floors. Poisons PR10, SP3, FL2 (content past the body at 677), AT3, AP3.
- PNG `scripts/worksheet-gen/out/dev/G1-382-null-d2-en.png` — read: bank of four, six lanes, turned rectangle + square, elongated rectangle, no repeated neighbours.
- Deviations (measured): (1) lens **84** / row **541** / R [33, 36] (design 78 / 547 / [33, 35]): at 78 the figures met the lens edge with 1-2.5 px to spare (R 35 + 1.5 in a 39 radius) and read cramped; 84 keeps 4.5 px and still fits the 677 lane (inner 86.7). (2) The two extra lanes draw from square / triangle / rectangle only — a second circle can differ from the first only by 1-2 px of R and read as a duplicate. (3) Lanes grow to fill (the slack is spread inside lanes, never below the page).
- Refusals: none.

### F4 · G1-383 · `2d-shape-riddles` · CODE `mode:'riddles'` · G1
- Config `{cards:6, tags:3, tagW:132, tagH:44, tagPx:18, px:16, lh:20, slotMaxShare:0.6, cols:2, rows:3}`.
- Child: reads each riddle (attributes only, no drawing) and circles the shape's name.
- Owns: exactly one tag names the riddle's kind; square riddles never offer rectangle; the riddle text free of every name and listed inflection (NFC, case-fold, `\p{L}` boundaries); riddle text === the bank riddle; per-page slot spread (all 3 slots used, ≤ 60 %) + pooled (measured 0.32-0.35); tags ≥ 44; bubble ≤ 3 lines. Poisons PR9, SP4, OV4, AT4, AP4.
- PNG `scripts/worksheet-gen/out/dev/G1-383-null-d2-en.png` (+ `G1-383-gate-fi677.png`).
- Deviations (measured): (1) the bubble GROWS to its card's slack (flex) and centres its text — a fixed 80 px bubble left a 40 px band at the one-line chrome; (2) the card number badge is lifted above the full-width bubble (`z-index:2` on cardGrid's own badge span, a string rewrite inside the builder — `page.css` untouched) and the bubble's left padding is 20 (the badge otherwise covered the bubble corner and the first letters).
- Refusals: none from the bank shape. Panel note: a German compound that forces a 4th bubble line at 282 px is sent back (the gate measures it).

### F5 · K-372 · `2d-shapes-draw-on-dots` · CODE `mode:'dot-draw'` · K · OPEN (structure + lints, no answer)
- Config `{cards:4, kinds:['triangle','square','square','rectangle'], given:[null,null,[1,2],[2,4]], pitch:46, n:6}`.
- Child: reads the name and joins the dots to draw it; two cards start from a coral slanted side, so the square and the 2 : 1 rectangle are drawn TURNED.
- Owns (the gate's own completion code): every given side joins two lattice points; the square card closes as a square inside the lattice; the rectangle card closes as a 2 : 1 rectangle and has NO square completion; no text in the lattice; drawn pitch ≥ 46; the spec refuses a start where the rectangle side could close as a square. Poisons PR12 (render) + PR12g (config guard), SP5, AP5.
- PNG `scripts/worksheet-gen/out/dev/K-372-null-d2-en.png`.
- Deviation (measured): the lattice svg scales (meet) to fill its card instead of a fixed 242 px box — at the one-line chrome the fixed lattice left a 51 px band; the pitch never falls below 46 (gated). No answer-tell gate: a drawing face has no answer position.
- Refusals: none.

## Gate lines
- `node qa/verify-b5-2d-shapes.js` — **PASS (3818 assertions, 48/48 poisons killed)** (base 22 + faces 26: PR2 PR3F PR6 PR8 PR9 PR10 PR12 PR12g · FL1 FL2 · SP1-SP5 · OV1 OV4 · AT1-AT4 · AP1-AP5; each face's untouched render at the poison chromes is the control). Face renders: every face at its own chrome, 814, 722, 677 — max blank band ≤ 35 px, FILL 95.7-99.2 %, verify 0, lints 0. `--quick` also PASS.
- `node tools/gate-variation-distinct.js --batch=b5 --diffs=2 --family=2d-shapes` — `compared 15 pairs over 5 faces against their bases + pairwise within family` / **every variation differs from the deck its base publishes and from its siblings**.
- `node tools/b3-baseline.js --check --quick` — `checked build 3760 + enum 277 in 21s: 0 drifted (0 expected), 0 missing` / **PASS**.

## Open items for the panels
- Rule 6's apparatus lint is en-only (no per-locale lexicon in the §5 bank shape): each panel checks its own six instructions name only the apparatus on the page (row / picture / line + box / riddle / dots + thick line).
- F2: rule on the lollipop and the tablet pictures (design §4).
- F4: riddles at ≤ 90 chars must stay ≤ 3 lines at Nunito 800 16 in 256 px of text (the badge padding took 8 px from the design's 264; measured in the gate).
- F1: the triangle row no longer carries a rounded triangle (see F1 deviation 1); if a panel wants the "round corners" near-miss for triangles, it needs a smaller fillet knob in `primitives/flat-shape.js` (a primitive change, not a face change).
- FILL (lead note): gated per face (content bottom ≥ 85 % of the body at the one-line chrome, inside the body at 677), poisoned both ways (FL1 ends high, FL2 runs past the body).
