# G3-377 `division-with-remainder` : BASE build record (2026-09-14)

Built from `G3-377-division-with-remainder.md` §2 + §5 under `_BUILD-BRIEF.md`, `_SUBSTRATE.md` and the README cross-type rulings (unitAxis · 722 body · blankNumeralBox · notation.js item 11 · no visible "free"). Base face only (Phase 1); the five faces of §3 are NOT emitted, but every additive seam the faces need (`_buildWith(bank, cfg, …)`, `divisionLine({lines:1, shown, ghost})`, `casitaFrame({shown, ghost})`, `dealBoxes`) is in place. Nothing shared was edited (`git status --short -- scripts/worksheet-gen` = 3 untracked files + the gitignored bank, 0 modified); nothing committed.

## Files (all type-scoped, all new)

| file | what |
|---|---|
| `scripts/worksheet-gen/types/g3/G3-377-division-with-remainder.js` | the spec: `{id:'G3-377', slug:'division-with-remainder', gradeBand:'G3', assetClass:'icon-placement', exerciseType:'division-with-remainder', themeAxis:{applicable:true, minNouns:4, excludeBw:true}, unitAxis:{applicable:true, units:()=>['2-5','6-9','2-9'], exemplar:(loc)=>bank.exemplar, tokens:(unit,loc)=>bank.unitLabels[unit]}, difficulty:{1,2,3}, i18n.en, build(), _buildWith() (the gate's injection seam), verify()}` + `UNITS` / `divisorsOf` exported for the gate |
| `scripts/worksheet-gen/templates/components-b3/division-with-remainder.js` | `remBox` · `divisionLine` · `casitaFrame` · `dealBoxes` — exactly the four NEW names of §2 (namespace merge verified: 80 exports, no duplicate) |
| `scripts/worksheet-gen/data/b3/division-with-remainder.js` | `DIVISION.en = { exemplar:'2-5', unitLabels, notation:{op:'÷', template:'{n} ÷ {d} = {q} R{r}', boxStyle:'inline', remWord:'R', exactWord, restWord, leftoverLabel, rightWord, wrongWord, kinds}, strings:{'G3-377', F1..F5} }` + the exported `validateBank(all)` (§5 rules 1-8 + rule 9 below). `data/` is gitignored: the reviewer force-adds. |
| `scripts/worksheet-gen/qa/verify-b3-division-with-remainder.js` | the §5 gate (sections A / C / D below) |
| `docs/worksheet-gen/b3-designs/_work/G3-377-build.md` | this record |

The design names the bank `data/b3/division.js`; the build task names it `data/b3/division-with-remainder.js` (= the family key, the `bank('<key>', loc)` convention every sibling uses). The task's name is used; the export is `DIVISION` as in §5.

## What the base page is

Four wide numbered cards (`cardGrid({cols:1, rows:4, numbered:true})`, gap 14; every `.ws-card-stage` inline `padding:0 0 0 <inset>px`, flex row, `justify-content:space-between`, gap 14). Left: a pile of n themed pictures in ROWS OF TEN (`iconRows({iconPx, perRow:10, gapX, gapY:6, rng})`, ±4° warmth, the rows container forced `align-items:flex-start` so a partial last row starts at the left like a real pile), one countable noun per card from the wave theme. Right: `divisionLine({template, n, d, locale, boxStyle, remWord, lines:2, w:zone})` — line 1 `29 ÷ 3 =` (numerals Baloo 2 700 26 ink `data-lcs-num`, operator Baloo 2 700 24 teal, `=` inkSoft), line 2 `[ ] R[ ]` (two `.ws-blankbox` 44 × 40 dashed coral, `data-lcs-answer=""` + `data-lcs-role="q"|"r"`, the remainder word Nunito 800 18). Casita locales get `casitaFrame` (one SVG: dividend left, bracket, divisor right, q box under the divisor, r box under the dividend). The pile shows n only; q and r live on the item root's stamps and nowhere else.

Ladder (resolved config keys; every guard keys on these — `d.layout / d.divisors / d.nMin / d.nMax / d.minR / d.iconPx / d.perRow / d.gapX / d.inset / d.minDistinctD / d.minQ2Cards / d.rVaries / d.distinctN`):

| key | d1 | d2 (ships) | d3 |
|---|---|---|---|
| layout / cards | rows / 4 | rows / 4 | scatter (`iconScatter` 414 × 120) / 4 |
| divisors | [2, 3] | the unit (exemplar 2-5 → [2,3,4,5]) | [2..9] |
| n | 5..15 | 7..30 | 11..30 |
| r | 1..d−1 | 1..d−1 | 1..d−1 |
| pictures / gap / pile | 40 / 5 / 445 × 86 | 36 / 6 / 414 × 120 | 36 / — / 414 × 120 |
| inset / zone | 4 / 184 | 20 / 199 | 20 / 199 |
| page rules | ≥ 2 distinct d, n distinct | ≥ 3 distinct d, q ≥ 2 on ≥ 3 cards, r not constant, n distinct | same |

Generation = the design's `pickItem` verbatim (draw d, q in `max(1, ceil((nMin − d + 1)/d)) .. min(qMax, floor((nMax − 1)/d))`, r in `minR .. min(d − 1, nMax − q·d)`, n = q·d + r, duplicate (n, d) rejected, 200 tries then THROW) inside a page loop that retries the whole page against the page rules (300 attempts then THROW). Refusals (throw, never a filler): a BW-marked theme; a theme with < 4 countable nouns in the locale; an unknown unit; an unauthored locale (no bank block — `lib/b3-common.js bank()`); a bank `op` / template glyph that disagrees with `types/_shared/notation.js`; a template without each slot exactly once; `minR < 1`; pictures < 36; a pile taller than the 700-chrome card inner (136); a notation zone narrower than the widest measured line (176 inline / 170 casita); a casita remWord that cannot fit left of the r box.

Answer hiding: root `<div data-ws-content data-lcs-dwr data-lcs-cards data-lcs-layout data-lcs-divisors data-lcs-nmin data-lcs-nmax data-lcs-minr data-lcs-iconpx data-lcs-op data-lcs-style data-lcs-distinctn data-lcs-unit data-lcs-locale data-lcs-theme data-lcs-zonew data-lcs-pilew>`; item root `<div class="ws-card-stage" data-ws-content data-lcs-item data-lcs-n data-lcs-d data-lcs-q data-lcs-r data-lcs-noun>`; pile `[data-lcs-pile]`; zone `[data-lcs-notation data-lcs-style data-lcs-lines data-lcs-template]`; boxes `[data-lcs-answer=""][data-lcs-role]`. `verify(page)` (browser, stamps only): q === floor(n/d), r === n − q·d, q·d + r === n, 1 ≤ r < d ("remainder too big" / "exact division"), r ≥ minR, d in the divisor set, nMin ≤ n ≤ nMax, (n, d) distinct + n distinct when stamped, pile img count === n (loaded, ≥ 36 px rect, styled ≥ iconPx, no alt, never a localized-BW directory, no img outside the pile), no `[data-lcs-group] / [data-lcs-ring] / [data-lcs-slot] / [data-lcs-leftover]` on a rows/scatter pile, exactly one zone with `scrollWidth <= clientWidth` inside the card inner box, `data-lcs-num` = {n, d} only and text === stamp, every division sign printed === the root's `data-lcs-op`, sv: no `÷` in any text node or the body, exactly two boxes q then r with empty text and an empty `data-lcs-answer`, no `data-lcs-shown-*` on the base, no text node equal to String(q) / String(r) unless it is also n or d, items === cards, no ground truth outside an item.

## Deviations from the design file (each measured)

1. **Left inset 20 px at d2/d3, 4 px at d1 (design: none).** The card badge (30 × 30 at −1/−1) intrudes 15 px into the card's inner box; a 3-row pile is 120 tall in a 142 inner (11 px above it) and its ±4° rotated 36-px pictures have bounding rects 1.3 px wider (±8° scatter: 2.3). Measured in the gate: with no inset the first picture of every 3-row pile sits under the badge (11 of the first run's 37 failures) and the first rotated picture of a 445/454-wide pile overshoots the inner box by 1.35 px (d1). The inset clears both; the zone shrinks accordingly.
2. **d2/d3 zone 199, not 219; d1 zone 184, not 179.** 647 − 20 − 414 − 14 = 199. Measured line widths in the real render (Letter/A4, the shell's woff2): en line 2 109 · de 129 · sv 137 · nl 137 · fi 134 · fr `(4 × [ ]) + [ ]` **173** (design est. 176) — every one fits 184 and 199. The casita draws at `min(200, zone)` (see 4).
3. **d1 pictures 40 px at gap 5 (design gap 6):** 10 × 40 + 9 × 5 = 445 + inset 4 + 14 = 463 leaves 184 for the zone; at gap 6 (454) the zone would be 175, 2 px over the measured fr line. Pencil lane 5 px = 1.3 mm plus the PNGs' own transparent air.
4. **`casitaFrame` is width-parametric** (the design's 200-px coordinates scaled by w ≥ 170: dividend 0.2w, bracket 0.5w, divisor 0.74w; at w = 200 the numbers are the design's 40 / 100 / 148 / 126 / 18) so the d1 zone (184) draws a 184-px casita. **The remainder word slides the r box right** to make room (design: word "left of the r box" at x 14 — a 5-char word cannot sit in 14 px); `resto` (measured 39.4 px) fits the 199-px casita at d2/d3 but NOT the 184-px one at d1 → the frame THROWS. The pt panel authors `''` (the BR chave writes no word) or `r.`; the gate's synthetic pt block ships `''`, and two poisons prove `residuo` (d2) and `resto` (d1) are refused. **Open item for the pt panel.**
5. **`remBox` is `.ws-blankbox`, not `.ws-answerbox` + a border override.** The README ruling: every OPEN numeral box is the coral blank box (`.ws-blankbox` = white, 2.5 dashed coral, r 10 — visually the design's "dashed coral 2.5"); `.ws-answerbox`'s dash is `grid` and its class implies an `answer` stamp. The box stamps `data-lcs-answer=""` (EMPTY, as designed) + `data-lcs-role`; the ground truth is on the item root.
6. **Every sign is resolved through `types/_shared/notation.js`** (README item 11; the design put the glyph in DATA). Both hold: the bank's `notation.op` / template glyph IS data, and the build + `divisionLine` REFUSE when it disagrees with `divGlyph(locale)` / `mulGlyph(locale)`; the printed operator span is `divGlyph(loc)` itself. `validateBank` rule 9 (added): `op` and every `÷ : / × ·` in the template must equal the locale's sign. So an sv block carrying `÷` fails the validator AND the build (P3), on top of the design's sv-specific rule 3.
7. **`distinctN:true` on every level** (design: "(n, d) distinct"). The first d1 render printed `7 ÷ 3` and `7 ÷ 2` on one page; distinct n is stricter, never conflicts, and is stamped (`data-lcs-distinctn`) so `verify()` keys on the resolved config. Recorded, not silent.
8. **The unit resolves d2's divisors only** (design §2 ladder: d1 = [2,3], d3 = [2..9] "divisors 2..9"). A fanned d1/d3 deck differs from the exemplar's by seed (the unit is folded into the seed) but not by divisor set. If the emitter wants d3 to follow the unit, it is one config token (`3: {divisors:'unit'}`).
9. **Units are the declared three only** (`2-5 | 6-9 | 2-9`); any other range is refused (G1-310 precedent). `{U}` resolves through `bank.unitLabels` ("by 2 to 5") with a raw-key fallback; the shipped titles carry no `{U}` today.
10. **The worst legal chromes measure 733 / 710 / 677, not 722.** LONG (68-char en title → 3 lines + 150-char instruction → 2) = 733; WORST (61-char de title → 3 + the 150-char fi instruction G1-310 proved wraps to 3) = 710; DEEP (70-char fi title → **4 lines** + the 3-line instruction) = **677** — a 4-line title and a 3-line instruction are each legal, so their combination is (G2-316 measured 700 with a 2-line instruction). At 677 the card inner is 130.75 vs the 3-row pile 120 + 2.6 rotation → 8 px slack; the gate renders all three at d1-d3 and every probe proves its own line count (a vacuous probe fails). Every domain-worded fi instruction I wrote at ≤ 150 chars wrapped to TWO lines (the third needs a long word at the very end; 151 chars wrapped to 3) — the WORST/DEEP instruction is G1-310's proven fixture, content irrelevant to the probe.
11. **The rows container is left-aligned by a string edit of `iconRows`' output** (`.ws-icon-rows` is `align-items:center`, which would centre a partial last row under the full rows). The needle is asserted (a changed `iconRows` shape throws), no shared file is touched.
12. **`validateBank` head list + sibling titles:** rule 6's "equal to a shipped G3-309 / G3-310 / G3-370 landing title" is implemented against an EN list (`Divide It Up` · `Repeated Subtraction Division` · `Multiplication and Division Word Problems`); each panel extends `SIBLING_TITLES.<loc>`. The bare-head check reads `HEAD.<loc>` (§1's eleven heads, accent-folded).

## Gate `qa/verify-b3-division-with-remainder.js`

Own ground truth (q/r re-derived in the gate's own in-page code; stamps diffed against the build's `meta`; notation.js read directly; the bank validated directly). Sections: **A** `validateBank` on the real bank (0 faults) · `strings['G3-377'] === spec.i18n.en` · the three units parse · the exemplar is a declared unit · `divGlyph` for all 11 locales === the design's §1 column (a drift of the ONE sign table fails first) · probe lengths · every synthetic block passes the validator · **C** real-pipeline renders: en d1/d2/d3 (Letter), a second theme (fruits), units 6-9 + 2-9 at d2, LONG × d1-d3, WORST × d1-d3, DEEP × d1-d3, the seed sweep × d1-d3, SYNTHETIC blocks de/sv/fr/fi/nl/it/es/pt at d1 (under WORST chrome) + d2; per render: lints clean, verify empty, the audit (q/r re-derived, pile === n, pictures ≥ 36 px + loaded + inside the card + clear of the badge, numerals ≥ 26 px === stamp, boxes ≥ 44 × 40 / 44 × 36, zone inside the card ≤ 88 tall with `scrollWidth <= clientWidth` and every line inside it, word ≥ 14 px, sign === notation.js, sv no ÷), items === cfg, meta ↔ stamps, the page rules, the chrome probes' own line counts, non-vacuity; sweep: no two seeds identical · four refusals (BW theme, emotions, unit 3-4, unauthored de) · **D** poisons.

```
--quick (6 seeds):  PASS (2782 assertions, 42/42 poisons killed; 5 deferred to the faces)
full (20 seeds):    PASS (5260 assertions, 42/42 poisons killed; 5 deferred to the faces)   (sweep: d1 20/20, d2 20/20, d3 20/20 distinct pages)
```

Poisons killed (42/42; the correct EN bank + d2 page is the control): **P1** r = d (`q−1, r+d` → "remainder too big") · **P2** the quotient printed as text · **P3a** sv `op:'÷'` refused at build + fails the validator · **P3b** an sv page whose `/` was rewritten to `÷` ("historic minus") · **P4** q one too big · **P8** `[data-lcs-group]` on the pile ("pre-boxed pile") · **P9** two cards with one (n, d) ("duplicate item") · **P11a** `zoo animals bw` refused at build · **P11b** a picture path rewritten into the bw directory · **P12a** the bare de head "Teilen mit Rest" as a title · **P12b** an sv instruction containing `÷` · **P12c** an fr product template without `×` · **P13** n above nMax (`49 ÷ 2` at d2) · an exact item (r = 0) · `minR 0` stamped · the en bank with op `:` (validator + build) · a pile one picture short · pictures shrunk to 30 · the sign rewritten `÷` → `:` on an en page · the boxes swapped (r then q) · a box carrying text · a third printed numeral (q) · an F1 deal row on the base · a shown pair on the base · alt text on a picture · n repeated across cards · a page of 3 cards · a blank page · the flat-760 stack (cards `min-height:190` under WORST chrome → footer lint) · a 19-char remainder word (zone overflow) · a 9-char remainder word (validator) · a casita r box carrying text · a d2 casita `residuo` refused · a d1 casita `resto` refused · unit `3-4` refused · a theme below the floor refused · an unauthored locale refused · a template with `{q}` twice refused · `minR 0` config refused · 30-px pictures config refused.
**Deferred (need face code): P5 (F3 exact stamp with r = 2), P6 (F4 shown pair / rBig with shown r < d), P7 (F5 printed hop / 24-px hop), P10 (F1 slot with an img), P13's F2 `q > qMax` form** — printed as DEFERRED, never counted.

## Renders (looked at every one)

`scripts/worksheet-gen/out/dev/`:
- **`G3-377-animals-d2-en.png`** (the deliverable) · `G3-377-animals-d1-en.png` · `G3-377-animals-d3-en.png` — lints + verify clean (`render/one.js G3-377 animals <d> en`).
- gate renders under `out/dev/g3377-gate/`: `G3-377-d2-en-fruits.png`, `G3-377-d2-en-u6-9.png`, `G3-377-d2-en-u2-9.png`, `G3-377-d{1,2,3}-en-longchrome.png` (733), `-worstchrome.png` (710), `-deepchrome.png` (677), the seed sweep, `G3-377-d{1,2}-{de,sv,fr,fi,nl,it,es,pt}-synth.png` (d1 under WORST chrome), the poison pages.

What I saw and fixed: (1) the first run's d2 piles had their first picture under the card badge and the d1 piles' first rotated picture 1.35 px outside the inner box (found by the gate's badge / inner-box audit, not by eye) → the insets of deviation 1; (2) two `7`s on one d1 page → `distinctN`; (3) es/pt casita refused at d1 (zone 179 < 200) → the width-parametric frame; (4) pt `resto` off the left edge of the frame → the sliding r box + the refusal.

Pictures opened (the base is THEMED — the wave picks the theme; the pictures on the pages I rendered and read, `animals` + `fruits`): elephant, zebra, cat, duck, leopard, panda, fox, dolphin, penguin, dog, giraffe, orangutan, whale, rabbit, fish — every one is what its name says; no BW art (the `excludeBw` + `BW_MARK` refusal). A per-theme picture audit for the 48 eligible colour themes (≥ 4 countable nouns in every locale; `colors` 3 and `emotions` 0 are refused by the floor) is the wave's step, as for every themed sibling.

## `i18n/build-en.js` + baseline

`node i18n/build-en.js` → `498 types … (title lint clean)`; `strings.en.json` restored with `git checkout --` afterwards (the reviewer rebuilds it once at the end of the phase). `tools/b3-baseline.js` NOT run (the reviewer runs it); nothing shared changed, so the build section cannot drift.

## Open items for the faces / panels

1. **pt `remWord`**: `resto` fits the d2/d3 casita (199) but not d1's (184) → the pt panel authors `''` (the chave convention) or `r.`; the build refuses the rest. es `''` as designed; it stays inline `r.` (classe seconda) with the casita as the classe-terza data switch.
2. **`{U}` labels**: `bank.unitLabels` ("by 2 to 5") is the token source; a panel that speaks the unit in a title authors its three labels; the shipped titles carry no `{U}`.
3. **d3 follows the ladder's [2..9], not the unit** (deviation 8) — one config token if the emitter wants otherwise.
4. **The faces reuse the base's seams:** `_buildWith(bank, cfg, {theme, locale, unit}, ctx)`; `divisionLine({lines:1})` for F2/F3/F5, `{shown:{q,r}}` + `{ghost:true}` for F4, `casitaFrame({shown, ghost})`; `dealBoxes({d, slotW, leftoverLabel})` for F1 (the base renders none; verify() already rejects a slot/leftover on a rows/scatter pile). F1 needs its own `data-lcs-mode='share'` branch that ALLOWS them.
5. **`validate-b3-draft.js` (absent)** should import `validateBank` from the data module rather than re-author rules 1-9; the sibling-title list is EN-only until each panel adds its locale's shipped G3-309 / G3-310 / G3-370 titles.
6. **Hub registration** (§7): `apps['division-with-remainder']` + `axes['exercise-type']['division-with-remainder']` slug/name ×11 are ABSENT; `scripts/verify-hub-type-rows.js` is absent — the batch's registration step, not this build.
7. **The design's `data/b3/division.js` name** vs the shipped `division-with-remainder.js` (the family key) — the apply tool must write the latter.
