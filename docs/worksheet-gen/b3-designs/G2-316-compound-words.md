# G2-316 `compound-words` (G2) : FINAL design (editor merge, 2026-09-13)

Merged from `_work/G2-316-pedagogy.md` + `_work/G2-316-design.md`. Every file, primitive, class and option below was read in the repo (record + what was changed: `_work/G2-316-critic.md`). (m) = re-measured by node 2026-09-13 under ONE rule: W is a **both-pictured pair** iff W, A, B are distinct pictured singulars (the 50 colour dirs of `cache/manifest.json`, `bw:false`, `lib/b2-common.js entriesFor` so `B2_EXCLUDE` applies, `/^\p{L}+$/u`, case-folded, A and B >= 3 letters) and `W === A + link + B` with `link` in the locale set (de `'' s n en e er es` · nl `'' s n en e ne` · sv `'' s e a o u` · da/no `'' s e er` · fi `'' n` · en/Romance `''`); the flagged false hits of both work files struck. Numbers marked *est.* are the engineer's to measure.

**Boundary (load-bearing).** A compound is two WORDS with a new MEANING. K-231 builds one word from LETTER tiles; G1-249 orders WORD tiles into a sentence; G1-305 splits SOUND units (syllables: `Son-nen-blu-me`, never `Sonne|n|blume`); G1-306 reads printed syllables; `singular-plural` (6 types) owns the plural of any noun; G1-307 F5 owns the negative PREFIX (`un-`); G2-315 owns spelling rules. No face here prints a syllable, a letter tile, a plural or a prefix.

## 1 Identity

| field | value |
|---|---|
| id / key / band | `G2-316` / `compound-words` / G2 in all 11 (faces `G2-320+ TBD by the emitter`). `default_subject: letters`, `default_age_range: 7-9`, `assetClass: icon-placement`, `exerciseType: compound-words`. Key ABSENT from `topics-taxonomy.json` (grep = 0 (m)); register `apps['compound-words'] = {default_subject:'letters', default_age_range:'7-9', exercise_type_axis_key:'compound-words'}` + slug/name x11 (registrar = the `tools/register-b2-taxonomy.js NEW_FAMILIES` shape). |
| theme axis | `themeAxis:{applicable:false}` on all six (m: the best single theme holds 4-6 both-pictured pairs; pairs are cross-theme by nature: `sun`+`flower`). `enumerate.js:20` emits ONE instance per (type, difficulty, locale), theme part `nothm`; pictures via G1-306's NEW `lib/b3-picture-index.js pictureFor(rng, key, loc)` (BW dirs skipped, `excluded()` applied). Landings carry `coordinate.theme:''` (`letter-tracing` precedent in `frontend/content/seo-landing/en.json`). |
| fan lever | curated pair SETS via **`unitAxis`** (README cross-type ruling; never `setAxis`): `unitAxis:{applicable:true}`, bank `units:[{id:'A', items:[...]}, {id:'B', ...}]`, `exemplar:'A'`; the wave ships set A; `-u<set>` suffix, `{U}` unused in titles (a set is not a search head). Until the knob exists `build()` reads `bank[loc].exemplar`. |
| three rebuild SHAPES (locale DATA, not faces) | **compound** en de nl sv da no fi · **family** es fr · **alterati** it pt. ONE item schema `{a, link, b, whole}` (§5); ONE row apparatus (§2). |
| pools (m) | both-pictured pairs / linked / disjoint-parts max: en 20 / 0 / 15 · de 25 / 5 / 15 · nl 24 (23 after `blauw|e|regen`, an inflected adjective, is excluded by data) / 5 (4) / 16 · sv 21 / 0 / 14 · da 28 (27 after `flag|er|mus`, a false Fuge analysis) / 5 (4) / 20 · no 22 (21 after `flagg|er|mus`) / 3 (2) / 16 · fi 23 (22 under the 14-letter cap: `varvassandaalit` 15) / 0 / 16 · es 0 · fr 1 (`portefeuille`) · it 0 · pt 0. Pictured Romance derivations: es `florero florista panadero jardinero cocinero librero` 6 · fr `fleuriste jardinier cuisinier pâtisserie` 4 (`boulanger` is NOT in the `pain` family) · it 0 · pt `patinho` 1 (+ `galinha cozinha` = foils). |
| CCSS (en only; national framework NAME elsewhere) | base F2 F3 F5 **L.2.4.d**; F1 L.2.4.d; F4 L.2.4.d + RF.2.3. Family / alterati anchors L.2.4.b / L.2.4.c in JSON-LD only. |
| data | picture + singular + gender from the vocab; every pair, link, affix, foil, hub and title is a panel LITERAL in `data/b3/compound-words.js`; approved-words is NOT read (a seam is a meaning boundary, not a syllable boundary). |

| loc | genre head (base title) | ASCII slug | G2 label (level key) | shape |
|---|---|---|---|---|
| en | Compound Words | `compound-words` | grade 2 (`grade-2`) | compound (closed only) |
| de | Zusammengesetzte Nomen | `zusammengesetzte-nomen` | 2. Klasse (`2-klasse`) | compound + Fugenelement |
| es (MX) | Familia de palabras | `familia-de-palabras` | segundo grado (`segundo-grado`) | family (+ aumentativo/diminutivo on F5) |
| pt (BR) | Diminutivo e aumentativo | `diminutivo-e-aumentativo` | 2º ano (`2o-ano`) | alterati (BNCC EF02LP, the pt panel's own re-target) |
| fr | Mots de la même famille | `mots-de-la-meme-famille` | CE1 (`ce1`) | family |
| it | Nomi alterati | `nomi-alterati` | classe seconda (`classe-seconda`) | alterati |
| nl | Samengestelde woorden | `samengestelde-woorden` | groep 4 (`groep-4`) | compound + tussen-n |
| sv | Sammansatta ord | `sammansatta-ord` | åk 2 (`ak-2`) | compound + foge-s |
| da | Sammensatte ord | `sammensatte-ord` | 2. klasse (`2-klasse`) | compound + fuge-e/-s |
| no | Sammensatte ord | `sammensatte-ord` | 3. trinn (`3-trinn`) | compound + binde-e |
| fi | Yhdyssanat | `yhdyssanat` | 2. luokka (`2-luokka`) | compound (nominatiivi; genetiivi via `aStem`) |

Level keys = `LEVEL_KEYS` in `scripts/seo-landing/gen-b2var-landings.js:112-124`. Heads from `_PANEL-FINDINGS.md` §7; the panel may rename. de copy never says `Wortfamilie` (that is the root-family concept).

## 2 The base page

**Concept.** "Two pictures make one word." Eight full-width cream rows read left to right as an equation: `[picture A] + [cue B] = [school-line lane]`; the child names both, decides the join, writes the new word. No text on the page but the teal `+` and `=`. Compound shape: cue B = the second part's picture. Family shape (es fr): A = the root picture, cue B = an affix chip (`-ista`), the child writes `florista`. Alterati shape (it pt): A = the base noun's picture scaled 0.55 or 1.35 (the brief allows scaling), cue B = the chip (`-inho`/`-ão`), the child writes `gatinho`. One apparatus, three shapes; the row is the pedagogy in all 11 (the design file's Romance tree is NOT the base; the tree survives only as F5, §3).

**Layout d2** (page 703x945, `.ws-page` padding 0 14 -> inner 675, `page/page.css:16-26`; body ≈ 760, exact UNKNOWN, engineer measures):
```
+------------------------------ .ws-lane 675x84 ------------------------------------+
|(1) [pic A 64]  +  [pic B 64 | chip -ista]  =  |________ writingRow 410x64 ________|
|                                                |- - - - - - - glyphH 28 - - - - - -|
+-----------------------------------------------------------------------------------+
   8 rows, gap 12: 8x84 + 7x12 = 756 <= 760 (gap 8 -> 728 if the body measures 736)
```
- Row = `.ws-lane` (`page.css:401`: cream, 2 px `#F0E4CB`, r 14, padding 12 16 by default) with inline `padding:8px 14px` -> inner 643x64, one flex line, `align-items:center`, `[data-ws-content]` (the lint content selector, `qa/lints.js:32`). `countBadge(i+1)` (`components-b2.js:243`, `.ws-countbadge` absolute). Widths: 64 + 10 + 28 + 10 + 64 + 10 + 28 + 12 = 226; 643 - 226 = 417 -> lane 410. Pictures 64 >= G2 element floor 36 (`density.G23`, `_tokens.js:70`); 8 items within [8,16]; glyphH 28 >= 24.
- `writingRow({w:410, h:64, glyphH:28, xHeight:true}).svg` (`primitives/trace-path.js:681`, `data-lcs-prim="writing-row"`). Capacity *est.* ≈ 21 px per glyph -> 19 letters; **pool cap `[...whole].length <= 14`** (de max 14 `Kleiderschrank`, fi 15 -> one refusal). Engineer measures a 14-letter hand-width sample (critic OPEN 1).
- Affix chip = `wordTiles({tokens:['-ista'], fontPx:20, tileH:40})` (`components-b2.js:26`, `.ws-tile` Nunito 800, white, teal 2): width *est.* 24 + 0.62 x 20 x len -> `-inho` ≈ 86; the lane shrinks by `chipW - 64`.

**Ladder** (resolved config; guards key on these keys, never the index):

| key | d1 | d2 (ships) | d3 |
|---|---|---|---|
| rows / rowH / pic | 6 / 116 / 68 | 8 / 84 / 64 | 10 / 66 / 48 |
| lane w / h / glyphH | 400 / 76 / 34 | 410 / 64 / 28 | 426 / 50 / 24 |
| partWords (word printed under each picture, `displayWord`) | true | false | false |
| bank (the wholes, `collation.js` order) | true | false | false |
| minLinked / maxLetters | 0 / 12 | 2 where the locale has links, else 0 / 14 | 4 where available / 14 |
| distinctParts | true | true | true |

d1: 6 x 116 + 5 x 12 = 756, inner 96, part words Nunito 800 18 under the pictures. d3: 10 x 66 + 9 x 10 = 750, inner 50, no badges. `sampleEntries(rng, pool, d.rows, 'G2-316')` (`lib/b2-common.js:64`) throws when short: a locale below 8 disjoint items in the set is REFUSED and recorded, never filled. `distinctParts`: no part on the page twice (m: reachable in all 7 compound locales, disjoint max >= 14).

**Answer-hiding + uniqueness.** Bank literal per item: `{a:{vocabKey,word}, aStem:null|'auringo', link:'', b:{vocabKey,word}|{affix:'-ista'}, whole:{word, vocabKey|null, scale:1|0.55|1.35}, opaque:false}`. Hidden stamps per row: `data-lcs-a` `data-lcs-b` (vocab keys or the affix), `data-lcs-link`, `data-lcs-whole` (display form), `data-lcs-cut` (index where the second part starts = `(aStem||a.word).length + link.length`), root `data-lcs-face`, `data-lcs-set`. `verify(page)` (browser): rows === `d.rows`; per row two cues (`img.naturalWidth > 0` or one `.ws-tile`), `[data-lcs-op]` reading `+` then `=`, one empty `[data-lcs-prim="writing-row"]` <= 410 wide; **no text node equals any stamped whole** (base F1 F3 F5; the chip text is an affix, never a whole); no part key twice; svg widths <= 643. Node re-derivation (`page.evaluate` cannot require): `tools/gate-compound-words-data.js` (§5): `whole === casing(aStem||a.word) + link + (b.word||affix minus '-')`, case-folded after the first letter (de keeps the capital: `Sonnenblume`, never `SonnenBlume`; poison `Sonneblume` -> FAIL).

**Reused (exact).** `.ws-lane .ws-tile .ws-tilerow .ws-match .ws-match-col .ws-match-item .ws-match-dot(--left/--right) .ws-bank .ws-bankword .ws-bank--icons .ws-scene-banner .ws-pill .ws-icon` (`page/page.css`); `cardGrid({cards,cols,rows})` (`templates/layouts/card-grid.js`, F1); `writingRow`; `wordTiles rulingBlock({rows,w,h,glyphH}) wordBank({words,wordPx,withIcons}) countBadge` (`components-b2.js:26/58/210/243`); `svgRoot roundedRect line label el esc` (`primitives/_svg.js`); `displayWord excluded distinctByWord sampleEntries fileUri KEEP_CASE` (`lib/b2-common.js`); `compare` (`data/b2/collation.js`); tokens `T.teal T.coral T.ink T.grid T.white T.inkSoft`, `F.display F.body`. NOT used: `answerBox` (numeral box), `letterBoxes` (leaks length), `strokeWordLane` (nothing to trace), `pillChoice` (no per-item stamp), G1-306's `syllableJoin` (prints parts as text = the answer half-given).

**NEW in `templates/components-b3.js`** (file absent (m); the batch's first design creates it): `compoundRow({cueA:{pic,scale?,word?}, cueB:{pic}|{chip}, lane:{w,h,glyphH}, index, linkBox=null, ghost=null})` (the §2 row; `linkBox` inserts a box between two `wordTiles` tiles, F1; `ghost` = a second copy of the hub picture at `opacity:.55`, F5) · `opGlyph(ch)` (`svgRoot 28x28`, `label` Baloo 2 700 26 `T.teal`, `data-lcs-op`) · `linkBox({w,h=36})` (`roundedRect r8 T.white T.coral 2.5 dash 6 5`, `data-lcs-linkbox`; width FIXED by `d.linkBoxW`, never by the answer) · `splitWord({word, cell, fontPx})` = G1-305's `syllableWord` (equal cells, no borders) + a `mode:'cut'` rail: `line({y:cell+8, strokeColor:T.grid, strokeWidth:1})` across the cells, no marker · `webBlock({hub:{pic,word}, lanes:[{cue:{pic}, ghost, lane}], w:675})` (one `.ws-lane`: hub picture 96 in a white `roundedRect r16` with the hub WORD printed under it, Baloo 2 700 24; an SVG bracket `T.teal` 2 round caps to each lane; `data-lcs-hub`). No new primitive.

**Locale slot structure.** `i18n/strings.<loc>.json` keyed by id (`G2-316` + five face ids) `{title, instruction}`; `i18n/skill-sentences.<loc>.json` key `compound-words` `{full, short}` (65 keys today (m), none for this type); taxonomy slug + name; `frontend/messages/<loc>.json topicMeta.compound-words`; `data/b3/compound-words.js[loc]` (§5). Casing: de `keep-first` (`a` keeps its capital, `b` lowercased inside the word); all others `lower`; sv/da/no parts in the INDEFINITE singular (`bana`, never `banan`); fi instruction a whole literal (case).

**Alternatives.** Alt A 2x4 cards with the equation above the lane (G1-244 shape): lane 302 caps at ~14 x 21 = 294 with 8 px slack and d1 part words no longer fit; rejected for the base, ADOPTED for F1. Alt B the design's Romance `familyTree` as base: 8 derivations from 2 roots halves the root breadth per page and makes F5 the base again; rejected, kept as F5. Alt C compound picture + two part lanes: pool halves, it is F2 in another apparatus; rejected.

**Risks -> mitigations.** Lane capacity is an estimate -> OPEN 1. Link thinness (sv fi 0, no 2 in the both-pictured pool) -> F1 is data-conditional (§3). de capital inside the join -> gate case-folds after letter 1. Opaque compounds (en `bluebell buttercup cornflower`, sv `gräshoppa blåregn`, da `rødhals`, de `Löwenzahn`) -> `opaque:true`, allowed on F2/F4 only (analysis), never on the base/F3/F5 (meaning synthesis). da `skovsnegl` parses two ways (`skov|snegl` / `skov|s|negl`) -> the bank pins one parse, the gate rejects a second. **Print check:** lane 64 = 16.9 mm, glyphH 28 = 7.4 mm, pictures 64 = 16.9 mm, `+`/`=` 26 px ≈ 6.9 mm, cells 32 = 8.5 mm; teal ≈ 60 % grey, dashed coral ≈ 45 %, ghost at .55 ≈ 30 % (mono proof: OPEN 2); nothing within 14 px of the edge; smallest text 18 (d1 part words) + the footer; `qa/lints.js` on de + fi renders before any copy claims a level.

## 3 Faces 2-6

All five are CODE faces (`d.mode` knob + a `verify()` branch, stamped only when declared; base byte-identical). `tools/gate-variation-distinct.js` reads `waves/wave-b2-en.json` + `gen-b2var-specs.js ROWS` (m: `gate-variation-distinct.js:31-36`), so a b3 wave file / ROWS list is required before it sees this type (OPEN 7). Floors: 8 disjoint items (base F1 F2 F5), 6 (F3), 6 + 6 foils (F4); a cell below its floor is REFUSED and recorded. Word web and word star are ONE face (F5).

### F1 : What Goes in the Middle? (`mode:'link'`) : the joint
EN never ships. **Move:** the SEAM morpheme is the decision (Fugen-s/-n, tussen-n, foge-s, binde-e, fi genitive `-n`): both parts printed as word tiles, a dashed box between them, the whole's picture as clue; the child writes the joint (or leaves the box empty) and the whole word. Parts need NOT be pictured (only the whole): the one-part pool applies. **Layout:** `cardGrid({cols:2, rows:4})`, card (760 - 42) / 4 = 179 -> inner 302x151, stage `padding:6px 0`: line 1 `[tile A][linkBox 36][tile B][pic whole 40]` (`wordTiles` font 18 tileH 36; A <= 9, B <= 8 letters: `Geburtstag + Kuchen` = 144 + 8 + 36 + 8 + 96 = 292 <= 302 with the picture in the card badge corner *est.*) + 8 + `writingRow 302x60 glyphH 28` + 6 = 116 <= 151. **Config** `{mode:'link', cards:8, cols:2, rows:4, linkBoxW:36, minLinked:3, minEmpty:3, maxLetters:14, tileFont:18}`; d1 6 cards with the joint choices printed (`pillChoice` of `links`); d3 8 cards, whole only, no box. **Verify:** 8 boxes all 36 wide (poison: a 1-letter and a 2-letter link with different widths -> FAIL); >= 3 rows with `link !== ''` and >= 3 with `''`; tiles equal `a.word` / `b.word` verbatim, the whole nowhere as text. fi prints the NOMINATIVE tile (`aurinko`); a graded stem cannot be "written in the box" -> set A uses ungraded genitives (`sateen`, `karhun`, `talon`), graded ones (`auringon`) only in set B with `aStem` (OPEN 3). **Ships (m):** de (5 linked) · nl (4) · da (4) from measured data; **no (2), sv (0), fi (0) only when the panel authors >= 3 one-part linked items** (no `jul|e|kule barn|e|seng`; sv `nattduk|s|bord anteckning|s|bok` + 1; fi `sate|en|kaari karhu|n|vatukka talo|n|mies`); **REFUSED en** (link always empty = no decision); **REFUSED it pt** (alterati has no joint: the vowel drop is the base's own act); **es fr default REFUSED**, liftable if the panel authors >= 3 items with an INSERTED literal (es `pan|ad|ero`, fr `jardin|i|er lait|i|er poisson|n|ier`). ⚠ Rule 3: en it pt = 3 firm refusals; if es AND fr also refuse, F1 sits at 5 and is swapped for the fallback in OPEN 4. **Query face:** the joint (Fugen-s und Fugen-n · tussen-n · foge-s · yhdyssanan alkuosa · fr lettre de liaison).

### F2 : Split the Compound (`mode:'cut'`)
EN "Split the Compound: Find the Two Words". **Move:** analysis: the whole is printed in letter cells with its own picture; the child draws ONE pencil line where the second word starts. **Layout:** 8 rows h 84, inner 64: `[badge][pic whole 56][splitWord n <= 14, cell 32, Baloo 2 700 30]` = 56 + 12 + 448 = 516 <= 643. Pool = items whose `whole.vocabKey` is pictured (the whole both-pictured pool + one-part items; `opaque` allowed). Family: derived word in cells, cut after the root (`flor|ero`, es "subraya la raíz"); alterati `gatt|ino`. **Config** `{mode:'cut', rows:8, cell:32, fontPx:30, pic:56, maxLetters:14}`; d1 6 rows cell 36, a `T.grid` tick printed at the cut; d3 10 rows cell 28 + two short rulings (`rulingBlock` 2 x 150x50) where the child also writes the two parts. **Verify:** cells `n === [...whole].length`; the cell svg has no `<path>`, no gap, no tick at d2 (poison: the d1 tick at d2 -> FAIL); `data-lcs-cut` re-derives from the bank; no text equals the whole with a separator inserted (`[\s\-|·/]`); Baloo 2 advance <= cell - 2 (G1-305's browser gate reused). **Ships:** all 11 (es/fr/it/pt from >= 8 panel-authored derivations; wholes need not be pictured there: `data-lcs-picfallback` shows the ROOT picture at 40). **Query face:** split (zerlegen · verdelen · jaa yhdyssana osiin · la raíz · la parola base).

### F3 : Match the Halves (`mode:'match'`)
**Move:** pair 6 first-part pictures with 6 second-part pictures by MEANING, then write each compound on the lane of its first picture. **Layout:** 6 rows h 100 (6 x 100 + 5 x 12 = 660): `[writingRow 300x64 glyphH 28][left .ws-match-item 88 + dot --right][gap 120][right .ws-match-item 88 + dot --left]` = 300 + 12 + 88 + 120 + 88 = 608 <= 643; 12 distinct pictures. Right column = a DERANGEMENT of the partner order (no partner on its own row, `rng.shuffle` until no fixed point, the `lit-vocab-match.js:44` idiom) and !== identity. **Cross-product gate (m, necessary):** for i != j, `a_i + link + b_j` must not be a pictured whole nor a panel `crossWords` entry; measured hits inside the pools: nl `ijs+vogel`, da `pande+kage pande+bånd vand+ring`, no `panne+kake panne+bånd`, fi `maa+pallo`, sv `blå+s+fisk`; en/de none. Family (es only): left = 6 root pictures, right = 6 pictured DERIVATIONS (`florero florista panadero jardinero cocinero librero` (m) = exactly the floor), the child writes the derived word. **Config** `{mode:'match', pairs:6, pic:88, laneW:300, glyphH:28, derange:true}`; d1 4 pairs pic 100 with part words; d3 8 pairs h 76 pic 64 lane 50 (refused where < 8 disjoint). **Verify:** 6 left keys === 6 right keys as a bijection, derangement, no cross hit, no part twice, lanes empty. **Ships:** en de nl sv da no fi es; **REFUSED fr (4 pictured derivations) it (0) pt (1, and same-object big/small is trivial)**. **Query face:** halves (Wortteile verbinden · verbind de delen · yhdistä sanan osat · une la raíz con su familia).

### F4 : Compound Detective (`mode:'detect'`)
**Move:** recognition with FOILS: 12 pictured words in a bank, 6 real compounds + 6 look-alikes that only seem to hide two words (`carpet` ≠ car + pet, `hamster`, de `Traktor`, sv `sparris`, fi `porkkana`); the child circles the 6 compounds and writes each one's two parts. **Layout:** `wordBank({words:12, wordPx:18, withIcons:true})` (`.ws-bank--icons`, 44 px icons; 3 rows *est.* ≈ 290, engineer measures) + 16 + six two-part lanes: `rulingBlock({rows:6, w:290, h:56, glyphH:26})` twice with a 28 px `opGlyph('+')` column between (290 + 28 + 290 = 608); 290 + 16 + 366 = 672 <= 760. Bank shuffled; foils are pictured words that decompose into NO two pictured parts (machine check, my hub scan (m) supplies candidates: en `carrot pelican pumpkin`, nl `spiegel fauteuil`, da `uniform papegøje`, fi `makkara haikara`). Family: intruders share letters not the root (`flor` / `flan`, fr `fleur` / `fleuve`); alterati: `-inho`/`-ino` that is no diminutive (pt `galinha cozinha caminho`, it `mattino bambino tacchino`). **Config** `{mode:'detect', bank:12, compounds:6, foils:6, iconPx:44, glyphH:26}`; d1 8 words 4 + 4 with a two-column cue; d3 12 words 7 + 5, no icons. **Verify:** exactly 6 `data-lcs-foil` chips from `foils[]`, 12 distinct pictured words, no foil equals or contains a pool whole; 12 empty rulings. **Ships:** all 11 (foils exist in every locale (m); Romance foils are panel literals). **Query face:** compound or not (Wortdetektiv · samenstelling of niet · yhdyssana vai ei · l'intrus de la famille · `-inho` que não é diminutivo).

### F5 : One Head, Many Words (`mode:'web'`) : word web = word star, merged
**Move:** productivity of ONE head: a hub picture with its WORD printed (`fish`, `Blume`, `kala`, es `flor`); four satellites around it, each cued by the COMPOUND's own picture beside a ghost of the hub in its true position; the child writes the four new words. The head is fixed and the join changes with the partner (`Sonne|n|blume` vs `Butter|blume`): the move the base cannot make. Satellites are one-part items (only the whole must be pictured), which is why the hub availability is measured on that pool. **Layout:** two `webBlock` 675x352 (2 x 352 + 16 = 720): hub 96 + word 24; 4 lanes `[pic whole 56][+][ghost hub 56 @.55][=][writingRow 320x64 glyphH 28]` = 56 + 10 + 28 + 10 + 56 + 10 + 28 + 12 + 320 = 530 <= 643 - hub column 110. **Hub availability (m, upper bound: whole pictured, hub pictured, remainder >= 3 letters, the panel strikes non-compounds such as nl `ster` <- `hamster`):** en `fish 6` (jellyfish starfish clownfish angelfish pufferfish goldfish) `sun 3 rain 3 cake 3` · de `Fisch 6 Blume 5 Band 5 Hand 5 Regen 5 Hose 4` · nl `vis 6 bloem 6 regen 5 kast 4 bal 4 broek 4` · sv `fisk 6 blå 5 sol 4 regn 4 bil 3 band 3` · da `regn 5 vand 4 sol 4 kage 3 fugl 3 bukser 3` · no `fisk 5 bukse 4 fugl 4 sol 4 regn 4 kake 3` · fi `kala 6 pallo 6 kakku 4 kukka 4 maa 4 käsi 4`. a-side hubs (`sun|glasses`, `regen|jas`) carry `hubSide:'a'`. da reaches 4 only on a-side hubs (`regn`, `vand`, `sol`); b-side hubs of 3 -> the panel confirms 2 x 4 or the face ships `lanes:3` (recorded). **Alterati / family re-target (`shape` dispatch inside the same face; landing mode `'size'`):** it pt es render **Big or Small** rows instead: the base row with the base noun's picture at 0.55 or 1.35 as the ONLY cue, no chip, no adjective (`gatinho` / `gatão`, es `gatito` / `gatote`); the child PRODUCES the ending the base only copied. fr: `-ette/-on/-eau` is irregular in CE1 -> **REFUSED by default**, liftable by the panel with >= 8 regular `sizePairs`. **Config** `{mode:'web', webs:2, lanes:4, hub:96, pic:56, ghost:.55, laneW:320, glyphH:28}` (size rows: `{mode:'web', rows:8, scaleCue:true, chip:false}`); d1 1 web x 3 + bank; d3 2 webs x 5, one foil satellite per web. **Verify:** hub word printed once and equals no whole; satellites distinct, every satellite whole `startsWith`/`endsWith` the hub word on `hubSide` (case-folded after letter 1); ghost `opacity` .55 on all lanes; size rows: `scale` in {0.55, 1.35}, no `.ws-tile`, no visible text. **Ships:** en de nl sv fi (>= 2 hubs of >= 4) · no da (panel confirms or `lanes:3`) · it pt es as size rows (>= 8 `sizePairs`) · **fr default REFUSED**. **Query face:** words with -fish (Wörter mit -blume · ord med -fisk · -kala sanat · aumentativo e diminutivo · aumentativos y diminutivos).

**Rejected non-moves.** Theme swap (max 6 pairs per theme) · longer / 3-part compounds (= d3; both-pictured 3-part = 1-2 per locale) · scrambled compound tiles (two tiles have one non-identity order; K-231 / G1-305 Scramble in disguise) · compound in a sentence (G1-249; frames not in the bank) · plural of compounds (`singular-plural`) · trace the compound (K-284) · word bank + pictures (= d1) · open vs closed (en `ice cream`, fi yhteen vai erikseen: 0 pictured open pairs, needs a wrong spelling printed; candidate future fi type) · "which form is right?" (prints `Sonneblume`) · explain the meaning (open-ended) · the Romance tree as a face beside the base (same act, other layout) · d1/d3 relabelled.

## 4 Native rebuild plan x11

Unit = a per-locale DATA block; the code substitutes literals and never joins, inflects, cases or infers a link. The panel authors >= 20 items (set A: 8 with disjoint parts; set B: 8; 4 spare), 4-6 hubs x >= 4 satellites, >= 8 foils, `sizePairs` where the shape needs them, 6 titles/instructions, the skill sentence, slug + name, topicMeta. EN handed over as a SOURCE TO AUDIT. Every panel OPENS every picture it keeps (`picOpened:true`; sv #35 precedent: `fruits/plum` is a red apple).

| loc | shape | measured set-A seed (a|link|b) | refusals | traps |
|---|---|---|---|---|
| en | compound | `water|melon tooth|brush hand|bag basket|ball arm|chair book|shelf star|fish rain|coat` (+ 12 more (m), critic §3); `blue|bell butter|cup corn|flower` opaque; one-part `rain|bow snow|man sun|glasses` | F1 | `carpet` the canonical foil; closed compounds only, open/closed is a landing sentence |
| de | compound + Fugenelement | `Sonne|n|blume Glocke|n|blume Löwe|n|zahn Kleid|er|schrank Zahn|bürste Regen|mantel Fuß|ball Käse|kuchen` (+ 17 more (m)); Fugen-s both-pictured = 0 -> one-part `Sonne|n|brille` only if the title says Fugen-n; a Fugen-s title needs >= 3 one-part Fugen-s items (OPEN 5) | none | `a` keeps the capital, `b` lowercased inside, `ß` kept; never `Wortfamilie` |
| nl | compound + tussen-n | `tand|en|borstel boek|en|kast paard|en|bloem zon|ne|bloem water|meloen hand|schoen voet|bal regen|jas` (+ 15 more (m)); `blauw|e|regen` excluded | none | tussen-n is groep 5-6 in SLO: F1 copy claims groep 4 only for picture-transparent items; `ij` one letter |
| sv | compound + foge-s | `sol|ros mask|ros blå|klocka smör|blomma vatten|melon tand|borste fot|boll bok|hylla` (+ 13 more (m)); one-part foge-s `nattduk|s|bord anteckning|s|bok` + 1 for F1 | F1 unless >= 3 foge-s | INDEFINITE singular parts; `grupp` unusable; adjective-first (`vit|lök`) is Lgr22-legitimate |
| da | compound + fuge-e/-s | `vask|e|bjørn får|e|kylling fugl|e|hus næsehorn|s|fugl vand|melon tand|børste fod|bold regn|frakke` (+ 19 more (m)); `flag|er|mus` struck | F5 `lanes:3` unless 2 x 4 confirmed | `pande` = pan AND forehead (pan picture); `skrivebord` verb-first (panel decides); `rødhals` opaque |
| no | compound + binde-e | `vask|e|bjørn ost|e|kake løve|tann vann|melon tann|børste fot|ball bok|hylle regn|frakk` (+ 13 more (m)); `flagg|er|mus` struck; one-part `jul|e|kule barn|e|seng` for F1 | F1 unless >= 3 binde-e; F5 as da | `treblad` (tre = three) and `selebukse` excluded; bokmål only |
| fi | compound, nominatiivi vs genetiivi | nominative `vesi|meloni hammas|harja jalka|pallo kirja|hylly jää|kaappi juusto|kakku voi|kukka sade|takki` (+ 14 more (m)); genitive one-part `sate|en|kaari karhu|n|vatukka talo|n|mies`, `auringo|n|kukka` via `aStem` set B | F1 unless >= 3 genitive | instruction a whole literal (case); gradation never "in the box" (OPEN 3); `[NSR-FLAG][fi]` |
| es (MX) | family (+ size on F5) | `flor -> florero florista floral` · `pan -> panadero panadería` · `zapato -> zapatero` (`aStem:'zapat'`) · `jardín -> jardinero` · `cocina -> cocinero` · `libro -> librero librería` · `pez -> pescador` (`aStem:'pesc'`) · `leche -> lechero`; F3 = the 6 pictured derivations (m); `sizePairs` `gato -> gatito gatote` … >= 8 | F1 default | "familia léxica" (SEP); intruders share letters not root (`flan`, `foca`); never print an article |
| fr | family | `fleur -> fleuriste fleurir` · `jardin -> jardin|i|er jardinage` · `dent -> dentiste dentifrice` · `lait -> lait|i|er laitage` · `pomme -> pommier` · `glace -> glacier glaçon` · `livre -> libraire` · `poisson -> poisson|n|ier` | F1 default, F3, F5 default | `boulanger` NOT in the `pain` family; `fleuve` THE intruder; accents kept in cells |
| it | alterati | `gatto -> gattino gattone` · `casa -> casetta casona` · `libro -> libretto librone` · `palla -> pallina pallone` · `scarpa -> scarpetta scarpone` · `tavolo -> tavolino` · `naso -> nasino nasone`; EXCLUDE `cagnolino pesciolino fiorellino` (irregular) | F1, F3 | one alterato per base per page; `-one` changes gender: never an article; foils `mattino bambino cantina tacchino` |
| pt (BR) | alterati | `gato -> gatinho gatão` · `pato -> patinho` (pictured) · `bola -> bolinha bolão` · `carro -> carrinho carrão` · `sapato -> sapatinho` · `casa -> casinha`; EXCLUDE `casarão boquinha cãozinho pãozinho` from set A | F1, F3 | regular = `-o/-a` drops + `-inho/-inha`; the scale is the ONLY visual cue on F5; foils `galinha cozinha caminho moinho vizinho`; EF02LP cited by the panel |

## 5 Data + gates

`data/b3/compound-words.js`, GENERATED from `i18n/.draft-b3-<loc>.json` by `tools/apply-b3-locale.js` after `tools/validate-b3-draft.js` (the `apply-b2-locale.js` / `validate-b2-draft.js` pattern, both exist (m); the b3 tools are absent (m), owned by the batch's first design; `data/` gitignored, `git add -f`):
```
COMPOUNDS[loc] = {
  shape:'compound'|'family'|'alterati', casing:'keep-first'|'lower', links:['', 'n', 'en', 'er'], linkBoxW:36|90,
  exemplar:'A', units:[{ id:'A', items:[ { a:{vocabKey:'sun', word:'sun'}, aStem:null, link:'',
      b:{vocabKey:'flower', word:'flower'} | {affix:'-ista'},
      whole:{word:'sunflower', vocabKey:'sunflower'|null, scale:1|0.55|1.35}, hubSide:'b', opaque:false, picOpened:true } ] }],
  onePart:[ /* same item shape; a OR b unpictured; whole.vocabKey REQUIRED */ ],
  hubs:[ { hub:{vocabKey:'fish', word:'fish'}, side:'b', satellites:['starfish','angelfish','jellyfish','goldfish'] } ],
  foils:[ { vocabKey:'carpet', word:'carpet', looksLike:['car','pet'] } ],
  sizePairs:[ { base:{vocabKey:'cat', word:'gato'}, small:'gatinho', big:'gatão' } ],
  crossWords:[ ['pande','kage'] ], refuse:{ F1:false, F3:false, F5:false },
  strings:{ 'G2-316':{title,instruction}, F1..F5:{...} } }
```
`whole.vocabKey` REQUIRED for compound items and every `onePart`/`hubs` satellite (the F1/F2/F4/F5 clue); absent ONLY for family/alterati wholes; `a.vocabKey` REQUIRED always; `b.vocabKey` REQUIRED for compound, absent for affixes; `scale` != 1 only for alterati.

**`tools/validate-b3-draft.js` (compound-words block; every rule runs, exit 1 on any).** (1) `whole.word === casing(aStem||a.word) + link + (b.word || affix minus '-')` (poison: `{Sonne, '', Blume -> Sonnenblume}` -> FAIL link; `SonnenBlume` -> FAIL casing); (2) every `vocabKey` resolves in `cache/manifest.json` with `bw:false`, has a locale entry, not in `B2_EXCLUDE`, `picOpened:true`; (3) `link` in `links`; `affix` starts with `-`; (4) each set: 8 items, 16 distinct parts (poison: two `-fish` in A -> FAIL); (5) hubs >= 2 with >= 4 satellites (or `refuse.F5` / `lanes:3` recorded), every satellite a pool or `onePart` whole sharing `hub.word` on `side`; (6) foils pictured, no item's whole, and not `x + link + y` for any two pictured words (poison: foil `sunflower` -> FAIL); (7) `sizePairs` >= 8 where `shape` is alterati or F5 is size, `small`/`big` start with the base stem; (8) 6 x `{title, instruction}`: no worksheet-word, title <= 70 unique in band, instruction <= 150 with an end mark; (9) `refuse.F1` forced for en it pt, `refuse.F3` forced for fr it pt; (10) `crossWords` pairs are two pool parts and the joined word is not a pool whole.

**`tools/gate-compound-words-data.js`** (node, every locale x face x set before a wave; prints items checked per cell, 0 checked = FAIL):
1. rendered `data-lcs-whole` re-derives from the bank, never from the page (poison: stamp `sunflowers` -> FAIL);
2. page uniqueness of parts (poison: `cupcake` + `pancake` on one page -> FAIL);
3. F3 cross-product `a_i + link + b_j` (i != j) not a pictured whole nor in `crossWords` (poison: `pande|kage` + `honning|kage` with `pande|bånd` present -> FAIL, m: da `pande+kage` is real);
4. F1 >= 3 empty and >= 3 non-empty links, box width === `linkBoxW` on every card;
5. F4 exactly 6 `data-lcs-foil` chips from `foils[]`; no foil contains a pool whole;
6. F2 d2: `[data-lcs-seam]` count 0 (poison: the d1 tick at d2 -> FAIL);
7. every `<img>` resolves via `fileUri`; `manifest.themes[t].bw === false` (poison: `animals bw/cat` -> FAIL);
8. de tiles: `a` starts capital, `b` lowercase; other locales all-lower (poison: nl tile `Zon` -> FAIL);
9. F5 satellites distinct, hub word printed once, ghost opacity .55; size rows: `scale` in {0.55, 1.35}, no chip;
10. Baloo 2 advance in F2 cells <= cell - 2 (browser); lane writable width >= 60;
11. pool >= floor per cell (8 / 6 F3 / 6 + 6 F4 / 2 x 4 F5) or the cell is recorded REFUSED, never filled.
Every poison must FAIL; the correct draft is the control. **QA lint** (`qa/lints.js`): every row / card / web stamps `[data-ws-content]`; overflow, palette, min-text 9 px as today.

## 6 SEO plan

| face | title pattern (Germanic / Romance / Nordic+fi) | meta MIDDLE (whole description 120-170; the child's instruction) | coordinate |
|---|---|---|---|
| base | "Compound Words: Picture + Picture" · "Zusammengesetzte Nomen: Bild + Bild" / "Familia de palabras: raíz y terminación" / "Diminutivo e aumentativo" / "Sammansatta ord: bild + bild" · "Yhdyssanat: kuva + kuva" | Name the two pictures, join the two words and write the new word on the line | `{type:'compound-words', mode:null, theme:'', level:<G2 key>}` |
| F1 | "Fugen-s und Fugen-n" / "Tussen-n in samenstellingen" / "Foge-s i sammansatta ord" / "Fuge-e og fuge-s" / "Yhdyssanan alkuosa" / fr "La lettre de liaison" | Both words are printed: write the letter that joins them in the box, or leave it empty, then write the whole word | `mode:'link'` |
| F2 | "Split the Compound Word" · "Zusammengesetzte Wörter zerlegen" / "Busca la raíz" / "Retrouve le mot-base" · "Jaa yhdyssana osiin" | The whole word is printed: draw one line where the second word starts | `mode:'cut'` |
| F3 | "Match the Halves" · "Wortteile verbinden" / "Une la raíz con su familia" · "Yhdistä sanan osat" | Draw a line from each first picture to the picture that finishes the word, then write it | `mode:'match'` |
| F4 | "Compound Detective" · "Echte zusammengesetzte Wörter finden" / "L'intrus de la famille" / "-inho que não é diminutivo" · "Onko se yhdyssana?" | Twelve words, six are made of two words: circle them and write their two parts | `mode:'detect'` |
| F5 | "Word Web: One Word, Many Compounds" · "Wörter mit -blume, -fisch, -ball" / "Aumentativos y diminutivos" / "Nomi alterati: grande o piccolo?" · "Yhdyssanoja sanasta kala" | One word in the middle, four pictures around it: write the four new words (size rows: "big or small: write the word with its ending") | `mode:'web'` (size rows `mode:'size'`) |

Titles <= 70, no worksheet-word, unique per band, no theme; the meta lead is read from shipped bytes (`seo.words.free_printable`; the "free" claim is the standing open item, MEMORY "tier truth"). h1 = title; eyebrow = the G2 label; strand = the **`'Vocabulary Acquisition and Use'` row of `frontend/lib/seo/strand-names.ts:191`** (m: de `Wortschatz untersuchen`, fr `Le lexique`, es `Ampliación del vocabulario`, pt `Ampliação do vocabulário`, it `Lessico`, nl `Woordenschat en woordvorming`, sv `Ord och begrepp`; **da / no / fi entries UNVERIFIED in this row, OPEN 8**). JSON-LD `LearningResource`, `educationalAlignment.targetName` = L.2.4.d (en; L.2.4.c family/alterati) or the framework NAME, no `targetUrl` (§22.1). `topicMeta['compound-words']` + `skill-sentences.en.json` entry via the b3 registrar.

Non-cannibalisation (3-gram Jaccard, `scripts/seo-landing/gate.js` FAIL >= 0.80; *est.*, the gate measures):

| pair | what the copy says differently | est. |
|---|---|---|
| base vs K-231 Build the Word | LETTER tiles make one word there; two PICTURES make a new word here, no tiles | 0.15 |
| F1 vs G1-249 Unscramble the Sentence | a sentence in the wrong order there; two words and the letter between them here | 0.15 |
| F2 vs G1-305 Syllable Division | a syllable is a sound unit; a seam is a MEANING unit; "syllable" never in copy | 0.25 |
| F3 vs K-225 / `visual-matching` | word-to-its-own-picture there; two pictures make a third word here | 0.20 |
| F4 vs G2-315 F2 Rule Detective | rule graphemes there; "is it made of two words?" here | 0.20 |
| F5 vs G1-306 Word Families (R shape) | rime tables (`-at`) there; whole WORDS share one head here; de avoids `Wortfamilie` | 0.25 |
| F5-size vs `comparing-sizes` / `big-small` | sizes compare objects with no writing there; the size is an ENDING the child writes here | 0.15 |
| es/fr family vs G2-315 F5 / `singular-plural` | derivation (new word, new meaning) vs inflection (plural) | 0.20 |

Boundary sentence per shape on every landing: "Two words make a new word with a new meaning" / "One root, many words" / "The ending makes it small or big".

## 7 Hub visibility contract

A face appears under `compound-words` on `/[locale]/worksheets` IFF all four hold (brief, "Hub visibility contract"): (1) `apps['compound-words']` exists in `frontend/config/topics-taxonomy.json` (absent (m); missing = rendered NOWHERE in the rail); (2) `axes['exercise-type']['compound-words']` has `slug` + `name` in all 11 locales (§1 slugs); (3) exactly one landing per face per locale with `coordinate.type === 'compound-words'` verbatim, the G2 level key of §1, `theme:''`, a unique slug, `canonicalDeckSlug` = the published deck; a face REFUSED in a locale has NO landing there and the expectation is lowered explicitly, never padded; (4) the landing JSON committed AND deployed (per-process cache).

Gate: `node scripts/verify-hub-type-rows.js --keys=compound-words`. Script absent (m) (nearest siblings `scripts/verify-worksheets-hub-order.js`, `verify-worksheets-hub-render.js`); write + poison-test it (a short locale and a wrong `coordinate.type` must FAIL) before `apps['compound-words']` lands.

**Expected rows per locale after the refusals recorded in §3:**

| loc | rows | refused | conditional (panel data lifts) |
|---|---|---|---|
| en | 5 | F1 | none |
| de nl da | 6 | none | da F5 `lanes:3` if hubs of 3 stand |
| sv no fi | 5 -> 6 | F1 until >= 3 linked one-part items | F1 |
| es | 5 -> 6 | F1 (default) | F1 with >= 3 inserted-joint items |
| fr | 3 -> 5 | F3; F1 + F5 (default) | F1, F5 |
| it pt | 4 | F1, F3 | none |

Design total = 47 rows (en 5 · de 6 · nl 6 · sv 5 · da 6 · no 5 · fi 5 · es 5 · fr 3 · it 4 · pt 4), ceiling 55 with every conditional lifted. ⚠ F1 and F3 each carry exactly 3 firm refusals; a 4th firm F1 refusal (es AND fr) triggers OPEN 4.
