# G1-352 `pronouns` : PEDAGOGY + CONTENT (nt10-D, 2026-09-21)

Read: `_ROLE-PEDAGOGY.md`, `_STUDIO-BRIEF.md`, `_SUBSTRATE.md`, `_PANEL-FINDINGS.md` §8 + the lock (line 12) + Facts 7/8, `K-354-human-body.md` + `_work/G1-350-pedagogy.md` (density + the objForms idiom), `../b3-designs/G2-317-verb-forms.md` §1-2, `types/g2/G2-317-verb-forms.js` (`pictureOf`, `pickRows`, `VERIFY_FACE`), `data/b3/verb-forms.js` (`bank('verb-forms', loc).columns` = the pronoun labels it prints as GIVENS), `data/b2/articles.js` + `types/k/K-288-articles.js` (`articleChips` idiom, `keyFor -> null` refusal, the `sortWords` bin layout + its verify branch), `data/b2/sentences.js` (8 untagged names per locale; its frames have NO pronoun slot), `templates/components-b2.js` (`articleChips`, `rulingBlock`, `wordBank`, `pillChoice`), `lib/b3-picture-index.js` (`candidates`), `lib/b2-common.js` (`entriesFor`, `vocab`, `B2_EXCLUDE`), `scripts/seo-landing/gen-b2var-landings.js` (`LEVEL_KEYS`), `frontend/lib/seo/strand-names.ts` (the `Language` row), `frontend/config/topics-taxonomy.json`. Every number below was measured 2026-09-21 with read-only node (scratch `G1-352-sheet.js`; the two contact sheets `G1-352-people-{1,2}.png`, 200 px tiles, every person picture OPENED by me). *est.* = the engineer measures in the real render with the shell fonts. No em-dashes.

**Boundary (load-bearing).** This family owns exactly one thing nobody has: **the PRONOUN as the answer, chosen for a NAMED PERSON (or two) from a portrait + name caption, in nominative subject position (base, F1, F4, F5), as an anaphor across two sentences (F2), and as the possessive of a named owner (F3).** It is NOT G2-317 `verb-forms` (pronoun columns `ich du er/sie/es ...` are PRINTED GIVEN labels and the answer is a VERB form; fr "pronoms sujets avec conjugaison" is theirs; no face here ever asks a verb form, every verb is a panel literal that carries NUMBER as the cue), NOT K-288 / K-306 / K-307 / G1-292 `articles` (the answer is der/die/das off the vocab gender; `articles.name.sv` = "En eller ett" owns den/det, so no face here asks a pronoun for a THING except the de/nl re-target where the object noun is captioned and the pronoun er/sie/es or hij/zij/het is the answer, a different answer from the article), NOT `word-classes` G2-275/285/286/287 + G1-293/300 (chips sorted by CLASS noun/verb/adjective; no face here sorts by class, F4 sorts NAMES by the pronoun they take), NOT G1-350 `cloze` (a noun gap; here the gap is never a noun), NOT G1-249 (word order), NOT G2-274 (capitals + end marks: the sentence-initial capital of the pronoun is graded case-insensitively here), NOT K-225 / K-235. Structural fences the validator enforces: every answer literal is a member of the locale's fixed pronoun table (`bank.chips` / `bank.initial` / `bank.possessive`), never a noun, article or verb; every item's key is derived from `depicted` tags of OPENED portraits + the panel's `gender` tag on names (object items only in de/nl, keyed off the vocab gender code); the occupation noun is NEVER printed (a portrait carries a NAME caption); no frame text is a `SENTENCES[loc].frames` literal; "I", "you", "we", formal "Sie/usted/você", "on", "a gente", the object forms (him/her/dem/de/dem, no `de/dem`) and reflexive sin/sitt never appear as a chip. Visual signature: a portrait (or two side by side) with a name caption under it and a row of identical white chips (base), or a two-line lane "Mia reads. / ___ reads." with the pronoun bank on top (F1).

## A. Identity

| loc | genre head (the panels; title = head; ASCII slug) | school year per face (`LEVEL_KEYS`) | national strand (framework NAME; the `Language` row of `strand-names.ts` already carries the literal, m) | CCSS (en only, honest) |
|---|---|---|---|---|
| en | **Personal Pronouns** (`personal-pronouns`; F3 owns the second head **Possessive Pronouns**; "subject pronouns" META; 123, 14/14, HARD at head (K5 / Education.com / Twinkl), WINNABLE "replace the noun with a pronoun worksheet 1st grade", "possessive pronouns worksheet 1st grade pictures") | base / F1 / F4 grade 1 (`grade-1`); F2 / F3 / F5 grade 2 (`grade-2`) | Language | **L.1.1.d** (personal + possessive pronouns) on every face; F5 also L.1.1.j (complete sentences). No G2 pronoun code exists (L.3.1.f agreement is G3 and NOT claimed). |
| de | **Personalpronomen** (`personalpronomen`; "Pronomen Klasse 3" + "Fürwörter" META only, "Fürwörter" appears in no harvest line; 96, 14/14; the genre verb "einsetzen" is F1's title) | base / F1 / F2 / F4 / F5 2. Klasse (`2-klasse`); **F3 3. Klasse (`3-klasse`)** (Possessivartikel mit Endung sits at Klasse 3-4) | Sprache untersuchen (Lehrplan "Sprache und Sprachgebrauch untersuchen", Wortarten) | n/a |
| es (MX) | **Pronombres personales** (`pronombres-personales`; "los pronombres" META; 103 but 18 are "en inglés" ELT; WINNABLE "ficha de pronombres personales primer grado para imprimir") | base / F1 / F4 primer grado (`primer-grado`); F2 / F5 segundo grado (`segundo-grado`); **F3 REFUSED** | Reflexión sobre la lengua (SEP / NEM) | n/a |
| pt (BR) | **Pronomes pessoais do caso reto** (`pronomes-pessoais-do-caso-reto`; the BR school term; 58, band 9; WINNABLE "atividade pronomes pessoais 3o ano para imprimir") | **3º ano (`3o-ano`) on EVERY face** (BNCC EF03LP: "pronomes pessoais" is taught in 3º ano, band words 3o ano x5 vs 1/2 ano x0; the landing says so in every pt face) | Análise linguística/semiótica (BNCC EF03LP) | n/a |
| fr | **Les pronoms personnels sujets** (`pronoms-personnels-sujets`; "les pronoms personnels" META; 101, band 58, HARD (Bout de Gomme, Maître Lucas); WINNABLE "pronoms personnels sujets cp fiche à imprimer", "étiquettes pronoms personnels à imprimer" = F4 verbatim) | base / F1 / F4 CE1 (`ce1`; the measured "cp" tail is served by the CE1 page's copy naming CP-CE1); F2 / F3 / F5 CE2 (`ce2`) | Étude de la langue (programmes officiels cycle 2) | n/a |
| it | **I pronomi personali soggetto** (`pronomi-personali-soggetto`; "i pronomi personali" META; 81, band 31; WINNABLE "esercizi sui pronomi personali classe seconda scheda da stampare") | classe seconda (`classe-seconda`) on every face | Riflessione linguistica (Indicazioni nazionali) | n/a |
| nl | **Hij, zij of het** (`hij-zij-of-het`; the panel's re-target: "persoonlijke voornaamwoorden" is groep 6-8 demand, C in K-3, and the title is never "voornaamwoorden" alone; "verwijswoorden" is nl's real groep 4 head and F2's title; 27, net ~10; the landing says "groep 4" and expects little traffic) | groep 4 (`groep-4`) on every face | Taal (SLO kerndoelen) | n/a |
| sv | **Pronomen: han, hon eller de** (`pronomen-han-hon-eller-de`; head "Pronomen" + face head "han, hon, den, det" measured; den/det stays OFF (persons only, `articles` owns den/det); 21 (16); WINNABLE "pronomen arbetsblad"; Lgr22 ordklasser is åk 4-6 but åk 2-3 teachers use it as reading support) [NSR] | base / F1 / F4 åk 2 (`ak-2`); F2 / F3 / F5 åk 3 (`ak-3`) | Språkliga strukturer och normer (Lgr22) [NSR] | n/a |
| da | **Personlige stedord** (`personlige-stedord`; "pronominer" META; 20 (14); WINNABLE "stedord opgaver") `[NSR-FLAG][da]` stedord vs pronominer | base / F1 / F4 2. klasse (`2-klasse`); F2 / F3 / F5 3. klasse (`3-klasse`) | Sprog og sprogbrug (Fælles Mål) [NSR] | n/a |
| no | **Personlige pronomen** (`personlige-pronomen`; bokmål; a nynorsk "personlege pronomen" sibling is REFUSED as a separate authoring, recorded not faked; 30 (13), the largest Nordic cell; WINNABLE "pronomen oppgaver for barneskole") | base / F1 / F4 3. trinn (`3-trinn`); F2 / F3 / F5 4. trinn (`4-trinn`) | Språket som system og mulighet (LK20) [NSR] | n/a |
| fi | **Hän vai he?** (`han-vai-he`; "Persoonapronominit" META only (24 hits, almost all foreign-language); NEVER "Persoonamuodot" (= `verb-forms.name.fi`); the NUMBER re-target: hän is genderless) `[NSR-FLAG][fi]` | base / F1 / F4 1. luokka (`1-luokka`); F2 / F5 2. luokka (`2-luokka`); **F3 REFUSED** | Kielen rakenteet ja oikeinkirjoitus (OPS 2014) [NSR] | n/a |

Measured: `apps.pronouns` ABSENT, `axes['exercise-type'].pronouns` ABSENT (register with the b4 shape, `default_subject:'letters'`, `default_age_range:'6-8'`); the 11 slugs above grepped against every `axes.*` slug in every locale: 0 collisions (`word-classes` slugs `wortarten / ordklasser / sanaluokat / woordsoorten / nature-des-mots` are neighbours, not collisions). `verb-forms.name` per locale (m): "Verbformen", "Conjugar verbos", "Conjugaison au présent", "Il presente dei verbi", "Werkwoorden vervoegen", "Verb: presens och preteritum", "Udsagnsord: nutid og datid", "Verbin persoonamuodot", "Conjugação de verbos" = the titles no pronoun face may echo.

**Theme axis: FIXED-PICTURE, THEMELESS in the wave** (`themeAxis:{applicable:false}`, landings carry `coordinate.theme:''`). The people pool is a curated, OPENED allowlist pinned per key (`pic:{theme,noun}`, resolved through `lib/b3-picture-index.js candidates(key, loc)` exactly as G2-317 `pictureOf`; a pinned picture the colour index does not carry for the locale = REFUSAL, never a substitute). Measured 2026-09-21: `occupations` = 49 files / 35 vocab keys (14 files have no vocab and are unusable as keys: `bus_driver_2 construction_worker crossing_guard crossing_guard_2 delivery_driver doctor_2 flight_attendant mail_carrier mail_carrier_2 office_worker park_ranger police_officer sanitation_worker truck_driver`). **All 57 candidate pictures opened** (49 occupations + `classroom/{student,teacher,librarian}` + `toys/{girl,baby,baby_girl}` + `hospital/{doctor,nurse}`); depicted-sex tags:

| depicted | keys (pinned theme/noun) | n |
|---|---|---|
| `m` | occupations/{actor, architect, author, baker, barber, carpenter, chef, coach, detective (a boy), doctor, farmer, firefighter (face under the helmet reads male: the panel confirms), gardener, janitor, judge, musician, pharmacist, photographer, singer} + classroom/student (a boy with a book) | **20** |
| `f` | occupations/{athlete, ballerina, cashier, florist, librarian, nurse, paramedic (a girl in a red helmet), tailor, teacher, veterinarian, waitress} + toys/girl | **12** |
| `ambiguous` (EXCLUDED as persons) | occupations/artist (a small child seen from behind at an easel), astronaut (helmet, no face), cook (a young person stirring, sex not legible at 44-80 px), mechanic (the car dominates, the figure is tiny), pilot (a plane with a dot of a pilot), toys/baby (a baby in a nappy) | 6 |

Second files of the same key (`hospital/doctor` bearded m, `hospital/nurse` f, `classroom/teacher` f, `classroom/librarian` f, `music/singer` NOT opened) are NOT pinned; the bank pins ONE theme per key so the seed is deterministic. `vocab gender is grammatical, not depicted` holds in the data: nurse = de Krankenschwester f / fr Infirmière f / en none; teacher = de Lehrer m / fr Professeur m for a WOMAN portrait; waitress = f in every gendered locale; cashier pt Caixa f / es Cajero m for the same woman. The occupation noun is never printed, so none of this reaches the page; `depicted` is the only person key. **Unambiguous portraits available: 32 (20 m + 12 f)**; a base d2 page uses 11 (5 singles + 3 pairs), the f pool is never drawn below 12 - 5 = 7 spare. `objForms` coverage of the occupation keys is 0 of 35 in every locale but fi (5): irrelevant, no face prints the noun.

**Object pictures (de / nl re-target only)** come from the reviewed pool (nouns with a colour picture + an `objForms` entry, m): de 131 = m 53 / f 48 / **n 30** (`airplane armadillo bicycle boat calf camel canoe car chick chicken duckling foal hippopotamus horse jet kangaroo lamb llama meerkat motorcycle pig rabbit rhinoceros sailboat ship skateboard sloth submarine taxi zebra`); nl 103 = d 91 / **h 12** (`airplane armadillo chick duckling hippopotamus horse lamb pig rabbit sheep ship van`). Both clear the 2-per-page neuter floor with margin; the de/nl panels pin 12 object keys each (4 per gender code in de; 6 d + 6 h in nl), opened, from strong themes (vehicles / farm animals / zoo animals). No `unitAxis`.

**Names are the type's own tagged list, not `sentences.js`.** `SENTENCES[loc].names` = 8 per locale, alternating f/m by construction but UNTAGGED (en Mia Ben Emma Leo Anna Tom Lily Max; de Emma Ben Mia Finn Lina Paul Lea Noah; es Sofía Mateo Lucía Diego Valentina Hugo Camila Martín; fr Léa Hugo Chloé Louis Emma Nathan Inès Jules; pt Sofia Miguel Alice Davi Laura Pedro Helena Lucas; it Sofia Leonardo Giulia Francesco Aurora Lorenzo Alice Matteo; nl Emma Daan Julia Sem Tess Finn Lotte Luuk; sv Elsa Hugo Alma Liam Maja Oscar Vera Elias; da Ida William Freja Oscar Alma Noah Clara Emil; no Nora Jakob Emma Oliver Ella Aksel Sofie Theodor; fi Aino Eeli Sofia Onni Aada Väinö Helmi Leo). A page needs up to 12 distinct names (F2: 6 pairs), so `data/b4/pronouns.js` carries **12 names per locale = the 8 above + 4 panel additions, each `{name, gender:'f'|'m'}`** (EN source: + Zoe f, Ava f, Jack m, Eli m); the validator requires exactly 6 f + 6 m and refuses a name the panel marks `ambiguousInLocale:true` (sv Alex, fi Kim class). Names are NEVER derived from list position.

**The one rule that locks the type.** Every item is a set of REFERENTS (1 or 2 named portraits; in de/nl also 1 captioned object) whose key is computed, never typed: `keyOf(refs)` = `m1` (one m) | `f1` (one f) | `mp` (two m) | `fp` (two f) | `xp` (one of each) | `n1` / `m1` / `f1` for an object off the vocab gender code (de m/f/n -> er/sie/es; nl d -> hij, h -> het, the panel's `het meisje` ruling sits on the `girl` key as a data override) | `p` for any pair in fi. The answer is `bank.map[face][key]` -> a chip index into the fixed table, or `null` = the item is REFUSED for that locale (fi `mp/fp/xp` all map to `he`; de/nl `mp/fp/xp` map to `null` on the base so the sie/zij collision cannot reach a chip, and to the literal "sie"/"zij" on F1 where the verb proves the number). Frames never inflect: the panel writes the singular and the plural variant of every sentence as two whole literals with a `{subj}` token that the code fills with `{name}` or `{name} {and} {name}` (the `and` is a locale literal); the pronoun printed in F2 is `bank.initial[key]` (sentence-initial form, "Er" / "Hän"), never a lowercase chip capitalised by code.

## B. The six faces

Common: `templates/components-b4/pronouns.js` (`portraitCard`, `pairPortrait`, `nameCaption`, `replaceLane`, `anaphoraCard`, `ownerCard`, `nameBins`, `rewriteLane`), reusing `articleChips` (components-b2: identical white `.ws-achip` chips, `data-lcs-chip=<i>`, `data-lcs-correct` on the key, the K-288 stamp contract), `cardGrid`, `wordBank`, `rulingBlock`, K-288's `sortWords` bin idiom (`.ws-bin` + ruled `line`s, `data-lcs-sortbin` / `data-lcs-sorthead`). Every face is a CODE face on ONE additive `layout` knob (`d.layout` undefined = the base path, byte-identical; a face stamps `data-lcs-layout` only when declared; the G1-307 / K-288 shape) with its own `verify()` branch; guards key on the CONFIG (`d.cards`, `d.pairs`, `d.bank`, `d.bins`, `d.ruling`, `d.objects`), never on the level index. Resolved d2 configs are pairwise distinct (`tools/gate-variation-distinct.js --batch=b4`). Chip order per locale is FIXED and canonical on every card of a page (K-288 verify: "chip order differs (position leak)"), chips identical apart from text. Portraits >= 44 (G1) / 36 (G2), single 72-80, pair 56-64 each, name caption Nunito 800 16-18. Density G1 items [6, 12], G2 [8, 16].

**Pronoun tables (the bank's `chips`, in the FIXED printed order; `initial` = sentence-initial forms for F1's bank / F2's text):**

| loc | base / F4 chips (3-4; de/nl re-targeted) | F1 bank (`initial`, the gap is sentence-initial) | key map on the base | notes |
|---|---|---|---|---|
| en | he · she · they | He · She · They | m1 0 · f1 1 · mp/fp/xp 2 | never "it" (persons only), never singular they |
| de | er · sie · es (objects supply es AND some er/sie) | Er · Sie | m1/obj-m 0 · f1/obj-f 1 · obj-n 2; **mp/fp/xp = null on the base** | "Sie" formal never; F1 pl rows -> "Sie" proven by the plural verb literal |
| es | él · ella · ellos · ellas | Él · Ella · Ellos · Ellas | m1 0 · f1 1 · mp/xp 2 · fp 3 | usted/ustedes never; `y` -> `e` before i-/hi- names (validator refuses such pairs) |
| pt | ele · ela · eles · elas | Ele · Ela · Eles · Elas | m1 0 · f1 1 · mp/xp 2 · fp 3 | você / a gente never; "do caso reto" in the base title |
| fr | il · elle · ils · elles | Il · Elle · Ils · Elles | m1 0 · f1 1 · mp/xp 2 · fp 3 | elle/elles homophones -> the pair picture shows the count; "on" never |
| it | lui · lei · loro (or egli · ella · loro: the it panel picks ONE for the whole type and says so in the landing) | Lui · Lei · Loro | m1 0 · f1 1 · mp/fp/xp 2 | loro genderless: the pair does not test gender in it |
| nl | hij · zij · het | Hij · Zij | m1/obj-d 0 · f1 1 · obj-h 2; **mp/fp/xp = null on the base** | `het meisje -> zij` in practice: the panel rules the `girl` key (data override `keyOverride.girl`) |
| sv | han · hon · de (+ optional `hen` literal, never a default chip [NSR]) | Han · Hon · De | m1 0 · f1 1 · mp/fp/xp 2 | den/det OFF (things belong to `articles`); never «grupp» in copy |
| da | han · hun · de | Han · Hun · De | same | "stedord" title, "pronominer" meta [NSR] |
| no | han · hun · de | Han · Hun · De | same | de/dem object form above band, never a chip; bokmål only |
| fi | hän · he | Hän · He | m1/f1 0 · mp/fp/xp 1 | NUMBER re-target; "hän vai se" is the WRITTEN norm and is served by the copy, never by an animal item (persons only) [NSR] |

### Base: Personal Pronouns (G1, `G1-352`, layout undefined)
**Move:** LOOK at a named person (or two) and CIRCLE the pronoun that stands for the name: the child maps a depicted person + name to he/she and a pair to they (the K.G.A-style circle-the-chip verb of K-288, applied to a person instead of a noun; de/nl: er/sie/es or hij/zij/het for named persons AND captioned things, the Klasse 2 / groep 4 "das Kind -> es" task).
**Child:** "Look at the person and read the name. Circle the word we use instead of the name." (86; apparatus: person, name, words.)
**Params:** d1 `{cards:6, cols:2, rows:3, singles:4, pairs:2, pic:96, pairPic:72, chipW:96, chipH:52, chipFont:26}` · **d2 (ships)** `{cards:8, cols:2, rows:4, singles:5, pairs:3, pic:80, pairPic:60, chipW:84, chipH:48, chipFont:24, mixFloor:2}` (fr/es/pt: the 3 pairs are one each of mp/fp/xp; en/it/sv/da/no: pairs any; fi: pairs 3 -> `he`, singles 5 -> `hän`) · **de / nl d2** `{cards:8, singles:5, pairs:0, objects:3, objectCaption:'bare', ...}` (5 named persons + 3 captioned objects, >= 1 neuter; caption = the noun WITHOUT its article at d2, the article only at d1 `objectCaption:'article'`, so the child recalls the gender, which is the Klasse 2 move; K-288 asks the article itself, so the answers never coincide) · d3 `{cards:10, cols:2, rows:5, singles:6, pairs:4, pic:64, chipH:44, chipFont:22}` (unpublished). Card *est.*: stage = portrait 80 (pair: 2 x 60 + gap 8) + caption 22 + chips 48 + gaps 12 = 162; 4 rows x 162 + 3 x 12 = 684 <= 722 (fi 677 stack: `pic:76` or `chipH:44`, the engineer measures). Composer: sample `singles` distinct portrait keys with distinct names, both sexes present (>= 2 m, >= 2 f); sample `pairs` pairs of distinct portraits + names (no portrait or name twice on the page: 11 names <= 12); every chip key appears >= `mixFloor` times over the page (K-288's `okMix`); de/nl: object keys pinned from the panel's 12, every chip key >= 2; shuffle cards; chips in canonical order on every card.
**verify():** root `[data-lcs-pron][data-ws-content]` (no `data-lcs-layout`); per card `data-lcs-item data-lcs-key=<m1|f1|mp|fp|xp|n1|...> data-lcs-refs=<key,key> data-lcs-names=<a|b> data-lcs-chip-key=<i>`; chips via `articleChips` (`data-lcs-chip`, `data-lcs-label`, `data-lcs-correct`). Re-derived on the render: `cards` items; exactly one `data-lcs-correct` chip per card and its index === `data-lcs-chip-key`; chip labels identical in order on every card (position leak) and styled identically; caption text is a bank NAME (node: `names[loc]` membership) or, in de/nl, a bank object literal, and NEVER an occupation noun (node: the caption is not any locale singular of the pinned key's vocab entry) and never a chip label; single card = 1 `img` >= 44 px, pair card = 2 `img`s >= 44 each with two DIFFERENT names joined by the locale `and`; no portrait `src` twice on the page; every chip key appears >= `mixFloor`; nothing pre-marked. Node cross-check: `keyOf(refs)` from the bank's `depicted` + `gender` tags === the stamped key, `bank.map.base[key]` === the stamped chip index (poison: a `nurse` portrait tagged `m` -> FAIL against the OPENED tag; `Mia` keyed `m1` -> FAIL).
**Refusals:** none as a face. de/nl item-level: pairs never on the base (map null). fi: no object items (persons only, number).
**Query face:** the bare head. en "personal pronouns worksheet he she they" · de "Personalpronomen Klasse 2 Bilder" · es "pronombres personales primer grado" · pt "pronomes pessoais do caso reto 3o ano" · fr "pronoms personnels sujets CE1 fiche" · it "pronomi personali soggetto classe seconda" · nl "hij zij of het groep 4" · sv "pronomen han hon de" · da "personlige stedord" · no "personlige pronomen oppgaver" · fi "hän vai he".

### F1: Replace the Name with a Pronoun (G1, `G1-3xx TBD`, CODE `layout:'replace'`)
**Move:** READ a sentence whose subject is a name (or two names), then WRITE the pronoun that replaces the subject in the copy of the sentence below it, from the fixed pronoun bank (the de "einsetzen" / fr "sujets" / pt "do caso reto" genre; the verb literal carries the number, so de "Sie" for a pair and nl "Zij" are proven by the reader, which is why this face and not the base carries the de/nl plural).
**Child:** "Read the sentence. Write the word from the bank that replaces the name in the second sentence." (95; apparatus: sentence, bank, second sentence.)
**Params:** d1 `{layout:'replace', rows:6, bank:true, singles:4, pairs:2, pic:56, fontPx:19, blankW:96}` · **d2** `{layout:'replace', rows:8, bank:true, singles:5, pairs:3, pic:48, fontPx:18, blankW:96, textW:575, laneMin:66}` · d3 `{layout:'replace', rows:8, bank:false, singles:4, pairs:4}` (unpublished). Lane *est.*: `.ws-lane` padding 4 16 -> inner 639: `[img 48][line 1: subject-filled sentence 18 px][line 2: `.ws-blank--short` 96 px + the rest]` = 8 + 22 + 6 + 30 + 8 = 74 at `minmax(66px,1fr)`; bank 60 + 10 + 8 x 74 + 7 x 6 = 704 <= 722 (the fi 677 stack: `rows:7` or `laneMin:60`, engineer). Frames: the panel's `frames[]` (12 per locale), each `{id, sg:'{subj} liest ein Buch.', pl:'{subj} lesen ein Buch.', maxLine}`; `{subj}` MUST be the first token (validator), so the gap is sentence-initial and the bank prints `initial` forms; line 2 = the same literal with `{subj}` -> the blank. The composer fills `{subj}` with one name (singles) or `{name} {and} {name}` (pairs), distinct names over the page (11 <= 12), distinct frames, both sexes among singles, in fr/es/pt the 3 pairs = mp + fp + xp, in de/nl the pairs answer "Sie"/"Zij" (map `F1`: mp/fp/xp -> the she-literal index) and singles >= 2 "Er"/"Hij"; the line-1 subject gets the row's portrait(s) at 48 px as the gender cue (the name is primary, the picture secondary, per the panel's quality rule).
**verify():** stamps per lane `data-lcs-frame data-lcs-num=sg|pl data-lcs-key data-lcs-answer=<initial literal>`; `data-lcs-bank-word` per bank pill. Re-derived: `rows` lanes; line 1 starts with a bank name (node); line 2 starts with an EMPTY `.ws-blank` and its remaining text === line 1 minus the subject phrase (string equality after stripping the subject, node: `sg`/`pl` literal minus `{subj}`); the answer literal is NOT printed in either line (word boundary, case-insensitive; poison: "Sie liest ein Buch." on line 1 -> FAIL); the bank set === `bank.initial` (the whole fixed table, deranged, never sorted by answer frequency); a `pl` lane has 2 portraits + 2 names, an `sg` lane 1 + 1; de/nl: >= 2 lanes keyed to the he-literal; portraits >= 44; nothing pre-written.
**Refusals:** none (every locale's table has >= 2 initial forms; fi Hän/He). es pairs whose second name starts with i-/hi- are skipped by the composer (the `y -> e` rule lives in the data as `andBefore:{i:'e'}`, never in code).
**Query face:** en "replace the noun with a pronoun worksheet 1st grade" (measured) · de **"Personalpronomen einsetzen Klasse 2"** (measured) · es "cambia el nombre por un pronombre" · pt "substitua o nome pelo pronome" · fr "remplace le prénom par un pronom" · it "sostituisci il nome con un pronome" · nl **"woorden vervangen door hij of zij groep 4"** (measured) · sv "byt ut namnet mot ett pronomen" · da "erstat navnet med et stedord" · no "bytt ut navnet med et pronomen" · fi "korvaa nimi pronominilla".

### F2: Who Is He? Pronouns in Sentences (G2, `G2-3xx TBD`, CODE `layout:'anaphora'`)
**Move:** RESOLVE an anaphor: two named people (one f, one m; in fi one person and a pair) are introduced in a first sentence; two further sentences each start with a pronoun; the child draws a line from each pronoun to the name it refers to (nl's real head "verwijswoorden"; the G2 reading move: the pronoun's gender/number is the ONLY key, the sentence content is neutral by validator rule).
**Child:** "Read the sentences. Draw a line from each word in a box to the name it stands for." (86; apparatus: sentences, boxes, names.)
**Params:** d1 `{layout:'anaphora', pairs:4, sentencesPerPair:2, pic:64, fontPx:19}` · **d2** `{layout:'anaphora', pairs:6, sentencesPerPair:2, pic:56, fontPx:18, cardH:108, dotR:6}` (12 line items in [8, 16]; 6 pairs x 2 names = 12 names = the whole tagged list, so the composer shuffles which name plays which role) · d3 `{layout:'anaphora', pairs:6, sentencesPerPair:3, distractorName:true}` (a third named person of the SAME sex as one referent appears in the intro but no sentence refers to it; unpublished). Card *est.*: `[two portraits 56 with name captions, stacked, 108 high] | [intro line 18 px + 2 pronoun sentences 18 px, the pronoun in a `.ws-nchip`-style box at the line start, a `.ws-match-dot` at the box and at each name]`; 6 x 108 + 5 x 12 = 708 <= 722 (fi 677: `cardH:100`, engineer). Frames: the panel's `anaphora[]` (8 per locale): `{id, intro:'{a} und {b} sind im Park.', s:[{text:'{P} hat einen Ball.', key:'sg'}, {text:'{P} hat einen Drachen.', key:'sg'}]}`; non-fi: both sentences singular, the code assigns sentence 1 to one referent and sentence 2 to the other at random and prints `{P}` = `initial[m1|f1]`; fi: `s` carries one `key:'sg'` ("{P} leikkii pihalla.") and one `key:'pl'` ("{P} leikkivät pihalla."), referents = one single + one pair (`{a}` = a name, `{b}` = "{name} ja {name}"), `{P}` = Hän / He. `intro` names both referents with `{a}` / `{b}` (the pair order random). Validator: the two sentences of a frame are `contentNeutral:true` (the panel signs that neither sentence's content identifies a sex or a number; the code cannot check semantics, the poison is a frame "{P} trägt ein Kleid." signed false).
**verify():** stamps per card `data-lcs-pair data-lcs-a=<key> data-lcs-b=<key>`; per sentence `data-lcs-anaphor data-lcs-ref=a|b data-lcs-pronoun=<literal>`; per name `data-lcs-target=a|b`. Re-derived: `pairs` cards x 2 sentences; on each card the two `data-lcs-pronoun` literals DIFFER (poison: two "Er" sentences -> FAIL: no single solution); each pronoun literal === `initial[keyOf(ref)]` (node); the intro contains both names (node: `names` membership, joined by `and` for a fi pair); no name appears in a pronoun sentence; every name has a dot, every box has a dot, no line pre-drawn; the two referents on a card have different keys (f1 vs m1; fi sg vs p); 12 distinct names over the page; portraits >= 36.
**Refusals:** none as a face (every locale distinguishes he/she, fi hän/he). it: if the panel picks egli/ella, F2 prints them (the lock: one choice for the whole type).
**Query face:** en "pronoun antecedent worksheet 2nd grade" · de "Pronomen im Satz: Wer ist gemeint?" · es "¿a quién se refiere el pronombre?" · pt "a quem o pronome se refere" · fr "à qui renvoie le pronom" · it "a chi si riferisce il pronome" · nl **"verwijswoorden groep 4"** (nl's measured head; the F2 title carries it) · sv "vem är han? pronomen i meningen" · da "hvem er han? stedord i sætningen" · no "hvem er han? pronomen i setningen" · fi "kuka on hän? pronomini lauseessa".

### F3: Possessive Pronouns (G2; de G3, `G2-3xx TBD`, CODE `layout:'possessive'`)
**Move:** CHOOSE the possessive that fits: a named owner (or two) + a pictured THING; the sentence "This is Mia. This is ___ ball." takes his/her/their (en, nl, pt dele/dela/deles/delas, sv/da/no hans/hennes/deras), the de literal WITH its nominative ending (sein/seine/ihr/ihre, keyed by owner AND the thing's gender: Klasse 3), or, re-targeted where the possessive agrees with the THING, fr son/sa/leur and it suo/sua/loro (owner number + thing gender).
**Child:** "Look at who owns the thing. Circle the word that shows it belongs to that person." (85; apparatus: person, thing, words.)
**Params:** d1 `{layout:'possessive', cards:6, cols:2, rows:3, pairs:1, pic:72, thingPx:64, chipH:52}` · **d2** `{layout:'possessive', cards:8, cols:2, rows:4, singles:5, pairs:3, pic:64, thingPx:56, chipW:84, chipH:48, chipFont:22, mixFloor:2}` (de: `pairs:0, singles:8`, chips 4, mix >= 2 per chip; fr/it: `pairs:3` so leur/loro appear, thing gender mixed >= 3 m / >= 3 f) · d3 `{layout:'possessive', cards:8, write:true}` (write the possessive on a line instead of circling; unpublished). Card *est.*: `[owner portrait 64 + caption] [thing 56]` on one stage row 70 + frame line 18 px (two short sentences, the second with a 84 px blank) + chips 48 = 162; 4 x 162 + 36 = 684 <= 722. Data: `possessive.chips[loc]` (fixed order: en his her their · nl zijn haar hun · pt dele dela deles delas · sv hans hennes deras · da hans hendes deres · no hans hennes deres · de sein seine ihr ihre · fr son sa leur · it suo sua loro), `possessive.things[]` = 12 panel-pinned object keys with a colour picture (opened) each carrying `forms:{m1, f1, p}` -> chip index or null: en/nl/pt/sv/da/no forms are noun-independent (`byOwner`); de per noun by nominative agreement (Ball m: sein/ihr; Puppe f: seine/ihre; Auto n: sein/ihr; `p:null` because ihr = her AND their); fr per noun by gender (m: son/son/leur; f: sa/sa/leur; vowel-initial f nouns REFUSED: "son amie"); it (m: suo/suo/loro; f: sua/sua/loro; the frame prints the article "Questo è il ___ pallone." so the agreement cue is on the page). The frame literal per locale is ONE two-sentence template with `{subj}` + `{thing}`: the thing's literal comes from `objForms.unique`/`sg` (de nominative: the bare singular with capital, NEVER `objForms.unique`, the accusative trap of G1-350 §C) and is printed WITHOUT its article in de (the chip carries the ending, the child must know the gender) and WITH it in it/fr where the article is the cue. Answer = `forms[thing][keyOf(owner)]`.
**verify():** stamps per card `data-lcs-item data-lcs-owner=<key> data-lcs-thing=<vocabKey> data-lcs-chip-key=<i>`; re-derived: one correct chip whose index === the stamp; the thing picture `src` = `fileUri(pic.theme, pic.noun)` of the stamped key; the printed thing literal === `answerFor(loc, thing, form)` (node) and the chip literal never occurs in the sentence text; owner portrait(s) + name(s) as on the base; chip order canonical on every card; every chip appears >= `mixFloor`; de: no pair owners (poison: a de card with two names -> FAIL), 4 chips; fr/it: >= 1 pair owner AND both thing genders present (else son/sa or suo/sua never discriminates); no article printed before the blank in de (poison: "Das ist der ___ Ball." -> FAIL).
**Refusals:** **es REFUSED (`su` is invariant for his/her/their: no chip discriminates) + fi REFUSED (`hänen`/`heidän` + the -nsa suffix: the panel's ruling, not a printable K-2 task)** = the recorded 2 rows (the hub matrix expects 5 rows in es and fi; the landing never promises possessives in those locales even though es measured x3). de sits at 3. Klasse honestly (`3-klasse`). sv/da/no: never sin/sitt/sin as a chip (reflexive is åk 3+ and would make two answers correct).
**Query face:** en **"possessive pronouns worksheet 1st grade pictures"** (measured; the second en head) · de "Possessivpronomen sein oder ihr Klasse 3" ("besitzanzeigende Fürwörter" META) · pt "dele ou dela atividade" · fr "son sa ou leur exercices CE2" · it "suo o sua esercizi classe seconda" · nl "zijn haar of hun werkblad" · sv "hans hennes eller deras" · da "hans hendes eller deres" · no **"possessive pronomen oppgaver"** (verbatim demand).

### F4: Sort the Names: He, She or They (G1, `G1-3xx TBD`, CODE `layout:'sort'`)
**Move:** CLASSIFY a whole set at once: 10 name cards (portrait + name; pairs as two portraits + "Mia and Ben") at the top, one labelled bin per pronoun with ruled lines below; the child writes every name under the pronoun it takes (the K-288 `sortWords` act on names; fr "étiquettes pronoms personnels à imprimer" verbatim; fi bins hän | he = one vs two; de/nl bins er | sie | es and hij | zij | het with 4 captioned object cards among the 10, so the es/het bin is fillable).
**Child:** "Read each name card. Write the name under the word we use for that person." (75; apparatus: name cards, words, lines.)
**Params:** d1 `{layout:'sort', cards:8, singles:6, pairs:2, bins:'chips', pic:64, lineCount:4}` · **d2** `{layout:'sort', cards:10, singles:6, pairs:2, pic:56, cardW:118, binH:330, lineMin:4}` (fr/es/pt: `singles:5, pairs:3` = mp/fp/xp so all 4 bins are fillable, 11 names; fi `singles:5, pairs:3`; de/nl `singles:6, pairs:0, objects:4` with >= 2 neuter) · d3 `{layout:'sort', cards:12, cut:true}` (a cut-and-paste sheet: `cutLines` + `scissorsGlyph` from `picture-word-cards.js`, the fr "étiquettes" proper; unpublished). Stack *est.*: card shelf `.ws-card` 660 wide, 2 rows of 5 cards 118 x 84 (portrait 56 + caption 18) = 84 x 2 + 10 + 24 padding = 202; gap 18; bins `.ws-bin` at `floor(640/bins) - 12` wide x 330 (K-288: `gapY = min(58, floor(330/(lineCount+0.5)))`, `lineCount = max(lineMin, hardest bin's card count)`); 202 + 18 + 26 (pill heads) + 330 = 576 <= 722 (slack absorbed by `binH` up to 400, engineer). Bins = `bank.chips` in canonical order (a bin the locale pool can NEVER fill is dropped, the K-288 `reachable` rule: none here by construction, every chip key is reachable from the tagged list).
**verify():** the K-288 sort contract cloned: `[data-lcs-layout="sort"]`, `data-lcs-sortword` (the name text, or "a and b") + `data-lcs-key` per card, `data-lcs-sortbin` / `data-lcs-sorthead` per bin; re-derived: >= 6 cards, >= 2 bins, heads === `bank.chips`, no card text repeats a head, keys in range, >= 2 distinct keys, every bin's ruled lines >= its card count (the K-288 stranded-word rule), pair cards have 2 portraits and 2 names, no name twice on the page (the pair's names count), no card is pre-placed; node: `keyOf(refs)` === the stamped key.
**Refusals:** none as a face. The panel's "thin" note stands in the copy: name-gender is cultural, so the landing frames it as a reading-and-sorting page, and every name is one the panel tagged.
**Query face:** en "sort the names he she they cut and paste" · de "Namen sortieren er sie es" · es "ordena los nombres él ella ellos ellas" · pt "separe os nomes ele ela eles elas" · fr **"étiquettes pronoms personnels à imprimer"** (measured) · it "ordina i nomi lui lei loro" · nl "sorteren hij zij het" · sv "sortera namnen han hon de" · da "sorter navnene han hun de" · no "sorter navnene han hun de" · fi "lajittele nimet hän vai he".

### F5: Rewrite the Sentence with a Pronoun (G2, `G2-3xx TBD`, CODE `layout:'rewrite'`)
**Move:** PRODUCE the whole sentence on school lines with the pronoun in place of the name(s), no bank: retrieval + handwriting (distinct from F1, which copies one bank word into a gap; the config differs on `bank:false` + `ruling:true` and the code path is `rulingBlock`).
**Child:** "Read the sentence. Write it again on the lines, but use a pronoun instead of the name." (86; apparatus: sentence, lines.)
**Params:** d1 `{layout:'rewrite', rows:6, ruling:true, glyphH:30, rowH:60, pic:48, singles:4, pairs:2, maxChars:24}` · **d2** `{layout:'rewrite', rows:8, ruling:true, bank:false, glyphH:24, rowH:48, pic:44, fontPx:18, singles:5, pairs:3, maxChars:30}` (8 items in [8, 16]) · d3 `{layout:'rewrite', rows:8, twoSentences:true}` (a two-sentence frame, only the second rewritten; unpublished). Row *est.*: printed sentence 18 px (22) + `writingRow` w 639 h 48 glyphH 24 (>= 24 at G2) + gap 8 = 78; 8 x 78 + 7 x 6 = 666 <= 677 <= 722 (the engineer measures `rowH` 48 at glyphH 24 against `primitives/trace-path.js writingRow`; no starter, so no `starterFontPx` surface). Frames: the SAME `frames[]` as F1 filtered by `maxChars` on the FILLED literal (the written line must fit 639 px at glyphH 24: 30 chars x 0.75 x 24 = 540 + 16 <= 639, the G1-350 `need()` formula), so the validator requires >= 8 of the 12 frames to have an `sg` literal <= 30 chars with the longest name and a `pl` literal <= 30 chars with the two longest names (a data rule, reported per locale; de/fi run 30-40 % longer, so their panels write short frames). Answer = the literal with `{subj}` -> `initial[key]`.
**verify():** stamps per row `data-lcs-frame data-lcs-num data-lcs-key data-lcs-answer=<the whole rewritten sentence>`; re-derived: `rows` rows, each with one printed sentence starting with a bank name and one EMPTY writing row (`emptyRows`) of glyphH >= 24 and w >= the answer's `need`; the answer sentence is NOT printed anywhere (poison: the rewritten sentence as a starter -> FAIL); node: answer === literal minus subject plus `initial[keyOf(refs)]`; no name twice; portraits >= 36; no bank on the page (poison: a `wordBank` banner on the F5 render -> FAIL).
**Refusals:** none as a face; per frame by `maxChars`.
**Query face:** en "rewrite the sentence using a pronoun worksheet" · de "Sätze mit Pronomen umschreiben Klasse 2" · es "escribe la oración con un pronombre" · pt "reescreva a frase com um pronome" · fr "réécris la phrase avec un pronom" · it "riscrivi la frase con un pronome" · nl "schrijf de zin met een verwijswoord" · sv "skriv om meningen med ett pronomen" · da "skriv sætningen om med et stedord" · no "skriv setningen om med et pronomen" · fi "kirjoita lause uudelleen pronominilla".

**Rejected non-moves.** A theme swap (fixed people pool) · object-noun -> pronoun as a FACE (a non-task in en/fi/es/it: "the ball -> it" tests nothing; it survives ONLY as the de/nl re-target inside the base/F4) · "I / you / we" chips (not derivable from a picture) · "hän vai se" as an animal-vs-person face (the written norm belongs in the fi copy, not on a K-2 page; the type stays on persons) · object pronouns him/her/them (COD/COI is CE2+, no `de/dem` in no) · reflexive sin/sitt · a verb-form gap (G2-317) · an article gap (K-288) · re-labelling the base's d1 (`objectCaption:'article'`) or d3 (10 cards) as a face (same move) · a "write your own sentence with he" open face (picture-writing G2-278; and an open face on a pronoun type is a non-task) · a nynorsk sibling (a separate authoring, REFUSED and recorded, never a filler).

## C. Native rebuild x11

Frames NEVER inflect: every pronoun, every verb form and every possessive ending is a stored literal; the code substitutes `{subj}` (a tagged name, or two joined by the `and` literal), `{P}` (an `initial` literal), `{thing}` (an `objForms` / vocab literal) and nothing else. Per locale the panel authors: `strings` (6 titles + 6 instructions), `names` (12, each with `gender`), `and` (+ `andBefore` for es), `chips` / `initial` (3-5 each; de/nl `objectMap`; fi 2), 12 `frames` (each `sg` + `pl` literal = 24 sentences), 8 `anaphora` frames (intro + 2 sentences = 24), `possessive` (chips + 12 things with `forms` = 12 opens + up to 36 literals; es/fi none), the 12 object keys for de/nl (opened), and audits the EN as a SOURCE. = 12 + 12 + 1 + ~8 + 24 + 24 + ~40 = **~120 literals** (es/fi ~85). Instructions name only apparatus on that face's page, one imperative a 6-8-year-old understands; the F1 instruction contains the locale's bank word and no other face's does; the F4 instruction contains the bin-writing verb.

| loc | literals the panel authors (counts) | slots / forms needed and where they already exist | refusal / re-target | traps |
|---|---|---|---|---|
| en | 12 strings · 12 names · 12 frames x2 · 8 anaphora · possessive 3 chips + 12 things = ~120 | pronouns are a closed table; `{thing}` = vocab singular lowercase (`displayWord`); names Mia Ben Emma Leo Anna Tom Lily Max + 4 | none | "I" never a chip; "they" never for one person; "it" never (persons); US spelling; "Personal Pronouns" base, "Possessive Pronouns" F3 (both heads kept); F1 frames use present simple 3rd person (`reads` / `read`) so the verb carries number |
| de | ~125 (+ 12 object keys with `objectMap` m/f/n -> er/sie/es) | `initial` Er/Sie; verb literals `liest`/`lesen`; `{thing}` de = the bare capitalised singular (NEVER `objForms.unique`, which is the accusative phrase "den Apfel"); F3 `forms` per thing with nominative endings (sein/seine/ihr/ihre) | base/F4: pairs -> null, objects supply es (+ er/sie); F1: pairs -> "Sie" (verb proves number); F3 G3, no pair owners (ihr collision) | Nomen capital; "Sie" formal never; older "Fürwörter" META only; "das Mädchen / das Kind / das Baby -> es" is the Klasse 2 trap: `girl` + `baby` may enter the OBJECT list with a NOUN caption ("Mädchen" -> es) but never as a named person; engine appends "Arbeitsblatt" |
| es (MX) | ~85 (no possessive) | Él/Ella/Ellos/Ellas; `and` = "y", `andBefore:{i:'e', hi:'e'}` (composer skips such pairs); `{thing}` unused | **F3 REFUSED** (`su` invariant) | usted/ustedes never; MX register; "oraciones" never "frases"; mixed pair = ellos (masculine, the 1º teaching point, the landing says so) |
| pt (BR) | ~120 | Ele/Ela/Eles/Elas; `and` = "e"; F3 dele/dela/deles/delas encode the owner (clean 4 chips; seu/sua never); the F3 frame puts the gap AFTER the noun ("Esta é a bola ___.") | none; **every face at 3º ano** | você takes 3rd-person verbs -> 2nd person off the page; "a gente" never; "atividade" never in a title; "do caso reto" on the base title only |
| fr | ~120 | Il/Elle/Ils/Elles; `and` = "et"; NBSP before `?` `!`; F3 re-target: son/sa by the THING's gender + leur for pair owners, things = fixed panel list, vowel-initial f nouns refused ("son amie") | F3 re-targeted (agreement with the possessed) | elle/elles homophones -> pair pictures show the count; "on" never; COD/COI never; "fiche" never in a title; the CE1 landing names CP-CE1 in copy for the measured "cp" tail |
| it | ~120 | Lui/Lei/Loro OR Egli/Ella/Loro (ONE choice for the type, stated in the landing); `and` = "e"; F3 re-target: suo/sua by the thing + loro for pairs, article printed before the gap ("il ___ pallone") | F3 re-targeted | "loro" genderless (pairs never test gender); "scheda" never in a title; the frame's article is the agreement cue and is legal here (the answer is not the article) |
| nl | ~125 (+ 12 object keys d/h -> hij/het) | Hij/Zij; `and` = "en"; `objectMap` d -> hij, h -> het; `keyOverride.girl` = the panel's "het meisje -> zij" ruling (data, not code) | base/F4: pairs -> null, objects supply het; F1: pairs -> "Zij" | title never "voornaamwoorden" alone; "verwijswoorden" is F2's head; het-nouns are 12 in the pool (list above): pin 6; IJ irrelevant (no boxes); engine appends "werkblad"; the landing says "groep 4" and expects little traffic |
| sv | ~120 | Han/Hon/De; `and` = "och"; F3 hans/hennes/deras; optional `hen` literal in `extraChips` (never rendered unless the operator flips `useHen`) [NSR] | none | den/det OFF; sin/sitt never; never «grupp»; "pronomen" head, "personliga pronomen" META; engine appends "arbetsblad" |
| da | ~120 | Han/Hun/De; `and` = "og"; F3 hans/hendes/deres | none | "stedord" title / "pronominer" META [NSR]; sin never; "opgaver" appended by the engine |
| no | ~120 | Han/Hun/De; `and` = "og"; F3 hans/hennes/deres | none; nynorsk sibling REFUSED (recorded) | bokmål only; de/dem never a chip; "oppgaver" appended |
| fi | ~85 (no possessive) | Hän/He; `and` = "ja"; frames `sg` with a singular verb ("{subj} lukee kirjaa."), `pl` with the plural verb ("{subj} lukevat kirjaa."); anaphora `s` = one `sg` + one `pl` sentence, referents one name + one pair | **F3 REFUSED**; base/F1/F2/F4/F5 re-targeted to NUMBER | fixed nominative names drop into subject position only (no case trap: `{subj}` is always the subject); "hän vai se" in the copy, never an animal item; instructions "Ympyröi sana, joka korvaa nimen." / "Kirjoita lause uudelleen viivoille."; 4-line fi title -> every stack checked at 677 [NSR] |

Every panel OPENS every picture it pins (the 32 person keys are opened above; the de/nl 12 object keys + the 9 locales' 12 possessive things are the panel's opens, `picOpened:true`), tags the 12 names, signs `contentNeutral:true` on every anaphora frame, and reads one render of every face in its locale.

## D. Data + gates

`data/b4/pronouns.js` (the EN block, hand-authored) + `data/b4/locales/pronouns.<loc>.json` GENERATED from `i18n/.draft-b4-<loc>.json` by `tools/apply-b4-locale.js` after `tools/validate-b4-draft.js` (b4 clones of the b3 pair; `data/` gitignored, `git add -f`); read through `lib/b4-common.js bank('pronouns', loc)`. Person tags live in the EN block ONLY (`people[]` is locale-neutral: the same 32 portraits with the same `depicted` in all 11) and are never re-authored per locale; the type reads `objForms` / vocab through `bank('instructions', loc)` + `lib/b2-common.js vocab()` for the de/nl objects and the F3 things.

```
PRONOUNS (data/b4/pronouns.js EN block; per-locale JSON overlays; every literal whole):
  people: [                                          // locale-neutral, 32 keys, every one OPENED (the table in §A)
    { key:'teacher', pic:{theme:'occupations', noun:'teacher'}, depicted:'f', picOpened:true },
    { key:'farmer',  pic:{theme:'occupations', noun:'farmer'},  depicted:'m', picOpened:true },
    { key:'student', pic:{theme:'classroom', noun:'student'},   depicted:'m', picOpened:true },
    { key:'girl',    pic:{theme:'toys', noun:'girl'},           depicted:'f', picOpened:true },
    ...                                              // artist astronaut cook mechanic pilot baby: NOT listed (ambiguous)
  ],
  names: [ {name:'Mia', gender:'f'}, {name:'Ben', gender:'m'}, ... ],   // 12 = 6 f + 6 m per locale
  and: 'and', andBefore: null,                       // es: 'y' + {i:'e', hi:'e'}
  chips:   ['he','she','they'],                     // fixed printed order; fr/es/pt 4; fi 2; de er/sie/es; nl hij/zij/het
  initial: ['He','She','They'],                     // sentence-initial forms, same order
  map: { base:{m1:0,f1:1,mp:2,fp:2,xp:2}, F1:{...}, sort:{...} },   // a missing key = the item is refused for that face
  objectMap: null,                                   // de {m:0,f:1,n:2} · nl {d:0,h:2}; keyOverride: {girl:1} (nl panel)
  objects: [],                                       // de/nl: 12 {key, pic, picOpened:true} from the reviewed pool
  frames: [                                          // 12; {subj} FIRST token; verb literal carries number
    { id:'read-book', sg:'{subj} reads a book.', pl:'{subj} read a book.', maxChars:30 },
    ...
  ],
  anaphora: [                                        // 8; intro names {a} + {b}; two content-neutral sentences
    { id:'park', intro:'{a} and {b} are in the park.',
      s:[{text:'{P} has a ball.', key:'sg'}, {text:'{P} has a kite.', key:'sg'}], contentNeutral:true },
    ...                                              // fi: one key:'sg' + one key:'pl' per frame
  ],
  possessive: { chips:['his','her','their'], byOwner:{m1:0,f1:1,mp:2,fp:2,xp:2},
    frame:'This is {subj}. This is ___ {thing}.',
    things:[ { key:'ball', pic:{theme:'toys', noun:'ball'}, form:'sg', forms:null, picOpened:true }, ... ] } | null,   // es fi null
  refuse: { possessive:false },                      // es, fi: true
  strings: { base:{title,instruction}, replace:{…}, anaphora:{…}, possessive:{…}, sort:{…}, rewrite:{…} }
```
`keyOf(refs)` (lib, pure): persons -> `m1 | f1 | mp | fp | xp` from `depicted` (the NAME's `gender` must EQUAL the portrait's `depicted` on every referent: the composer pairs a name with a portrait of the same tag, and the validator/gate refuse a mismatch); objects -> `objectMap[vocab[key][loc][2]]` (or `keyOverride[key]`); fi collapses every pair key to `p`. `answerChip(face, key)` = `map[face][key]` -> index or null. `possForm(ownerKey, thing)` = `thing.forms ? thing.forms[ownerKey] : byOwner[ownerKey]` -> index or null.

**`tools/validate-b4-draft.js` (pronouns block; every rule exit 1; the correct draft is the control):**
1. `people[]` is the EN block's list in every locale (overlays may not add, drop or retag a person; poison: a de overlay tagging `nurse` `m` -> FAIL); every `pic` resolves as a colour-index candidate in every locale (`candidates(key, loc)` carries `theme/noun`; no B&W marker `BW|SW|BN|NB|ZW|SH|PB|MV|SV`); `depicted` in {`m`,`f`}; `picOpened:true`; the 6 ambiguous keys are absent.
2. `names` = exactly 12, exactly 6 `f` + 6 `m`, distinct (NFD, case-insensitive), no name equals a chip / initial literal or a locale word of `and`; `ambiguousInLocale` absent or false.
3. `chips.length === initial.length` in {2,3,4}; every `map[face]` value is a valid index; `map.base` reaches every chip index from some key in the locale's reachable key set (persons: m1 f1 + pairs unless null; objects if `objectMap`); de/nl `map.base` has no `mp/fp/xp` (poison: de `map.base.xp:1` -> FAIL) and `map.F1.xp` === the she-index; fi `chips.length === 2` and every pair key -> 1.
4. `frames` >= 12; each `sg` and `pl` starts with `{subj}` (poison: "Ein Buch liest {subj}." -> FAIL), contains exactly one `{subj}`, no `{P}` / `{thing}` / other slot, no `___`, ends with `.`; `sg !== pl` (the verb must differ, poison: identical literals -> FAIL, since then the number is unproven); no `initial` / `chip` literal occurs as a token in either (poison: "{subj} und sie lesen." -> FAIL); no frame text equals a `SENTENCES[loc].frames[].text` with `{noun}` filled; >= 8 frames satisfy `maxChars` for F5 with the longest names.
5. `anaphora` >= 8; `intro` contains `{a}` and `{b}` once each; exactly 2 `s` entries, each starting with `{P}`, `key` in {`sg`,`pl`}; non-fi: both `sg`; fi: one `sg` + one `pl`; `contentNeutral:true` on every frame; no name / chip literal inside `s` texts.
6. `possessive`: null iff `refuse.possessive` (es, fi; poison: an es draft with a possessive block -> FAIL); else `chips` 3-4 distinct, `frame` contains `{subj}` once + `___` once + `{thing}` once; `things` >= 12, each key with a colour picture + (non-fi) an `objForms`/vocab singular, `picOpened:true`; `forms` present per thing in de/fr/it (each value an index or null) and absent elsewhere (`byOwner` present); de: every `forms.p === null` and `byOwner` absent; fr: a thing whose vocab singular is f and vowel/h-initial is refused; every chip index reachable from some (owner, thing).
7. `objects` present iff `objectMap` (de, nl): 12 keys, each with picture + `objForms` entry, gender code in `objectMap`, >= 4 neuter (de) / >= 6 h (nl), `picOpened:true`; absent elsewhere (poison: an en draft with `objects` -> FAIL).
8. `and` non-empty; es `andBefore` present; `strings`: 6 titles <= 70, no worksheet word (`seo.words.worksheet` of the locale), unique in the band, base title === the head, no title contains the locale's `verb-forms.name` or `articles.name` or `word-classes.name` (poison: fi "Persoonamuodot" -> FAIL); 6 instructions <= 150; the F1 instruction contains `strings.replace.bankWord` (a declared literal) and no other does.
9. `refuse.possessive === true` for es and fi (a draft without the refusal FAILS: the refusal is the record).
10. The draft's EN strings, `people[]` and `map` shapes equal the module's EN block (the EN is the source).
11. (sv) `extraChips` may hold `hen` but `chips` may not (a default chip FAILS); (it) `chips[0..1]` ∈ {lui/lei, egli/ella} and the choice is recorded in `strings.base.note`; (de) `initial` = ['Er','Sie'] exactly and `chips` = ['er','sie','es'].

**Poison cases (each must FAIL; the correct draft passes):** (1) `nurse` retagged `m` in an overlay -> rule 1; (2) 7 f + 5 m names -> rule 2; (3) de `map.base.xp:1` -> rule 3; (4) a frame `sg === pl` ("{subj} kan svømme." twice) -> rule 4; (5) "{subj} und sie lesen." -> rule 4; (6) a non-fi anaphora frame with `key:'pl'` -> rule 5; `contentNeutral` absent -> rule 5; (7) an es draft carrying `possessive` -> rule 6/9; (8) de `forms.p:2` on a thing -> rule 6; fr thing `amie` -> rule 6; (9) an en draft with `objects` -> rule 7; de `objects` with 2 neuter -> rule 7; (10) fi title "Persoonamuodot" -> rule 8; a base instruction containing the bank word -> rule 8; (11) sv `chips:['han','hon','hen','de']` -> rule 11; (12) `pic:{theme:'animals bw', ...}` on an object -> rule 7; `picOpened` absent -> rule 1; (13) render-side: a card whose name `Mia` (f) sits under a `farmer` portrait (m) -> the gate's node cross-check FAILS (name/portrait tag mismatch); two "Er" sentences on one F2 card (injected through the `_buildWith(bank, …)` seam) -> `verify()` FAILS; a de F3 card with two owners -> FAIL; an F5 render with a starter -> FAIL. Each poison runs against BOTH the validator and, where it survives to a render, `verify-b4-pronouns.js` with the validator bypassed (the #43 lesson).

**`qa/verify-b4-pronouns.js` (render gate; every face; every locale; d2 and the 677 fi stack):** floors (portraits >= 44 G1 faces / >= 36 G2 faces; chips >= 44 high; text >= 16; writing glyphH >= 24 on F5; bin lines >= card count on F4); uniqueness (no portrait `src` twice, no name twice, answers per card single); **no answer printed** (the correct chip is marked only by `data-lcs-correct`, never by style: the K-288 identical-style check; no `initial` literal in any F1/F5 sentence; no name in an F2 pronoun sentence); position leak (chip order identical on every card; F1/F5 the answer-key literal never sits first in the bank more than `ceil(rows/2)` times is N/A since the bank is the whole table, so instead: the bank order !== chip order on >= 1 render of 10 seeds); the captions are NAMES never occupation nouns (node: caption ∉ any locale singular/plural of the pinned key, NFD, case-insensitive; poison: caption "Lehrerin" -> FAIL); the 722 stack (`.ws-body` scrollHeight <= clientHeight at the 3-line chrome AND at the fi 4-line 677 chrome; nothing intrudes on the footer); `[data-ws-content]` on every stage; palette tokens only; node cross-checks (`keyOf` from the OPENED tags === every stamped key; `map`/`possForm` === every stamped chip index; F1/F5 answers === literal minus `{subj}` plus `initial`; F2 pronoun literals === `initial[keyOf]`; de/nl object keys off the vocab code). Wired into `deploy.sh` browser-free where possible (the contract half) and run with puppeteer as the dev gate (the render half).

## E. SEO

Titles (never the worksheet word; <= 70; base = the head; each face adds ONE element: the verb of the move):

| pattern | base | F1 | F2 | F3 | F4 | F5 |
|---|---|---|---|---|---|---|
| Germanic en | Personal Pronouns | Replace the Name with a Pronoun | Who Is He? Pronouns in Sentences | Possessive Pronouns | Sort the Names: He, She or They | Rewrite the Sentence with a Pronoun |
| Germanic de / nl | Personalpronomen · Hij, zij of het | Personalpronomen einsetzen · Vervang de naam door hij of zij | Wer ist gemeint? Pronomen im Satz · Wie is hij? Verwijswoorden in de zin | Possessivpronomen: sein oder ihr · Zijn, haar of hun? | Namen sortieren: er, sie oder es · Namen sorteren: hij, zij of het | Sätze mit Pronomen umschreiben · Schrijf de zin met een verwijswoord |
| Romance es / pt / fr / it | Pronombres personales · Pronomes pessoais do caso reto · Les pronoms personnels sujets · I pronomi personali soggetto | Cambia el nombre por un pronombre · Troque o nome por um pronome · Remplace le prénom par un pronom · Sostituisci il nome con un pronome | ¿Quién es él? Pronombres en oraciones · Quem é ele? O pronome na frase · Qui est-ce ? Le pronom dans la phrase · Chi è? Il pronome nella frase | REFUSED · Dele, dela, deles ou delas? · Son, sa ou leur ? · Suo, sua o loro? | Ordena los nombres: él, ella, ellos, ellas · Separe os nomes: ele, ela, eles, elas · Étiquettes pronoms : il, elle, ils, elles · Ordina i nomi: lui, lei o loro | Escribe la oración con un pronombre · Reescreva a frase com um pronome · Réécris la phrase avec un pronom · Riscrivi la frase con un pronome |
| Nordic sv / da / no | Pronomen: han, hon eller de · Personlige stedord · Personlige pronomen | Byt ut namnet mot ett pronomen · Erstat navnet med et stedord · Bytt ut navnet med et pronomen | Vem är han? Pronomen i meningen · Hvem er han? Stedord i sætningen · Hvem er han? Pronomen i setningen | Hans, hennes eller deras? · Hans, hendes eller deres? · Hans, hennes eller deres? | Sortera namnen: han, hon eller de · Sortér navnene: han, hun eller de · Sorter navnene: han, hun eller de | Skriv om meningen med ett pronomen · Skriv sætningen om med et stedord · Skriv setningen om med et pronomen |
| fi | Hän vai he? | Korvaa nimi pronominilla | Kuka on hän? Pronomini lauseessa | REFUSED | Lajittele nimet: hän vai he | Kirjoita lause uudelleen pronominilla |

Meta MIDDLE = the face's instruction (75-95 chars in EN above), so that `"Free printable {TITLE} … {LEVEL}. {MIDDLE}. …"` lands in the 120-170 window whole; de/fi/pt run 30-40 % longer, so the panel keeps each instruction <= 120 chars and `tools/measure-instruction-window.js` picks the longest fitting candidate (the family skill sentence competes). The "free" lead is metadata only (never visible: title / instruction / h1 / hub card carry no free-predication in any locale, the §25 lint). pt metas and landings name "3º ano" on every face; nl "groep 4"; de F3 "3. Klasse".

`coordinate` per face: `{type:'pronouns', mode:'base'|'replace'|'anaphora'|'possessive'|'sort'|'rewrite', level: LEVEL_KEYS[loc][band per the §A table], theme:''}`; `canonicalDeckSlug` = the published d2 deck; one landing per face per locale, es 5 + fi 5 (no `possessive`) = **64 rows** for `scripts/verify-hub-type-rows.js` (the panel's "= 64 rows" confirmed).

Non-cannibalisation (raw word-3-gram Jaccard, the gate's FAIL >= 0.80 / WARN 0.65; all *est.*, the gate measures): base vs F1 ~0.28 (same table, circle vs write) · base vs F4 ~0.30 (the closest pair: same chips as bins; the F4 prose is about SORTING names and CUTTING/WRITING under headings, the base about ONE card at a time: mode-true sharpening if it reads high) · base vs F2 ~0.15 · base vs F3 ~0.22 (possessive head) · base vs F5 ~0.18 · F1 vs F5 ~0.33 (same frames; bank-copy vs handwriting, no bank: the second-closest pair, sharpened by the handwriting prose) · F2 vs F5 ~0.12 · F3 vs F4 ~0.10; vs the Boundary: base vs K-288 ~0.20 (circle-a-chip prose; noun vs person) · base vs G2-317 ~0.10 · F1 vs G2-335 ~0.18 (sentence-gap prose; verb vs pronoun) · F4 vs G2-275 word-classes ~0.15 (sorting prose; class vs pronoun) · F5 vs G2-274 ~0.12 · F4 vs K-288 sortWords G1-292 ~0.22 (the same bin idiom; names vs nouns). The lever if any pair reads high: mode-true sharpening (what the child DOES on this face), never the gauge.

## F. Open questions + summary

**The engineer measures:** (1) the base card stage at 162 px x 4 rows on the 677 fi stack (`pic:76` or `chipH:44` fallback); (2) F1 lane at 74 px x 8 rows + the 60 px bank (`rows:7` fallback on fi); (3) F2 card 108 px x 6 with two 18 px sentences + a 56 px portrait pair stacked; (4) F5 `writingRow` at glyphH 24: the row h that keeps 8 rows <= 677, and the 639 px width against the longest de/fi rewritten literal (`need <= 623`); (5) F4 shelf 2 x 5 cards at 118 px inside 660 with a pair card (2 portraits 56 + "Mia and Ben" caption at 16 px: does the caption wrap?); (6) whether `articleChips` at 4 chips x 84 px fits a 2-column card (K-288 uses `w:66, fontPx:20` at 4 chips: reuse those numbers); (7) whether `bank('instructions', loc)` can be required from a b4 type without the additive `dir` argument (README open item 3); (8) `candidates('singer', loc)` returns `music/singer` too: the pin `occupations/singer` must be selected by theme, never `pictureFor`'s random pick; (9) the K-288 `sortWords` verify branch cloned for F4 with pair cards (2 portraits) and the stranded-line rule.

**Only a native panel can rule:** the 4 added names per locale + every name's `gender` tag (and any `ambiguousInLocale`); the `firefighter`, `coach`, `photographer`, `chef` tags I read as `m` (confirm at 44 px); nl `het meisje -> zij` (`keyOverride.girl`) and which 6 het-nouns; de which 12 objects and whether `Mädchen`/`Baby` enter the object list with a noun caption; it egli/ella vs lui/lei (one choice); sv `hen` (an operator/school-register call, `extraChips` only) [NSR]; da stedord vs pronominer [NSR]; fi the base title "Hän vai he?" vs "Persoonapronominit" as h1 [NSR]; fr whether the CE1 base should be titled for CP (the measured "cp fiche" tail) and the possessive thing list (no vowel-initial f nouns); pt whether "do caso reto" stays in the base title or moves to meta; every `contentNeutral` signature on the anaphora frames; the 12 possessive things per locale (opened).

**Summary.** Person-based pronoun type on a curated, OPENED 32-portrait pool (20 m / 12 f; 6 pictures excluded as ambiguous) with name captions (12 panel-tagged names per locale, 6 f + 6 m; the occupation noun never printed), the answer always a member of a fixed per-locale pronoun table keyed by `keyOf(depicted tags)` (de/nl base and sort re-targeted to er/sie/es and hij/zij/het with 12 captioned objects keyed off the vocab gender code; fi re-targeted to NUMBER hän/he; fr/es/pt 4 chips with the mixed pair masculine; it loro genderless), six distinct moves (circle / replace from a bank / resolve an anaphor with a line / choose the possessive / sort names into bins / rewrite on school lines), two recorded refusals (possessives es + fi) = 64 hub rows, pt at 3º ano and de F3 at 3. Klasse said honestly, CCSS L.1.1.d on every en face, national strand literals already in `strand-names.ts`; frames never inflect (every verb, pronoun and possessive ending is a whole literal, the code fills only `{subj}` / `{P}` / `{thing}`).
