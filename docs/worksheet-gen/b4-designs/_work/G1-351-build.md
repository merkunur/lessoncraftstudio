# G1-351 `odd-and-even` : EN BASE build record (2026-09-21)

Built from `G1-351-odd-and-even.md` (§2 base, §5 data + gates; §3 read for the additive `mode` knob), `_SUBSTRATE.md`, the README "Cross-type rulings" and `_work/G1-351-critic.md` (rulings 2, 3, 4, 5, 16, 18, 19, 22, 23 applied), then **re-budgeted on the reviewer's sparse ruling (2026-09-21, from K-357: the page's slack falls BELOW the stage, never inside it; the stage is top-anchored under the instruction; every base gate asserts a sparse floor)** — see "Reviewer ruling" at the end. Exemplars read: `types/k/K-016-odd-or-even-pairs.js` (the chip literals x11, reused verbatim), `types/g1/G1-310-hundreds-chart-puzzles.js` + `templates/components-b3/hundreds-chart-puzzles.js` (the `mode` dispatch + `blankNumeralBox` through the b3 namespace at call time), `types/g1/G1-352-pronouns.js` + `qa/verify-b4-pronouns.js` (the b4 conventions, the 722 / 677 chrome fixtures). No em-dashes.

## Files (all type-scoped; nothing shared edited; `node tools/b3-baseline.js --check --quick`: `checked build 3428 + enum 255 in 17s: 0 drifted (0 expected), 0 missing` / **PASS**)

| file | what |
|---|---|
| `scripts/worksheet-gen/types/g1/G1-351-odd-and-even.js` | the spec: exerciseType `odd-and-even`, deck slug `odd-house-even-house`, G1, `assetClass:'numeral-charts'`, THEMELESS (`themeAxis:{applicable:false}`, `unitAxis:{applicable:false}`), d1 / **d2 (ships)** / d3 (unpublished), `build` -> `_buildWith(bank, d, …)` (the gate's injection seam) -> `resolveBase(d, GLOBAL)` (every guard on the RESOLVED config: range lo >= 1, open chips in [6, 12], `worked.split` two positive counts larger-first, the worked pool <= 10 inside the range, the parity split reachable, and the leak guard `boxes > open AND boxes !== open` for EVERY possible open count of each parity) -> `compose` (RNG order: the split -> the side of the larger worked count -> the worked numerals -> the open numerals -> the chip order, re-shuffled until it is neither ascending nor alternating for more than 4 consecutive chips; locale-neutral) -> `houseBin` x2 in `bank.houseOrder` + `chipStrip` in a `flex:1` grid `stripH px houseH px` (NO 1fr; `align-content:start`; row gap = the config `gap`, 28, guarded 20..40; the house height DERIVED by `houseHeight(boxes, layout)`; the stack strip + gap + house guarded into [600, 677]). Pure helpers exported for the gate: `parity`, `pairsOf`, `resolveBase`, `compose`, `maxAscRun`, `maxAltRun`, `orderOk`, `stripHeight`. `verify(page)` = the design's list + the sparse floor (the strip top-anchored at the root's top, the strip -> houses band === the stamped gap and <= 40, the stage = the stamped stack and >= 600) (chips distinct / in range / never 0 / no parity stamp / no cue beside a chip / floors 44 + numeral 26; houses in the bank order with distinct non-numeric signs that never overflow 260; worked tiles inside their parity's house, <= maxN, cue count === worked with a `[data-lcs-given]` ancestor, pairs / single re-derived, both parities worked; the split; boxes per house === config, empty, `> open`, `!== open`, inside the house; no img / ten-frame / correct / stamped answer; no standalone `0` and no `+ = : ÷` text node in the body; no stray text). `d.mode` set -> `_buildFace` THROWS "Phase 2" (dispatched before the RNG is touched; the base stays byte-identical). |
| `scripts/worksheet-gen/data/b4/odd-and-even.js` | `{ ODD_AND_EVEN: { en: {…} }, GLOBAL }`: the EN block = `chips {odd, even}` (K-016's), `houseOrder ['odd','even']`, `rule`, `leftover 'left over'`, `pairs 'pairs'`, `placeHeads ['T','O']`, `nameMaxGraphemes 6`, `strings` x6 (base === the spec's i18n.en; the five §6 EN face candidates), `refuse {}`, `strand`. GLOBAL (code, not data) = `worked.maxN 10`, `tableOrder`, `modes`, `pvHeaders` (the lock-given letters de Z E · fr d u · es D U · nl T E · sv T E · fi K Y) and `doublesBan` x11 (labels.js `doublesHalves` + the taxonomy `doubles-halves` name tokens with stop-words removed + inflected forms). `data/` is gitignored: the reviewer `git add -f`. |
| `scripts/worksheet-gen/templates/components-b4/odd-and-even.js` | the eleven NEW components of §2 (the critic's ruling-23 export set), exported by exactly those names: `chipStrip`, `houseBin`, `workedTile`, `pairDots` (base) + `dotRowCard`, `shareLane`, `countLane`, `placeValueRow`, `tickPair`, `ruleStrip`, `parityTable` (the faces; built to the §3 contracts, smoke-rendered + measured by the gate, consumed by no spec yet). Barrel `templates/components-b4.js` loads them beside the sibling families with 0 duplicate names. The house is component-level SVG on tokens (chimney first, roof polygon tealSoft / coralSoft, body drawn after the roof, its height `houseHeight(boxes, layout)` = grid + rows x h + (rows - 1) x gapY + pad + 2) under three absolutely positioned HTML layers (the `layout` tops: sign 152 / worked row 220 / box grid 314, pad 30; d3 compact 140 / 196 / 270, pad 20) over the body's inner 290; `houseHeight`, `HOUSE_LAYOUT`, `HOUSE_BOXES` are exported for the spec's guards. |
| `scripts/worksheet-gen/qa/verify-b4-odd-and-even.js` | the gate; exports `validateBank(block, loc, opts)` (rules 1-12; `tools/b4-probe-child.js` calls it as `(block, loc)`), `crossCheck`, `banRegex`, `TABLE_B`, `HEAD`, `synthetic`. |
| `scripts/worksheet-gen/i18n/strings.en.json` | regenerated by `node i18n/build-en.js` (607 types, title lint clean; the diff also carries the sibling agents' new specs). |

## Deviations from the design file (each measured)

1. **The strip stamp is `data-lcs-strip`, not `data-lcs-chips`.** The root stamps `data-lcs-chips="<open count>"` (design §2 "Answer-hiding + stamps"), so a lane carrying the same attribute made `querySelector('[data-lcs-chips]')` return the ROOT and the strip measured 766 px in the first gate run. One name per surface: the root keeps `data-lcs-chips`, the lane is `[data-lcs-strip="<n>"][data-ws-content]`.
2. **The design's stack (92 + 20 + 426 = 538, the houses centred in a `minmax(426px, 1fr)` row) was SPARSE and is re-budgeted (reviewer ruling 2026-09-21).** At the default 2-line chrome (body 766) it left a ~130 px blank band between the strip and the roofs (measured 134 by the gate's PRs poison, which re-creates that build and must FAIL). Now: **strip 92 + gap 28 (fixed; config `gap`, guarded 20..40) + house 518 = 638 at d1 / d2**, the grid rows fixed (`92px 518px`, `align-content:start`, the houses `align-items:flex-start`), so the slack falls BELOW the stage. The house grew by HEIGHT (the width lever is capped: the body is 290 wide, 4 x 68 + 3 x 12 = 308 > 284, so the boxes went 62 x 60 / gap 12 x 14 -> **64 x 74 / gapX 8, gapY 24** (4 x 64 + 3 x 8 = 280 <= 284; grid 74 + 24 + 74 = 172) and the layers spread: sign 140 -> 152, worked 196 -> 220, grid 270 -> 314, pad 20 -> 30; house = 314 + 172 + 30 + 2 = **518**). The arithmetic at every chrome: **677** (4-line fi title, measured 677): 677 - 638 = 39 slack under the stage · **722** (3-line title + 3-line instruction, measures 710): 72 · **the default 2-line chrome** (766): 128 · **the one-line chrome** (README 814, measures 811): 173 <= 180. **d3** (unpublished, two chip rows, strip 164) keeps the compact 426 house (62 x 60 / 12 / 14; sign 140 / worked 196 / grid 270 / pad 20) as a per-level `house` config: 164 + 28 + 426 = **618** (<= 677, slack 59; the roomy house would be 710 > 677 and is refused). The 8-box-per-house count stays (> the 6 needed at d2; the design's leak guard).
3. **d1 draws 9 of the 10 numerals in 1..10** (6 open + 3 worked); the ladder's `4 + 5 or 5 + 4` is a coin flip of the seed (`splits: [[4, 5], [5, 4]]`), d2 `[[6, 6]]`, d3 `[[7, 7]]`; the side of the two worked tiles is a second coin flip.
4. **`pairDots` cannot see its ancestors** (it builds a string), so the design's "throws when called without a `data-lcs-given` ancestor" is a `host:'worked'` token: only `workedTile` passes it; a bare `pairDots({n})` throws. The render-side rule (every cue has a `[data-lcs-given]` ancestor, count === worked, none beside a chip) is verify()'s and is poisoned (PR5).
5. **`dotRowCard` measures 118, not the design's 126**: the dots panel is 52 (two rows of r 8 at pitch 24 = 40 + padding 8 + border 4; `min-height:52` so a one-row card is the same height), the equation row is 56 (the r column = box 40 + 4 + caption 16 hangs the caption UNDER the r box inside the row, as §3 draws it), + 10 gap. Equation rows 231 / 243 <= 302. 4 x 118 + 3 x 14 = 514 at F1: 144 px more slack than the design's 658.
6. **Rule 3 ("the rule carries both chips as whole words") is tighter than a natural plural rule sentence in the Romance locales**: es "Los números pares / impares …" carries neither `par` nor `impar`. The synthetic es / pt controls use the singular-predicate frame ("Un número es par si termina en …. Es impar si termina en …"), sv the neuter "Ett jämnt tal … Ett udda tal …" (the pill reads `jämnt`), and fi cannot satisfy it with the plural nominatives ("Parilliset … parittomat") — the fi fixture appends "luku on parillinen tai pariton" (a `fiRuleFix` the gate documents). **OPEN for the panels / the reviewer:** either the panels write the rule in the chip's bare form, or rule 3 is relaxed to a 5-letter stem for fi.
7. **Rule 7's fi " vai " check is narrowed to question titles that name BOTH parities**: the design's own F2 fi title "Voiko jakaa tasan kahdelle?" is a yes / no question with no `vai`; " tai " stays forbidden in every fi title.
8. **The zero ban vs the F3 rule strip (Phase 2, OPEN).** §5 wants "`0` absent from every text node on every page", but the F3 strip prints "… end in 0, 2, 4, 6 or 8" by design. The base gate bans a standalone 0 in the whole body (poisoned: PR9); the F3 build must exempt `[data-lcs-rule]` (the fi trap is the WORD "nolla", which never appears) or the panels rewrite the rule without the digit 0.
9. **The export set is the critic's (ruling 23)**, not the task prompt's pre-critic list: `friendGlyph`, `parityChips`, `sumCard`, `pictureParityCard`, `shareCard` have no face after rulings 1 / 7 / 11 and are not built.
10. **"Nothing else in the catalogue draws a house" is overstated**: G1-209 / G1-220 / G1-221 (fact families) draw a Rechenhaus roof triangle with numerals. This house has a chimney, a word sign under the eave, a coloured roof and dashed boxes — distinct at arm's length; recorded, not a build concern.
11. **Rule 4's caption widths are measured in the RENDER section** (a node validator has no fonts): en `left over` 55.6 <= 84 (47.6 at 12 px on F1), `pairs` 32.7 <= 56; the pills 69.1 <= 102 at 44 high; a non-EN block is measured the same way the moment it exists.
12. **Rule 12 reads the 11 `seo-landing/<loc>.json` files** for the landing-slug collision (en 4,155 landings; 0 collisions); the non-EN taxonomy slugs are not registered yet, so the synthetic controls filter that one message (the registrar's next step, as G1-352 recorded).
13. `GLOBAL.worked` carries only `maxN:10`; `count` / `split` are per-level ladder keys (d3 is `2 = 1 + 1`), read on the RESOLVED config.
14. **The sparse floor is a config REFUSAL as well as a render assertion**: `resolveBase` throws when strip + gap + house < 600 ("sparse: the stage must use the page") or > 677 (the fi chrome) or `gap` leaves 20..40; the gate's config poisons cover the compact house at d2 (546), gap 12 / 60, 96-high boxes (682) and the roomy house at d3 (710). Render poisons: PRs (the design's 538 / centred build re-created by mutation -> "a 134.0 px blank band inside the stage (sparse)"), PRt (`align-content:center` -> "the strip is not top-anchored"), PRu (a `1fr` house row -> the band is 92 px, not the gap).

## Gate output (`node qa/verify-b4-odd-and-even.js`, 26 s)

```
bank en: clean (chips odd/even, order odd<even, faces 6)
render d1 en: verify 0 lints 0 body 766 strip 92.0 house 330x518 signs odd@76.2 even@84.1 chips 10,5,3,6,1,4 stage 638 band 28 slack-under 128 lowest 793 vs foot 921
render d2 en: verify 0 lints 0 body 766 strip 92.0 house 330x518 signs odd@76.2 even@84.1 chips 11,10,8,5,12,14,1,13,19 stage 638 band 28 slack-under 128 lowest 793 vs foot 921
render d3 en: verify 0 lints 0 body 766 strip 164.0 house 330x426 signs odd@76.2 even@84.1 chips 7,30,24,15,3,6,1,20,8,23,19,22 stage 618 band 28 slack-under 148 lowest 773 vs foot 921
render d2 en one-line chrome: body 811.0 stage 638 slack-under 173 (<= 180)
render d2 en 3+3 chrome: body 710.0 lowest 849 vs foot 921 (stack 638, slack 72)
render d2 en 677 chrome: body 677.0 lowest 882 vs foot 921 (slack 39)
render d3 en 677 chrome: body 677.0 lowest 862 vs foot 921 (stack 618, slack 59)
components: rule 40.0 · dotRowCard 118.0/118.0 (eq 231.3/243.4) · shareLane 106.0/605.1 · countLane 106.0/601.1 · captions left over@47.6 left over@47.6 left over@55.6 pairs@32.7 · pills 69.1/69.1/69.1/69.1 · pv 48.0 · table 92.0x170.5
sweep d1: 20 seeds, 19 distinct sets, 20 distinct orders · sweep d2: 20 / 20 · sweep d3: 20 / 20
verify-b4-odd-and-even: 660 assertions, 0 failures in 26s → PASS     (--quick: 176 assertions, 0 failures)
```

Poisons, every one killed for its OWN reason (the correct EN bank + the synthetic de / es / pt / it / sv / da / fi controls pass; the must-pass ban controls "Gerade Zahlen bis 20" / "jämnt" / "dele ligeligt" / "Der Zufallsbeutel" / "on behalf of the pairs" pass and "Gerecht halbieren" / "hälften av talet" / "write the double" / "puolet luvusta" / "de helft" / "det halve" / "Dubbelt och hälften" fire): P1 (de `Gerade` -> rule 2), P2 (`odd` -> rule 1), P3 (sv `jämna` -> rule 1, K-016 says `jämnt`), P4 (even digits only -> rule 3) + P4b (a rule without the odd chip), P5 (21-char leftover) + P5b (pairs === leftover), P6 x2 (`Tens/Ones`; de `T/O` vs the lock-given `Z/E`), P7 (de `halbieren` -> rule 6) + P7b (worksheet word) + P7c ("With Answers") + P7d (a free claim) + P7e (a base title without the head) + P7f (the doubles-halves head) + P7g (K-016's own title), P8 ("Even and Odd"), P9 (fi `tai`), P10 (an ones title without 100), P11 (Big vs Bigger -> rule 8), P12 (sv `vecka`), P13 (it "Pari e dispari"), P14 (no `sums`, no refusal -> rule 9), P15 ("Circle the dots" on the base -> rule 7), P16 (it `nameMaxGraphemes 3` -> rule 10), P2b (`houseOrder odd,odd`), P6b (the neighbour head "Ten Frames"); config poisons (resolveBase refuses): range [0, 30] · boxes 2 x 2 at d2 (= the open 4: leak) · boxes 1 x 3 (< open) · worked.split [1, 2] · [3, 0] · chips 5 · range [1, 10] for 6 + 6 · the compact 426 house at d2 (546 < 600 sparse) · gap 60 / gap 12 · 96-high boxes (682 > 677) · the roomy house at d3 (710 > 677) (+ the control that 2 x 3 = 6 resolves); render poisons PR1 (a pre-filled box), PR3 (a ten-frame), PR5 (a cue beside an open chip), PR6 (a house with 4 boxes and 4 open chips), PR9 (a 0 chip), PRa (a parity stamp on a chip), PRb (a worked 3 stamped with 2 pairs), PRc (a worked even numeral in the odd house), PRd (chip numerals at 20 px), PRe (a sign literal wider than 260 -> "refuse the literal, never shrink the font"), PRf (the chips re-ordered ascending), PRg (a 600 px house under the 677 chrome -> footer overlap), PRs (the design's 538 / centred build -> "a 134.0 px blank band inside the stage (sparse)"), PRt (`align-content:center` -> not top-anchored), PRu (a 1fr house row -> the band is 92, not the gap 28), PRh (a sign printing a numeral), PRi (a stamped answer); component throws: `pairDots` without host / n 0 / n 11, `workedTile` n 12, `houseBin` w 200 / 5 columns / an even worked in the odd house / 3 worked tiles / an empty sign, `chipStrip` 10 x 64 / a 0, `tickPair` keys, `parityTable` order, `shareLane` the same name twice, `placeValueRow` 07. PR2 / PR4 / PR7 / PR8 / PR10 of the design are FACE poisons (Phase 2).

Measured for the faces (the design's *est.* items, at real fonts): `ruleStrip` en **40** (one line), `dotRowCard` **118** (eq rows 231 / 243), `shareLane` **106** / row 605 (634 with the fi pill), `countLane` **106** / row 601 (616 with the fi pill), `placeValueRow` **48** with the ones box coralSoft + coral 2.5 solid, `tickPair` 2 x 44 EMPTY, `parityTable` **92 x 170.5** (en; de 217 + 32 = ~250 per the design's measure).

## Renders for the reviewer (LOOKED at, every one)

- `scripts/worksheet-gen/out/dev/G1-351-null-d1-en.png` · **`G1-351-null-d2-en.png`** (the re-budgeted base: strip, a 28 px gap, the houses top-anchored, 128 px of slack under them at the 2-line chrome) · `G1-351-null-d3-en.png` (render/one.js, lints + verify clean) · `G1-351-d1-d3-contact.png`
- `out/dev/G1-351-gate-d2-en-chrome814.png` (the one-line chrome, 173 under the stage) · `G1-351-gate-d2-en-chrome722.png` (the 3+3 chrome, body 710) · `G1-351-gate-d2-en-chrome677.png` + `G1-351-gate-d3-en-chrome677.png` (the 4-line title; the base fits with 39 / 59 to spare)
- `out/dev/G1-351-gate-components.png` (rule strip · two F1 cards · an F2 lane · an F5 lane · an F3 row + tick pair · the F4 table)
- `out/dev/G1-351-null-d2-en-grey.png` (the design's mono-print check: the roofs are indistinguishable in greyscale as expected, the sign word is the house's identity, the hollow coral ring reads as "not a pair" beside the solid dots)
- What I saw (after the re-budget): the stage starts 28 px under the strip and the blank page lies only BELOW the houses; the boxes are taller (64 x 74) with pencil room, the sign / worked row / grid spread evenly down the body; the two houses READ as houses (a roof, a chimney standing above the right roof edge, a white body, a word sign under the eave); the three worked numerals are visibly "done" (a white tile with the numeral and its pair columns; 3 = one pair + a ring, 6 = three pairs, 9 = four pairs + a ring); the nine open chips are bare and in a mixed order (11 10 8 5 12 14 1 13 19 at the shipped d2 seed); nothing cut off, nothing overlapping, the boxes inside the walls; the chimney does not read as a flag at 330 px; the ring does not read as a zero. `G1-351-null-d2-en-grey.png` re-made on the new build.

## Open items for the faces / panels

1. **Rule 3 in the plural-rule locales** (deviation 6): the panels write the F3 rule with the chip's bare form (singular predicate) or rule 3 takes a stem for fi — a reviewer ruling before the drafts.
2. **The zero ban vs the F3 rule strip** (deviation 8): the F3 build exempts `[data-lcs-rule]` from the standalone-0 scan, or the rule sentence is rewritten without the digit 0.
3. Phase 2 builds the five faces on `d.mode` (`proof` / `share` / `ones` / `sums` / `count`) inside `_buildFace` + a `verify()` branch per stamp; the components exist and are smoke-measured; `tools/b4var-rows/odd-and-even.js` rows carry the §3 configs (the F2 / F5 pills = the K-016 pill at h 44 in `houseOrder`, the gate's `pills()` helper is the shape). `tools/gate-variation-distinct.js` still has `b2` / `b3` branches only (critic OPEN 4).
4. The landing strand literal per locale (critic OPEN 1: the OA row of `strand-names.ts` renders pt / sv / nl wrongly) is a landing-generator concern, not the spec's; `bank.strand` carries the §1 table-B literal so the generator can read it.
5. Non-en taxonomy slugs + names (table B) are the registrar's next step; rule 12 holds each locale to table B once its block exists.
6. `strings.en.json` was regenerated by build-en (tracked; includes the sibling agents' new specs).

## Reviewer ruling (2026-09-21, after the first d2 read) — applied

"A ~130 px blank band sits between the chip strip and the houses because the 538 stack is centred in the body cell. The page's slack falls BELOW the stage, never inside it; a stage sits top-anchored under the instruction with a fixed 24-32 px gap, and every base gate asserts a sparse floor." Done as deviation 2 / 14 above: gap **28** (config, guarded 20..40), the houses top-anchored (fixed grid rows, `align-content:start`), the house grown by HEIGHT to **518** (boxes 64 x 74, gapX 8 / gapY 24, layers 152 / 220 / 314, pad 30 — the width lever is capped by the 290 body: 4 x 68 + 3 x 12 = 308 > 284) so the d2 stack is **638** = 92 + 28 + 518: at 677 (fi 4-line) 39 px under the stage, at 710 (the measured 3+3) 72, at 766 (default) 128, at 811 (one-line, README 814) 173 <= 180. d3 keeps a compact 426 house (618). Gate: stage >= 600 at every level, band === gap and <= 40, slack under the stage <= 180 at the one-line chrome, the design's 538 / centred build re-created as poison PRs and killed as "sparse" (134 px band). Full gate 660 / 0 → PASS; `node tools/b3-baseline.js --check --quick`: `checked build 3428 + enum 255 in 17s: 0 drifted (0 expected), 0 missing` / PASS; renders re-made and looked at (`out/dev/G1-351-null-d2-en.png`).
