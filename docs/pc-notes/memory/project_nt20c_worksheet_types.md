---
name: project-nt20c-worksheet-types
description: "nt20-C BUILD (2026-09-14 → LIVE 2026-09-20) — the last batch: 20 printable worksheet types × (1 base + 5 variations) × 11 locales; 1,295 decks + 1,295 landings live, hub gate hard; the fix rounds the landing panels forced, and the traps bought while building"
metadata:
  node_type: memory
  type: project
  originSessionId: 215a8fe6-4312-4c85-bb49-8f9c20e5d24e
  modified: 2026-09-14T08:02:27.378Z
---

# nt20-C BUILD — 20 types × 6 faces × 11 locales (COMPLETE 2026-09-20; LIVE)

**Operator /goal:** "build the last batch of 20 worksheet types and their expansion." Plan (approved) = `C:\Users\rkgen\.claude\plans\purring-discovering-floyd.md` (Phases 0-8). Contract = the 20 design files in `docs/worksheet-gen/b3-designs/` + README (recipe, open items, cross-type rulings) + `hub-expectations.json`. Design record: [[project-nt20c-design]]. Nothing is re-designed.

## RESUME POINTER (update at every session end)
- **Phase 0 — DONE** (baseline `2f4c3ddf` · notation `a74c5a2e` · unitAxis `5ee5603a` · hub gate + deploy.sh `483a992a` · G3-311 gap + pin waves `4dffc43a` · 72-deck divfix republish LIVE + repointed ×11 · animal-facts + gate · free-claim lint 5 + gate.js ratchet · registrars run).
- **Phase 1 — DONE 2026-09-14: all 20 EN base specs COMMITTED** — K-317 `aa9b16b0` · G1-305 `d0286949` · K-318 `5fb7cd02` · G1-306 `3f1cc446` · K-319 `e19e5feb` · K-321 `8da9fb46` · K-320 `23967bba` · K-322 `a3c1cc54` · K-324 `776152e3` · K-323 + G1-307 `3a88eb2d` · G1-308 `cf27b706` · G2-315 + G1-310 `1cd0aef9` · G1-309 `d072a2e0` · G2-316 `76d6e5b0` (+ b3-baseline keys pin waves by filename) · G2-318 `a399ae73` (bank/gate renamed to the family key) · G2-317 `0b2e6712` · G2-319 `cd45f5e3` (+ primitives/logic-grid.js) · G3-377 `95690d07` · strings.en.json `f1a89b8f` (+112 ids, 0 changed). Release baseline re-captured (`out/b3-baseline.json`: 23,496 coords + 211 waves; 853 b3 contract refusals + 188 pre-existing K-288-class errors are IN the baseline — 0 drift from here). Pushed; `deploy.sh` launched (log `/root/deploy-b3-phase1.log` on Hetzner — check it ran the hub gate in warn mode).
- **Phase 2 (EN variation faces, 100) — DONE 2026-09-14, close `3b8e37d7` (pushed).** Scaffold `c3edc625` (`alloc-b3var-ids.js` → `_records/b3var-id-allocation.json`, ids FIXED by content band; `gen-b3var-specs.js` over `tools/b3var-rows/<key>.js`; `gate-variation-distinct --batch=b3`; `gen-b3-probe-jobs.js`; `_FACE-BRIEF.md`). Per-family face commits: K-319 `d569556d` · K-317 `3cf12a46` · K-318 `f9ed07ad` · K-320 `7d71687d` · K-321 `4101e809` · K-323 `d4cc83ac` · K-324 `f87e070a` · K-322 `4d50fdf8` · G1-305 `5b1bc640` · G1-306 `8ab0185e` · G1-307 `ef215fa3` · G1-308 `65b3f6df` · G1-310 `ff6096f9` · G1-309 `11bf755b` · G2-315 `b23ee898` · G2-316 `840771d5` (reviewer ruling: en bank webLanes:3) · G2-318 `7de3359a` · G2-317 `edbf6ad2` · G2-319 `626a1c01` (F4 second attribute = a picture theme, never colors) · G3-377 `bc34aa6c`. 93 emitted + 7 handwritten (K-333 factory instance; G1-311, K-350, G1-333, G2-324/326/328 own unitAxis). Distinctness 300 pairs clean; full baseline 27,984 coords 0 drift (re-captured after the close = release baseline); strings.en.json +200/0 changed. Per-family face records `_work/<ID>-faces.md` carry the per-locale refusals + wave pins the next phases need (G1-318 racer theme; G2-318 animal theme; G2-319 allowlisted set; G1-313 around-the-house; K-324 F6 animals in en/da; G2-316 F1 refused en; G3-380 unit 2-5 only; G2-315 pair faces fan over c-k-ck / y-ies-f-ves).
- **Phase 3 (EN) — DONE `ff971519`:** `tools/gen-b3-waves.js` (every themed spec pinned by a MEASURED build(); themeless measured too; refusals recorded in the wave's `_refused`, never padded; main guarded, exports measure()) → `wave-b3-en` 20/20 + `wave-b3var-en` 97/100 (en refuses G1-327 scramble, G1-329 vowel king, G2-329 link — by design). 117 EN decks generated QA-clean into `out/staging/wave-b3-en` + `wave-b3var-en` (pooled copy `out/upload/wave-b3-en-all`); desc band 117/117; title similarity 0 FAIL / 4 WARN (accepted band). scan-duplicate-problems is b2-story-card-only (n/a). `_records/en-instruction-window.txt` = panel input.
- **Phases 4 + 5 CLOSED 2026-09-14 (pushed at 9894dd5d).** All 11 locales applied (fi `5d2facb6`, 118/120); hub matrix reconciled to the declared refusals (design total 1,295 of 1,320, ceiling 1,296; `hub-expectations.json` --check clean); every locale generated + pooled to `out/upload/wave-b3-<loc>-all` (en 117 · de 119 · nl 117 · es 118 · fr 116 · it 117 · pt 117 · sv 118 · da 119 · no 119 · fi 118 = 1,295), desc band N/N + similarity 0 FAIL in all 11; 20/20 family gates green; RELEASE BASELINE re-captured (`e0383ecd`; the only accepted drift vs the pre-panel baseline = the 120 b3 ids + the 15 da calendar coords of the dayAbbr fix, `_records/da-dayabbr-expected-drift.txt`). The critic round (contact sheets read by me for de/pt/fi, visual-critic agents for the other 8, then native fix panels for sv/da/no/it + my own fixes for en/nl/es/fr) is folded in — see the traps below. Tooling: `publish-readiness.js --batch=b3 --locales=`, `render/batch.js` + `gen-b3-probe-jobs.js` (theme = cacheTheme), `gen-b3-faces-table.js` → `out/b3-faces.<loc>.json` (sweep PNGs in `out/b3-sweep/<loc>`, all 11 rendered clean after the fixes), `b3-baseline.js --dump=`. Vocab-level defects the panels routed around are listed at the end of the README (§10.3, not fixed).
- **Phase 6 (landings ×11) — DONE 2026-09-20** (the 09-14 session was cut by the weekly limit mid-panel: en Q2/Q4 survived with 41 overlong metas, Q1/Q3 were lost — re-authored 09-20). Per locale: four 3-agent native quarter panels (Q1 letter-of-the-week/sound-boxes/syllable-split/syllable-reading/spelling-rules · Q2 opposites/compound-words/verb-forms/read-and-do/rhyming-words · Q3 feelings/animal-fact-file/hundreds-chart-puzzles/division-with-remainder/ordinal-numbers · Q4 days-and-months/all-about-me/seasons/logic-puzzles/picture-word-cards) → merge → `gen-b3-landings.js` → `gate.js` (diff FAIL/WARN vs the `git show HEAD:` corpus — only the recorded slot-token class may grow) → `verify-hub-type-rows.js`. Commits: en `2f4fa147` · de `8c6076fe` · nl `f23d0b5d` · es `f11fc6cc` · fr `551d26de` · it `43c6e8fc` · pt `5e5372e9` · sv `f0e889a2` · da `f4c64d3b` · no `215b9ebe` · fi `b06aa80c`. ⚠ es = MEXICAN register (SEP / Nueva Escuela Mexicana; my prompt said Spain — the panels caught it); pt = pt-BR (BNCC); no levels carry the Nordic +1 shift (K → 1. trinn).
- **Phase 7 (publish ×11) — DONE 2026-09-20:** `scripts/publish-cli/b3-publish-locale.sh <loc> [--confirm]` (`e78a8bf8`; dry-run gate ok=N / collisions 0 / halts 0 → confirm → OG ×11 → hreflang ×11 → audit). 1,295/1,295 published, audit clean in every locale, 0 suffixed slugs (en K-336 republished in place → v2 after an 11-title ≤70 fix `f074e2e6`); `deploy.sh` EXIT=0 (1,660 tests); `repoint-deck-canonical.js --types=<20 keys> --locale=<loc>` 1,295 repointed / 0 missing; noindex exempt map unchanged (19,634); IndexNow 7 batches HTTP 200.
- **Phase 8 — DONE 2026-09-20 (`2024f1f9`):** OBS measured live — every key in every locale's rail, every `?type=` page lists exactly its sheets (1,295/1,295), real-browser de/fi/pt on the All tab; `--warn-missing-keys` dropped (gate HARD in deploy.sh, 0 warnings); housekeeping expires `deck.html.precanonical-bak` > 14 d; server `_staging/b3-*` removed (the pools live on the PC in `out/upload/wave-b3-<loc>-all`); CLAUDE.md §25 BUILT + LIVE, §14.10 95 → 115.
- **⭐⭐ THE LANDING PANELS WERE THE SHEETS' ONLY REVIEW — brief them as an AUDIT of the render, never as copywriting.** Reading every PNG, the 44 panels found ~40 shipped defects no gate saw, each fixed at the source + regenerated (fix rounds `b92f47c1` `e32e1892` `811044e1` `ed711633` `a50ec5a3` `5ef19986` `9f65b65b` `29c2416f` `e336bb5e` `f7bab6bd` `fe0f5a5e` `f074e2e6`): titles saying tick/cross while the page is circled (de/fi/sv G2-339, sv G1-341); instructions naming apparatus that does not exist (G2-330 'letter boxes' in 7 locales — an EN-source defect inherited by six; sv G1-312 numeral box; sv G1-314 writing line; nl G2-334 the tense layout nl never ships); a move omitted from a key (G2-321 'left is 1 less' in 7 locales); two sentence frames the instruction never mentioned (G2-334 ×6); a truth face printing ONE fact twice through two frames (G1-341 it/sv/da/fi → fact-key dedupe in builder + verify, poisoned OLD-vs-NEW); a cloze face repeating an answer (G1-326 de/pt/sv/no → distinct-syllable swap that leaves clash-free pages byte-identical; a hard refusal failed de 'pets' in the gate sweep); titles naming a whole pool the page draws two of (es/pt G2-336, fi G2-336); an ungrammatical pt frame ('Vocês estão muita fome' — dropping the fit alone REFUSED the page, the fix was a second frame); es labels doubling the title head; the free-claim regex banning Finnish 'ilmaisu' (expression); nine EN bases promising 'an easier level in the same file' (a ZIP holds ONE printable — measured). ⚠ Every fix that touches the RNG path: predict the drift set, `b3-baseline --check --dump=` (the DRIFT list truncates at 40), explain every delta, record `_records/*-expected-drift.txt`, `--capture`. ⚠ Re-render + `gen-b3-faces-table.js` after any string change or the next panel reads stale titles. ⚠ A landing that quoted the old draw must be re-quoted (diff old vs new render rows, not assumed).
- **2026-09-21 post-live fix (operator screenshots):** every rulingBlock starter (K-335 because · G2-278/299 · G2-318 factLane · G2-339..342) shipped with its caps ON the midline (0.78·glyphH, a derived factor) and G2-235 d3 clipped its animals — fixed at the factories with measured font metrics + rendered-geometry verifies, 142 decks republished in place ×11; record [[project-writing-frame-starter-fix]]. ⚠ The 44 landing panels read every PNG and did NOT report the starter height — a rendered-geometry defect needs a MEASURED gate, not a reader.
- **Open follow-ups (recorded, not built):** G2-315 base + G2-327 anchor sample the rule pool with no side balance (fr m-devant 1 rule : 7 contrast) and the base's `gapCells:'len'` box width answers 'one letter or two' (de/pt/da/fi); `animal-facts.json` ladybug `air`/`meat` (6 panels doubt it; the bank files bee/butterfly under air — operator ruling); K-346 friend-name oval masculine-only nl/it/pt; owl in the farm-animals theme pool, baby in toys (§10.3); the live pre-ruling landings still carry a visible free-claim in p3 (nl 2,789, sv, fi — ratchet only); es/pt division `chave`/`caja` layouts and the fr `16 = (5 × ▢) + ▢` form are locale-correct, never 'R'.
- **Recurring builder findings to carry into Phase 2/4:** worst legal chrome = 733/710/700 (677 with a 4-line fi title + 3-line instruction — G3-377/G2-319 measured it); `pictureStrip` contract = K-320 `lineUpStrip`; `nameBanner`/`drawBox` stand-ins in K-323/G1-308; sv/da/no `def` tables, de `dat`, fi `part` are panel work (bases refuse until then); each gate exports `validateBank` (feed `tools/validate-b3-draft.js`); `strand-names.ts` gaps; G2-318 + G2-319 + G3-377 waves must theme-pin (animal theme / allowlisted set); G2-319 colors luma 88-182 needs a ruling before F4; fi "Päättelytehtävät" hits the tehtäv title ban; ~31,000 live landing bodies claim "free" (ratchet only).

## Phase-0 facts (measured; do not re-derive)
- `tools/b3-baseline.js` = ALL 478 specs × 11 locales × d1-3 (22,572 hashes, 21 s) + a section-2 enumeration snapshot of all 200 wave files (deckId=seed). `--check --expect=<file>` = drift set must EQUAL the list; `--poison` 2/2. The baseline on disk was re-captured AFTER the two accepted fixes → 0 drift is the release condition from here on.
- `types/_shared/notation.js` is THE sign table (`divGlyph`/`mulGlyph`; sv `/`, de/it/nl/da/no/fi `:`, else `÷`; `·` in de/sv/da/no/fi). Accepted drift = exactly 189 coordinates (`_records/c1-expected-drift.txt`). MUL_DOT (`×`→`·`) deliberately NOT applied — a convention upgrade, ~100 more decks, follow-up.
- **G3-311 fact-family column gap 10→8**: the last answer box printed 3 px into the attribution band in EVERY locale (the footer lint `bfa8c2b2` was added 2026-09-03, after the June decks shipped, so nothing saw it). Accepted drift = 66 coordinates (`_records/g3311-gap-expected-drift.txt`).
- ⭐⭐ **THE ENUMERATOR'S ROUND-ROBIN INDEX HAS SHIFTED SINCE JUNE** (it is the spec's position in `loadAllTypes()`): a bare `--types=` re-run of wave-008 would put G2-216 on fruits+vehicles while the live decks are animals+shapes. `tools/divfix-regen.js` pins the PUBLISHED themes in `waves/wave-00N-divfix-{a,b}.json` (id kept so `deckIdFor` reproduces the live basenames; dry-run asserts 72/72). ⚠ NEVER `prune-stale-zips.js` with a pin wave.
- ⭐⭐ **THE NOUN POOLS HAVE ALSO SHIFTED**: `rng.sample(labelSafeNouns(theme))` consumes a pool-length-dependent RNG stream, so a regenerated themed deck is the SAME coordinate with a DIFFERENT valid instance (64 of the 72 differ in images_used; the 7 themeless G3-310 + the calendar differ only in the fix). Title/meta/slug byte-equal (72/72) → `--updates-manifest` republish is valid; landing prose quotes the config, not the instance.
- Republish mechanics that worked: `scripts/publish-cli/divfix-republish.sh <loc> [--confirm]` on Hetzner — probe `publish-bulk --dry-run` WITHOUT a manifest → `_collisions.txt` IS the DB count (every ZIP must collide with a `published` row; a non-colliding ZIP is removed, never INSERTed) → manifest `{zip: slug}` → `publish-wave.js --updates-manifest= --skip-preband --confirm` runs all 9 finalization steps scoped by `_results.txt` (`[publish] Slug:` grep is INSERT-only). Then `repoint-deck-canonical.js` (a new version dir is self-canonical again; landing-less decks like `tallinje-g3310` correctly stay self-canonical).
- `scripts/verify-hub-type-rows.js` runs the REAL `expandHubRows` + `applyLandingFilters` + `taxonomy.ts` (transpiled over an injectable taxonomy); poison 12/12; controls articles 4 / word-tracing 7 / letter-tracing 6; `--warn-missing-keys` in deploy.sh until the batch lands (260 warnings = 20 keys × 11 + taxonomy). Expectations JSON = `tools/export-hub-expectations.js` from the README matrix ("first integer" semantics → 1,285; the README's ≈1,247 netted contingencies — reconcile at close-out).
- `unitAxis`: spec `{applicable, units(locale), exemplar(locale), tokens?}`; wave `unitsPerType`/`unitOverrides`; tokens `{U}{L}{UNIT}` resolve at the STRINGS boundary (render-instance + cli), deck-html asserts none remain; `render/one.js <id> <theme|null> <d> <loc> [unit]`. The nt20-C waves ship exemplars only.
- Pre-existing, not mine: `emit/deck-html.test.js` nl sweep fails on K-302 vehicles/toys (desc 115-116) — themes it does not ship on; record, not fixed.
- pdftotext is NOT on Hetzner and raw PDF stream parsing is not a text oracle; the render HTML puppeteer printed is the proof for sheet text.

## Traps bought this session
- A quoted heredoc (`<<'EOF'`) with JS containing `\"` inside single-quoted strings still broke the shell tool twice — write scripts with the Write tool, run them by path.
- `echo "exit=$?"` after a pipeline reports the LAST command's status (tail), not the gate's.
- `git stash` with nothing tracked to stash is a silent no-op: the "before" render used the fixed code. Compare against `git show <commit~1>:<path>` swapped in, then restore from a copy.
- **Contact-sheet READ of de + pt (2026-09-14, Phase 5) found 4 defects that no gate saw, 3 of them EN-source defects inherited by every locale:** (1) G1-308 truth count frames iterated n from 1 over PLURAL frames → shipped en G1-341 said "There are exactly one sandboxes" (fixed `c0ada216`: n ≥ 2 in build AND verify, poison both ways); (2) the de panel's "Von den {pl} siehst du …" needs the dative plural (Züge → Zügen) the bank does not carry → frames rewritten "Du siehst {n} {pl}."; (3) pt left 2 out of `numbers` (dois/duas has gender) so one page mixed "quatro" and "2" → digits throughout like fr/sv (`d71b0cf5`); (4) G2-318 compare face: fixed 120-px label column let pt/fr/no labels run INTO the value ("Cobertura do corpocasco") — a `nowrap` span never clips, so the `scrollWidth > clientWidth` check was VACUOUS; now the column is sized to the longest label and the verify measures the span against ITS CELL (`746189fa`). Also `gen-b3-probe-jobs.js` read `i.theme` where the enumerator writes `cacheTheme` → every themed probe job was `undefined` and silently ERR (`8fec4c26`), and `render/batch.js` showed English chrome for non-en jobs (`686cc832`). Recipe that works: `gen-b3-probe-jobs.js waves/wave-b3{,var}-<loc>.json --out=` → `render/batch.js <jobs> --out out/b3-sweep/<loc>` → `$TEMP/family-sheets.js <loc> <renders> <sheets>` (2×3 per family, 600-px tiles) → READ all 20; visual-critic agents for the locales I do not read myself.
