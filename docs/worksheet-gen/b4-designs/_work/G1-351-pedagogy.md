# G1-351 `odd-and-even` : PEDAGOGY + CONTENT (nt10-D, 2026-09-21)

Read: `_ROLE-PEDAGOGY.md`, `_STUDIO-BRIEF.md`, `_SUBSTRATE.md`, `_PANEL-FINDINGS.md` (lock + entry 7 + Facts), `_work/K-354-pedagogy.md` + `K-355-five-senses.md` (density), `types/k/K-016-odd-or-even-pairs.js`, `types/g2/G2-218-odd-even-circle.js`, `types/g1/G1-130-chart-skip-count-color.js`, `types/g1/G1-247-doubles-halves.js` + `G1-288-halves-only.js` (+ the G1-270/271/286/287/296-299 rows), `types/k/K-021-before-and-after.js` (`_shared/neighbors.js`), `types/g3/G3-377-division-with-remainder.js` (divisor 2 lives there), `primitives/ten-frame.js`, `templates/components-b2.js` (`equalGroups`, `mirrorGroups`, `dotPanel`, `numberStrip`), `templates/components-b3/ordinal-numbers.js` (`blankNumeralBox`), `templates/components-b3/read-and-do.js` (`truthChips`) + `rhyming-words.js` (`yesNoChips`), `lib/b3-picture-index.js`, `lib/b2-common.js` (`countable`, `vocab`), `data/b2/labels.js` (`numberOfDay.tens/ones`), `data/b2/sentences.js` (names), `i18n/strings.<loc>.json` x11 (K-016, G2-218, G1-247 family, K-021, G1-130), `frontend/config/topics-taxonomy.json`, `frontend/lib/seo/strand-names.ts`, `frontend/content/seo-landing/<loc>.json` x11, `scripts/verify-hub-type-rows.js` (LEVEL_KEYS), `tools/gate-variation-distinct.js`, `tools/validate-b3-draft.js`, `qa/verify-b3-hundreds-chart-puzzles.js` (gate shape). Every px below marked (m) was measured 2026-09-21 in puppeteer with the shell's `assets/fonts/*.woff2` loaded from a `file://` page (`document.fonts.check` true for Baloo 2 700 and Nunito 800; scratch `G1-351-measure.js` / `G1-351-fontcheck2.js`); *est.* = the engineer measures. No em-dashes.

**Boundary (load-bearing).** This family owns the RULE of parity and its PROOF, on numerals: pairs of dots the child circles, the two equal addends of 2.OA.C.3 written as `n = a + a (+ 1)`, the ones digit isolated in a place-value box, the parity of a sum. It never duplicates: **K-016** `odd-or-even-pictures` (counting-pictures, K: 4 themed cards, counts d2 4-10 (d1 2-7, d3 6-13), the pictures ALREADY stacked in 2-icon columns with the leftover in a dashed coral column, the child only circles a chip; the chip words `i18n[loc].odd/even` are the ONLY odd/even literals in the catalogue and this family reuses them verbatim); **G2-218** `odd-and-even-numbers` (number-charts, G23: d2 16 loose numbers to 99 in a 4-column grid of 124 x 84 cells, "Circle every ODD number. Look at the last digit to decide.", d3 reaches 999); **G1-130** (colour a skip path on a full 100-chart, step 2 at d3); the **doubles-halves** family G1-247 + 9 rows (`mirrorGroups` reflection = the double, 2N pictures in two rows with a dashed cut line = the half, `4 + 4 = ▢` / `12 = ▢ + ▢`; heads `Verdoppeln und Halbieren` / `Dubbelt och hälften` / `Tuplaa ja puolita` x11 must not be echoed anywhere in this type); **K-021/K-131/K-132** (`makeNeighborsType`: a 1-D `[blank][given][blank]` strip); and **G3-377** `division-with-remainder` (quotative rings of d with `minR:1`, never exact, q/r notation boxes, divisor 2 at d1 and in the `2-5` unit). So: never pair-the-pictures-and-circle, never circle-odds-in-a-grid, never a coloured chart, never a double or a half, never a next-number strip, never a division glyph or a q/r box. What it therefore owns, per face: sort numerals 1-20 into an odd house and an even house with a paired-dot cue on the first three only (base) · dots the CHILD circles into pairs + `n = ▢ + ▢ + ▢(left over)` (F1) · deal pictures to two named friends, "each gets ▢, left over ▢", conclude odd/even (F2) · the ones digit in a highlighted place-value box + tick, 10-99 (F3) · parity of a sum from the ones digits without adding (F4) · count 11-20 pictures, write the PAIRS count and the leftover, conclude (F5, the lock's replacement for the provisional range-to-999 PARAM, see B).

## A. Identity

| loc | genre head (title = head verbatim; slug measured collision-free against every `axes.*` slug and every existing landing slug, m) | school year (brief band; base / F2 / F5 G1 · F1 / F3 G2 · F4 G3, per locale below) | national strand literal (framework NAME) | CCSS (en only, honest) |
|---|---|---|---|---|
| en | **Odd and Even Numbers** (slug `odd-and-even`; never "Even and Odd" in a title (lock); Tier A, unique 123, 14/14; HARD head (K5 / Education.com / Math-Drills / Twinkl / SplashLearn); WINNABLE "odd and even numbers worksheet grade 1 pdf with answers", "odd and even numbers 1 to 100 worksheet" (= F3), "even and odd kindergarten worksheet pairs" (= F5 at its lower bound); "chart printable" stays with `number-charts`) | grade 1 (base, F2, F5) · grade 2 (F1, F3) · grade 3 (F4) | Operations & Algebraic Thinking | base / F2 / F5: **none, readiness** (there is no G1 parity code; 1.OA.C.6 is NOT claimed) · F1 / F3: **2.OA.C.3** · F4: **3.OA.D.9** |
| de | **Gerade und ungerade Zahlen** (slug `gerade-und-ungerade-zahlen`; "gerade" lowercase, "Zahlen" capital; Tier A, unique 50 net ~44; HARD head (Frau Locke, PIKAS, Grundschulkönig); WINNABLE = the RANGE faces "gerade und ungerade zahlen bis 20 arbeitsblatt" (base) and "bis 100 arbeitsblatt klasse 2" (F3)) | 1. Klasse (base, F2, F5) · 2. Klasse (F1, F3) · 3. Klasse (F4) | Zahlen und Operationen (KMK Leitidee; the panel gave no de strand literal: OPEN 5) | n/a (Lehrplan "gerade und ungerade Zahlen bis 20 / bis 100") |
| es | **Números pares e impares** (slug `numeros-pares-e-impares`; euphonic "e impares", the only correct form; Tier A, unique 68, band 35; HARD head (Wikipedia / Smartick / Wordwall); WINNABLE "fichas de números pares e impares para imprimir primer grado", "del 1 al 100" (= F3); "preescolar" is REFUSED as a level (K-016 boundary)) | primer grado (base, F2, F5) · segundo grado (F1, F3) · tercer grado (F4) | Sentido numérico | n/a (SEP / NEM) |
| pt | **Números pares e ímpares** (slug `numeros-pares-e-impares`; accented; always the full phrase, bare "pares e ímpares" is the lottery intent; Tier A, unique 49, band 22; WINNABLE "atividades números pares e ímpares 1 ano para imprimir"; "de 1 a 100" = F3) | 1º ano (base, F2, F5: the harvest's year; the panel places parity at 2º, OPEN 6) · 2º ano (F1, F3) · 3º ano (F4) | Números | n/a (BNCC) |
| fr | **Nombres pairs et impairs** (slug `nombres-pairs-et-impairs`; programme wording; "pair impair CP" is the teacher shorthand for the meta; Tier A, unique 50, band 35; HARD at "pair impair" (parking / bridge / film); WINNABLE "exercice pair impair cp pdf", "nombres pairs et impairs cp exercices à imprimer"; F2 head "partage équitable") | CP (base, F2, F5) · CE1 (F1, F3; the panel's year for the notion) · CE2 (F4) | Nombres et calcul | n/a (programmes officiels) |
| it | **Numeri pari e dispari** (slug `numeri-pari-e-dispari`; never bare "pari e dispari" (a 1978 film); Tier A, unique 69, band 40; HARD at the bare head (Maestra Anita x4); WINNABLE "schede numeri pari e dispari classe prima da stampare") | classe prima (base, F2, F5) · classe seconda (F1, F3) · classe terza (F4) | Numeri | n/a (Indicazioni nazionali) |
| nl | **Even en oneven getallen** (slug `even-en-oneven-getallen`; always with "getallen"; Tier B+, unique 21 net ~14; Squla / Sommenfabriek / Junior Einstein; WINNABLE "werkblad even en oneven getallen groep 3"; `counting-frames` head "Tienraam" NEVER in a title, and this type uses no ten-frame at all, see the base) | groep 3 (base, F2, F5) · groep 4 (F1, F3) · groep 5 (F4) | Getallen | n/a (SLO kerndoelen) |
| sv | **Udda och jämna tal** (slug `udda-och-jamna-tal`; the more frequent order; "tal" never "siffror"; Tier A, r1 21 / r2 22; skolplus / mattelandet / UR own games, not sheets; framing "kan delas lika i två" for F2) | åk 1 (base, F2, F5) · åk 2 (F1, F3) · åk 3 (F4) | Taluppfattning och tals användning (Lgr22) | n/a |
| da | **Lige og ulige tal** (slug `lige-og-ulige-tal`; never "ulige og lige"; Tier A-, r1 17 / r2 6; WINNABLE "lige og ulige tal opgaver 1. klasse"; F2 "dele ligeligt", never "dele lige") | 1. klasse (base, F2, F5) · 2. klasse (F1, F3) · 3. klasse (F4) | Tal og algebra (Fælles Mål kompetenceområde) | n/a |
| no | **Partall og oddetall** (slug `partall-og-oddetall`; bokmål; "oddetall og partall" also fires; Tier A, r1 21 / r2 24; WINNABLE "partall og oddetall oppgaver 2. trinn"; "plakat" is a companion format, not ours) | 2. trinn (base, F2, F5) · 3. trinn (F1, F3) · 4. trinn (F4) | Tall og algebra (LK20 kjerneelement) | n/a |
| fi | **Parilliset ja parittomat luvut** (slug `parilliset-ja-parittomat-luvut`; Tier B, r1 17 / r2 11; OPS 1.-2. lk content, weak search ("no tehtäviä, no luokka: definitional + week-parity demand"); question titles use "vai" never "tai"; a page never contains 0, so "nolla parillinen vai pariton" never arises) | 1. luokka (base, F2, F5) · 2. luokka (F1, F3) · 3. luokka (F4) | Luvut ja laskutoimitukset (OPS 2014) | n/a |

Measured (m): `apps['odd-and-even']` and `axes['exercise-type']['odd-and-even']` are ABSENT: register via the `tools/register-b3-taxonomy.js NEW_FAMILIES` shape with `{subject:'math', age:'6-8', name:'Odd and Even Numbers', id:'G1-351'}` + slug/name x11 (the 11 slugs above hit 0 `axes.*` slugs and 0 landing slugs across the 11 `seo-landing/<loc>.json` files). The doubles-halves heads NOT to echo (m, `axes['exercise-type']['doubles-halves'].name`): Doubles and Halves · Verdoppeln und Halbieren · Dobles y mitades · Doubles et moitiés · Dobro e metade · Doppio e metà · Verdubbelen en halveren · Dubbelt och hälften · Det dobbelte og det halve · Det dobbelte og halvparten · Kaksinkertainen ja puolet, plus the shipped face titles ("Nur Halbieren bis 12", "Hälften upp till 12", "Puolita kuvaryhmät ..."): the validator bans the head TOKENS (Halb/hälft/halv/mitad/metade/metà/moitié/half/puolit) in every string of this type. Neighbour hubs that keep the bare head: `number-charts` (4 landings per locale, K/G1/G2/G3; G2-218 has NO landing of its own; K-016 has NO landing in any locale, m: 0 rows match `odd-or-even` / `k016`), `counting-frames` (nl "Tienraam").

**Strand row trap (m):** `strand-names.ts` `'Operations & Algebraic Thinking'` renders pt "Álgebra" and sv "Algebra" and nl "Bewerkingen", all wrong for parity in those curricula; the `'Number and Operations in Base Ten'` row (line 100) carries the literals the panel named for es / fr / it / pt / sv / fi and a serviceable de / nl / da / no. The landing template must take the per-locale strand literal from the table above (the design's job, as Facts 1 rules for the science types), never fall through to the OA row; en alone keeps "Operations & Algebraic Thinking". OPEN 5.

**Theme axis: THEMELESS.** `themeAxis:{applicable:false}`; every landing carries `coordinate.theme:''`; the base, F1, F3, F4 print numerals only. F2 and F5 print PICTURES, drawn from `lib/b3-picture-index.js` (`pictureIndex()` = 880 vocab keys with a colour picture, BW dirs skipped, `space/sun` blocked; `hasPicture(key, loc)` = 880 en / 879 de es pt nl sv da fi / 880 fr it / 878 no after `B2_EXCLUDE`, m) intersected with `countable(vocab[key][loc])` (plural differs from singular): **countable pool per locale = en 722 · de 608 · es 729 · pt 741 · fr 716 · it 658 · nl 751 · sv 628 · da 659 · no 659 · fi 741 (m)**; a page needs 6 distinct keys (one per card, cloned n times), so the floor (>= 40 keys, the 6-card page plus the seed sweep) is met x11 by two orders of magnitude and no theme floor applies. The picture is a COUNTING token only: no noun is ever printed, so no picture ruling (plum = apple) can reach a page as a word; the gate still asserts `fileUri` resolves for every picked key. **No `unitAxis`**: a range fan would put 1-100 on the base without the F3 scaffold (= the retired face); the range lives on exactly two faces and is part of their titles (de winner strings).

**The one rule that locks the type:** every item's answer is `n mod 2`, re-derived by verify() from the stamped `data-lcs-n` / `data-lcs-val` / `data-lcs-a`+`data-lcs-b`; the only words the code prints are the two K-016 chip literals (`bank.chips = {odd, even}`, asserted EQUAL to `K-016.i18n[loc].odd/even`), the F3 rule sentence, the three captions (`each`, `leftover`, `pairs`), the two place-value headers (`placeHeads`, lock-given labels) and two NAMES from `SENTENCES[loc].names`; no number word, no verb form, no noun, nothing inflects; `0` never appears on any page (fi "nolla" trap); `+` and `=` are universal and `=` never sits inside a dot row; a division glyph (`notation.js divGlyph`) never appears.

## B. The six faces

Common: `templates/components-b4/odd-and-even.js` (pairDots, houseFrame, proofRow, sharePlates, placeBoxes, tickPair, ruleStrip, parityTable, countLane) + the bank in D. All five variations are CODE faces on ONE additive `mode` knob (`d.mode`, absent on the base; `build()` dispatches BEFORE the base path and stamps `data-lcs-mode` only when set, so the base DOM stays byte-identical; the G1-310 precedent). Guards key on the RESOLVED config (`d.mode`, `d.min/d.max`, `d.items`, `d.cueCount`), never on the level index. `gate-variation-distinct.js` sees five distinct d2 configs (mode differs) and the base's own d2 published once. Chips everywhere = K-016's pill shape (`.ws-chip` with `width:auto; min-width:64px; height:40px; font-size:17px; padding:0 14px`, Baloo 2 700): the longest words are fi "parillinen" 73.2 px and de "ungerade" 73.0 px at 17 px (m), so a pill is <= 73 + 28 = 101 px and a pair of pills with a 10 px gap <= 212 px on every face; a tick-circle variant (`tickPair`: two 44 px `.ws-chip` circles under column headers) is used where the words would repeat per row (F3, F4).

### Base: Odd House, Even House (G1, `G1-351`, mode undefined)
**Move:** SORT: twelve numerals 1-20 on a strip; two houses side by side, each roofed with one chip word (`chips.even` left, `chips.odd` right); the child writes every numeral into a cell of the right house. The first THREE strip numerals carry a paired-dot cue (`pairDots({n})`: dots in a 2-row grid filled COLUMN-first so every column is a pair, the lone leftover dot coral with a dashed ring): worked examples, so the child sees WHY 7 is odd; the other nine are bare (the cue is the answer, so never on every item: the lock). Not a ten-frame: `ten-frame.js` fills the TOP ROW first (`i % 5`, `floor(i / 5)`), so 7 reads as "5 and 2", not as "3 pairs and 1"; the pair structure needs column-first fill, hence the new component (m, `primitives/ten-frame.js:31-33`).
**Child:** "Write each number in the house where it belongs: even house or odd house. The first three show the pairs to help you." (118; apparatus: numbers, houses, pairs.)
**Params:** d1 `{min:1, max:10, items:8, cueCount:3, cells:4}` · **d2 (ships)** `{min:1, max:20, items:12, cueCount:3, cells:6}` · d3 `{min:1, max:20, items:16, cueCount:0, cells:8}`. `items` numerals are `rng.sample` of `[min..max]` with EXACTLY `items/2` odd (equal split by construction, so both houses have `cells = items/2` writing cells and the cell count leaks nothing); the cue goes on the first `cueCount` strip tiles in DOM order (the split of the cued three is free: 2 + 1 either way). Strip tile = 52 px `.ws-chip`-sized square with Baloo 2 700 30 px ("20" = 33.5 px wide, m; 12 tiles x 50 = 600 <= 639); the three cued tiles sit on their own example row with the dot figure beneath each (n = 20 at dot 10 px + gap 4 = 10 columns = 136 px wide, 24 px tall; 3 x 160 = 480 <= 639, *est.*); houses: two `houseFrame` each ~300 wide, roof height 0.28 h with the chip word inside the gable, body with `cells` `blankNumeralBox({w:80, h:56})` in 2 columns (G1 answer numeral floor 26 met at 56 high); stack *est.* example row 92 + strip 64 + gaps 24 + houses 540 = 720 <= 722, the design agent sets `minmax`.
**verify():** stamps `data-lcs-val` on every tile, `data-lcs-cue="1"` on cued tiles (their SVG carries `data-lcs-dot` per dot and `data-lcs-leftover="1"` on the lone dot), `data-lcs-house="even|odd"` + `data-lcs-cells` on each house. Re-derived: `items` distinct values in `[min,max]`, none 0, exactly half odd; the cued tiles are the first `cueCount` in DOM order, their dot count === val and a leftover dot exists iff val is odd; every house cell is an EMPTY `.ws-blankbox` (no numeral text, no `data-lcs-answer` other than `''`); `cells === items/2` on both houses; the gable text equals `chips[loc]` (even left, odd right); no ten-frame primitive on the page.
**Refusals:** none (two chip words + the strings).
**Query face:** the bare head + the range: en "odd and even numbers worksheet grade 1" · de "gerade und ungerade zahlen bis 20 arbeitsblatt" (the de winner) · es "fichas números pares e impares primer grado" · nl "even en oneven getallen groep 3" · sv "udda och jämna tal åk 1".

### F1: Pair Proof: Two Equal Addends (G2, G2-3xx TBD, CODE `mode:'proof'`)
**Move:** PROVE: the numeral is GIVEN; beneath it a single row of n plain dots (teal, 16 px, gap 10) the CHILD circles two by two; beneath that the equation frame `n = ▢ + ▢ + ▢` where the third box is narrower, dashed coral, captioned with the `leftover` literal (Nunito 800 12 px), and takes 0 or 1. The child writes the two equal addends and the leftover, then circles the parity pill. This is 2.OA.C.3 literally (`8 = 4 + 4 + 0`, `7 = 3 + 3 + 1`); the uniform frame keeps the parity unstated (a frame that changed shape for odd numbers would print the answer). `+` universal; never "3 pairs + 1" as an equation; `=` sits in the equation row only, never in the dot row (the lock). Differs from the base: nothing to sort, a written equation; from K-016: the pairing is the child's act; from G1-247: no double, no reflection, an equal-addend SPLIT of a given number with a remainder.
**Child:** "Circle the dots two by two. Then write the number as two equal parts, and write what is left over in the small box." (115)
**Params:** d1 `{mode:'proof', min:4, max:12, items:6, cols:2}` · **d2** `{mode:'proof', min:5, max:20, items:8, cols:2}` · d3 `{mode:'proof', min:11, max:20, items:8, cols:2, pills:false}` (no pill: the leftover box is the conclusion). Card = `cardGrid({cols:2, rows:items/2})`, inner ~302 x 165 at 8 cards (*est.*: (722 - 3 x 14) / 4 - 28); dot row for n = 20: 20 x 26 = 520 > 302, so the dot row WRAPS at 10 per line (two lines of 10 = 2 x 26 = 52 px; a wrap at 10 keeps every pair inside one line); equation row Baloo 2 700 26 px: "7 = " 35.6 x 26/30 = ~31 + three boxes (52, 52, 36) + two "+" (14.2 each) + gaps = ~230 px (m for the glyphs, *est.* for the row); pills row 40. Stack per card *est.* 52 + 52 + 40 + gaps 24 = 168 vs 165: the design agent takes the dot to 14 px or the card padding to 8 (state which); G2 floors: answer boxes 52 x 44 >= 36, numeral 26 >= 22, 8 items in [8,16].
**verify():** card `data-lcs-n`; dot row `[data-lcs-dots]` with exactly n `circle` children and NO text node; boxes `data-lcs-answer` = `floor(n/2)`, `floor(n/2)`, `n % 2` in DOM order; the `=` glyph text is inside `[data-lcs-eq]` only; pills: exactly one `data-lcs-correct` and it is `chips[parity]`; n in `[min,max]`, distinct across the page, at least 3 odd and 3 even; no `.ws-blankbox` pre-filled.
**Refusals:** none.
**Query face:** "two equal addends" / "pair proof": en "even numbers two equal addends worksheet" · de "gerade Zahlen als Summe zweier gleicher Zahlen" · fr "nombre pair somme de deux nombres égaux" · sv "dela upp talet i två lika delar" · fi "kahden yhtä suuren luvun summa".

### F2: Can Two Friends Share Fairly? (G1, G1-3xx TBD, CODE `mode:'share'`)
**Move:** DEAL: one lane per item: n identical pictures (clones of ONE index picture, 44 px, up to two rows of 6) on the left; on the right two plates, each labelled with a NAME from `SENTENCES[loc].names` (two different names, `rng.sample`, 0 new literals) and holding one `blankNumeralBox`; then a small dashed box captioned `leftover` and the two pills. The child draws a line from each picture to a plate in turn (one for Mia, one for Ben, one for Mia ...), writes the count on each plate (both = floor(n/2)) and the leftover (0 or 1), and circles the pill. Sharpened from yes/no to "each gets, left over 0/1" (the lock) so it is not a coin toss. Fenced from G1-288 (2N pictures in two equal rows with a printed CUT line, `12 = ▢ + ▢`, even only): here odd counts, no cut line, named plates, a leftover, a parity conclusion; from G3-377 (quotative rings, `minR:1`, printed q/r notation): here partitive between exactly two, exact sharing allowed, NO notation, no glyph; from K-016: the child deals, nothing is pre-paired.
**Child:** "Share the pictures between the two friends, one each in turn. Write how many each friend gets and how many are left over." (121)
**Params:** d1 `{mode:'share', min:4, max:9, items:6, iconPx:44, perRow:6}` · **d2** `{mode:'share', min:5, max:12, items:6, iconPx:44, perRow:6}` · d3 `{mode:'share', min:9, max:15, items:6, iconPx:40, perRow:8, pills:false}`. Equal parity split (3 odd, 3 even) by construction. Lane *est.*: pictures block 6 x 48 = 288 wide, 2 rows = 96 high; plates block: two plates 110 x 44 (name Nunito 800 13 px above a 52 x 40 box) + leftover box 40 + pills 2 x <= 101: the pills wrap under the plates, so the right block is ~300 wide x 96 high; lane 639 wide x ~104; 6 lanes x 104 + 5 x 8 = 664 <= 722. G1 floors: pictures 44, numeral boxes 40 high with a 26 px numeral, 6 items in [6,12].
**verify():** lane `data-lcs-n` + `data-lcs-pic="<theme>/<noun>"`; exactly n `<img>` with one identical `src` that `fileUri` resolves; two `[data-lcs-plate]` with DIFFERENT `data-lcs-name` both in `SENTENCES[loc].names`; plate boxes `data-lcs-answer === floor(n/2)` both; leftover box `=== n % 2`; one correct pill = `chips[parity]`; no text matches `divGlyph(loc)`, `÷`, `:` between digits, or any doubles-halves head token; n in `[min,max]`, 3 odd + 3 even, 6 distinct picture keys, every key `countable` for the locale.
**Refusals:** none (names exist x11, m; the picture pool is 608-751 keys).
**Query face:** "share fairly / between two": en "share equally between 2 odd or even worksheet" · de "gerecht teilen: gerade oder ungerade" · fr "partage équitable pair impair" · sv "dela lika i två" (the Lgr22 framing "kan delas lika i två") · da "dele ligeligt" (never "dele lige") · fi "jaetaan tasan kahdelle".

### F3: Odd and Even to 100: Look at the Ones Box (G2, G2-3xx TBD, CODE `mode:'ones'`)
**Move:** ISOLATE the ones digit: a rule strip at the top (the `rule` literal: "Even numbers end in 0, 2, 4, 6 or 8. Odd numbers end in 1, 3, 5, 7 or 9."); then rows in two columns, each row = the number printed digit-by-digit in two place-value boxes under the lock-given headers (`placeHeads`: en T|O · de Z|E · es D|U · pt D|U · fr d|u · it da|u · nl T|E · sv T|E · da T|E · no T|E · fi K|Y; headers printed ONCE per column at the top, Baloo 2 700 16 px, all <= 12 px wide except it "da" ~18, m), the ONES box tealSoft-filled and teal-framed (the highlight IS the scaffold), and to the right two 44 px tick circles under the column heads `chips.even` | `chips.odd` (printed once per column). The child ticks. This is the only face that reaches 100 (the lock); the range is 10-99 on the sheet (a one-digit number would leave the tens box empty and 100 needs a third box: both refused as items; "to 100 / bis 100 / del 1 al 100" is the number space the titles name, as the de/es/pt harvest heads do). Fenced from G2-218 (loose numbers, circle the odds, no place-value boxes, no rule strip): here every number is decided, in boxes, from the highlighted digit.
**Child:** "Read the rule at the top. Look only at the ones box of each number and tick even or odd." (88)
**Params:** d1 `{mode:'ones', min:10, max:30, items:8, cols:2}` · **d2** `{mode:'ones', min:10, max:99, items:12, cols:2}` · d3 `{mode:'ones', min:10, max:99, items:16, cols:2, rule:false}` (the strip withdrawn: the child recalls the rule). Rule strip: en 72 chars = 497 px at Nunito 800 15 (one line in 639), de 87 = 616 (one line), pt 85 = 616 (one line), fi 107 = 684 at 14 px (TWO lines; the strip is `minmax(48px, auto)`), m; cap 214 chars (= two full lines at 14 px). Row *est.*: boxes 2 x 44 + gap 6 + 16 + ticks 2 x 44 + 10 = 208 <= 319 per column; 6 rows x 72 + header 28 + strip 48 + gaps = ~530 <= 722 (generous: the design agent may use 56 px boxes). G2 floors: boxes 44 >= 36, digits Baloo 2 700 26 >= 22, 12 items in [8,16].
**verify():** row `data-lcs-val` in `[min,max]`; tens box text `=== floor(v/10)`, ones box text `=== v % 10` and carries `data-lcs-ones="1"` + the highlight class; exactly two tick circles per row, `data-lcs-tick="even|odd"`, one `data-lcs-correct` matching `v % 2`; header texts `=== placeHeads[loc]` (2 entries, 1-2 chars each, distinct) and `=== chips[loc]`; the rule strip text `=== rule[loc]` when `d.rule !== false`, absent otherwise; values distinct, at least a third of each parity; no value < 10 or > 99.
**Refusals:** none; the placeHeads convention is the panel's (it "da|u" vs "D|U": OPEN 4).
**Query face:** the range head: en "odd and even numbers 1 to 100 worksheet" · de "gerade und ungerade zahlen bis 100 arbeitsblatt klasse 2" (the second de winner) · es "pares e impares del 1 al 100" · pt "de 1 a 100" · fi "parilliset ja parittomat luvut sataan asti".

### F4: Odd or Even Sum? Decide Without Adding (G3, G3-3xx TBD, CODE `mode:'sums'`)
**Move:** REASON about parity of a sum (3.OA.D.9): a three-row `parityTable` given at the top, built ONLY from the chip words and glyphs (`even + even = even` / `odd + odd = even` / `even + odd = odd`; longest row de "ungerade + ungerade = gerade" 217 px at Baloo 2 700 16, fi 205, no 198, m: three rows in a 260 px panel), then rows of `a + b` printed in Baloo 2 700 26 px with the ones digit of each addend underlined (coral 2 px), and the two tick circles. The child classifies each addend from its ones digit, applies the table, ticks. Three-digit addends at d2 so that adding is slower than the rule (the honest "without computing"; this is where the 3-digit demand the panels measured lands, absorbed into a face with its own head instead of the retired range face). Never a sum printed anywhere. Fenced from mental-math (computes the sum) and from F3 (single numbers).
**Child:** "Do not add. Look at the ones digit of each number, use the rule, and tick whether the sum is even or odd." (105)
**Params:** d1 `{mode:'sums', min:10, max:99, items:8, cols:2, terms:2}` · **d2** `{mode:'sums', min:100, max:999, items:10, cols:2, terms:2}` · d3 `{mode:'sums', min:100, max:999, items:12, cols:2, terms:3, table:false}`. "14 + 23" = 76.9 px at 26 px, "347 + 258" *est.* 2 x 43.5 + 14 + 16 = ~117 (from "999" 43.5 and "+" 14.2 at 26, m); row = 117 + 16 + 98 = ~231 <= 319 per column; table 3 x 24 + 16 = 88; 5 rows x 64 + table 88 + gaps = ~440 <= 722. G3 floors: ticks 44 >= 36, numerals 26 >= 22, 10 items in [8,16].
**verify():** row `data-lcs-a`, `data-lcs-b` (and `data-lcs-c` at terms 3); printed text is exactly `a + b` with the locale-neutral `+`; the row contains NO text equal to `String(a + b)`; ticks: one `data-lcs-correct` matching `(a + b) % 2`; the table rows read `chips[x] + chips[y] = chips[z]` with the three true combinations and nothing else (`table:false` = absent); at least 4 even and 4 odd sums at d2; addends distinct across the page; all in `[min,max]`.
**Refusals:** none (chip words only; the table's `+`/`=` are universal).
**Query face:** "parity of sums / without adding": en "odd and even sums worksheet grade 3" · de "Summe gerade oder ungerade ohne zu rechnen" (3. Klasse) · fr "somme paire ou impaire sans calculer" · it "la somma è pari o dispari?" · fi "onko summa parillinen vai pariton" (3. lk).

### F5: Odd or Even? Count the Pictures in Pairs (G1, G1-3xx TBD, CODE `mode:'count'`; REPLACES the provisional PARAM range-to-999)
**Move:** COUNT then PAIR: one lane per item with 11-20 identical pictures (clones, 44 px, rows of 10 so the second row shows "ten and some more", never the parity); the child counts, circles the pictures two by two, writes `pairs ▢` and `leftover ▢` (captions = the `pairs` and `leftover` literals) and circles the pill. K-016 reaches 13 at d3 and SHIPS 4-10, always pre-paired: this face's numbers are beyond it and the pairing plus the written pairs count are the child's. Fenced from F1 (numeral given, dots, an equation) and F2 (deal to two friends, "each gets"): here the count is unknown and the written quantity is the number of PAIRS.
**Why the replacement (the lock's provisional clause):** measured 3-digit demand is es "del 1 al 1000" x1 and sv/da/no "till/til 1000" as panel query faces only; the range-to-999 PARAM would be the base's sort with bigger numbers, i.e. F3's move without its scaffold, and G2-218 d3 already hunts to 999; F4 d2 carries the 3-digit numbers under a head of its own. The pictures face fills the en "even and odd kindergarten worksheet pairs" intent at the G1 end (11-20) with a move no live type reaches.
**Child:** "Count the pictures and circle them in pairs. Write how many pairs and how many are left over, then circle odd or even." (118)
**Params:** d1 `{mode:'count', min:8, max:14, items:6, iconPx:44, perRow:10}` · **d2** `{mode:'count', min:11, max:20, items:6, iconPx:44, perRow:10}` · d3 `{mode:'count', min:15, max:20, items:6, iconPx:44, perRow:10, pills:false}`. Equal parity split. Lane *est.*: pictures 10 x 46 = 460 wide, 2 rows = 94 high; right block 130 wide: `pairs ▢` / `leftover ▢` stacked (captions Nunito 800 12 px: longest es "parejas" 41.3, de "bleibt übrig" 66.5 at 12 px, m) + the two pills below the lane text or stacked at 40 high; 460 + 12 + 130 + 12 = 614 <= 639; 6 lanes x 100 + 5 x 8 = 640 <= 722. G1 floors met (44 / 26 / 6 items).
**verify():** lane `data-lcs-n` in `[min,max]`, exactly n `<img>` of one `src`, `data-lcs-pic` resolves; pairs box `data-lcs-answer === floor(n/2)`, leftover `=== n % 2`; one correct pill; 3 odd + 3 even; 6 distinct countable keys; caption texts `=== pairs[loc]` / `leftover[loc]`; no numeral printed near the pictures (no `countBadge`).
**Refusals:** none.
**Query face:** "count and decide / pairs": en "even and odd worksheet pairs count" · de "Bilder zählen: gerade oder ungerade" · es "cuenta y forma parejas: par o impar" · nl "tel en maak paren: even of oneven" · fi "laske kuvat pareittain".

**Rejected non-moves.** A range as a face (bis 20 / bis 50 / bis 1000 on the SAME sort: the range only earns a face when the scaffold changes, F3) · the PARAM range-to-999 (thin demand, F3 without its scaffold, G2-218 d3 already reaches 999; its numbers live in F4 d2) · a ten-frame cue (fills top row first, hides the pairs) · a coloured 100-chart (G1-130) · circle-the-odds in a grid (G2-218) · pre-paired picture columns (K-016) · doubles / halves / a cut line (G1-247 family) · a `[before][n][after]` strip (K-021) · yes/no chips on F2 (a coin toss; replaced by "each gets / left over") · a q/r notation row (G3-377) · odd/even as a COLOURING key (read-and-color family, and the K-016 chip is the only literal the child needs) · printing "+ 1" only on odd cards in F1 (prints the answer).

## C. Native rebuild x11

Frames NEVER inflect: this type has NO frame with a slot at all. Every printed word is a whole literal from the bank: the two chips (REUSED from `K-016.i18n[loc]`, asserted equal), `rule` (one sentence), `each`, `leftover`, `pairs` (three captions), `placeHeads` (two 1-2 char labels, lock-given) and two names from `SENTENCES[loc].names` (exist x11, m). Per locale the panel authors 12 strings (6 titles + 6 instructions) + 1 rule + 3 captions + 2 placeHeads = 18 literals and CONFIRMS the 2 chips (20 in all; the ≤ 4 new-literal cap of the lock holds: rule, each, leftover, pairs; placeHeads are the lock's own GIVEN labels). Widths (m, Nunito 800 14 px): `each` candidates run 51.8 (da "hver får") to 103.5 (es "cada uno recibe"), `leftover` 28.8 (nl "over") to 77.5 (de "bleibt übrig"), `pairs` 22.1 (sv/da/no "par") to 48.2 (es "parejas"); at 12 px the longest is 88.7: the caption cell is 110 px wide at 12 px, the validator caps a caption at 16 chars.

| loc | literals the panel authors (counts) | slots / forms and where they already exist | refusal / re-target | traps |
|---|---|---|---|---|
| en | 12 strings · rule (72 ch, 1 line) · each "each gets" · leftover "left over" · pairs "pairs" · placeHeads T\|O · chips odd/even (K-016) = 20 | names Mia/Ben/Emma/Leo/Anna/Tom/Lily/Max (`sentences.js`) | none | title never "Even and Odd" (lock); "pdf" / "with answers" belong to the meta (answer key shipped); US spelling; "chart" never in a title (number-charts owns it) |
| de | 12 · rule ("Gerade Zahlen enden auf 0, 2, 4, 6 oder 8. Ungerade Zahlen enden auf 1, 3, 5, 7 oder 9.", 87 ch = 616 px, 1 line) · each "jeder bekommt" · leftover "bleibt übrig" · pairs "Paare" · Z\|E · chips ungerade/gerade = 20 | names Emma/Ben/Mia/Finn/Lina/Paul/Lea/Noah | none | chips print the BARE lowercase adjective (never "Gerade", never "gerade Zahl"); "Zahlen" capital in titles; "Paare" not "Pärchen"; range words "bis 20" (base) / "bis 100" (F3) in the FACE titles; no "Verdoppeln/Halbieren" token anywhere; engine appends "Arbeitsblatt" |
| es | 12 · rule · each "cada uno recibe" (103.5 px at 14: the widest caption, drops to 12 px) · leftover "sobra" · pairs "parejas" · D\|U · chips impar/par = 20 | names Sofía/Mateo/Lucía/Diego/... | none; level "preescolar" REFUSED (lock, K-016 boundary) | "e impares" (euphonic e) in every title; MX register; "del 1 al 100" on F3; never "mitad/mitades" (doubles-halves head) |
| pt | 12 · rule · each "cada um recebe" · leftover "sobra" · pairs "pares" · D\|U · chips ímpar/par = 20 | names Sofia/Miguel/Alice/Davi/... | none | accented "ímpares"; ALWAYS "números pares e ímpares" (bare phrase = lottery); "de 1 a 100" on F3; "atividade" never in a title; year placement OPEN 6 |
| fr | 12 · rule · each "chacun reçoit" · leftover "il reste" · pairs "paires" · d\|u · chips impair/pair = 20 | names Léa/Hugo/Chloé/Louis/... | none | "pair/impair" masculine with "nombre" ("nombres pairs"); F2 head "partage équitable"; "fiche" never in a title; never "moitié" |
| it | 12 · rule · each "ognuno riceve" · leftover "avanza" · pairs "coppie" · da\|u (the abaco convention; OPEN 4) · chips dispari/pari = 20 | names Sofia/Leonardo/Giulia/... | none | never bare "pari e dispari" (film); "scheda" never in a title; never "metà" |
| nl | 12 · rule · each "ieder krijgt" · leftover "over" · pairs "paren" · T\|E · chips oneven/even = 20 | names Emma/Daan/Julia/Sem/... | none | always "getallen" with the head; "Tienraam" never in a title (counting-frames head) and no ten-frame is drawn; "werkblad" is appended; never "halveren/verdubbelen" |
| sv | 12 · rule · each "var och en får" · leftover "blir över" · pairs "par" · T\|E · chips udda/jämnt = 20 | names Elsa/Hugo/Alma/Liam/... | none | "jämnt" (neuter, agrees with "tal") on the chip, "jämna tal" in the title; "tal" never "siffror"; "vecka" in NO string (the week-parity meaning pollutes the meta); never "hälften/dubbelt"; engine appends "arbetsblad" |
| da | 12 · rule · each "hver får" · leftover "tilovers" · pairs "par" · T\|E · chips ulige/lige = 20 | names Ida/William/Freja/Oscar/... | none | "lige og ulige" order, never reversed; F2 "dele ligeligt" never "dele lige"; never "det halve" |
| no | 12 · rule · each "hver får" · leftover "til overs" · pairs "par" · T\|E · chips oddetall/partall = 20 | names Nora/Jakob/Emma/Oliver/... | none | bokmål only ("partall og oddetall"); the chips are NOUNS (partall / oddetall), fine as labels; never "halvparten" |
| fi | 12 · rule ("Parilliset luvut päättyvät numeroon 0, 2, 4, 6 tai 8. Parittomat luvut päättyvät numeroon 1, 3, 5, 7 tai 9.", 107 ch = 684 px at 14 px, TWO lines) · each "kumpikin saa" · leftover "jää yli" · pairs "parit" · K\|Y · chips pariton/parillinen = 20 | names Aino/Eeli/Sofia/Onni/Aada/Väinö/Helmi/Leo | none | question titles use "vai" never "tai" ("Parillinen vai pariton?"); "tai" is correct inside the digit list of the rule; 0 never on a page; "20:een asti" / "sataan asti" range idiom (existing fi titles); never "puolita/tupla"; instruction cases are the panel's; the 4-line fi title (677 budget) is the stack every face must survive |

## D. Data + gates

Bank = `data/b4/odd-and-even.js` (EN block hand-authored) + `data/b4/locales/odd-and-even.<loc>.json` (GENERATED from `i18n/.draft-b4-<loc>.json` by `tools/apply-b4-locale.js` after `tools/validate-b4-draft.js`; `lib/b4-common.js bank(name, loc)` reads `data/b4/`). `data/` is gitignored (`git add -f`). Read at render: `bank('odd-and-even', loc)`, `SENTENCES[loc].names`, `pictureIndex()` + `countable` (F2/F5), `fileUri`; never `image-vocabulary.js` directly beyond `vocab()` for the countable test, never `approved-words-*.json`, never `LABELS.numberOfDay` (the full tens/ones words are NOT printed; the lock wants the abbreviations).

```
ODD_AND_EVEN[loc] = {
  chips:      { odd, even },            // MUST equal K-016.i18n[loc].odd / .even (the catalogue's only parity literals)
  rule:       'Even numbers end in 0, 2, 4, 6 or 8. Odd numbers end in 1, 3, 5, 7 or 9.',   // F3 strip, <= 214 chars, contains the ten digits 0-9 once each
  each:       'each gets',              // F2 caption (informational: the plates carry the boxes)
  leftover:   'left over',              // F1 third box + F2 + F5 caption, <= 16 chars
  pairs:      'pairs',                  // F5 caption, <= 16 chars
  placeHeads: ['T', 'O'],               // F3 column labels, 1-2 chars each, distinct (lock-given: de Z|E fr d|u es D|U nl T|E sv T|E fi K|Y ...)
  strings:    { 'G1-351':{title, instruction}, <F1..F5 ids>:{title, instruction} },
  refuse:     [],                       // per-face refusals: 'proof'|'share'|'ones'|'sums'|'count' (none expected)
}
GLOBAL (code, not data): ranges per d-level, the equal-parity split, dot geometry, the parity table order [even+even, odd+odd, even+odd].
```

**`tools/validate-b4-draft.js` (odd-and-even block; every rule runs, exit 1 on any):**
1. `chips.odd` and `chips.even` non-empty, distinct (case-insensitive), and `=== require('types/k/K-016-odd-or-even-pairs.js').i18n[loc].odd/.even` (one source; a divergence is a K-016 edit request, never a second literal).
2. de chips are lowercase (`/^[a-zäöü]/`); no chip contains a space; every chip <= 12 chars (fi "parillinen" 10; pill <= 101 px).
3. `rule` <= 214 chars, contains each digit 0-9 exactly once, contains both chip STEMS (the first 4 letters of each chip, case-insensitive; en "even"/"odd", de "gera"/"unge", fi "pari"/"pari" collapses: for fi require both full chips), no `{`, ends in a mark.
4. `each`, `leftover`, `pairs` non-empty, <= 16 chars, pairwise distinct, no digit, no `{`; none equals a chip.
5. `placeHeads` = exactly 2 entries, each 1-2 chars, distinct, letters only.
6. No string in the block (chips, rule, captions, titles, instructions) contains a doubles-halves head token: `/halb|hälft|halv|mitad|metade|metà|moitié|\bhalf|puolit|verdopp|verdubb|doppio|dobro|doble|double|dubbelt|dobbelte|tupla|kaksinkert/i`; no string contains a neighbour head (`Tienraam` nl, `siffror` / `vecka` sv, `Hundertertafel` de); en titles never contain "Even and Odd"; it titles never equal "Pari e dispari"; pt titles never start "Pares e ímpares".
7. Titles <= 70 chars, no worksheet-word (the b3 gate regex), no free-claim, unique within the band against the locale's existing strings; the base title contains the head; F3's title contains the locale's 100-range idiom (a digit string "100" or "sataan"); instructions <= 150 ending in a mark; fi question titles must not contain " tai " (must use "vai").
8. Every face title differs from every other by at least one NOUN or verb token (never only an adjective).
9. `refuse` entries name a real mode; `strings` ids === {base + the 5 allocated ids}.
10. Build probe (the b3 gate's shape): every face builds at d2 for the locale with 3 seeds; F2/F5 must find >= 40 countable keys via `pictureIndex()`; the base must find no `data-lcs-prim="ten-frame"` in its DOM.

**Poison (each must FAIL for its OWN rule; the correct EN draft is the control):**
P1 de `chips.even = 'Gerade'` -> rule 2 (capital). P2 `chips.even = 'odd'` (= odd) -> rule 1. P3 sv `chips.even = 'jämna'` -> rule 1 (differs from K-016's "jämnt"). P4 `rule` with "0, 2, 4, 6 or 8" only (no odd half) -> rule 3 (digits 1-9 missing). P5 `leftover = 'what is left over now'` (19 ch) -> rule 4. P6 `placeHeads = ['Tens','Ones']` -> rule 5. P7 de F2 title "Gerecht halbieren: gerade oder ungerade?" -> rule 6 (halb). P8 en base title "Even and Odd Numbers" -> rule 6. P9 fi F5 title "Parillinen tai pariton?" -> rule 7 (tai). P10 F3 title "Odd and Even Numbers: Look at the Ones Box" without "100" -> rule 7. P11 F1 title "Odd and Even Numbers: Bigger Pair Proof" vs base "Odd and Even Numbers" -> rule 8 passes (a noun added) but F1 vs a second face "Odd and Even Numbers: Big Pair Proof" -> rule 8 FAILS (adjective only). P12 sv instruction containing "vecka" -> rule 6. P13 it base title "Pari e dispari" -> rule 6. P14 `strings` missing F4 with no `refuse` entry -> rule 9.

**`qa/verify-b4-odd-and-even.js` (render gate; own ground truth, clone of the b3 gate shape):** renders base + 5 faces x 11 locales x d2 (+ d1/d3 for the base, + a 5-seed sweep per face) through `render/render-instance.js` (file:// fonts) under the 722 stack (3-line de title + 150-char instruction) AND the 4-line fi title (677); asserts `verify()` empty and `qa/lints.js` clean, then the floors ITSELF: strip tiles >= 52 and their numerals >= 26 px (base), house cells >= 56 high, cue dots >= 10 px with the leftover dot distinguishable (fill coral + a dashed ring), F1 dots >= 14 px with gap >= 8 and the dot row containing no text node, every answer box >= 40 high, every tick circle / pill >= 40 high, pictures >= 44 (F2/F5 d2), F3 boxes >= 44 and the ones box's computed background === tealSoft while the tens box's === white, the rule strip <= 2 lines, F4's printed sums absent (no text node equals `a + b` anywhere on the page), the parity table exactly 3 rows of chip words, NO `data-lcs-prim="ten-frame"` on any face, no `÷` / `:` glyph between digits, no doubles-halves token in the rendered text, headers `placeHeads` present exactly once per column, the equal-parity split on every page, `0` absent from every page, the chip words rendered === the bank's, and answer hiding: no `.ws-blankbox` with a non-empty text node, no `data-lcs-answer` whose value is printed inside its own card. Poison-tested by mutating the RENDER (a pre-filled box, a swapped tick stamp, a ten-frame injected into the base, a sum printed in an F4 row): each must fire.

## E. SEO

`coordinate = {type:'odd-and-even', mode:<face>, theme:'', level:<band key of the locale>}`; modes: base `'base'` · F1 `'proof'` · F2 `'share'` · F3 `'ones'` · F4 `'sums'` · F5 `'count'` (`verify-hub-type-rows.js` demands a non-empty mode STRING, m). Level keys (m, `LEVEL_KEYS`): base / F2 / F5 = en `grade-1` · de `1-klasse` · es `primer-grado` · pt `1o-ano` · fr `cp` · it `classe-prima` · nl `groep-3` · sv `ak-1` · da `1-klasse` · no `2-trinn` · fi `1-luokka`; F1 / F3 = `grade-2` · `2-klasse` · `segundo-grado` · `2o-ano` · `ce1` · `classe-seconda` · `groep-4` · `ak-2` · `2-klasse` · `3-trinn` · `2-luokka`; F4 = `grade-3` · `3-klasse` · `tercer-grado` · `3o-ano` · `ce2` · `classe-terza` · `groep-5` · `ak-3` · `3-klasse` · `4-trinn` · `3-luokka` (all present in the map). Meta lead inherits `seo.words.free_printable` (metadata only, never visible copy). The MIDDLE is the instruction stem; the exact per-face window is measured on the staged ZIPs with `tools/measure-instruction-window.js` (exists, m).

| face | title pattern Germanic (en / de / nl) | Romance (es / pt / fr / it) | Nordic + fi (sv / da / no / fi) | meta MIDDLE (en) |
|---|---|---|---|---|
| base | Odd and Even Numbers: Odd House, Even House · Gerade und ungerade Zahlen bis 20 · Even en oneven getallen tot 20 | Números pares e impares hasta el 20 · Números pares e ímpares até 20 · Nombres pairs et impairs jusqu'à 20 · Numeri pari e dispari fino a 20 | Udda och jämna tal till 20 · Lige og ulige tal til 20 · Partall og oddetall til 20 · Parilliset ja parittomat luvut 20:een asti | Write each number in the house where it belongs, even house or odd house; the first three show the pairs to help you |
| F1 | Odd or Even? Write Two Equal Addends · Gerade oder ungerade? Zwei gleiche Summanden · Even of oneven? Twee gelijke delen | ¿Par o impar? Dos sumandos iguales · Par ou ímpar? Duas parcelas iguais · Pair ou impair ? Deux nombres égaux · Pari o dispari? Due addendi uguali | Udda eller jämnt? Två lika delar · Lige eller ulige? To lige store dele · Partall eller oddetall? To like deler · Parillinen vai pariton? Kaksi yhtä suurta lukua | Circle the dots two by two, then write the number as two equal parts and write what is left over in the small box |
| F2 | Can Two Friends Share Fairly? · Gerecht teilen: gerade oder ungerade? · Eerlijk delen: even of oneven? | ¿Se puede repartir entre dos? Par o impar · Dá para dividir entre dois? Par ou ímpar · Partage équitable : pair ou impair ? · Si può dividere in due? Pari o dispari | Kan det delas lika i två? · Kan det deles ligeligt? · Kan det deles likt på to? · Voiko jakaa tasan kahdelle? | Share the pictures between the two friends, one each in turn, and write how many each friend gets and how many are left over |
| F3 | Odd and Even Numbers 1 to 100: Look at the Ones Box · Gerade und ungerade Zahlen bis 100 · Even en oneven getallen tot 100 | Números pares e impares del 1 al 100 · Números pares e ímpares de 1 a 100 · Nombres pairs et impairs jusqu'à 100 · Numeri pari e dispari fino a 100 | Udda och jämna tal till 100 · Lige og ulige tal til 100 · Partall og oddetall til 100 · Parilliset ja parittomat luvut sataan asti | Read the rule at the top, look only at the ones box of each number and tick even or odd |
| F4 | Odd or Even Sum? Decide Without Adding · Summe gerade oder ungerade? Ohne zu rechnen · Even of oneven som? Zonder te rekenen | ¿Suma par o impar? Sin sumar · Soma par ou ímpar? Sem somar · Somme paire ou impaire ? Sans calculer · Somma pari o dispari? Senza calcolare | Udda eller jämn summa? Utan att räkna · Lige eller ulige sum? Uden at regne · Partall eller oddetall sum? Uten å regne · Parillinen vai pariton summa? Laskematta | Do not add: look at the ones digit of each number, use the rule, and tick whether the sum is even or odd |
| F5 | Odd or Even? Count the Pictures in Pairs · Gerade oder ungerade? Bilder paarweise zählen · Even of oneven? Tel de plaatjes in paren | ¿Par o impar? Cuenta los dibujos en parejas · Par ou ímpar? Conte as figuras aos pares · Pair ou impair ? Compte les images par deux · Pari o dispari? Conta le figure a coppie | Udda eller jämnt? Räkna bilderna parvis · Lige eller ulige? Tæl billederne parvis · Partall eller oddetall? Tell bildene parvis · Parillinen vai pariton? Laske kuvat pareittain | Count the pictures and circle them in pairs, write how many pairs and how many are left over, then circle odd or even |

All titles are candidates the native panels rewrite (<= 70, no worksheet word, base = the head, each face adds one NOUN / verb / range, never an adjective alone; the de base and F3 carry the range because the range strings ARE the de winners). JSON-LD `LearningResource`; `educationalAlignment.targetName` en only and only where honest (F1 / F3 `2.OA.C.3`, F4 `3.OA.D.9`; none on base / F2 / F5: readiness), no `targetUrl`; every non-EN landing names the framework of A and the strand literal of A. `topicMeta['odd-and-even']` >= 50 chars x11 + a `skill-sentences.<loc>.json` entry (full 60-180, short 15-90) via the registrar; the skill sentence must not mention doubles.

**Non-cannibalisation** (whole-landing 3-gram Jaccard, `scripts/seo-landing/gate.js` FAIL >= 0.80 / WARN 0.65; *est.*, the gate measures):

| pair | what the copy says differently | est. |
|---|---|---|
| base vs F3 | houses, 1-20, worked pairs vs place-value boxes, 10-99, a rule strip; the shared head is the only shared n-gram cluster | 0.30 |
| base vs K-016 (deck page only: NO landing exists, m) | sorting written numerals into houses vs pre-paired pictures and a chip; the base never says "pair them up" | 0.15 |
| F1 vs G1-247 / G1-288 (`doubles-halves` landings, `mode:null`, themed) | a given number split into two EQUAL addends plus a leftover vs doubling a group / halving an even group with a cut line; F1 never uses a doubles-halves token (validator rule 6) | 0.15 |
| F1 vs F5 | numeral given, dots, an equation vs an unknown count of pictures and a written PAIRS count | 0.35 |
| F2 vs F5 (the closest pair) | two named friends, "each gets", dealing vs counting and pairs; F2 copy never says "count" / "pairs", F5 copy never says "friend" / "share" (a lint on the landing bodies) | 0.40 |
| F2 vs G1-288 halves | odd counts, a leftover, plates with names vs even groups cut in half | 0.20 |
| F2 vs G3-377 division-with-remainder | share between exactly two, no notation, grade 1 vs rings of d with q and r, grade 3 | 0.10 |
| F3 vs G2-218 (via the `number-charts` G2 hub landing, mode `skip-count-and-100-more-less`) | boxes and a highlighted digit, tick every number vs a loose grid, circle the odds | 0.15 |
| F3 vs base-ten / place-value hubs (`base-ten` de "Stellenwert", sv "Tiotal och ental") | the boxes are a scaffold for parity, no blocks, no expanded form; the copy leads with odd/even | 0.15 |
| F4 vs mental-math hubs | never a sum written; the rule table; grade 3 | 0.10 |
| F4 vs F3 | two addends and a table vs one number and a highlighted box | 0.30 |
| any face vs `counting-frames` (nl "Tienraam") | no ten-frame drawn or named | 0.05 |

Boundary sentence on every landing: "The child works out WHY a number is odd or even: pairs, two equal parts, the ones digit, the parity of a sum. Circling pre-paired pictures is on the Odd or Even? page; hunting odd numbers in a grid is on the Odd Number Hunt page."

## F. Open questions + summary

**Engineer must measure:** (1) the base stack at the fi 4-line title (677): example row + strip + two houses of 6 cells; if short, the example row folds INTO the strip (cue under the first three tiles at 8 px dots, n = 20 -> 10 columns x 12 = 120 px under a 52 px tile: the cue then overhangs its tile, so the design agent chooses row-vs-inline and states it). (2) F1 card inner height at 8 cards (2 x 4) vs the 168 px stack: dot 16 vs 14 px, padding 12 vs 8. (3) F2 lane at 6 lanes: pictures 2 x 6 at 44 vs the plates block; whether the 12 lines from pictures to plates cross (a line from every picture to a plate 200 px to the right is a K-3 convention, but the design agent may replace the lines by "write M or B under each picture" ONLY if the names' initials are distinct x11: they are in all 8-name banks, m, but a sampled pair must be checked at build). (4) the F3 ones-box highlight contrast on print (tealSoft on cream) and the fi rule strip at two lines inside `minmax(48px, auto)`. (5) F4 "347 + 258" width at 26 px (*est.* 117) and whether the coral underline under each ones digit survives the PDF (a `text-decoration` vs an SVG rule). (6) `gate-variation-distinct.js` sees all five modes as distinct configs (they differ in `mode`, and F1/F3 also in ranges). (7) the `apply-b4-locale.js` / `validate-b4-draft.js` clones carry rule 1 (a cross-file equality to `K-016.i18n`).

**Only a native panel can rule:** (OPEN 1) each locale's rule sentence and the three captions (the table above lists CANDIDATES with measured widths, not panel text); (OPEN 2) the F2 head per locale (sv "kan delas lika i två", da "dele ligeligt", fr "partage équitable", fi "jakaa tasan") and whether the plates carry names or a neutral "1 / 2"; (OPEN 3) the six titles, in particular whether the base carries the range ("bis 20") as in de or the houses sub-head as in en, and the fi "20:een asti / sataan asti" idiom; (OPEN 4) placeHeads: it "da|u" (abaco) vs "D|U", pt "D|U" vs "d|u", no/da "T|E"; (OPEN 5) the per-locale strand literal (de "Zahlen und Operationen", da "Tal og algebra", no "Tall og algebra", nl "Getallen" are my fill; the panel confirms or replaces; the OA row of `strand-names.ts` must NOT be what renders for pt/sv/nl); (OPEN 6) year placement where the harvest and the curriculum disagree (pt 1º vs 2º ano; fr CP vs CE1; no 2. vs 3. trinn): the landing states the curriculum year in prose either way.

**Summary.** Six faces, all CODE on one `mode` knob, base byte-identical; themeless, numerals only except the two picture faces (cross-theme countable index, 608-751 keys per locale, no theme floor); 20 literals per locale of which 2 are K-016's chips reused verbatim and 2 are the lock's given place-value labels; every answer is `n mod 2` re-derived from stamps; `0`, ten-frames, division glyphs and every doubles-halves token are banned by validator and gate; the provisional range-to-999 PARAM is replaced by the count-the-pictures-in-pairs face (11-20, the pairs count written), with the 3-digit numbers carried honestly by the parity-of-sums face at grade 3; no locale refusal is expected (the panels' refusal slots stay in the bank).
