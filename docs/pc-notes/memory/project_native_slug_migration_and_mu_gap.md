---
name: native-slug-migration-and-mu-gap
description: Non-EN deck URLs migrated to native exercise-mode slugs (complete); μ title-disambiguation gap remains for 8 locales
metadata: 
  node_type: memory
  type: project
  originSessionId: a483163b-b9ed-4fc1-b2d8-9b138b8d5678
---

**SEO remediation Part 5 (2026-05-31): native exercise-mode slug migration COMPLETE for all non-EN decks.**

The 8,781 non-EN catalog deck URLs whose slug carried an English exercise-mode token (`find-addend`, `secret-word`, `easy`, `findBig`, etc.) were re-slugged to native (`summand-finden`, `mot-secret`, `facil`, `grosses-finden`, …) via `scripts/publish-cli/migrate-native-mode-slugs.js`. Old URLs stayed 200 with canonical→new (no-404 soft consolidation via re-pointed old symlinks).

**[UPDATE 2026-05-31 — canonical audit] The "NO nginx 301 map" is SUPERSEDED: an nginx 301 old→new map is now LIVE.** A full canonical-URL audit (0 defects across 19,537 decks + 11,450 sitemap URLs; the crawler's "canonicalised" flags were exactly this soft-consolidation) upgraded the soft-consolidation to a hard 301. Old slugs (all 8,781, the exact symlink-derived set; en=0) now **301→native** at the edge. Mechanism (server-side, NOT git per §15.7): `map $uri $deck_redirect` (body `/etc/nginx/old-slug-redirects.map`, ~17.5k lines incl. slash+no-slash keys) + `if ($deck_redirect){return 301;}` at the top of the `^/<locale>/decks/(.+?)/$` location, ABOVE `try_files`. Needs `map_hash_max_size 131072; map_hash_bucket_size 256;` (17.5k long-key entries — smaller values fail/warn). **Regenerate** after any future slug migration: `node scripts/publish-cli/gen-old-slug-redirects.js` (walks decks symlinks → map+pairs) then `python3 scripts/publish-cli/patch-nginx-deck-redirects.py` (backup `/root/nginx-backups/` → install map → patch → nginx -t with auto-rollback → reload; idempotent). Verifier: `scripts/publish-cli/audit-canonicals-crawl.js` (every sitemap URL → 200 + self-canonical). All three committed `d69b3f5b`. Old symlinks left in place (nginx 301 intercepts before FS); cleanup deferred. Followed by `migrate-deck-url-columns.js` (the 5 `Deck.{html,thumbnail,pdf,answerKey,manifest}Url` columns also embed the slug — feed rendered deck-card + strip thumbnails), hreflang re-inject, fi `varten`→`tasolle`, deck-end strip re-run + alt-text re-retrofit. Sitemap reads `Deck.slug` → emits new slugs after 30-min ISR. Full FS+DB backup at `/var/www/lcs-media/backups/decks-premigration-20260530T234005Z*`.

**Hard-won lesson — the deck slug is embedded in MANY surfaces, not just the path.** A clean "slug-only" migration is wrong: also update the 5 DB URL columns + (cosmetically) the pdf/answer-key asset FILENAMES, and re-run the deck-end strips (their tile thumbnails come from `Deck.thumbnailUrl`, NOT slug-derived). `inject-deck-end-strip.js` must use `--by-version-dir` post-migration (the dual old/new bare symlinks make a symlink-walk double-process each deck.html); its strip removal was hardened to be regex-robust + whitespace-tolerant. Strip re-inject WIPES the §21 deckend-thumb alt-text → re-run `rewrite-deck-html-alt-text.js` after.

**[RESOLVED 2026-05-31] title-disambiguation extended to de/fr/it/nl/sv/da/no/fi.** The SEO-100pct title-disambiguation only ever covered en/es/pt; the other 8 locales had genuinely-colliding deck titles (de 297 groups, fr 337, it 285; nl/sv/da/no/fi 0). **The OLD `disambiguate-titles-mu.js`/`-finalize.js` (variant_id→"Set NNN") are OBSOLETE** — the title engine was overhauled (`build-seo-head.js composeTitle`, new `titleConfig` path) to use a content-derived **differentiator** phrase + a collision-safe bare **disambiguator** code (NOT variant_id). The modern fix (no code changes): `audit-title-differentiators.js --locales=… --emit-map=dmap.json` (read-only; builds locale→slug→code map; code = variantId else sha1(slug)[:6]) → verify with `--map` (expect 0 collisions) → `republish-seo --language <loc> --disambiguator-map <path> --confirm` per locale (space-separated flag!) → re-inject hreflang (republish-seo wipes the SEO-marker region). de needed a 2nd republish pass to clear sequential-unique-constraint ordering stragglers. Final `audit-deck-html.js`: de 2998/2998, it/nl/sv/da/fi clean, fr 3014/3015 (1 trivial), no 0/1 (single-deck strip). 1625 codes applied. URL-stable (titles only). Disambiguator is a bare code → NO native-speaker authoring needed; all 11 `seo-title-config.json` blocks already authored.

Related: [[long-locale-wave-publish-gaps]], [[feedback-content-publishing-seo-standard]].
