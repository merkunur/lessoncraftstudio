# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

/lib/generated/prisma

# =============================================================================
# WORKSHEET GENERATORS - COMPLETE EXCLUSION
# =============================================================================
# ALL files in worksheet-generators are managed independently from code.
# Source of truth: REFERENCE APPS and REFERENCE TRANSLATIONS folders
# These folders are in the main lessoncraftstudio directory, NOT in frontend.
#
# CRITICAL: Never track these files in git. Deployments must use REFERENCE folders.
# =============================================================================

# Ignore EVERYTHING in worksheet-generators (HTML, JS, translations, scripts, docs)
/public/worksheet-generators/*
/public/worksheet-generators/**/*

# Content managers at root level
/public/homepage-content-manager.html

# =============================================================================
# GOLDEN_BACKUPS and REFERENCE folders ARE tracked (they are in root, not frontend)
# No need for negation rules here - they're not in /public/
# =============================================================================
