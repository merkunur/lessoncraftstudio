/** G1-407 — What Animals Need. nt5-F variation of G1-398 (generated by tools/gen-b6var-specs.js from tools/b6var-rows/). */
'use strict';
const base = require('./G1-398-animal-habitats.js');
// One object for all three levels: the waves ship d2 only, so a face must
// render identically whichever level is asked for. Spreading the base entry
// (not a JSON literal) carries function-valued params through intact.
const D = { ...base.difficulty[2], ...{"layout":"needs","rows":4,"foodPair":true,"homePair":true,"pic":84,"chip":96} };
module.exports = {
  ...base,
  id: 'G1-407',
  slug: 'what-animals-need',
  difficulty: { 1: D, 2: D, 3: D },
  i18n: { en: { title: "What Animals Need", instruction: "For each animal, circle the food it eats and the home it lives in." } },
  gradeBand: "G1",
};
