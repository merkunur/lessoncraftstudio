---
name: project-premium-games-program
description: The premium games program — 300+ simple K-3 paid games as the free→paid conversion lever. Phase-0 catalog DONE; per-game build pending operator ruling.
metadata: 
  node_type: memory
  type: project
  originSessionId: c449e1ef-7c7c-48e7-af05-927e055606b1
---

**RESUME (2026-06-20):** Phase-1 FOUNDATION + the first pilot game are BUILT, all gates green, fully LOCAL (operator locked: NOTHING deploys until all ~300 games are ready). Strategy + 390-game catalog + 3-layer vision stay DONE+approved (do NOT re-litigate). **Operator decisions this session:** stay 100% local until 300 ready · build foundation+1 game first then expand · cross-game collection = per-device localStorage · English-only pilot. **What shipped (all NEW, `lcs-shell` 0-line, uncommitted on branch `pivot/printable-business-toolkit`):** game runtime `mini tools/game-shell.{js,css}` (sibling to lcs-shell — reuses its `LCS.i18n`/`LCSAudio`/`LCS.drag`/the exact `lcs-activity-resize` postMessage; auto-advance round flow, NO Check/score/timer; per-mount Fisher-Yates §A.13.60; non-numeric "world fills" vessel) · `game-assets.js` (pose-aware CSS/SVG STUBS so games play with zero art + `GameAssets.register(realSheet)` swap + `.manifest()` CA5 worklist) · `game-collection.js` (localStorage `lcs.games.collection.v1`, grants a keepsake per completed pass) · engine `engine-feed-the-friend.js` + game `games/mochi-counting-feast.js` (Mochi's Counting Feast, K.CC.B.4/5, 10 rounds, EN-only, tap-to-feed exactly N, belly tally, chomp/yum) · local harness `game-preview.html` (DOUBLE-CLICK, no server: game/locale/viewport pickers + asset-manifest panel + collection peek) + `collection.html` · pipeline `scripts/games/build-sprites.js` (CA5 PNG frames → trimmed+padded uniform-cell NxM webp sheet via sharp composite [og-image.js pattern] + frames.json + stepped-keyframe sprites.css) + `asset-manifest.js` · 3 LOCAL gates `scripts/games/audit-game-{variety,mobile,no-competition}.js` (≥7 rounds+reshuffle+stub-playable / overflow 280-768 / forbidden-token+protected-cores-0-line) — ALL GREEN. Verified end-to-end via puppeteer on `file://`: 10 rounds play, vessel fills, a full pass grants "Mochi the snow-bear" to the collection, 0 JS errors; 3-viewport screenshots at `mini tools/.preview-shots/`. **[2026-06-20] DIVERSITY PIVOT — operator rejected the "15 templates re-skinned ~300×" model:** "I want at least 100 DISTINCTIVE game designs" (the catalog's own critics had flagged re-skin fatigue as the #1 risk). Ran an **expert-ensemble distinctive-design pass** (operator-approved): 16 design-LENS ideation agents (partitioned by interaction-domain, NOT curriculum, to force mechanical diversity) → 265 raw designs → rigorous dedup/cluster (→152 distinct mechanics, 0 lost) → coverage map + feasibility/locks gate (120 keep / 23 keep-fix / 8 retarget / 1 drop) + 28 gap-fill designs for the real holes (Writing, Speaking/Listening, RI, grammar producers, gr2/3 data, gr3 math). **DELIVERABLE: `C:\Users\rkgen\.claude\plans\premium-games-distinctive-designs.{json,md}` = 179 GENUINELY-DISTINCT mechanics**, 17 interaction families, 110 distinct K-3 CCSS codes, math 112/ela 67, 143 carry `also_teaches` (the re-skin axis → the 179 distinct designs span the full ~300+ library as variations, not 15 templates). 15 flagged needs-richer-runtime. Grades K85/Gr1 51/Gr2 37/Gr3 6 — **Gr3 is THIN (the original strategy named Gr3 Math highest willingness-to-pay → a targeted gr3-deepening ideation pass is the obvious follow-up).** Supporting artifacts in the same dir: `distinctive-clusters.{json,md}`, `coverage-report.md`, `gate-verdicts.json`, `gap-fills.json`, `ideation/*.json` (the 16 raw pools). The `.md` has a family breakdown + full catalog + coverage summary + a top-30 build order (starts from the proven feed-the-friend engine; Mochi built). **This SUPERSEDES the original `premium-games-catalog`'s "15 reusable engines" framing as the design driver** (that catalog's 390 games become candidate SKINS that ride on these 179 distinct mechanics). **NEXT: operator reviews/prunes `premium-games-distinctive-designs.md` → the approved set becomes the build backlog → resume building one distinctive game at a time on the existing foundation (never a template re-skin).**

**(earlier NEXT, now superseded by the diversity pivot:** operator eyeballs `game-preview.html` locally + approves the LOOP → then expand to the rest of the 6-8 pilot (Monster Mouth Comparing, Number Train Conductor, Decompose the Dragon's Hoard, Array Garden, Multiplication Dojo, Fraction Feast Cart, a story-maker — one new ENGINE each, same data-driven pattern). Plan file `C:\Users\rkgen\.claude\plans\lets-start-building-the-lazy-rainbow.md`.** **LOCAL TEST LOOP (added 2026-06-20, frictionless art-iteration):** `npm run games:serve` (`scripts/games/serve.js` — zero-dep Node http server, serves `mini tools/`→`/mini-tools/` + local `image-library-webp/`→`/image-library-webp/` production-identically, `no-store`, **live-reload via fs.watch+SSE** so edit+save → browser auto-refreshes, auto-opens the harness). `build-sprites.js` now emits a **co-located RELATIVE** CSS `url()` + a self-registering **`<sheet>.register.js`** → built art loads by just existing in `mini tools/sprites/` (no per-game edit) on the server AND plain `file://`. Harness auto-injects each declared sheet's `register.js` (404=stays stub) + a **"use built art" stub⇄art toggle**. Verified: real art renders on server+file://, toggle flips, live-reload fires, library serves 200, traversal blocked, all 3 gates green, lcs-shell 0-line. Root `package.json` scripts: `games:serve`/`games:sprites`/`games:assets`/`games:audit`. Per-game art loop: stub → `asset-manifest.js`/harness surfaces CA5 worklist → operator renders in CA5 → `build-sprites.js --in=<frames> --sheet=<id>` → files land in `mini tools/sprites/` → harness auto-loads on next reload (toggle to compare). Post-pilot forks (deferred, operator's call): full launch-locale set (en+de+es/nl) · SSR `/games/[slug]` route+JSON-LD+sitemap+nav · paywall gate (`isLcsSubscriptionActive`) — ALL deferred until the 300-game library is ready.

**(Original RESUME, pre-build:** strategy + 390-game catalog + the 3-layer premium VISION are DONE + operator-APPROVED; build Phase 1 = local-preview loop + CA5 sprite pipeline + asset-stub kit, then pilot slice for LOCAL approval. Superseded by the line above now that Phase 1 foundation shipped.)**

**The platform's FIRST premium/paid product** (the §7 "future revenue-model commission" made real). 300+ **simple, one-level, low-animation, FUN** educational games covering the full K-3 CCSS curriculum (Math + ELA), grouped into curriculum-covering **bundles**, gated behind a **paid subscription** — the lever to convert free users to paid. **Everything else stays FREE forever** (decks, worksheet-generators, activities, tools) — so the games must be extremely good + marketable or nobody converts. Quality + full-curriculum coverage is the moat.

**Architecture (locked):** the **Rank-1 web shell** (DOM + CSS sprite-sheets; Pixi only if a single game truly needs a game loop) — NOT Godot (multi-MB wasm disqualified by the device budget + CC can't self-verify GDScript like puppeteer verifies DOM + full pipeline rebuild). Investigation in [[seo-landing-page-program]]-era plan file `cc-commission-calm-tiger.md`. SEO/locale is owned by the SSR Next page, NOT the iframe → render-tech change is SEO-neutral.

**5 build-workflow requirements (operator-locked):** (1) **LOCAL PREVIEW before every deploy** (deploys are 8+ min; games iterate hard) — deliver a self-contained double-click preview HTML per game; operator approves THEN deploy. (2) **Asset-stubbed first** — fully playable with programmatic placeholder art → surfaces the CA5 asset list the operator must render. (3) **CC builds the sprite-sheets** from the operator's CA5 image sequences (proven `sharp` pipeline, the CA5 probe). (4) Simple + low animation. (5) **Expert-ensemble-driven** (educational game designer + K-3 pedagogy + premium content creator) for BOTH the list AND each game.

**Phase 0 DONE (catalog):** expert-ensemble workflow (15 agents, ~1.9M tokens) → **15 game types, 390 games enumerated → ~299 keep after dedup/padding + ~16 coverage-gap games = ~315 launchable**. **Catalog persisted at `C:\Users\rkgen\.claude\plans\premium-games-catalog.{json,md}`** (558KB/436KB; the JSON feeds the build phase). 15 types: feed-the-friend, reveal-grid, grow-tend, build-a-world, build-a-buddy, dress-decorate, sticker-album, sort-home, match-pairs, pop-the-right-ones, i-spy-scene, magic-machine, path-journey, story-maker, magic-trace. 8 bundles (grade × subject "Game Clubs"). **Build order:** persistent cross-game reward/meta-collection shell FIRST → 6-8 pilot demo slice → Grade-3 Math (highest willingness-to-pay) → K Math+Reading (largest audience) → Gr2 ELA → Gr1 → rest → gap-fill last. **Load-bearing risk:** re-skin fatigue (15 types carry ~300 games) → the program lives/dies on (a) the persistent cross-game meta-collection shell + (b) the §20.2 per-session Fisher-Yates variety shuffle. **Coverage gaps to fill:** Writing across grades (W.1.x, W.2.1/2.2, W.3.2), grammar L.1.1.C-I, K.G.B.5, 2.MD.D.9, 2.MD.B.5, 3.OA.A.4, RF.1.3.F gr1.

**Open operator rulings (none blocked the catalog):** **F1** — premium bundle = games-only [clean, recommended] OR games + rebuilt lesson-plans/teaching-packages (a NUKED domain, 2026-05-17 commit `920aebbc`; CLAUDE.md top-AMENDMENT STOP rule — a separate from-scratch rebuild). **F2** — the paywall/entitlement track (dormant Lemon Squeezy + `model Subscription` schema:189 + `docs/SUBSCRIPTION-SCOPE.md`) + the price/tier ($69/yr retired 2026-05-17, "revenue model TBD" §7) — a PARALLEL track, operator sets pricing. **F3** (folded into ensemble brief, not open) — free indexable landing + free sample per game (preserve flywheel) + the no-competition lock (no timer/score/streak; fun via play/character/collection).

**CONVERSION STRATEGY (expert ensemble, 11 agents, 2026-06-19; report at `C:\Users\rkgen\.claude\plans\premium-strategy-report.{md,json}`):** Operator reaffirmed **NO progress tracking** (locked) + revealed the prior premium (teaching-packages/lesson-plans/bundles) was **deleted for bad quality** — generic prose filler. **CORRECTED MODEL (operator, 2026-06-19 — the strategy ensemble wrongly assumed games were FREE; operator corrected twice):** **The 300+ games are PAID/EXCLUSIVE to premium, NOT free.** FREE = ONLY the existing catalog (decks, 29 worksheet-gen apps, ~64 activities, mini-tools, PDFs, embedding) — the acquisition funnel; free users hunt + assemble it themselves + get NO games. **PREMIUM = THREE reinforcing layers (operator-approved north star, plan `cc-commission-calm-tiger.md`):** (1) **ACCESS** — the exclusive 300+ games, gated; (2) **ASSEMBLY** — the games AND the relevant FREE content ordered into a ready-to-use package per teaching point ("everything I need to teach addition-to-10 in German kindergarten, sequenced, in my language/framework") so prep is already done — the renewal driver; (3) **PREMIUM TEACHING MATERIALS (ongoing pipeline)** — NEW printable worksheets + activities built to FILL THE COVERAGE GAP per curriculum area (the free ~64 activities cover only PART of each area — e.g. measurement needs far more than the few free ones), premium-only, completing "everything a K-3 teacher needs per area." A sustained editorial pipeline, NOT a one-time build. Pitch: "Unlock the games + get everything (free+premium) organized for you by teaching point + the materials that complete every area." NATIONAL-framework alignment (Lehrplan/OPS 2014/Lgr22/SLO Kerndoelen/BNCC — NOT "Common Core translated") + 11 languages is the moat over ALL THREE; every major competitor is English-only. NO TRACKING (education.com even RETIRED its Progress Tracker — validates the lock); no-account teacher usage (projection / class-code / take-home). **Roadmap:** build the GAMES first (the draw) → then the assembly layer + the premium gap-filling-materials pipeline → then paywall+tiers; free catalog stays the open funnel. **The moat:** 300 games × 11 languages × national-framework alignment — every major competitor (education.com/ABCmouse/SplashLearn/Starfall) is ENGLISH-ONLY → the multilingual K-3 niche is wide open. **Anti-failure quality doctrine (structural):** premium artifacts are ASSEMBLY/CURATION/native-framing of already-vetted FREE assets — they author **NO new pedagogy prose** (the exact failure mode); a unit pack = a curated ORDER + a framework tag + one-click links to free games. **6 premium components:** (1) curated multilingual UNIT PACKS [flagship], (2) one-link/one-code CLASS-PLAY bundles [no accounts/no data], (3) printable CLASSROOM KITS ×11 langs, (4) at-home WEEKLY PACKS + session-only intrinsic reward [parents, fast-follow], (5) custom batch generation [later], (6) school portal + offline [later]. **3 tiers:** Parent/Family ~$45-49/yr · Teacher ~$69-79/yr (KEEP the $69 anchor — failure was quality not price; reuses dormant LS) · School/International-School license with TRANSPARENT published pricing (~$300/$700/$1,200 for 5/15/30+ teachers, undercutting education.com's $100-150/teacher). **Teacher usage WITHOUT tracking (4 market-proven workflows):** projection (smartboard, no devices) · assign-by-link/class-play-bundle (one link/code, any tablet, no login, no data) · printables · take-home codes. **Phased:** Phase 0 depth-first locale pick (en+de+es/nl where the free catalog fills a real sequence) → Phase 1 launch the 3 launch components there → Phase 2 parent tier → Phase 3 school license; budget premium as a CURATION/EDITORIAL product, not a feature build.
**OPEN operator rulings (from the strategy):** locale priority for premium launch; revenue/runway expectation (no-friction = slower conversion); editorial/curation bandwidth/month; confirm price points; the parent reward-loop must stay session-only (any server-persisted progress would break the no-tracking lock); willingness to do school-coordinator outreach.
**NEXT (await operator ruling):** rule the conversion strategy (premium composition + tiers + locale pick) → THEN the games-build Phase 1 (local-preview loop + CA5 sprite pipeline + asset-stub kit) + the unit-pack/class-play-bundle infra + the paywall track. Catalog approval + build order also still pending. Cross-ref [[project-activities-architecture]], [[feedback-activities-approval-cadence]], §7, §20.4, §A.13.49 (national-framework lexicon — the moat's substrate already exists).
