---
name: project-seo-quality-program
description: "Ongoing \"build perfect SEO\" program (meta/images/alt/cannibalization); operator fully delegated. Part 1 (rich-result robots) DONE."
metadata: 
  node_type: memory
  type: project
  originSessionId: dde9f1c7-9413-40cd-ae99-95dc48cc73fc
---

Operator (non-technical, fully delegating: "employ expert SEO + research agents and build perfect SEO; I don't have the knowledge to direct you") wants: keyword-rich **long-tail, web-search-grounded** meta titles+descriptions for **decks/landings/PDFs**, **no cannibalization**, great alt text, and especially **Google showing each deck with its image in search results**. This is the broader SEO program — SoT plan `C:\Users\rkgen\.claude\plans\employ-expert-agents-to-optimized-stonebraker.md` + the current plan `most-of-the-pdf-floating-hickey.md` (top section). Distinct from the (closed) [[project_deck_asset_url_drift_fix]] and the [[project_seo_recovery_program]] (the 12-part one, which already did much: PDF metadata, alt retrofit, hreflang, structured data — all audited healthy 2026-06-20).

**Audit (2 expert-SEO agents + WebSearch + raw-curl, 2026-06-25) — STRONG, don't redo:** structured data (LearningResource+ImageObject+thumbnailUrl+BreadcrumbList, 2026-compliant); image sitemap (2 imgs/deck + 119k landing entries from drift-task Part 3); og-image composite; alt-text (rich/localized via `buildDeckRichAlt`/`buildOgImageAlt` in `frontend/lib/deck-seo.ts`); PDF metadata; **hreflang present** (the agent "no hreflang" was a lossy-fetch artifact — ALWAYS verify such claims on raw curl); EN/ES/PT landing copy. **VERIFIED GAPS:** (1) [HIGH·done] max-image-preview/max-snippet dropped site-wide; (2) [LARGE] 8/11 locales' landing copy NSR-pending (de/fr/it/nl/sv/da/no/fi); (3) [MEDIUM] landing-less deck titles vocab-differentiated not demand-driven; (4) [DEFERRED] thin/near-dup at scale (needs GSC data).

**PART 1 DONE 2026-06-25 — site-wide rich-result robots fix.** Root cause: Next REPLACES (not merges) the root layout's `robots` when a page sets its own, so every indexable `generateMetadata` with `robots:{index,follow}` silently dropped the root's `max-image-preview:large`+`max-snippet:-1`+`max-video-preview:-1` (`layout.tsx:57`) → suppressed large worksheet thumbnails in SERP (the #1 goal) + truncated snippets. Fix: new SoT `frontend/lib/seo/robots.ts INDEXABLE_ROBOTS` at all 9 indexable routes; `build-seo-head.js` bakes the meta into deck.html (forward) + `inject-deck-robots-meta.js` retrofitted 45,756 deck.html (idempotent). Verified live (landings/activities/tools/topics/makers + deck.html all emit it; no regression on noindex pages; authored intersections inherit). **LESSON: Next page-level `robots` REPLACES root — use a shared INDEXABLE_ROBOTS const everywhere indexable, never bare `{index,follow}`.**

**PART 2 — BUILD MORE LANDINGS (operator chose this 2026-06-25; "Build more keyword-targeted landing pages").** The §22 landing fan never covered the **printable-gen math-skill types** (telling-time, fractions, geometry, measurement, arrays-multiplication, graphing-data, number-charts) — ~314 landing-less EN decks that collapse into ~85 landings. Build EN first via the §22 machinery (gen → `gate.js` → en.json → deploy → `repoint-deck-canonical`). EXCLUDE cryptogram (themeless decode wall) + math-worksheet (Gr3-6 off-ceiling). Plan SoT `most-of-the-pdf-floating-hickey.md` (top section).

**PILOT DONE 2026-06-25 — telling-time (themeless lead-slice), LIVE + verified (commit `9bb4791e`).** 3 EN landings `/en/worksheets/telling-time-grade-{1,2,3}` (1.MD.B.3 / 2.MD.C.7 / 3.MD.A.1; 21/20/40 decks collapsed), all 81 decks repointed canonical→landing. **Reusable mechanics proven:** themeless coordinate = `(type, granularity-mode, theme:"", level)` (distinct `mode` per grade avoids coordKey collision since coordKey excludes level); route got a `'grade-3'` LEVELS band (K-3 ceiling for printable types, additive/regression-safe) + a themeless-breadcrumb guard (`&& l.coordinate.theme`); `gen-telling-time.js` appends with a format-fidelity round-trip guard (no 171k-line reflow); gate PASS (0 FAIL, distinct granularity vocab per grade fences clean). **LESSON: en.json is 2-space `JSON.stringify(_,null,2)`; ALWAYS round-trip-guard an appender. coordKey = `type|mode|theme` (NO level) — themeless same-type landings MUST differ on mode.**

**BATCH 2 DONE 2026-06-26 — the 6 themed math-skill types, 18 EN landings LIVE (commit `4b3211e9`).** Granularity ruling: **per (type,level)** (NOT per theme) — one strong skill+grade page collapsing themes/modes/sub-skills (the dominant search intent is skill+grade for math; per-theme would be ~80 THIN pages). 18 landings cover **229 decks**, all repointed canonical→landing. fractions(gr2 2.G.A.3/gr3 3.NF.A.1) · geometry(K K.G.B.4/gr2 2.G.A.1/gr3 3.G.A.1) · measurement(K K.MD.A.2/gr1 1.MD.A.2/gr2 2.MD.A.1/gr3 **strand-only** temp+volume) · arrays-mult(gr2 2.OA.C.4/gr3 3.OA.A.1) · graphing-data(gr1 1.MD.C.4/gr2 2.MD.D.10/gr3 3.MD.B.3) · number-charts(K K.CC.A.3/gr1 1.NBT.A.1/gr2 2.NBT.A.2/gr3 3.OA.D.9). Machinery: `enum-printable-mathskills.js` (DB→coords JSON, absolute db-require so it runs from /root w/o git-pull conflict) + `gen-printable-mathskills.js` (authored prose + carousels + format-guard). Gate PASS. Excluded: arrays K+gr1 (4 tiny/off-skill), cryptogram, math-worksheet. **EN math-skill types COMPLETE (telling-time 3 + these 18 = 21 landings, ~310 decks).**

**LOCALE FAN-OUT (operator chose 2026-06-26: take the 21 into the other 10 languages; NATIVE SEO experts + native web-search per language; ASK operator to switch to plan mode before EACH language; one language per plan-mode session).** Decks exist in ALL 11 locales (314 each — the §14.10 "en-only" note was taxonomy, not deck publication). Proposed order de→es→nl→fr→it→pt→sv→da→no→fi (themeless → no `*-render.js` engine needed, unlike §22.5).

**de DONE 2026-06-26 (language #1, commit `0fc0d661`): 21 German landings LIVE, 310 decks repointed, hreflang EN↔de reciprocal.** Native German ensemble (3 agents) + German keyword research (Grundschulkönig/Mathefritz). German slugs/bands (Vorschule/1.-3. Klasse — ADDED de `3-klasse` LEVELS band)/strand; framework=Lehrplan; CCSS kept as machine targetName. Gate PASS. **Reusable machinery (clone per locale): `enum-printable-mathskills.js --locale=<loc>` (locale-aware canonical) → coords; native 3-agent ensemble → `printable-mathskills-prose-<loc>.json` (keyed by EN join-key); `gen-printable-mathskills-i18n.js --locale=<loc>` (extend BANDLBL_BY_LOCALE+TYPE_NOUN_BY_LOCALE) → `<loc>.json`; add grade-3 band if missing; gate; deploy; repoint --locale=<loc>.** LESSONS: non-EN prose runs shorter → ask ensemble ≥215 words (gate floor 200); p1 must contain the literal level-label (slotTokens lint); landing slug native but coordinate.type/mode stay English (hreflang); **Next emits camelCase `hrefLang` — grep accordingly.** `FRAMEWORK_BY_LOCALE` is **pre-wired for all 11** (es=Nueva Escuela Mexicana, sv=Lgr22, nl=SLO-kerndoelen, da=Fælles Mål, it=Indicazioni, no=LK20, fr=Éducation Nationale, pt=BNCC, fi=OPS 2014) → per-locale work = band-map + native slug/prose/strand only.

**es DONE 2026-06-26 (language #2, commit `58235c04`): 21 MX-Spanish landings LIVE, 310 decks repointed, 3-way hreflang EN↔de↔es.** MX-Spanish ensemble (primaria, NOT peninsular). MX slugs/bands (Preescolar/Primer–Tercer grado — added es `tercer-grado` LEVELS band); MX/SEP strand; framework=Nueva Escuela Mexicana (pre-wired). Gate PASS. gen-i18n now accepts prose field `band` (es) OR `deBand` (de); extended BANDLBL_BY_LOCALE+TYPE_NOUN_BY_LOCALE with es. MX agents hit ≥215 words first try (de lessons baked into prompts).

**nl DONE 2026-06-26 (language #3, commit `3970032a`): 21 Dutch landings LIVE, 310 decks repointed, 4-way hreflang EN↔de↔es↔nl.** Native Dutch ensemble. Dutch slugs/groep bands (Kleuters/Groep 3–5 — added nl `groep-5` band; the Dutch groep system is offset but the existing band AGES already mapped 1:1 to EN grades, so the map is clean kindergarten→kleuters/grade-1→groep-3/grade-2→groep-4/grade-3→groep-5); SLO strand; framework=SLO-kerndoelen. nl BANDLBL uses lowercase ("groep 4"/"kleuters") since Dutch writes them lowercase mid-sentence (the lint anchor must match the natural case the ensemble writes). Gate PASS. Group-3 agent came in short (162-193) → one SendMessage revision round fixed it (257-281). **fr DONE 2026-06-26 (language #4, commit `82c631ae`): 21 French landings LIVE, 310 decks repointed, 5-way hreflang EN↔de↔es↔nl↔fr.** Native French ensemble. French slugs/bands (Maternelle/CP/CE1/CE2 — added fr `ce2` band; clean 1:1-by-age map K→maternelle/gr1→cp/gr2→ce1/gr3→ce2); cycle-2 strand; framework=Éducation Nationale. Protected title QUAD (fiches+à imprimer+gratuit+PDF). Gate PASS. All 21 valid first pass (248-268 words — French ensemble nailed it). **it DONE 2026-06-26 (language #5, commit `052902a9`): 21 Italian landings LIVE, 310 decks repointed, 6-way hreflang.** Native Italian ensemble. Italian slugs/bands (Infanzia/Classe prima–terza — added it `classe-terza` band); Indicazioni-Nazionali nucleo strand; framework=Indicazioni Nazionali. Protected trio schede+da stampare+PDF. Gate PASS. (Validator gotcha: title trio check must be case-insensitive — IT titles start "Schede" capital.) **pt DONE 2026-06-26 (language #6, commit `541a6a6f`): 21 BR-Portuguese landings LIVE, 310 decks repointed, 7-way hreflang (pt→pt-BR code).** Native BR ensemble (anos iniciais, NOT pt-PT). BR slugs/bands (Educação infantil/1º–3º ano — added pt `3o-ano` band); BNCC unidade-temática strand; framework=BNCC. BANDLBL uses display labels w/ º ordinal; merge normalizes `°`→`º` in "Nº ano". Gate PASS. **5 of 10 done... → 6 of 10 done (de/es/nl/fr/it/pt) = 126 landings / 1860 decks. NEXT = sv (language #7), await operator plan-mode. Remaining: sv→da→no→fi.**

**⚠️ PROCESS NOTE (operator very frustrated 2026-06-26):** several standalone tool calls were emitted MALFORMED (bare `<invoke>`/`<parameter>` + stray prefix token instead of `antml:`-namespaced tokens) → harness rejected unparsed → command never ran → looked like a "freeze" (operator had to retype). MITIGATION: emit each tool call with exact `antml:`-namespaced tokens, no prose prefix on the risky standalone calls; if "malformed" returned, retry immediately. Do NOT let this recur on sv/da/no/fi.

**REMAINING after Part 2:** landing-less deck title/desc keyword opt (Part 3 — modest, the 32k landing-having decks are anti-cannibalization-split by design, don't touch); thin/near-dup at scale (GSC-data-driven); og-image XMP exiftool verify (deferred).
