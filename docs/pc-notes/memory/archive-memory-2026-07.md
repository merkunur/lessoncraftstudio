---
name: archive-memory-2026-07
description: Verbatim archive of all MEMORY.md entry bodies at the 2026-07-03 compaction (index went 212KB -> one-liners)
metadata:
  type: project
---

# MEMORY.md full-entry archive (compaction of 2026-07-03)

## 💰 STRATEGY REPORT DELIVERED 2026-07-03 (awaiting operator ruling): monetization+SEO analysis → "free discovery, paid depth" (storybooks-first premium, Teacher $69/yr; ALL current click-earning surfaces stay free; consolidate the 30k-landing theme tail; single move = GSC-driven CTR surgery + consolidation). Report: `C:\Users\rkgen\.claude\plans\business-strategy-report-2026-07.md`. Measured: ~33 clicks/day flat, 78% to topic pages, 55k indexed, 169 users/0 subs, deploy-window 502s, CF cache 4.4%. → [Business strategy 2026-07](project-business-strategy-2026-07.md).


---

## 📖 STORYBOOKS — foundation SHIPPED 2026-07-01 (`17962da7`, deployed WITH the German batch — the 2026-06-29 deploy-hold is RESOLVED): PixiJS third-sibling runtime + SBModules contract + sb-1 schema + SEP bridge + "Pip's Picnic" slice live at /en/activities/pips-picnic-interactive-storybook. Story #2..#N = data work (docs/storybook/authoring-guide.md; validate-story + qa-storybook gates). → [Interactive storybooks](project_interactive_storybooks.md).


---

## PREF — always write FULL URLs (never bare filenames/relative paths) for anything openable. Games harness: http://127.0.0.1:5173/mini-tools/game-preview.html (or file:///C:/Users/rkgen/lessoncraftstudio/mini%20tools/game-preview.html). → [Write full URLs](feedback-write-full-urls.md).


---

## 🇩🇪 ACTIVE — GERMAN SECOND-BATCH FAN-OUT (started 2026-06-29). The "second batch" = the **122 EN-only lcs-shell activities** (games build-walk, deployed `12327ce5`). Fanning them to **German, ONE per plan-mode session**, via a 3-agent native ensemble (linguist + Grundschule educator + SEO), **rebuild-not-translate**. **DEPLOY POLICY (2026-06-29): build ALL locally, deploy NOTHING until all built, then deploy ALL in one go** — per-activity = commit+push only, NO deploy (local visual-qa needs no deploy). Operator says "switch to plan mode + continue" before each; approve each via local screenshots. **NEVER skip (operator-locked): always find the relevant German teaching point + rebuild** (Fuß→Füße, in dem→im, German Anlaut/Silben, Nominativ/Akkusativ) — US vs German curricula+language differ. math/measurement first. **Through #93 (~30 EN-only remain), as of 2026-07-01:** #1 seriation deployed pre-policy; **#2–#93 DEPLOYED 2026-07-01 in the combined storybook deploy (`17962da7` chain) — the batch deploy is DONE; live de spot-checks 200 + 0 Common-Core leaks** ( latest #91 marina `e9dcac58` / #92 story-spine `c699ee62` / #93 pearl-opinion-page `7d7c2f7c`). Coverage now spans Math/Measurement/Geometry/Data + L (grammar+vocab) + RI + RL + W. **Reusable doctrine:** route `GRADE_OVERRIDE`/`STRAND_OVERRIDE` (+ K-cluster K-label: grade-K school-content → Klasse 1 unless content drives higher [bis-100→Kl.2, bis-1000 arith→Kl.3]; en stays Kindergarten) + ADDITIVE `strand-names.ts` entry per new CCSS strand (RI→„Sachtexte verstehen", RL→„Literarische Texte verstehen", W→„Texte verfassen", vocab→„Wortschatz untersuchen"…). **0-core is the norm** (engines grade by a kind/role/enum tag; the English `words`/`overlap`/`matchesPurpose` a-z helpers are AUDIT-ONLY, not the play grader). Per-round content = `roundsL10n.de`/`storiesL10n.de` (keep kind/role/id/band tags VERBATIM); `var LANG` from `api.lang`; image-bound → `_DE` noun map, drawn-SVG → none; core-blocked English-table engines → activity-layer wrapper fns (never edit the core). de.json prose inner quotes MUST be curly U+201E „/U+201C " (straight " breaks JSON), §A.13.29 examples match shipped rounds, plain „&". §A.13.62 DoD per build (node-assert + verify-de + preflight + visual-qa de/en/de + personal Read 360/768/1024 DESKTOP-first + EN-leak grep + git-diff 0-core → commit+push NO deploy). LESSON: never `*/` in a JS block comment; `node --check` before visual-qa. **The full #1–#93 blow-by-blow + per-activity ensemble rulings → the out-of-tree plan file `C:\Users\rkgen\.claude\plans\the-previous-session-you-groovy-pie.md`.** Per-activity recipe + gates + batch-deploy + doctrine → [German second-batch fan-out](project_german_secondbatch_fanout.md).


---

## 🔥 ACTIVITY PAGES (operator FURIOUS 2026-06-28, FIXED `16dd5a48`) — every new activity needs BOTH a hand-written `prose[<id>]` full-override in `frontend/messages/activity-content/en.json` (else the page renders templated "shit") AND a generated preview webp (`generate-activity-previews.js --only=<id>` → scp to `/var/www/lcs-media/mini-tools/previews/`, else the card has no thumbnail). EN done (194, `16dd5a48`); DE done (57, `8351a08a`); FR done (57, `617f616a`); es=MEXICAN done (57, `037d8d2a`+`44107fb2`); pt=BRAZILIAN done (57, `a6f49a56` — BNCC already BR so NO framework switch; cleaned 1 "jardim de infância"→"pré-escola" title + 2 "Comboio"→"Trem"; gate checks EU-PT tells comboio/autocarro/ecrã/rapariga/telemóvel; rendered pt-only preview BEFORE deploy so no redeploy needed); it done (57, `d1426132` — cleanest: framework already "Indicazioni nazionali", single-register so NO regional cleanup, no foreign residue; euro/centesimi money; review fixed inherited {strand} + an array build/equal-groups dup opener); nl done (58 — one more activity than others; `e7cffc95` — framework already "SLO-kerndoelen", Netherlands-Dutch groep system kleuters/groep 3·4, euro/cent; gate bans Flemish "gij"; review fixed inherited {strand} + de/het error + non-word typo + calque + anglicisms + 3 dup openers); sv done (57, `7c5ba5b9`, [NSR-FLAG][sv] per §17.5.1 Nordic — framework already "Lgr22", förskoleklass/årskurs 1·2, **kronor (kr) NOT euro**; review fixed inherited {strand} + semantic talkompis-vs-tiokompis + malformed words + anglicism compound + dup openers); da done (58, `963324ae`, [NSR-FLAG][da] — framework already "Fælles Mål", børnehaveklasse/1.·2. klasse, **danske kroner**; da-only id has `.da.` suffix; gate adds Cyrillic check [a drafting agent caught a стray Cyrillic typo]; review fixed inherited {strand}); no done (57, `9a9acda6`, [NSR-FLAG][no] — bokmål canonical §6, framework already "LK20", barnehage/skolestart/1.·2. trinn, **norske kroner**; no-only id has `.no.` suffix [shares slug-shape with sv per §20.2]; review fixed inherited {strand} + en/et error "et klokkeslett" + non-standard "tiekroning"→"tikrone" + dup openers); fi done (55, `67d7ac1c`, [NSR-FLAG][fi] — framework already "OPS 2014", esiopetus/1.·2. luokka, **euro** [not kroner]; §A.13.56 case-heavy: native review caught a serious terminology error alkuluku"prime"→vähennettävä"minuend" + colloquial number-words + calque kutsuu→vaatii + vowel-harmony clitic Meniko→Menikö ×6 + inherited {strand}) — each via native-language-expert agents (5 draft + 1 review) + gate `scripts/verify-activity-content-<loc>.js`. Each non-EN = 57 activities/54-to-author; touch only `prose` (labels/templates already localized). §20.10: prose cites national framework name (DE "Lehrplan", FR "les programmes officiels", es-MX "los planes y programas de estudio (SEP)")/omit, NO "Common Core"/CCSS code; localize content (Euro/Cent, MX pesos, grade bands) + the one locale-only activity (no slug.en) needs a one-off preview render — **render it BEFORE the deploy** so the server's preview cache + index pick it up (es scp'd-after-deploy → had to redeploy). **es=Mexican was "full Mexican consistency"** (operator AskUserQuestion): also fixed 11 manifest titles "jardín infantil"→"preescolar" (+154 more across es.json FAQ flight-data) + switched es framework Spain→Mexico in `EDUCATIONAL_FRAMEWORK_BY_LOCALE.es` (activity route) + `FRAMEWORK_BY_LOCALE.es` (standards route) + es standardsPage/FAQ flight-data strings (LOMLOE→SEP). es gate adds MX negative-checks (no LOMLOE/jardín-infantil/ordenador/móvil/vosotros). FLAGGED-not-fixed: 4 `topicMeta.*` LOMLOE deck-SEO strings (deck domain, separate audience). **ALL 11 LOCALES COMPLETE** (en/de/fr/es/pt/it/nl/sv/da/no/fi) — every activity page in every locale has hand-written per-activity prose + a real thumbnail; each gated by `scripts/verify-activity-content-<loc>.js`. Recurring catch across every non-EN locale: the inherited `{strand}` placeholder in `choice-board.sort-count.k-md-b-3` (the one existing-but-broken entry) — the review pass fixed it each time. Fold into the §A.13.62 DoD for new builds. → [Activity page copy + thumbnails](feedback-activity-page-copy-and-thumbnails.md).


---

## 🖥️ PREVIEW (operator 2026-06-28) — "activities don't show on http://127.0.0.1:5050/" = browser-cached index + stale duplicate `_preview-server.js` processes (found 4), NOT a build problem. curl the LIVE server first (it re-scans manifests per request); kill stale node procs; server now sends `Cache-Control: no-store` (patched); operator hard-refreshes (Ctrl+Shift+R) or opens `/?fresh`. ALSO: `page_title.en` MUST lead with the character name (the index labels rows by page_title) + grep existing page_titles for name/skill collisions at design time. → [Preview server stale index](feedback-preview-server-stale-index.md).


---

## ⚡ PREF (operator 2026-06-28) — LEAN activity QA: per build run ONLY the fast functional gates (verify-core + local-test + preflight) + activity-SCOPED `visual-qa-activity.js --activity=<id>` + a quick phone+desktop eyeball. **NEVER run whole-catalog sweeps** `audit-activity-mobile.js`/`audit-activity-variety.js` (they ignore --activity, audit all ~90, = the slow part). Still fix any real defect the scoped gate finds (never move a threshold). Relaxes the §A.13.62 full DoD for routine builds. → [Lean activity QA](feedback-lean-activity-qa.md).


---

## 🛑 STANDING RULE (operator-locked 2026-06-23, FOREVER) — every activity build/change is DONE only after: (1) `node scripts/visual-qa-activity.js --activity=<id>` GREEN across the FULL viewport sweep `320·360·412·768·1024·1366` × every round (measured FITS/no-cut-off **at DESKTOP 768+1024 too, not just phone** + no-overflow + not-tiny + not-sparse + tap≥44); (2) VISUAL-CRITIC AGENT reads the full screenshot sweep (incl. desktop 768 AND 1024); (3) **I personally Read the 768+1024+360 renders myself**; (4) git diff = 0 protected-core/lcs-shell. ROOT CAUSE fixed: I verified at phone-only while operator views desktop → cut-off/sparse/tiny shipped. NEVER move a gate threshold to pass (fix the layout, or fix what's measured if it's decorative). HTTP-200/tests-pass/one-width/"agent said PASS" ≠ done. Harness PROVED itself: caught real desktop cut-off in already-approved Echo Grove + Mochi (both fix-backlog items VERIFIED CLEARED 2026-06-25 — the prior 2026-06-23 fix held: echo-grove ≥680px desktop wrap-row + mochi scene-cap min(55vw,194px); re-run = visual-qa 48/48 each across all 6 viewports + core/activity/local-test gates PASS [incl. mochi filled-bowl no-overflow] + critic CLEAN; ZERO new edits needed). Container rule: objects-in-a-vessel need an OPAQUE FRONT WALL drawn after them (a stroke is not containment); realistic library images NEVER inside a hand-drawn faux-3D vessel (style clash) — use a soft flat plate + contact shadows. → [Visual QA + DoD + containment](feedback-visual-qa-container-containment.md).


---

## 🐛 BUGFIX 2026-06-27 — "Add the Magic e" (cvc-builder.build-magic-e.rf-1-3-silent-e) gave away the answer (the correct `e` tile was GILDED gold) → FIXED. Removed the gild CSS rule from PROTECTED `cvc-builder-core.js` (operator-AUTHORIZED protected-core touch, CSS-only/0-logic; kept `.cvc-prefill`+locked-slot dim; cache-buster v3→4); added a no-gild computed-style-equality guard to `local-test-cvc-builder-magice.js`. Verified 3 ways (guard + personal Read [4 tiles identical] + visual-critic GILD:CLEAN) + blend/vowel-team/endings regressions + preflight. NOT deployed (joins the un-deployed batch). Lesson: never gild/highlight the answer tile (§A.13.60 challenge bar). **FOLLOW-UP DONE 2026-06-27 — cvc-builder family desktop+phone fold FIXED** (html `@media` only, NO protected-core touch): capped subject-img + slots/palette max-width + forced the palette to ONE ROW (`minmax(46px,56px)` not `1fr` + justify-center — the 2-row wrap of build-cvc's 7 tiles was the hog) + trimmed gaps/phone-block; all 3 cvc-builder activities visual-qa GREEN at 6 viewports, magic-e gild fix intact, visual-critic FIT:CLEAN. Batched-not-deployed. Lesson: aspect-ratio:1 tiles balloon HEIGHT at desktop + a 2-row palette is the silent fold hog — cap tile size (minmax not 1fr)+one-row, don't just scale fonts. **FOLLOW-UP 2 DONE 2026-06-27 — word-builder/syllable-builder family fold FIXED** (`syllable-builder-activity.html` `@media` only, no protected-core touch): capped subject-img + wb-subject-text (was 72px) + wb-slot + wb-tile (one-row palette — flex-wrap 2-row was the hog) + gaps; all 13 activities (EN vowel-teams/endings/build-words + es/fi/pt/fr/it/de/nl/sv/no/da syllable+sound-chunk) visual-qa GREEN at 6 viewports; word-builder verify/local-tests intact; chunks unclipped (visual-critic CLEAN). Batched-not-deployed. New lessons: flex-wrap wide-tile palette = 2-row fold hog (cap min-width+one-row max-width); 72px word-only text subject wraps long words (cap it); VERBOSE-LOCALE prompts (es/de/it/sv) wrap 3-4 lines in the protected shell (~55px more chrome) → trim content to fit UNDER the longest locale prompt, dial off a verbose locale. **BOTH phonics families (cvc-builder + word-builder) now §A.13.62-fold-clean across all viewports+locales; both in the un-deployed batch.** → [games build walk](project_games_build_walk.md).


---

## 🏁 TIME-ACTIVITIES EXPANSION — COMPLETE (2026-06-27). **10 new time activities BUILT (Sprocket the rooster #76; all EN-only pilots; NOT deployed).** #1-5 reading ladder hour→half→quarter→five→minute (`clock-digital`) · #6 analog↔digital match (`clock-digital` reverse) · #7 elapsed + #8 add/subtract (`clock-elapsed`) · #9 12↔24 conversion (`clock-convert`) · #10 a.m./p.m. (`clock-ampm`). Screenshot spec fully covered. Each: own engine/variant (0 protected-core/clock-core/lcs-shell/game-shell lines; only tracked edits = `activities.ts` MANIFEST_FILES rows for the 4 new engines), full §A.13.62 DoD green (verify-core MEASURED + visual-qa 54/54 + probe + local-test + variety + preflight + personal Read + visual-critic CLEAN). **NEXT is the OPERATOR'S call (do NOT auto-start): (a) DEPLOY the batch [git pull → cp 'mini tools/clock-*' to /var/www/lcs-media/mini-tools/ → bash deploy.sh, the §20.4 cp-before-build rule]; (b) 11-LOCALE FAN-OUT (cheap — digital times locale-neutral; prompts/win-notes/intros localize; clock-ampm activity sentences need per-locale authoring); (c) FRESH commission.** Plan: `continue-with-the-next-shimmering-raccoon.md`; chars: spec #76 addenda 1-9. → [games build walk](project_games_build_walk.md).


---

## (prior, now CLOSED) ⏰ ACTIVE COMMISSION (2026-06-27, OVERRODE the 🟢 INDEX loop) — TIME-ACTIVITIES EXPANSION: built **≥10 NEW time activities** (screenshot spec — reading granularities Hour/Half/Quarter/Five-min/Minute + operations Elapsed/12↔24-Convert/Add-Subtract + Analog/Digital), **ONE per plan-mode session** (operator-locked cadence: "ask me to switch to plan mode before you start building each activity" → after each ships I PROMPT "switch to plan mode + say continue for #N+1"). Plan/queue SoT = `C:\Users\rkgen\.claude\plans\continue-with-the-next-shimmering-raccoon.md`. Engine = NEW clean-sibling `clock-digital` (`mini tools/clock-digital-core.js`+`-activity.js`, `handAngles` re-implemented, 0 lines to protected `clock-core`) serving the reading/match variants #1-7,11 as config; ops #8-10 get small engines. Code-dup across time standards is EXPECTED (breadth IS the product) → owned-code gate does NOT block. EN-only pilots; NO deploy. **9 TIME ACTIVITIES BUILT (Sprocket #76) + full §A.13.62 DoD green 2026-06-27, NOT deployed.** (#9 "12↔24 Conversion" `clock-convert.12-24.2-md-c-7` = NEW digital-only `clock-convert` engine — notation 12h↔24h, MIXED dirs, "forgot +12"/wrong-meridiem trap; aligned 2.MD.C.7 [operator-approved via AskUserQuestion: 24h isn't clean US-CCSS but 2.MD.C.7 owns a.m./p.m., extended to 24h for European audience]; ONE tracked edit = activities.ts. Lessons: FLAG no-clean-CCSS skills don't force-fit/invent; store format-agnostic value+dir flag render per-direction derived answer option===h24.) Reading ladder COMPLETE — #1 Hour + #2 Half (1.MD.B.3) + #3 Quarter + #4 Five-min (2.MD.C.7) + #5 Minute (3.MD.A.1). #6 "Match the Clocks" (`clock-digital.match-clocks.2-md-c-7`, digital→analog direction-branch). #7 "Elapsed Time" (`clock-elapsed.what-time.3-md-a-1`) = first OPERATION, NEW `clock-elapsed` engine (read START + "in N min" → computed END, 12h rollover). **#8 "Time Addition & Subtraction" (`clock-elapsed.add-subtract.3-md-a-1`) = clock-elapsed SUBTRACT mode, MIXED add+subtract** ("what time was it N min ago?", back-past-12 rollover) — ZERO new tracked edits + ZERO core change (`endTime`'s mod-720 wrap, written sign-safe in #7, already handled negative delta; #8 = manifest row + sign-aware copy qBack/winBack/hintBack). Lessons (cumulative): never fudge clock geometry for a thumbnail; short/long cue OBVIOUS ~55%; to-the-minute NEEDS ticks; desktop clock FOLD-constrained (never move FITS); flip representation by branching ONLY render; resolved-state fold catches cut-offs visual-qa misses; don't restate the shell prompt inside the stage; **a sign-safe engine (signed delta) pays off — the whole subtraction activity needed ZERO core change.** **NEXT on "switch to plan mode + continue" = #10 (FINAL) "A.M. / P.M." — cleanly 2.MD.C.7 ("using a.m. and p.m."); 2-choice tap (morning vs afternoon/evening) via a day-context cue; clock-digital-family variant OR tiny new shape; reuse Sprocket #76. COMPLETES the commission (≥10). After #10 the time-expansion is DONE → surface to operator (next is their call: 11-locale fan-out of these EN-only pilots / deploy the batch / fresh commission).** When all ≥10 ship → override closes, INDEX loop resumes. → [games build walk](project_games_build_walk.md).


---

## 🟢 ACTIVE LOOP — the operator phrase **"Good. Continue with the next activity"** = build the NEXT un-built `game-designs/INDEX.md` entry (re-derive: lowest-numbered status=**approved**; **⚠️ POLICY (operator 2026-06-26): NO SKIPS — REDESIGN every un-built entry clarity-first; target 80 activities. Do NOT AskUserQuestion about defer/which — just build the lowest un-built non-duplicate, one per "continue."** Games #47-56 + #58 + #61 + #62 + #64 + #66 + #67 + #68 + #69 + #70 + #71 + #72 + #73 + #77 + #79 + #81 + #83 + #84 + #85 + #86 BUILT (chars through #72 Crumb) → **🏁 #86 (3.NF.A.3 equivalent fractions, `fraction-equiv`, Crumb) was the LAST DESIGNED INDEX entry (#87 = placeholder; CATALOG-DEPLETION). I surfaced the strategic step-back; the operator chose option 3 = DEFERRED PRODUCTION-CORES. ⇒ THE LOOP HAS PIVOTED off the INDEX-walk onto the deferred-core backlog. ✅ FIRST deferred-core BUILT: place-value REGROUPING `place-value-regroup` (1.NBT.C.4 compose-a-ten, char #73 Tuck the hamster) — base-ten blocks + "Make a ten" bundle + keypad total; NEW config-ready core (operation/places), 0 protected-core/shell lines, EN-only pilot, full §A.13.62 DoD green. Fills the E12 OPERATE gap. NOT deployed (operator batches). ✅ The **2.NBT.B.7 fast-follow** chosen + BUILT: activity #2 **"Tuck Breaks a Ten"** (`place-value-regroup.subtract-decompose.2-nbt-b-7`, Grade 2) — 3-COLUMN mat (hundreds-flats/ten-rods/ones-cubes), "🔁 Break a ten" decomposes 1 ten→10 ones, the last `b` ones get a ✕ "take away" mark, type the difference on the keypad. SAME Tuck/engine (the config-ready core's `subtract`/`places:3` paths lit up — `gradeSubtract`, `breakTen`, `_flat`, op-aware setup/render/caption + a synchronous subtract title swap); ADD activity #1 stayed regression-green; **zero new tracked edits** (subtract row added to the already-registered manifest); 0 protected-core/shell lines; EN-only pilot; full §A.13.62 DoD green on BOTH activities (incl. the bar-min-height-36 broken-state fold fix + the visual-critic flat-magnitude polish). NOT deployed. ✅ **BOTH hundred-level facets BUILT (operator: "both")** — **"Tuck Makes a Hundred"** (`add-compose-hundred.2-nbt-b-7`: 11 tens → Make a hundred → 410) + **"Tuck Breaks a Hundred"** (`subtract-decompose-hundred.2-nbt-b-7`: borrow across zero, 302−5 → Break a hundred → Break a ten → 297). **The place-value-regroup core is now COMPLETE for K-2 add/subtract regrouping — 4 activities** (compose-ten 1.NBT.C.4 + decompose-ten + compose-hundred + decompose-hundred, last 3 all 2.NBT.B.7). New `makeHundred`/`breakHundred` + op/state-aware button dispatch + fat-column layout + thin rods (the 3-column busy-column fold, the program's tightest); all 4 regression-green; 0 protected-core/shell lines; ZERO new tracked edits; EN-only; full §A.13.62 DoD (verify 4 rows + visual-qa 48×4 + hundred-probe + hundred-local-test + 2 prior probes/local-tests + variety + visual-critic PASS both + personal Read pre+post-regroup 360/768/1024). NOT deployed. ✅ **PHONEME/ELKONIN core BUILT (RF.K.2.d)** — **"Coco's Sound Boxes"** (`sound-boxes.phoneme-position.rf-k-2-d`, char #74 Coco the koala). Target CVC word picture + 3 Elkonin boxes (one highlighted = first/middle/last) + Hear-it → tap the picture sharing that position's sound; 9 rounds (3 each position), whole-word audio only (isolated-phoneme TTS avoided), phonemes derived from worksheet-gen sounds JSONs + vocabulary-phonics. NEW clean-sibling `sound-boxes-core`; 0 protected-core/shell lines; EN-only-by-design; only tracked edit = the `activities.ts` row. **LESSON: picture RECOGNIZABILITY is a blocking gate — the visual-critic caught 4 unrecognizable image-library assets (net=racket, mop=broom, fan=pinwheel, bun=loaf) on targets/correct-tiles → rebuilt with only obviously-recognizable concrete nouns; the library label ≠ a recognizable picture.** Full §A.13.62 DoD green (verify [clean-CVC+approved+color-imaged cross-check] + visual-qa 54 + _coco-probe + local-test [images-load-no-404] + variety + visual-critic PASS after the recognizability rebuild + personal Read 360/768/1024). NOT deployed. ✅ **SENTENCE-BUILDING core BUILT (L.1.1.j + L.K.1.b)** — **"Wiggles' Sentence Builder"** (`sentence-builder.build-a-sentence.l-1-1-j`, char #75 Wiggles the caterpillar). Arrange scrambled word-tiles into a complete declarative sentence (capital-first/period-last baked on the boundary tiles → unique grammatical order → exact-order grade is safe); subject-noun picture; 9 sentences; tap-to-place slots + palette (word-builder pattern, clean sibling); shell Check visible (arrange-then-check). NEW `sentence-builder-core`; 0 protected-core/shell lines; EN-only-by-design; only tracked edit = the `activities.ts` row. **LESSON: the determinism guard for an arrange-into-one-order activity = bake the capital onto the determiner tile + period onto the verb + adj-noun middle + intransitive verbs (no second valid sentence) — the visual-critic's "could the tiles form another sentence?" gate confirmed all 9 rigid.** Full §A.13.62 DoD green (verify [ordered-grade strict, derived, subjects color-imaged] + visual-qa 54 + _wiggles-probe + local-test + variety + visual-critic PASS [second-valid-order gate clean] + personal Read empty/built/resolved 360/768). NOT deployed. **🏁 THE DEFERRED PRODUCTION-CORE BACKLOG IS NOW EXHAUSTED** (place-value-regroup ×4 [1.NBT.C.4 + 2.NBT.B.7 compose/decompose ten+hundred] + sound-boxes [RF.K.2.d] + sentence-builder [L.1.1.j] all built; owned-code cores K.CC.C.6/2.MD.A.1 dropped). On the NEXT "Good. Continue…" do NOT auto-pick — SURFACE the strategic step-back: the per-game catalog-walk AND the deferred-core backlog are both done → options are the **11-locale fan-out** of the place-value-regroup activities (cheap; only button labels+titles localize), **theme-waves** over existing engines, the **premium-games PROGRAM** thrust (separate trigger "let's start building the games"), or a **fresh-design commission**. Owned-code backlog cores DROPPED: K.CC.C.6 (E2), 2.MD.A.1 (Tildy).** **GATE-CHECK each trigger via `grep "code" mini tools/*-activities.json` — OWNED CODES ARE DUPLICATES, skip-past (count toward 80). Confirmed owned-dups: #74 3.MD.A.2, #75 3.OA.A.2, #76 3.NF.A.1, #78 2.MD.D.10, #80 3.MD.C.6, #81 3.NBT.A.1, #82 3.OA.A.1, #83 3.MD.D.8 (+ #59/#60/#63/#65).** Build each clarity-first, no skips, toward 80; redesign abstract specs to a clear readable form. Build each as a **lcs-shell FREE activity** (EnterPlanMode → gate-check code UNOWNED → plan → ExitPlanMode → core+activity+html+manifest+`activities.ts` row+verify-core/probe/local-test → full **§A.13.62 DoD** → append character + mark BUILT; NO deploy). One game per trigger. Char-spec tail = #72 Crumb the mouse (Game #86 "Crumb's Same-Amount Bakery", 3.NF.A.3 equivalent fractions, CLARITY-FIRST — tap the candidate showing the SAME amount; proportion-shaded bars + numerals; LESSON: the RESOLVED state is a fold cost visual-qa never measures [it never clicks Check] → always run the probe's resolved state at the 900-tall desktop viewports + cap desktop card sizes; straddle distractors one-less/one-more). **Preview server (no folder-hunting): `! node "mini tools/_preview-server.js"` → http://127.0.0.1:5050/ lists every activity with click-to-play.** **This is NOT the premium-games thrust** (that trigger = "let's start building the games"). Full loop + per-game pattern + the cohort anti-cheat tech-debt → [games build walk](project_games_build_walk.md).


---

## ACTIVE — DECK-ASSET URL DRIFT (deck-card PDF/answer-key 404s = SEO-killer). ~8,831 non-EN decks' DB pdf_url/answer_key_url kept the legacy `…-make-whole-…` filename after a 2026-06-20 on-disk rename never updated the DB; cards render the dead column → 404 (landings derive from slug → 200). **PART 1 DONE 2026-06-25**: fix = derive URLs from slug (`canonicalDeckAssets` at 9 surfaces, commit 8860035a) + DB canonicalized (`fix-deck-url-columns.js`, 8,831 rows + es-50, backup `/root/decks-backup-20260625T174003Z.sql`) → audit HARD=0. Thumbnail "mismatch" = collapseSiblings (operator: ACCEPT). 45,309 decks total. **PART 2 DONE 2026-06-25**: catalog scan found only real mismatch = chart-count pt 47 (§22.4 v32 emit bug, all mislabeled `4-de-julho`); fixed via cloned `salvage-pt-chartcount.js` → 49 decks re-themed + PDF-rename + url-columns + republish-seo/og/end-links cascade + 301 map + pt.json 39-landing remap + repoint + share-link fix; audit HARD=0, content correct. **PART 3 DONE 2026-06-25** (operator: "pdfs shouldn't be noindex" + "everything indexed" + delegated): expert DECISION = do NOT revert deck→landing canonical (not the crash cause; reverting = harmful 2nd migration). PDFs already indexable. Fixed 3 gaps: /worksheet-makers→sitemap, custom `sitemap/4.xml` route w/ 119k image entries, `patch-nginx-pdf-makewhole-301.py` (301 ~17k dead make-whole PDFs). GSC "duplicate deck pages"=healthy. **PART 4 DONE 2026-06-25 — TASK CLOSED**: `check-deck-url-drift.js` (fast DB-only) wired WARN-only into deploy.sh; CLAUDE.md §17.8.20 marked REVERSED + §8.1 slug-derived-asset invariant; final audit HARD=0 all 11 locales (45,309 decks), drift=0, all live spot-checks green. (CLAUDE.md left uncommitted — operator folds it.) Recovery now rides on GSC re-crawl time (~2026-07-10 checkpoint). → [Deck-asset URL drift fix](project_deck_asset_url_drift_fix.md).


---

## ACTIVE — SEO QUALITY PROGRAM (operator: "build perfect SEO", fully delegated — meta/images/alt/cannibalization, esp. "show each deck with its image in search results"). Much already healthy (PDF meta, alt, hreflang, structured data, image sitemap). **PART 1 DONE 2026-06-25**: root layout's `max-image-preview:large`+`max-snippet:-1` were dropped site-wide because Next REPLACES (not merges) page-level `robots` — every indexable `generateMetadata` used bare `{index,follow}`. Fixed via new `frontend/lib/seo/robots.ts INDEXABLE_ROBOTS` at 9 routes + `build-seo-head.js` + `inject-deck-robots-meta.js` retrofit (45,756 deck.html). Verified live. NEXT (plan-mode each): Part 2 keyword-research landing meta for 8 NSR locales, Part 3 landing-less deck-title keyword opt, Part 4 thin/near-dup (GSC-data). → [SEO quality program](project_seo_quality_program.md).


---

## ACTIVE — SEO RECOVERY PROGRAM (clicks crashed 80→15/day, started ~2026-05-31). Exhaustive expert-agent audit-and-fix, 12 digestible parts, each its OWN plan-mode session (operator switches to plan mode before each). Diagnosis (live-verified): PDF noindex + decks removed from sitemap + canonical-repoint-to-landings — all reversible. Roadmap SoT = plan file `employ-expert-agents-to-optimized-stonebraker.md`. **NEXT = Part 1 Click-Drop Recovery.** Full diagnosis + the 12 parts → [SEO recovery program](project_seo_recovery_program.md).


---

## DONE — cross-language PER-DECK LANDINGS (§22 tier-3) **COMPLETE: 3,076 landings, all 11 locales LIVE** (verified+repointed+hreflang-meshed+sitemap). 8 ESL+pt→en (de/sv/nl/no/da/fr/it/pt/fi) + en→10 targets (1400) + es→{en,pt} (280) + pt→es (132). Reuses /worksheets route via `coordinate.target` + coordKey `|t:` + gen-crosslang-v2 engine ({TGT} parametrization + anti-doorway per-target cell/neighbour offset + language-fact note for the dense en 10-target fan). 0 FAIL every locale. ~12-deck gap = it/pt/es-themes lack 4th_of_july copy. Full record + the multi-target doorway doctrine → [Cross-language landings](project_cross_language_landings.md).


---

## DONE — WORKSHEET-MAKER per-mode SAMPLES **COMPLETE, 11 locales LIVE**. Each `/[locale]/tools/<maker>` page now shows a "See what you can make" section: one playable sample deck per real exercise MODE (DB-honest groupBy) + localized name + 1-sentence explanation, in-language; crossword/wordsearch/matching also show a cross-language strip → /learn. Helper `lib/seo/maker-samples.ts` + `MakerSamples`/`MakerSampleTile` (cloned FeaturedDeckTileV3, thumbnail→modal-play). **GOTCHA: playable URL = `deckAssets().deckDir` (`/<locale>/decks/<slug>/`), NOT deckHtml — `/deck.html` 404s.** PDF-only makers → no section. Full record → [Maker mode samples](project_maker_mode_samples.md).


---

## INVARIANT — cross-language decks: monolingual deck queries MUST filter `contentLanguage:null`. The ~3,088 cross-language (grid+clue lang) decks live in the dedicated "Languages" `/[locale]/learn/` category, NOT the monolingual catalog. `Deck.contentLanguage` set at publish; `MONOLINGUAL_WHERE={contentLanguage:null}` (topic-decks.ts) spread into every monolingual surface (topic/variety/breadth-grid/facets). New monolingual deck query without it = pollution returns. Full record + Phase A/B + deferred polish → [Cross-language category](project_cross_language_category.md).


---

## ACTIVE — DISCIPLINED SET **COMPLETE** (2026-06-19). Marginals pass closed: ~63 firm distinct activities + 1 marginal (#8 read-CVC-word, RF.K.3 decode-to-meaning, EN-only, LIVE e8266818) = **~64 distinct activities** (≈50 CCSS codes), fanned to 11 locales where culture-neutral (~600+ instance-equivalents). NOT padded to the loose ~130-145 working figure — the gap is honestly theme-waves (gated reserve, most of it) + finer master-table granularity + 13 dropped re-skins (the skeptical marginals scope, plan file `cc-commission-calm-tiger.md`). **NEXT PHASE (HARD STOP — await operator commission): theme-wave reserve (gated) + CA5 gamified-manipulation retrofit + deferred-core backlog. NOTE: this "do not auto-start" applies to the DISCIPLINED-SET's own next phase — it is NOT the 🟢 ACTIVE LOOP games-build-walk (which DOES auto-continue on "Good. Continue with the next activity").** Deferred-core backlog (closed): compare-groups-core (K.CC.C.6) · place-value-regroup-core (1.NBT.C.4+2.NBT.B.7) · measurement-core (2.MD.A.1 procedural drag-measure) · phoneme/Elkonin (RF.K.2.d) · productive-language-engine (L.K.1.b+L.1.1.j) · fractions-core active-partition · multi-select-core (marginal #6, filed NOT greenlit). **DO NOT auto-start the next phase.**


---

## PARKED THRUST 2026-06-19 → PREMIUM GAMES PROGRAM (platform's first PAID product; §7 made real; CLAUDE.md top AMENDMENT 2026-06-19). **⚠️ TRIGGER = "let's start building the games" — NOT "Good. Continue with the next activity" (that's the 🟢 ACTIVE LOOP games-build-walk above; do not conflate).** **Strategy + 390-game catalog + the 3-layer premium VISION are all DONE + operator-APPROVED; this PAID-sprite/CA5 build has NOT started (operator doing OTHER tasks first).** Premium (paid) = 3 layers: (1) ACCESS — 300+ exclusive simple K-3 games; (2) ASSEMBLY — games + free content ordered per teaching point; (3) ongoing PREMIUM TEACHING MATERIALS (new printable worksheets+activities filling each area's gap). FREE = today's whole catalog (funnel). Moat = 11 langs × national-framework; NO tracking; no-account teacher usage; tiers parent~$45-49/teacher~$69-79/school-license. **RESUME TRIGGER: when operator says "let's start building the games" → go STRAIGHT to games Phase 1 (local-preview loop + CA5 sprite pipeline + asset-stub kit → pilot slice of 6-8 games for LOCAL approval before scale). Do NOT re-investigate strategy or re-plan the vision (both done+approved).** Full record + catalog/strategy/plan file paths + the 5 locked build-workflow rules → [Premium games program](project_premium_games_program.md).


---

## (historical) DRIVE-THE-SET phase: per-engine order E18→E4→E1→E12→E14→E2-math→E2-ELA→E7→E8/E9→marginals, theme-waves gated to last. **E18 COMPLETE (6). E4 COMPLETE (7; K.CC.C.6→compare-groups-core deferred). E1 COMPLETE (8). E12 COMPLETE at 3 REPRESENT activities (1.NBT.C.4+2.NBT.B.7 OPERATE codes DEFERRED to a regroup core). E14 COMPLETE. E2-math COMPLETE (17). E2-ELA DISCIPLINED SET COMPLETE (3). **E7 cvc-builder COMPLETE (EN-only, 3): RF.K.3 build-cvc (live) + RF.1.3 blends/digraphs (unit-tile chunks) + RF.1.3 silent-e "Add the Magic e" (prefill+add-the-e, LIVE en).** **E8/E9 word-builder tail COMPLETE (2): #1 RF.1.3 vowel-teams "Build the Vowel-Team Word" (EN-only, unit-tile) LIVE (55a7e424) + #2 RF.1.3.f endings "Build Words with Endings" (-s/-ed/-ing, EN-only, WORD-ONLY text subject) LIVE (a513a180). NEXT = MARGINALS PASS — phase boundary, AWAIT the operator's commission (do NOT auto-start).** **E7 scope (closed): L.K.2.d = DUPE of live RF.K.3 — NOT built. blends/digraphs ✅. silent-e ✅. vowel-teams ✅. endings ✅. RF.K.2.c done (choice-board).** Finish line now: MARGINALS pass only. **E2-ELA 6-code scope (closed):** RF.K.1.d ✅ FULL-11 · RF.K.3.a ✅ FULL-11 · RF.K.2.c ✅ reduced en/nl/da (de/no/sv native-EXCLUDED: their K uses syllable+phoneme not onset-rime; fi/es/it/pt/fr excluded by ruling) · RF.K.2.d DEFER→phoneme/Elkonin core · L.K.1.b + L.1.1.j DEFER→productive-language engine. Deferred-core/engine backlog: compare-groups-core (K.CC.C.6), place-value-regroup-core (1.NBT.C.4+2.NBT.B.7), measurement-core (2.MD.A.1 procedural drag-measure), phoneme/Elkonin (RF.K.2.d), productive-language-engine (L.K.1.b+L.1.1.j). [active-partition is the live fractions-core, NOT deferred.]**


---

## DONE 2026-06-19 — MARGINAL #8 → RF.K.3 read-CVC-word "Read the Word, Tap the Picture" choice-board, EN-only — CLOSES THE DISCIPLINED SET (commit e8266818)
- [Activities live inventory](project_activities_live_inventory.md) + marginals scope `cc-commission-calm-tiger.md`. **The skeptical marginals pass (investigation, no-build) judged ~14 candidates against the §22.1 4-axis bar: 13 RE-SKIN-DROP, 1 FORK-DEFER (#6 multi-select-core, filed not greenlit), 1 OPERATOR-CALL (#8). I recommended BUILD 0; operator UP-RULED #8 only, overriding my DROP** — whole-word DECODE-to-meaning is a genuinely uncovered RF.K.3 facet (beginning-sound=initial-phoneme-only, read-sight-word=sight-no-decode, cvc-build=encode); a child can fail decode-to-meaning while passing all three. Distinctness rests on standard-facet + artifact (3-letter printed-word subject); identical tap-1-of-4-pictures interaction accepted (§22.1 never required all 4 axes). **LESSON: the recognition engines (E2 choice-board 22 + E4 match-pairs 7) already absorbed the distinct recognition skills during the firm phase — that's WHY the marginals are mostly re-skins; the honest 2nd-mechanic precedent (pick-bigger/smaller, read-sight-word/tell-words-apart, build-array/equal-groups) requires artifact AND standard-facet to diverge, not interaction-novelty or a swapped subject alone.**
- Mechanic: printed CVC word = text subject; child decodes + taps the picture it names (1-of-4). **Decode-is-load-bearing distractor rule (the differentiator vs beginning-sound, which is no-shared-onset): ≥1 distractor word SHARES the first 2 letters (onset+vowel) but differs in full word (cat ← cap/can/car) → reading only the onset can't disambiguate; the final phoneme decides.** 9 rounds, share-onset sets ca/bu/ha/ba/ja/li (44 imageable CVC words exist; feasibility scanned filesystem∩vocab∩approved-words upfront). EXACTLY ONE correct; NO audio (printed-word visual stimulus → no inventory-404). New `read-cvc-word` template = near-exact clone of `beginning-sound` (text subject = word not letter; share-onset not no-shared-onset). **choice-board-core.js 0-line (empty diff); beginning-sound sibling re-verified intact. 5 protected cores 0-line.**
- Gates: verify-choice-board-readcvc.js (MEASURED — REAL core: target CVC + word===image-vocabulary[noun].en[0], ≥1 share-onset distractor, all 4 image-exist+color §5 + in approved-words-en §20.7, exactly-one-correct, ≥7) + local-test-choice-board-readcvc.js (printed-word text subject no-image + audio-silence spy=0 speaks + tap-foil→no-celebrate/tap-correct→celebrate + reshuffle + 280-768). choice-board-activity.js?v=72 / ACTIVITY_WRAPPER_VERSION 9.03 (core ?v=8 unchanged). flat params.rounds → poolSize counts it (no new shape). EN-only (slug.en → 404 non-EN). Verified LIVE: en 200, JSON-LD RF.K.3 + Common Core, iframe v9.03, de/es 404, served wrapper+manifest fresh, all 18 picture imgs 200 (space-themed need %20), live variety 9-distinct+reshuffle, mobile 8/8 @280-768, printed-word "bun" + 4 picture tiles eyeballed @360 (word-only, no overflow). **DISCIPLINED SET COMPLETE — next phase is a HARD STOP, await operator commission.**


---

## DONE 2026-06-19 — E8/E9 #2 → RF.1.3.f inflectional endings "Build Words with Endings" word-builder, EN-only, WORD-ONLY LIVE — CLOSES E8/E9 tail (commit a513a180)
- [Activities live inventory](project_activities_live_inventory.md) — the last E8/E9 tail code. **FORK surfaced + operator-ruled: the image library is NOUNS-only, so -ed/-ing VERBS (jumping/jumped) have no pictures — only -s plurals are picture-anchorable. Operator chose WORD-ONLY, all 3 endings.** Mechanic: the base word is the subject TEXT (`wb-subject-text`, no picture); the child reads it, then builds the NAMED target by adding the right ending (-s/-ed/-ing) as ONE unit tile. Palette = [base, correctEnding] + the OTHER TWO endings → the ending discrimination is load-bearing; prompt NAMES the target (so exactly one ending is correct — jump→jumping not jumped). Ordered _tilesEqual; nextTask reshuffle. 9 words, CLEAN concat (base+ending===target, NO doubling/drop-e): cats/dogs/books (-s), jumping/walking/helping (-ing), played/rained/painted (-ed).
- **word-builder-core UNTOUCHED this build (git diff --stat empty; the only word-builder touch ever was the vowel-teams mutePerTile, already shipped). 5 protected cores 0-line.** New `build-endings` template on the syllable-builder wrapper — a near-clone of build-vowel-teams but subject type 'text' instead of 'image'; reuses mutePerTile (single-letter endings would speak as letter names), the generic tile-palette builder, _tilesEqual, nextTask, the _loadActivity gate (extended to `build-vowel-teams || build-endings`). build-syllables-word + build-vowel-teams siblings re-verified unaffected (tasks[]/unit-tile paths + mutePerTile-absent intact). **LESSON: when an asset gap (nouns-only library) blocks the picture-anchored shape for part of a standard (-ed/-ing verbs), that's a genuine FORK — surface it; a word-only TEXT-subject variant (base word as `wb-subject-text` + Hear-it speaks the named target) covers all 3 endings with the SAME engine, the ending-discrimination palette carrying the skill.**
- Gates: verify-word-builder-endings.js (MEASURED — REAL core: tiles=[base,ending]∈{s,ed,ing}, CLEAN concat base+ending===target, distractors = the other two endings, build target only on the right ending, wrong ending→≠, pool spans all 3, ≥7; NO image/vocab check — word-only by design) + local-test-word-builder-endings.js (word-only TEXT subject = base, no image; per-tile audio muted via spy; Hear-it+speakBlend speak the target; prompt names target; wrong ending→not; reshuffle; 280-768). syllable-builder-activity.js?v=16 + manifest ?v=16 / ACTIVITY_WRAPPER_VERSION 9.02. EN-only (slug.en only → 404 non-EN). Verified LIVE: en 200, JSON-LD RF.1.3.F + Common Core, iframe v9.02, de/es 404, served wrapper+manifest fresh, live variety 9-distinct+reshuffle, mobile 8/8 @280-768, word-only render eyeballed @360 (base "jump" as text + Hear-it, palette ing/ed/s + base, prompt "…build jumping", 0 overflow). **E8/E9 TAIL COMPLETE. NEXT = MARGINALS PASS — phase boundary, AWAIT the operator's commission.**


---

## DONE 2026-06-19 — E8/E9 #1 → RF.1.3 vowel-teams "Build the Vowel-Team Word" word-builder, EN-only, unit-tile LIVE (commit 55a7e424)
- [Activities live inventory](project_activities_live_inventory.md) — first E8/E9 tail code. **Build a single-syllable word where the vowel-team (oa/ai/ea/ee) is ONE selectable unit tile (boat=[b,oa,t]); palette = tiles + distractors incl. a CONFUSABLE vowel-team so picking the right team is load-bearing.** Ordered `_tilesEqual` check; nextTask per-pass reshuffle (8 words: boat/goat/leaf/seal/jeep/bee/coat/rain). Step-0 distinct: vs live syllable build (sub-syllabic VOWEL-TEAM vs syllable) + vs #2 cvc-blends (vowel pair vs consonant cluster; word-builder per-tile-audio vs cvc silent).
- **word-builder-core is NOT a protected core → ONE gated additive touch: `opts.mutePerTile` suppresses per-tile TTS in `selectTile` (`if(!this.mutePerTile) this.speakTile(...)`).** Rationale: single-consonant tiles (b,t) are spoken as letter NAMES ("bee","tee") which don't blend to the word (the isolated-sub-word-TTS issue, consistent w/ RF.K.3.a/RF.K.2.c) → mute per-tile; Hear-it (whole word) + speakBlend-on-correct carry the audio. **Absent the flag → syllable-builder siblings BYTE-IDENTICAL (core diff = only the 8/2 mutePerTile lines; re-verified LIVE: es forma-las-silabas keeps tasks[] path + per-tile audio ON + mutePerTile false).** 5 protected cores 0-line. **LESSON: a "build from tiles" engine hosts a sub-syllabic-unit standard wrapper-only when the unit is a multi-char tile + ordered-equality check; the only core touch a vowel-team needs is MUTING the misleading per-tile letter-name audio (a gated additive flag, default-off keeps siblings byte-identical).**
- Gates: verify-word-builder-vowelteam.js (MEASURED — REAL core: tiles.join=targetWord, exactly 1 vowel-team tile + rest single-char, ≥1 confusable-team distractor, place tiles→tileAnswers===tiles + answer===targetWord, wrong team in vowel slot→≠, image exists+color, **targetWord===image-vocabulary[noun][en][0]**, ≥7) + local-test-word-builder-vowelteam.js (per-tile audio MUTED via LCSAudio spy = 0 'syllable' speaks on tap; Hear-it + speakBlend speak the word; unit-tile slot render; wrong-team→not; reshuffle; 280-768). poolSize already counts params.words (#2) — no new shape. word-builder-core.js?v=11 / syllable-builder-activity.js?v=15 / ACTIVITY_WRAPPER_VERSION 9.01. EN-only (slug.en only → 404 non-EN by design; preflight WITHOUT --all-locales).
- Verified LIVE: en 200, JSON-LD RF.1.3 + educationalFramework Common Core + on-page "Common Core", iframe v9.01, de/es 404, served JS has build-vowel-teams branch + core has mutePerTile, all 8 subject images 200 (space-themed dirs need %20 in curl), live variety 8-distinct+reshuffle, mobile 8/8 @280-768, vowel-team unit-tiles eyeballed @360 (ee/oa as single tiles + confusable ee/oa/ai distractors + b/t single-letters, bee subject renders, 0 overflow). **NEXT = RF.1.3.f endings (-s/-ed/-ing) — last E8/E9 tail code, EN-only; proceed straight, no stop unless a fork.**


---

## DONE 2026-06-19 — E7 #3 → RF.1.3 silent-e "Add the Magic e" cvc-builder, EN-only, prefill+add-the-e LIVE — CLOSES E7 (commit 1759d40d)
- [Activities live inventory](project_activities_live_inventory.md) — E7's last distinct code. **MAGIC-E TRANSFORM mechanic (operator-described + ruled): the CVC base (cak_/kit_/con_) is PRE-FILLED + LOCKED; the child adds the GILDED magic-e tile → the long-vowel target (cake/kite/cone) matching the picture.** Isolates the magic-e as THE action → surfaces the cap→cape transform, NOT a word-swap re-skin (distinct from the live full-word CVC build + #2 chunk build; the prefilled base + the picture IS the short→long contrast). 8 words (kite/cake/rose/cube/cone/gate/nose/vase). **LESSON: a transform standard (X→Xe) is honestly instantiated on a build engine by PRE-FILLING the base + isolating the transforming element (the magic-e) as the only action — pre-fill+lock makes "add the e, vowel goes long" the point, vs building the whole word (a word-swap).**
- **cvc-builder-core NOT protected → 2nd DECLARED additive gated touch (independent of #2's chunks; both gated → live RF.K.3 + #2 blends byte-identical, BOTH re-verified):** setupTask seeds slotValues from `opts.prefill` (nulls=empty) + activeSlot=first-empty; `opts.lockPrefilled`→lockedSlots; clearSlot no-ops on a locked idx; render tags `.cvc-prefill`; gated CSS gilds `.cvc-prefill .cvc-letter[data-letter=e]`. Per-slot wrong-feedback uses char-index (chunks null here) → base green, wrong magic-letter red. git diff = gated-additive only. **LESSON: stack independent gated features on one unprotected core (chunks for #2 + prefill for #3, each gated on its own opt) — each defaults off → every prior sibling stays byte-identical; verify each sibling's gate after.**
- Wrapper: build-magic-e branch (derive prefill=targetWord.slice(0,-1)+null, magicLetter=last char ["e"], palette=[e]+distractors) + nextTask gate += build-magic-e + promptMagicE. poolSize reuses params.words (no new shape). EN-only (non-EN 404).
- Gates: verify-cvc-builder-magice.js (MEASURED 8: CVCe, base prefilled+LOCKED [clearSlot(0) no-op via real core], magic-e completes to target / distractor≠target, image-exists+color, targetWord=image-vocabulary[noun][en]) PASS · local-test (EN: base locked, magic-e gilded, add-e→correct, wrong→not, Hear-it spy speaks word, ≥7 reshuffle, 280-768) · live RF.K.3 + #2 blends siblings re-verified (prefill+chunks absent → empty slots, byte-identical) · cvc-builder-core.js?v=3 + activity?v=3, WRAPPER 9.00.
- Verified LIVE: en 200, JSON-LD RF.1.3, Common Core, iframe v9.00, de→404, tile img(cake) 200, preview 200 + index card, variety 8+reshuffle, build-the-cvc-word + build-blend-and-digraph-words siblings 200 (NO regression), preview eyeballed (cake: c·a·k base + gold "e", o/i/a distractors) + en@360 (cone: c·o·n base green + wrong "y" red, gold magic-e). **E7 CLOSED (3: build-cvc + blends/digraphs + magic-e). NEXT = E8/E9 word-builder (RF.1.3 vowel-teams) — engine boundary, AWAIT commission.**


---

## DONE 2026-06-19 — E7 #2 → RF.1.3 "Build the Blend/Digraph Word" cvc-builder, EN-only, UNIT-TILE chunks LIVE (commit 819336ef)
- [Activities live inventory](project_activities_live_inventory.md) — first E7 build this phase, on the cvc-builder engine (EN-only-by-design exception; non-EN 404 via resolveActivitySlug-null). **UNIT-TILE mechanic (operator-ruled, AskUserQuestion): the blend/digraph cluster (sh/ch/st/fr/cl/ck) is a SINGLE selectable chunk tile (tap "sh" as one unit; slots=chunk-count)** — unambiguously the digraph/blend facet, NOT "the same builder with a different word list" (the operator's caution). 8 words (ship/chin/frog/flag/drum/crab/sled/clock).
- **E7 STEP-0 SCOPE READ (load-bearing): L.K.2.d DUPLICATES the live RF.K.3 build** — both = see picture/hear word → spell from single-letter tiles; L.K.2.d-on-CVC is the identical encode mechanic → flagged a DUPE, NOT built. The 2 distinct cvc-builder facets = blends/digraphs (this) + silent-e (next; needs a cap→cape transform touch). vowel-teams + RF.1.3.f endings = word-builder (E8/E9) tail. RF.K.2.c done (choice-board). **LESSON: on a builder engine, a new "spell a word" code DUPES the live build unless the WORD CLASS + the TILE UNIT change the skill — confirm the mechanic engages the facet (unit-tile chunk), not just a longer word list.**
- **cvc-builder-core is NOT protected → DECLARED additive GATED touch** (all gated on `opts.chunks` → live RF.K.3 byte-identical, re-verified: chunks-absent drives "cat" + char-index feedback unchanged): (1) setupTask stores this.chunks (len===slots); (2) showFeedback wrong-path compares per-slot `slotValues[i]===chunks[i]` (chunk-wise) instead of char-indexing targetWord[i] (which mis-rings multi-char chunks); (3) render tags wrap `.cvc-chunks`; (4) gated `.cvc-chunks` slot/tile-widen CSS (auto-width + padding + smaller font so 2-char chunks fit). git diff = 19 ins / 2 byte-equiv del. **LESSON: a per-slot-feedback core that char-indexes the target string breaks for multi-char unit-tiles — the fix is an optional `chunks` array (gated, additive) so feedback compares chunk-wise; default-off keeps the single-char sibling byte-identical.**
- Wrapper: build-blend-word branch + `_buildChunkPalette` (cluster as 1 tile; the existing `_buildPalette` char-splits) + **nextTask reshuffle (§A.13.60; cvc-builder had NONE — was shell tasks[] loop)** + promptBuildBlend(en). **poolSize += params.words** (was unrecognized → N/A; the standing new-shape DoD; covers BOTH cvc-builder rows). Subject = picture + Hear-it (click-triggered, NOT on-load → no /audio/inventory.json 404 → mobile gate clean, unlike speak-on-load activities).
- Gates: verify-cvc-builder-blend.js (MEASURED 8 words: chunks.join===targetWord, ≥1 cluster chunk, ≥1 confusable-cluster distractor, drive REAL core — exact chunk sequence builds target / wrong-cluster-first doesn't, image-exists+color §5, targetWord===image-vocabulary[noun][en][0]) PASS · local-test-cvc-builder-blend.js (EN: tap chunks→feedbackMode 'correct', wrong-cluster→not, chunk-slot renders 2-char, Hear-it speaks word via spy, ≥7 reshuffle, 280-768) · live build-cvc-word sibling re-verified (chunks-null path byte-identical + 200 live) · cvc-builder-core.js?v=2 + cvc-builder-activity.js?v=2, WRAPPER 8.99.
- Verified LIVE: en 200, JSON-LD RF.1.3, Common Core, iframe v8.99, de→404 by design, tile img(ship) 200, preview 200 + index card, live variety 8+reshuffle, build-the-cvc-word sibling 200 (no regression), preview eyeballed (clock + cl/ch/cr/ck chunk tiles) + en@360 (sled, "sl" chunk-slot, chunk-wise b/e/sl feedback) clean. **NEXT = RF.1.3 silent-e — await commission (needs a transform core-touch).**


---

## DONE 2026-06-19 — E2-ELA #3 → RF.K.2.c "Blend Onset & Rime" choice-board, REDUCED FAN en/nl/da + audio LIVE — CLOSES E2-ELA disciplined set (commit 2ca0fd00)
- [Activities live inventory](project_activities_live_inventory.md) — **SCAFFOLDED-BLEND mechanic (operator-ruled via AskUserQuestion).** Onset shown as a letter-chunk + rime SPOKEN (TTS-safe syllable) → child blends → taps the picture; ≥1 same-rime distractor makes the onset load-bearing. **THE LOAD-BEARING FINDING: TTS cannot speak an isolated onset** — `LCSAudio.speak` passes text verbatim to SpeechSynthesisUtterance, so a single consonant is spoken as its LETTER NAME ("c"→"see") not /k/ (only whole words + multi-letter syllables reliable). That blocked a fully-oral "blend c+at"; operator chose the scaffolded form (onset visual, rime oral). **LESSON: confirm the audio channel's actual TTS capability BEFORE committing an audio mechanic — isolated-onset/phoneme TTS is unreliable across all 11 voices; reserve audio for whole-word/syllable units (the RF.K.1.d-visual + this scaffold are the same per-code-channel discipline).** Reuses read-sight-word speak/replay (rime on setup + replay on wrong) + E8 speakBlend (whole word on correct). **choice-board-core 0-line (empty diff).**
- **REDUCED FAN = en/nl/da, NATIVE-CONFIRMED at Step-0 (the operator's "native-determined fan, not a fixed list" principle in action):** nl CONFIRMED (hakken en plakken/VLL), da CONFIRMED (forlyd-isolation+lydsyntese/Fælles Mål lydmetode), **de EXCLUDED ITSELF** (German K = Silbe+Anlaut+phoneme-synthesis, onset-rime is "an Anglophone construct, no Lehrplan step" — operator HONORED the native exclusion OVER the original "de in by ruling" via AskUserQuestion), no+sv EXCLUDED (LK20/Lgr22 use phoneme+syllable; E8/E9 covers their unit). fi/es/it/pt/fr excluded by ruling. **LESSON: a strong native-ensemble exclusion can override an operator's by-ruling inclusion — surface it (don't silently drop OR force); the de/no/sv exclusions are the §20.8 inverse of the fi-consonant restriction (don't force a non-native pedagogical unit).** Excluded locales OMITTED from the slug map → resolveActivitySlug null → notFound() 404-by-design (cvc/sight-word precedent; verified de→404 live).
- Data flow: temp generator filtered approved-words count===1 (single-syllable) ∩ color-image ∩ image-vocabulary → per-locale rime-family pools → 6 candidate-locale ensembles (confirm-or-exclude + author). en authored by me; nl/da agent rounds normalized to {noun,themeDir,word} + gate-verified.
- nextTask gate += onset-rime-blend. poolSize reuses byLocale.rounds; strand already localized.
- Gates: verify-choice-board-onsetrime.js (MEASURED 24 rounds: **blend identity onset+rime===word** [forces correct split], rime≥2 chars, **≥1 distractor ends with rime** [onset load-bearing], image-exists+color §5, word=image-vocabulary, no-two-correct via real core, **byLocale keys===exactly {en,nl,da}** [excluded absent]) PASS · local-test-choice-board-onsetrime.js 3/3 with an **AUDIO SPY** (override LCSAudio.speak → assert rime spoken type:syllable on load + blended WORD type:word on correct) + native prompt + onset·rime subject + 4 picture tiles + tap-foil/correct + reshuffle + 280-768 · beginning-sound + letter-case siblings re-verified · core git diff EMPTY. choice-board-activity.js?v=71, ACTIVITY_WRAPPER_VERSION 8.98. [NSR-FLAG] da.
- **BANKED GOTCHA: audit-activity-mobile.js hard-fails on ANY console-404, but speak-on-load activities (this + E9 sight-word) fetch `/audio/inventory.json` which 404s by design (LCSAudio TTS fallback) → a FALSE-POSITIVE mobile-gate fail. The 404 is benign (not a broken tile image — verified: all tile images 200, only the audio-inventory 404s); layout is confirmed clean via the local-test 280-768 + the gate's captured screenshot. For audio activities, the mobile-gate console-404 is expected; verify the 404 is the audio-inventory (not an image) + eyeball the screenshot.**
- Verified LIVE: en/nl/da 200, JSON-LD RF.K.2.c, framework localized (SLO-kerndoelen/Fælles Mål), 0 CC-leak non-EN, iframe v8.98, **de(excluded)→404 by design**, preview 200 + index card, tile img 200, live variety 8+reshuffle, en-preview (b·ed → bed/sled same-rime) + da@360 (K·at → cat/Hat same-rime) eyeballed clean. **E2-ELA DISCIPLINED SET COMPLETE (RF.K.1.d+RF.K.3.a+RF.K.2.c). NEXT = E7 cvc (EN-only) — engine boundary, AWAIT commission.**


---

## DONE 2026-06-19 — E2-ELA #2 → RF.K.3.a "Beginning Sounds" letter→sound, PICTURE tiles on choice-board LIVE 11/11 (commit 092d5b6f)
- [Activities live inventory](project_activities_live_inventory.md) — FIRST choice-board literacy build using IMAGE option tiles. A consonant LETTER (subject) → tap the PICTURE whose locale-word starts with that letter's PRIMARY SOUND. **choice-board-core 0-line** (image tiles already supported via shape-id option-imgUrl + even-odd text-subject — both combined; empty diff). **NO audio** (letter is the visual stimulus; LCSAudio has no phoneme type, single-phoneme TTS unreliable; the per-code channel call — audio is reserved for RF.K.2.c later). Step-0 not a fork.
- **THE PER-LOCALE SOUND TRAP (the load-bearing work):** the same image has different onsets per locale (en ball/b, es pelota/p, fi pallo/p), so picture↔sound is locale-specific. Each native ensemble owns target consonants + onset-words + 3 no-shared-onset distractors. fi correctly restricted to NATIVE consonants (h/j/k/l/m/n/p/r/s/t/v — NOT loanword b/c/d/f/g). **LESSON: for a picture-onset activity the candidate data must be generated per-locale (scan color image themes → noun→themeDir; JOIN §20.7 approved-words + image-vocabulary[noun][locale][0] → onset by first-letter), handed to native ensembles who pick + sound-judge; NEVER cross-apply EN pairings.**
- **Data flow (new):** a temp generator scanned `image-library-webp/themes/` (52 color dirs, excluded " bw"), joined approved-words-<loc>.json (§20.7-safe keys) + IMAGE_VOCABULARY (the per-locale word, `const`-eval'd via new Function) → 1042-noun per-locale pools grouped by first letter → handed each of 11 ensembles their pool. They returned {letter, correct:{noun,themeDir,word}, distractors:[3]}×8 + chrome.
- Wrapper: beginning-sound branch (4 image tiles `{key:noun, imgUrl:/image-library-webp/themes/<themeDir>/<noun>@2x.webp, label:word}`, subject=letter text, correct=correct.noun) + nextTask gate + promptStartsWith ×11. manifest byLocale.rounds ×11. poolSize reuses byLocale.rounds (no audit edit); strand already localized (all 11).
- **Gate caught a real agent data error:** pt used noun-key `pan`+word "Panela" but vocab[pan][pt]="Frigideira" (Panela=pot, a different key — hallucinated) → fixed to piano/Piano + window/Janela. **LESSON: the word=IMAGE_VOCABULARY[noun][locale][0] cross-check is the load-bearing gate assertion for picture activities — agents conflate near-synonym noun-keys; verify every triple against vocab + filesystem existence.**
- Gates: verify-choice-board-beginningsound.js (MEASURED, 88 rounds: onset match, no-shared-onset, IMAGE EXISTS on disk + color theme §5, word=vocab no-fabrication, no-two-correct via real core, consonants, ≥7/locale) PASS · local-test (11/11 native prompt via i18n.current + 4 .cb-tile-img + target=core.targetKey tap-foil/correct + reshuffle + 280-768; images 404 locally so checks STRUCTURE) · audit-activity-mobile 8/8 @280-768 · letter-case sibling re-verified · core git diff EMPTY. choice-board-activity.js?v=70, ACTIVITY_WRAPPER_VERSION 8.97. [NSR-FLAG] sv/da/no/fi.
- Verified LIVE: en/de/fi 200, JSON-LD RF.K.3.a, framework+strand localized (Lehrplan/Schriftspracherwerb, OPS 2014/Lukemisen perustaidot), 0 CC-leak, iframe v8.97, preview 200 + index card, **sample tile image tiger@2x.webp HTTP-200** (real image SEO check), live variety 8+reshuffle, de@360 eyeballed (subject G + 4 picture tiles, "Welches Bild beginnt mit diesem Laut?" clean wrap, 0 overflow), en-preview eyeballed (T → fish/tiger/dog/moose). **NEXT = RF.K.2.c HALTED at boundary — await operator scope brief (reduced-fan, per-locale ruling).**


---

## DONE 2026-06-19 — E2-ELA #1 → RF.K.1.d "Match the Letter" uppercase↔lowercase on choice-board LIVE 11/11 (commit f1b48d6c)
- [Activities live inventory](project_activities_live_inventory.md) — FIRST E2-ELA literacy build + the 6-code BLOCK SCOPE READ (see ACTIVE line: RF.K.1.d full-11 done · RF.K.3.a full-11 next-clean · RF.K.2.c reduced-fan · RF.K.2.d DEFER-phoneme-core · L.K.1.b reduced/design · L.1.1.j EN-or-reduced/weak-fit). **Step-0 (operator-flagged scope-block + mechanic): visual uppercase↔lowercase MATCH** (operator delegated "confirm the cleanest" → case-match; **audio name→letter considered + REJECTED** — LCSAudio supports only {word,syllable,ui,number} (lcs-shell.js:190), so letter-NAME audio needs per-locale phonetic spellings via type:'syllable' + unreliable single-letter TTS across 11 voices; AND the SOUND codes later in the block (RF.K.2.c/RF.K.3.a) are where audio is genuinely needed → visual-for-letters is the right per-code channel). Honest + canonical RF.K.1.d kindergarten activity (directly exercises "upper- AND lowercase"); distinct (no letter facet live; shape-id is shapes). **choice-board-core 0-line (protected, empty diff).** **LESSON: not every code in a block uses the same answer-channel — pick per-code; visual for letter-shape recognition, audio reserved for the sound/phoneme codes where it's load-bearing. And a "name" standard's case-pairing facet (upper↔lower match) is an honest, lower-risk instantiation than forcing an unreliable single-letter-TTS naming task.**
- Mechanic: subject = one letter glyph (type:'text'); 4 tiles = the OTHER-CASE match + 3 visually-confusable same-case foils (b/d/p/q etc.) → EXACTLY ONE correct. **Bidirectional** (UPPER→lower + lower→UPPER); the small/big prompt (promptFindSmall/promptFindBig) is DERIVED from the answer's case in the wrapper. nextTask gate extended (`...||letter-case`).
- **PER-LOCALE NATIVE ALPHABETS (binds hard, §A.13.48):** all 11 ensembles supplied their OWN rounds incl. diacritic letters — de ä/ü · es ñ · pt ç · fr é · sv å/ä/ö · da/no æ/ø/å · fi ä/ö; it/nl base alphabets (no native diacritic letters — gate-exempt). NEVER a shared A–Z. params.byLocale.<loc>.rounds = [{prompt,answer,distractors[3]}], content locale via window.LCS.i18n.current (the sight-word shape). **poolSize reuses byLocale.rounds (line 54 — no audit edit).** Strand "Reading: Foundational Skills" ALREADY localized in strand-names.ts (all 11) — no edit; framework auto per §20.10.
- Gates: verify-choice-board-lettercase.js (MEASURED per-locale via real core: answer=other-case-of-prompt, 4 distinct single-char tiles, no-two-correct, bidirectional ≥1 each dir, ≥7/locale, non-EN ≥1 diacritic [it/nl exempt] — 88 rounds 0 violations) PASS · local-test-choice-board-lettercase.js 11/11 (native small/big prompt via i18n.current — TEST DERIVES correct = otherCase(subject), tap-foil/tap-correct, reshuffle, 280-768) · audit-activity-mobile 8/8 @280-768 · read-ruler/compare/ten-more-less + sight-word siblings re-verified · core git diff EMPTY. choice-board-activity.js?v=69, ACTIVITY_WRAPPER_VERSION 8.96. [NSR-FLAG] sv/da/no/fi.
- Verified LIVE: en/de/fi 200, JSON-LD RF.K.1.d, framework localized (Lehrplan/OPS 2014) + STRAND localized (Schriftspracherwerb/Lukemisen perustaidot), 0 CC-leak non-EN, iframe v8.96, preview 200 + index card, live variety 8-distinct+reshuffle, de@360 eyeballed (subject n, tiles {U,N,M,H}, "Tippe auf den großen Buchstaben" 3-line wrap, 0 overflow). **NEXT = next E2-ELA firm code per operator's sequence off the scope read (likely RF.K.3.a).**


---

## DONE 2026-06-19 — E2-math → 2.MD.A.1 "Read the Ruler" code-drawn cm ruler + to-scale object on choice-board LIVE 11/11 — CLOSES E2-math (commit f1e17b4b)
- [Activities live inventory](project_activities_live_inventory.md) — **STEP-0 LIKELY-FORK, operator-confirmed BUILD (AskUserQuestion).** 2.MD.A.1 = measure length with a tool. The ACTIVE drag-to-measure verb already lives in the free-play `ruler` manipulative; on a pick-an-answer engine the honest, distinct option = a **read-the-ruler RECOGNITION** (a canonical 2.MD.A.1 assessment item: object aligned to 0, read where it ends, pick the cm). Honest (reads the tool), distinct (NO measurement-reading facet live — vs K.MD.A.2 compare-length tap-bigger, K.MD.A.1 describe-attribute tap-word, vs the ruler manipulative's drag-verb), wrapper-only. **choice-board-core 0-line (protected, empty diff).** Surfaced 2 forks via AskUserQuestion: (1) BUILD-recognition vs HALT-to-measurement-core → operator BUILD; (2) en unit → operator **cm for ALL 11** (simplest, identical render/gate, no inches anywhere). **LESSON: a measurement standard whose full pedagogy is an active-manipulation verb CAN still ship an honest graded RECOGNITION on a pick-engine if a real assessment-item form exists (read-the-tool) — the active verb defers to a manipulation core; surface the BUILD-vs-defer + the per-locale UNIT as operator forks (the metric/customary idiom-trap class).**
- **THE to-scale render (novel, 0-core):** a code-drawn cm ruler (0..20, major+minor ticks + numerals, each tick `data-tick=k`) + a to-scale object (`RULER_OBJECTS` map of 8 distinct parametric builders — pencil/crayon/marker/ribbon/worm/stick/straw/eraser — each spanning x(0)..x(N)) injected ABOVE the board via the render() override (the `_fractionAnchor` precedent). ONE scale fn `_rrX(cm)` places BOTH ticks AND the object's right edge → the object honestly spans N cm. **Fully code-drawn SVG, NO image-library dependency → §5 asset rules sidestepped (no asset-blocked risk).** 4 bare-numeral tiles = canonical-misread set {N−1,N,N+1,N+2} (correct=N; foils=read-one-short/last-tick-edge/off-by-two), exactly-one-correct; n∈3..18 keeps tiles on the 1..20 scale. promptHowManyCm (cm every locale). nextTask gate extended (`...||read-ruler`).
- **THE to-scale GEOMETRY gate (the honesty proof):** local-test-choice-board-readruler.js reads the rendered `.cb-ruler-object` right-edge x + the `[data-tick="N"]` x via getBoundingClientRect and asserts they coincide (±3.5px) AND object-left===tick-0 → **measured Δright=0.0px across all 11 locales** (the drawn object genuinely spans N cm, not just tile math). **LESSON: for a code-drawn to-scale render, the puppeteer gate must MEASURE the rendered geometry (object edge === scale tick), not just the answer math — that's what makes "to-scale" honest vs decorative.**
- 11 native ensembles (3-agent) authored promptHowManyCm + slug/title/intro; framework NAME per §20.10 (0 CC-leak non-EN, EN=Common Core 2.MD.A.1), no-competition lock; clean slugs (no garbled tokens this round); [NSR-FLAG] sv/da/no/fi.
- Gates: verify-choice-board-readruler.js (MEASURED no-two-correct via real core, {N−1,N,N+1,N+2}, valid objects, on-scale, ≥7) PASS · local-test 11/11 (native prompt via i18n.current + Δright=0.0px to-scale + tap-foil/tap-correct + reshuffle + 280-768) · audit-activity-mobile.js 8/8 @280-768 (POST-deploy — the gate renders the live PAGE route, so it runs after deploy not before) · next-number + compare-2digit + ten-more-less siblings re-verified · core `git diff` EMPTY. choice-board-activity.js?v=68, ACTIVITY_WRAPPER_VERSION 8.95. poolSize reuses params.rounds (no audit edit).
- Verified LIVE: en/de/fi 200, JSON-LD 2.MD.A.1, framework localized (Lehrplan/OPS 2014), 0 CC-leak non-EN, iframe v8.95, preview 200 + index card, live variety 8-distinct+reshuffle, de@360 eyeballed (straw n=6 ending on the 6 tick, tiles {5,6,7,8}, "Wie viele Zentimeter lang?" clean wrap, 0 overflow). **E2-math CLOSED (17 skills). NEXT = E2-ELA top RF.K.1.d — engine boundary, AWAIT commission.**


---

## DONE 2026-06-19 — E2-math → 1.NBT.C.5 "Ten More, Ten Less" ±10 place-value jump on choice-board LIVE 11/11 (commit 00c8c921)
- [Activities live inventory](project_activities_live_inventory.md) — E2 now 16 live skills. 1.NBT.C.5 = given a two-digit number n (text subject), tap 10 MORE or 10 LESS (only the tens digit changes — no counting). New `ten-more-less` template; **choice-board-core 0-line (protected, empty diff vs HEAD).** Step-0 distinct (grounded vs the live 16-row dispatch): vs **next-number K.CC.A.2** (±10 mental jump vs +1, two-digit vs 0-10, AND the ±1 foil deliberately separates "+10" from "+1" thinking — the canonical misconception); vs **compare-2digit #15** (COMPUTE n±10 vs pick a true relation); vs E12 place-value (mental jump vs build-columns). NOT a re-skin.
- **THE place-value-foil design (load-bearing):** the 4 tiles are the FIXED offset set **{n−10, n−1, n+1, n+10}** — correct = op==='more'?n+10:n-10; foils = **n±1 (ones-digit trap, "±1 not ±10")** + **n∓10 (wrong-direction = the OTHER op's answer)** + n∓1. Offsets pairwise-distinct → EXACTLY ONE correct by construction. For 'more' vs 'less' on the SAME n the 4 tiles are identical and only `correct` flips → a pure direction×magnitude check. **n∈20..89 keeps every tile two-digit (10..99).** seededShuffle(vals, n*31) for cross-locale-identical order. **LESSON: a genuine place-value check is built by making the FOILS the canonical misconceptions (off-by-power-of-ten + wrong-direction), not random distractors — the gate then MEASURES both traps present every round.**
- Wrapper-only: ten-more-less branch + nextTask gate extended (`read-sight-word||compare-2digit||ten-more-less`; §A.13.60 per-pass reshuffle) + promptTenMore/promptTenLess ×11. manifest row (8 rounds, 4 more/4 less). **poolSize reuses params.rounds → already handled (line 44) → no audit edit (DoD by construction; live probe read pool=8 first try).** kid_prompt_template='promptTenMore'.
- 11 native ensembles (3-agent) authored promptTenMore/Less + slug/title/intro; framework NAME per §20.10 (Lehrplan/Currículo LOMLOE/Indicazioni nazionali/BNCC/programmes officiels/SLO-kerndoelen/Lgr22/Fælles Mål/LK20/OPS 2014), 0 CC-leak non-EN (EN=Common Core 1.NBT.C.5), no-competition lock; fi prompt idiom improved to "Paljonko on 10 enemmän/vähemmän?"; **da agent slug had a garbled "-toplerstal" suffix → fixed to `ti-mere-ti-mindre`** (LESSON: sanity-check agent-authored slugs for hallucinated tokens). [NSR-FLAG] sv/da/no/fi.
- Gates: verify-choice-board-tenmoreless.js (MEASURED via real core: tiles===offset set, correct=op±10, ±1 ones-trap + ∓10 direction-trap present, no-two-correct, two-digit, n∈20..89, spans both ops, ≥7) PASS · local-test-choice-board-tenmoreless.js 11/11 (native prompt via window.LCS.i18n.current — infer op from prompt, parse subject n, compute correct, tap-foil→no-celebrate/tap-correct→celebrate, reshuffle, 280-768) · audit-activity-mobile.js 8/8 @280-768 · next-number + compare-2digit siblings re-verified · `git diff --stat` core EMPTY. choice-board-activity.js?v=67, ACTIVITY_WRAPPER_VERSION 8.94.
- **DEPLOY GOTCHA (re-confirmed): audit-activity-mobile.js enumerates rows from `frontend/public/mini-tools/` (a SEPARATE local copy), NOT `mini tools/` (source).** Had to cp source→public locally before the gate saw the new row (preflight/verify/local-test read the source). Previews are GITIGNORED → scp to server `/var/www/lcs-media/mini-tools/previews/` BEFORE deploy.sh (done first this time → preview 200 + index card on first deploy).
- Verified LIVE: en/de/fi 200, JSON-LD 1.NBT.C.5, framework localized (Lehrplan/OPS 2014), 0 CC-leak non-EN, iframe v8.94, preview 200 + index card, live variety 8-distinct+reshuffle, de@360 eyeballed ("Was ist 10 weniger?" 2-line wrap, subject 57, 4 tiles, 0 overflow). **NEXT = E2-math LAST firm code 2.MD.A.1 — fast-follow, no stop unless a fork.**


---

## DONE 2026-06-19 — E2-math → 1.NBT.B.3 "Compare Two-Digit Numbers" >,=,< on choice-board LIVE 11/11 (commit 7b2af2b4)
- [Activities live inventory](project_activities_live_inventory.md) — E2's DENSEST distinctness surface (14 live skills). 1.NBT.B.3 = tap the TRUE comparison STATEMENT ("47 < 52" / "47 = 52" / "47 > 52") for two two-digit numbers. **choice-board-core 0-line (protected core, empty diff vs HEAD).** Step-0 distinct vs the crowded comparison neighbors (read the live templates to confirm): **K.CC.C.7 = pick-bigger/pick-smaller → tap a NUMBER (single-digit, NO symbol, NO equal)**; 1.NBT.B.3 = tap a relation STATEMENT (two-digit + >/=/< + the equal case) = distinct option-set + cognitive task + grade-1 symbols (operator-confirmed grade-appropriate, unlike K's no-symbol K.CC.C.7); vs K.CC.C.6 which-more (groups); vs 2.NBT.A.4 (E4 MATCH, 3-digit). NOT a re-skin. **LESSON: on a crowded engine, read the live neighbor TEMPLATES (not just codes) — K.CC.C.7's instantiation is pick-bigger/pick-smaller (tap-a-number); the distinctness is the option-set (statements vs numbers) + symbols + range, not the code.**
- Wrapper-only: compare-2digit branch (3 statement tiles via the even-odd text-tile precedent `setupTask(options, correctKey, null)`, correct = the true relation; EXACTLY ONE true by construction) + nextTask gate extended to compare-2digit (per-pass reshuffle §A.13.60, gated with read-sight-word) + promptCompareTrue ×11. manifest: 8 two-digit pairs (mix </=/>, place-value-varied incl. digit-reversal trap 29-vs-92 + equal pairs).
- **poolSize() += params.pairs UP FRONT (the banked new-shape audit applied proactively):** params.pairs was unhandled (pick-bigger/which-more use it but never hit the live probe; compare-2digit DOES via nextTask → would false-fail N=0). Added before deploy — live variety probe read pool=8 first try (no mid-flight catch). The new-shape-audit DoD working as designed.
- Gates: verify-choice-board-compare.js (measured: 3 options lt/eq/gt, EXACTLY ONE true via the REAL core's setupTask+answer, two-digit, spans </=/>, ≥7 — no-two-correct) + local-test-choice-board-compare.js (11/11: native prompt per locale via window.LCS.i18n.current, EVALUATES each tile arithmetically, tap-true→celebrate / tap-false→no-celebrate). sight-word (nextTask path) + pick-bigger (tasks[] path) siblings re-verified unaffected. choice-board-activity.js?v=66 (core ?v=8 unchanged), WRAPPER 8.93. [NSR-FLAG] sv/da/no/fi. Verified LIVE: en/de/fi 200, JSON-LD 1.NBT.B.3, framework localized, 0 CC-leak, iframe v8.93, live variety 8-distinct+reshuffle, tiles eyeballed @280/390 (de 3-line wrap, 0 overflow). **NEXT = E2-math 1.NBT.C.5 — fast-follow, no stop unless a fork.**


---

## DONE 2026-06-19 — E14 → 2.G.A.2 "Make and Count Squares" partition a rectangle into R×C squares + count LIVE 11/11 — CLOSES E14 (commit d0e9fc13)
- [Activities live inventory](project_activities_live_inventory.md) — **SCOPE CORRECTION banked: the active-partition engine (fractions-core) is LIVE, NOT deferred** (its header: "E14 #3 — the ACTIVE-PARTITION VERB engine"; the partition pair 1.G.A.3 halves/fourths + 2.G.A.3 thirds run on it; `cut:'v'|'h'|'grid'` axis). The "deferred active-partition #3" in the prior backlog was STALE (§A.13.6 spec-vs-shipped) → removed from the deferred-core backlog. So 2.G.A.2 (the partition VERB) was a thin extend on the live engine, NOT a fork.
- 2.G.A.2 = partition a rectangle into rows×cols same-size squares + COUNT the total. Step-0 distinct: vs the fraction-partition pair (those teach FRACTIONS/equal-shares; 2.G.A.2 counts UNIT SQUARES — geometry/area precursor) AND vs E13 build-array 2.OA.C.4 (both end at R×C count, but the VERB differs — partition-by-cutting vs fill-with-dots; §22.1 same-endpoint/different-mechanic + different strand — operator-ratified, NOT a re-skin).
- **DECLARED fractions-core touch (+28 lines, additive/gated; partition pair BYTE-IDENTICAL re-verified):** a `'gridrect'` shape with a generalized grid `_lines` ((cols-1) vertical + (rows-1) horizontal evenly-spaced cuts → R×C same-size squares; square-cell box via s=80/max(R,C)) + a gridrect `_bodySVG` branch. Gated on `opts.gridCols` → the partition pair never reaches it (5 protected cores 0-line; fractions-core is NOT one of them). **LESSON: a manipulative core's existing `cut`/mode axis can host a NEW standard via a gated `_lines`/`_bodySVG` branch — additive (+N lines, 0 deletions) keeps siblings byte-identical; verify with `git diff HEAD --stat` = inserts-only + the sibling gate.**
- Mechanic: interactive partition stage (the live cut-to-commit verb) + answerType `number` keypad-count (the K.OA.A.2 interactive-stage+keypad pattern; shell doesn't lock the stage). nextTask reused (the fractions wrapper already owns ordering). **GATE-COVERAGE FIX: `verify-fractions-core.js` (equal-area partition gate) iterated ALL manifest rows → crashed on the shapeless grid-count round (polyOf(undefined)); scoped it to `task_template==='partition-equal-shares'` (the grid-count row has its own gate). LESSON: when a new template lands on a shared engine, the SIBLING measured-gate that assumes the old round shape must be scoped to skip it.** verify-fractions-gridcount.js (measured: (cols-1)V+(rows-1)H=R×C square cells, answer===R×C, ≥7 varied) + local-test-fractions-gridcount.js (11/11 native prompt + partition + keypad). fractions-core/activity ?v=7, WRAPPER 8.92. [NSR-FLAG] sv/da/no/fi. Verified LIVE: en/de/fi 200, JSON-LD 2.G.A.2, framework localized, 0 CC-leak, iframe v8.92, live variety 8-distinct+reshuffle, R×C grid eyeballed @280/390 (de 3×4, 0 overflow). **E14 CLOSED. NEXT = E2-math (1.NBT.B.3) — engine boundary, await commission.**


---

## DEFERRED 2026-06-19 — E12 #4 (1.NBT.C.4 add within 100, compose-a-ten) — STEP-0 FORK → deferred to a new regroup core (NO code shipped)
- [Activities live inventory](project_activities_live_inventory.md) — **protected-core fork, operator-ruled defer.** `place-value-core.js` is a REPRESENT-only engine: addTen/addOne/removeTen/removeOne + strict-decomposition check; **NO regroup/compose/bundle/carry/sum primitive** (grep=0; "bundled" = the ten-rod VISUAL only). 1.NBT.C.4's defining pedagogy = **compose-a-ten regrouping** (trade 10 ones → 1 ten). Both halt conditions fired: (A) honest compose-a-ten needs a NEW core operation → place-value-core is a 5-PROTECTED core (0-line mandatory) → fork; (B) the only wrapper-only path ("build the sum's canonical tens/ones") = a re-skin of 1.NBT.B.2 (build-a-number + addition label; regroup NOT enforced — child builds the answer directly, strict check is path-independent) → re-skin HALT. **Surfaced that 2.NBT.B.7 (the standing "next" code) forks IDENTICALLY** (same regroup operation) — the place-value engine cannot operate, only represent; ALL E12 operate-with-regrouping codes are protected-core-blocked.
- **RULING (operator): defer 1.NBT.C.4 + 2.NBT.B.7 to a future regroup-capable `place-value-regroup-core`** (clean-sibling, compose-a-ten/trade verb; post-set gamified-manipulation phase — same disposition as K.CC.C.6→compare-groups-core). **E12 = COMPLETE at its 3 represent activities.** No code shipped (fork-ruling). **LESSON: a "build-a-number" manipulative core cannot host an "operate-with-regrouping" standard without a regroup primitive — represent-vs-operate is the engine-fit fault line; check the core's verb set against the standard's VERB at Step-0, and a whole code FAMILY (all regrouping codes) can be blocked by one protected-core gap.** Deferred-core backlog now: compare-groups-core (K.CC.C.6), place-value-regroup-core (1.NBT.C.4 + 2.NBT.B.7), fractions-core active-partition (E14 #3), phoneme/Elkonin.
- **NEXT = E14 (fractions) per the per-engine order.** Engine boundary + fork-ruling → AWAIT the operator's next CC COMMISSION (do NOT auto-start). Note: E14 recognition (#1 1.G.A.3 + #2 2.G.A.3) + the partition activities are already LIVE; the operator's E14 commission will scope what remains.


---

## DONE 2026-06-19 — E1 #8 → K.OA.A.5 "Quick Facts to 5" fluency within 5 on the ten-frame LIVE 11/11 — CLOSES E1 (commit 0b1cf2d7)
- [Activities live inventory](project_activities_live_inventory.md) — E1's last firm code. **0 CORE TOUCH** (ten-frame-core empty diff vs HEAD; 5 protected cores 0-line). Fluency within 5 via a bare expression "3 + 2 = ?" + a fast **CHOICE-tap** (recall) — a THIRD answer mode for ten-frame (neither #6's frame-STATE nor #7's KEYPAD). **Step-0 distinct:** vs #6 K.OA.A.1 (choice-recall vs two-colour BUILD, within-5 vs within-10); vs #7 K.OA.A.2 (CHOICE vs keypad, bare fact vs story, recall vs comprehend-model); vs counting facets (recall-a-fact vs read-a-count). **LESSON: when two siblings already occupy the state + keypad answer surfaces, answerType:'choice' (shell-supported, chips in answerEl, renders in array order so pre-vary the choices) is the clean third distinct surface — and "fluency" maps to fast recognition.**
- ten-frame-activity.js: quick-fact branch (bare expr prompt, answerType 'choice', empty interactive within-5 frame as optional scratch; choices = p.choices.map(v=>({value:v}))) + nextTask gating extended + taskQuickAdd/taskQuickSub (universal math notation ×11). manifest row: 8 {a,b,op,answer,choices} mix +/− within 5, **choices pre-varied order + EXACTLY ONE correct (no-two-correct)** + frames:1 + slug/title/intro ×11 (native chrome; framework localized, 0 CC-leak; no-competition lock — sv/da prose explicitly DISCLAIMS racing/timing). **poolSize already covered params.problems** (the #6 fix). 
- Gates: verify-ten-frame-quickfact.js (measured: answer===a±b within 5, choices len-3 distinct exactly-one-correct, pool spans +/−, ≥7) + local-test-ten-frame.js extended to **[op tap-cells / story keypad / choice chip-tap] ×11** (choice: tap wrong chip→no-celebrate, answer chip→celebrate). #6/#7/make-n re-verified byte-identical. activity ?v=4 (core ?v=3 unchanged), ACTIVITY_WRAPPER_VERSION 8.91. [NSR-FLAG] sv/da/no/fi. Verified LIVE: en/de/fi 200, JSON-LD K.OA.A.5, framework localized, 0 CC-leak, iframe v8.91, live variety 8-distinct+reshuffle, choice-chip render eyeballed @280/390. **E1 CLOSED (8 live). NEXT = top of E12 (place-value) 1.NBT.C.4 — fast-follow, no stop unless a fork.**


---

## DONE 2026-06-19 — E1 #7 → K.OA.A.2 "Solve the Story" word problems within 10 on the ten-frame LIVE 11/11 (commit 3b384df7)
- [Activities live inventory](project_activities_live_inventory.md) — **0 CORE TOUCH** (ten-frame-core untouched, empty diff vs HEAD; 5 protected cores 0-line). Two distinctness checks (operator-mandated): **vs #6 K.OA.A.1** — #6 = represent a bare expression via frame-STATE; K.OA.A.2 = native STORY prompt + interactive frame (model) + **KEYPAD-solve** (the child comprehends the story + extracts the operation, not represents a given one). The mechanic difference is the ANSWER MODE (state→keypad). **KEY ENABLER: the shell renders the keypad in a separate `answerEl` and does NOT lock the stage for answerType:number** → interactive frame + keypad COEXIST (how-many CHOOSES readOnly=true; this keeps it false). **vs E18 #6 2.OA.A.1** — ten-frame counters within-10 K vs numeral part-whole bond within-100 grade-2 (distinct engine+representation+range+grade). Both keypad (universal solve surface).
- **CONTENT-LOCALE BUG caught by the local-test: read `window.LCS.i18n.current`, NOT `self.language`/`api.lang`.** First wrote `self.language` (= api.lang = 'en'/default) → de/es/etc. served ENGLISH story text. The local-test surfaced it (de showed "2 ducks swim…"). Fix = the E18 #6 pattern `window.LCS.i18n.current`. **LESSON: for per-locale byLocale content in a ten-frame/shell tool, the content locale is `window.LCS.i18n.current` — api.lang is the tool default, not the page locale.**
- nextTask gating extended to word-problem (other ten-frame templates keep tasks[] path). taskStoryProblem = "{text}" passthrough. **poolSize already covered byLocale.rounds** (the money fix) → no new shape. per-locale native story text (8 fixed {a,b,op,answer} tuples, numbers fixed once, native situation text hardest-grammar-first fi partitive/de case; DIGITS required so the verify can assert both quantities present). 
- Gates: verify-ten-frame-storyproblem.js (measured: 88 rounds [11×8], answer===a±b, story states both quantities, core.setCount(answer) fits the frame, pool spans +/−, ≥7/locale; paint stubbed DOM-free) + local-test-ten-frame.js GENERALIZED to [op tap-cells-state, story keypad-via-.answer-metadata] ×11. activity ?v=3 (core ?v=3 unchanged), ACTIVITY_WRAPPER_VERSION 8.90. [NSR-FLAG] sv/da/no/fi. Verified LIVE: en/de/fi 200, JSON-LD K.OA.A.2, framework localized, 0 CC-leak, iframe v8.90, served manifest has native de+fi text, live variety 8-distinct+reshuffle, story+frame+keypad eyeballed @280/390 (de longest, 0 overflow). **NEXT = E1 LAST firm code K.OA.A.5 — fast-follow, no stop unless a fork.**


---

## DONE 2026-06-19 — E1 #6 → K.OA.A.1 "Show the Operation" represent +/− on the ten-frame LIVE 11/11 (commit e08febb9)
- [Activities live inventory](project_activities_live_inventory.md) — first E1 build this phase; **first DECLARED ten-frame-core touch** (ten-frame-core is NOT a 5-protected core; operator permitted + required re-verify of siblings). Step-0 distinct (grounded vs live engine): the engine tracks a single `count` (tap-add/tap-remove); make-n=fill-to-N, how-many=read+keypad. K.OA.A.1 represents the OPERATION: **addition = fill to a+b with the two addends shown as TWO COLOURS** (first a coral #F2784B, next b teal #1B9E8F — a paint() split), **subtraction = pre-fill the minuend, child REMOVES the subtrahend** (engine already supports tap-to-remove → near-zero core for sub). Within 10, single frame, answerType state. Distinct from make-n (build a number), how-many (read), teen-numbers K.NBT.A.1 (teen place-value, double frame), and E18 K.OA.A.4 bond (part-whole nodes).
- **DECLARED core touch = minimal + default-off:** `init` adds `splitAt=null/splitColor=null`; `paint()` colours cells `ord>splitAt` with splitColor when splitAt set, else `s.color`. splitAt null → BYTE-IDENTICAL → **all 5 live E1 siblings re-verified** (splitAt null, tokens render img/svg, how-many readout hidden+keypad, no overflow). 5 protected cores 0-line. **LESSON: the image-theme paint OVERRIDE (themed make-n/how-many) is separate from core paint — the splitAt branch lives in CORE paint, used only by themeless activities (teen-numbers + the new op); themed siblings use the override, untouched.**
- **subtraction via the engine's existing tap-to-remove (`setCount(ord-1)`) = near-zero core** — pre-fill minuend in setup (`tool.setCount(a)`), child taps to reduce to a−b. The take-away IS the representation. Addition's two-colour split is the only genuinely new render.
- nextTask per-pass reshuffle (§A.13.60) ported + gated to represent-operation (other ten-frame templates keep tasks[] path). **poolSize() += `params.problems`** (NEW task-pool shape — the standing DoD fix; 3rd shape after byLocale + tasks). 2 prompts ×11 ({a}/{b} ICU placeholders preserved — Finnish kept them literal per case-trap rule). 10 problems (5 add/5 sub, within 10).
- Gates: verify-ten-frame-operation.js (measured — real core.setCount reaches a+b/a−b within the single frame, predicate count===result discriminates, pool spans +/−, ≥7; **paint stubbed DOM-free** since ten-frame paint assumes cells, unlike match-pairs) + local-test-ten-frame.js (11/11 tap-to-count, parse "a op b" from prompt — universal across locales). core ?v=3, activity ?v=2 (added — had none), ACTIVITY_WRAPPER_VERSION 8.89. Preview scp'd BEFORE build (clean first deploy). [NSR-FLAG] sv/da/no/fi. Verified LIVE: en/de/fi 200, JSON-LD K.OA.A.1, framework localized, 0 CC-leak, iframe v8.89, live variety 10-distinct+reshuffle, two-colour addition + pre-fill subtraction eyeballed @280/390. **NEXT = E1 next firm code K.OA.A.2 — fast-follow, no stop unless a fork.**


---

## DONE 2026-06-19 — E4 #7 → 2.OA.B.2 "Match Facts to 20" fluency-within-20 LIVE 11/11 — CLOSES E4 (commit dd5cf993)
- [Activities live inventory](project_activities_live_inventory.md) — E4's LAST firm code. **Step-0 distinct (grounded vs LIVE card data, not assumed):** D.7=expr↔expr, D.8=**BLANK**-equation↔number (solve-for-unknown), B.3=expr↔commuted-expr. 2.OA.B.2 = the ONLY E4 face pairing a **COMPLETE expression to its ANSWER number** ("8 + 5"↔"13", "17 − 9"↔"8"), mixing **+/− within 20** = direct fact RECALL. Distinct from D.8 on BOTH card shape (complete vs blank) AND skill (recall vs inverse). **LESSON: complete-expr↔value (recall) is a genuinely distinct face from blank-equation↔number (D.8 solve) — the blank-vs-complete card + the recall-vs-inverse skill carry the distinctness; framed the prompt as "work out each FACT" (not "find the missing number") to hold the query face apart.**
- **match-pairs-core.js BYTE-IDENTICAL (protected core 0-line, empty diff).** Reuses the find-missing CODE shape (a.value===b.value, object cards) with COMPLETE expression cards. 8 tasks, each 6 cards = 3 expr↔value pairs, 3 distinct values, pool MIXES +/−. nextTask per-pass reshuffle (§A.13.60) — extended the `_loadActivity` install gate to `commutative || fluency-within-20`. local-test-match-pairs.js GENERALIZED to loop [commutative, fluency] ×11. groups() parser extended to "a + b"/"a − b"(U+2212)/bare-number so expr+answer group by value.
- **poolSize already covered params.tasks (the #6 fix) → no new param shape → poolSize DoD satisfied by construction** (the standing instruction: poolSize coverage is DoD when a NEW task-pool shape is introduced; this reused params.tasks, so no action — and the live variety probe read pool=8 directly).
- 11-locale native chrome (recall framing, each locale's match-pairs voice; framework NAME per §20.10, 0 CC-leak; EN=Common Core 2.OA.B.2). **nl agent's titleFluency "Som-tweekamp" (sum-DUEL) discarded — competition term violates the no-score/no-competition lock; set titleFluency:=page_title for all locales (neutral synced value, also the cleanest title source).** [NSR-FLAG] sv/da/no/fi.
- verify-match-pairs-fluency.js (measured: real core correct pairing + cross-value rejected + complete-fact≤20↔number + pool spans +/− + ≥7). wrapper ?v=62, ACTIVITY_WRAPPER_VERSION 8.88. **Preview scp'd BEFORE deploy.sh build this time (the #6 lesson) → preview 200 + index card rendered on the FIRST deploy** (no second build needed). Verified LIVE: en/de/fi 200, JSON-LD 2.OA.B.2, framework localized, 0 CC-leak, iframe v8.88, live variety 8-distinct+reshuffle. **E4 CLOSED (7 activities). NEXT = E1 top K.OA.A.1 (ten-frame) — fast-follow, no stop unless a fork.**


---

## DONE 2026-06-19 — E4 #6 → 1.OA.B.3 "Add It Both Ways" commutative-property matching LIVE 11/11 (commits 12ae9db7 + ea164608)
- [Activities live inventory](project_activities_live_inventory.md) — **K.CC.C.6 (E4 top) HIT A STEP-0 FORK → operator pivot to 1.OA.B.3.** Both named halt conditions fired (confirmed vs live engine+manifests): (a) honest K.CC.C.6 compares two groups of OBJECTS, but match-pairs renders TEXT-ONLY card faces → object-group cards need a match-pairs-core RENDER change = breaks protected-core 0-line; (b) the wrapper-only symbol form is K.CC.C.7 (already live ×2 on E2, which also owns K.CC.C.6 ×1) + `<,>,=` not K-appropriate. Surfaced via AskUserQuestion; operator chose **1.OA.B.3**. **LESSON: a comparison standard ships wrapper-only on match-pairs ONLY via the compare-3digit expr↔symbol value-pairing pattern; an OBJECT-group compare (K.CC.C.6) needs a NEW core = fork.** A new compare-groups-core.js is the genuine home for the one-to-one-correspondence/leftover pedagogy (deferred).
- **6th E4 activity; match-pairs-core.js BYTE-IDENTICAL (protected core, 0-line — `git diff --stat` empty).** Commutative is match-pairs-NATIVE: pair `a + b` ↔ `b + a` (same sum, swapped order); the matching VERB *is* commutativity. Reuses the `a.value===b.value` model (object cards, number-free celebration) like equal-value/find-missing/compare-3digit. Distinct from 1.OA.D.7 equal-value (add↔SUBTRACT) — this is add↔commuted-add. 8 tasks, each 6 cards = 3 commuted pairs with **3 DISTINCT sums** (each sum's only 2 cards are its a+b/b+a → forced pairing, no false-correct).
- **§A.13.60 per-pass reshuffle, NEW for match-pairs:** ported number-bond's `shuffledOrder`/`nextTask`+`_pool`, **gated to the commutative template ONLY** (in `_loadActivity`: set `_pool`, `tasks=null`, install `nextTask`). **The shell prioritizes `tasks` over `nextTask` (getTask line 711), so tasks MUST be nulled** for the nextTask path to engage; the other 5 match-pairs activities keep the per-mount `tasks[]` path byte-identical. Title-swap IIFE (`id.indexOf('add-it-both-ways')`) swaps title/instruction at module-eval.
- 11-locale native authoring (chrome only; math is language-neutral numerals, fixed once): title/instruction/taskCommutative/hintCommutative/hintTryCommutative/slug/page_title/page_intro. Framework NAME localized per §20.10 (Lehrplan/Currículo LOMLOE/Indicazioni nazionali/BNCC/programmes officiels/SLO-kerndoelen/Lgr22/Fælles Mål/LK20/OPS 2014), **no code; 0 CC-leak on the 10 non-EN** (EN keeps Common Core). [NSR-FLAG] sv/da/no/fi.
- Gates: `verify-match-pairs-commutative.js` (MEASURED — drives the REAL core headless via onCardTap [paint early-returns w/o `_cardEls`, `_speakCard` guards on LCSAudio], asserts correct pairing→allPaired+equal-value, cross-value rejected, true commuted pairs a+b/b+a a≠b, ≥7 tasks) + `local-test-match-pairs.js` (11/11). wrapper `?v=61`, ACTIVITY_WRAPPER_VERSION 8.87 (core `?v` unchanged).
- **TOOLING FIX (ea164608): `audit-activity-variety.js poolSize()` now handles `params.tasks`** (match-pairs board shape) — read rounds/targets/items/byLocale but not tasks → null → 2*N=0 → false "0 distinct" live-probe FAIL. Same class as the money byLocale poolSize fix (77764607). Now reads 8; live probe PASS 8-distinct+reshuffle.
- **DURABLE GOTCHA: activity previews must be scp'd to `/var/www/lcs-media/mini-tools/previews/` BEFORE `deploy.sh`** — `/mini-tools/` static is served from the Next BUILD SNAPSHOT of `public/` (symlink contents copied at build; NO nginx alias). A post-build scp 404s + the index card's build-time fs-existence-check renders no `<img>` → needed a SECOND deploy.sh. Fold preview scp into the pre-build cp step next time.
- Verified LIVE: en/de/fi 200, JSON-LD 1.OA.B.3, framework localized, 0 CC-leak non-EN, iframe v8.87, live variety 8-distinct+reshuffle, preview 200 + index card renders. **E4 progress: 6 activities (K.OA.A.3, 1.OA.D.7, 2.NBT.A.3, 1.OA.D.8, 2.NBT.A.4, 1.OA.B.3). NEXT E4 firm code = 2.OA.B.2 (fluently add/subtract within 20) — fast-follow, no stop unless a fork.**


---

## DONE 2026-06-19 — E18 #6 "Story Problems" (2.OA.A.1) LIVE 11/11 — word problem as a NUMERAL bond; E18 COMPLETE (commit 0ca032de)
- [Activities live inventory](project_activities_live_inventory.md) — one-step add/subtract word problems within 100, modeled as a part-part-whole bond. Child reads the problem (prompt strip + TTS), the bond shows the two knowns as NUMERALS + "?" at the unknown (any position), types the answer (keypad, graded vs `answerKey()`). Two-step DEFERRED (doesn't reduce to one bond). **CORE EXTENSION: `mode:'word-problem'` + `_renderWordProblem` (NUMERAL nodes, no dots — within-100 can't render 47 counters; reuses 2-part geometry) + `answerKey()`. Dot-bond modes #1-#5 byte-identical + re-verified.** **LESSON: within-100 forces a numeral bond (dots only work ≤~10); the word-problem TEXT rides the shell PROMPT strip + its TTS speaker (NOT a money-style context row) — so the core touch is just the numeral render.**
- **PER-LOCALE word problems = native-authored language (the load-bearing work), NOT label swaps.** Pattern: **NUMBERS FIXED across all 11 locales** (math verified ONCE), each native agent writes only the SITUATION TEXT with correct agreement (fi partitive "20 punaista tulppaania", de case) → manifest `params.byLocale.<loc>.rounds` carries native text + the shared {first,second,whole,unknownPos}. promptWordProblem="{text}" passthrough (native sentence rides promptArgs.text). Hardest-grammar-first (fi/de). es flagged+reframed problem-8 (kept answer 47). **LESSON: for fixed-math/native-text activities, give agents the fixed number tuples + EN model → they return only `texts[]` in order; merge with the verified tuples = math-error-proof + native-correct.**
- NOVEL RENDER PROVEN on the artifact @280px AND 390px incl. de (longest text): 0 overflow, numerals/"?" legible, prompt wraps. verify word-problem branch (byLocale, parts+whole consistent, answerKey correct, ≤100; 128 rounds/6 coords — needed a guard so the top-level `params.rounds` variety check skips byLocale rows). local-test #6 (keypad vs answerKey + real-text assert; 6×11). preflight 11/11 (57 coords); variety pool=8. core/activity ?v=6, wrapper 8.86. [NSR-FLAG] sv/da/no/fi. Verified LIVE: en/de/fi 200, 2.OA.A.1, de/fi 0 CC-leak (Lehrplan/OPS 2014), live variety + live de@280 0-overflow.


---

## DONE 2026-06-19 — E18 #5 "Add Three Numbers" (1.OA.A.2) LIVE 11/11 — 3-part bond (core extension, commit 75f943db)
- [Activities live inventory](project_activities_live_inventory.md) — E18's 5th firm code; first GENUINE core extension of the drive-the-set phase (not data-only). 3 addends → one whole ("?"), child reads the parts, adds, keypads the total. Distinct vs #3 (1.OA.A.1 two-addend) by code + the **3-part structure IS the standard** (1.OA.A.2 = add three). EXTEND = additive `mode:'whole-unknown-3'` on number-bond-core.js: new `_renderWholeUnknown3` (3 part-nodes at cx 18/50/82, **r=15 reused** — spans 3..97 in the 100 viewBox, no r-shrink needed, dots stay tested) + `total()` now sums a `third` (2-part: third null→0, **byte-identical**). #1–#4 2-part paths untouched + re-verified. 4 other protected cores 0-line. **LESSON: a 3-node bond reuses the 2-node `_partGroup` r=15 at cx 18/50/82 — 3 circles fit the 100-wide viewBox with 2u gaps; no geometry rescale, dot-packing stays the tested path.**
- **NOVEL-RENDER-PATH RULE honored: 3-part bond eyeballed on the actual artifact at 280px AND 390px PRE-deploy** (0 overflow, no node overlap, legible numerals, dot-packing 6=5+1/7=5+2) — by-construction is not proof. verify add-three branch (3-part sum + each part≤7 render-safety; 40 rounds/5 coords); local-test #5 keypad + 3-node assert (5×11); preflight 11/11 (56 coords); variety pool=8. core/activity ?v=5, wrapper 8.85. [NSR-FLAG] sv/da/no/fi. Verified LIVE: en/de/fr 200, 1.OA.A.2, v8.85, de/fr 0 CC-leak (Lehrplan/Programmes officiels), live variety + 390px 3-part render. **GOTCHA: heredoc with `${...}` template literals mangles the temp puppeteer script — Write the .js via the Write tool, not a heredoc.** **NEXT (no pacing stop): E18's last firm code 2.OA.A.1 (word problems within 100), then E4.**


---

## DONE 2026-06-19 — E18 #4 "Find the Missing Part" (1.OA.B.4 subtraction-as-unknown-addend) LIVE 11/11 (commit 1bc88a60)
- [Activities live inventory](project_activities_live_inventory.md) — first DRIVE-THE-SET build. Subtraction as an unknown-addend bond ("8−3=? → 3 and ? make 8"): whole + one part given, child taps the empty part to fill the missing part (=difference). **EXTEND reuses the make-ten MODE — NO new core mode** (make-ten already generalizes: `setupTask({whole,given})`, `isCorrect=given+filled===whole` IS the unknown-addend check; whole=minuend, given=subtrahend). number-bond-core.js change = **ONLY 3 new localized strings** (titleSubtraction/instructionSubtraction/promptSubtraction ×11); make-ten render/check reused byte-identical; #1/#2/#3 untouched. **LESSON: a "new" coordinate can be PURE-DATA+STRINGS on an existing mode — when the mechanic + grade-check already fit, don't add a mode; a new manifest row (task_template 'unknown-addend' → reuses setupTask make-ten path) + a subtraction-framed prompt + the title-swap branch is the whole build.** Render-safety: rounds keep both bond parts ≤9 dots (the tested make-10 packing range), wholes avoid 10 (so it doesn't read as make-10); gate asserts it.
- Step-0: **1.OA.B.4 was FREE** — re-confirmed on the LIVE E4 page (`find-the-missing-number` = 1.OA.D.8/match-pairs, NOT B.4; older handoff wrong). Distinct vs #1 make-10 (code+range+framing) and vs E4 1.OA.D.8 (engine/interaction/artifact). verify-number-bond-core default make-ten branch covers #4 + render-safety (32 rounds/4 coords); local-test #4 (tap-fill, 4×11); preflight 11/11 (55 coords); variety pool=8. core/activity ?v=4, wrapper 8.84. [NSR-FLAG] sv/da/no/fi. Verified LIVE: en/de/fr 200, 1.OA.B.4, v=8.84, de/fr 0 CC-leak (Lehrplan/Programmes officiels), live variety + 390px bond render (8/3). **NEXT (no pacing stop): E18's next firm code 1.OA.A.2 (add 3 numbers, 3-part bond).**


---

## DONE 2026-06-19 — E9 sight-word #2 "Tell the Words Apart" (RF.K.3.c) LIVE 10 locales — audio near-word discrimination (commit 52df99c2)
- [Activities live inventory](project_activities_live_inventory.md) — fast-follow to E9 #1 (Read Common Words). #2 = SAME audio-tap mechanic (hear→tap), but every round's distractors are the target's NEAR-TWINS / minimal pairs (was→saw, of→off/for, der→dem/den, les→mes). Distinct skill = **discriminate the commonly-confused HF words** (vs #1's broad recognition); harder Tier-2 band; §22.1 same-code/different-content. **Operator-ruled this clause over the no-audio inverse-visual option** (keeps the proven audio mechanic + homophone gate, which binds HARDEST on minimal pairs — de ist/isst, fr est/et). **LESSON: when #2 keeps #1's exact mechanic + a homophone gate, that's the signal the operator wants AUDIO retained (the "hardest-orthography-first to flush homophones" instruction only makes sense WITH audio); the no-audio inverse would moot the homophone doctrine.**
- **EXTEND = 1-line promptKey hook (leanest possible):** #2 RIDES the existing `read-sight-word` task_template — the ONLY wrapper change is `promptKey: (row.params && row.params.promptKey) || 'promptReadSight'`. #1 has no `params.promptKey` → default → **byte-identical**. #2's row sets `params.promptKey:'promptTellApart'` (a NEW prompt "Listen carefully…" ×10). choice-board-CORE 0-line; audio (LCSAudio) + nextTask reshuffle inherited unchanged. **A 2nd activity that's mechanically identical to #1 = a NEW MANIFEST ROW on the same template + a promptKey param; the distinctness is content (minimal-pair word sets) + framing + own slug/page/JSON-LD.**
- Hardest-orthography-first native lead (de/fr/es/it/pt sharpest homophone warning, then en/nl/sv/da/no). First native pass had non-word defects (fr "tune"=English, es "vis"=Latin, nl "da"/"mep") + several self-corrected dups/target-in-distractors → re-fixed. Gate `verify-sight-word.js` **generalized to ALL read-sight-word rows** (now covers #1+#2 = 160 rounds/20 locale-rows; per-locale homophone blocklist extended with the minimal-pair traps); `local-test-sight-word.js` drives BOTH rows (speechSynthesis spy, both prompts). preflight 54 coords; variety pool=8 reshuffle=yes. wrapper 8.83, choice-board-activity.js?v=65. fi excluded (404 by design). [NSR-FLAG] sv/da/no. Verified LIVE: en/de/fr 200, RF.K.3.c, fi 404, 0 CC-leak de/fr (Lehrplan/Programmes officiels), live variety both rows, live audio confirmed (en tell-words-apart spoke "want", tiles want/went/what/wait), 390px render clean (preview's 3-letter wrap was a desktop-4-col artifact; mobile 2×2 = one line/tile).


---

## DONE 2026-06-19 — E9 sight-word OPENED: "Read Common Words" (RF.K.3.c) on E2 choice-board, 10 locales (commit 95db45b3)
- [Activities live inventory](project_activities_live_inventory.md) — high-frequency / sight-word reading skill rides the proven `choice-board-core.js` (tap-the-word), NO new core (choice-board-CORE 0-line; all logic in the wrapper `choice-board-activity.js`). **Mechanic = HEAR the word → TAP it:** the target HF word is SPOKEN, the child reads 4 written word tiles and taps the one heard. Generic prompt "Tap the word you hear" never reveals the target → genuine reading, not a string-match. **AUDIO is wrapper-driven via the `window.LCSAudio.speak({type:'word',text,lang,rate})` global** (the shell's file-first/TTS-fallback voice; spoken in the task `setup()` + replay-on-retry in `check()`) — 0 core/shell touch. First activity to use audio-as-the-answer-channel.
- **SCOPE = 10 locales, Finnish EXCLUDED (operator-ruled on native early-literacy evidence).** Per-locale native reads: en/fr/da STRONG (genuine irregular sight words — Dolch/Fry, mots outils, ordbilleder); nl/sv/no/pt strong; de/es/it ok ONLY reframed as "high-frequency-word FLUENCY" (transparent/regular orthographies — NOT irregular sight words); **fi = NO sight-word tradition (transparent orthography, native: "a forced fit") → excluded, E7-cvc-style, fi route 404s by design.** Framed "read common high-frequency words". RF.K.3.c = EN anchor; non-EN cite national frameworks (0 CC-leak). **LESSON: sight-word is language-specific — NOT the universal-math fan; bring native early-literacy evidence per locale before scoping (don't guess word lists). Transparency-heavy locales reframe as HF-fluency or get excluded.**
- **Distractor doctrine (the audio no-two-correct rule): distractors are VISUALLY similar but must NOT be HOMOPHONES of the target** (a homophone = a 2nd aurally-correct tile). Native agents authored 8 rounds/locale; the FIRST pass had many defects (homophones de ist/isst, es que/qué, pt que/quê, fr il/ils + elle/aile; target-in-own-distractors es con / no har; non-words/foreign it ceh/du, nl hot/ot, da pår, no Swedish jag; malformed leading-space tokens) → re-dispatched all 9 with sharper rules + their drafts. `verify-sight-word.js` (NEW measured gate) is the backstop: 4 distinct single-token (`/^\p{L}+$/u`) options, target∉distractors, **per-locale HOMOPHONE_BLOCKLIST**, real core grades target→correct/distractor→wrong, ≥7 rounds (80 rounds pass). `local-test-sight-word.js` (NEW) **stubs speechSynthesis via Object.defineProperty** (window.speechSynthesis is a READ-ONLY accessor in headless Chrome — plain assignment silently no-ops) + serves `/audio/inventory.json` as `{}` so LCSAudio resolves into TTS → captures + asserts the target is spoken on load. Live audio confirmed on prod (route spoke "like").
- **Per-pass nextTask reshuffle added to choice-board for THIS template only** (§A.13.60): `cbShuffledOrder`/`nextTask` + `_loadActivity` branch sets `self._pool`+`self.tasks=null` for read-sight-word (shell calls nextTask); the other 13 choice-board skills keep `self.tasks=[]` (untouched, still shell-loop backlog). Generic iframe title stays "Choice Activity" (choice-board never swaps per-skill title — identity is the prompt + page title; NO title-swap needed unlike money/array single-activity engines). wrapper 8.82, choice-board-activity.js?v=64. [NSR-FLAG] sv/da/no. Verified: routes 200, RF.K.3.c, fi 404, 0 CC-leak de/it, live variety + audio.


---

## DONE 2026-06-19 — E13 #2: "Equal Groups" (2.OA.C.4) LIVE 11/11 — additive mode on array-core (commit 105baacd)
- [Activities live inventory](project_activities_live_inventory.md) — fast-follow to E13 #1 (build-the-array). Equal-groups = N discrete groups of M, repeated-addition total via an ADDITIVE `mode:'equal-groups'` on array-core.js. **KEY TRICK: map group→row, each→col internally** → `total()` (=groups×each), `builtEquation()` (one `each` per completed group), `_rowComplete`, `paint()` (the strip + filled reflection) are ALL UNCHANGED; only `render()` layout + strings differ. #1's grid branch wrapped in `else` (2-line insertion, grid code bytes untouched) + a new `_renderGroups` builds `.arr-groups`/`.arr-group` clusters. Distinct mechanic + query face + DOM vs #1 (re-confirmed on the LIVE #1 page first: `.arr-grid`, "Make N rows of C"); same CCSS code (same-code/different-mechanic §22.1). 3.OA.A.1 (true multiplication) stays out (K-2 ceiling). Title swap synchronous from ?activity (E3 #2 pattern). 2nd row in the SAME array-activities.json (already in MANIFEST_FILES). 4 new strings ×11 native (Finnish promptGroups partitive-correct "Tee N ryhmää, joissa kussakin on M"). Group cells 38px (K-2 tap min §A.13.55). Gates: verify-array-core mode-aware (16 rounds/2 coords, group→row/each→col map), local-test 2 activities×11 (layout grid-vs-groups asserted), preflight 11/11, live variety both rows. array-core/activity ?v=2, wrapper 8.81. [NSR-FLAG] sv/da/no/fi. **LESSON: an "equal-groups" repeated-addition verb reduces to a MODE on the rectangular-array core — same math layer (group≡row, each≡col), only the render layout (discrete clusters vs grid) + prompt/title localize; no new core.**


---

## DONE 2026-06-18 — E11 #2: "Make Change" (2.MD.C.8) LIVE 11/11 — additive mode on money-core (commit 96eac878)
- [Activities live inventory](project_activities_live_inventory.md) — fast-follow to E11 #1 count-out. **Make-change = compute paid−cost, tender the difference** via an ADDITIVE `mode:'make-change'` on money-core.js (target=paid−cost + a `.mn-context` "Costs X · You pay Y" row). #1 count-out path BYTE-IDENTICAL (mode absent → unchanged), re-verified 11/11. Distinct verb + query face on the SAME CCSS code (same-code/different-mechanic, §22.1). **Per-locale rulings:** bills/notes appear as the worded PAID amount (display-only via formatMoney — no note token); child tenders the CHANGE in coins; Nordic sv/da/no whole-krona; es=MXN currency + LOMLOE framework (currency call ≠ framework call — locked doctrine). **Title swap:** shell reads tool.strings.title at MOUNT → wrapper selects titleChange/instructionChange SYNCHRONOUSLY from ?activity (E3 #2 pattern, 0 lcs-shell touch). 2nd row in the SAME money-activities.json (already in MANIFEST_FILES — no new engine). 10 native agents authored rounds+strings (3 self-corrected single-coin solutions mid-flight; CC fixed fr R4). Gates: verify-money-core mode-aware (176 rounds/22 rows), local-test 2 activities×11, preflight 11/11, live variety both rows; live render eyeballed @390px. money-core/activity ?v=2, wrapper 8.80. [NSR-FLAG] sv/da/no/fi. **LESSON: a make-change ("give the difference") verb reduces cleanly to a tender-the-difference MODE on a count-out core — additive flag, NOT a new core; deltas are only target=paid−cost + a context render row.**


---

## DONE 2026-06-18 — E11 money engine OPENED: #1 "Count Out the Money" (2.MD.C.8) LIVE 11/11 (commit d519374d)
- [Activities live inventory](project_activities_live_inventory.md) — NEW `mini tools/money-core.js` clean-sibling (12th engine; **0 protected-core lines**). New TENDER verb: tap coins from a palette into a tray + running sum, win when tray total === target (answerType:'state', isCorrect = sum(tray)===target). Asset-free SVG-ish coin tokens (per-locale labels), `formatMoney()` per currency descriptor. Clean debut facet: single-currency, coins-only, count-to-target; **make-change / mixed coin+bill deferred to #2**. Registered in `frontend/lib/activities.ts MANIFEST_FILES`; wrapper v8.79; money-activity.html ?v=1.
- **MONEY IS PER-LOCALE (the trap, like the clock idiom):** currency/denominations/symbol/placement/decimal differ → manifest carries **per-locale `params.byLocale.<loc>` = {currency, coins, rounds}** (NOT a shared round set). Operator rulings: **en=USD ($/¢), es=Mexican peso (MXN $)**; de/fr/it/nl/fi=€, pt=R$ (Brazilian), sv=SEK/da=DKK/no=NOK (kr, minorPerMajor:1 whole-krona). **Currency `space` flag**: before-symbol currencies vary — $ no space ("$0.30"), but R$/nl-€ space ("R$ 0,35"/"€ 0,30"). **es flagged mismatch:** currency MXN but framework citation stays "Currículo LOMLOE" (§A.13.49 es register) — operator-aware.
- 11 native agents each owned a full currency system + 8 solvable count-out rounds + 6 strings. **LESSON: a measured gate that validates per-round solvability (solution⊆palette, sum===target, ≥2 coins, no one-tap, near-miss≠target) catches ALL agent arithmetic errors — 88 rounds (11×8) passed clean.** `verify-money-core.js` + `local-test-money.js` (tap-to-tender keypad-free) + framework carry + [NSR-FLAG] sv/da/no/fi.
- **TOOLING FIX (commit 77764607): `audit-activity-variety.js poolSize()` now handles per-locale `byLocale.<loc>.rounds`** — it only read flat `params.rounds`, so money false-failed the live variety probe (N=0 → 0 distinct). Any future per-locale-rounds activity needs this. Money now PASSES variety 11/11 live (8 distinct + reshuffle).


---

## DONE 2026-06-18 — E18 #3 "Find the Total" (1.OA.A.1) LIVE 11/11 — whole-unknown mode on number-bond core (commit 752320cd)
- [Activities live inventory](project_activities_live_inventory.md) — 3rd number-bond activity. Step-0: 1.OA.A.1 (putting-together, WHOLE unknown — find the total within 20) free + provably distinct from #1 K.OA.A.4 (missing PART, ≤10, tap, decompose) and #2 1.OA.C.6 (make-ten strategy): unknown=WHOLE, operation=compose, **modality=keypad**. find-missing-part-to-20 rejected as a re-skin of #1. Additive `mode:'whole-unknown'` on number-bond-core.js (render whole "?" + 2 given parts, display-only; new `total()`); #1/#2 byte-identical + re-verified. **LESSON: a new ANSWER MODALITY (keypad vs tap-fill) is NOT a new core — the bond core just declares `answerType:'number'` on the wrapper task and the shell renders its existing keypad (`renderAnswerSurface`/`check(tool,answer)`); 0 new keypad code.** Wrapper `makeWholeTasks` (answerType number, answerMax 20, check=answer===tool.total()) + title-swap IIFE extended for `find-the-total`. local-test extended with a keypad path (read total → click `.lcs-activity-key` digits → Check → celebrate; wrong total → no celebrate). 24 measured rounds pass; 33/33 local (11×3); live variety 8-distinct+reshuffle 11/11. html ?v=3, wrapper v8.78. [NSR-FLAG] sv/da/no/fi. **E18 now: #1 K.OA.A.4 make-10, #2 1.OA.C.6 make-a-ten-to-add, #3 1.OA.A.1 find-the-total.**
- STANDING (operator ruling 2026-06-18): the 39 NO-RESHUFFLE backlog activities are **≥7 distinct (NOT out of spec)** — they only lack the per-pass `nextTask` order-only reshuffle (§A.13.60 separate dimension). Convert each to `nextTask` **WHEN NEXT TOUCHED for another reason — ride-along, never a dedicated sweep**. Keep the list, don't action it.


---

## DONE 2026-06-18 — fractions.partition-thirds variety 5→7 distinct (2.G.A.3) LIVE (commit 90c15ca6)
- [Activities live inventory](project_activities_live_inventory.md) — live activity was 5 distinct < the ≥7 variety floor (§A.13.60); brought into spec. Root cause: `fractions-core.js _lines` hardcodes the partition per (shape,n) and only circle/triangle/hexagon/square/rect had an `n===3` branch. Added 2 genuinely-distinct shapes with EXACT thirds (declared fractions-core touch; 5 other cores 0-line): **diamond** = 2 cuts parallel to an edge (x−y=±R/3 → 3 equal strips, square-rotated-45°; `_edgeLine` helper) + **ellipse** = 3 affine-radii spokes param 90/210/330 (affine image of circle's 120° wedges → 3 equal sectors; `_eRadius` helper). **LESSON: the verify-fractions-core.js gate is GENERAL point-sampling (no per-shape theorem) — de-risk a new construction BEFORE coding by replicating its sampler on inline candidate cuts (read-only); diamond sampled 1.013, ellipse 1.001, both ≤ the 1.06 EQ_TOL.** n=2/n=4 branches untouched → halves+fourths byte-identical; recognition siblings on choice-board-core untouched. 0 new strings (prompt keyed by n; shapes have no displayed name; diamond/ellipse already in the halves/fourths pool). fractions-core/activity ?v=6, wrapper v8.77. verify 15/15 rounds exact; live variety probe 7 distinct + order-only reshuffle 11/11; routes 200.
- **Catalog-wide variety close-check: GATE PASSED. 39 backlog activities remain, NONE under-7-distinct** — all flagged `NO-RESHUFFLE (shell-loop)` (the SEPARATE §A.13.60 dimension: they don't use the `nextTask` per-pass reshuffle; pools are manual/≥7). By engine: choice-board 14, syllable-builder 11, ten-frame 5, match-pairs 5, place-value 3, cvc-builder 1. Operator to sequence; convert each to the `nextTask` pattern when next touched.


---

## DONE 2026-06-18 — E18 #2 "Make a Ten to Add" (1.OA.C.6) LIVE 11/11 (additive mode on number-bond core, commit 7e2514db)
- [Activities live inventory](project_activities_live_inventory.md) — fast-follow on the number-bond core. **Step-0 correction: E18 #1 shipped as K.OA.A.4 (make-10), NOT K.OA.A.3** (K.OA.A.3 "Make the Number" is on E4 match-pairs). 1.OA.C.6 (make-a-ten strategy to add within 20) was FREE as an activity + provably distinct (range ≤10→≤20; decompose-an-addend-to-make-ten vs find-complement; worked-addition artifact). 1.OA.B.3 rejected (no clean bond artifact). **Additive `mode:'make-ten-to-add'` on `number-bond-core.js`; default `make-ten` (K.OA.A.4) path byte-identical + re-verified 11/11** (declared core-touch). New `_renderAddMode`/`_paintAddMode`: "A + B = ?" strip + bond decomposing B so one part makes ten with A; `isCorrect` mode-aware (`filled===10−first`); in-stage = numerals/symbols only (language-neutral). **LESSON: the shell reads `tool.strings.title` at MOUNT (before init) — a 2nd activity needing a different in-deck title swaps `strings.title/instruction` to the variant SYNCHRONOUSLY at wrapper module-eval (`?activity=` detect), since clock's two activities accepted a SHARED generic title but "Make 10" can't fit an add activity.** 8 distinct cross-ten rounds + nextTask reshuffle; verify gate extended with a measured make-ten branch (16 rounds pass); local-test covers both activities 11/11; preview webp regenerated (content-rich "9+8=?" bond). html ?v=2, wrapper v8.76. [NSR-FLAG] sv/da/no/fi. **Pre-existing variety-gate FAIL on `fractions.partition-thirds` (5<7) is unrelated backlog (§A.13.60), NOT touched.**


---

## DONE 2026-06-18 — Activities index redesign: faceted catalog + real-preview thumbnails (LIVE, commit 37d02b1a)
- [Activities index redesign](project_activities_index_redesign.md) — `/[locale]/activities` rebuilt into a professional Direction A faceted catalog (Subject/Grade/Skill filter rail + sort + chips + responsive paper cards), 11/11 localized, 0 CC-leak. **Card thumbnails = REAL rendered previews of each activity's play area** (per-engine glyphs can't distinguish activities sharing an engine). Previews generated locally by `scripts/generate-activity-previews.js` (puppeteer screenshots `.lcs-stage` + Sharp→webp; ten-frame/array/place-value seeded to a filled state), gitignored, served at `/mini-tools/previews/<id>.webp` from `/var/www/lcs-media/mini-tools/previews/` (public/mini-tools is a symlink → that dir satisfies BOTH the `<img>` AND the build's fs existence check). Card uses eager `<img>` (next/image lazy → blank below-fold panels). **DURABLE: regenerate + re-scp previews when an engine's visuals change or a new activity ships.** Used `/frontend-design` skill + 3 design-critic agents. LESSON: TaskStop doesn't kill the Windows dev-server process tree → zombie port binds; kill via PowerShell `Get-NetTCPConnection -LocalPort N | Stop-Process`.


---

## DONE 2026-06-18 — E18 OPENED: "Make 10" number-bond (K.OA.A.4) LIVE 11/11 + activities-index → grade-band/strand IA
- [Activities live inventory](project_activities_live_inventory.md) — opens E18 (number-bond, ~20). NEW `mini tools/number-bond-core.js` clean sibling (**0 protected-core lines**): part-part-whole bond SVG (whole 10 + two parts; one given), TAP empty part to add counter-dots + "−" to remove; answerType:'state'; `isCorrect = given + filled === whole` (single targeted answer = 10−given, clean discrete gate). 8 distinct rounds given∈1..9 + `nextTask` reshuffle. **Chose K.OA.A.4 over commission-default K.OA.A.3** — K.OA.A.3 already lives on E4 match-pairs ("Make the Number"); K.OA.A.4 (find-the-number-that-makes-10) was free + iconic + single-answer. Gate `verify-number-bond-core.js` (parts-sum-to-whole + discriminating grade, MEASURED) + `local-test-number-bond.js` (11 locales tap-fill + 280px). 11th engine in MANIFEST_FILES; wrapper v8.74→8.75; bond `?v=1`. K.OA.A.3 = fast-follow on same core. [NSR-FLAG] sv/da/no/fi. Commit `a7233783`. Post-deploy: routes 200, JSON-LD K.OA.A.4, framework localized (de Lehrplan/fi OPS 2014), 0 CC-leak non-EN, 12-entry hreflang. SEO audit 364/364.
- **Activities INDEX reorganized** (`frontend/app/[locale]/activities/page.tsx`): flat grid → **grade-band (K→1→2) → localized-strand → cards** IA. Grade heading from `seo.educational_level.*` (the detail-page chip SoT, 11/11) + strand heading via `localizeStrand` (MERGES §17.4.3 collapses); STRAND_ORDER (math domains first, then literacy); chip simplified to CCSS code; auto-slots a new activity purely from `alignment.{grade,strand}` (0 hand-edit). Verifier `scripts/local-test-activities-index.js` (grouping/order/no-orphan/localized/0-CC-leak/auto-slot, all 11). **LESSON: `grep -c` counts LINES not matches → on minified single-line HTML it always reads ~1; use `grep -oE … | wc -l` for occurrence counts in curl checks.**


---

## DONE 2026-06-18 — E10 #2: "Tell Time to the Nearest 5 Minutes" (2.MD.C.7) LIVE 11/11
- [Activities live inventory](project_activities_live_inventory.md) — 2nd E10 coordinate on the proven `clock-core` via an ADDITIVE `minuteStep` config (30=#1 / 5=#2). Only setupTask+_snapMinute+keyboard changed; _handAngles/_snapHour/isCorrect/render were already minute-general → small declared core-touch, **#1 re-verified 11/11**. Same SET (drag hands) mechanic, 8 five-min rounds, isCorrect discrete-exact. **5-min idiom native-owned per locale (harder than half-hour): toward-next-hour (de "Viertel vor 8"), half-relative (nl "tien voor half tien"=9:20, Nordic "fem i halv 1"), Romance subtractive (es "ocho menos cuarto"=7:45), fi case-correct (yli=nom/vaille=part).** a.m./p.m. DEFERRED (can't show on a 12h analog SET). Gate `verify-clock-core.js` branches minute-scope by alignment.code; `local-test-clock.js` runs both activities. Commit `e7d95741`; wrapper v8.74, clock `?v=2`. [NSR-FLAG] sv/da/no/fi. **LESSON: a "config extension" of an existing engine can still need a tiny core change — make it minimal+gated (a step flag) so the prior path is behavior-identical, declare it, re-verify #1.**


---

## DONE 2026-06-18 — E10 OPENED: "Set the Clock" (1.MD.B.3) NEW clock-core LIVE 11/11
- [Activities live inventory](project_activities_live_inventory.md) — opens E10 (clock, ~25). NEW `mini tools/clock-core.js` clean sibling (**0 protected-core lines**) — platform's FIRST RADIAL-DRAG verb (atan2 angle + snap; sort-bins Pointer-Events pattern). Drag the analog hands to a worded target time. Discrete-state setHour/setMinute, coupled render (30H+0.5M / 6M), digital readout, answerType:'state'. Build-gate `verify-clock-core.js` proves exact angles + discriminating grade. **HALF-HOUR IDIOM native-owned per locale: toward-next-hour de/nl/sv/da/no/fi (halb/half/halv/puoli 8 = 7:30) vs current-hour+half es/pt/fr/it (siete y media). LESSON: fi "puoli" is toward-next-hour like German (not like Romance) — split the agent guidance by convention.** CC caught + fixed sv/no clock-word doubling in the prompt frame (o'clock → bare digit). Commit `7c0fe2a4`; 10th engine in MANIFEST_FILES; wrapper v8.73. [NSR-FLAG] sv/da/no/fi. Fast-follow: 2.MD.C.7 to-5-min on same core.


---

## DONE 2026-06-18 — SHELL-POLISH: keypad K-2 tap targets ≥36px (authorized protected-core touch)
- [Shell keypad tap-target fix](feedback_shell_keypad_tap_target_fix.md) — the shared shell keypad (`answerType:'number'` surface: ten-frame how-many/write-numeral + array build-array) shrank to 18px@iframe-216 (page-280) because `@media(max-width:600px)` kept FIXED 6 cols. Fix = added `@media(max-width:360px){ .lcs-activity-keypad{ grid-template-columns: repeat(auto-fit, minmax(36px,1fr)) }}` → cols drop 6→5→4→3, keys ≥36px, no overflow, engages only in the failure band (wider unchanged). **LESSON: minmax MAX as a fixed length drives auto-fit's column count → use `1fr` not `var(--lcs-tap)` so the 36px MIN packs denser (3c×4r not 2c×6r).** Pre-deploy proof = standalone at the real iframe widths [216…640] (worst 37px, 0 overflow); + `lcs-shell.css?v=16→17` in 12 htmls. Commit `cc2235e4`. **OPEN FOLLOW-UP: audit ALL shell tap targets (chrome/chips/Check-Next/drawer) for ≥36px — separate work unit, not done** (CLAUDE.md §A.13.55).


---

## DONE 2026-06-18 — E13 OPENED: "Build the Array" (2.OA.C.4) NEW array-core LIVE 11/11
- [Activities live inventory](project_activities_live_inventory.md) — opens E13 (array/multiplication-readiness, ~35). NEW `mini tools/array-core.js` clean sibling (**0 protected-core lines**, cloned from fractions-core skeleton). BUILD-AN-ARRAY verb: tap-to-fill R×C grid (pointerdown + touch-action:manipulation, no drag) → repeated-addition strip (4+4+4=?) → shell keypad total (answerType:number, check=answer===rows×cols). R,C∈[2,5], 8 distinct rounds + nextTask reshuffle. Build-gate `verify-array-core.js` MEASURES total=R×C + equal-addends (drives the real core). 11-locale native authoring (digit-substitution-safe prompt template; per-locale array term Gitter/arreglo/matriz/quadrillage/schieramento/rooster/rutnät/gitter/rutenett/taulukko). Registered in activities.ts MANIFEST_FILES (9th engine; preflight 11/11). Commit `5976de72`; wrapper v8.72, core `?v=1`. [NSR-FLAG] sv/da/no/fi. Fast-follow E13 (~34): equal-groups, larger arrays, total-given-find-a-dim.


---

## DONE 2026-06-18 — E3 #2 "Spot the Shape" (1.G.A.1 defining vs non-defining) LIVE 11/11
- [Activities live inventory](project_activities_live_inventory.md) — 2nd E3 coordinate via ADDITIVE `mode:'defining-attributes'` on the SAME `sort-bins-core` (operator-ruled fork: extend, not sibling). Target-box vs "Not"-box; true tiles vary in seeded colour/size/orientation (non-defining attrs INERT to the placement check). **G1: sort-by-sides behavior-identical, 2.G.A.1 re-verified 11/11. G2: build gate asserts answer-key is side-count-derived + decoration-invariant.** Wrapper selects defining title/prompt synchronously from `?activity` (no lcs-shell touch). 11-locale native authoring (formal positive labels reused per naming-standard doctrine §A.13.61; full-phrase negatives, fi partitive "Ei kolmioita"). Commit `7c514747`; wrapper v8.71, core `?v=2`. **NEW build-failing gate `scripts/preflight-activity-routes.js` institutionalizes the MANIFEST_FILES 404 class (54b9a4dc)** — §21.5. Naming-standard label doctrine → [naming-standard-label-doctrine](feedback_naming_standard_label_doctrine.md) + §A.13.61.


---

## DONE 2026-06-18 — E3 sort/drag-to-bin engine + first activity "Sort Shapes by Sides" (2.G.A.1) LIVE 11/11
- [Activities live inventory](project_activities_live_inventory.md) — NEW E3 engine (`mini tools/sort-bins-core.js`, clean sibling, **0 protected-core lines**): drag shape tiles into Triangles/Quadrilaterals/Pentagons/Hexagons bins by side-count. Pointer-Events touch+mouse drag, deferred-check review marks, code-drawn SVG (asset-free, culture-neutral), 8 distinct rounds + `nextTask` reshuffle. 11-locale native-expert strings. Commits `fccf285f` + `54b9a4dc`. Verified LIVE: 11 routes 200, real drag-play en/de/fi pass, variety 11/11, mobile 14/14, pages SEO 342/342, hreflang 12-chain, JSON-LD 2.G.A.1. **LESSON: a new activity engine MUST be registered in `frontend/lib/activities.ts MANIFEST_FILES` or the route 404s (missed on first deploy).** Gates: `scripts/verify-sort-bins-core.js` + `scripts/local-test-sort-bins.js`. Remaining E3 leverage ~40 (more sort/classify skills reuse the core).


---

## DONE 2026-06-18 — EN find-and-count landings un-collapsed (per-deck, two families)
- [find-and-count un-collapse](project_find_and_count_uncollapse.md) — 47 collapsed landings (one by-object deck per theme under a "Beginning Sounds" frame, all theme worksheets funnelled to it) → 377 per-deck landings (330 Beginning Sounds per-(theme,letter) + 47 Find&Count by-object). Decks were always correct; bug was landing collapse. Gate clean, 0 dup titles, 0 URL orphans, live-verified distinct interactives per worksheet. Reusable enum/gen(3D coprime cell-assign)/repoint machinery. **EN-ONLY bug** — other locales were already built per-deck with correct two-mode (hidden-object + letter-spotting) framing; no fan-out needed. Commits 88ad34c8+37ae2a5b.


---

## ACTIVE — SEO Rescue Program (multi-part, plan-mode-per-part, started 2026-06-14)
- [SEO rescue program](project_seo_rescue_program.md) — GSC ground-truth shows the real traffic came from worksheet-MAKER tool pages that the pivot 410'd; K-3 catalog (~30K pages) is thin-content-suppressed. Operator ruled: resurrect maker tools as SEO channel (amend §3.3), quality-over-quantity prune OK, low-competition locales first (de/nl/sv/it), 301-recover deleted-page equity. **SoT plan: `~/.claude/plans/the-seo-of-the-mossy-hippo.md`.** Roadmap: P1 resurrect maker tools+301 (**COMPLETE — 363 maker landings live: all 33 generators × 11 locales, 2026-06-14**); P2 catalog thin-content consolidation; P3 index hygiene+identity; P4 locale keyword alignment; P5 authority/off-page. Each Part = its own plan-mode session.


---

## NEW VERTICALS — Science DONE+LIVE (231 decks); Literacy NEXT (resume in fresh session)
- [Science+Literacy verticals](project_science_literacy_verticals.md) — Fable commission: 2 new K-2 worksheet-gen verticals. **SCIENCE design-once = COMPLETE+LIVE** (21 types × 11 locales = 231 decks; commits fbfec84e+dc5b03ed; 3 new factories, cores untouched; topic hubs live). **LITERACY native-per-language = NOT STARTED** — operator resumes vertical 2 in a new session. Gate-1 LOCKED (printable; distinct-query-face dedupe; full EN + commission EN phonics pool). **Resume brief: `docs/science-literacy-build-tracker.md` `▶▶ LITERACY — START HERE ◀◀` + plan `~/.claude/plans/two-new-worksheet-verticals-polymorphic-axolotl.md`.** Science build = the literacy blueprint (same 9-step pattern). Key gotchas: grade-prefixed ids (age from id prefix), taxonomy family-key registration required, themeless decks need skill-sentences ×11 (120-char desc floor), data/ gitignored→`git add -f`, scoped publish (publish-bulk→og --slugs-file --locales=all11→hreflang→deploy.sh hubs).


---

## DONE — Worksheet-gen program: ALL 11 LOCALES LIVE — 11 × 313 = 3,443 printable decks (2026-06-13)
- [Worksheet-gen program](project_worksheet_gen_program.md) — 200 types built+fanned; emission machinery live; wave-001 313 EN + all 10 non-EN waves (de/es/nl/fr/it/pt/sv/da/no/fi, sv/da/no/fi [NSR-FLAG]) published+audited clean (313/locale passAll), full 11-locale hreflang families. Per-locale cycle = 3-agent native ensemble → `i18n/apply-locale.js` → lint+all-themes-sweep → wave-NNN.json (seedEpoch 1) → publish-wave. Gotchas (per-theme desc-band tuning; fr "multiplication" lexicon exception; pt-BR hreflang grep) + pre-existing legacy-catalog defects surfaced (da/no LOCALE_RESIDUE, de/es/nl/sv CANONICAL_NO_TRAILING_SLASH — each needs own retrofit ruling) in memory file + tracker plans/commission-to-fable-modular-aurora.md. **+169 EN expansion 2026-06-13** (commits 78b0a387+9459c2bd): time 76 (8 clock categories incl. 4 NEW types G2-250 quarter / G3-354 minute / G3-355 12-24h / G3-356 add-subtract) + fractions 50 + geometry 20 + measurement 23. Introduced a **variant axis** (`variantsPerType`/`variants` in wave plans → `-vN` deckId + "Set N" title disambiguator; default 1, backward-compatible) — THE lever for N distinct worksheets of one themeless type. **GOTCHA: wave-001 published every type at d2 only → any new (type,d2,variant1) collides; avoid via difficulties [1,3] or variant≥2** (dry-run reads ok=/collisions= before --confirm). **Visual-defect fixes (commits d74152b6+8e71c8c2):** symmetry blank-on-poor-theme (vehicles sym=0 → throw+retry), find-the-whole bar size, cube-towers all-visible walls (w=1), non-empty-body QA lint, + a publish-cli bulk.js UPDATE self-collision fix (deployed to Hetzner) enabling in-place deck updates via `publish-wave --updates-manifest`. Remediated EN (4 decks re-published in place) + unpublished 11 blank g2248-vehicles decks all-locales. **Round 2 (commits 4b5ca6b0+7854a793):** solid-counts used full 5-solid pool (cone excluded — frustum art); whole-mode fixed-cell bars + 3 cards (clipping fix); re-published 63 decks all-11-locales in place. **PERF GOTCHA: `publish-wave --locales=<all 11>` runs `regenerate-og-images` catalog-wide (~20k decks × Sharp ~300ms = ~50 MIN); hreflang/audit are catalog-wide too.** For small in-place UPDATE batches: run publish-bulk (the UPDATE) then ONLY the needed post-steps scoped — `populate-and-inject-hreflang` is fast (~16s, restores the hreflang the UPDATE strips), and `regenerate-og-images --slugs-file=<newline slugs>` (NEW flag, commit 7854a793) regenerates only the batch (~10s) instead of the whole catalog. Do NOT run full publish-wave at all-11-locale scope for a handful of deck updates. **Cube clip fix (commit c3648876):** `unit-cubes.js` dropped a bad `(l+w)*SIN*u - u` oy offset that pushed the stack below the SVG H, slicing the front-bottom cube row (worse as width grows); only volume-cubes uses it. **Clock chip fix (commit 37cf5a09):** convert-24h cards cols:1 + nowrap (committed late; matches published). **Scoped in-place UPDATE recipe (proven):** generate ZIPs → build `{zip:slug}` manifest via `deriveSeedFromManifest`+`slugify` → `index.js publish-bulk <dir> --confirm --updates-manifest <json>` → `populate-and-inject-hreflang --confirm --locales=<all11>` (16s, UPDATE strips hreflang) → `regenerate-og-images --locales=<all11> --slugs-file=<slugs>` (seconds). **Image cache: thumbnail.png/og-image.png served `Cache-Control max-age=14400` (4h) → after an in-place correction, returning viewers see stale thumbnails up to 4h (browser cache; origin+CDN are fresh — hard-refresh to confirm).** All EN expansion + all visual-defect fixes are LIVE all-11-locales. **🏁 NON-EN FAN-OUT OF THE EXPANSION DONE (2026-06-13 "witty-squid"): all 10 non-EN locales LIVE = 1,690 new decks (169/locale; de…fi).** Replicated the 4 EN expansion waves verbatim (seedEpoch 1) → the 4 new time types now have full 11-locale hreflang families. Per-locale = 9 authored strings only (4 time title+instr + seo.words.set) via 3-agent ensemble + plan-mode approval; everything else pre-authored. DURABLE GOTCHA: K-038 "heavy/light" title collides with the easy/hard mode word in **de/sv/da/no** (schwer/lätt/let/lett = light AND easy) → deduped d1/d3 titles → TITLE_NON_UNIQUE; fixed by renaming K-038 title (Was wiegt mehr? / Vad väger mest? / …). Used SCOPED publish recipe (publish-bulk → hreflang all-11 → og --slugs-file → scoped on-disk audit) NOT full publish-wave. Per-locale commits 780f28ce…84950185. Full doctrine in [project_worksheet_gen_program.md]. Other deferred: landing fans for 22 family keys (§22.5); catalog-axes.ts homepage inclusion.


---

## STANDING DOCTRINE — SEO is automatic+implied on every publish (CLAUDE.md §21)
- "Publish decks/worksheets/activities/tools" implies FULL SEO treatment — NEVER ask about indexability/alt-text/JSON-LD/hreflang/per-locale metadescriptions. Decks: `publish-wave.js`. No per-item linguist authoring (template-derived); native ensemble only for new TYPE/LOCALE. See [feedback_content_publishing_seo_standard.md](feedback_content_publishing_seo_standard.md).
- NON-EN deck waves are now ONE-COMMAND: `publish-wave.js` folds preband (desc-banding + title-disambig) + alt-text + 11-locale hreflang + a `--themeless-ok`/`--fail-on-rewrite` pre-flight (commit b5da0965, 2026-05-30). Just `publish-wave.js <folder> --locales=<loc> --confirm` (flatten subfolders first). Details + manual-step reference in [feedback_long_locale_wave_publish_gaps.md](feedback_long_locale_wave_publish_gaps.md). fr wave (2,986 decks) published 2026-05-30.


---

## STANDING DOCTRINE — Activity variety + shuffle (CLAUDE.md §A.13.60; 5th workflow rule)
- Every activity needs **≥7 ORIGINAL distinct exercises + a post-pass ORDER-ONLY reshuffle** (child never gets the same sequence twice). Default-on done-line gate — OUT OF SPEC without it, even if the commission never mentions variety. Canonical impl = wrapper `nextTask` per-pass reshuffle (NOT the protected `lcs-shell.js`; ref `mini tools/fractions-activity.js`). Gate = `node scripts/audit-activity-variety.js`. Both fraction partition activities (1.G.A.3 8-pool / 2.G.A.3 7-pool) are the first conformers (2026-06-17); 39 other activities are backlog (shell-loop, some <7) — convert when next touched. See [feedback_activity_variety_shuffle_rule.md](feedback_activity_variety_shuffle_rule.md).


---

## STANDING DOCTRINE — Activity mobile-QA gate (CLAUDE.md §A.13.55)
- Every new/changed activity MUST pass `node scripts/audit-activity-mobile.js` (all phone widths 280-768, empty+filled, no clip/off-screen/overflow) BEFORE ship; eyeball the screenshots too. Source = `mini tools/` (git); server sync = `cp "mini tools"/* /var/www/lcs-media/mini-tools/` then `deploy.sh`. See [feedback_activity_mobile_qa_standard.md](feedback_activity_mobile_qa_standard.md). 2026-06-02 sweep fixed 27 fails+empty-band → 376/376 (commit 64f21f7e).


---

## 🏁 SEO/Landing-Page Program — COMPLETE: ALL 11 LOCALES DONE (en/de/es/sv/nl/da/no/fr/it/pt/fi = all 28 landable types + cryptogram unlandable). fi closed 2026-06-17 (fan 2540). The multi-commission landing rollout is finished.
- **`fi` is CATALOG-READY (NOT blocked — that framing was STALE): 3,640 published fi decks, all 28 landing types** (operator confirmed the fi deck wave shipped; DB-verified 2026-06-16). **fi is the HARDEST locale — Finnish is agglutinative + case-heavy + NO gender/articles → a genuinely NEW render engine** (can't clone Romance os/as or Nordic definiteness).
- **`fi` WAVE A LIVE (2026-06-16, `9faa3bd9`): readiness ×7 = 493 landings (esikoulu) — the FIRST live Finnish landings; the new fi-render engine PROVEN in production.** grid-match/shadow-match/missing-pieces/find-objects/bingo/picture-path/odd-one-out. Native Finnish 7-agent ensemble; **grid-match = first-config gate → the 5-placeholder/4-case set is SUFFICIENT (no engine extension; counting types in Wave B/E will test {N_PART_SG} "montako eläintä?").** Gate over 493 with the `\p{L}` fi tokenizer: all-pairs 0.686, 0F/0dup/0lint. Live titles "Ruudukkoyhdistys – Asusteet | tulostettavia tehtäviä", OPS 2014 + esikoulu chips, hreflang fi. **2 DURABLE fi-machinery fixes: (1) `gen-fi-readiness` slotTokens MUST include `d.genPl` (a `{N_GEN}`-only p1 "eläinten kuvat/varjot" legitimately names the theme but fails the theme-noun lint without genPl). (2) rekey per-mode tokens for mode-split readiness types (else both modes of a theme collide → ordinals).** [NSR-FLAG][fi] minor stylistic (odd-one-out "suurin osa…on eläimet" nom-vs-partitive).
- **🏁 `fi` CLOSE — THE 11-LOCALE LANDING ROLLOUT IS COMPLETE (2026-06-17). fi = all 28 landable types (2540 landings) + cryptogram unlandable.** Foundation + Waves A/A2/B/C/D/E; the FINAL locale. **All 11 locales DONE: en/de/es/sv/nl/da/no/fr/it/pt/fi.** The new `fi-render.js` (pure-substitution, vowel-harmony assertThemeTable, 5-placeholder/4-case + render2 dual-theme) proved sufficient across every wave without computing Finnish morphology. SoT plan `go-luminous-moon.md`; detail [project_8locale_landing_rollout.md].
- **`fi` WAVE E LIVE (2026-06-17, machinery `1f7f68f5` + wave `0c2655a4`): picture-sort 125 + chart-count 40 = 165 → fi fan 2540; cryptogram unlandable.** picture-sort (esikoulu readiness, Luokittelu): NEW `fi-render.render2` dual-theme pure-substitution ({A_*}/{B_*} from existing fi-themes fields), gen-fi-picturesort 16×12=192 coprime cells; count-fence (sort-not-count, **Unicode-safe negation-window — the JS `\b`-after-Finnish-`ä` bug fixed**) + sibling-fence 0 leak; within-class 0.642. **chart-count = OPTION B theme-level Quiz (operator-resolved):** Finnish partitive is irregular across 266 category nouns (Susi→sutta) + computing it violates the pure-substitution doctrine → category nouns NOMINATIVE-only + generic "kuvaa" + bar-refs (answers = real bar heights max/min/sum, honest-count assertion). 1-luokka K.MD.B.3 Tilastot. **KEY FIX: initial 4 p1/2 p2/4 p3 + periodic i%len reused IDENTICAL templates (Option B dropped sv's per-category count-weave) → 0.880/180 FAIL; expanded to 8/6/6 + coprime distinct-template assignment → 0.532/0 FAIL.** §20.5 trailing-num strip on display names. cryptogram PROVEN-UNLANDABLE (97 decks 0 subjectTags, stub). Gate over all 2540: all §4.B lint pass, ALL-PAIRS max 0.716 (unchanged Wave-C ceiling), 0 FAIL/0 dup. Repoint 167/0. Live-verify GREEN (picture-sort render2 both -vs- themes; chart-count K.MD.B.3 theme-level Quiz). **DURABLE LESSON: a theme-agnostic dedicated-gen with few templates + periodic i%len assignment reuses identical templates → severe FAILs; needs cells>decks + coprime bijection (the picture-sort/§22.1 pattern), AND when an honest-count weave is dropped for a morphology wall the lost per-deck variation must be replaced by more+diverse templates.**
- **`fi` WAVE D LIVE (2026-06-17, `f76ce299`): literacy ×7 (MIXED-band) = 485 → fi fan 2375.** alphabet-train 29 / matching 86 (letter+name) / prepositions 95 (mc+fillin) / word-guess 93 / word-scramble 92 / wordsearch 47 / crossword 43. **fi crossword + matching-name are TAGGED (unlike pt → NO thumbnail-recovery; all 7 build by theme).** MIXED-band: readiness esikoulu (alphabet-train, matching-letter, prepositions-mc) / 1-luokka std (word-guess+word-scramble L.K.2.d easy / L.1.2.d normal decoupled-uniform, wordsearch RF.K.3.c, prepositions-fillin L.K.1.e, matching-name RF.K.3.c) / **2-luokka std crossword L.2.2.d — FIRST fi 2-luokka literacy.** matching + prepositions mixed-band via per-mode cfg.level/standard/strand/**P3** functions (gen already supported; per-mode P3 stops letter-frame leaking into name, the pt Recovery-B lesson). Pattern-B titles (literacy not range-led; grade on chip+alignment). rekey per-mode quals → 0 ordinals. Fences airtight: FENCE-A word-guess MISSING vs word-scramble JUMBLED 0/0; FENCE-C matching-letter alkuäänne vs alphabet-train aakkosjärjestys 0/0; wordsearch FIND vs crossword WRITE 0/0. **LENGTH-RATIO held — word-guess/word-scramble within-mode ≤0.599** (substantial ~60-75w P2, no no-0.811 collapse; word-guess+word-scramble authored FIRST as the first-config gate). Gate over 2375: all §4.B lint pass, ALL-PAIRS 0.716 (unchanged Wave-C math-puzzle ceiling, 0 cross-type exceed), 0 dup. Repoint 486/0. Live-verify GREEN (matching-letter readiness no-align vs matching-name RF.K.3.c — per-mode band same type; crossword 2. luokka L.2.2.d). **NEXT: fi Wave E (dedicated gens picture-sort + chart-count + cryptogram prove-unlandable) → fi CLOSE (ends the 11-locale rollout).** [NSR-FLAG][fi].
- **`fi` WAVE C LIVE (2026-06-17, machinery `d2de09ea` + wave `b3bc2fc3`): arithmetic CARRIES ×5 = 634 → fi fan 1890 — the FIRST standard-bearing fi wave.** addition 162 / subtraction 161 / math-puzzle 120 / math-worksheet 95 / code-addition 96. **PIVOTAL RULING: NO esikoulu for symbolic arithmetic** (the sv §22.5 within-10-symbolic=grade-1 / förskola=no-standard ruling) → annotate-fi-bands fixed (band floor 1-luokka; CCSS code decoupled from band — K.OA.A.2 stays on within-10 → honest 0–10 range + ilman kymmenylitystä, chip reads "1. luokka"). addition/subtraction uniform 1-luokka; math-puzzle/-worksheet/code-addition band-split 1-/2-luokka. **3 machinery edits:** annotate-fi-bands esikoulu-fix + extend to all 5 types (find-addend K.OA.A.4 / find-subtrahend 1.OA.D.8 / direct K.OA.A.2≤10·1.OA.C.6≤20·2.NBT.B.5 21-100); gen-fi-readiness prefers `co.standard` over cfg.standard (the per-coord annotated standard encodes the range — level alone can't, since ≤10 and ≤20 both 1-luokka); rekey per-mode quals on addition/subtraction/math-worksheet → 0 ordinals. **Fences airtight:** addition↔subtraction operation-true (laske yhteen/yhteensä vs vähennä/jää, 0 leak); 3-laskutapa-faces math-puzzle REVEAL / code-addition KEY-given / math-worksheet HIDDEN-VALUE-no-key (0/0/0). code-addition = Pattern-B title (carries chip+alignment, NOT in NUMERIC_TYPES so no range). Gate over 1890: all §4.B lint pass, ALL-PAIRS 0.716 (within-class math-puzzle same-SKEL, 0 FAIL), 0 dup. Repoint 687/0. Live-verify GREEN (FIRST fi standard-bearing: educationalAlignment no-targetUrl targetName=CCSS + OPS 2014 + grade chip 1./2. luokka [first live use] + strand Luvut ja laskutoimitukset + range-led title + hreflang fi). **NEXT: fi Wave D (literacy, mixed-band) → E dedicated gens → fi CLOSE.** [NSR-FLAG][fi].
- **`fi` WAVE B LIVE (2026-06-17, `e7e56ff1`): math-reasoning readiness ×5 = 573 → fi fan 1256 (esikoulu).** pattern-train 240 (5 distinct mode-worlds AB/AAB/ABB/AABB/ABC — AAB doubles-FIRST vs ABB doubles-LAST load-bearing, cross-mode ≤0.667) · pattern-worksheet 47 (generic printable face, 0 AB-codes) · big-small 77 (findBig/orderAsc SIZE-fenced) · more-less 118 (check-cross/image-image/image-number QUANTITY-fenced; single-token "Enemmän vai vähemmän" → ordinal "(2)/(3)" dedup, the grammatically-clean choice for a question-phrase op) · find-and-count 91 (hidden count-POSITIVE / letter-spotting). **THE PARTITIVE PLACEHOLDERS PROVEN:** find-and-count first use of `{N_PART_SG}` "montako eläintä?" + `{N_PART_PL}` "laske eläimiä" — a pre-gen render smoke-test across all 50 themes confirmed the 5-placeholder/4-case set is SUFFICIENT (no engine extension); first-config-gate = author find-and-count FIRST then parallelize 4. **Fences airtight:** more-less↔big-small 0/0 SIZE/QUANTITY whole-word separation, 0 AB-codes. Gate over 1256: all §4.B lint pass, ALL-PAIRS 0.696, 0 FAIL/0 dup/0 lint. Repoint 574/0 missing. **NEXT: fi Wave C (arithmetic CARRIES — range-led titles, esikoulu/1.luokka/2.luokka bands via annotate-fi-bands, the FIRST standard-bearing fi wave) → D literacy → E dedicated gens (chart-count "montako X?" Quiz; cryptogram prove-unlandable) → fi CLOSE (ends the 11-locale rollout).** [NSR-FLAG][fi].
- **`fi` WAVE A2 LIVE (2026-06-16, `d51ab365` + title-fix `166ea229`): sudoku 96 + treasure-hunt 94 = 190 → fi fan 683 (esikoulu readiness, mode-true, CLEAN — no recovery).** sudoku (easy 48/medium 48, NO hard; strand `Looginen ajattelu`; token `Kuvasudoku` qual helppo/keskivaikea) + treasure-hunt (cardinal-arrows 47/compass 47; strand `Avaruudellinen hahmottaminen`; `Aarteenetsintä` qual nuolilla/kompassilla). Count-fence (no laske/montako) + FENCE vs picture-path (no labyrintti) 0 leaks; pt hardcoded-example-object bug avoided (generic kuva). Gate over 683: sudoku ≤0.639, treasure-hunt ≤0.641, all-pairs 0.686, 0F/0dup/0lint. Repoint 191/0 missing. **DURABLE LESSON: any post-rekey re-gen WIPES the title field → `<title>` falls back to the H1 form; the fold-fix re-gen of sudoku did exactly this → re-rekey before commit (`166ea229` restored the 96 RANGE-LED Pattern-B titles).** Also: 18 sudoku §4.B lint fails = 2 medium SKEL intros named the difficulty but no theme object → injected {N_GEN}/{N_PL}. **NEXT: fi Wave B (math-reasoning readiness ×5: pattern-train, pattern-worksheet, big-small, more-less, find-and-count) → C arithmetic CARRIES (range-led, bands via annotate-fi-bands, first standard-bearing) → D literacy → E dedicated gens (chart-count "montako X?" Quiz tests {N_PART_SG}; cryptogram prove-unlandable [97 fi decks]) → fi CLOSE (ends the 11-locale rollout). Each wave = plan-mode + 3-agent native Finnish ensemble, [NSR-FLAG][fi].**
- **`fi` FOUNDATION LIVE (2026-06-16, `a08e7e90`, deployed clean):** NEW `fi-render.js` = pure-substitution engine, 5 placeholders / 4 cases ({H1} nom-pl display, {N_PL} nom-pl, {N_PART_SG} partitive-sg "montako eläintä", {N_PART_PL} partitive-pl "laske eläimiä", {N_GEN} genitive-pl "eläinten kuvat") + a **vowel-harmony assertThemeTable** (the Finnish *djurna analog — catches omenat→*omeniä). `fi-themes.js` 50 themes native multi-case, assertThemeTable CLEAN. Ledger (OPS 2014; **RANGE-LED titles** operator-confirmed by google.fi harvest — "Yhteenlaskutehtäviä 0–10 ilman kymmenylitystä – Tema | tulostettava PDF", CLOSED compounds; **AGE-anchored bands esikoulu/1.luokka/2.luokka** — Finland starts latest, 1.luokka=age 7=EN-Grade-2's age, 3.luokka above-ceiling; math strand collapse → Luvut ja laskutoimitukset). Machinery gen/rekey[range-led]/enum/annotate-fi cloned; **gate.js tokenizer widened to `\p{L}` fi-conditionally** (default [a-z0-9] strips ä/ö → under-reports fi; other 10 locales byte-unchanged). Shared maps wired (FRAMEWORK/UI_STRINGS/LEVELS/LEVEL_CHIP/FILES.fi + strand-names calque fixes; hreflang already fi). Verified: enum smoke-test GREEN (addition 162/grid-match 46/sudoku 96/alphabet-train 29), N-band regression clean, fi route 404 (no landings). **NEXT: fi Waves A-E + recovery (plan-mode + native Finnish ensemble per wave, [NSR-FLAG][fi]) — fi's close ENDS the 11-locale rollout.** Ledger SoT = `.claude/plans/commission-to-cc-swirling-bengio-agent-a55bd192a78057a67.md`; active plan = go-luminous-moon.md (fi section). fi-themes [NSR-FLAG] edge partitives (simpukoita/kurpitsoita).
- **🏁 `pt` LANDING FAN COMPLETE (2026-06-16): all 28 types landed (cryptogram unlandable), pt fan 2653.** Waves A-E (2311) + Recovery-A (sudoku/treasure-hunt, 210) + **Recovery-B (`09f2ed6a`): chart-count 39 + crossword 46 + matching-name 47 = 132.** **Recovery-B lessons: (1) chart-count theme was PROGRAMMATIC — manifest `exercises[0].chartImages[].theme`+`counts` survive the v32 4th_of_july tag-bug → no thumbnails (only crossword+matching-name needed them; crossword thumbnails PRINT the theme name). (2) crossword = pt's FIRST 2º-ano literacy (L.2.2.d). (3) matching-name = a 2nd `name` mode added to pt-readiness-matching (mode-fn level/standard/strand + PER-MODE P3 — the shared letter-P3 "som inicial" leaked into name). (4) gen-pt-chartcount = clone gen-it-chartcount (OVERRIDE_PT icon/plural fixes, os/as gender-articles, Quantos/Quantas honest-count Quiz+assertion, CULL non-countable + same-plural).** Rollout NEXT = `fi` (catalog-blocked → a deck-publish-wave decision upstream of the landing program). The 11-locale landing rollout is effectively DONE pending the fi deck catalog.
- **`pt` Recovery-A LIVE (2026-06-16, `ca561c56`): sudoku 122 + treasure-hunt 88 = 210 readiness landings → pt fan 2521, via THUMBNAIL-RECOVERY.** 2 broken-tag types (sudoku=themeless catalog; treasure-hunt=v32 `4th_of_july` mis-tag) recovered: 5 batched agents Read all 250 `thumbnail.png` (Read tool sees images) → `{slug→theme}` maps (0 invalid, 48 themes; spot-checks confirmed; one agent caught an image-cache scramble + re-read as JPEG) → synthesized coords (group by mode+theme) → native configs pt-readiness-sudoku (3 modes easy/medium/hard mode-true, Raciocínio lógico; fixed 3 hardcoded theme-wrong example objects) + pt-readiness-treasure-hunt (cardinal-arrows/compass, Orientação espacial, FENCE vs picture-path labirinto). Gate 0F/0dup/0lint over 2521. **treasure-hunt deck SLUGS keep the bogus `4-de-julho` (cosmetic); landing content = the REAL recovered theme.** Operator ruled SPLIT → **Recovery-B next** (crossword 2º-ano L.2.2.d + matching-name 1º-ano RF.K.3.c + chart-count 1º-ano K.MD.B.3 new gen-pt-chartcount). DECK-ASSET PATHS: `/var/www/lcs-media/decks/pt/<slug>/thumbnail.png` (symlink→-vN); recovery flow = es/en treasure-hunt precedent (no script, agent Read → map → synth coords).
- **`pt` Wave E LIVE (2026-06-16, `9b2ecb67`): picture-sort 122 (readiness) → pt fan 2311 + cryptogram unlandable (stub).** New `gen-pt-picturesort.js` (clone of gen-IT; render2 dual-theme 16×12=192>122, pt aos/às+dos/das, strand Agrupar e classificar) + native `pt-picturesort-frames.js` (16 SKEL+12 P2). Title SPLIT: SERP `Separar e classificar`/H1 `Separe as imagens`. COUNT-FENCE+SIBLING-FENCE 0 leaks; gate 0F/0dup/0lint over 2311. **chart-count DEFERRED (same v32 `4th_of_july` tag-bug as treasure-hunt, 48/49 decks) → 5-surface thumbnail-recovery backlog (sudoku/treasure-hunt/crossword/matching-name/chart-count; counts survive in manifest, only theme needs thumbnails).** Wave D (`898e5915`): literacy ×6, 404; prepositions MIXED via `cfg.level` FUNCTION; **fold-fix — §4.B lint never grepped in A/B/C → 263 LIVE landings carried gate-fails (h1 "undefined"; "no theme-noun in P1"); LESSON: grep BOTH lint AND similarity, `gate.js|tail` hides the lint.** SoT `go-luminous-moon.md`; detail [project_8locale_landing_rollout.md](project_8locale_landing_rollout.md).
- **[INFRA INCIDENT 2026-06-16 RESOLVED] disk-full → Postgres crash → sign-in down (surfaced by the Wave-E deploy smoke-test; NOT the landing commit).** `/dev/md2` hit 100%: `backup-decks.sh` kept 3 weekly deck tarballs (~78G ea = 234G) + pruned AFTER tar → the Sunday run briefly held 4 (~312G) + 141G live decks → overflowed the 436G volume; `set -e` left 4 tarballs + 0 free. Freed ~234G: 4 deck tarballs (178G) + **flashcards+materials (13G = 2026-05-17 teaching-packages-nuke ORPHANS — domain 410 but nginx still served the raw PDFs; the nuke missed the media)** + /root staging (15G) + .publish-cli-staging (18G) + pre-deploy pile → disk 100%→44%. Patched `backup-decks.sh` prune-before-create + free-space guard + keep-2 (`a5c81792` + `cd95d7aa` exec-bit). **DOCTRINE: deploy smoke-fail with DB-health 500 → check `df -h /` FIRST (full disk crashes Postgres; the app commit is usually innocent). Off-host backup still owed (Scaling Arc 3 Q3).**
- **🏁 `it` (Italian, 10th locale) — FAN COMPLETE (CLOSED 2026-06-16): 28 types / 2288 landings + cryptogram unlandable** (infanzia 1285 · classe-prima 808 · classe-seconda 195; standard-bearing 784 · readiness 1504). **A FINISH, not a from-scratch build:** `it` was the FIRST Romance reference fan (built earlier, `dc48c108`→`0636f7be`) BEFORE the keyword-rekey discipline → at kickoff it was ~85% LIVE but with inside-out H1-titles + 3 missing types + a LEVEL_CHIP bug + hidden cannibalization. Finished in 3 parts (SoT `go-luminous-moon.md`): **P1 `3ac0031f`** (google.it harvest → `rekey-it-titles.js` RANGE-LED `Schede di [op] entro il N [senza cambio] – Tema | da stampare PDF`, readiness Pattern-B; `senza cambio` honest-fit; tokens Tombola/Crucipuzzle/Anagrammi/Labirinti/Cruciverba; 2,060 re-keyed 0 dup; LEVEL_CHIP fix; + **discovered & fixed 102 pre-existing pattern-train cross-mode FAILs** via boilerplate→mode-true rewrite 0.871→0.400) · **P2 `cbdfecae`+`6256d337`** (+228: treasure-hunt 94 infanzia/Caccia al tesoro cardinal↔compass mode-true · bingo 37 infanzia/Tombola illustrata · math-worksheet 97 standard-bearing per-coord band 70 classe-prima 1.OA.C.6 / 27 classe-seconda 2.NBT.B.5, HIDDEN-VALUE-no-key fence; machinery enum-it-coords + annotate-it-bands) · **P3 close** (gate 0.777/0 FAIL/0 dup over 2288; cryptogram 100 decks 0 subjectTags UNLANDABLE; live-verify GREEN both bands). **TWO DURABLE LESSONS (apply to pt): (1) run the full similarity gate at any locale FINISH not just build (early fans hide cannibalization); (2) fr↔it enum slug-field-name differs — `enum-fr-coords` emits `canonical` but `gen-it-readiness` reads `canonicalDeckSlug`; the fr-cloned enum-it wrote `canonical` → 228 landings `slug:undefined` → 404, caught at verify.** Framework Indicazioni Nazionali; 3-band infanzia/classe-prima/classe-seconda (terza OUT, NO offset); Romance it-render (i/gli/le). **NEXT LOCALE = `pt`** (clones it/fr Romance machinery — match the `canonicalDeckSlug` field at the enum clone). Detail [project_8locale_landing_rollout.md](project_8locale_landing_rollout.md).
- **🏁 `fr` (French, 9th locale) — FAN COMPLETE (CLOSED 2026-06-15): 28 types / 2510 landings + cryptogram proven-unlandable** (maternelle 1470 · cp 934 · ce1 106; standard-bearing 1040 · readiness 1470). Built in DIGESTIBLE PARTS (plan-mode + native ensemble each). Part 1 Foundation DONE (`65d44ffd`): clones the **Italian (Romance) + da machinery** with the native fr ledger-lock. **3-band collapse maternelle/CP/CE1 (NO offset — France CP=age6=first formal year), CE2 OUT.** Framework chip **`Éducation Nationale`** (operator) / JSON-LD "Programmes de l'Éducation Nationale". Title frame (operator+SEO-locked): grade-led `[Token] [CP/CE1] [sans retenue] – [Thème] | à imprimer PDF gratuit`, readiness `[Token] – [Thème] | fiches à imprimer PDF gratuit`; head=**Fiches**; protected cluster `à imprimer`>`PDF`>`Fiches`>`gratuit`; **`sans retenue` ATTESTED** (honest-fit gated). chart-count token=**`Graphiques`** (operator). Native tokens: Loto(NOT bingo)/Mots mêlés(NOT mots cachés)/Algorithmes/Labyrinthe/L'intrus. **fr-render = pure substitution, French contractions (les/des/aux+plIndef, de/d' by vowelInit) ALL DERIVED from plIndef+vowelInit (none stored); assertThemeTable FAIL-halts on a vowelInit/initial-letter mismatch.** fr-themes 50 themes (gen m/f + vowelInit + plIndef + h1Display, native; œufs/objets/outils/arbres vowel-initial). Strands: Nombres et calculs / Organisation et gestion de données / Premiers outils mathématiques / Observation et logique / Trier et classer / Lecture et compréhension de l'écrit. **Bands (LEVELS): maternelle{3-6}/cp{6-7}/ce1{7-8}; collision-free keys.** Enum smoke GREEN. Dedicated gens (picture-sort/chart-count) DEFERRED to Wave E. **Wave A COMPLETE (`8a611edf`): 9 visual/logic readiness types, 686 landings, maternelle/no-standard.** Native ensemble; operator forks odd-one-out=`L'intrus`(H1 "Trouve l'intrus") / find-odd=`Trouve les différences`(plural). **FENCE-A** (compléter split: shadow=l'image/la moitié vs missing-pieces=le puzzle/la pièce) + **FENCE-B** (find-odd=différence perceptual vs odd-one-out=intrus categorical) + i-spy count-fence + path↔treasure + calque(loto-not-bingo) — rendered-body greps 0 leaks. Gate 0F/0dup over 686, max 0.725; treasure-hunt cross-mode sharpened 0.870→0.725 (boilerplate→mode-true: cardinal flèches vs compass boussole). Repoint 804 deck.html. **Wave B COMPLETE (`1de6e4a0`): 5 math-reasoning readiness types, 528 landings (fr fan total 1214), maternelle/strand `Premiers outils mathématiques`.** Operator fork: more-less title=`Comparer les quantités`(H1 "Plus ou moins") / find-and-count=readiness. **"Algorithme"=official France-maternelle term** (not motif/pattern); pattern-train 5-mode AAB↔ABB mirror risk AVOIDED first-try via native doubler-anchor recipes (aab 0.635/abb 0.645, no cross-mode FAIL). more-less(QUANTITY)↔big-small(SIZE) fence held (more-less SIZE-leak 0, big-small QUANTITY-leak 0). big-small was findBig-only in the fr catalog. Gate 0F/0dup over 1214, max 0.725. **Wave C COMPLETE (`2ec57254`+`7cf3f102`): 5 arithmetic CARRIES types, 631 landings (fr fan total 1845), FIRST fr standard-bearing wave (CP/CE1, educationalAlignment + Éducation Nationale framework + grade-led + strand `Nombres et calculs`).** Operator token-swap (removes calcul/maths cannibalization): math-puzzle=`Calcul mystère`, math-worksheet=`Fiches de calcul`, code-addition=`Additions codées`. Per-coord band re-grade via annotate-fr-bands.js (clone): addition/subtraction uniform CP (all ≤10); math-puzzle/math-worksheet/code-addition band-split CP/CE1. addition↔subtraction operation-true fence HELD first-try (combien en tout vs combien il reste); sans retenue BOTH ops (France calls borrow "retenue" too — `sans emprunt`→0); 3-calcul-faces fence (math-puzzle REVEAL / code-addition KEY-given / math-worksheet HIDDEN-VALUE-no-key, 0 leaks). math-puzzle add↔sub MODE collide sharpened 0.860→0.728. Gate 0F/0dup over 1845. **Wave D COMPLETE (`bc97b907`): 7 literacy types, 486 landings (fr fan total 2331), MIXED-band (readiness maternelle + standard-bearing CP via mode-function cfg.level/standard/strand).** Operator forks: word-guess+word-scramble=both CP (no CE1 split — diverges from no); prepositions=split (mc maternelle/fillin CP). Bands: alphabet-train + matching-letter + prepositions-mc = maternelle readiness; matching-name(RF.K.3.c) + word-guess/scramble(L.K.2.d/L.1.2.d) + wordsearch(RF.K.3.c) + crossword(L.1.2.d) + prepositions-fillin(L.K.1.e) = CP. Fence A (word-guess MISSING vs word-scramble JUMBLED), Fence B (wordsearch FIND vs crossword WRITE; Mots mêlés>Mots cachés, never mots fléchés), Fence C (son initial vs ordre alphabétique) — 0 leaks. **LENGTH-RATIO applied PROACTIVELY: word-guess P2 86-94w/word-scramble P2 104-119w → within-mode ≤0.58 (the no word-scramble 0.811 collapse avoided first-try).** Gate 0F/0dup over 2331, max 0.757. **Wave E COMPLETE (`9be1ee87`): dedicated gens + close. picture-sort 138** (maternelle readiness, `gen-fr-picturesort.js`+`fr-picturesort-frames.js`, render2 dual-theme 16×12>138 coprime; operator title SPLIT live = SERP `Tri des images`/H1 `Trier les images`; count-fence ADDED the **`dénombrer`** paradigm [the #1 France leak, absent from the original ledger fence] + sibling-fence not-`l'intrus`; PAIR_DISAMBIG for shared `animaux`×4) + **chart-count 41** (CP standard-bearing K.MD.B.3, `gen-fr-chartcount.js`+`fr-chartcount-frames.js`; Quiz `Combien de/d' {pluriel} ?` MANDATORY elision [d' vowel/mute-h, `de` aspirate-h hibou/haricot via ASPIRATE_H set — 39 d'/0 miss]; 8 plural-trap OVERRIDE incl. -al→-aux médicaux/postaux; **substring-collision guard** drops ambiguous generics chapeaux⊂chapeaux-de-fête/tortues⊂tortues-terrestres; honest-count assertion clean) + **cryptogram PROVEN-UNLANDABLE** (101 fr decks, 0 subjectTags, generic `cryptogramme-XXXX`; `cryptogramme`=credit-card register — same as es/en/de/sv/da/no, DON'T-LAND). Gate all-pairs 0.757, **0 FAIL / 0 dup over 2510**; repoint 179 deck.html/1432 repl/0 missing; live-verify GREEN both bands + deck.html canonical→landing + hreflang + sitemap shard-4 (498 picture-sort+368 chart-count refs). **Reusable Wave-E doctrine: substring-collision guard for chart-count category nouns where one plural ⊂ another (ambiguous Quiz); add the `dénombrer` paradigm to any French sort-not-count fence; the it/no Wave-E gens clone cleanly to Romance.** **`it` ✅ DONE (2026-06-16, see the it line above) → NEXT LOCALE = `pt`.** SoT plan `~/.claude/plans/go-luminous-moon.md`; detail [project_8locale_landing_rollout.md](project_8locale_landing_rollout.md).
- **`no` (Norwegian, 8th locale) — operator wants it built in DIGESTIBLE PARTS; before EACH part the operator switches to plan mode + a fresh native-expert bokmål ensemble is employed.** Part 1 Foundation (`c722d002`) + **Waves A (`e2e07a77`: 9 visual/logic readiness, 691) + B (`880bcfe8`: 5 math-reasoning readiness, 576) + C (`e7ab766e`: 5 math CARRIES, 625, STANDARD-BEARING) + D (`10cf40c2`: 7 literacy, 498, MIXED-band) + E (`0ad078f2`: dedicated gens — picture-sort 180 readiness + chart-count 41 standard-bearing K.MD.B.3, cryptogram proven-unlandable) COMPLETE — total 2611 no landings LIVE+repointed+verified. Only Part 7 CLOSE (mechanical) left.** A+B all 1-trinn/no-standard; **C all 2-/3-trinn with CCSS educationalAlignment + LK20 framework + strand Tall og tallforståelse** (first standard-bearing wave); **D mixed-band** (readiness 1-trinn + standard-bearing 2-/3-trinn via mode-function cfg.level/standard/strand; gradeFor LEVEL-DRIVEN). Gate maxes: A 0.703, B 0.749, C 0.750, D 0.750 — all PASS 0F/0dup. **Wave-D NEW LESSON: within-mode similarity is driven by the P1/P2 LENGTH-RATIO** — word-scramble FAILed 0.811 (P2 only ~11w → shared SKEL P1+P3 dominated same-theme pages); fix = lengthen P2 to ~76-84w (match sibling word-guess's 73w) → 0.598. Distinct from cross-mode boilerplate→mode-true; when same-SKEL pages converge, GROW P2, don't just diversify the template. Durable lessons: pattern-train 5-mode + treasure-hunt + math-puzzle 3-mode + math-worksheet 2-mode all needed boilerplate→mode-true sharpening (**multi-mode-of-ONE-type is the real cross-mode risk**); addition↔subtraction cross-TYPE held first-try via operation-true verb divergence; the minst-trap fence (more-less færrest vs big-small minst=size); band-split re-grade via `annotate-no-bands.js` (math-worksheet was band-split, NOT da's "all ≤10" — never inherit da's band ruling). Ledger: LK20; bands +1 offset `1/2/3-trinn` (Norway uses **trinn**, NO barnehage band); render = bokmål `-ene` but **DROPS `-er`** (frukter→**fruktene**, ≠Danish; only `tree` irregular); grade-led arithmetic title (Addisjon/Subtraksjon, two op-distinct no-carry `uten tierovergang`/`uten låning`); readiness Pattern-B `[X] [Tema] – oppgaver for barn til utskrift | PDF gratis` (no trinn number). **Wave-A forks: grid-match=`Para sammen bilder`, strand=`Visuell oppmerksomhet og logisk tenkning`.** Reusable build pattern: native-agent-per-config with OWNED-lexicon+fences = first-try gate pass; boilerplate→mode-true sharpening for elevated cross-mode; GEN-LINT gotcha = SKEL ~140-165w (rendered body ≥200) + never `et {H1}` (article-before-placeholder). **Wave-E forks: picture-sort token=`Sortering` (H1 keeps `Sorter bildene`); chart-count=`Søylediagram` 2-trinn STANDARD-BEARING K.MD.B.3. Cryptogram PROVEN-UNLANDABLE (0 subjectTags).** Wave-E dedicated gens cloned da: render2 dual-theme picture-sort (16×12=192>180 pairs, B5 double-def gate DROPPED, bokmål count-fence `/tell|talt/`+negators NOT da `tæl`/sv `räkn`, **band-label `1. trinn` digit exempted**) + chart-count honest-count Quiz (`Hvor mange <fl>?`=rendered bar, native OVERRIDE plural map: tre→trær, postbud neuter zero-pl, de-dup-by-plural guard). Gate 0F/0dup over 2611, picture-sort within-type 0.739. **🏁 `no` FAN COMPLETE (Part 7 CLOSE done 2026-06-15): 28 types / 2611 landings / cryptogram unlandable; bands 1-trinn 1523 · 2-trinn 802 · 3-trinn 286; sitemap shard-4 carries all incl. 221 Wave-E; both bands live-verified. NEXT LOCALE = `fr`** (own plan-mode + native ensemble per part; then it→pt; fi catalog-ready). Deferral: future 1.-trinn standard-bearing split for more-less (K.CC.C.6)/find-and-count (K.CC.B.5); puppeteer verify-deorphan `no` block when Chrome on Hetzner. SoT plan `~/.claude/plans/go-luminous-moon.md`; detail [project_8locale_landing_rollout.md](project_8locale_landing_rollout.md).
- **2026-06-12 mega-session (Fable unified commission, SoT plan `commission-to-fable-twinkling-tide.md` — READ FIRST on resume):** nl CLOSED (27 types/2427, `fb7996f5`; de-de defect root-fix + B3 lint) · **browse foundation LIVE** (`67e6867f`+`4f17264a`: sitemap SHARD 4 = all landings w/ route-identical hreflang [index lists 5 shards]; landing↔landing mesh; /worksheets hub SSR landing browser w/ query-facets canonical-to-bare; standards-hub worksheets section; reserved-slug lint; landing-content O(1) coord+facet indexes; es themeless-coordinate null-guard) · da CLOSED (28 types/2636, `76905828`, PERFECT-PASS gate 0F/0W over 3.47M pairs; da:1-klasse locale-prefixed LEVELS lookup; B5 no-double-definiteness engine; math-worksheet LANDED) · **fi deck wave PUBLISHED (2,983 decks, full SEO incl. Google-thumbnail stack; 46+1 theme defects salvaged)** — fi landing fan now unblocked.
- CARRY FORWARD: fr→it→pt→fi landing fans (no DONE 2026-06-15; playbook ×8-proven); **shard-0/1 deck-dedup ≥2026-06-26** (one landingSlugForDeck boolean in sitemap/{0,1}.xml routes); puppeteer de-orphan run (sv/nl/da) when Chrome on Hetzner; Quiz-retrofit es/en/de; treasure-hunt theme-emit app-fix es+en (§10.3); /en/topic/sudoku 404; LEVELS/LEVEL_CHIP shared-lib fold.
- **All 3 built locales DONE on both axes:** every deck type landed AND every title+meta keyword-re-keyed to harvested demand. **es 1927** (`c0eb3650`) / **en 2005** (`d3cff15f`) / **de 1972** (`f1bd2550`) = **~5,904 pages**, 0 dup-titles per locale. The inside-out→outside-in keyword fix is live across all three.
- **Reopened-5 resolved per locale** (deck types deferred during the wave build): math-worksheet (per-deck band re-grade via ceiling+subtraction sweep → primer/1.OA.A.2 or segundo/2.OA.A.1; en/de 2.OA.A.1), word-scramble, wordsearch, **treasure-hunt** LANDED; **cryptogram = proven themeless wall** (UNLANDED, logged). treasure-hunt APP-BUG: the v32 generator wrote ONE theme for every deck (es=accessories×96, en=4th_of_july×98) — real themes recovered by reading the rendered thumbnails (6 agents, ~46-48/locale, clean theme-slug + canonicalDeckSlug→real deck); **de treasure-hunt manifest was CLEAN** (48 themes, built directly). Deck-side app-fix (theme-emit) for es+en remains a separate §10.3 task.
- **Keyword re-key engines** (DATA-ONLY title+meta; H1/body/coordinate untouched): `scripts/seo-landing/rekey-es-titles.js` + `rekey-en-titles.js` (es/en GRADE-LED: `[op] para/for [Grado] [qual] – [Tema] | …PDF`) + `rekey-de-titles.js` (de RANGE-LED: `Arbeitsblätter [op] bis [N] – [Thema] | zum Ausdrucken PDF kostenlos`). honest-fit-gated quals (es sin llevar / en without regrouping·borrowing / de ohne Zehnerübergang ONLY on within-10 no-carry; de bis-N gated to band 10/20/100; grade gated to ledger) + per-mode distinguishers + ordinal dedup + length trade (keyword lead within display, honest distinguisher trail accepted). Locked patterns + full doctrine = **CLAUDE.md §22.4**.
- **es/en/de keyword harvests** accumulate in [seo-keyword-strategy.md](seo-keyword-strategy.md); each new rollout locale (nl next) needs its OWN ~20-min autocomplete harvest on the right regional Google BEFORE its build (born keyword-correct, no retrofit — the es/en/de lesson). Open forks still operator's-call: **tracing/handwriting deck-PRODUCTION** (grafomotricidad / Schwungübungen / tracing — 3-locale-validated demand; a deck-CREATION decision, NOT a landing task — writing/drawing-lines apps are PDF-only) + the **fi deck-catalog gap**.
- Full detail (per-locale counts, reopened-5, app-bug recovery, rekey engines/patterns, the EN-wave + de-fanout arc history) = [project_seo_landing_page_program.md](project_seo_landing_page_program.md) + [project_seo_landing_es_arc.md](project_seo_landing_es_arc.md) + [project_de_landing_fanout_investigation.md](project_de_landing_fanout_investigation.md) + CLAUDE.md §22.


---

## VOCAB GENDER AUDIT — DE+NL COMPLETE (2026-06)
- DE+NL gender-code audit in image-vocabulary.js DONE: 136 fixes / 5 commits (DE 58 `7bef6173`+`3735199c`+`7aa69cde`; NL 78 `5310b329`+`c7de1a40`). DE m/f/n, NL d/h, per-locale authority. Carry-forward = held twijfel/lemma/does-more-plural items. See [project_de_nl_gender_audit_complete.md](project_de_nl_gender_audit_complete.md) + CLAUDE.md §A.13.58.


---

## PAGE-SPEED AUDIT + FIXES (LIVE 2026-06-02)
- Site was fast on desktop (94-98), slow on MOBILE everywhere (54-82) — image weight, NOT the decks. Fixed (Next.js): deck thumbnails → next/image (~95% off), hero video poster, worksheets revalidate. Fixed (decks, all 22,612 live + publish-wave STEP 4b/4c): img width/height (CLS), lazy-load deck-end thumbnails (LCP), async Fredoka font (FCP), un-hide suggestions section, `.lcs-bar` nowrap (intermittent CLS root cause). **All 6 deck families now reliably 89-99 mobile (was ~70).** Harness `scripts/lighthouse-audit.js` (PSI API 429→use local Chrome); CLS-debug via `lighthouse --save-assets` trace LayoutShift.impacted_nodes. See [project_page_speed_audit_2026_06.md](project_page_speed_audit_2026_06.md).


---

## PDF NOINDEX + SLUG AUDIT (LIVE 2026-05-31)
- Deck printable/answer-key PDFs are now `X-Robots-Tag: noindex` at nginx (deck.html is the single indexable surface; PNGs stay indexable). Server-side config (not git); re-run `scripts/publish-cli/patch-nginx-pdf-noindex.py` if nginx rebuilt. Full slug catalog (19,537) audited 0 hard defects via new `audit-slug-fs-db-consistency.js`. 73 decks carry a `thanksgivinng` typo (surfaced, not auto-fixed). CLAUDE.md §17.8.20; commit `1bc9f21e`. See [project_pdf_noindex_and_slug_audit.md](project_pdf_noindex_and_slug_audit.md).


---

## ACTIVITY + TOOL SEO (LIVE 2026-05-30)
- Activity detail pages carry full crawlable body (3-tier prose, 11 locales) + LearningResource/FAQPage JSON-LD + link mesh; mini-tools stay iframe-only (NOT noindexed, per operator). Guardrail `node scripts/audit-activity-pages.js` (187/187). Extend via `frontend/messages/activity-content/<locale>.json`. See [project_activity_page_seo.md](project_activity_page_seo.md). Commits `1646c46f`..`1da00b95`.
- Per-manipulative SSR landing pages at `/[locale]/tools/<native-slug>/` (ten-frame/number-line/ruler, 11 locales, native slugs e.g. zehnerfeld/lukusuora/regua) LIVE commit `fef2b985`; embeds mini-tool + crawlable body + LearningResource(Manipulative) JSON-LD; 33 sitemap URLs. See [project_tool_landing_pages.md](project_tool_landing_pages.md).


---

## INFRA GOTCHA — CDN stale-chunk breaks login after redeploys (FIXED 2026-05-29)
- Frequent redeploys churn Next.js `_next/static` hashes; nginx let Cloudflare cache transient deploy-window 404s → signin page JS 404s → "can't log in" (the login API actually SUCCEEDS). Fixed nginx `/_next/static` to return `no-store` on 404. Stranded user = one hard refresh. See [project_cdn_stale_chunk_login_break.md](project_cdn_stale_chunk_login_break.md).


---

## INFRA GOTCHA — deploy.sh can briefly 500 the site (rm .next/standalone race, NOT fixed)
- `deploy.sh` does `rm -rf .next/standalone` BEFORE build while the live ISR server writes into its cache → `rm: Directory not empty` → `set -e` abort → half-gutted standalone → `MODULE_NOT_FOUND` 500s (homepage may stay 200; `/worksheet-generators/*` 500). Recovery: `rm -rf .next/standalone` then re-run `deploy.sh` (smoke passes); if it recurs `pm2 stop` first. Hit 2026-06-04. See [project_deploy_standalone_rm_race.md](project_deploy_standalone_rm_race.md).


---

## ACTIVITY-PAGE SEO (LIVE 2026-05-30)
- Activity detail pages now carry a full crawlable body (3-tier prose, 11 locales) + LearningResource/FAQPage JSON-LD + internal link mesh; mini-tools stay iframe-only (NOT noindexed, per operator). Guardrail: `node scripts/audit-activity-pages.js` (187/187 live). Extend via `frontend/messages/activity-content/<locale>.json`. Commits `1646c46f`→`1da00b95`. See [project_activity_page_seo.md](project_activity_page_seo.md).


---

## SEO REMEDIATION COMPLETE — Parts 1–6 + title-disambiguation (2026-05-31)
- Full code-side remediation off the 6-part audit is LIVE. Highlights: 8,781 non-EN deck URLs re-slugged to native exercise-mode tokens (no-404 soft consolidation); title-disambiguation extended to all 11 locales (`audit-title-differentiators.js` map + `republish-seo --disambiguator-map`; OLD mu/finalize variant_id flow OBSOLETE); Part 6 shipped 6A (Romance topic titles via `lib/seo/topic-title-format.ts`, root-404 server component, sitemap sibling hreflang via `lib/seo/deck-sitemap-hreflang.ts`, axis-aware intersection prose `introByPair`) + 6B (count-inbound HTML-aware, new `topic-coverage.test.js`, reconcileLocaleResidue English-equality HALT, deleted 4 dead cannibalization scripts) + 6C (`seo.featuresSentence` differentiator sentence in deck descriptions, 19.6k-deck retrofit) + 6D-R15.1 (30 intent-pair intersection variants × 10 non-EN locales). See [project_native_slug_migration_and_mu_gap.md](project_native_slug_migration_and_mu_gap.md) + the session plan file.
- **[FOLLOW-UP] R15.3/R15.4 rich single-axis hub prose** → dedicated §A.13.48 native-ensemble commission (NOT yet done). Demand-ranked: fr 93-key topicProse gap (188 themes have fr decks) + full-market hubs (4th_of_july/accessories/ocean_life/bakery/at_the_supermarket/… + grade-1/2). Out of scope (operator dashboard): R1 GSC indexation, R9 Cloudflare AI-crawler, R10 Pinterest.


---

## ACTIVE WORK — Thin-page SEO remediation (multi-part, plan-mode-gated)
- Part 1 DONE (commit `769d2a9a`): audit instrument + 8,144-thin-page ranked queue + keyword-ownership seed map. Catalog now 16,551 decks across 5 full markets (en/de/es/pt/it) — overturns stale "en/es/pt only". See [project_thin_page_remediation.md](project_thin_page_remediation.md). Next: Part 2 structural floor.


---

## ACTIVE WORK — Homepage-v3 (LIVE as of 2026-05-24)
- Homepage-v3 PROMOTED to live at `/[locale]/page.tsx` (commit `bc215a5c`). 9-section v3 stack across all 11 locales. homepage-v2 components preserved on disk for rollback. See [project_homepage_v3_live.md](project_homepage_v3_live.md).
- `/[locale]/worksheets/` chrome localized in all 11 languages via `worksheetsPage` namespace (commit `3dc57dfe`). NSR-flag deferred for SV/DA/NO/FI.
- CategoryNav dropdowns "Browse all" → `/[locale]/worksheets/`; SignupV3 CTA → `/[locale]/activities` (commit `69d7cfdb`).
- Standing rule LOCKED for 11-locale i18n recreations: plan-mode-per-locale + 3-agent native ensemble. See [feedback_11_locale_recreation_discipline.md](feedback_11_locale_recreation_discipline.md) and CLAUDE.md §A.13.48.
- Deferred follow-ups queued (NSR review, v2 cleanup, OG image v3, preview-route deletion, activities slug fan-out). See [project_homepage_v3_deferred_followups.md](project_homepage_v3_deferred_followups.md).


---

## ACTIVE WORK — Interactive-HTML export
- **29 of 31 eligible apps complete (28 live, treasure-hunt v32 ported locally — as of 2026-04-26).** v4 addition, v5 subtraction, v6 code-addition, v7 more-less, v8 math-puzzle, v9 math-worksheet, v10 alphabet-train, v11 pattern-train, v12 prepositions, v13 word-guess, v14 word-scramble, v15 wordsearch, v16 cryptogram, v17 big-small, v18 pattern-worksheet, v19.1.2 find-and-count, v20.0 chart-count, v21.0 matching, v22.3 grid-match, v23.0 shadow-match, v24.3 bingo, v25.1 picture-sort (strict scoring + Custom Words text-tile mode), v26.1 missing-pieces (shape-masked placed pieces + amplified wrong-piece cue), v27.0 odd-one-out (Family A choice-tap with deferred Check + reveal-correct on wrong), v28.0 sudoku (4×4 picture-sudoku with image-index matching, no reveal-on-wrong), v29.2 picture-path (drag-to-trace + wall blocking + Treasure Trail maze redesign with on-path collectibles, off-path distractors, and typed legend inputs), v30.0 find-objects (Family C tap-to-mark, dual-mode I-Spy + Odd One Out with auto-fill legend counts), v31.0.3 crossword, v32.0.1 treasure-hunt (Family B drag-to-trace, no walls, **locked-to-startCell** pointer-down, hidden endpoint, solutionPath computed in-exporter from `(startPos, moves)` traversing every intermediate cell so multi-step direction clues like "2 north" require enacting both cells; **v32.0.1 dropped both the no-self-loop guard AND the drag-back-to-undo branch from onPointerMove and added a `trace.length>=totalCells` cap** so routes that revisit a cell — e.g. "2 east, 1 west" → solutionPath `[A1,A2,A3,A2]` — are solvable; mistakes are corrected by releasing pointer + clicking startCell to reset, NOT by dragging back, since drag-back can't be disambiguated from a legitimate immediate-backtrack step without solution-awareness; do NOT replicate this in picture-path/wordsearch — those puzzles' solutions are simple paths and the no-self-loop guard there is correct; per-cell world rects captured at DOWNLOAD time by walking gridGroup children with cellRow/cellCol tags and using `calcTransformMatrix` + `transformPoint` — the gridGroup's getBoundingRect can't be used for cell-area math because A-E / 1-5 row+col labels at local x:-21/y:-21 inflate the group bbox beyond the cell area) (Family A 2D-grid type-and-check — FIRST 2D-grid Family A port: typed letter input across an intersecting word grid with click-to-focus + direction toggle on dual-direction cells + arrow/backspace nav + click-clue-to-focus-its-word; cells stay visually IDENTICAL to the baked grid with input fully transparent until focused, active = clean 2 px blue inset ring only — NO active-word strip, the corresponding picture clue glows instead for clear 1:1 mapping; per-cell tagging + calcTransformMatrix per cell DEFERRED TO DOWNLOAD TIME so operator edits between Generate and Download are honored; Unicode letter input via `/[^\p{L}]/gu` so all 11 site languages work). All carry the shared LCSAttribution footer. Six runtime families: **Family A (letter fill-in incl. choice variant + 2D grid, v4–v14, v16–v18, v27.0, v31.0.2)** reference `REFERENCE APPS/word-guess.html` v13 for the cleanest example, `REFERENCE APPS/more-less.html` v7 for the choice-button variant, `REFERENCE APPS/odd-one-out.html` v27.0 for the **deferred-Check choice-tap variant** (single-select per problem, no instant feedback, Check Answers reveals green/red, wrong picks get loud cue + the correct option gets a green-dashed reveal ring — first Family A port to use Family F's deferred semantics), and `REFERENCE APPS/crossword.html` v31.0.3 for the **2D-grid type-and-check variant** (every blank cell is a transparent typed-input over its baked rect; cells participate in two words simultaneously so click-to-focus toggles direction on already-focused dual-direction cells; auto-advance along the active word; click a picture/text clue to focus its first cell + set its direction; case-insensitive comparison; strict per-cell Check; **CRITICAL** when overlaying typed inputs on a baked Fabric Group grid: (a) tag each cell rect individually and use `calcTransformMatrix` per cell rather than scaling pre-group local rects by the group's `getBoundingRect`, because the gridGroup's outline path inflates the bbox by ~strokeWidth and Fabric normalizes child coords to the group center post-construction; (b) **CAPTURE WORLD RECTS AT DOWNLOAD TIME, NOT GENERATE TIME** — the operator can move/scale/rotate the gridGroup and any clue group between Generate and Download, so do the recursive walk + per-element rect math inside `extractDeckBundle`, not in the post-`renderWorksheet` block (otherwise the bundle has stale positions and overlays misalign); (c) keep the input fully transparent by default with NO border/background/padding-inflation — duplicate visible borders or inflation by even 2px creates "halo" misalignment with the baked stroke and the kid sees what looks like duplicated cells, lesson reinforced from find-objects v30.0; (d) **input character filter must be Unicode-aware** — use `/[^\p{L}]/gu` not `/[^A-Za-z]/g`, since all the Nordic + Romance + German vocabulary the apps generate contains diacritics that the ASCII regex would silently strip, breaking 10 of 11 site languages); **Family B (puzzle drag, v15, v29.2, v32.0)** references `REFERENCE APPS/wordsearch.html` v15 (drag across letter grid, snap to direction, validate placed-word endpoints), `REFERENCE APPS/picture-path.html` v29.2 (drag-to-trace cell-by-cell with orthogonal-only moves + back-track + per-cell wall blocking + strict path validation; works for picture-pathway / classic-maze / choose-the-right-path uniformly), and `REFERENCE APPS/treasure-hunt.html` v32.0.1 (variant: locked-to-start drag-to-trace on a 5×5 grid with NO walls, NO visible endpoint marker, and the solutionPath computed from operator-generated direction clues — toggling `mazeWalls`, `endCell`, and start-lock through bundle flags makes picture-path's runtime reusable for any orthogonal grid path puzzle; revisit-allowed runtime variant since solutionPath can repeat cells); **CRITICAL** to remember when adding interactive overlays inside `.lcs-overlay`: the overlay has `pointer-events: none` so the trace pad below stays clickable, so ANY interactive child (input, button, etc.) needs an EXPLICIT `pointer-events: auto` or it will silently swallow all clicks); **Family C (grid-tap / tap-to-mark + count-in-blank, v19, v30.0)** references `REFERENCE APPS/find-and-count.html` v19.1.2 (cell-based tap-mark per task type — circle/square/cross/count-dot — with inputs overlaying baked legend blanks, tap-only auto-celebrate for letter-spotting mode) and `REFERENCE APPS/find-objects.html` v30.0 (busy-scene tap-to-mark with rotation/scale-aware bboxes, dual-mode I-Spy + Odd One Out shared runtime via `mode` switch in the bundle, auto-fill legend counts from taps with `imgPath`-based direct lookup; **CRITICAL** when overlaying typed inputs on baked legend boxes: use `border-radius: 0` + sharp corners + no `box-shadow` so the input visually REPLACES the baked stroke instead of leaving a halo of the baked outline visible behind rounded corners, and inflate the bundled rect by ~3 px on every side so the input fully covers the baked box's stroke); **Family D (bar-chart cell tap, v20)** reference `REFERENCE APPS/chart-count.html` — column-snap cell fill, per-column grading vs counts, no typed inputs; **Family E (tap-to-connect line drawing, v21, v23)** reference `REFERENCE APPS/matching.html` (horizontal column pairs) and `REFERENCE APPS/shadow-match.html` (top/bottom row pairs) — tap left item then tap right item to draw SVG line, 1-to-1 replacement semantics, tap-line-to-undo, green-dashed hint overlay for wrong lines; **Family F (drag-and-drop, deferred Check-Answers feedback, v22.3, v24.3, v25.1, v26.1, v28.0)** references `REFERENCE APPS/grid-match.html` v22.3 (tile-puzzle layout), `REFERENCE APPS/bingo.html` v24.3 (bingo card + chip strip layout, optional caller bar variant), `REFERENCE APPS/picture-sort.html` v25.1 (multi-occupancy bins + STRICT scoring — operator labels bins via text tool — + Custom Words mode where each side's dropdown gains a "Custom Words" option that turns typed words into text-only Fabric.Textbox tiles), `REFERENCE APPS/missing-pieces.html` v26.1 (cutout-hole drop targets with shape-masked placed pieces via CSS border-radius for the 6 piece-shape variants, 1-to-N options-with-distractors palette, strict 1-to-1 scoring per hole, amplified wrong-piece cue with thick red border + outer glow + ✖ overlay + shake animation), and `REFERENCE APPS/sudoku.html` v28.0 (4×4 picture-sudoku grid with cutout palette + **image-index matching** instead of cutout-identity matching — duplicate images interchangeable since multiple sudoku cells legitimately need the same image; NO reveal-on-wrong, preserving the puzzle's logic-deduction nature) — pointerdown on palette tile spawns ghost (chip imgDataUrl cropped from worksheetImage at extract time, sized to grid-cell), drag to ANY empty grid cell. Two-phase model: during "fill" every drop is accepted with no right/wrong feedback (no green, no red), pieces are freely movable between cells (cell-pickup re-enters drag), Check Answers button is disabled. When all non-clue cells are filled, Check enables. Click Check → "reviewed" phase: correct cells get green glow, wrong cells get red inset border, all locked, celebration on all-correct. Try Again resets to fill. Pointer Events for mouse + touch parity. **Bingo's variant (v24.3) adds a caller bar above the worksheet that announces one shuffled file name at a time and advances on each new palette→cell placement (cell→cell moves do not consume a call). The caller is purely a literacy prompt — it does NOT pre-highlight any chip and does NOT affect the right/wrong evaluation, which still happens on Check Answers.** **NOTE — Family G (drag-with-caller) was prototyped in bingo v24.0–v24.1 but retired in v24.2 in favor of Family F. The original caller-driven model gave the kid pre-drag location hints that defeated the literacy task; v24.3's caller is announce-only with no chip hint.** Coloring and writing are excluded (PDF-only). 2 remaining: draw-and-color, drawing-lines. See [project_interactive_export_status.md](project_interactive_export_status.md) for the scoreboard, [feedback_interactive_export_porting_recipe.md](feedback_interactive_export_porting_recipe.md) for the recipe and pitfalls. Architecture documented in `CLAUDE.md` §14.


---

## CRITICAL USER RULES
- **`REFERENCE APPS/` is the ONLY source of truth for all 33 worksheet apps.** Always edit `REFERENCE APPS/<app>.html` — never read, search, grep, or reference any other location for worksheet HTML. The dormant `legacy-apps/` and `worksheet generators/` parallel deployments were deleted on 2026-04-24; the only other location that still holds worksheet HTMLs is `frontend/public/worksheet-generators/` (gitignored, populated by `scripts\master-sync.bat`, served only by the Next.js dev server) and the production `chattr +i` copy at `/var/www/lcs-media/worksheet-generators/`. **If you ever find a sibling worksheet copy that isn't one of those two, stop and flag it — it shouldn't exist.** After any production deploy, run `/var/www/lcs-media/scripts/update-worksheet.sh` to refresh the immutable served copy — `deploy.sh` does `git pull` but does NOT sync the chattr +i worksheet files. REFERENCE APPS must always reflect what is live. See [feedback_reference_apps_source_of_truth.md](feedback_reference_apps_source_of_truth.md) and [feedback_worksheet_deploy_two_step.md](feedback_worksheet_deploy_two_step.md).
- **User is non-technical.** Once work is approved, execute the full commit → push → PR → deploy cycle without asking between steps. See [feedback_push_deploy_authorization.md](feedback_push_deploy_authorization.md).
- **NEVER say "free" without qualifying it as "free trial with watermark"** — the apps are NOT free. They are free to TRY with full features, but downloads have a watermark. A paid license removes the watermark. Every mention of "free" must say "free trial" or "try free with watermark".
- NEVER start work on multi-part plans without asking permission first
- ALWAYS ask "Do you want me to start Part X?" and WAIT for explicit YES
- ONE PART AT A TIME - never rush, use full capacity on every single part
- Parts exist to overcome context limits - each part deserves 100% focus
- DO NOT mention or tease the next part until current part is fully complete
- **NEVER commit, push, or deploy any image files to git** — images (JPEG, PNG, WebP, PDF samples, etc.) must NEVER enter the repository. This includes `samples/`, `sample-inventory.json`, and any generated image data. Always verify `git status` before committing to ensure no image files are staged.
- **NEVER author per-(app, mode) prose templates or behavior-describing content from assumption** — content describing what an external component does MUST be ground-truth-verified against source code BEFORE authoring. Phase 1 of any such commission launches an Explore agent or direct Read+Grep against `REFERENCE APPS/<app>.html`; every template entry cites `// Verified against: <file>:<lines>` in a comment block; empty citation = defect. The Sub-Phase 2.4 incident (4 of 10 templates drifted from actual app behavior at first ship) is the empirical anchor. See [feedback_template_ground_truth_discipline.md](feedback_template_ground_truth_discipline.md) and CLAUDE.md §A.13.29.
- **NEVER author user-facing copy from engineer perspective** — i18n strings, PDF text, page descriptions, UI labels, download buttons MUST be written from the READER perspective (what they get + what they do with it). Forbidden in teacher/parent-facing copy: internal taxonomy names (`composedExercises`, `pedagogical framing`, `package metadata`), implementation primitives (`IMAGE_VOCABULARY`, `auto-resolved`, `gender data`), architectural concepts (`packages`, `decks`, `target language`, `locale resolution`). Use "the language your kids are learning" NOT "target language"; "what kids worked on" NOT "composed exercises". Third-party brand stamps are FORBIDDEN in private external communications (parent letters, take-home content). The Sub-Phase 2.5 incident (parent-letter PDF shipped with LessonCraftStudio branding + 7×4=28 section descriptions leaked operator jargon) is the empirical anchor. See [feedback_audience_perspective_user_facing_content.md](feedback_audience_perspective_user_facing_content.md) and CLAUDE.md §A.13.30.
- **NEVER author content ABOUT a specific package/deck/topic/lesson without reading its canonical artifact** — any commission whose scope names a specific package (e.g., `compare-by-weight`), deck, topic, or material instance MUST in Phase 1 read the canonical YAML / manifest / data file for that named instance BEFORE Phase 2 design. The NAME is a label; the artifact is content. Never infer package contents from the slug. Canonical paths: package → `docs/lesson-plans/packages/<slug>/package.yaml`; topic → `frontend/config/topics-taxonomy.json` + composing packages; deck → manifest + bundle data. Doctrine ALSO triggers on operator's IDE-open signals (a package YAML open in IDE alongside a commission = read it). The Sub-Phase 2.4 find-and-count drift was partly per-instance content-blindness (didn't read count-objects-1-to-10/package.yaml to verify the specific themeSelect config). See [feedback_per_instance_content_awareness.md](feedback_per_instance_content_awareness.md) and CLAUDE.md §A.13.31.


---

## File Encoding — ALL CONTENT FILES (ABSOLUTE RULE — ZERO TOLERANCE)
- **NEVER use `\uXXXX` escape sequences in ANY file — English, German, French, ALL languages.**
- **ALWAYS write real characters directly:** `—` not `\u2014`, `ä` not `\u00e4`, `ç` not `\u00e7`
- **NEVER use curly/smart quotes** (`'` `'` `"` `"`) in .ts files — SWC rejects them. Use straight quotes only.
- **ALWAYS escape apostrophes** inside single-quoted strings: `'Valentine\'s'` not `'Valentine's'`
- This applies to ALL languages: French `d\'addition`, Italian `dell\'utente`, English `don\'t`
- **After writing ANY content file**, run `node scripts/find-broken-quotes.js` to verify no broken strings
- See [feedback_unicode_escapes.md](feedback_unicode_escapes.md) and [feedback_content_quotes.md](feedback_content_quotes.md)


---

## Project Architecture (Post-Pivot Feb 2027)
- **Business model:** Professional Printable Business Toolkit (Etsy sellers, KDP publishers, entrepreneurs)
- **Payment processor:** Lemon Squeezy (migrated from Stripe/WarriorPlus April 2027)
- **Products:** 33 individual apps ($49 each) + 6 category bundles ($149 each) = 39 products
- **All products:** One-time payment, full access, commercial license. NO tiers.
- 11 locales: en, de, fr, es, pt, it, nl, sv, da, no, fi
- 33 worksheet generator apps (free trial with watermark, no signup)
- 7 page types: app-content (33), tool-content (33), guide-content (65), bundle-content (6), idea-content (45), start-content (12), compare-content (3)
- 194 content files per locale across 6 core types + 3 comparison pages (EN only so far)
- 11 locales: en, de, fr, es, pt, it, nl, sv, da, no, fi
- Source of truth for apps: `frontend/config/products.ts` (6 categories, 33 apps)
- Comparison pages: `frontend/config/compare-page-slugs.ts` + `compare-content/` + `/[locale]/compare/[slug]`
- Schema generation: `frontend/lib/schema-generator.ts` (Organization type, not EducationalOrganization)
- Middleware: `frontend/middleware.ts` — handles 410 Gone for removed routes + locale routing
- Full content creation guide: `docs/reference/12-content-creation-guide.md`
- Auth system: intact (login, signup, email verification, sessions)
- Admin system: intact (dormant, `/dashboard/admin`)
- Member portal: License key activation at `/member`


---

## Lemon Squeezy Payment System (April 2027)
- **39 products:** 33 individual apps ($49) + 6 category bundles ($149)
- **All one-time, full access, commercial license** — NO tiers or upsell funnels
- **Webhook:** `https://www.lessoncraftstudio.com/api/webhooks/lemonsqueezy` (TODO: build)
- **License flow:** LS purchase → webhook → generate LCS-XXXXX key → email delivery → `/member` activation
- **Config:** `frontend/config/lemonsqueezy-products.ts` (TODO: create after products are created on LS)
- **Env vars:** `LEMONSQUEEZY_API_KEY`, `LEMONSQUEEZY_STORE_ID`, `LEMONSQUEEZY_WEBHOOK_SECRET`, `LEMONSQUEEZY_STORE_SLUG`
- **Old Stripe backup** still at `/opt/lessoncraftstudio/stripe-backup/` (immutable, for reference only)

### FTC Compliance (ABSOLUTE RULE)
- NEVER generate fake testimonials, reviews, or social proof
- NEVER fabricate person names, roles, or quotes for sales pages
- NEVER claim specific results (e.g., "made $X", "sold Y units") without real data
- All testimonials must be from REAL, identifiable people with provable claims
- If no real testimonials exist, set `testimonial: undefined` — never invent one


---

## What Was Removed in Pivot
- Blog system (routes, API, components, content)
- Theme/worksheet pages (550 locale content files, 17 components)
- Pricing page + Stripe integration (13 API routes, 5 lib files)
- Product content files (363 files)
- Category/grade hub pages
- /buy pages
- Subscription dashboard pages
- ~2,100 dead i18n keys (blogPage, pricing, subscription)
- ~10,900 lines of dead code (schema generator, orphaned components/configs)
- All removed URLs return **410 Gone** via middleware


---

## SEO Patterns
- Framer Motion `initial={{ opacity: 0 }}` renders invisible content in SSR HTML - use CSS `@keyframes` instead
- `\b` regex word boundary breaks on accented characters - use `(?:^|[\s,;.!?()])` pattern instead
- 6 app categories: Activity Worksheets, Math & Number, Letters & Words, Drawing & Art, Puzzles & Games, Pattern & Logic
- See [seo-keyword-strategy.md](seo-keyword-strategy.md) for seller-focused keyword rules (ALL content must follow)
- See [feedback_keyword_strategy.md](feedback_keyword_strategy.md) — CRITICAL: target long-tail seller-intent keywords, NOT broad head terms
- Validation: `node scripts/validate-content.js --all` catches bare "free" without "trial" qualifier (rule 14)
- Tool page slugs and titles no longer use "free-" prefix (removed March 2026)


---

## Deployment Protocol (NEVER SKIP)
- EVERY part ends with git commit (no exceptions, no piling up)
- At deploy points: commit + push + deploy.sh + verify
- At non-deploy points: commit locally only (no push)
- FIRST ACTION of every session: run git status to check for uncommitted work
- If uncommitted work exists: commit first, check for missed deploys, THEN start new work
- **CLAUDE.md edits commit in the same session, full stop.** No leaving CLAUDE.md modifications in the working tree across sessions — the doc is too load-bearing to risk to PC failure / stray reset / accident. See [feedback_claude_md_commit_in_session.md](feedback_claude_md_commit_in_session.md). Surfaced 2026-04-27 when §15–§19 were found sitting uncommitted across multiple prior sessions during the SEO amendment merge.


---

## Pre-existing TS errors
- Test files (`__tests__/`, `e2e/`) have pre-existing errors (missing @jest/globals, etc.)
- These don't affect app code compilation


---

## Content Voice — Seller-Focused (March 2026 Rewrite)
- ALL content must use seller/entrepreneur voice — NEVER teacher/classroom/student language
- "Teachers Pay Teachers" / "TPT" → "Gumroad" everywhere
- "students" → "solvers" / "users" / "buyers"
- "classroom" → "store" / "product line" / remove
- See [project_seller_content_rewrite.md](project_seller_content_rewrite.md) for full replacement dictionary per locale
- 6 content types: app-content, tool-content, guide-content, bundle-content, idea-content, start-content + compare-content
- 194 files per locale (33+33+65+6+45+12) + 3 compare pages (EN only so far)
- Seller rewrite completed for: en, de, fr, es, pt, it, nl, sv (partial)
- Missing ALL content: da, no, fi (0 files for any content type)
- Missing partial: sv (no idea-content, 1/12 start-content), compare translations (EN only)


---

## K-3 Activities + Literacy Engines (2026-05 commission)
- [project_activities_north_star.md](project_activities_north_star.md) — Activities are THE product. Target 500+ K-3 Math+ELA, 11 locales, 2-builder workflow, non-technical operator.
- [project_activities_architecture.md](project_activities_architecture.md) — Shell+tool split, Direction A card, mini-tools nginx-served (NOT Next.js), ActivityIframe postMessage, CC code NEVER in URL.
- [feedback_activities_approval_cadence.md](feedback_activities_approval_cadence.md) — "Tests pass" ≠ approval. Live render + 3-viewport screenshots + operator eyeball is the bar. No timer/score/SmartScore. One-at-a-time cadence.
- [feedback_activities_asset_rules.md](feedback_activities_asset_rules.md) — Color-only; LOCALIZED B&W markers per locale (da:SH de:SW en:BW es:BN fi:MV fr:NB it:BN nl:ZW no:SH pt:PB sv:SV); naive "BW" match is WRONG; strip trailing numbers.
- [project_activities_live_inventory.md](project_activities_live_inventory.md) — **READ FIRST next session.** Latest: **E14 fractions #1 (1.G.A.3) + #2 (2.G.A.3 non-congruence) BOTH 11/11 LIVE** — recognition family on the E2 choice-board engine, anchor-and-match + FRACTION_FIGURES, 0-line cores; #2 fan-out CLOSED at NO `c2bb562c`; `choice-board-activity.js?v=62`/wrapper `8.63`; active-partition **#3 deferred (needs fractions-core.js)**. See [project_e14_fractions_series.md](project_e14_fractions_series.md). Prior E2/E4/E12 work all 11/11 (E4 match-pairs 3×11, E12 place-value 3×11). **No pending activity locales — await next written instruction** (operator flagged the worksheet-hub initiative next). Cadence LOCKED (STEP-0→go-ahead→build→hold, no chaining). Fan-out doctrine: §A.13.48/.53/.54/.56/.59.
- [project_e14_fractions_series.md](project_e14_fractions_series.md) — E14 fractions #1+#2 (both 11/11 LIVE on E2 choice-board); all 22 native slugs; the two-sentence anchor+comparison construction + per-locale grammar-trap taxonomy; recognition-scope (claim 2.G.A.3 via recognition clause; #3 active-partition deferred); §20.10 + content-vs-discovery split; Nordic full-phrase cognate-clearance. CLAUDE.md §A.13.59.
- [project_danish_deck_wave.md](project_danish_deck_wave.md) — 3,065 `da` catalog decks published via `publish-wave.js --locales=da --confirm`; pre-flight salvaged 47 theme-defect ZIPs; audit "129/3094 clean" = benign "print" Danish-loanword lexicon false-positive (deck SEO correct, Google-thumbnail stack matches prior sets). Optional: add "print" to da locale-residue exception lexicon.
- [project_norwegian_deck_wave.md](project_norwegian_deck_wave.md) — 3,073 `no` catalog decks published 2026-06-08 via `publish-wave.js --locales=no --confirm` (first `no` wave; native slugs); pre-flight salvaged 46 theme-defect ZIPs; audit 2981/3074 clean = benign "matching" loanword lexicon false-positive (taxonomy `no` name="Matching" intentional; decks correct). Google-thumbnail stack verified live (og:image 1200×630 + ImageObject + og-image.png 200); sitemap shards include `no` after 30-min ISR. Inbound da+no recovery backups PRUNED (~24G, disk 91%→86%) once verified live; both locales still serve 200. Mirrors [[project-danish-deck-wave]].
- [project_server_disk_cleanup_2026_06.md](project_server_disk_cleanup_2026_06.md) — Server-wide safe cleanup 2026-06-08: ~77 GB reclaimed (86%→67%), app stayed online. Pruned publish-cli-staging old batches, 1,627 pre-deploy SQL→30d, 59G May-migration backups (superseded), journal/npm/pm2/puppeteer/tmp. Durable fixes: pre-deploy-SQL retention cron + journald SystemMaxUse=1G + pm2-logrotate. ⚠️ Flagged URGENT: NO offsite backup (100G deck tarballs are sole DR) — recommend off-host commission (§A.14.6). Kept tarballs untouched.
- [project_activity_national_framework_localization.md](project_activity_national_framework_localization.md) — Non-EN activity layer cites NATIONAL framework NAME (no code); CCSS code kept as JSON-LD targetName + /standards/<code> anchor. EN+decks keep Common Core. Commits fe15a60b/f76588ad/adb1a976. CLAUDE.md §20.10 + §A.13.49.
- [project_activities_master_queue.md](project_activities_master_queue.md) — Triage 240 buildable / engine-blocked / 90 asset / 40 open. E4 BUILT + 11/11 first activity LIVE; ~119 more K-skills unlocked from this engine.
- [feedback_cognate_aware_verify_discipline.md](feedback_cognate_aware_verify_discipline.md) — Sequential per-locale fan-out doctrine (SV→DA→NO→FI Nordic-trio + substring-trap discipline). Drop shared cognates from FORBIDDEN; use full-phrase tokens. CLAUDE.md §A.13.53. DISTINCT from §A.13.48 parallel-agent recreation.
- [project_phonics_safety_pipeline.md](project_phonics_safety_pipeline.md) — Pipeline + gate v1.1 + río + iã+o + NO STEP 2 + DA STEP 1 shipped through 4b014cbc. ES 958/305, FI 1120/143, SV 964/299 (post sv.js), PT 891/372, **NO 829/434 (post no.js+WORD_BLACKLIST), DA 794/469 (STEP 1; 402 K-1-safe pool, no da.js by operator)**. NO ng-rule: default 2-chunk n|g split (NOT SV's ng-as-coda); DA follows NO rule. E8 live in ES/FI/PT; E9 live in DE/NL/SV/NO/DA (Group-C COMPLETE).
- [project_activities_parked.md](project_activities_parked.md) — K.G.A.1 asset-blocked; "demonstrate not touch the standard" rule (Decompose-10 removed); CC code assignment discipline; hint i18n strand-label bug (deferred fix).


---

## Detailed Topic Files
- [feedback_template_ground_truth_discipline.md](feedback_template_ground_truth_discipline.md) — behavior-describing content (per-app templates, UI-mechanic guidance) MUST cite source code per CLAUDE.md §A.13.29; empty citation = defect; Sub-Phase 2.4 empirical anchor
- [feedback_audience_perspective_user_facing_content.md](feedback_audience_perspective_user_facing_content.md) — user-facing copy (i18n, PDF text, descriptions) MUST be written from READER perspective per CLAUDE.md §A.13.30; no operator-jargon; no vendor branding in private external communications; Sub-Phase 2.5 empirical anchor
- [feedback_activity_css_pitfalls.md](feedback_activity_css_pitfalls.md) — 10 unignorable activity-CSS rules from v7.1→v7.13 polish session: iframe vh/vmin circular dep, flex-row axis confusion, grid max-width left-shift, 1fr inter-tile gaps, width vs max-width, runtime CSS specificity, overflow:hidden clipping, tablet (768) separate breakpoint, iframe-src cache buster required, tests ≠ visual approval. CLAUDE.md §A.13.47.
- [feedback_per_instance_content_awareness.md](feedback_per_instance_content_awareness.md) — content ABOUT specific packages/decks/topics MUST be authored after reading the canonical artifact YAML/manifest per CLAUDE.md §A.13.31; never infer from slug; IDE-open signals trigger discipline; compare-by-weight surfacing is the empirical anchor
- [feedback_cache_buster_discipline.md](feedback_cache_buster_discipline.md) — every change to *-core.js or *-activity.js in mini tools/ MUST bump ?v=N in the html wrapper; FI title cache bug at f59a7ad2 is anchor; CLAUDE.md §A.13.42
- [feedback_programmatic_textcontent_assertions.md](feedback_programmatic_textcontent_assertions.md) — chrome i18n changes need Puppeteer .textContent + setCacheEnabled(false), not just PNG eyeball; cache-buster commission is anchor; CLAUDE.md §A.13.43
- [feedback_pipeline_regression_snapshot.md](feedback_pipeline_regression_snapshot.md) — snapshot output JSONs to .before-<arc>.json before any rule-syllabifier/gate change; diff is the regression-safety proof; CLAUDE.md §A.13.44
- [feedback_pre_flight_hand_trace_encoding.md](feedback_pre_flight_hand_trace_encoding.md) — keep diacritics intact when hand-tracing syllabifier behavior; Phase 1 agent traced pão as p+a+o missing that ã is U+00E3; verify via direct Read; CLAUDE.md §A.13.45
- [feedback_publish_wave_audit_sample_all.md](feedback_publish_wave_audit_sample_all.md) — §A.14.8 step 1 theme-emit audit MUST sample EVERY ZIP per app, NOT first-only; theme-emit defects affect 100% of an app's wave; ES Spanish wave 2026-05-14 missed 5 apps × 628 ZIPs from sampling first-only
- [feedback_pt_11app_wave_defect_recoveries.md](feedback_pt_11app_wave_defect_recoveries.md) — 3 recovery patterns from pt 11-app wave 2026-05-19 commit 0db3090e3: bingo currentLocale-fallback typeName salvage; shadow-match constant-variant_id sha1(deck_id) salvage; missing-themeName republish-seo (NOT rewrite-deck-html-title.js — clobbers level). regenerate-og-images full-locale safe.
- [feedback_dual_themeselect_anti_pattern.md](feedback_dual_themeselect_anti_pattern.md) — 8 apps (chart-count/code-addition/crossword/pattern-train/pattern-worksheet/sudoku/treasure-hunt/wordsearch) have dual theme pickers. buildCatalogManifestSettings MUST read worksheetThemeSelect, not themeSelect (filter). Also covers bingo createHeaderGroup must-invoke pattern + shadow-match contentKeys 'pairs' registration. Authoring fix at commit dbeb1058 closes the underlying defects from 0db3090e3 wave.
- [feedback_content_quotes.md](feedback_content_quotes.md) — CRITICAL: apostrophe/quote escaping rules for .ts content files (SWC build breaks)
- [feedback_nginx_bypass.md](feedback_nginx_bypass.md) — /worksheet-generators, /samples, /image-library are served by nginx directly; Next.js redirects() never fire
- [project_seller_content_rewrite.md](project_seller_content_rewrite.md) — Seller rewrite status, replacement dictionary, remaining work
- [feedback_design_elements_integration.md](feedback_design_elements_integration.md) — Before adding Design Elements accordion to another app, read `docs/reference/design-elements-integration.md` (22 load-bearing rules from coloring.html integration)
- [feedback_reference_apps_source_of_truth.md](feedback_reference_apps_source_of_truth.md) — REFERENCE APPS/, REFERENCE TRANSLATIONS/, and REFERENCE CONTENT MANAGERS/ are source of truth; always edit those first, verify after deploy
- [reference_sr_only_translation_keys.md](reference_sr_only_translation_keys.md) — naming convention for srExercise* / srPuzzle* / srWorksheetQuestions translation keys; canonical docs live as JSDoc on buildSrRows + buildSrPuzzleSummary in catalog-export.js
- [project_brief_a_translation_debt.md](project_brief_a_translation_debt.md) — Brief A en+de-only translation debt across 9 priority languages, plus math-worksheet's equation-concatenation limitation that breaks in Finnish (case marking) and produces awkward output in Spanish/Italian/Portuguese/Dutch — needs per-language renderer when commissioned
- [feedback_brief_a_5a_translation_cadence.md](feedback_brief_a_5a_translation_cadence.md) — preserve the brief's en-first / de-second split for future Group B/C apply work; don't collapse into one commit even if it feels efficient (cadence makes spot-check fixes cheap in one language before propagating)
- [feedback_bundle_shape_investigation_methodology.md](feedback_bundle_shape_investigation_methodology.md) — per-app bundle-shape investigations MUST dump the actual extractDeckBundle() runtime return (parse `var DECK_BUNDLE = {...}` from a real generated deck.html), not grep source-side data-construction sites; Brief A 5A.2 failed 5/5 spot-checks for source-side inference
- [project_k3_phrasing_native_speaker_review.md](project_k3_phrasing_native_speaker_review.md) — K-3-naturalness + gender/case-agreement phrasing decisions flagged for validation; **57 keys flagged across en/de/sv/fi/no/da** (17 prior: 4 EN + 13 DE shape/exercise/puzzle keys; 40 new from Brief B Phase 2: `seo.educational_level.*` + `endDeck.*` for sv/fi/no/da × 10 keys per Q3 NSR posture). Romance trio (fr/it/pt) deliberately NOT flagged at Phase 2 per §17.5 stronger-Claude-quality posture.
- [feedback_diagnostic_order_for_post_deploy_symptoms.md](feedback_diagnostic_order_for_post_deploy_symptoms.md) — when an app shows symptoms after a deploy, parse the inline JS and git blame the suspicious line BEFORE rolling back; pre-existing latent bugs can surface late and look like recent-deploy regressions; matching.html `try {}`-without-catch from March 2026 surfaced as Brief-A-attributed regression in April
- [feedback_verification_regex_from_real_samples.md](feedback_verification_regex_from_real_samples.md) — verification regexes / assertions checking bundle shape MUST be derived from real bundle samples, not theoretical expectations; same source-side-inference family as Brief A 5A.2's 5/5 spot-check failures, just one level deeper (the verifier itself can be wrong)
- [feedback_coverage_dimensions_emerge_from_postmortems.md](feedback_coverage_dimensions_emerge_from_postmortems.md) — coverage dimensions cannot be enumerated upfront; they accumulate from bug postmortems. **Six dimensions surfaced so far** (shape correctness, code-path coverage, image source, path encoding, upload-source-vs-Set-membership linkage loss, structural-vs-identity coverage). For vocab lookup, use shared `LCSCatalogExport.vocabKeyFromImage(img)` which dispatches across all 3 image-source forms (theme / server-upload-suffix / data URL). Two non-fix categories distinguished: latent (consumer-additive risk) vs self-consistent (editor-coupling risk). Dim 6 surfaced by Group B Phase 1: bundle exposes WHERE puzzle elements are but not WHAT they are; recovery requires sourcing identity-mapping data from puzzle generator's script-scope state.
- [project_claude_md_pending_updates.md](project_claude_md_pending_updates.md) — operator-side queue of CLAUDE.md amendments. **Queue currently empty** (social-share-v1 Sub-phase E close-out batch applied 2026-04-28 in commit `388dd7d6`; previous batches `b03ff5c9` and `0c3e8ffe`). Read at session start to detect new pending items.
- [project_group_b_brief_drafting_notes.md](project_group_b_brief_drafting_notes.md) — when Group B brief drafts (after Group A close): methodology-preamble must (a) cite Group A MEMORY entries by name as binding, AND (b) declare an asymmetric verification budget anticipating novel bug dimensions in sudoku/picture-path/cryptogram bundle work. Both flags structural, not just citation.
- [feedback_json_quote_safety.md](feedback_json_quote_safety.md) — German „word" / Dutch „word" patterns break JSON because the closing straight quote IS the JSON string delimiter. Use curly close U+201D for de/nl, guillemets for romance/danish/norwegian, BR-curly for portuguese/swedish/finnish. Surfaced Arc 6d.
- [feedback_publish_cli_slug_theme_awareness.md](feedback_publish_cli_slug_theme_awareness.md) — slug-derivation that drops manifest fields propagates SEO degradation at wave-scale. Slug shape `<exercise-type>-<exercise-mode>-<theme>` (deck identity grammar) distinct from intersection-URL theme→level→type ordering. Phase 1 wave inventory should include slug-pattern preview via dry-run. Surfaced 443→440-wave 2026-05-05.
- [feedback_publish_wave_pre_check_audit.md](feedback_publish_wave_pre_check_audit.md) — ALWAYS run 4-step audit before publish-bulk --confirm: theme-emit + seoMeta + canonical-host (www) + post-publish spot-check. Two recurring defect classes per CLAUDE.md §A.14.8; canonical solutions exist — don't reinvent.
- [feedback_runtime_i18n_bake_at_source.md](feedback_runtime_i18n_bake_at_source.md) — Runtime UI strings: bake STRINGS_ALL (11 locales) into INTERACTIVE_RUNTIME_LINES literal + force-set every UI element (titleEl/checkBtn/resetBtn) from STRINGS in init(); never trust bundle.runtimeStrings or HTML literals. Element IDs canonical: #lcs-title/#lcs-check/#lcs-reset. Verified 29 apps × 11 locales 2026-05-10.
- [project_homepage_v3_live.md](project_homepage_v3_live.md) — Live homepage IS v3 as of 2026-05-24 commit `bc215a5c`. 9-section stack at `/[locale]/page.tsx`. homepage-v2 components preserved on disk for rollback. CLAUDE.md §4.3 amended.
- [feedback_11_locale_recreation_discipline.md](feedback_11_locale_recreation_discipline.md) — 11-locale i18n recreations require plan-mode-per-locale + 3-agent native ensemble (linguist + marketing + K-3 educational expert). NSR-flag Nordic+Finnic. CLAUDE.md §A.13.48.
- [project_homepage_v3_deferred_followups.md](project_homepage_v3_deferred_followups.md) — Queue of post-homepage-v3-promotion commissions: NSR review, v2 cleanup, /[locale]/activities UX, OG image v3, preview deletion, activities slug fan-out. Priority-ranked.
- [reference_homepage_v3_file_paths.md](reference_homepage_v3_file_paths.md) — Canonical homepage-v3 file path index: page entry, 14 v3 components, preview route, i18n namespaces, CTA targets, card3 per-locale map, verification helper.
- [project_brief_b_phase_status.md](project_brief_b_phase_status.md) — Brief B publish-cli phase ledger. **Phases 1–5 sealed** (5.4+5.5 impl `0ad626cb`; 5.1/5.2/5.3/5.6/5.7/5.8 verification only). Production: 7 published (en+de addition-image-image v7/v3, en+de cryptogram v4/v3, en+de sudoku v3/v4, de picture-path v3) + 1 archived (en picture-path v2). **Phase 6 (documentation-only close-out batch) is next**, blocked on operator surfacing Cloudflare proxy-mode investigation outcome first.
- [project_deferred_items_queue.md](project_deferred_items_queue.md) — out-of-current-brief findings deliberately deferred. **Active (28; 1 OPERATOR-ACTION-REQUIRED pre-launch — Cloudflare proxy-mode):** 3 vocab-data (IMAGE_VOCABULARY casing / "octogon" typo / Phase 3 data-quality Fröhlich-Dragonfruit-Avocado-Grapefruit); 1 chart-count locale-stale; 1 cryptogram cipher-letter/DE-first-letter (Gate 1); 1 sudoku image-namespace; 2 picture-path coverage gaps (choose-path-with-legend, classic-maze-without-collectibles UI); 1 picture-path source-shape fragility; 1 vocab dispatch `_fallbackSingular` hardening; 1 per-app `t()` locale-binding divergence; 1 Intl.ListFormat 3-site → shared helper at 4th-consumer threshold; **7 social-share-v1 family** (predicted-slug vs publish-cli collision; 404 UX trade-off; catalog-page work deferred; English-title-hardcoded slug; slugify-non-ASCII-becomes-hyphens; deck.html runtime STRINGS en-only; deck.html title bakes English axis-key as theme value — surfaced Brief B Phase 2 real-ZIP spot-check); **1 prisma-migration-history-drift family** (`20251122011743_add_language_to_blog_pdfs` DB-recorded + folder-missing — surfaced during Brief B Phase 1); **1 Brief B Phase 2 slug-family** (Romance-apostrophe v1 hyphenation — v2 strip-instead-of-hyphen refinement candidate if Tier 4 launch surfaces awkward URLs); **3 runtime-hygiene family** (1 Hetzner Node 18.20.8 past EOL — surfaced at Phase 3 cheerio→node-html-parser fix `9bed3bd4`; upgrade to Node 22 LTS overdue; 1 nginx www-canonicalization 301 redirect — pre-existing nginx rule documenting `lessoncraftstudio.com → www.lessoncraftstudio.com`, undocumented in CLAUDE.md — surfaced at Phase 5 Sub-phase 5.5 verification when post-unpublish curl returned 301 instead of 404; **1 Cloudflare proxy-mode investigation — OPERATOR-ACTION-REQUIRED pre-launch** — surfaced at Phase 5 Sub-phase 5.8 verification when no `cf-cache-status` headers + `server: nginx/1.18.0` observed at every passive-curl checkpoint, indicating DNS-only mode; CLAUDE.md §3.5 CDN expectations diverge from production state; operator decides flip-to-proxy vs amend-§3.5 before launch traffic; not bundled with Phase 6 documentation close-out); **1 dead-code-cross-app family** (legacy `window.downloadPDF`/`window.downloadJPEG` override blocks dead in all 29 apps; broke pattern-train at top-level scope — fixed `be49dae6`; cross-29-app sweep deferred); **1 taxonomy-coverage family** (`topics-taxonomy.json` registers 4 of 29 apps; 25 missing — surfaced at Phase 4 first 8-ZIP real-mode attempt 2026-04-29 when bingo/crossword/pattern-train/word-scramble aborted; deferred ZIPs preserved at Hetzner `/opt/lessoncraftstudio/publish-inbound/.deferred-pending-taxonomy/`; resolves in dedicated taxonomy-expansion brief); **1 publish-cli-feature family** (dry-run preview of predicted version bumps — surfaced as Phase 5 Sub-phase 5.7 Divergence #1, accepted as v1 architectural property; revisit when bulk volumes justify); **1 archive-cleanup family** (`/var/www/lcs-media/decks/.archived/` grows unbounded under KEEP_VERSIONS=3 + per-unpublish archiving — Q3 lock at Phase 5 v2 keeps cap-forever; trigger condition: >1 GB or 100+ decks; cleanup-cron defer until then). **Resolved:** find-and-count stale-translations; Group A image-source coverage sweep.



## Moved from MEMORY.md 2026-08-06 (index was nearing its read limit)
## 🧰 PREMIUM TOOLS LINEAGE + v4 CATALOG (approved 2026-07-31; 20 language-light verifiable-only instruments). ⭐ LAW: **no words on the apparatus** (TTS reliable in only 5/11). ⚠ CLOSED strands: time · money · capacity/weight · corpus science · puzzles-with-a-solution. ⚠ **A tool is not shippable until `config/live-tool-slugs.ts` has its key** — else 410 in all 11. ⭐ Fence FIRST, subtract overlap. → [v4](project_premium_tools_v4_catalog.md) · [v3](project_premium_tools_v3_catalog.md) · [v2](project_premium_tools_v2_design.md) · [v1](project_premium_tools_program.md)
## 🔎⭐ DONE 2026-07-31 — SEO INFRA RE-AUDIT + INTERNAL LINKING. ⭐⭐ **A DOC IS NOT A FACT — measure before you defer**; 5 of 9 spec tasks were FALSE PREMISES. Doctrine = §21.8. → [infra audit](project_seo_infrastructure_audit_2026_07_31.md) · [indexable-route gate](feedback_indexable_route_gate.md)
## 🎯 LIVE 2026-07-12 — DE+FR LONGTAIL (270 de · 242 fr). ⚠ every NEW activity needs `generate-activity-og-images.js --only=<id>` + scp BEFORE deploy; re-run `patch-nginx-minitools-noindex.py` after any nginx rebuild. → [DE](project_de_longtail_application.md) · [FR](project_fr_longtail_application.md) · [og:images](project_activity_og_images.md)
## 💳✅ LIVE 2026-07-12 — LEMON SQUEEZY PAYMENTS: webhook secret must match; LIVE webhook subscribes `subscription_*`; `syncUserTierForSubscription` bridges legacy tier. → [LS payments](project_ls_payments_live_2026_07_12.md) · [sharing](project_activity_sharing.md) · [homepages](feedback_native_locale_homepage_build.md)
## 🛑🎨 FEEDBACK 2026-07-10 — STORYBOOK MEANS STORY (every page develops the story; storyTurn gate) → [storybook](feedback_storybook_means_story.md) · MECHANIC VARIETY (from #7 pull ≥3 verb families, ≤50% of pages) → [variety](feedback_story_mechanic_variety.md)
## 🖼️ DONE 2026-07-06 — WORKSHEETS HUB + 429 FIX: carve out any extension-less fan-out endpoint from bot-defense. → [hub + 429](project_worksheets_hub_redesign_429_thumbnails.md)
## 🔑 SERVER ACCESS (2026-07-03): `ssh -i %USERPROFILE%/.ssh/id_ed25519 root@65.108.5.250`; DB secret in `/opt/lessoncraftstudio/.deploy-env`; runbook `docs/ops/server-access.md`.
## 💰 STRATEGY — Master Roadmap rev.2 (2026-07-03) = SoT: free discovery, paid depth; GAMES CANCELLED FOREVER; Teacher $69/yr / Family $39. → [Business strategy 2026-07](project-business-strategy-2026-07.md)
