# K-353 `tangram` (K/G2) : FINAL design (editor merge, 2026-09-21)

Merged from `_work/K-353-pedagogy.md` + `_work/K-353-design.md` (record of every ruling: `_work/K-353-critic.md`). (m) = re-measured 2026-09-21 by read-only node: the four scratch enumerators `K-353-final.js` (30 tilings: area, overlap, connectivity, bbox, silhouette symmetry group), `K-353-unique.js` + `K-353-unique3.js` (the lattice tiler with its two controls), `K-353-seams.js` (the vertex-alignment enumerator, superseded where it disagrees), plus `frontend/lib/seo/strand-names.ts:58-70`, `scripts/seo-landing/gen-b2-landings.js:51-63`, `scripts/worksheet-gen/page/page.css:119-162,220,405-410`, `templates/layouts/card-grid.js`, `templates/components-b3/picture-word-cards.js:73`, `primitives/_tokens.js:68-70`, `frontend/config/topics-taxonomy.json`; the render `K-353-figures.png` was OPENED by the design author and by the editor. *est.* = the engineer measures in the real render with the shell woff2 from `file://` (`render/one.js`). No em-dashes.

**Boundary (load-bearing).** This family owns COMPOSITION and DECOMPOSITION of the fixed 7-tan set (2 large + 1 medium + 2 small triangles, 1 square, 1 parallelogram; areas 1/4 1/4 1/8 1/16 1/16 1/8 1/8 = 1, m): cutting the set, laying it on a model or a shadow, drawing the seam that splits an outline into 2-3 tans, finding which tan fills a hole, telling the solution that stands like its shadow from turned or flipped copies, and counting the tans of a kind inside a built sub-figure. It never asks sides / corners / faces / vertices (`geometry` G2-241..248, G3-337..342, `types/_shared/geometry-tasks.js` count-sides / solid-counts modes), never symmetry yes-no or draw-the-other-half (G2-247/248/253, `sym-grid`), never copies a pixel picture on a grid (`grid-copy` K-286 family), never fills a picture-row gap (K-068 `visual-logic`), never cut-sort-pastes pictures (`cutting` K-240/264), never same-area on a unit grid (G3-338), never a `shapes`-theme naming task (`shapes` is a theme slug in all 11, m; no title carries it). Shape names are never printed: the count face uses DRAWN glyphs. Every closed-answer drawing is a STORED tiling from `data/b4/tangram-figures.js`; no face invents a silhouette, a hole or a seam at render. Visual signature: cream tans with 3 px teal seams, solid-teal shadows, one scale (S = 216 px) for every page where the child lays pieces, no words on the body in any locale.

## 1 Identity

| field | value |
|---|---|
| id / key / bands | `K-353` / `tangram` / base **K**; F1 F5 **K** (`K-358+ TBD by the emitter`); F2 F3 **G1** (`G1-354+ TBD`); F4 **G2** (`G2-347+ TBD`). `default_subject: spatial-reasoning` (README table; the pedagogy's `math` is overruled, §16.4 closed set), `default_age_range: 5-7`, `assetClass: geometry`, `exerciseType: tangram`. `apps.tangram` + `axes['exercise-type'].tangram` ABSENT and the string `tangram` occurs 0 times anywhere in `topics-taxonomy.json` (m): register `{default_subject:'spatial-reasoning', default_age_range:'5-7', exercise_type_axis_key:'tangram'}` + slug `tangram` x11 (0 collisions by construction) + name x11 (table B). |
| theme axis | **OFF**: `themeAxis:{applicable:false}`, 0 pictures on any face, `coordinate.theme:''`. Not `withFixedTheme` (nothing is drawn from the library), not `b3-picture-index` (nothing is ever needed). Every drawing is the NEW palette-only primitive `primitives/tangram.js` (§2). **No `unitAxis`** (the set is fixed; "4 / 5 fichas" is F1's query face, not a unit fan). |
| CCSS (en, honest) | base **K.G.B.6** (compose simple shapes to form larger shapes) · F1 **K.G.B.6** (its own example, two triangles make a bigger shape) · F5 **K.G.A.2** (name shapes regardless of orientation or size; K.CC.B.5 in prose only) · F2 **1.G.A.2** · F3 **1.G.A.2** (the composite minus one part) · F4 **readiness, NO code** (orientation discrimination has no K-2 code; 2.G.A.1 is not what the child does): `teaches` = "Spatial reasoning: the same figure, turned or flipped (readiness)", no `educationalAlignment`. Strand row `'Geometry'` (`strand-names.ts:58-70`, all 11 present, m: es prints "Forma, espacio y medida", da "Geometri og måling"). Non-EN prose names the national framework only. |
| new primitives / data | `primitives/tangram.js` (§2, geometry + node gate `tools/gate-tangram-figures.js`), `templates/components-b4/tangram.js` (7 components, behind the `templates/components-b4.js` barrel: ABSENT, the build clones the b3 barrel), `data/b4/tangram-figures.js` (GLOBAL, locale-neutral, hand-authored, node-gated; `data/` is gitignored, `git add -f`). No per-locale bank exists for this type: the only localized text is the 6 titles + 6 instructions in `i18n/strings.<loc>.json`. |
| scale law | min tan edge = small-triangle leg = square side = 0.3536 S (m). Floors `_tokens.js:68-70`: K 56 → S >= 158.4; G1 44 → S >= 124.5; G2 36 → S >= 101.8. **Family scale S = 216** on base, F1, F2 (small edge 76.4 px = 20.2 mm; one cut set fits every page); F3 S = 128 (45.3 >= 44), F4 S = 102 (36.1 >= 36), F5 S = 160 (56.6 >= 56). The pedagogy's per-band floors 159 / 125 / 102 are the SAME law rounded up; they are kept as the gate's assertions, S = 216 satisfies every band. |

| loc | genre head (panel) | ASCII slug | level K / G1 / G2 (`LEVEL_KEYS`, m) | piece / shadow / template words (panel literals, titles + instructions only) | national strand (framework NAME) |
|---|---|---|---|---|---|
| en | Tangram Puzzles | `tangram` | `kindergarten` / `grade-1` / `grade-2` | pieces / shadow / cut-out set; "printable" and "pdf" in the meta only; never "with answers" | Geometry |
| de | Tangram-Figuren (title head; "Tangram-Vorlage" meta only, the engine appends "Arbeitsblatt") | `tangram` | `vorschule` / `1-klasse` / `2-klasse` | Teile / Schattenbild / Vorlage; nouns capitalised; hyphenated compounds | Lehrplan Mathematik: Raum und Form |
| es (MX) | Tangram (+ "para recortar" / "figuras") | `tangram` | `preescolar` / `primer-grado` / `segundo-grado` | piezas (NEVER "fichas" beside "tangram": it is the worksheet word) / sombra / para recortar; "tangrama" meta only; "recortar" not "cortar" | SEP/NEM: Forma, espacio y medida |
| pt (BR) | Tangram (+ "para recortar e montar") | `tangram` | `educacao-infantil` / `1o-ano` / `2o-ano` | peças / sombra / para montar; "atividade" never in a title | BNCC (EF01MA14 / EF02MA15 in prose) |
| fr | Tangram (+ "à découper" / "modèles") | `tangram` | `maternelle` / `cp` / `ce1` | pièces / ombre / modèle; "fiche" never in a title; NBSP before "?" and ":"; "taille réelle" is BANNED on d2 (57 mm set) | programmes cycle 1-2: Espace et géométrie |
| it | Tangram (+ "da ritagliare" / "figure") | `tangram` | `infanzia` / `classe-prima` / `classe-seconda` | pezzi / ombra / da ritagliare; "scheda" never in a title; "10x10" BANNED on d2 | Indicazioni nazionali: Spazio e figure |
| nl | Tangram (+ "printen" / "figuren") | `tangram` | `kleuters` / `groep-3` / `groep-4` | stukken / schaduw / printen (never "afdrukken"); the engine appends "werkblad" | SLO kerndoel 32, Meetkunde |
| sv | Tangram (+ "mall" / "figurer") | `tangram` | `forskola` / `ak-1` / `ak-2` | bitar / skuggbild / mall ("mall" is correct Swedish in a title); never "grupp"; the engine appends "arbetsblad" [NSR] | Lgr22 Geometri |
| da | Tangram (+ "skabelon" / "opgaver") | `tangram` | `boernehaveklasse` / `1-klasse` / `2-klasse` | brikker / skygge / skabelon; the da SERP is German/Swedish-polluted: every title unmistakably Danish [NSR] | Fælles Mål: Geometri og måling |
| no | Tangram (+ "mal" / "figurer") | `tangram` | `1-trinn` / `2-trinn` / `3-trinn` | brikker / skygge / mal; bokmål; the engine appends "oppgaver" [NSR] | LK20 (utforske og beskrive figurer) |
| fi | Tangram (+ "malli" / "kuviot") | `tangram` | `esikoulu` / `1-luokka` / `2-luokka` | palat / varjo / malli; "tangram" indeclinable in titles ("Tangram-malli"); instructions decline the PARTS ("seitsemästä palasta"), never the word tangram [NSR] | OPS 2014 Geometria |

`axes['exercise-type'].tangram.name.<loc>` = "Tangram" x11 (en "Tangram Puzzles", de "Tangram-Figuren"; a panel may refine). Harvest (panel): en 72 (A-) · de 49 (A-) · es 64 (A) · fr 64 (A) · pt 38 (A-) · it 39 (A-) · nl 24 (B+) · sv 21 (B) · da 34, mostly leakage (B-) · no 20 (B-) · fi 13 (C+). Swedish suggestions surface in the da and no SERPs: **hreflang siblings across the six faces must be exact (same face = same cluster)** or the sv landing cannibalises da/no.

**The rule that locks the type.** Every drawing on every face is a STORED placement of the 7 tans (or a declared subset) from ONE locale-neutral bank, placed by `{id, x, y, rot, flip}` on the √2 lattice, validated at load (7 ids, area, no overlap, edge-connected, silhouette, symmetry group, unique seam for compose outlines); `rng` only picks WHICH stored entries, which rotation and which order. Open faces (base, F2) print stored figures and carry no `verify()`; closed faces (F1 F3 F4 F5) re-derive every answer from the bank in node, never from the DOM. The only text on the body is the numbered card badge; the child's language is never on the apparatus.

## 2 The base page

**Concept.** "Tangram Puzzles: Cut Out the Pieces and Build Two Figures." Top-left, the 216 px cut-out square with its seven pieces drawn as cut lines (3 px teal on cream) and a scissors glyph; beside it a five-row colouring legend (swatch + piece glyph, no words); below, two figures at the SAME scale drawn WITH their seams (the K model the child copies line for line: fr "reproduire un assemblage de formes", de "Vorlage mit Lösung"). The pedagogy's EMPTY outlines lose at K: covering a bare outline is F2's G1 move, and a K page whose model shows the seams is the honest teaching page. The legend is kept (colour-coded classes are what every classroom set looks like; the codeColors are SVG fills the palette lint accepts).

**Chrome budget (README ruling).** Body **722** with 3-line title + 3-line instruction (814 one-line; 677 with a 4-line fi title). `.ws-page` inner 675. `.ws-body` flex column, `justify-content:space-evenly`, gap 16; rows `minmax(<min>px,1fr)`; slack opens between the three objects, never inside them. Every stack below is quoted at 722 and checked at 677.

**Layout d2**
```
row 1 (246 tall)  [scissors strip 30: scissorsGlyph(26) at x 0, data-lcs-scissors]
                  +-- template 216 x 216 --+  28  legend 5 rows x 30, gap 6 = 174 (v-centred)
                  | 7 cream tans, teal 3 px |      [swatch 20 r5][8][L glyph 64x32]  codeRed
                  | seams + outer edge      |      [swatch][8][M glyph 32x32]         codeBlue
                  | (classic square tiling) |      [swatch][8][S glyph 32x16]         codeYellow
                  +-------------------------+      [swatch][8][Q glyph 23x23]         codeGreen
                  block 216 + 28 + 92 = 336, left   [swatch][8][P glyph 48x16]         codeOrange
row 2 (375 tall)        +-- figure A <= 312 x 375 --+  24  +-- figure B <= 312 x 375 --+   centred, <= 648
                        | seams:true (2 px teal)    |      | seams:true                 |
                        +---------------------------+      +----------------------------+
30 + 216 + 16 + 375 = 637 <= 722 (slack 85)  and  <= 677 (slack 40)
```
- Template = `tangramFigure({figure:'square', S:216, mode:'template'})`: 7 `<polygon>` `T.cream` fill, `T.teal` 3 px (`stroke.primitive`), `stroke-linejoin:round`; the outer edge is the same 3 px teal (cut lines = 3 px teal on cream). `cutLines` NOT used (rectangular dashed grid; a tangram is cut on diagonals). The seven inner cuts are straight, edge-to-edge or vertex-to-vertex; the child never cuts a notch. Small leg 76 px = 20 mm; the K-240 104 px floor was ruled for cutting AROUND card tiles, not for straight cuts through a square (critic ruling; the print check below is the mitigation).
- Legend = `tanLegend({rows:[L,M,S,Q,P], Sg:64})`: swatch `<rect 20x20 rx5 fill=codeColors[x] stroke T.ink 1>` (the `colorLegend` swatch idiom, `components-b2.js:95-101`; no word), then `tanGlyph(cls, 64)`. Sizes are RELATIVE by construction (the S glyph is half the L glyph): that is how a child tells big from small without a word. The legend is reference chrome, not a task element; the K 56 px floor is asserted on the apparatus (template + figures), not on legend glyphs.
- Figures: `rng.sample` 2 distinct keys from the base pool `{tree, boat, arrow, cat, rectangle}` (every entry <= 1.414 wide and <= 1.707 tall at S 216: widths 312 / 312 / 312 / 273 / 312, heights 312 / 375 / 312 / 327 / 156, all +3 stroke, m; 2 x 312 + 24 = 648 <= 675). `square` is excluded from row 2 (it is the template already on the page); `rabbit` (381 tall), `triangle` / `trapezoid` / `parallelogram` (> 430 wide) do not fit two-up. `mode:'solution'`: cream tans, 2 px teal seams (`stroke.grid` + 0.5), 3 px teal outline. Seed locale-neutral (same two figures in all 11).
- Every SVG root: `data-ws-content` (the QA blank-page selector), `data-lcs-prim="tangram"`, `data-lcs-figure`, `data-lcs-mode`, `data-lcs-scale`, `data-lcs-tans=<n>`. No text inside any drawing; orientation and size words are never printed.

**Ladder** (resolved config keys; guards key on THESE, never the level index):

| key | d1 | d2 (ships) | d3 |
|---|---|---|---|
| `S` (template) | **378** (100 mm: the de "Originalgröße" / fr "taille réelle" / it "10x10" sheet) | 216 | 216 |
| `figures` / pool | 1 / `['rectangle']` (535 x 267 at 378; 30 + 378 + 16 + 273 = 697 <= 722) | 2 / `tree boat arrow cat rectangle` | 2 / same pool |
| `seams` (on the figures) | true | true | **false** (solid shadows: the K hard page) |
| `legend` | true | true | true |
| smallest tan edge | 134 px | 76 | 76 |

**d3 ruling:** d3 = the base at `seams:false` is a genuinely harder K page (shadow, not model) but it is NOT a face (F2 owns shadows at G1 with four figures and no template; `gate-variation-distinct` would still pass it, the doctrine does not). d1 is the only true-size sheet; no d2 copy may claim true size (the wave ships d2 = 57 mm).

**Composer.** `figures = rng.sample(BASE_POOL, 2)`; each drawn at `rot:0` (the stored pose; the base never rotates a model). Legend order fixed L M S Q P (a reading order, not an answer).

**Answer-hiding + uniqueness.** OPEN: no `verify()`; the model IS the page. Layout gate (§5): root stamps `data-lcs-set` with 7 `data-lcs-tan` polygons that re-derive to `SET`; two `data-lcs-figure` keys distinct and in the pool; `data-lcs-scale >= 159` on every drawing; the scissors glyph present once; legend rows 5 with distinct `data-lcs-legend` classes and only `codeColors` fills.

**Reused (exact).** `svgRoot roundedRect line circle el esc label` (`primitives/_svg.js`) · tokens `color / codeColors / stroke / density` (`_tokens.js`) · `scissorsGlyph(26)` (`components-b3/picture-word-cards.js:73`, exported by the b3 barrel, m; the pedagogy's "file-local in K-240" is stale) · `answerBox({w,h,answer})` (`components.js:105`; `.ws-answerbox` white, 2 px dashed `#C8BFAE`, r 10, `page.css:220`) · `cardGrid({cards,cols,rows,numbered})` (`layouts/card-grid.js`: `.ws-cardgrid` gap 14, `page.css:119-124`; `.ws-card` cream, border 2 `#F0E4CB`, r 14, padding 12, `:126-132`, so a card's inner = cell - 28) · `.ws-achip` (white, 2.5 teal, r 24, `page.css:406-410`) · `.ws-card-badge` (30, the only numeral glyph on any face) · `rng.pick / sample / shuffle / int`. **NOT used.** `cutLines` · `pillChoice` (text pills; the page is wordless) · `colorLegend` as a whole (prints a colour word) · `iconRows / sceneStage / entriesFor / fileUri` (no pictures) · `blankNumeralBox` (every F5 box HAS an answer) · `grid-copy.js` / `sym-grid.js` · `image-vocabulary.js`, `objForms`, `approved-words-*.json` (never read by this type).

**NEW `primitives/tangram.js`** (pure SVG on tokens; Node-testable). Unit square side 1, SVG axes (y down), `h = Math.SQRT2 / 4 = 0.353553`. Local polygons, reference vertex at the origin:

| id (class) | local polygon (unit) | area | kind |
|---|---|---|---|
| `L1` `L2` (L) | (0,0) (1,0) (0.5,0.5) : hyp along +x, apex +y | 0.25 | triangle |
| `M` | (0,0) (0.5,0) (0,0.5) : right angle at the origin | 0.125 | triangle |
| `S1` `S2` (S) | (0,0) (0.5,0) (0.25,0.25) | 0.0625 | triangle |
| `Q` | (0,0) (0.25,0.25) (0,0.5) (-0.25,0.25) : diamond, vertex at the origin | 0.125 | square |
| `P` | (0,0) (0.25,0.25) (0.25,0.75) (0,0.5) : long sides vertical; **CHIRAL** | 0.125 | parallelogram |

Placement `{id, x, y, rot, flip}`: `flip` mirrors local x before rotation (legal on `P` only; the gate rejects it elsewhere); `rot` in 45° steps applies (x cos - y sin, x sin + y cos) (clockwise on screen); then translate (x, y); then multiply by S. Every vertex of every placed tan is a + b√2 with integers a, b in units of S/4 (m: every tan vertex has an even a1 - a2, so a 45° turn stays on the lattice); the pedagogy's ring-coordinate insight is therefore a PROPERTY the gate relies on (1e-9 tolerances are safe), not a storage format: the bank stores the measured decimal form verbatim (§5) and the loader never converts.

API: `placeTans(tiling, S)` → `[{id, cls, pts:[[px,py]..], area}]` (px, origin = bbox min minus the stroke pad `sp = stroke/2 + 1`) · `silhouette(tiling)` → `[loop]` (unit coords; the edge-cancellation tracer that traced all 30 stored tilings, m: orient every polygon positive, split every edge at every vertex lying on it, drop equal-and-opposite segment pairs (6-decimal keys, |v| < 5e-7 snapped), chain survivors taking the LARGEST signed turn at a fork (fixes pinch vertices, e.g. the boat's hull/sail corner), drop collinear vertices; assert loop area = tan-area sum at 1e-9) · `tangramFigure({figure|tans, S, mode, missing, stroke})`, modes `template` (cream tans, 3 px teal seams + edge) · `solution` (cream tans, 2 px teal seams, 3 px teal outline; `data-lcs-tan` + `data-lcs-class` per polygon) · `outline` (one `<path>` of the silhouette, WHITE fill, 3 px teal, NO tan polygons in the DOM) · `silhouette` (one `<path>`, `T.teal` fill, 3 px teal stroke, round joins, no seams, no polygons) · `hole` (solution minus `missing`: six polygons `T.tealSoft` fill + 2 px teal seams, the hole `<path data-lcs-hole>` on page cream with a 3 px dashed `8 6` coral edge and NO fill; root `data-lcs-missing=<class>`) · `transformTiling(tiling, t)` for `t in {id, rot90, rot180, rot270, mirX, mirY, mirD, mirA}` (the 8 lattice symmetries; mirrors toggle `flip` on `P` AND mirror its placement; re-bbox) · `symmetryGroup(tiling)` → the subset of the 8 whose silhouette equals the original's as a point set after bbox normalisation (6 decimals) · `tanGlyph(cls, Sg, {flip})` → inline svg icon in the canonical pose: L hyp horizontal apex up (Sg x Sg/2), M hyp horizontal apex up (Sg/2 x Sg/4 x √2: at Sg 64 = 45 x 23), S hyp horizontal apex up (Sg/2 x Sg/4), Q axis-aligned square (0.354 Sg), P long sides horizontal leaning right (0.75 Sg x 0.25 Sg; `flip` mirrors it); cream fill, 2 px teal; `data-lcs-glyph=<cls> data-lcs-flip` · `tanChip(cls, S, {flip})` → the SAME canonical pose but TRUE-SIZE at the figure's S (F3 chips: congruent to the hole) · `tanLegend({rows, Sg})`. Root of every figure: `svgRoot` sized bbox x S + 2 sp.

**NEW `templates/components-b4/tangram.js`**: `templateBlock({S, legend})` · `figureRow({figures, S, seams})` · `composeCard({mini, S, Sg})` (legend strip of `tanGlyph`s + `outline`) · `shadowCard({figure, S})` (`silhouette`, card padding 6 override) · `missingCard({figure, missing, S, chips})` (figure in `hole` mode left, `.ws-achip` column right: chips `tanChip` true-size, box 104 wide x (glyph h + 12, min 44), `data-lcs-chip=<cls> data-lcs-flip`) · `matchRow({figure, S, candidates})` (cream target box 160 + 3 white boxes 160, 2.5 teal, r 14) · `countCard({sub, S})` (`solution` over the answer row `[tri glyph 22][6][answerBox 64x50][16][sq glyph 22][6][answerBox 64x50]` = 200; glyphs are drawn `<polygon>`s, never Unicode; the square glyph is axis-aligned, the figure's `Q` is tilted: that difference IS K.G.A.2).

**Alternatives (rejected).** Alt A one true-size template (100 mm) and nothing else: satisfies the "Originalgröße / taille réelle / 10x10" tail literally but is not a worksheet and leaves the K child nothing to lay; kept as d1. Alt B the pedagogy's S 272 square + two EMPTY true-size outlines: 96 px small leg is still under the K-240 scissor floor it cited, an empty outline is F2's move, and 272 cannot be the family scale (a 2 x 2 shadow grid holds 1.414 x 216 + 3 = 309 <= 310 at padding 6; at 272 nothing two-up fits), so the base set would not fit F2's shadows. Alt C shadows and template on one page: four shadows plus the square force S ~ 125 (a 33 mm square) and the pieces match no other page. Rejected.

**Risks → mitigations.** Cut size 57 mm (under the 100 mm sets teachers know): the landing never claims true size; d1 is the true-size sheet; the print check (engineer, mono laser, 100 %): cut one template with child scissors, colour a small triangle, lay the set on the tree. Greyscale: teal shadows print near-black, cream tans on white with teal seams print light grey with dark lines, the coral dashed hole survives as a dashed line, the codeColors swatches become greys but each sits beside its glyph. Pencil space: F1 outlines are WHITE (not cream) so a pencil seam shows. Fit: every bbox is S-arithmetic + 3 px stroke; the render gate re-measures each `<svg>` client rect against its container at 722 AND 677. Figure recognisability is a HUMAN check: the editor read `K-353-figures.png`: tree (the former "house"), arrow, boat, cat read; rabbit reads weakly (two ears on a stepped body) and is flagged for the panels; a new figure is a data edit PLUS a looked-at render. **Palette:** cream, creamDeep, white, teal, tealSoft, coral, ink, inkSoft, grid + the 8 `codeColors` on legend swatches. **Font floor:** no body text; the smallest drawn thing is the F4 tan at S 102 (36 px edge). Every root stamps `[data-ws-content]`.

## 3 Faces 2-6

All five are CODE faces on ONE additive `mode` knob (`d.mode` undefined = the base path, byte-identical; a face stamps `data-lcs-mode` only when declared); each adds a `verify()` branch (F2 excepted: open); guards key on the config (`d.mode`, `d.S`, `d.items`, `d.pool`, `d.chips`, `d.candidates`, `d.boxes`), never on the level index. Resolved d2 configs are pairwise distinct and distinct from the base (`tools/gate-variation-distinct.js --batch=b4`; the script exists, its b4 branch is the build's clone of the b3 branch, m). None is PARAM: each changes the apparatus a teacher sees (template + models / outlines + glyph legends / four shadows / holed figures + chips / shadow + three solutions / sub-figures + answer boxes). Density: K faces 4-8 asks at S >= 159 (F1 4 seams, F5 8 numerals); G1 faces 6-12 at S >= 125 (F2 4 open builds, F3 6 circles); G2 face 8-16 at S >= 102 (F4 9 comparisons). Every stack is quoted at 722 and checked at 677.

### F1 : Easy Tangram Puzzles: Draw the Lines for 2 and 3 Pieces (K, CODE `mode:'compose'`)
**Move:** the SEAM is the answer: a glyph strip shows the 2-3 tans, the child DRAWS in pencil the line(s) inside the empty white outline where those pieces meet (K.G.B.6's own example). Single-answer ONLY on outlines whose tiling by the listed tans is proven UNIQUE by the lattice tiler (m). The child may lay real pieces on the outline first (it is true-size to the base set) and then draw. **Child:** "Look at the pieces above each shape. Draw the lines inside the shape to show where those pieces meet." (104). **Layout:** `cardGrid 2x2 numbered`: cells 330 x 354, inner 302 x 326 (677: inner 303); card = `[glyph strip 44: tanGlyph at Sg 64, gap 10][12][outline at S 216, white fill, 3 px teal]`; outline boxes (+3, m): `m-from-2s` 111 x 111 · `p-from-2s` 165 x 57 · `trap-m-s` 111 x 165 · `bigtri-2l` 219 x 219 · `trap-l-m` 219 x 111 · `trap-q-2s` 165 x 165 · `kite-l-q-s` 165 x 219 · `pent-l-s-p` 232 x 156; tallest stack 44 + 12 + 219 = **275 <= 326** (<= 303 at 677); widest 232 <= 302. `para-2l` (327 wide) is excluded at S 216. **Pool (m, `K-353-unique.js` + `-unique3.js`, brute force over lattice positions x 8 rotations x P flip, counted modulo swapping congruent tans; controls `sq-from-2s` = 2 and `rect-m-2s` = 2 behaved):** two-tan UNIQUE `m-from-2s` (S+S) · `p-from-2s` (S+S) · `trap-m-s` (M+S) · `bigtri-2l` (L+L) · `trap-l-m` (L+M) [· `para-2l` (L+L), unique, too wide at d2]; three-tan UNIQUE `trap-q-2s` (Q+S+S) · `kite-l-q-s` (L+Q+S) · `pent-l-s-p` (L+S+P). **REFUSED, two tilings (m):** `l-from-2s-q` (the large triangle from Q+S+S: the square sits axis-aligned in the corner OR as a diamond off the apex; the pedagogy's enumerator reported 1 and is superseded) · `sq-from-2s` (either diagonal) · `rect-m-2s`. **Config d2** `{mode:'compose', S:216, Sg:64, items:4, cols:2, rows:2, pool2:['m-from-2s','p-from-2s','trap-m-s','bigtri-2l','trap-l-m'], pool3:['trap-q-2s','kite-l-q-s','pent-l-s-p'], mix:[2,2]}`; d1 `{items:3, cols:3, rows:1, mix:[3,0], S:216}` (two-tan only; 3 x 219 + 28 = 685 > 675 → d1 pool excludes `bigtri-2l`/`trap-l-m`: three of `m-from-2s`, `p-from-2s`, `trap-m-s`) ; d3 `{items:4, mix:[1,3]}`. **Composer:** `rng.sample` 2 of `pool2` + 2 of `pool3`, `rng.shuffle` the four cards, each outline drawn at a `rng.pick` of the 8 lattice transforms (the seam is transform-invariant); glyph strip in a fixed class order L M S Q P. **Verify:** each card `data-lcs-mini=<key>` in `MINIS` with `unique:true`; glyph multiset (`data-lcs-glyph`) === the mini's classes; the outline `<path d>` vertex list === `silhouette(transformTiling(mini, t))` at the stamped scale (re-derived in node); NO `data-lcs-tan`, no `data-lcs-seam` in the DOM; keys distinct per page; `mix` honoured; `data-lcs-scale >= 159`. **Refusals:** none x11 (wordless). **Query face:** "easy / 2 and 3 pieces": en "easy tangram puzzles 2 pieces" (WINNABLE) · de "einfache Tangram-Figuren" · es "tangram de 2 y 3 piezas" (the "4 fichas" tail lands here) · pt "tangram fácil 2 peças" · fr "tangram facile 2 pièces" · it "tangram facile" · nl "makkelijke tangram" · sv "enkla tangram" · da "nemme tangram" · no "enkle tangram" · fi "helppo tangram".

### F2 : Tangram Puzzles: Four Shadows to Build (G1, CODE `mode:'silhouette'`; OPEN, no verify)
**Move:** SOLVE four solid shadows with the seven pieces (1.G.A.2): no template, no seams, no legend; the shadow is true-size to the base set. The pedagogy's printed KEY (a 7-tan square at S 125) loses on arithmetic: 125 + 2 x 309 + 24 = 767 > 675 at the family scale, and a key on the page turns the shadow page into a copy page. **The instruction names the child's tangram set, a physical material this family makes on the base sheet: a recorded, deliberate exception to "apparatus on the page" (critic ruling 1); every F2 landing says "use the pieces from the cut-out sheet or any tangram set".** **Child:** "Build each dark figure with the seven pieces of your tangram. Use all seven pieces every time." (93). **Layout:** `cardGrid 2x2 numbered` with an inline `padding:6px` override on `.ws-card` (inner 314 x 338 at 722; **314 x 315 at 677**; the design's padding 8 gave 310 x 311 at 677 = a 1 px overflow on the tree, m); shadows at S 216 + 3: `tree` 309 x 309 · `arrow` 309 x 309 · `cat` 273 x 327 · `rectangle` 309 x 156 · `square` 219 x 219; **`cat` (327) fits at 722 only** → the d2 pool at 677 is `tree arrow rectangle square` (the gate resolves the pool from the measured body height: `cat` enters when body >= 722 - 11; the engineer confirms the fi title line count at build). `boat` (378 tall), `rabbit` (381), `triangle` (435 wide), `trapezoid` / `parallelogram` (461) are excluded at S 216 by bbox. **Config d2** `{mode:'silhouette', S:216, items:4, cols:2, rows:2, pool:['tree','arrow','cat','rectangle','square'], padding:6}` (4 of 5 per seed, `rng.sample`; each at a `rng.pick` of the 8 transforms so the same figure is not always upright); d1 `{items:2, cols:2, rows:1, S:216, pool: all 10}` ; d3 `{items:4, S:196, pool: + boat 283 x 341, rabbit 196 x 343}`. **Layout gate (no verify):** 4 distinct `data-lcs-figure` keys, all in the pool; every shadow one `<path>` with fill === stroke === `T.teal` (no seam, no `data-lcs-tan` in the DOM); each `<svg>` client rect inside its card inner at 722 AND 677; `data-lcs-scale >= 125`. **Refusals:** none x11. **Query face:** "figures / shadows / modèles": en "tangram animals printable" is NOT honestly this page until the bank holds >= 5 animals with bbox <= 1.414 (today 2: `cat`, `rabbit`, and `rabbit` does not fit at 216: critic OPEN 2); so the face carries "tangram figures / shapes to build": de "Tangram-Figuren legen Schattenbilder" · es "tangram figuras para armar" · pt "tangram figuras para montar" · fr "modèles tangram CP" · it "tangram figure" · nl "tangram figuren" · sv "tangram figurer" · da "tangram figurer" · no "tangram figurer" · fi "tangram kuviot".

### F3 : Tangram Puzzles: Which Piece Is Missing? (G1, CODE `mode:'missing'`)
**Move:** DECOMPOSITION by congruence: a stored figure printed with SIX tans and one HOLE; the child circles, among three chips of pairwise distinct CLASSES, the one that fills the hole exactly (shape AND size, under rotation). Chips are TRUE-SIZE at the figure's S (`tanChip`), so "fits exactly" is congruence and size is the lesson (the design's Sg 80 glyph chips at another scale lose: a child cannot use size, and "another triangle size" would be meaningless). **Child:** "One piece is missing from each figure. Circle the piece that fits the empty space exactly." (89). **Layout:** `cardGrid 2x3 numbered`: cells 330 x 231, inner 302 x 203 (**677: 302 x 188**); card = `[figure in hole mode at S 128 (unit 32 px, min edge 45.3 >= 44)][12][chip column 104 wide]`: figure boxes (+3, m) `tree` 184 x 184 · `arrow` 184 x 184 · `cat` 163 x 195 · `rectangle` 184 x 93 · `square` 131 x 131 (`cat` 195 > 188 → **excluded at 677**; `boat` 221, `rabbit` 227, `triangle` 259 wide excluded at 128); 184 + 12 + 104 = **300 <= 302**. Chips in the canonical pose (every triangle hyp-horizontal, apex up) at S 128: M 90.5 x 45 · S 64 x 32 · Q 45 x 45 · P 96 x 32 (+ 3 px stroke); chip box = glyph h + 12 (min 44): M 60 · Q 60 · S 44 · P 44; worst column M + Q + S/P = 60 + 60 + 44 + 2 x 8 = **180 <= 188** (the design's upright-M column of 193 overflowed the 677 stack, m). **Hole classes at d2: `M S Q P`** (an L chip is 128 wide and 64 tall: it breaks the column; L holes enter at d3 with `bboxW <= 1.0` figures). **Chirality rule (the parallelogram is CHIRAL: its edge-turn sequence has no mirror-by-rotation, m):** when the hole is `P`, the correct chip is drawn in the SAME `flip` as the hole (a rotation maps chip onto hole; the child never turns the paper over); when `P` is a distractor it may take either flip. **Distractor rule:** 3 chips of pairwise distinct classes, exactly one = the hole's class; when the hole is a triangle (M or S) at least one distractor is a triangle of ANOTHER size (so "triangle" alone does not answer); no chip is congruent to the hole except the correct one (guaranteed by distinct classes). **Config d2** `{mode:'missing', S:128, items:6, cols:2, rows:3, pool:['tree','arrow','cat','rectangle','square'], holes:['M','S','Q','P'], chips:3, chipW:104}`; d1 `{items:4, cols:2, rows:2, S:160, holes:['S','Q'], chips:3}`; d3 `{items:6, S:128, holes:['L','M','S','Q','P'], pool:[bboxW <= 1.0 figures for an L hole], chips:3, mirrored:true}` (half the cards drawn `mirX`: the hole is read in a mirrored context). **Composer:** 6 (figure, hole) pairs, no pair twice, >= 4 distinct hole classes per page, figures `rng.pick` transform per card (any of the 8; the hole's `flip` follows the transform). **Verify:** root `data-lcs-figure data-lcs-missing=<class> data-lcs-transform data-lcs-scale`; six `data-lcs-tan` ids + the hole === the figure's 7 (node: `FIGURES[key].tans` minus the missing id under the stamped transform); the hole `<path>` area === class area x S^2 within 1 %, no fill, dashed coral stroke; chips `data-lcs-chip=<class> data-lcs-flip` with exactly one `data-lcs-correct` whose class === `data-lcs-missing`, classes pairwise distinct, chip polygons TRUE-SIZE (chip edge lengths === the class's edges at S within 0.5 px), the triangle-size rule, `P`-correct chip `flip` === the hole's flip; >= 4 hole classes per page; no (figure, class) twice; `data-lcs-scale >= 125`. **Refusals:** none x11. **Query face:** en "which piece is missing tangram" · de "Welches Teil fehlt?" · es "¿Qué pieza falta?" · pt "Qual peça falta?" · fr "Quelle pièce manque ?" · it "Quale pezzo manca?" · nl "Welk stuk ontbreekt?" · sv "Vilken bit saknas?" · da "Hvilken brik mangler?" · no "Hvilken brikke mangler?" · fi "Mikä pala puuttuu?" (the four Nordic panels named this face verbatim).

### F4 : Tangram Puzzles: Which Solution Matches the Shadow? (G2, CODE `mode:'match'`)
**Move:** ORIENTATION DISCRIMINATION: a solid shadow on the left; three drawn solutions of the SAME figure on the right; exactly one stands exactly like the shadow (`transform:'id'`); the others are TURNED or FLIPPED copies whose shadow differs. The pedagogy's inverted task (the TURNED copy is correct) is overruled by the lock ("match silhouette to solution among rotated/mirrored distractors"). **The distractor law (the lock, states the chirality rule exactly):** a distractor's transform must lie OUTSIDE the figure's measured silhouette symmetry group, else its shadow equals the target's and it is a second right answer. Groups (m): `square` all 8 (REFUSED for this face) · `rectangle` {id, rot180, mirX, mirY} · `tree` {id, mirX} · `arrow` {id, mirY} · `triangle` / `trapezoid` {id, mirX} · `parallelogram` {id, rot180} · `boat` / `cat` / `rabbit` {id}. A mirror of a mirror-symmetric figure (tree, triangle, trapezoid) is NEVER a distractor (same shadow, only the parallelogram inside flips); a mirror of an asymmetric figure (cat, boat, rabbit) is, and the flipped `P` is the visible tell. **Child:** "Look at each shadow. Circle the solution that stands exactly like the shadow. Turned or flipped ones do not match." (108). **Layout:** 3 rows `minmax(160px,1fr)`, gap 14: 3 x 160 + 28 = **508 <= 722** (and 677); row = `[cream box 160 x 160, the shadow][10][white box 160 x 160, 2.5 teal, r 14] x 3` = 160 + 3 x 170 = **670 <= 675**. S 102 (G2 floor 101.8, m): max figure dimension 1.5 x 102 + 3 = 156 = box inner (160 - 2 x 2). **Pool (m; needs max dim <= 1.5 AND >= 2 transforms outside the group):** `tree` (rot90 rot180 rot270 mirY mirD mirA) · `arrow` (rot90 rot180 rot270 mirX mirD mirA) · `rectangle` (rot90 rot270 mirD mirA; two distractors share the standing shadow, accepted: the seams differ) · `cat` (all 7); `square` refused; `boat` / `rabbit` / `triangle` / `trapezoid` / `parallelogram` too large at 102 (max dim 1.707..2.121). **Config d2** `{mode:'match', S:102, rows:3, candidates:3, box:160}`; d1 `{rows:2, candidates:2, S:130, box:200}`; d3 `{rows:4, candidates:3, S:102}` (4 x 160 + 42 = 682 <= 722; refused at 677 → d3 is not shipped). **Composer:** 3 distinct figures per page from the pool of 4; per row the target at a `rng.pick` transform `t0` (the shadow itself may stand turned), the correct candidate = the same tiling at `t0`, two distractors = `t0` composed with two distinct transforms outside the group; candidate order `rng.shuffle` (the correct one never in the same position on all three rows). **Verify:** per row `data-lcs-figure data-lcs-transform`; candidates `data-lcs-candidate data-lcs-t` with exactly one `data-lcs-correct` whose `t === 'id'` relative to the target; every distractor `t` NOT in `symmetryGroup(figure)` (node re-derives the group from the bank, never trusts a stored list); the DOM candidate polygons re-derive to `transformTiling(figure, t0 ∘ t)`; the shadow is one `<path>` with fill === stroke; `P`'s orientation sign flips on every mirror candidate; figures distinct per page; `data-lcs-scale >= 102`. **Refusals:** none x11. **Query face:** "find the solution / turned or flipped": en "tangram puzzles solutions grade 2" (the "with answers" tail is a recorded LOSS: no page claims an answer key) · de "Tangram-Figuren gedreht oder gespiegelt 2. Klasse" · es "tangram girado y reflejado segundo grado" · pt "tangram 2º ano" · fr "tangram CE1" · it "tangram classe seconda" · nl "tangram groep 4" · sv "tangram åk 2" · da "tangram 2. klasse" · no "tangram 3. trinn" · fi "tangram 2. luokka".

### F5 : Tangram Puzzles: Count the Triangles and Squares (K, CODE `mode:'count'`)
**Move:** COUNT by KIND inside a built sub-figure: triangles at three sizes and any tilt (K.G.A.2), squares (0 or 1, and the tilted `Q` is still a square while `P` is not). A FULL figure always holds 5 triangles and 1 square, so full figures are a memorised constant, never a card (the lock). The pedagogy's second box "pieces" is overruled by the lock ("triangles / squares"); the band is K (K.G.A.2 is a K code; 8 answers = the K ceiling), overruling the design's G1. **A second single-answer rule the inputs missed (m by reading the renders):** a sub-figure whose OUTLINE is itself a triangle or a square (`half-h`, `smallsq`, `bigsq`, `bigtri-2l`) is EXCLUDED, else a K child honestly counts the outline too. **Child:** "Count the triangles in each picture and write the number in the first box. Then count the squares for the second box." (117). **Layout:** `cardGrid 2x2 numbered`: inner 302 x 326 (677: 303); card = `[sub-figure in solution mode at S 160 (unit 40, min edge 56.6 >= 56)][12][answer row 200 x 50]`: figure <= 326 - 62 = 264 tall at 722 and **<= 241 at 677**, <= 302 wide; pool bboxes (+3, m): `cat-head` 83 x 123 · `rabbit-head` 83 x 163 · `tree-4tri` 229 x 229 · `rabbit-body` 163 x 163 · `arrow-head-q` 173 x 229 · `trap-q-2s` 123 x 123 · `kite-l-q-s` 123 x 163 · `pent-l-s-p` 173 x 116 (all <= 241: the pool is 677-safe; `boat-sail-hull-m` 276 tall and `cat-no-tail` (5 triangles, 6 tans) are d3). Answer row `[tri glyph 22][6][answerBox 64x50][16][sq glyph 22][6][answerBox 64x50]` = 200 <= 302; boxes 50 >= 44; nothing printed in a box. **Pool with counts (m, `K-353-final.js`; tans / triangles / squares):** `cat-head` 3/2/1 · `rabbit-head` 4/2/1 · `tree-4tri` 5/4/0 · `rabbit-body` 3/3/0 · `arrow-head-q` 4/3/1 · `trap-q-2s` 3/2/1 · `kite-l-q-s` 3/2/1 · `pent-l-s-p` 3/2/0 = 8 sets; triangle values {2, 3, 4}; squares {0, 1}; `P` present in `rabbit-head`, `tree-4tri`, `pent-l-s-p`. **Page rule:** 4 distinct sets; triangle counts take >= 3 distinct values; both square values 0 and 1 appear; `P` on >= 2 cards (the not-a-square decoy); >= 1 card with a triangle at a tilt not a multiple of 90; each card drawn at a `rng.pick` transform. **Config d2** `{mode:'count', S:160, items:4, cols:2, rows:2, boxes:['triangles','squares'], tans:[3,6], triangles:[1,4], pool: the 8 above}`; d1 `{items:4, S:176, tans:[3,4], triangles:[1,3]}`; d3 `{items:6, cols:2, rows:3, S:144, tans:[3,7], triangles:[2,5], boxes:['triangles','squares','pieces'], pool: + cat-no-tail, boat-sail-hull-m, one full figure allowed}`. **Verify:** card `data-lcs-sub=<key> data-lcs-transform data-lcs-scale`; `data-lcs-answer` of box 1 === number of stored tans with class in {L, M, S}, box 2 === class Q count, re-derived from the bank in node (never from the DOM polygons); the set's silhouette has neither 3 vertices nor is a square (node); page rule; no numeral or word anywhere but the card badges; `data-lcs-scale >= 159`. **Refusals:** none x11 (no label, no case issue). **Query face:** en "how many triangles in a tangram" · de "Tangram Dreiecke zählen" · es "tangram cuántos triángulos" · pt "tangram quantos triângulos" · fr "combien de triangles tangram" · it "quanti triangoli nel tangram" · nl "tangram driehoeken tellen" · sv "räkna trianglarna" · da "tæl trekanterne" · no "tell trekantene" · fi "laske kolmiot".

**Rejected non-moves.** "Square from two triangles" as a closed answer (2 seams, m) · the large triangle from Q+S+S (2 seams, m) · counting a full 7-tan figure (constant 5/1) · counting a sub-figure whose outline is a triangle or square (two honest answers) · a rotated copy of the base ("theme swap" of orientation) · a colour-the-tans page as a face (the legend already sits on the base) · shape NAMING chips over the set (`geometry` / `shapes` theme) · a 4- or 5-piece "mini set" as a separate type (it is F1's query face) · the "turned copy is correct" inversion of F4 (contradicts the lock; the pedagogy's version) · a printed key on F2 (does not fit; turns a solve page into a copy page) · captions under F2 shadows (11 x ~10 figure names to author and open for a wordless family) · d1 / d3 relabelled as faces (d3 `seams:false` is the same move on a shadow).

## 4 Native rebuild plan x11

The page is WORDLESS: the panel authors exactly 12 strings (6 titles + 6 instructions) and the landing copy; no figure name, no label, no `{noun}` slot, no inflected frame exists in this type; `objForms` is never consulted. The EN is a SOURCE TO AUDIT (the #41 rule). Each instruction is one or two imperatives naming only the template, the pieces, the legend, the figures / shadows, the lines, the empty space, the boxes; F2 alone names the child's tangram set (the recorded exception).

| loc | literals | forms / slots | refusal | traps |
|---|---|---|---|---|
| en | 12 | none | none | "Tangram Puzzles" head, "puzzles" plural; never "printable" / "pdf" / "with answers" in a title; US spelling ("color" on the base) |
| de | 12 | none | none | title head "Tangram-Figuren legen" (panel), "Vorlage" meta only (engine appends "Arbeitsblatt"); nouns capitalised (Teile, Dreiecke, Quadrate, Schattenbild); "Teil" vs "Stück" = panel call (harvest: "Teile") |
| es (MX) | 12 | none | none | "piezas" on the sheet and in titles; "fichas" NEVER beside "tangram"; "tangrama" meta only; "recortar"; "sombra" for the shadow |
| pt (BR) | 12 | none | none | "peças"; "para montar" is the compose head; "atividade" never in a title; "sombra" |
| fr | 12 | none | none | "pièces"; "modèle" = the figure card word; "ombre" for the shadow; "fiche" never in a title; NBSP before "?" ":"; "taille réelle" BANNED (d2 = 57 mm); "MHM" meta only if the panel confirms |
| it | 12 | none | none | "pezzi"; "figure" (F2); "ombra"; "scheda" never in a title; "10x10" BANNED on d2 |
| nl | 12 | none | none | "de tangram"; "printen" never "afdrukken"; "stukken" (set) vs "figuren" (shadows); "schaduw"; engine appends "werkblad" |
| sv | 12 | none | none | "tangram" invariant; "mall" allowed in a title; "bitar" not "brikker"; "skuggbild"; NEVER "grupp"; engine appends "arbetsblad" [NSR] |
| da | 12 | none | none | "brikker"; "skabelon" (da) vs "mal" (no) vs "mall" (sv): keep per locale; "skygge"; every title unmistakably Danish (SERP leakage); engine appends "opgaver" [NSR] |
| no | 12 | none | none | "brikker", "mal", "figurer", "skygge"; bokmål; engine appends "oppgaver"; K = `1-trinn` (Nordic +1) [NSR] |
| fi | 12 | none | none | "Tangram-malli" / "Tangram-kuviot" compounds; instructions decline the PARTS ("seitsemästä palasta", elative), never "tangram"; "varjo"; engine appends "tehtäviä" [NSR] |

Every panel OPENS one render of every face in its locale (no library picture exists to open) and reads the six figures used (tree, boat, arrow, cat, rectangle, square) before writing landing prose that names any of them (a shape that reads differently in one culture is described, not named).

## 5 Data + gates

**`data/b4/tangram-figures.js`** (GLOBAL; hand-authored; validated AT LOAD like `data/b2/figures.js`; no per-locale overlay; `git add -f`). The bank stores the placements exactly as measured (`K-353-final.js`), with `h = Math.SQRT2 / 4` and two reusable half-assemblies:
```
const h = Math.SQRT2 / 4;                        // small leg = square side
const T = (id, x, y, rot, flip) => ({ id, x, y, rot, flip: !!flip });
const SMALLSQ = (ox, oy) => [T('M',ox+h,oy+h,45), T('Q',ox+2*h,oy,45), T('S1',ox+2*h,oy+2*h,225), T('S2',ox+h,oy,135), T('P',ox+h,oy,45)];   // 5 small tans as a square of side 2h
const H = (ox, oy) => [T('M',ox,oy+1,270), T('Q',ox+.5,oy+.5,0), T('S1',ox+1,oy+1,180), T('S2',ox+.25,oy+.75,270), T('P',ox,oy,0)];        // 5 small tans as the classic half
FIGURES = {   // key: placements                                                     bbox (unit)   symmetry group (m)   used by
  square:        [T('L1',0,0,0), T('L2',1,0,90), ...H(0,0)],                        1 x 1         all 8               base template, F2, F3
  rectangle:     [T('L1',0,2*h,315), T('L2',2*h,0,135), ...SMALLSQ(2*h,0)],        1.414 x 0.707 id rot180 mirX mirY  base, F2, F3, F4
  triangle:      [T('L1',0,0,90), T('L2',0,1,180), ...H(0,0)],                     2 x 1         id mirX             d1 / d3 only
  tree:          [T('L1',-2*h,2*h,315), T('L2',0,0,45), ...SMALLSQ(-h,2*h)],       1.414 x 1.414 id mirX             base, F2, F3, F4  (was "house": reads as a fir tree)
  arrow:         [...SMALLSQ(0,0), T('L1',2*h,-h,45), T('L2',4*h,h,135)],          1.414 x 1.414 id mirY             base, F2, F3, F4
  boat:          [T('M',0,2*h,225), T('Q',0,0,315), T('S1',2*h,0,135), T('S2',0,h,225), T('P',-2*h,0,315), T('L1',.5,0,180), T('L2',-.5,0,270)],  1.414 x 1.707  id   base
  cat:           [T('Q',.25,0,0), T('S1',0,.25,270), T('S2',.5,-.25,90), T('L1',.5,.25,90), T('L2',.5,1.25,180), T('M',-.5,.75,0), T('P',.5,1.25,180,true)],  1.25 x 1.5  id   base, F2 (722), F3 (722), F4
  rabbit:        [T('Q',.25,0,0), T('P',0,.25,180,true), T('S1',.5,-.25,90), T('S2',.25,0,270), T('L1',.5,.25,90), T('L2',.5,1.25,180), T('M',-.5,.75,0)],  1 x 1.75  id   d3 only
  trapezoid:     [...SMALLSQ(2*h,0), T('L1',0,2*h,315), T('L2',4*h,0,45)],         2.121 x 0.707 id mirX             d1 only
  parallelogram: [...SMALLSQ(2*h,0), T('L1',0,2*h,315), T('L2',6*h,0,135)],        2.121 x 0.707 id rot180           d1 only
};
MINIS = {      // F1 outlines; unique:true PROVED by the tiler at load (never authored)
  'm-from-2s':  [T('S1',0,0,0), T('S2',.5,0,90)]            'p-from-2s': [T('S1',0,0,0), T('S2',.75,.25,180)]
  'trap-m-s':   [T('M',0,0,0), T('S1',.5,0,180)]            'bigtri-2l': [T('L1',0,0,0), T('L2',1,0,90)]
  'trap-l-m':   [T('L1',0,0,0), T('M',1,.5,180)]            'para-2l':   [T('L1',0,0,0), T('L2',1.5,.5,180)]   (d3 / wide)
  'trap-q-2s':  pick(square, [Q,S1,S2])   'kite-l-q-s': pick(square, [L2,S1,Q])   'pent-l-s-p': pick(trapezoid, [L1,S2,P])
  REFUSED at load (must report 2): 'l-from-2s-q' [T('S1',..),T('S2',..),T('Q',..)] · 'sq-from-2s' · 'rect-m-2s' (the three controls)
};
SUBS = {       // F5 count sets: pick(<figure>, ids); tans / triangles / squares derived at load
  'cat-head': pick(cat,[Q,S1,S2])  'rabbit-head': pick(rabbit,[Q,P,S1,S2])  'tree-4tri': pick(tree,[L1,L2,S2,P,M])  'rabbit-body': pick(rabbit,[L1,L2,M])
  'arrow-head-q': pick(arrow,[L1,L2,Q,S1])  'trap-q-2s'  'kite-l-q-s'  'pent-l-s-p'  (d2 pool, 8)
  'cat-no-tail': pick(cat,[Q,S1,S2,L1,L2,M])  'boat-sail-hull-m': pick(boat,[L1,L2,M,S2,Q])  (d3)
  EXCLUDED from F5 by the outline rule: 'half-h' pick(square,[M,Q,S1,S2,P]) (triangle outline) · 'smallsq' · 'bigsq' pick(rectangle,[L1,L2]) (square outlines) · 'l-q' (2 tans)
};
SET = FIGURES.square;   BASE_POOL = ['tree','boat','arrow','cat','rectangle'];   F4_REFUSED = ['square'];
```
`pick(fig, ids)` filters the figure's placements (a sub-figure keeps its parent's coordinates, so every count set is a real fragment of a real figure). Derived by the loader for every entry (never authored): vertex lists, `bbox`, `area`, `silhouette`, `symmetryGroup`, `chiral` (= group has no mirror), `outlineKind` (`triangle` | `square` | `other`), `counts {tans, triangles, squares}`, and for MINIS `unique` (the lattice tiler: every placement of every listed class on the lattice `(a + b√2)/8`, 8 rotations, `P` flip, kept when the clip area equals the tan area; tilings counted modulo swapping congruent tans). Helper contract: `figure(key)`, `mini(key)`, `sub(key)`, `poolFor(face, cfg, bodyH)` (filters by bbox against the face's measured container at the given body height) all throw on a missing key.

**`tools/gate-tangram-figures.js`** (node; runs in `deploy.sh` beside the b3 gates; refuses to run on an empty bank; every rule throws): (1) every `id` in `TANS`; a FIGURE has exactly the 7 ids once; a SUB 3..6 distinct ids; a MINI 2..3 ids within the multiplicities (<= 2 L, 1 M, 2 S, 1 Q, 1 P). (2) `rot % 45 === 0`; `flip` only on `P`. (3) Area: FIGURES and `SET` sum to 1 (1e-9); SUBS / MINIS to the sum of their tans. (4) No two tans overlap: pairwise clip area < 1e-9 (touching allowed). (5) Edge-connected: shared collinear opposite segments > 1e-6 form a connected graph; corner-only touching does not count. (6) Silhouette: one loop, area = tan area sum; `symmetryGroup` recorded and DIFFED against the table above (a silent change of a figure changes F4's distractor set). (7) MINIS: `unique === true` or the entry does not load; the tiler tests containment against a possibly CONCAVE outline (point-in-polygon on every vertex + no proper edge crossing + centroid inside, or triangulate), because the scratch's convex clipper returned 0 on the concave heads (a tool limit, m); poison: `l-from-2s-q` must report 2, `sq-from-2s` 2, `rect-m-2s` 2, `m-from-2s` 1. (8) F4 eligibility: `square` refused; every pool figure has >= 2 transforms outside its group. (9) F5: `outlineKind === 'other'` for every d2 set; triangle counts in 1..4 and tans in 3..6; the pool covers >= 3 triangle values and both square values. (10) Per-face pool floors at the SHIPPED body heights 722 and 677: base >= 2 (5), F1 >= 4 with >= 2 in each of pool2 / pool3 (5 + 3), F2 >= 4 (5 at 722, 4 at 677), F3 >= 4 figures x 4 hole classes (5 / 4), F4 >= 3 (4), F5 >= 4 (8). (11) keys unique across FIGURES + MINIS + SUBS, ASCII slug shape; every key used by a face exists. (12) No `name`, no label, no string in any locale lives in this file.

**`tools/validate-b4-draft.js` (tangram block; exit 1 on any):** (1) `strings` ids === {`K-353` + the 5 allocated face ids}; titles <= 70, no worksheet-word (`WORKSHEET_WORD` regex of the b3 gate), unique in band, pairwise differing by a noun or verb token (never only an adjective); no title equals `axes.theme.shapes.name/slug.<loc>` (K-322 rule) or contains the locale's "printable" word. (2) Instructions <= 150; F1 / F3 / F4 / F5 name no off-page apparatus (a lint list per locale: no "key", "template", "legend" outside the base); F2 may name the tangram set. (3) BANNED strings per locale: "with answers / mit Lösungen / con respuestas / com respostas / avec corrigé / con soluzioni / met antwoorden / med facit / med facit / med fasit / vastauksineen" (poison-tested BOTH ways: "Finde die Lösung" must PASS); the locale's "true size" phrase on any d2 string ("taille réelle", "Originalgröße", "10x10", "tamaño real", "tamanho real", "ware grootte", "naturlig storlek", "naturlig størrelse", "luonnollinen koko"); es "ficha" adjacent to "tangram"; sv "grupp"; the visible free-word. (4) Every string NFC, no `{`, no em-dash.

**`qa/verify-b4-tangram.js`:** renders base + 5 faces x 11 locales x d2 through `render/render-instance.js` (file:// fonts) under the 722 stack (3-line de title + 150-char instruction) AND the 677 stack (4-line fi title); asserts `verify()` empty and `qa/lints.js` clean (overflow `:37-45`, footer `:50-66`, font `:86`, palette: tokens + `codeColors` on legend swatches only); then the floors ITSELF (no size lint exists): smallest rendered tan edge from the stamped polygon points >= 56 on K faces / 44 on G1 / 36 on G2, `data-lcs-scale` >= 159 / 125 / 102; every `<svg>` client rect inside its card inner or box; F3 chip boxes >= 44 tall, chips true-size (edge ratio chip : hole = 1 within 0.5 px); F5 boxes 64 x 50 empty; F4 rows `scrollWidth <= clientWidth`; no `<img>`, no `<text>` inside any tangram svg, no numeral outside `.ws-card-badge`; every `[data-lcs-answer]` re-derived in node from the bank; base seams 2 px on the models and 3 px on the template; F2 shadow paths fill === stroke; F1 outlines fill white and no `data-lcs-tan`. SWEEP: 20 seeds x d2 per face render distinct pages (base pair varies; F1 mini set and transforms vary; F2 set varies; F3 hole classes cover all 4 across seeds; F4 correct position varies across rows and seeds; F5 triangle values never constant). **Poison (each must FAIL for its OWN reason; the correct bank + EN draft are the control):** P1 a figure with `S1` twice and no `S2` → rule 1 · P2 two tans overlapping 0.5 unit² → rule 4 · P3 tans touching at a corner only → rule 5 · P4 MINI `sq-from-2s` loaded as `unique:true` → rule 7 (tiler reports 2) · P5 F4 page using `square` → rule 8 · P6 an F4 distractor `rot180` on `rectangle` (inside its group) → verify · P7 an F4 candidate `data-lcs-correct` with `t:'rot90'` → verify · P8 an F3 card whose chips are two triangles of the same size → verify · P9 an F3 `P` hole with the correct chip in the other flip → verify · P10 an F3 chip drawn at Sg 80 instead of true size → the chip-ratio floor · P11 an F5 page whose four cards all hold 3 triangles → page rule · P12 `half-h` on F5 d2 → rule 9 (outline is a triangle) · P13 a full figure on F5 d2 → tans 7 > 6 · P14 a render at S 150 on a K face → floor 53 < 56 · P15 an F1 outline whose DOM carries one `data-lcs-tan` → verify · P16 a full figure of area 15/16 (a tan dropped) → rule 3 · P17 an F4 decoy congruent to the target under `rot90` (a second figure whose outline matches) → verify (the design's "decoy" option is not used at d2; the poison guards its d3 return) · P18 F2 rendered with `padding:12` under the 677 stack → the tree's client rect exceeds the card (overflow lint) · P19 F3 chips in the upright-M pose under 677 → column 193 > 188 · P20 a base d2 whose two figures are `square` + `tree` → pool rule · P21 a de F4 title "Tangram-Figuren mit Lösungen" → validator rule 3 (control: "Finde die Lösung" passes) · P22 fr base instruction containing "taille réelle" → rule 3 · R1 a face rendered at d2 with the base's own resolved config → `gate-variation-distinct.js` rejects · R2 a shadow with a 1 px stroke and cream fill (seams visible) → fill !== stroke · R3 an F4 row with two `data-lcs-correct` → verify. **Page reads:** `data/b4/tangram-figures.js`, `primitives/tangram.js`, `i18n/strings.<loc>.json`; never `image-vocabulary.js`, `objForms`, `b3-picture-index`, `approved-words-*.json` or any image cache at render.

## 6 SEO plan

| face | title pattern (Germanic / Romance / Nordic + fi; drafts the panels rebuild) | meta MIDDLE (whole 120-170; the child's instruction, en) | coordinate |
|---|---|---|---|
| base | "Tangram Puzzles: Cut Out the Pieces and Build Two Figures" (57) · "Tangram-Figuren legen: 7 Teile ausschneiden und zwei Figuren" · "Tangram printen: 7 stukken en twee figuren" / "Tangram para recortar: 7 piezas y dos figuras" · "Tangram para recortar e montar: 7 peças e duas figuras" · "Tangram à découper : les 7 pièces et deux modèles" · "Tangram da ritagliare: i 7 pezzi e due figure" / "Tangram mall: 7 bitar och två figurer" · "Tangram skabelon: 7 brikker og to figurer" · "Tangram mal: 7 brikker og to figurer" · "Tangram-malli: 7 palaa ja kaksi kuviota" | Color each piece like the legend, cut out the seven pieces along the lines, then lay them on the two figures below, line for line | `{type:'tangram', mode:'base', theme:'', level:<K key>}` |
| F1 | "Easy Tangram Puzzles: Draw the Lines for 2 and 3 Pieces" (54) · "Einfache Tangram-Figuren aus 2 und 3 Teilen: Linien zeichnen" · "Makkelijke tangram met 2 en 3 stukken: teken de lijnen" / "Tangram fácil de 2 y 3 piezas: dibuja las líneas" · "Tangram fácil com 2 e 3 peças: desenhe as linhas" · "Tangram facile à 2 et 3 pièces : trace les traits" · "Tangram facile con 2 e 3 pezzi: disegna le linee" / "Enkla tangram med 2 och 3 bitar: rita linjerna" · "Nemme tangram med 2 og 3 brikker: tegn stregerne" · "Enkle tangram med 2 og 3 brikker: tegn strekene" · "Helpot tangram-kuviot 2 ja 3 palasta: piirrä viivat" | Look at the pieces above each shape and draw the lines inside the shape to show where those pieces meet | `mode:'compose'`, K |
| F2 | "Tangram Puzzles: Four Shadows to Build" (38) · "Tangram-Figuren: vier Schattenbilder legen" · "Tangram: vier schaduwen om te leggen" / "Tangram: cuatro sombras para armar" · "Tangram: quatro sombras para montar" · "Tangram : quatre ombres à reproduire" · "Tangram: quattro ombre da costruire" / "Tangram: fyra skuggbilder att lägga" · "Tangram: fire skygger at lægge" · "Tangram: fire skygger å legge" · "Tangram: neljä varjokuviota rakennettavaksi" | Build each dark figure with the seven pieces of your tangram and use all seven pieces every time | `mode:'silhouette'`, G1 |
| F3 | "Tangram Puzzles: Which Piece Is Missing?" (40) · "Tangram-Figuren: Welches Teil fehlt?" · "Tangram: welk stuk ontbreekt?" / "Tangram: ¿qué pieza falta?" · "Tangram: qual peça falta?" · "Tangram : quelle pièce manque ?" · "Tangram: quale pezzo manca?" / "Tangram: vilken bit saknas?" · "Tangram: hvilken brik mangler?" · "Tangram: hvilken brikke mangler?" · "Tangram: mikä pala puuttuu?" | One piece is missing from each figure; circle the piece that fits the empty space exactly | `mode:'missing'`, G1 |
| F4 | "Tangram Puzzles: Which Solution Matches the Shadow?" (50) · "Tangram-Figuren: Welche Lösung passt zum Schattenbild?" · "Tangram: welke oplossing past bij de schaduw?" / "Tangram: ¿qué solución es igual a la sombra?" · "Tangram: qual solução é igual à sombra?" · "Tangram : quelle solution correspond à l'ombre ?" · "Tangram: quale soluzione è uguale all'ombra?" / "Tangram: vilken lösning passar skuggan?" · "Tangram: hvilken løsning passer til skyggen?" · "Tangram: hvilken løsning passer til skyggen?" · "Tangram: mikä ratkaisu on sama kuin varjo?" | Look at each shadow and circle the solution that stands exactly like it; turned or flipped ones do not match | `mode:'match'`, G2 |
| F5 | "Tangram Puzzles: Count the Triangles and Squares" (48) · "Tangram-Figuren: Dreiecke und Quadrate zählen" · "Tangram: tel de driehoeken en vierkanten" / "Tangram: cuenta los triángulos y los cuadrados" · "Tangram: conte os triângulos e os quadrados" · "Tangram : compte les triangles et les carrés" · "Tangram: conta i triangoli e i quadrati" / "Tangram: räkna trianglarna och kvadraterna" · "Tangram: tæl trekanterne og firkanterne" · "Tangram: tell trekantene og kvadratene" · "Tangram: laske kolmiot ja neliöt" | Count the triangles in each picture and write the number in the first box, then count the squares for the second box | `mode:'count'`, K |

All titles <= 70, no worksheet-word, unique per band; the base owns the bare head + the set; each face adds ONE element (draw the lines / four shadows / missing piece / matching solution / count), never only an adjective. `coordinate.mode` is ALWAYS the face's mode string (base `'base'`), `theme:''`, level = the K key for base / F1 / F5, G1 for F2 / F3, G2 for F4 (no: `1-trinn` / `2-trinn` / `3-trinn`). h1 = title; eyebrow = level label; strand chip = the `'Geometry'` row (framework NAME only in prose). JSON-LD `LearningResource` with `educationalAlignment` = the CCSS code per face for en (F4: none, `teaches` readiness), non-EN without a code. Meta lead inherits `seo.words.free_printable` (metadata only; no visible copy says "free"; "printable" / "pdf" live in the meta, never in a title; the MIDDLE is the instruction, measured per locale with `tools/measure-instruction-window.js`, exists, m). `topicMeta.tangram` >= 50 x11 + `skill-sentences.<loc>.json.tangram` via the `register-b4-en-content.js` clone. **Answer key: NONE.** Printable decks ship no answer key (`manifest.printable_only:true`, schema 1.1; README open item 6); the en "with answers" (7 strings), de "mit Lösungen" and sv "lösning" tails are REAL demand and a recorded LOSS: no title, meta, landing or hub card may promise one (validator rule 3). F4 is the one page that honestly SHOWS solutions, and says so as a task ("which solution matches"), never as a key. **hreflang:** the six faces form six exact cross-locale clusters (same mode = same cluster) so the sv landing does not cannibalise da / no.

**Non-cannibalisation** (whole-landing 3-gram Jaccard, `scripts/seo-landing/gate.js` FAIL >= 0.80 / WARN 0.65; *est.*, the gate measures):

| pair | what the copy says differently | est. |
|---|---|---|
| base vs F2 | a cut-out template + two seamed models to copy, K vs four solid shadows and no template, grade 1 | 0.30 |
| base vs F1 | cut and lay vs draw the seam inside an empty outline with a two-piece legend | 0.25 |
| F1 vs F3 | draw a line where two pieces meet vs circle the one piece that fills a hole | 0.22 |
| F3 vs F4 | a hole and three true-size chips vs a shadow and three solutions at other orientations; the highest pair (both "which fits"), sharpened by the orientation lexicon (turned / flipped) on F4 only | 0.28 |
| F2 vs F4 | build with pieces, nothing written vs circle, nothing built; shadows shared by name only | 0.22 |
| F5 vs any | the counting lexicon (how many triangles, the tilted square is still a square); no other face counts | 0.15 |
| any vs geometry G2-241..248 / G3-338 | sides / corners / faces / same-area never mentioned | 0.08 |
| F4 vs symmetry G2-247 / 253 | "flipped" shares 2-3 grams; the copy says "stands like the shadow" and never "line of symmetry" or "mirror line" | 0.10 |
| base vs cutting K-240 / 264 | one square cut on seven straight lines vs cut-and-paste of pictures | 0.12 |
| base / F2 vs grid-copy K-286 | lay pieces vs copy squares on a grid | 0.06 |
| F3 vs visual-logic K-068 / missing-pieces | a hole in a built figure with true-size chips vs a picture-row gap | 0.10 |

Boundary sentence on every landing: "The child composes and decomposes the seven tangram pieces; no sides or corners are counted, no picture is copied on a grid, no mirror line is drawn, and no answer key is included."

## 7 Hub visibility contract

A face appears under `tangram` on `/[locale]/worksheets` IFF all four hold (brief): (1) `apps.tangram` exists in `frontend/config/topics-taxonomy.json` (ABSENT today, m; missing = rendered NOWHERE in the rail) with `default_subject:'spatial-reasoning'`; (2) `axes['exercise-type'].tangram` has `slug` (`tangram` x11) + `name` in all 11 locales (§1 table B); (3) exactly one landing per face per locale with `coordinate.type === 'tangram'` verbatim, `mode` per §6, `theme:''`, the level key of §1 (K for base F1 F5, G1 for F2 F3, G2 for F4), a unique slug (`deckSlugFor` = `tangram-<idlower>`) and `canonicalDeckSlug` = the published deck; a REFUSED face has NO landing and the expectation is lowered explicitly, never padded; (4) the landing JSON committed AND deployed (per-process cache).

Gate: `node scripts/verify-hub-type-rows.js --keys=tangram` (script EXISTS since nt20-C, m; expectations from `hub-expectations.json` exported from the README matrix; poison = drop one landing / drop `apps.tangram` must FAIL), then the All tab of `https://www.lessoncraftstudio.com/<loc>/worksheets?type=tangram` in a real browser for de, fi, es.

**Expected rows per locale:** en 6 · de 6 · es 6 · pt 6 · fr 6 · it 6 · nl 6 · sv 6 · da 6 · no 6 · fi 6 = **66**. No refusal is measured or foreseeable: the family is wordless (12 strings per locale, no noun, no slot, no picture), every pool is global and measured at both shipped body heights (base 5 figures, F1 5 + 3 minis, F2 4-5, F3 4-5 figures x 4 hole classes, F4 4 figures, F5 8 sets), and the only locale-sensitive strings are titles a panel rewrites. Contingencies, each recorded in the draft before the wave, none expected to fire: a panel refusing F2's "your tangram" instruction outright (then F2 ships with a 100 px inline piece-glyph strip in the title area and the expectation stays 6); the fi 4-line-title case on F2 / F3 drops `cat` from the pool (rows unchanged).
