---
name: project-homepage-v6-lesson-line
description: "Homepage v6→v9 redesign arc — v9 MORNING LESSONS, RUNNING is the live ruling: the working-apparatus ANIMATIONS are the product's signature, multiplied, never removed"
metadata: 
  node_type: memory
  type: project
  originSessionId: db802b44-8a35-4a83-a213-df384528d8d3
  modified: 2026-08-01T21:49:09.005Z
---

# Homepage v6→v9 — "Lesson Line" → "Installation" (rejected) → "OPEN HOUSE" (2026-08-01) → "MORNING LESSONS, RUNNING" (2026-08-02)

## ⭐⭐ v9 "MORNING LESSONS, RUNNING" — THE LIVE RULING (deployed 2026-08-02, commit f82bffe2)
**The operator's v8 verdict is a STANDING LAW: the pure-CSS working-apparatus ANIMATIONS are "the only creative elements" — they are the product's signature and must be MULTIPLIED, never removed.** v9 = v8's daylight + product density with ~13 working instruments (96 CSS animations): the FULL Calder mobile as hero (restored from commit 58457f61, amplitudes 1.2-3.2°, arm 3 + c-a-t word rack + dissolving drop-thread back); six tool cards ARE live machines (balance settles / clock turns / number-sieve dims to survivors / lid flips over counters / planks morph under a truth-keeping chevron / draw-bag pops a token + writes tallies); seam instruments between bands (beetle+trail on an arrow strip, thermometer, ten-frame — lg-only, ≤96px); one wall worksheet visibly CHECKS ITSELF (teal stamps); letter tiles + chalk rule in Make; traveler twist + self-drawing arrow + fluttering QR tag in Share; choral chart writes itself in Keep. Zero new i18n (tool names from MANIPULATIVES ×11; captions reuse penBalance/penTiles/penChoral).

**MOTION LAWS (standing):** transform/opacity only (clip-path solely on draw-on effects) · no keyframe moves >35% of its cycle (≥65% rest — THE concurrency budget lever) · coprime periods 7-23s + negative phases · one dominant motion per viewport (the fold's mobile is the page's only large one) · readable text/QR/tier surfaces never animate · reduced-motion = COMPOSED POSES mid-story, never blank · never stack two animations on one node (twist rides an inner wrapper) · containment by the 100cqw law (beetle walk + rekenrek push derive from their OWN container).

## 🎨 v9.2 "THE QUIET VERNISSAGE" (2026-08-02, commit 1d531d89) — the gallery layer
Operator commissioned a SLIGHT art-gallery atmosphere via expert agents (gallery-design lead + art curator; specs converged, synthesized). Six devices, ALL static or hover-only, zero new keyframes, ZERO new i18n (labels reuse localized deck titles + MANIPULATIVES names): (1) museum glazing on every .hv7-sheet mat — hairline frame ring + picture-light glint (inset top warm-white) + mat-window outline on the img + hover glass sheen (::after, @media hover:hover); (2) LEAN-IN LABELS on wall sheets — white slip bottom-left inside the mat, counter-rotated LEVEL via rotate(calc(-1 * var(--tilt))) (the levelness against the tilt is the gallery joke), hover/focus reveal ≤120ms, hidden <640px, and NO label on edge-bled sheets (text is never cut); (3) one static placard signing the close rekenrek (localized name, Nunito italic — italic sans legal, serif refused); (4) ONE wall-wash (warm radial, z-index:-1 inside the section's stacking context) over the Practice wall — the only wash that coincides with a band, deliberately; (5) vitrine light prepended to .hv9-vig-art; (6) the hang leveled: activity cards tilt so the FEATURED tile is the only dead-level work + one breathing seam (mt-14/16) before the wall.
**REFUSE-LIST (both experts, binding forever):** picture rails (banding + rejected scaffolding at 1px) · plinths · red dots · gold/wide frames · room labels/numbered works · fabricated tombstone lines (medium/year) · darkened rooms/vignettes/lightbox · serif label type · "curated by" · benches/wayfinding · any new animation (the machines stay the only performers).
⚠ Gate lesson: piping a background audit through `tail -N` swallows the FAIL lines — run gates untruncated.

## ⛔ v9.1 STANDING LAWS (operator, 2026-08-02 — permanent)
1. **NO NUMBERS FOR ANY KIND OF CONTENT** — no counts of worksheets/tools/activities/makers/images/curricula/languages, numeral or written out ("Two hundred–plus"), anywhere on marketing surfaces. Content grows; counts go stale. Express breadth in WORDS ("a full library", "for every exercise type", "in your language"). Exceptions: tier-quota facts (10 plays/3 PDFs — tier-truth law), "25 kids" classroom imagery, grade bands (K–grade 3), curriculum proper nouns (Lgr22/LK20/BNCC). Nine homepageV6 keys de-numbered ×11 via panels; {count}/{tools} params removed — grep-gate = 0 across locales.
2. **NOTHING VISUALLY CUT OFF at any viewport edge** — half-off-screen sheets read as breakage to the operator, never as "intentional bleed". Close-band flanks are IN FLOW (flex, hidden below xl) — flow beats absolute for never-cut. Gate covers 2560 ultrawide + per-width close-sheet containment + drop-thread∩fan assertions.
3. ⚠ **`.hv7-sheet { display:block }` silently defeated Tailwind `hidden`/`xl:block`** (single-class tie, later stylesheet wins) — the chalkpanel-position trap, third appearance. Default display now via zero-specificity `:where(.hv7-sheet)`. RULE: any layout-critical property on a shared single-class in homepage-v6.css must go through :where() or the utilities lose.
4. The fold shows NO drop-thread (it ran onto the fan worksheet — operator screenshot). The close band carries the signature rekenrek machine above the headline.

**Traps bought on v9:** ⚠ The mobile was inked for the DARK board — cream threads/knots vanish on paper; restoring it required a systematic daylight re-ink (slate rgba(90,80,60) lines, wood #C9A76F knots, soot→teal-tinted shadows). ⚠ `mx-auto` does NOT center a box WIDER than its column (auto margins → 0): center explicitly with margin-left calc(50% - half). ⚠ The hero column is 628px at EVERY width ≥1280 (container caps at max-w-6xl) — the 690px sculpture always needs scale 0.87. ⚠ The v6-era compact sculpture was designed for the 350px chalk panel: on a bare phone its clock cluster spilled 78px and the a3-carrier thread dangled to nothing — phone = deliberate two-arm form (beam+sheet+rekenrek; a cleanly ABSENT member beats a sawn-off one), measured contained 320-768. ⚠ Lazy CDN thumbnails outrun any fast scroll: eager wall row A; capture gates must force-eager ALL images before a full-page stitch or the stitch lies about the densest band. Gate grew: reduced-motion composed-pose pass (zero running CSSAnimations + sculpture visible), beetle-containment, animation census (≤220). 28/28 responsive PASS en+es · 220 links · 0 EN leaks.


## ⭐ v8 "OPEN HOUSE" — THE LIVE STANDING RULING (deployed 2026-08-01, commit `1d634a62`)
v7 dark Installation was REJECTED hard ("worse than ever… you haven't changed even the background colour"): ~65-70% flat dark green, product smallest thing on its own page. **v8 inverts it: daylight paper ground (`.hv7-ground` #FDFBF6 + cream/sage washes), the PRODUCT is the decoration** — fold fan of 5 real locale worksheets + ONE bounded 350px chalk panel (the surviving compact Calder mobile), full-bleed WALL of 12 decks + featured tile + 4 activity cards at 42% scroll, merged Make+Print band with the real maker UI **eager-loaded** (lazy-decode raced fast scrollers into an empty flagship frame), share sheet→arrow→QR handoff, saved-unit shelf on Keep, white plan cards w/ slim teal header strip, typographic close + cream class index. ~7,300px total.

**THE PROCESS RULE THAT MADE IT SHIP (standing):** build → gated screenshot capture (assert H1 pre+post scroll, HTTP 200, ALL /homepage/ + maker images decoded naturalWidth>0, zero pageerrors) → **visual-critic agent judges against TEN falsifiable laws** → iterate locally → deploy ONCE on SHIP verdict. Four critic rounds; the critic caught: 404 captures posing as finals, the empty builder frame, the blank play-card, 3 measured contrast fails (2.7-3.4:1), an artifact-free viewport. NEVER hand the critic an unverified capture.

**TEN LAWS (reusable):** ≤40% flat bg/crop · product dominates every viewport · daylight ≥L*85, dark ≤15% · page ≤~6,700px (earned overage OK) · zero non-product ornaments · text ≥4.5:1 · fold densest ≥8 artifacts · concrete count ≤2 viewports · no orphan geometry · artifacts ≥280px-or-absent + wall before 50% scroll.

**Traps bought this arc:** ⚠ EIGHT stale `next dev` processes (ports 3000-3006) shared one `.next` → hydration breaks, phantom 404/500s, corrupted webpack pack cache — kill all + wipe `.next` before trusting any dev capture. ⚠ dev-only: `/favicon.ico` 500 (app/ + public/ favicon conflict) compiles pages-router `/_error`, which POISONS the app-router graph (invalid hook call → every later SSR 500s) — block favicon requests in puppeteer captures. ⚠ `.hv7-chalkpanel{position:relative}` in late-loaded CSS beat Tailwind's `absolute` (single-class specificity tie → source order wins) — the class carries NO position; call sites position it. ⚠ Flex children without explicit widths inflate to natural image size (the QR card) — flex-none + fixed widths. ⚠ The responsive gate's `querySelector('.hv6-mob')` measured the display:none DESKTOP instance on phone widths → check ALL instances; and v8's designed collage bleed (`.hv7-fan`/`.hv7-overlap-x`/`.hv7-peek-l/r`/`.hv7-keepstack`, clipped by `.hv7-ground overflow-x:clip`) is exempted from the edge-breakout walk while the hscroll check stays the true overflow guard. Phone fixes: CTAs `sm:whitespace-nowrap` (es@320 overflow), share row `flex-wrap` + arrow `hidden sm:block`.

**Ship state:** critic SHIP verdict · 26/26 responsive widths PASS (en+es) · 220 internal links/locale · 0 EN sentinels on de/fi · 2 new hero keys (`hero.countsLine` w/ {tools} ICU, `hero.fanLabel`) authored natively ×11 by the RETAINED panel agents (da chose "Titusindvis" = tens of thousands; nl+da flagged EN "Thousands" as underselling ~45k — kept, operator rejected count-focus; it+nl flagged EN lexical drift tools/instruments — open polish). Curated `fallbackShowcase(18)` in `frontend/lib/showcase-decks.ts` (18 prod-verified EN decks w/ CDN thumbnail URLs) keeps every band honest when the local DB is down. BothWaysV6 + ToolVignette are OFF the page (files remain on disk).

## v7 "THE INSTALLATION" — REJECTED same day (historical)
One continuous dark board masthead→floorline; everything hung from wires/pegs/tags; 45s wire light-pulse. Rejected: dusk register, density inversion, scaffolding ornament. Its geometry law (one --var per suspension) and the anti-kitsch discipline fed the v8 laws. Phone price tags must hang straight (a full-width card at 1.5° breaks the viewport edge).

## v6/v6.1 "THE LESSON LINE" (historical substrate v8 still stands on)
Number-line-as-page concept; five moments not sections. **Still live in v8:** `frontend/components/homepage-v6/` component tree + self-contained `homepage-v6.css` (scope .hv6, ports the 3 .hv3-* classes FeaturedDeckTileV3 needs); preview `/[locale]/preview/homepage-v6` (noindex); `BrowseByTopicSSR` STRICTLY ADDITIVE variant='hv6' (link code byte-identical, default output untouched); `homepageV6` namespace ×11 locales (native panels, rebuild-not-translate, key-tree+ICU machine-verified); ClassroomMobile Calder hero (compact form inside the chalk panel; geometry law: every pivot/thread is ONE --var; sway prime periods, ≤0.8° compact); rekenrek containment via 100cqw-computed --push + overflow:clip.

**Traps honored (permanent):** ActivityIframe meters play on MOUNT → the live tool embed (LiveToolEmbedV6, rekenrek) is CLICK-gated — never auto/scroll-mount it. Plain `<a>` for /decks/ URLs (§15.7). Crawl-bait floor gated by `scripts/audit-homepage-link-count.js` (≥140). Counts/prices only via ICU params from code constants (TOOL_KEYS.length, SUBSCRIPTION_PRODUCT.priceUsd). Copy law: no 45,309, no per-language counts, no competitor framing, no efficacy claims. Tier truth per entitlements code before ANY plan copy ([[feedback-tier-truth-before-marketing-copy]]).

**Rollback:** revert `1d634a62` → v7 Installation; revert further per file history for v6.1.

Related: [[feedback-visual-qa-container-containment]] · [[project_homepage_redesign_2026_07]] (the superseded .hv5 pass)
