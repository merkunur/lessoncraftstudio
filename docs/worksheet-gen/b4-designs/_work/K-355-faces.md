# K-355 `five-senses` : the FIVE variation faces (Phase 2, 2026-09-21)

Built from `K-355-five-senses.md` §3 (+ §1 / §4 / §5 for the rulings, the bank and the gates) under `_FACE-BRIEF.md` + the README cross-type rulings, on the base substrate of `_work/K-355-build.md` (its "open items for the faces" all closed here: `_buildFace` for the five layouts, the face render sections + PR3-PR12 / PR14 in the gate). Ids from `_records/b4var-id-allocation.json` (all five G1). Nothing shared was edited; nothing committed. Every number below is measured (node against the real files, or puppeteer through `render/render-instance.js` with the shell's `file://` fonts). No em-dashes.

## Files (all family-scoped)
| file | what |
|---|---|
| `scripts/worksheet-gen/tools/b4var-rows/five-senses.js` | NEW: 5 rows (G1-357 G1-358 G1-359 G1-360 G1-361), `HANDWRITTEN` empty; every row spreads the base's d2 and sets the additive `layout` knob + its face keys, carries `extra {gradeBand:'G1'}`; the only file the emitter reads |
| `scripts/worksheet-gen/types/k/K-355-five-senses.js` | the Phase 2 stub replaced: `_buildFace` dispatches on `d.layout` to `_buildSort / _buildWhich / _buildOdd / _buildLabel / _buildWrite` (every guard on the RESOLVED config, never the level index); shared samplers `_newState / _sampleSense` (the family fence + no-noun-twice page-wide; fewer than `n` REFUSES) + `_organSrc`; `verify()` now reads the root's `data-lcs-layout` first and hands a face to `_verifyFace` (a second `page.evaluate`); the base's own evaluate, `_buildWith` base path, configs and `senseMatch` output are byte-untouched (baseline PASS). `C2` (components-b2 `wordBank`) + `tokens` (the G1 floor 44) are the two new requires |
| `scripts/worksheet-gen/templates/components-b4/five-senses.js` | `senseMatch` byte-untouched; `senseSortStage` gains the FIXED `.fs-zone` line-zone block (220..260, K-357 ruling) + `binH`, and `.fs-sort` is top-anchored (`flex:0 0 auto; justify-content:flex-start`, was `space-between`); `senseOddRow` boxes GROW to their fractional row (`height:min(boxMax,100%)`, square, picture 75 %); `organLabelRow` gains the d1 `starter`; NEW root wrappers `whichSenseStage`, `senseOddGrid` (the row sense stamped on the CARD: `cardGrid` cannot stamp a section), `organLabelStage`, `senseLaneStage`. The barrel accepted all 14 names (0 duplicates) |
| `scripts/worksheet-gen/types/g1/G1-357-five-senses-sort-by-sense.js` · `G1-358-five-senses-which-sense.js` · `G1-359-five-senses-which-one-does-not-belong.js` · `G1-360-sense-organs-write-the-sense-word.js` · `G1-361-five-senses-i-can-hear.js` | emitted by `node tools/gen-b4var-specs.js` (`emitted 5 variation specs + 0 handwritten`) |
| `scripts/worksheet-gen/data/b4/five-senses.js` | UNCHANGED: the EN `strings['G1-357'..'G1-361']` the base authored are the rows' titles + instructions verbatim (the gate asserts the pair is ONE source); no new bank material was needed (the faces print only the 5 verbs + 5 starters) |
| `scripts/worksheet-gen/qa/verify-b4-five-senses.js` | the gate grows: sections 5-7 (rows ≡ spec ≡ strings; 5 faces x en + the 69/150 long de chrome + the body PINNED to 722 / 677 with `verify()` re-run on the re-flowed layout; floors; node cross-checks with the bank; every picture the faces keep + all 45 items + 6 organs exist on disk as `<theme>/<noun>@2x.webp`; the F5 starters measured on the real row; a 20-seed face sweep; 18 face poisons). Final line format `PASS (N assertions, M/M poisons killed, …)` |
| `scripts/worksheet-gen/i18n/strings.en.json` | regenerated by `node i18n/build-en.js` (`628 types … title lint clean`; left as generated, it also carries the sibling builders' faces) |
| `docs/worksheet-gen/b4-designs/_work/K-355-faces.md` | this record |

## The five faces

### G1-357 : Five Senses: Sort by Sense : CODE `layout:'sort'` (F1)
- **Row:** `{...base.d2, layout:'sort', perBin:2, bins:5, cols:5, iconPx:64, maxPerSense:2, pool:'strong', organInBin:true, zone:260}`.
- **Child:** ten different pictures in a 2 x 5 strip (tiles 84, picture 64, coral dot on the bottom edge), five bins below (123 x 185, teal, dashed top) each headed by its sense VERB pill and the organ picture at 44; the child draws one line per tile into the bin of the sense it uses, two tiles per bin. Bins in a shuffled NON-canonical order (never see·hear·smell·taste·touch).
- **Build:** per sense `_sampleSense(perBin)` under the family fence (smell = one flower-family item + garlic / onion, or garlic + onion: the sweep hit all 11 legal pairs); strip shuffled; bin order re-rolled until non-canonical; stack guard 14 + 2 x 84 + 12 + zone + 26 + 185 = 665 <= 677.
- **Stamps:** root `data-lcs-layout="sort" data-lcs-tile data-lcs-icon-px data-lcs-organ-px data-lcs-zone data-lcs-bin-h data-lcs-pool data-lcs-max-per-sense data-lcs-per-bin data-lcs-cols data-lcs-organ-in-bin`; tiles `data-lcs-tile="<sense>" data-lcs-item`; rows `data-lcs-strip-row`; the zone `data-lcs-line-zone`; bins `data-lcs-bin`, pills `data-lcs-bin-label`, organs `data-lcs-bin-organ`.
- **verify():** 10 tiles, `perBin` per sense (smell min(perBin, 2)), <= maxPerSense, no noun twice, picture stem === noun, colour dir, >= 44, no text on a tile, a dot each; no strip row wraps (all tiles of a row share one top, PR12); bins = the five senses in a non-canonical order; every pill one client rect, narrower than its bin, no numeral, neighbours never touch, no two bins one word; the organ picture === the organ of the bin's sense (or `mouth` for taste), >= 44; bins === binH; the fixed zone block === the stamp within 220..260; **dot-to-pill zone 200..266 (above = SPARSE)**; **stage >= 630; slack under the bins <= 180**; no text outside the pills. **Node gate:** every tile a bank item of the stamped sense admitted by the pool, the family fence, maxPerSense; **pill === `verbs[loc][sense]`** (PR3), bin organ === `_organOf`.
- **Poisons owned:** PR3 (pill "Nase"), PR4 (rose + lilac tiles), PR12 (one flex-wrap row of ten), PS1 (the zone stretched to 425), PR16 (touch pool emptied → refuses).
- **PNG:** `scripts/worksheet-gen/out/dev/G1-357-null-d2-en.png` (gate: `G1-357-gate-d2-en.png`, `-longchrome.png`; poisons `K-355-gate-poison-PR3/PR4/PR12/PS1.png`; greyscale crop `K-355-faces-grey-F1-bins.png`). Read: lemon / binoculars / candy / jasmine / pillow · sand / garlic / violin / star / drum; bins taste · smell · see · hear · touch with tongue / nose / eye / ear / hand; the nose at 44 reads as a nose; nothing clipped; measured (en, 2-line instruction, body 799): stage 665, zone 258, stageH 651, slack under the bins 142; long chrome (733): slack 76; pins 722 / 677 ok.

### G1-358 : Five Senses: Which Sense? : CODE `layout:'which'` (F2)
- **Row:** `{...base.d2, layout:'which', items:8, chipOrder:'fixed', tile:60, chipPx:46, pic:60, pool:'strong', maxPerSense:2, minPerSense:1}`.
- **Child:** eight rows, each a badge + one object picture (tile 72, picture 60) + the five organ chips (60, picture 46, rounded squares, never rings) in the FIXED order eye · ear · nose · tongue · hand on EVERY row; the child circles the one organ used. 8 independent decisions with the full choice set.
- **Build:** counts {2,2,2,1,1} (every sense >= minPerSense, the 3 extras over a shuffled sense order), the family fence, rows shuffled; `chipOrder:'shuffled'` (d3, unpublished) = a fresh permutation per row, never the canonical one.
- **Stamps:** root `data-lcs-layout="which" data-lcs-rows data-lcs-row-min data-lcs-chip-order data-lcs-chip-px data-lcs-chip-tile data-lcs-pic data-lcs-min-per-sense …`; rows `data-lcs-row data-lcs-n data-lcs-item data-lcs-sense`; the strip `data-lcs-chips data-lcs-chip-order`; chips `data-lcs-chip="<organ>"`.
- **verify():** 8 rows in 6..8; per row exactly 5 chips = the five organ files each once, equal size >= 44, **no chip pre-marked** (no extra child; border / background / radius / shadow / outline identical across the strip: PR6), at `fixed` every strip in the canonical order, exactly one chip answers the row's sense; the object picture === the stamped noun, >= pic, no noun twice; sense counts within [minPerSense, maxPerSense]; badges 1..8 the only text; **the grid fills the body** (rows open 76 → 94 with the chrome) and **the object tile >= 0.7 of its row**. **Node gate:** every row a bank item of the stamped sense admitted by the pool, the family fence, exactly one chip === `_organOf(sense)`, counts sorted 1,1,2,2,2, the strip === the canonical order at `fixed`.
- **Poisons owned:** PR5 (4 chips), PR6 (ear chip ringed), PR11 (`chipOrder:'random'` → the spec guard; the d3 shuffled config at level 2 builds shuffled strips: control), PS2 (rows fixed at 76, grid not filling).
- **PNG:** `out/dev/G1-358-null-d2-en.png` (gate `G1-358-gate-d2-en{,-longchrome}.png`, `K-355-gate-control-PR11-shuffled.png`, poisons `-PR5/-PR6/-PS2.png`; greyscale crop `K-355-faces-grey-F2-rows.png`: the nose chip = a grey drop with two nostrils, the fixed position carries it). Read: pillow / rock / lemon / popcorn / guitar / lights / flashlight / rose → touch touch taste taste hear see see smell; every strip eye ear nose tongue hand; rows 94 (en 811) / 85 (733) / 76 (677 pin); the grid ends on the footer line.

### G1-359 : Five Senses: Which One Does Not Belong? : CODE `layout:'odd'` (F3)
- **Row:** `{...base.d2, layout:'odd', rows:5, items:4, box:96, pic:72, boxMax:120, majorityRows:{hear:2, taste:2, see:1}, pool:'strong', maxPerSense:2}`.
- **Child:** five numbered cards, four DIFFERENT pictures a row, three share a sense (unstated), one does not; cross out the intruder. Reasoning about a concept across themes (never the K-043 identical-icon shape).
- **Build:** row senses = the `majorityRows` multiset shuffled; per row 3 distinct items of the row sense + 1 intruder from another sense with an unused admitted item (`maxPerSense` counts the intruders); all 20 pictures distinct page-wide, the family fence page-wide; the odd column vector re-rolled until no column repeats on 3 consecutive rows and none holds more than ceil(rows/2) = 3.
- **Stamps:** root `data-lcs-layout="odd" data-lcs-rows data-lcs-items data-lcs-box data-lcs-pic data-lcs-box-max data-lcs-majority="hear:2,see:1,taste:2" …`; cards `data-lcs-card data-lcs-row-sense`; boxes `data-lcs-item data-lcs-item-sense` (+ `data-lcs-odd="1"`).
- **verify():** 5 rows in 4..5; 4 pictures per row, exactly one intruder whose sense !== the row sense, the other three === the row sense (PR7 "majority ∉ config"), the row-sense multiset === the majority stamp; all pictures distinct, >= 72, boxes >= 96 and <= boxMax, inside their stage, not crossed out / greyed / filtered at render; intruder column never on 3 consecutive rows, never > 3 rows (PR8); badges the only text; **each box GROWS to min(boxMax, its stage)** (PS3: a fixed 96 box in a 114 px stage fires) and the picture >= 0.5 of its card; the grid fills the body. **Node gate:** every picture a bank item of the stamped sense admitted by the pool, the family fence, intruders <= maxPerSense per sense, row senses === `majorityRows`.
- **Poisons owned:** PR7 (row 1 = 3 smell + 1 hear), PR8 (every intruder in column 4: cap + run both fire), PS3 (the design's fixed 96 / 72 boxes).
- **PNG:** `out/dev/G1-359-null-d2-en.png` (gate `G1-359-gate-d2-en{,-longchrome}.png`, poisons `-PR7/-PR8/-PS3.png`). Read: flower · chocolate · cake · lollipop (taste, odd 1) / cupcake · feather · cookie · honey (taste, odd 2) / candy · xylophone · saxophone · guitar (hear, odd 1) / lights · whistle · drum · piano (hear, odd 1) / telescope · star · rainbow · popcorn (see, odd 4); cards 142 with pictures 86 (en, 2-line title, body 766), 135 / 81 at 733, 96 / 72 at the 677 pin; every picture what its name says (the jasmine is a pink 5-petal flower, the garlic purple-striped, the cake the strawberry slice).

### G1-360 : Sense Organs: Write the Sense Word : CODE `layout:'label'` (F4)
- **Row:** `{...base.d2, layout:'label', organs:5, bank:true, starter:false, laneW:440, laneH:62, glyphH:36, organPx:72, rowMin:112, rowMax:128, pool:'strong', maxPerSense:2}`.
- **Child:** a 5-word bank of the sense VERBS (wordBank, 18 px, one row) then five lanes, each an organ picture (tile 84, picture 72) + a school-lined writing row 440 x 62 (glyphH 36, renders 66); the child copies the right verb beside the organ that does it. No object pictures; the word is what the organ DOES, never its name (the K-344 / K-354 fence).
- **Build:** organ rows shuffled; bank = `rng.shuffle(verbs)` re-rolled until it differs from the row order in >= 3 positions (the sweep: 18 distinct bank orders over 20 seeds, none following the rows).
- **Stamps:** root `data-lcs-layout="label" data-lcs-organs data-lcs-bank data-lcs-starter data-lcs-glyph-h data-lcs-lane-w data-lcs-lane-h data-lcs-organ-px data-lcs-row-min data-lcs-row-max`; rows `data-lcs-organ` ONLY (no verb / sense stamp on a row: verify fails one); bank words `data-lcs-bank-word` (the wordBank stamp).
- **verify():** 5 organ rows = the five organ files each once, picture >= 72; one writing row per lane with its 3 rules at 440 wide; no starter and no text in a lane at d2 (d1: exactly one one-letter starter); rows within 112..128; the bank = 5 distinct words, no numeral / slot, one row (all tops equal), at the body top; no text outside the bank; slack under the last row <= 180; the stage at the body top. **Node gate:** bank set === `{verbs[loc][s]}`; **bank order differs from the row order in >= 3 positions** (PR9); organs === the five `_organOf`.
- **Poisons owned:** PR9 (the bank in the row order → "in only 0 positions (order leak)"), PS4 (rows fixed at 112 + the stage centred → "the stage floats 48 px").
- **PNG:** `out/dev/G1-360-null-d2-en.png` (gate `G1-360-gate-d2-en{,-longchrome}.png`, poisons `-PR9/-PS4.png`). Read: bank hear · touch · see · smell · taste; rows tongue / eye / ear / hand / nose (row verbs taste see hear touch smell: all five positions differ from the bank); rows 128 (en, 2-line title, slack 16) / 125 (733, slack 0) / 113 at the 677 pin; the lanes carry nothing but the three rules.

### G1-361 : Five Senses: I Can Hear ... : CODE `layout:'write'` (F5, OPEN-ENDED)
- **Row:** `{...base.d2, layout:'write', rowsPerSense:2, rowH:46, glyphH:30, laneW:560, organPx:56, draw:null, rowMin:126, rowMax:140, pool:'strong', maxPerSense:2}`.
- **Child:** five lanes in the SENSES order hear · see · smell · taste · touch (= the title's "I Can Hear ..." first and the instruction's list), each an organ tile 64 (picture 56) + a `rulingBlock` of two 560 x 46 rows (glyphH 30) with the locale's starter printed on the first row at `starterFontPx({h:46, glyphH:30}) = 34.5 px` (the measured metrics, never 0.9 x glyphH); the child finishes the sentence from experience.
- **Build:** nothing sampled (no rng use); every starter from `starters[loc][sense]` (throws on a missing literal); lane height 2 x 46 + 6 + 8 + 16 + 4 = 126; 5 x 126 + 32 = 662 <= 677.
- **Stamps:** root `data-lcs-layout="write" data-lcs-lanes data-lcs-rows-per-sense data-lcs-row-h data-lcs-glyph-h data-lcs-lane-w data-lcs-organ-px data-lcs-draw data-lcs-row-min data-lcs-row-max`; lanes `data-lcs-organ data-lcs-rows`; the starter `data-lcs-starter data-lcs-starter-px` (rulingBlock's).
- **verify() = structure only (open, no grade):** 5 lanes = the five organs each once, picture >= 56; `rowsPerSense` writing rows per lane; exactly ONE starter per lane, on the first row, ending in "..." / "…" / ":", no slot, carrying `data-lcs-starter-px`, **width <= 0.5 x the row (gate D)**; a draw box only when configured; lanes within 126..140; no text outside the starters; slack <= 180; the stage at the body top. **Gate (node):** the starter literal per organ === `starters[loc][SENSE_OF[organ]]`, `data-lcs-starter-px === 34.5` (PR10), width <= 280 on the real 560 row: en 190 / 173 / 206 / 199 / 208. `node qa/verify-ruling-starters.js --quick --types=G1-361,G1-360` → discovers G1-361 by itself (d1 / d2 / d3 x 5 starters), G1-360 carries none, `PASS: 15 starters measured over 3 renders, 0 failure(s)` (gates A-E: baseline on the base rule, x-height on the mid rule, ascender under the top line, width, left inset).
- **Poisons owned:** PR10 (starters at 27 px = 0.9 x glyphH), PR14 (the design's 430 row + draw box with the de "Ich kann …" frame → "Ich kann schmecken: " 327 > 215, gate D), PS5 (lanes fixed + centred → "the stage floats 74 px").
- **PNG:** `out/dev/G1-361-null-d2-en.png` (gate `G1-361-gate-d2-en{,-longchrome}.png`, poisons `-PR10/-PR14/-PS5.png`). Read: ear "I can hear ..." / eye "I can see ..." / nose "I can smell ..." / tongue "I can taste ..." / hand "I can touch ..."; the starters sit on the base rule with the small letters filling the dashed x band; lanes 140 (en one-line chrome, slack 79 under the last lane) / 140 (733, slack 1) / 126 at the 677 pin.

## Gate output
```
node scripts/worksheet-gen/qa/verify-b4-five-senses.js
global: 45 items = 38 strong + 7 signed; per sense hear 10/10 co · see 7/7 co · smell 7/3 co · taste 9/9 co · touch 5/5 co
… (base sections 1-4 unchanged: bank en, d1/d2/d3 + three long-chrome renders, widths, the 20-seed base sweep, 20 base poisons)
render G1-357 sort: verify 0 lints 0 body 799 stage 665 lowest 779 vs foot 921 slack 142 zone 258 stageH 651 bins taste/smell/see/hear/touch (pins 722 ok / 677 ok)   · long chrome: body 733 slack 76
render G1-358 which: verify 0 lints 0 body 811 stage 811 lowest 921 vs foot 921 slack 0 rows 94 x8 senses touch,touch,taste,taste,hear,see,see,smell   · long chrome: body 733 rows 85 x8
render G1-359 odd: verify 0 lints 0 body 766 stage 766 lowest 921 vs foot 921 cards 142 x5 pics 86 rows taste,taste,hear,hear,see odd cols 12114   · long chrome: body 733 cards 135 pics 81
render G1-360 label: verify 0 lints 0 body 766 stage 766 slack 16 rows 128 x5 organs tongue,eye,ear,hand,nose bank hear/touch/see/smell/taste   · long chrome: body 733 rows 125 slack 0
render G1-361 write: verify 0 lints 0 body 811 stage 811 lowest 842 vs foot 921 slack 79 lanes 140 x5 starters 190/173/206/199/208 px   · long chrome: body 733 slack 1
pictures on disk: 51 refs (45 bank items + 6 organs + every face picture) checked as <theme>/<noun>@2x.webp under image-library-webp/themes, 0 missing
G1-361 starters at 34.5 px: ear "I can hear ..." 190/560 · eye "I can see ..." 173/560 · nose "I can smell ..." 206/560 · tongue "I can taste ..." 199/560 · hand "I can touch ..." 208/560
sweep faces: sort 20 item sets (smell bin 11 pairs) · which 20 answer sequences · odd 19 column vectors / 16 row orders · label 18 bank orders
poisons: P1 … P16, PR1, PR2, PR13 (base, all KILLED) · PR3 PR4 PR12 PS1(zone+stamp) PR5 PR6 PR11(random; shuffled-at-level-2 PASSES control) PS2 PR7 PR8(cap+run) PS3 PR9 PS4 PR10 PR14 PS5 PR15 PR16 KILLED
PASS (1311 assertions, 38/38 poisons killed, 50 poison verdicts clean)          --quick: PASS (995 assertions, 38/38 poisons killed, 50 poison verdicts clean, --quick: sweeps skipped)
```
`node tools/gate-variation-distinct.js --batch=b4 --diffs=2 --family=five-senses` → `[b4:five-senses] compared 15 pairs over 5 faces against their bases + pairwise within family` / **`every variation differs from the deck its base publishes and from its siblings`**.
`node tools/b3-baseline.js --check --quick` → **`checked build 3512 + enum 255 in 18s: 0 drifted (0 expected), 0 missing` / `PASS`** (the base's d1 / d2 / d3 output is byte-identical: `senseMatch` and the base path of `_buildWith` untouched).
`node i18n/build-en.js` → `628 types -> i18n/strings.en.json (title lint clean)` (left as generated).

## Deviations from §3 (each with the measurement)
1. **F1 line zone is a FIXED 260 px block, top-anchored, not `space-between`.** §3 quoted "line zone 317 at 722, 272 at 677 (`justify-content:space-between`)"; at the one-line en chrome that is ~400 px of blank paper between the strip and the bins = the K-357 defect class the nt10-D SPARSE ruling names verbatim. Built: strip 194 + zone 260 (the K-357 ceiling) + 26 + bins 185 = 665 at every chrome; measured dot-to-pill 258; slack under the bins 142 (en, body 799) / 76 (733) / 12 (677 pin), all <= 180; stage 651 >= 630. `zone` is a guarded config (220..260) and a stamp; verify + the gate measure it (PS1 = the design's 425 fires twice).
2. **F3 boxes GROW with their fractional row** (`height:min(120px,100%)`, square, picture 75 %), not a fixed 96 / 72: at the 677 fi stack they ARE 96 / 72 (the design's numbers), at 733 they are 108 / 81, at the en chrome 114 / 86, at the one-line chrome 120 / 90. A fixed 72 px picture in a 142-151 px card read as a small picture floating in a big card (PS3 reproduces it: "box 96 in a 114 px stage does not grow"). `box` / `pic` stay the floors the stamps carry; `boxMax:120` (= the design's d1 box) caps the growth.
3. **F4 rows `minmax(112px, 128px)` and F5 lanes `minmax(126px, 140px)`, not `1fr`.** With `1fr` the F4 rows opened to 140 and the F5 lanes to 156 at the one-line chrome, i.e. an 84 px organ tile floating in a 140 px lane; capped (the K-322 F4 precedent) the rows hold their content and the slack sits UNDER the last row, top-anchored: F4 16 (en) / 0 (733) / 0 (677 pin), F5 79 (en 811) / 1 (733). The gate asserts the caps and slack <= 180; PS4 / PS5 (a centred stage) fire.
4. **F3 stamps the row sense on the CARD (`data-lcs-row-sense`) via a family `senseOddGrid`**, not through `layouts/card-grid.js` (which cannot stamp a section); the `.ws-cardgrid` / `.ws-card` / badge markup is the same.
5. **F5 lanes in the SENSES order hear · see · smell · taste · touch** (§3 does not state an order): it matches the title ("I Can Hear ..." first) and the instruction's list; nothing is sampled, so the face is seed-invariant (asserted in the sweep).
6. **F2 / F3 / F4 / F5 / F1 all carry the base's `pairs / itemH / organWords` keys along in D** (the emitter spreads base.d2); each builder reads only its own keys; `pool` and `maxPerSense` are re-set by every row (maxPerSense 2 = the page rule; the base's 1 was by construction).
7. **d1 / d3 of every face are supported by the builders (F1 `perBin:1 / 3`, F2 `chipOrder:'shuffled'` + `strong+signed`, F3 `rows:4` / `items:5`, F4 `starter:true` / `bank:false`, F5 `draw` / `rowsPerSense:1 / 3`) but have no row**: the emitter ships ONE config for all three levels (the waves publish d2; README ruling). F1's smell bin at `perBin:3` caps at min(perBin, 2) by construction (one co-occurrable triple).
8. **The `MUST_HAVE` entry for G1-361 in `qa/verify-ruling-starters.js` was NOT added** (a shared qa file, outside the face brief's file scope); the gate discovers G1-361 by itself (`discovered 3 starter coordinates … G1-361/null/d1-3 x5`) and PASSES, so the entry is a one-line non-vacuity add for the reviewer.

## Per-locale refusals visible from the bank shape (Phase 4 `hub-expectations.json`)
- **None lowers a row.** The faces print only the 5 verbs (F1 pills, F4 bank) + the 5 starters (F5); every picture is locale-neutral (45 items resolve x11 by the base gate's rule 2; the 51 refs the faces keep exist on disk as `@2x.webp`). Expected rows stay **6 x 11 = 66**.
- **F1 pills:** the widest measured verb + 29 <= 123 in every §1 literal (de `schmecken` 114.9, it `assaggiare` 112.1): no locale refuses `sort`; a panel verb wider than 94 px at Baloo 2 700 17 would (rule 5 measures; none anticipated).
- **F4 bank:** one row everywhere per the design's measure (de 509 · it 567 · fi 490 <= 647); verify fails a wrapped bank.
- **F5 starters:** the "I can …" ability frame fits only in en / nl / da / no (<= 280 at 34.5 px); de / fr / es / pt / it MUST author the bare present ("Ich schmecke ...", "J'entends ...", "Pruebo ...", "Eu cheiro ...", "Assaggio ...") — a frame rewrite, not a refusal (PR14 / P9 show the ability frame failing at 327).
- **The `taste:'mouth'` override** (a panel refusing the tongue as the K taste organ) is honoured on every face (bins, chips, F4 rows, F5 lanes, verify's loose organ test); 0 rows lost.
- **Authoring that REFUSES if missing (never a filler):** every locale block needs `verbs` x5 + `starters` x5 + `strings['G1-357'..'G1-361']`; an unauthored locale throws on every face (PR15: sv 5/5).

## Open items for the panels / Phase 4
- **F1: the top strip row's lines cross the bottom row's tiles** (the design's 2 x 5 strip with dots on the bottom edge: a line from row 1 to a bin passes through / between row-2 tiles). Built as designed (one row of ten needs a 42 px picture, under the G1 floor); the panels / critic should read one F1 render with lines drawn.
- **F2 instruction "Circle the body part you use"** (the bank's EN string): the design's §6 middle says "Circle the eye, ear, nose, tongue or hand" — the panels choose; the chips are rounded squares, so "ring" / "mark" are also honest.
- **F3 band:** a locale may publish `odd` at its G2 level key (a landing-file decision, §1).
- **nl F3 title must contain "zintuig"**, fi titles never head with the bare "Aistit", fr keeps the digit, nl no digit (validator rule 9, already in the base gate).
- **Greyscale:** the F1 bin organs and the F2 chips were read in greyscale crops (`out/dev/K-355-faces-grey-F1-bins.png`, `-F2-rows.png`): the nose is a grey drop with two nostrils at 44 / 46 — readable by position + shape as the design predicted; a real mono-laser print is still the operator's / panels' check.
- **Registration (not this build):** `coordinate.mode` = `sort | which | odd | label | write`, all five at the G1 level key, `hub-expectations` 66; `apps['five-senses']` + the slug / name x11 per §1; the 11-locale Science strand row (`strand-names.ts`, the K-354 row).
- **Reviewer:** add `'G1-361'` to `MUST_HAVE` in `qa/verify-ruling-starters.js` (deviation 8).
