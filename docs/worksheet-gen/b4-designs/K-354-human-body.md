# K-354 `human-body` (K/G1) : FINAL design (editor merge, 2026-09-21)

Merged from `_work/K-354-pedagogy.md` + `_work/K-354-design.md` (record of every ruling: `_work/K-354-critic.md`). (m) = re-measured 2026-09-21 by read-only node (`lib/b2-common.js entriesFor/displayWord`, `templates/components-b3/all-about-me.js placeLanes`, `page/page.css`, `templates/components-b2.js letterBoxes`, `data/color-words.js`, `data/b2/articles.js`, `scripts/seo-landing/gen-b3-landings.js LEVEL_KEYS`, `frontend/config/topics-taxonomy.json`; scratch `K-354-measure.js`) or by OPENING the picture. *est.* = the engineer measures in the real render with the shell woff2 from `file://` (`render/one.js`). No em-dashes.

**Boundary (load-bearing).** This family owns exactly two things nobody has: (1) the WHOLE-BODY DRAWN FIGURE, a NEW palette-only SVG child (`primitives/body-figure.js`: 16 anchor ids `head hair eye ear nose mouth neck shoulder arm elbow hand finger leg knee foot toe`, 12 fill regions, 8 hideable classes) and (2) BODY FACTS about the child's own body (how many; which come in twos) from ONE locale-neutral `data/b4/body-facts.json`. It is NOT K-344 (label the FACE picture `body parts/face` with eye ear nose mouth hair eyebrow chin from a bank onto ruled lanes), NOT K-225 (word to picture lines over separate cards), NOT K-235 (category sort), NOT G1-242 (read a sentence, colour n pictures), NOT G1-244 (picture-card + letter-box dictation on any theme), NOT K-016 (pair pictures, circle odd or even) or G1-351 (parity), NOT K-355 (object to SENSE ORGAN: no face here names a sense or uses tongue as an organ), NOT K-207 / K-211 / the science-sort banks. Every label face LEADS with `arm leg hand foot knee elbow shoulder neck head finger toe` (verify: at least 4 of 6 targets from that set) and admits at most 2 of the K-344 set `eye ear nose mouth hair` per page (`faceMax:2`). No face prints teeth (count varies), internal organs or a skeleton (G3, no K art); no face is a bare picture-card + word page; no face is a sentence-to-colour page; no card ever prints a plural beside a singular (the answer). Visual signature: one tall teal line-art child, cream or white fill, coral ONLY as a ring or a numbered marker; K-344 uses solid coral dots on a photo-like 260 px face, so a teacher tells the two apart across the room.

## 1 Identity

| field | value |
|---|---|
| id / key / bands | `K-354` / `human-body` / base **K**; F1 F2 F4 F5 **K** (`K-358+ TBD by the emitter`); F3 **G1** (`G1-354+ TBD`). `default_subject: science` (6th science family: science-sort, science-sequence, science-match, seasons, animal-fact-file exist, m), `default_age_range: 5-7`, `assetClass: geometry`, `exerciseType: human-body`. `apps['human-body']` + `axes['exercise-type']['human-body']` ABSENT (m); register with the `tools/register-b4-taxonomy.js` shape + slug/name x11. The 11 compound slugs below grepped against every `axes.*` slug in their locale: **0 collisions** (m; also `o-corpo-humano-e-suas-partes`, `der-menschliche-koerper`, `het-menselijk-lichaam` 0). |
| theme axis | **FIXED** via `withFixedTheme(type, 'body parts')` (`types/_shared/fixed-theme.js`: `themeAxis.applicable:false`); `body parts` = 34 files / 34 nouns with vocab x11 (m); the family uses the 16 anchorable ids (all present x11, m; all OPENED by both inputs) plus the opened chip pictures of §5. Landings carry `coordinate.theme:''`. **No `unitAxis`** (nothing fans on a non-theme unit). |
| CCSS (en, honest) | no K-2 CCSS code names body parts; NGSS has no K-1 body standard. **All six faces = readiness:** NO `educationalAlignment`; `teaches` = "Parts of the body (readiness)"; F1 landing prose says it practises counting to answer "how many" (K.CC.B.5 named in prose only, never as an alignment on a science page). Non-EN names the national framework + the science strand literal below (`strand-names.ts` has **0 Science rows**, m: the build adds the row from table B). pt cites BNCC EF01CI02 verbatim in prose on every face. |
| new primitives / data | `primitives/body-figure.js` (§2, geometry + node gate), `templates/components-b4/human-body.js` (6 components), `data/b4/body-facts.json` (global), `data/b4/human-body.js` + `data/b4/locales/human-body.<loc>.json` (§5), `lib/b4-common.js bank()` over `data/b4/` (README open item 3). |

| loc | genre head (compound, the panels verbatim) | ASCII slug | level K / G1 (`LEVEL_KEYS`, m) | theme slug it must not equal (`axes.theme.body_parts`, m) · word notes | national strand literal (this table IS the strand row) |
|---|---|---|---|---|---|
| en | Parts of the Body | `parts-of-the-body` | `kindergarten` / `grade-1` | `body-parts` / "Body Parts" · US "color"; "printable" beats "worksheet" in the meta | Science (readiness) |
| de | Der Körper | `der-koerper` | `vorschule` / `1-klasse` | `korperteile` / "Körperteile" · nouns keep the capital; `Haare` is plural-only as stored | Sachunterricht |
| es (MX) | El cuerpo humano | `el-cuerpo-humano` | `preescolar` / `primer-grado` | `partes-del-cuerpo` · "oreja" (depicted) never "oído"; toe = "dedo del pie" (multi-word, m) | Conocimiento del Medio |
| pt (BR) | O corpo humano e suas partes | `o-corpo-humano-e-suas-partes` | `educacao-infantil` / `1o-ano` | `partes-do-corpo` · "boneco" = the child's word for the figure; toe = "dedo do pé" (m) | Ciências (+ BNCC EF01CI02 in prose) |
| fr | Le corps humain | `le-corps-humain` | `maternelle` / `cp` | `parties-du-corps` · eye = "œil" (m), `cheveux` plural-only, `nez` invariable; NBSP before `?` `:` | Questionner le monde (GS "Explorer le monde" named in landing prose only) |
| it | Il corpo umano | `il-corpo-umano` | `infanzia` / `classe-prima` | `parti-del-corpo` · gender-switching plurals as literals (le braccia, le dita, le ginocchia, le orecchie, le mani); `ginocchio` 9 letters (m) | Scienze |
| nl | Het lichaam | `het-lichaam` | `kleuters` / `groep-3` | `lichaamsdelen` · het oog / het oor / de neus in the bank literal; "haar" also = her (harmless label) | Oriëntatie op jezelf en de wereld |
| sv | Kroppens delar | `kroppens-delar` | `forskola` / `ak-1` | `kroppsdelar` · indefinite only on the page (never "ögat"); "ben" = leg and bone (the figure disambiguates) [NSR] | Naturorienterande ämnen (Lgr22 NO åk 1-3 "Människans kroppsdelar, deras namn och funktion") |
| da | Kroppens dele | `kroppens-dele` | `boernehaveklasse` / `1-klasse` | `kropsdele` · "tå" 2 boxes [NSR] | Natur/teknologi |
| no | Kroppen vår `[NSR-FLAG][no]` (the panel may rule "Kroppens deler"; OPEN 1) | `kroppen-var` | `1-trinn` / `2-trinn` | `kroppsdeler` · bokmål only; vocab neck = "nakke" (nape) on a FRONT view: `overrides.neck = {word:'hals', reason}` recommended (OPEN 2) | Naturfag |
| fi | Kehon osat | `kehon-osat` | `esikoulu` / `1-luokka` | `ruumiinosat` / "Ruumiinosat" · "ruumiinosa" banned everywhere in the type; leg = "sääri", foot = "jalka" (m; OPEN 2); "parit" never "parilliset"; `käsivarsi` / `kyynärpää` 9 letters (m) [NSR] | Ympäristöoppi |

**The rule that locks the type.** The figure is the apparatus and the answer is a FACT of the figure or of the child's own body, never a word the code inflects: every printed word is a whole panel literal from the bank (`partWords[id]` = the vocab singular verbatim unless an audited `overrides[id]` exists; `bankWords`, `plural`, `factLabels` = whole literals, article included when the locale wants one) and every count is read from `body-facts.json` once for all 11 locales. The code substitutes and stamps; it never composes an article, a plural or a case.

## 2 The base page

**Concept.** "Parts of the Body: Label the Figure." One drawn child, standing, arms a little out, 504 px tall in the middle of the page. Six empty label lanes, three per side, each tied by a straight teal leader to a coral RING on a part of the body. A word bank of the six part words on top. The child copies each word onto the lane whose leader ends on that part. Pencil first, one apparatus, no library art on the base (the figure IS the art). The pedagogy's 560 px figure / 200 px lanes were replaced by the design's measured 504 / 195 stage (674 <= 675 wide, 649 <= 722 high); the pedagogy's sampled target set replaces the design's fixed six so the 20-seed sweep varies.

**Chrome budget (README ruling).** Body **722** with 3-line title + 3-line instruction (814 one-line; 677 with a 4-line fi title). `.ws-page` inner 675 (`page.css:16-26`, m). Root grid rows `59px minmax(580px,1fr)`; the stage absorbs slack (figure stays 504, lanes re-place against the taller stage). Every stack below is quoted at 722 and checked at 677.

**Layout d2**
```
y 0    wordBank 675 x 59   (.ws-scene-banner, white, dashed coral; 6 .ws-bankword Nunito 800 18)   + margin-bottom 10
y 69   labelStage 675 x 580 (position:relative; data-ws-content)
       [ lane L 195x64 ][7][      bodyFigure h 504 w 270 (viewBox 300x560, scale 0.9)      ][7][ lane R 195x64 ]
       [ lane L        ]   [ figure top at stage y 38, centred: anchor px = (202.5 + ax*0.9, 38 + ay*0.9) ]   [ lane R        ]
       [ lane L        ]   [ coral RING r 7 stroke 3 at each target; teal leader 2.5 px to the lane-edge midpoint ]   [ lane R        ]
       lane = .ws-blankbox (white, dashed coral 2.5, r 10) holding writingRow({w:189,h:58,glyphH:40,xHeight:true})
```
Width 195 + 7 + 270 + 7 + 195 = **674 <= 675** (m). Height 59 + 10 + 580 = **649 <= 722** (and <= 677). Bank two rows (nl article literals, 8-word d3) = 108: 108 + 10 + 580 = **698 <= 722** (nl titles are short; fi bank stays one row *est.*: six fi chips at 18 px ~ 600 *est.*, engineer measures `kyynärpää`/`käsivarsi`). Worked example (design targets; m): L head (118,72) leg (124,452) foot (122,520) · R arm (214,178) hand (262,318) knee (176,404) -> stage anchors head (308.7,102.8) leg (314.1,444.8) foot (312.3,506) arm (395.1,198.2) hand (438.3,324.2) knee (360.9,401.6) -> `placeLanes(ys, 64, 580, 20)` = L **71 / 413 / 497**, R **166 / 292 / 376** (m: the foot lane is pushed to 497 by the leg lane, the knee lane to 376 by the hand lane; all lanes inside 580). Leaders never cross by construction (per side, lanes are ordered by anchor y); the K-344 pointer stamps (`data-lcs-pointer -ax -ay -lx -ly`) are reused verbatim so the K-344 crossing sweep re-runs on this page.

**Ladder** (resolved config keys; guards key on THESE, never the level index):

| key | d1 | d2 (ships) | d3 |
|---|---|---|---|
| `labels / perSide / faceMax` | 4 / 2 / 1 | **6 / 3 / 2** | 8 / 4 / 2 |
| `figureH / laneW / laneH / glyphH` | 504 / 195 / 64 / 40 | 504 / 195 / 64 / 40 | 504 / 195 / 56 / 36 |
| `bankPx / bank rows` | 18 / 1 | 18 / 1 (2 with article literals) | 18 / 2 (108) |
| `leadMin` (targets from the lead set) | 3 | 4 | 6 |

**d3 ruling:** 4 lanes per side at 56 high: 4 x 56 + 3 x 20 = 284 per side inside 580; bank 108 -> 698 <= 722. Not shipped; no copy describes it.

**Composer.** `rng.sample` 6 ids from the 16 under `faceMax:2` (ids in `{eye,ear,nose,mouth,hair}`) and `leadMin:4`; bilateral ids take the side the composer assigns (3 per side; `nose`/`mouth` are side-free); the sample is REJECTED and redrawn (bounded 40 tries, then the design's fixed six `head leg foot | arm hand knee`) when the crossing sweep fails (a leader passes within 16 px of another target's ring or two leaders intersect; e.g. eye-L + ear-L both at y 66 on one side). Bank order = `rng.shuffle` re-drawn until it differs from the top-to-bottom lane order (position leak; the G1-244 idiom). Seed is locale-neutral: same ids, same sides, same bank order in all 11; only the literals change.

**Answer-hiding + uniqueness.** Lanes are EMPTY; the mapping word -> lane lives only in `data-lcs-label="<id>"` on the lane and `data-lcs-bank="<id>"` on the bank word (never text). Each part has exactly one lane and one ring. Root stamps `data-lcs-parts` (ids in lane order), `data-lcs-anchors` (px, per id, with side), `[data-lcs-figure]` with `data-lcs-body-h`. `verify(page)`: bank id set <=> lane id set (bijection); 3 lanes per side; every pointer end within 2 px of the primitive's returned anchor for its id and inside the figure bbox; sweep (no leader intersects another; none passes a foreign ring's 16 px disc); lanes contain no text and no SVG `<text>`; no `<img>` on the page; `[data-lcs-region]` count 12 (nothing hidden on the base); `faceMax` and `leadMin` honoured; bank order !== lane order; every bank literal === `bankWords[id]` of the locale (node cross-check).

**Reused (exact).** `writingRow({w,h,glyphH,xHeight:true})` (`primitives/trace-path.js`) · `placeLanes(targets, laneH, h, gap)` (`templates/components-b3/all-about-me.js:282`, imported, never edited, m) · `wordBank({words,wordPx})` (`components-b2.js:228`, m; `data-lcs-bank` via `vocabKey`) · `letterBoxes({n,box,gap})` (`components-b2.js:291`, m: width `n*box + (n-1)*gap + 2`, height `box + 2`) · `blankNumeralBox({w,h})` (`components-b3/ordinal-numbers.js`; stamps `data-lcs-answer` ONLY when passed) · `cardGrid({cards,cols,rows,numbered})` (`templates/layouts/card-grid.js`) · `SWATCH` (`components-b2.js:21`, the 8 `codeColors`) · `COLOR_WORDS` (`data/color-words.js`, 8 x 11, m) · `svgRoot el roundedRect line circle label esc` (`primitives/_svg.js`) · tokens cream/creamDeep/white/teal/coral/ink/inkSoft/grid, `stroke.primitive 3 / grid 1.5 / accent 4` · `.ws-card` (cream, border 2, padding 12, m) `.ws-card-badge` (30, the only numeral glyph allowed outside a marker) `.ws-chip` (52 default, m; overridden inline to 44) `.ws-blankbox` (white, dashed coral 2.5, r 10, m) `.ws-scene-banner .ws-bankword .ws-icon .ws-lane` · `fileUri` (`image-cache/resolve.js`) · `displayWord` (`lib/b2-common.js`, m) · `bank(name, loc)` via `lib/b4-common.js`.

**NOT used.** `aboutMeFaceLabels` (hard-codes the 260 px `<img>` and the 674 x 360 box; only `placeLanes` + the stamp names are reused) · `answerBox` (stamps `data-lcs-answer="undefined"` without an answer) · `colorLegend` (one word per swatch; F2 needs colour word + part word) · `pillChoice`, `sceneStage`, `iconRows`, `dotFigure`, `rulingBlock` · `data/b2/articles.js` at render (its shape is a chip-choice config `{mode, chips, chipDots, refuse}`, m, NOT an article lookup; articles are authored into `bankWords` literals) · `image-vocabulary.js` / `objForms` / `approved-words-*.json` at render (no cloze frame exists; `objForms` holds 0 of the 16 nouns in 10 locales, m by the pedagogy).

**NEW `primitives/body-figure.js`** (pure SVG on tokens; Node-testable; exports `bodyFigure`, `ANCHORS`, `REGIONS`, `HIDEABLE`, `MIN_H`, `LABEL_MIN_H`). `bodyFigure({ h = 504, fill = 'cream'|'white', hide = [], markers = [], id }) -> { svg, width, height, scale, anchors:{[id]:{L:{x,y}, R:{x,y}} | {x,y}}, regions:[…] }`.
- **viewBox `0 0 300 560`**, midline x 150; `width = h * 300/560` (m: h 504 -> 270.0 · 540 -> 289.3 · 440 -> 235.7 · 420 -> 225.0 · 300 -> 160.7). `MIN_H = 240` (throws below; the eye disc falls under 2 px); `LABEL_MIN_H = 420` (asserted by the label component: three 64 px lanes per side need anchors spread >= 400 px). Max on a page 560.
- **Stroke** teal 3 px, round joins and caps; **fill** cream (default; the page is white, m `page.css:11`) or white (`fill:'white'`: inside cream cards and on the colouring face); eyes teal-filled discs; detail lines (hair strands, knee arc, finger and toe seams) teal 1.5 (`stroke.grid`); coral ONLY in `markers` and the component's rings. No other hex (palette lint).
- **Proportions** (a child ~5 heads): head disc cx 150 cy 64 r 46 (top 18, chin 110) · neck rect x 140 y 108 w 20 h 20 · torso rounded rect x 100 y 126 w 100 h 176 rx 22 (a plain t-shirt; hips 302) · arms in a slight A-pose · legs a little apart · front-view feet. Draw order: neck, legs (thigh, shin), feet, torso, arms (upper, fore), hands, head, hair, ears, eyes, nose, mouth, knee arcs, markers.
- **Geometry, right side (page right; left = mirror x -> 300 - x). Limb widths RULED UP from the design's 24/22/34/28 so every colouring region is at least one crayon stroke wide (§F2):** upper arm capsule (196,138) -> (232,218) **width 30** · forearm capsule (232,218) -> (252,292) **width 26** · palm ellipse cx 262 cy 318 rx 17 ry 19 · four finger capsules width 7 from the palm's lower edge, bases x 250/258/266/274 at y 330, tips y 350/354/352/346, 1.5 px seams · thumb capsule (247,312) -> (238,326) width 8 on the body side · thigh capsule (173,300) -> (176,402) **width 40** · shin capsule (176,402) -> (178,494) **width 34** · knee arc `M 168 406 Q 176 414 184 406` stroke 2.5 · foot ellipse cx 178 cy 522 rx 26 ry 18 · five toe bumps on the lower edge, big toe inside: r 7 (160,537), r 6 (172,540), r 5.5 (183,540), r 5 (193,538), r 4.5 (202,535). Hands reach x 277.5, feet bottom 544.5: inside the box. (The thighs' inner edges sit 6 units apart at the hip and the shins' 18: legs read "a little apart"; engineer confirms no self-overlap in the node gate.)
- **Head:** hair = the head disc above the fringe polyline `(104,62) (114,46) (126,50) (138,42) (150,50) (162,42) (174,50) (186,46) (196,62)` extended 4 units outside the disc (a cap), fill as the body, stroke 3; three strand lines 1.5 inside the cap ((150,20)-(146,40); (128,26)-(130,44); (172,26)-(170,44)) so it never reads as a hat in mono · ears = half-ellipses outside the disc at cx 104 / 196, cy 66, rx 9 ry 13 with a 1.5 inner arc · eyes = teal discs r 5 at (133,66) and (167,66) · nose = polyline (150,74) (145,84) (155,84) stroke 3 · mouth = `M 136 94 Q 150 106 164 94` stroke 3. No eyebrows, cheeks or chin line (K-344 territory).
- **`REGIONS` (12 fill ids, each a `<path>`/`<g data-lcs-region="<id>">`):** `hair head neck torso arm-L arm-R hand-L hand-R leg-L leg-R foot-L foot-R` (`arm-X` = upper + fore; `hand-X` = palm + fingers + thumb; `leg-X` = thigh + shin + knee arc; `foot-X` = ellipse + toes; features draw on top of `head`). Legend colouring merges `-L/-R` under one plural part word.
- **`ANCHORS` (16 ids, figure units; bilateral carry L and R):** head (118,72)/(182,72) · hair (128,30)/(172,30) · eye (133,66)/(167,66) · ear (100,66)/(200,66) · nose (150,80) · mouth (150,98) · neck (140,118)/(160,118) · shoulder (106,132)/(194,132) · arm (86,178)/(214,178) · elbow (68,218)/(232,218) · hand (38,318)/(262,318) · finger (30,346)/(270,346) · leg (124,452)/(176,452) (the SHIN, below the hands at 354, so a leader never crosses a hand) · knee (124,404)/(176,404) · foot (122,520)/(178,520) · toe (140,538)/(160,538). Every anchor lies inside its own drawn shape (checked against the geometry above, m). Returned scaled and offset in px.
- **`HIDEABLE` (`hide:[…]`):** `arm-L arm-R hand-L hand-R leg-L leg-R foot-L foot-R hair mouth nose ear-L ear-R` (hiding `hand-X` leaves the forearm's round cap as a wrist; `foot-X` leaves the shin's cap; `leg-X` removes thigh + shin + knee arc + foot; `arm-X` removes upper + fore + hand). NEVER hideable: eye (r 2.7 px at h 300, m), finger, toe, knee, elbow, shoulder, neck, torso, head. A hidden id is stamped `data-lcs-hidden` on the root and its region element is ABSENT from the DOM (never `display:none`).
- **`markers:[{id:'hand', side:'R', n:1}]`:** coral disc r 13 at the anchor + white Baloo 2 700 16 numeral, `data-lcs-marker="<id>" data-lcs-n`; the component asserts marker centres >= 30 px apart.
- Root `<svg data-lcs-body data-lcs-body-h="<h>" data-lcs-body-fill role="img" aria-label="">`. **Node gate `qa/verify-body-figure.js`:** parses the emitted SVG; asserts every anchor inside its shape's bbox, every region present unless hidden, hidden ids absent, `MIN_H` throw, marker spacing, no self-overlap of the two thighs, only token hexes; the region bbox table per h (the F2 and F4 floors read it).

**NEW `templates/components-b4/human-body.js`** (behind the `templates/components-b4.js` barrel; scoped inline CSS; no `page.css` edit): `bodyLabelStage({targets:[{id, side}], figureH=504, laneW=195, laneH=64, glyphH=40, w=675, h=580, gapX=7})` (rings `circle r 7 stroke coral 3 fill none` `data-lcs-anchor-ring`, leaders + dots per K-344, lanes `.ws-blankbox data-lcs-label data-lcs-side`) · `bodyLabelBank({words:[{id, word}], wordPx=18})` -> `wordBank` with `data-lcs-bank` · `bodyCountCard({id, src, label, pic=72, boxW=68, boxH=56})` · `bodyColorLegend({entries:[{color, colorWord, id, partWord}], w=374})` · `bodySpellRow({n, id, len, box=44, gap=4, h=64})` · `bodyMissingCard({hidden, figureH=300, chips:[{id, src, correct}], chip=80, pic=64})` · `bodyPairCard({id, src, word, pic=64})`.

**Alternatives (rejected).** Alt A K-344 verbatim with a library picture of a whole child: no such picture exists (34 `body parts` files are parts or a head; `occupations` are adults in uniform), any photo-like child fixes skin and gender. Alt B a cut-and-assemble paper doll (pt/es "para imprimir y armar"): open-ended, scissors first (`cutting` K-240/264 owns the move), no label genre; backlog beside picture-word-cards. Alt C the pedagogy's 560 px figure with 200 px lanes: 200 + 7 + 300 + 7 + 200 = 714 > 675; rejected on the arithmetic.

**Risks -> mitigations.** Mono print: teal 3 px outline ~30 % grey carries the figure (cream fill vanishes on white paper); the coral ring at 3 px survives; hair strands keep the cap from reading as a helmet; the engineer prints one d2 of the base, F2 and F4 on a mono laser at 100 %, writes `shoulder` on a lane in HB, colours a forearm with a crayon (§F2 floor), rings one chip. Leaders: the leg anchor sits on the shin so no leader crosses a hand; the sweep + rejection sampling remove the eye/ear same-y case. Long locales: bank 2 rows budgeted (698). What only a human eye catches: a leader that grazes a limb outline, a chip picture whose art contradicts the word after a cache refresh (open again). **Palette:** cream, creamDeep, white, teal, coral, ink, inkSoft, grid + the 8 `codeColors` on F2 swatches. **Font floor:** smallest text 16 (marker numerals) vs the 9 px lint (`qa/lints.js:86`); words 17-18; footer 10 is the shell's. Every stage/grid root stamps `[data-ws-content]`.

## 3 Faces 2-6

All five are CODE faces on ONE additive `layout` knob (`d.layout` undefined = the base path, byte-identical; a face stamps `data-lcs-layout` only when declared); each adds a `verify()` branch; guards key on the config (`d.labels`, `d.mix`, `d.legend`, `d.boxes`, `d.figures`, `d.pairs`), never on the level index. Resolved d2 configs are pairwise distinct (`tools/gate-variation-distinct.js --batch=b4`, exists, m). None is PARAM: each changes the apparatus a teacher sees (lanes + rings / cards + boxes / legend + white figure / markers + letter boxes / four hollow figures + chips / referent + 8 cards). Density: K faces pictures >= 64, numeral boxes 56, 6-8 asks; F3 letter boxes 44 (the G1 element floor; G1-244 ships 24 px boxes, m, but this type asserts its own floor), 6 rows. Every face draws the figure (the apparatus of the type).

### F1 : How Many? (K, CODE `layout:'count'`)
**Move:** RECALL a body fact and WRITE a numeral: for each pictured part, how many the child has (1 / 2 / 10). The picture is the cue, the label a word the teacher reads; the fact lives in the child's body (and, for 1s and 2s, on the figure beside the cards). **Layout:** `[figure h 540 w 289, fill cream, no rings][12][grid 362: 2 cols x 3 rows, cards 175 wide, gap 12, rows minmax(214px,1fr)]` = 289 + 12 + 175 + 12 + 175 = **663 <= 675**; card inner 147 x 186 (m: padding 12 + border 2) = `[cue pic 72][6][factLabel Nunito 800 18, 2-line reserve 44][8][blankNumeralBox 68x56]`; 3 x 214 + 2 x 12 = **666 <= 722** (<= 677). **Config d2** `{layout:'count', cards:6, cols:2, rows:3, mix:{1:2, 2:3, 10:1}, pic:72, box:56, wordPx:18}`; d1 `{cards:4, cols:2, rows:2, mix:{1:1, 2:3}, pic:96}`; d3 `{cards:6, mix:{1:1, 2:2, 5:2, 10:1}}` (the 5s = `fingersOneHand` cue `hand`, `toesOneFoot` cue `foot`: pictures that honestly show five). **Composer:** count 1 = sample 2 of `{head, nose, mouth}`; count 2 = sample 3 of `{eye, ear, arm, hand, leg, foot, knee, elbow}`; count 10 = `finger` (cue picture `finger` = a fist with the index finger raised, OPENED: one instance, never reveals the count). `toe` (10) is EXCLUDED from this face: its picture is a foot with five toes and an arrow (OPENED) = a wrong count printed. `neck` (count 1, picture = a necklace) and `hair` (uncountable) never; `shoulder` never (picture rejected). Cue pictures on a page pairwise distinct; no (5,10) pair of one part; `.ws-card-badge` numerals are the only digits on the page. **Verify:** `data-lcs-fact=<id> data-lcs-count=<n>` per card; `count === BODY_FACTS[id].count`; multiset of counts === `mix`; >= 3 distinct counts; one `img` per card, complete, `src` names `BODY_FACTS[id].cue`; distinct cue `src`s; label text === `factLabels[id]`; every `[data-lcs-answer]` absent or empty; no numeral glyph outside `.ws-card-badge`. **Query face:** "how many body parts" (en WINNABLE) · de "Wie viele Körperteile" · es "cuántas partes del cuerpo" · sv "hur många kroppsdelar" · fi "montako kehon osaa". The en "and their uses" / de "Was macht der Körperteil?" facts live as landing PROSE on this face (a `use` literal per part in the locale bank, page-free), never on the sheet.

### F2 : Colour by Legend (K, CODE `layout:'color'`)
**Move:** READ a legend (swatch + colour word + part word) and COLOUR that region of the figure; no sentence, no count, no number (that is what keeps it off G1-242 and off colour-by-number). **Layout:** `[legend column 374][12][figure h 540 w 289, fill WHITE, no rings, no markers]` = **675**; the legend reads first. Legend = 5 white rows 374 x 48 (r 12, border 2 creamDeep), gap 12 = 5 x 48 + 4 x 12 = 288, vertically centred beside the figure; row = `[swatch 26 (r 6, ink 1 stroke)][10][colour word Nunito 800 18][10][arrow glyph 14 in T.grid][10][part word 18]`; widest fi row `vaaleanpunainen` + `käsivarret` ~ 265 *est.* <= 350 inner (no wrap possible: one entry per row, the design's flex-wrap banner and its fi 3-row risk are gone). Stack = max(540, 288) = **540 <= 722**. **Config d2** `{layout:'color', legend:5, figureH:540, regionPool:['hair','head','arm','hand','leg','foot'], swatch:26, wordPx:18}`; d1 `{legend:3, regionPool:['hair','head','arm','leg']}`; d3 `{legend:6, regionPool: the six}`. **Composer:** `rng.sample` 5 of the 6 regions, `rng.shuffle` 5 of the 8 `codeColors`; colour word from `COLOR_WORDS[loc]`, part word = `plural[id]` (a separate literal; de "blau" and "Arme" never form one string: no agreement ever). The t-shirt and shorts are never in the legend (free colouring). **Floor (the reason the limbs were widened):** every legend region's smaller bbox dimension >= **24 px** at the rendered figure (6.3 mm, one crayon stroke; m at h 540 with the ruled widths: forearm 25.1, upper arm 28.9, shin 32.8, thigh 38.6, palm 33 x 37, foot 50 x 35, head 89, hair 89 x 46; the design's original forearm 22 gave 21.2 and fails). The print check colours a forearm with a crayon. **Verify:** `data-lcs-legend=<id> data-lcs-color=<key>` per row; every legend id maps to >= 1 rendered `[data-lcs-region]` (both `-L/-R` present); all region fills `#FFFFFF`; legend ids distinct, colours distinct, count === `legend`; no `[data-lcs-anchor-ring]`, no `[data-lcs-marker]`, no `<img>`; each row's part text === `plural[id]` and colour text === `COLOR_WORDS[loc][key]`. **Query face:** "colour the body parts" · de "Körper ausmalen nach Legende" · es "el cuerpo humano para colorear" · fr "les grandes parties du corps" · it "corpo umano da colorare" · pt "e colorir" · sv "måla kroppsdelar" · fi "väritä kehon osat".

### F3 : Write the Word (G1, CODE `layout:'write'`)
**Move:** RETRIEVE the name of a numbered part from the figure alone (no bank, no picture card) and WRITE it into letter boxes; the box count is the only scaffold. Differs from G1-244 (picture cards, any theme) because the cue is a numbered marker on ONE body; differs from the base because there is nothing to copy. **Layout:** `[figure h 420 w 225, fill cream, coral MARKERS 1..6][12][rows 438]` = 675; row = `[.ws-chip 44 (inline override of the 52 default, m) with the numeral Baloo 26][10][letterBoxes({n, box:44, gap:4})]`; n <= 8 -> 8 x 44 + 7 x 4 + 2 = **382** (m), row 44 + 10 + 382 = **436 <= 438** (2 px slack; if the chip renders 46 the figure drops to h 400 / w 214 and the rows widen to 449); row h 64, 6 rows + 5 x 16 = **464 <= 722**; figure 420 <= 464. Markers top-to-bottom carry 1..6 so the child scans down. **Config d2** `{layout:'write', labels:6, faceMax:2, leadMin:4, boxes:true, box:44, gap:4, maxLetters:8, figureH:420, markerMin:30}`; d1 `{labels:4, bank:true, maxLetters:6}` (a bank to copy from); d3 `{labels:8, rows h 56, gap 10, maxLetters:8}` (8 x 56 + 7 x 10 = 518). **Word eligibility is a CONFIG guard:** `partWords[id]` NFC matches `/^[\p{L}][\p{L}'’-]*$/u` (single token; es/pt/it `toe` = "dedo del pie" / "dedo do pé" / "dito del piede" excluded, m) and `n = [...word.replace(/['’-]/g,'')].length <= maxLetters` (a long vowel or a digraph is two boxes by construction; de `Fuß` 3, fi `pää` 3, sv `tå` 2, pt `pé` 2). **Eligible pools at d2, n <= 8 (m):** en 16 · de 16 (`Schulter`, `Ellbogen` 8 fit) · es 15 (toe) · pt 15 (toe; `cotovelo` 8 fits) · fr **15** (`eye`: lock, see refusals) · it 14 (`ginocchio` 9, toe) · nl 16 (`schouder`, `elleboog` 8 fit) · sv 16 · da 16 · no 16 · fi 14 (`käsivarsi`, `kyynärpää` 9): every pool >= `labels + faceMax` = 8. **Marker spacing:** ids whose anchors sit < 30 px apart at scale 0.75 never share a page: hand/finger 21.8 px (m), foot/toe 19.1, neck/shoulder 27.6, head/ear 14, eye/head 12, eye/nose 16.5, nose/mouth 13.5; the composer rejection-samples under `markerMin`. **Verify:** 6 `[data-lcs-marker]` with distinct `data-lcs-n` 1..6, centres >= 30 px apart; per row `[data-lcs-spell=<id>] [data-lcs-n]` and `data-lcs-letterboxes === n` of `partWords[id]`; no word printed anywhere (no `[data-lcs-bank-banner]` at d2, no text but the six numerals); no `<img>`; every id from the locale's eligible pool; distinct words (`toLocaleLowerCase(loc)`); `faceMax` + `leadMin` honoured; fr never selects `eye`. **Refusals:** fr `eye` per WORD (`refuseWords.write:['eye']`: `œ` is one code point = one box while a CP child writes "o-e-i-l" in four; the lock's Œ ruling); es/pt/it `toe` by the guard; no locale refuses the face. **Query face:** "write the body parts" · de "Körperteile schreiben Buchstabenkästchen" · es "examen partes del cuerpo primer grado" · it "verifica parti del corpo classe prima" · fr "écris les parties du corps" · sv "kroppens delar namn" · da "hvad hedder kroppens dele" · fi "kehon osien nimet".

### F4 : What Is Missing? (K, CODE `layout:'missing'`)
**Move:** DISCRIMINATE an incomplete figure: four small figures, each drawn with ONE part class omitted (both instances of a pair); the child CIRCLES the picture chip of the missing part (verified) and DRAWS it on (open; pt "complete o corpo humano" verbatim). No text on the item. **Layout:** `cardGrid 2x2 numbered`: cards 331 wide, inner 303 (m): `[bodyFigure h 300 w 161, fill white, hide:[x]][16][column of 3 white chips 80x80 r 14 border 2 creamDeep with a 64 px .ws-icon, gap 16 = 272]` = 257 <= 303, centred; card h 328 (300 + 24 + 4); 2 x 328 + 12 = **668 <= 722** (<= 677 by 9 px: the engineer confirms the fi title line count; a 4-line fi title with a 3-line instruction is the only stack that could bite, and rows `minmax(328px,1fr)` give no rescue, so the fi F4 instruction is written to 2 lines). **Config d2** `{layout:'missing', figures:4, cols:2, rows:2, figureH:300, chips:3, chip:80, pic:64, omitPool:['arm','hand','leg','foot','hair']}`; d1 `{figures:2, cols:2, rows:1, figureH:340, chips:2, omitPool:['arm','leg','hair']}`; d3 `{figures:4, figureH:300, chips:4, omitPool:['arm','hand','leg','foot','hair','mouth','ear']}` (the design's 15 px mouth at h 300 is legible enough for d3 only). **`omitPool` is measured, not chosen:** an omitted class must be >= 14 px in its smaller dimension at the rendered figure (the pedagogy's floor applied to the design's geometry, m at h 300: hair 49 x 26 passes; ear 9.6 x 14 fails; mouth 15 x 6.4 fails; nose 5.4 fails; eye is never hideable). The composer samples 4 distinct classes of the 5, alternating sides for bilateral ids. **Chips** = library pictures (`fileUri('body parts', id)`) = target + 2 distractors from `chipPool` (§5; 14 ids, every one OPENED) under the **confusable fence**: a distractor never comes from the target's group `{arm, hand, elbow, finger}` (the `arm` picture includes a hand), `{leg, foot, knee, toe}` (the `leg` picture includes a foot and a red knee dot; `toe` is the foot picture with an arrow), `{hair, head}` (the `hair` picture is a face with an arrow); `{eye, ear, nose, mouth}` is NOT a group. Distractors must be present on that figure (only one class is hidden, so this holds; verify checks it). Chips shuffled; chip order not the same triple-order on every card. **Verify:** 4 cards; each root `data-lcs-hidden` names one class whose region(s) are ABSENT while the other regions are present; per card 3 `[data-lcs-chip=<id>]` distinct, exactly one `data-lcs-correct="1"` whose id === hidden; fence honoured (the group table is exported by `body-facts.json` and read by the gate); hidden classes distinct across cards; chips unmarked, equal size, `img` complete; no text inside the item. **Refusals:** none (pictures are global; no text). **Query face:** "what is missing body parts" · de "Was fehlt?" · nl "wat ontbreekt?" · pt "complete o corpo humano" · es "qué le falta al cuerpo" · fr "qu'est-ce qui manque" · sv "vad saknas" · fi "mikä puuttuu".

### F5 : Which Come in Twos? (K, CODE `layout:'pairs'`)
**Move:** CLASSIFY parts by a body fact: circle every part the child has TWO of. Every card prints the SINGULAR word (a plural on the pair cards would print the answer to a reader) beside ONE picture; the fact lives in the child's body, with the figure as a referent. The pedagogy's G1 band is ruled down to K (the "two eyes, two ears" fact is a pre-school fact in every curriculum; a locale panel may still publish it under its G1 `level` key: a data decision, no code change). **Layout:** `[figure h 440 w 236, fill cream, no rings, no markers][12][grid 427: 2 cols x 4 rows, cards 207 wide gap 13, rows 104 gap 12]` = 236 + 12 + 427 = **675**; card inner 179 x 76 (m): `[pic 64][10][singular word Nunito 800 18, <= 105 px, 2 lines max]`; 4 x 104 + 3 x 12 = **452 <= 722**; figure 440 <= 452. **Config d2** `{layout:'pairs', cards:8, cols:2, rows:4, pairs:4, singles:4, pic:64, wordPx:18, figureH:440}`; d1 `{cards:6, cols:2, rows:3, pairs:3, singles:3, pic:72}`; d3 `{cards:8, pairs:4, singles:4}` with `finger` allowed as a "ten is not a pair" distractor. **Pools:** pairs = sample 4 of `{eye, ear, arm, hand, leg, foot, knee, elbow}` (count 2); singles = `{head, nose, mouth, neck}` (count 1; all four ship, m: the pool is exactly 4). `neck` is accepted as a CARD here only (picture OPENED by all three: a neck with a necklace and a coral arrow pointing at the neck; the arrow does the pointing). `chin` and `tongue` (the pedagogy's spare singles) are EXCLUDED: OPENED by the editor, `chin` is a whole face with an arrow (reads "head" beside the `head` card) and `tongue` is an open mouth with teeth (confusable with `mouth`, and K-355's organ). `finger`, `toe` (10), `hair` (uncountable), `thumb` (a thumbs-up hand, OPENED; not one of the 16) never at d2. Cards shuffled (not all pairs first). **Verify:** 8 `[data-lcs-card=<id>] [data-lcs-pair="1|0"]`; `pair === (BODY_FACTS[id].count === 2)`; pairs / singles === config; no `finger toe hair thumb chin tongue`; every word === `partWords[id]` (singular; node cross-check); one `img` per card, complete; no pre-ringed card; no numeral outside badges. **Refusals:** none. fi title/instruction use "parit" / "kaksi", NEVER "parilliset" (= even numbers; validator regex); sv "par"; de "zweimal" vs "Paare" is the panel's (OPEN 3); es never needs "dedos" here (fingers excluded). **Query face:** "body parts that come in pairs" · de "Was gibt es zweimal?" · sv "hur många har vi två av" · es "partes del cuerpo que tenemos dos" · fi "parit".

**Rejected non-moves.** "Parts and their uses" (needs a verb phrase x11 and lands on see / hear / smell = K-355's move; deferred to F1 landing prose) · cut-and-assemble figure (open, scissors; `cutting` family) · internal organs / skeleton (G3, no K art) · a plural-word face (singular-plural K-302..316) · a sense-organ face (K-355) · a theme swap (the theme is fixed) · relabelling d1 or d3 of the base as a face (same move at another count; fails `gate-variation-distinct`) · the design's "plural on every F5 card" (harmless on singles but odd, and one slip away from printing the answer).

## 4 Native rebuild plan x11

Frames NEVER inflect: this type has NO cloze frame and reads NO `objForms`. Per locale the panel authors: `strings` (6 titles + 6 instructions); `partWords` (16 singulars, confirmed against the vocab by the validator; any deviation = `overrides[id] = {word, reason}`); `bankWords` (16 bank literals, default = `partWords`; the article is written INTO the literal where the locale wants it: de "das Auge", "die Haare", nl "het oog", "de neus"; never composed by code); `plural` (16 nominative plural literals for the F2 legend, default = vocab plural; it "braccia", "dita", "ginocchia", "orecchie", "mani" as bare plurals); `factLabels` (18 whole literals for F1: 16 parts + `fingersOneHand` + `toesOneFoot`, in the locale's "how many X" form; fi PARTITIVE "silmää", "sormea yhdessä kädessä"; it "le braccia" with the article if the panel wants it); `use` (16 landing-prose phrases, optional); `refuseWords` per face; `refuse` per face. Colour words come from `COLOR_WORDS` (x11, m). The instruction names only apparatus on the page (bank, lines, figure, boxes, legend, pictures) and, at K, is one imperative sentence. The EN is a SOURCE TO AUDIT.

| loc | literals (counts) | forms / slots and where they exist | refusal / re-target | traps |
|---|---|---|---|---|
| en | 12 strings · 16 partWords (= vocab) · 16 bankWords (= partWords) · 16 plural · 18 factLabels = 78 (0 new forms) | `COLOR_WORDS.en` | none | "Parts of the Body" never "Body Parts" (theme name, m); US "color"; "pdf"/"printable" in the meta only |
| de | 12 · 16 (Kopf, Arm; `Haare` as stored) · 16 bankWords with articles ("das Auge", "die Haare": the article is the panel's, never `articles.js`) · 16 plural · 18 ("Augen", "Finger an einer Hand") | none | none | ß one box (Fuß); `Schulter`/`Ellbogen` 8 letters fit F3 (m); "zweimal" vs "Paare" on F5 (OPEN 3); engine appends "Arbeitsblatt"; title never "Körperteile" (theme name) |
| es (MX) | 12 · 16 (toe "dedo del pie") · 16 · 16 · 18 ("ojos", "dedos de la mano", "dedos del pie") | none | F3: `toe` (multi-word guard) | MX register; "oreja" never "oído"; strand literal fixed (Conocimiento del Medio); the preescolar campo may be named in prose; never "partes del cuerpo" as a title |
| pt (BR) | 12 · 16 · 16 · 16 · 18 ("olhos", "dedos da mão", "dedos do pé") | none | F3: `toe` | "boneco" allowed in instructions; pé = 2 boxes; `cotovelo` 8 fits (m); EF01CI02 on every landing; "atividade" never in a title |
| fr | 12 · 16 (œil, cheveux, nez) · 16 (no article: elision) · 16 (yeux, genoux) · 18 ("les yeux", "les doigts d'une main") | none | F3: `eye` (`refuseWords.write`); base/F5 `eye` contingent on `document.fonts.check('œ')` (OPEN 4) | NBSP before `?`/`:`; `oreille` 7 fits; strand literal fixed (Questionner le monde; "Explorer le monde" in GS prose); "fiche" never in a title |
| it | 12 · 16 · 16 · 16 (le braccia etc. as bare plurals: "braccia") · 18 | none | F3: `toe`, `ginocchio` by the guard (m) | never vocab plural + code article; "scheda" never in a title; "in inglese" tail is not ours |
| nl | 12 · 16 · 16 bankWords with de/het ("het oog", "de neus") · 16 · 18 | none | none | bank wraps to 2 rows with articles (698 budgeted); `schouder`/`elleboog` 8 fit (m); IJ absent from the 16; engine appends "werkblad"; title never "Lichaamsdelen" |
| sv | 12 · 16 (indefinite) · 16 (optional en/ett in the literal) · 16 · 18 ("ögon", "fingrar på en hand") | none | none | never "ögat"; "ben" = leg and bone; title locked "Kroppens delar" (theme "Kroppsdelar"); "par" on F5; engine appends "arbetsblad" [NSR] |
| da | 12 · 16 · 16 · 16 · 18 | none | none | title "Kroppens dele" (theme "Kropsdele"); "tå" 2 boxes; engine appends "opgaver" [NSR] |
| no | 12 · 16 (`neck` "nakke" -> `overrides.neck = {word:'hals', reason:'front view; nakke = nape'}` recommended, OPEN 2) · 16 · 16 · 18 | none | none | bokmål only; title "Kroppen vår" `[NSR-FLAG]` vs "Kroppens deler" (OPEN 1); engine appends "oppgaver" |
| fi | 12 · 16 (leg "sääri" / foot "jalka" as stored, m; OPEN 2: the panel may override `leg` with a reason, never both to "jalka") · 16 · 16 · 18 PARTITIVE ("silmää", "sormea yhdessä kädessä") | none | none | "parit" never "parilliset"; "ruumiinosa" banned type-wide; `käsivarsi`/`kyynärpää` drop from F3 (m); cases are the panel's ("Kirjoita kehon osan nimi laatikoihin"); the F4 instruction is written to 2 lines (668 vs 677) [NSR] |

Every panel OPENS every picture its faces keep (the 16 anchorable ids + `finger`, `toe`, `neck`, `knee`, `elbow`, `hair`, `head` chip pictures; the sv #35 `fruits/plum` precedent) and reads one render of every face in its locale.

## 5 Data + gates

`data/b4/body-facts.json` (GLOBAL, one file, never per locale) + `data/b4/human-body.js` (EN literals, hand-authored) + `data/b4/locales/human-body.<loc>.json` GENERATED from `i18n/.draft-b4-<loc>.json` by `tools/apply-b4-locale.js` after `tools/validate-b4-draft.js` (clones of the b3 pair, both exist for b3, m; `data/` gitignored, `git add -f`); read through `lib/b4-common.js bank('human-body', loc)`.
```
BODY_FACTS (data/b4/body-facts.json):
  parts: { head:{count:1, cue:'head'}, hair:{count:null, cue:'hair'}, eye:{count:2, cue:'eye'}, ear:{count:2, cue:'ear'},
           nose:{count:1, cue:'nose'}, mouth:{count:1, cue:'mouth'}, neck:{count:1, cue:'neck'}, shoulder:{count:2, cue:null},
           arm:{count:2, cue:'arm'}, elbow:{count:2, cue:'elbow'}, hand:{count:2, cue:'hand'}, finger:{count:10, cue:'finger'},
           leg:{count:2, cue:'leg'}, knee:{count:2, cue:'knee'}, foot:{count:2, cue:'foot'}, toe:{count:10, cue:'toe', countFace:false} }
  facts: [ {id:'fingersOneHand', part:'finger', count:5, cue:'hand'}, {id:'toesOneFoot', part:'toe', count:5, cue:'foot'} ]
  chipPool:   ['hand','foot','arm','leg','eye','ear','nose','mouth','head','hair','knee','elbow','finger','toe']   // picOpened:true each; shoulder, neck, thumb, chin, tongue excluded
  confusable: [['arm','hand','elbow','finger'], ['leg','foot','knee','toe'], ['hair','head']]
  hideable:   ['arm','hand','leg','foot','hair','mouth','nose','ear']            // eye never
  omitPool:   { 340:['arm','leg','hair'], 300:['arm','hand','leg','foot','hair'] }  // measured: smaller bbox dim >= 14 px
  pairsPool:  { pairs:['eye','ear','arm','hand','leg','foot','knee','elbow'], singles:['head','nose','mouth','neck'] }
  countPools: { 1:['head','nose','mouth'], 2:['eye','ear','arm','hand','leg','foot','knee','elbow'], 10:['finger'] }
HUMAN_BODY[loc] = {
  partWords:  { head, hair, eye, ear, nose, mouth, neck, shoulder, arm, elbow, hand, finger, leg, knee, foot, toe },  // === displayWord(vocab[id][loc][0]) unless overrides[id]
  overrides:  { neck:{word:'hals', reason:'front view; nakke = nape'} },   // audited, may be empty
  bankWords:  { ...16 },        // default partWords; may carry an article; must END with partWords[id]
  plural:     { ...16 },        // default vocab plural literal; F2 legend
  factLabels: { ...16 parts, fingersOneHand, toesOneFoot },   // whole literals; F1
  use:        { ...16 } | {},   // landing prose only
  refuseWords: { write:['eye'] } | {},   refuse: [],   // 'count'|'color'|'write'|'missing'|'pairs'
  strings:    { 'K-354':{title, instruction}, <F1..F5 ids>:{title, instruction} } }
```
Helper contract: `bank('human-body', loc)` returns the merged object; `partWord(loc, id)`, `bankWord`, `pluralWord`, `factLabel` throw on a missing literal (never fall back to the vocab); `countOf(id)` reads `BODY_FACTS`.

**`tools/validate-b4-draft.js` (human-body block; every rule runs, exit 1 on any):**
1. `BODY_FACTS.parts` keys === the 16 anchor ids; every `count` in {1, 2, 10, null}; `facts[].count` in {1, 2, 5, 10}; no `tooth`, `brain`, `heart`, `skeleton`, `muscles` anywhere; `hair.count === null`; `toe.countFace === false`.
2. Every non-null `cue` resolves via `fileUri('body parts', cue)` (throws on a missing cache entry), has a vocab singular in all 11 locales, is not `B2_EXCLUDE`d; every `chipPool` id has `picOpened:true`; `shoulder`, `neck`, `thumb`, `chin`, `tongue` NOT in `chipPool`.
3. `confusable` groups disjoint, ids ⊂ `chipPool`; every `omitPool[h]` id ⊂ `hideable`; `eye` ∉ `hideable`; every `countPools[10]` id has a single-instance cue (`toe` excluded).
4. `partWords[id] === displayWord(vocab[id][loc][0], loc)` for all 16 OR `overrides[id]` exists with a non-empty `reason`; no two `partWords` equal (`toLocaleLowerCase(loc)`); `bankWords[id]` ends with `partWords[id]` (case-insensitive).
5. Every `plural` / `factLabels` literal non-empty, no `{`, no digit, <= 34 chars; fi `factLabels[id] !== vocab[id].fi[1]` (partitive rule); `fingersOneHand` / `toesOneFoot` present or `refuse` names `count`.
6. `refuseWords.<face>` ids ⊂ the 16; the F3 eligible pool per locale (single token, n <= 8, minus `refuseWords.write`) >= 8, else `refuse` must name `write`; the base / F5 pools minus `refuseWords` >= 8 / >= 4 + 4.
7. `pairsPool.pairs` all `count === 2`, `singles` all `count === 1`; neither contains `finger toe hair thumb chin tongue`; `singles.length >= 4`.
8. Titles <= 70 chars, no worksheet-word (`WORKSHEET_WORD` regex of the b3 gate), unique in the band, NEVER equal (case-insensitive) to `axes.theme.body_parts.name.<loc>` or `.slug.<loc>` (the K-322 compound-head rule); fi strings must not contain "ruumiinosa" or "parillis"; instructions <= 150; no visible string contains the locale's free-word.
9. No literal on the type contains a colour word (`COLOR_WORDS[loc]`) adjacent to a part word in ONE string (guards "blauer Arm").
10. `strings` ids === {base + the 5 allocated face ids}; the six titles differ pairwise by at least one NOUN or verb token (never only an adjective).

**`qa/verify-b4-human-body.js`:** renders base + 5 faces x 11 locales x d2 through `render/render-instance.js` (file:// fonts) under the 722 stack (3-line de title + 150-char instruction) AND the 4-line fi title (677); asserts `verify()` empty and `qa/lints.js` clean; then the floors ITSELF (no size lint exists): chip / cue pictures >= 64, numeral boxes >= 56, letter boxes 44, lanes >= 64 high, legend rows 48 with words >= 18 px, the figure >= 300 px tall on every face and every rendered region stroke 3 px teal, F2 region smaller-dimension >= 24 px, F3 rows `scrollWidth <= clientWidth`, leaders never intersect (sweep), bank <=> lanes bijection (base), no bank on F3, one chip === hidden per card (F4), `pair` flags === facts (F5), counts === facts (F1), no numeral glyph outside badges and markers, no `data-lcs-answer` with a non-empty value, every printed literal === the locale bank (node cross-check), everything above the footer, titles <= 2 lines in de / fi. SWEEP: 20 seeds x d2 per face render distinct pages (base label SET varies; bank order never equals lane order; F1 count-2 set varies; F2 colour map varies; F4 hidden set varies; F5 pair set varies). **Poison (each must FAIL for its OWN reason; the correct EN draft is the control):** P1 `parts.tooth = {count:20}` -> rule 1 · P2 cue `'durian'` on `fingersOneHand` -> rule 2 · P3 `chipPool += 'shoulder'` -> rule 2 · P4 de `partWords.eye = 'Augen'` -> rule 4 · P5 fi `partWords.leg = 'jalka'` with `foot = 'jalka'` -> rule 4 · P6 no `overrides.neck` with `reason:''` -> rule 4 · P7 de `bankWords.hair = 'das Haar'` (does not end with the stored `partWords.hair` "Haare") -> rule 4 · P8 fi `factLabels.eye = 'silmät'` -> rule 5 · P9 `pairsPool.pairs += 'finger'` -> rule 7 · P10 sv title `'Kroppsdelar'` -> rule 8 · P11 fi F5 title `'Kehon osat: parilliset'` -> rule 8 · P12 de F2 literal `'blauer Arm'` -> rule 9 · P13 F3 title `'Parts of the Body: Big Words'` -> rule 10 · **render:** PR1 swap the sides of head and arm anchors -> crossing sweep · PR2 a `wordBank` injected on F3 -> "bank present" · PR3 F4 distractor `arm` on a `hand`-hidden card -> fence · PR4 F1 the `hand` cue on two cards -> "cue shown twice" · PR5 F5 pair cards printed with `plural` -> "word !== singular" · PR6 F2 a region pre-filled `codeBlue` -> "region not white" · PR7 F4 figure at h 300 hiding `nose` -> omitPool · PR8 `answerBox({w:68,h:56})` in place of `blankNumeralBox` -> `data-lcs-answer="undefined"` · PR9 F4 card h 340 under the fi chrome -> footer lint · PR10 base with `faceMax:3` resolved from a level-index guard -> the config guard fires before render · PR11 F3 `letterBoxes` box 40 -> floor · PR12 F2 figure with the design's forearm width 22 -> "region 21.2 < 24" · PR13 an F4 hidden id whose region stays in the DOM `display:none` -> "hidden region present".

**Page reads:** `HUMAN_BODY[loc]`, `BODY_FACTS`, `COLOR_WORDS[loc]`, `fileUri`, `body-figure.js`; never `image-vocabulary.js`, `articles.js`, `objForms` or `approved-words-*.json` at render.

## 6 SEO plan

| face | title pattern (Germanic / Romance / Nordic + fi) | meta MIDDLE (whole 120-170; the child's instruction, en) | coordinate |
|---|---|---|---|
| base | Parts of the Body · Der Körper · Het lichaam / El cuerpo humano · O corpo humano e suas partes · Le corps humain · Il corpo umano / Kroppens delar · Kroppens dele · Kroppen vår · Kehon osat | Copy each word from the bank onto the line that points to that part of the body | `{type:'human-body', mode:'base', theme:'', level:<K key>}` |
| F1 | Parts of the Body: How Many? · Der Körper: Wie viele? · Het lichaam: hoeveel? / El cuerpo humano: ¿cuántos hay? · O corpo humano e suas partes: quantos são? · Le corps humain : combien ? · Il corpo umano: quanti sono? / Kroppens delar: hur många? · Kroppens dele: hvor mange? · Kroppen vår: hvor mange? · Kehon osat: kuinka monta? | How many of each do you have? Count on your own body and write the number in the box | `mode:'count'`, K |
| F2 | Parts of the Body: Color by Legend · Der Körper: nach Legende ausmalen · Het lichaam: kleur volgens de legenda / El cuerpo humano: colorea según la clave · O corpo humano e suas partes: pinte pela legenda · Le corps humain : colorie selon la légende · Il corpo umano: colora con la legenda / Kroppens delar: färglägg efter nyckeln · Kroppens dele: farvelæg efter nøglen · Kroppen vår: fargelegg etter nøkkelen · Kehon osat: väritä ohjeen mukaan | Read the legend and color each part of the body in the color written next to its name | `mode:'color'`, K |
| F3 | Parts of the Body: Write the Word · Der Körper: Wörter schreiben · Het lichaam: schrijf het woord / El cuerpo humano: escribe la palabra · O corpo humano e suas partes: escreva a palavra · Le corps humain : écris le mot · Il corpo umano: scrivi la parola / Kroppens delar: skriv ordet · Kroppens dele: skriv ordet · Kroppen vår: skriv ordet · Kehon osat: kirjoita sana | Find each numbered part on the figure and write its name, one letter in each box | `mode:'write'`, G1 |
| F4 | Parts of the Body: What Is Missing? · Der Körper: Was fehlt? · Het lichaam: wat ontbreekt? / El cuerpo humano: ¿qué falta? · O corpo humano e suas partes: complete o corpo · Le corps humain : que manque-t-il ? · Il corpo umano: cosa manca? / Kroppens delar: vad saknas? · Kroppens dele: hvad mangler? · Kroppen vår: hva mangler? · Kehon osat: mikä puuttuu? | Each body is missing one part: circle the picture of the missing part, then draw it on | `mode:'missing'`, K |
| F5 | Parts of the Body: Which Come in Twos? · Der Körper: Was gibt es zweimal? · Het lichaam: wat heb je twee keer? / El cuerpo humano: ¿cuáles van en pares? · O corpo humano e suas partes: quais vêm em pares? · Le corps humain : lesquels vont par deux ? · Il corpo umano: quali sono in coppia? / Kroppens delar: vilka har vi två av? · Kroppens dele: hvilke har vi to af? · Kroppen vår: hvilke har vi to av? · Kehon osat: mitkä ovat parit? | Circle every part of the body that you have two of | `mode:'pairs'`, K |

All titles are candidates the native panels rewrite (<= 70, no worksheet-word, base = the bare compound head, each face adds one NOUN / verb, never an adjective). `coordinate.mode` is ALWAYS the face's mode string (base `'base'`), `coordinate.theme:''` (fixed theme), level = the K key for base / F1 / F2 / F4 / F5 and the G1 key for F3 (a locale publishing F5 at G1 swaps the key in its landing file only). h1 = title; eyebrow = level label; strand chip = the table-B literal (framework NAME only; the build adds the 11-locale Science row to `strand-names.ts`, never the English fallback). JSON-LD `LearningResource` with NO `educationalAlignment` on any face; pt prose names BNCC EF01CI02 on every face; en F1 prose names counting to answer "how many". Meta lead inherits `seo.words.free_printable` (metadata only; no visible copy claims "free"; the MIDDLE is the instruction; the per-locale window is measured with `tools/measure-instruction-window.js`, exists, m). `topicMeta['human-body']` >= 50 x11 + `skill-sentences.<loc>.json['human-body']` via the `register-b4-en-content.js` clone.

**Non-cannibalisation** (whole-landing 3-gram Jaccard, `scripts/seo-landing/gate.js` FAIL >= 0.80 / WARN 0.65; *est.*, the gate measures):

| pair | what the copy says differently | est. |
|---|---|---|
| base vs F3 | a bank to copy from onto lanes vs numbered markers and empty letter boxes; K vs grade 1 | 0.35 |
| base vs K-344 (label the face) | a whole drawn body with arm / leg / hand / foot vs a face picture with eye / ear / nose; the base p1 names the figure and the body parts, never "the line that points to that part of the face" | 0.30 |
| base / F3 vs K-225 (word to picture) | lines to one drawn figure + writing vs matching separate picture cards, no writing | 0.15 |
| F3 vs G1-244 (write the word) | numbered parts of one body, no picture cards; G1-244 is theme-generic | 0.20 |
| F2 vs G1-242 (read and colour) / colour-by-number | a colour-word legend on a line-art body vs a sentence or numbers over BW pictures | 0.15 |
| F1 vs F5 | write 1 / 2 / 10 in a box vs circle the twos; no numeral on F5 | 0.30 |
| F1 vs counting-pictures K-002.. | counting on your own body vs counting printed pictures | 0.15 |
| F5 vs K-016 (odd or even pairs) / G1-351 (parity) | a body fact vs parity; F5 never prints "even", "odd", a count or a pairing line | 0.10 |
| F4 vs missing-pieces / K-061 visual discrimination | a drawn body missing a limb + picture chips vs a picture with a cut-out piece | 0.15 |
| any face vs K-355 (five senses) | no sense verb, no object-to-organ, no tongue | 0.10 |
| F4 vs F5 | a hollow figure and chips vs a complete referent and 8 word cards | 0.20 |

Boundary sentence on every landing: "The child works on the whole body: labels, counts and colours the parts of a drawn figure. Face parts alone are on the This Is Me page, the senses on the Five Senses page."

## 7 Hub visibility contract

A face appears under `human-body` on `/[locale]/worksheets` IFF all four hold (brief): (1) `apps['human-body']` exists in `frontend/config/topics-taxonomy.json` (ABSENT today, m; missing = rendered NOWHERE in the rail) with `default_subject:'science'`; (2) `axes['exercise-type']['human-body']` has `slug` + `name` in all 11 locales (§1 table B; the 11 slugs measured collision-free); (3) exactly one landing per face per locale with `coordinate.type === 'human-body'` verbatim, `mode` per §6, `theme:''`, the level key of §1 (K for base F1 F2 F4 F5, G1 for F3), a unique slug and `canonicalDeckSlug` = the published deck; a REFUSED face has NO landing and the expectation is lowered explicitly, never padded; (4) the landing JSON committed AND deployed (per-process cache).

Gate: `node scripts/verify-hub-type-rows.js --keys=human-body` (script EXISTS since nt20-C, m; expectations from `hub-expectations.json`; poison = drop one landing / drop `apps['human-body']` must FAIL).

**Expected rows per locale:** en 6 · de 6 · es 6 · pt 6 · fr 6 · it 6 · nl 6 · sv 6 · da 6 · no 6 · fi 6 = **66**. No face is refused in any locale: every refusal on record is per WORD and leaves the face buildable on measured pools (fr `eye` on F3 by the lock, pool 15 >= 8; es / pt / it `toe` on F3 by the single-token guard, pools 15 / 15 / 14; it `ginocchio` and fi `käsivarsi` / `kyynärpää` by the 8-letter guard, pools 14 / 14; `toe`, `neck`, `hair`, `shoulder` on F1 and `finger`, `toe`, `hair`, `thumb`, `chin`, `tongue` on F5 by the facts file, in all 11). Contingencies, each recorded in the draft before the wave, none expected to fire: fr base / F5 if `document.fonts.check` finds no `œ` in the shell's Nunito subset (then `refuseWords.base/pairs:['eye']`, pools 15 / 7 pairs, still buildable: 0 rows lost); a locale panel refusing F5's "twos" framing outright (no panel signalled it; would lower that locale to 5).
