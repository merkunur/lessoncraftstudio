---
name: ""
metadata: 
  node_type: memory
  originSessionId: 713314ab-e86d-462c-a82b-6db7f6b01ac6
---

**[2026-06-26 — PART 2 "printable math-skill" landings COMPLETE 11/11 locales.]** Separate from the per-deck-type "Part 1" fan below: the Part 2 program = ~21 themeless math-skill landings/locale (telling-time, fractions, geometry, measurement, arrays-multiplication, graphing-data, number-charts; machinery `enum-printable-mathskills.js` + `gen-printable-mathskills-i18n.js` + `printable-mathskills-{coords,prose}-<loc>.json`). Was en/de/es/nl/fr/it/pt; this session shipped **sv `a024d18c`, da `85ca6b20`, no `6ada6ce3`, fi `633eb574`** — all deployed, decks repointed, verified live (chips/framework/CCSS, public www 200). Per-locale band notes: sv förskola/åk1-3 (also fixed a missing sv `ak-3` LEVELS band); da børnehaveklassen/1.-3. klasse (added `da:3-klasse`); **no LK20 +1 OFFSET** grade-1→2.trinn…grade-3→4.trinn (added `4-trinn`); fi OPS-2014 age-mapped esikoulu/1.-3. luokka NO offset (added `3-luokka`). Each gated clean (lint 100% / 0 within-class FAIL / 0 cross-page cannibalization). Prose native-ensemble per §A.13.48. CLAUDE.md §22.2 (says Part 2 missing sv/da/no/fi) is now STALE → operator folds. **Part 2 = DONE 11/11.**

The remaining-8 fan-out of the SEO/Landing-Page Program ([[seo-landing-page-program]], whose EN arc + es/en/de keyword-re-key are CLOSED). Each locale born keyword-correct on its locked pattern; **language is the work unit** (greenlight a locale → ledger-lock → fan every landable deck type of every app autonomously on green pre-flight, surface only at a novel curricular call / catalog defect / protected-core touch / the locale close).

**SoT = the plan file `C:\Users\rkgen\.claude\plans\commission-to-cc-swirling-bengio.md`** (the 8-locale investigation report + the sv ledger-lock + the full build tracker + the close) + its sibling agent reports (`...-agent-*.md`: it/nl/Scandinavian/fi curriculum frames + the sv render spec `ae16a8b8…`). **Read the plan file first when resuming.** Doctrine index = CLAUDE.md §22.5. **`no` + `fr` + `it` SoT = `go-luminous-moon.md`.**

## `pt` NEARLY DONE (11th/FINAL Romance locale; from-scratch fan; Foundation + Waves A/B/C/D/E LIVE = 2311; only CLOSE + recovery remain) — SoT plan `go-luminous-moon.md`
Clones the **it (gendered Romance) stack** (os/as + contractions dos/das/nos/nas/aos/às/pelos/pelas; `enum-pt`/`gen-pt` both use `canonicalDeckSlug` — the fr↔it slug-field lesson applied). BNCC framework (chip "BNCC"), 3-band educacao-infantil(3-6)/1o-ano(6-7)/2o-ano(7-8) NO offset (3º ano OUT), GRADE-LED title `Atividades de [op] para o [1º/2º ano] [sem reserva] – Tema | para imprimir PDF` / readiness `[Token] – Tema | atividades para imprimir grátis`; strands Números / Probabilidade e estatística / Alfabetização / readiness-math Raciocínio lógico.
- **Foundation `397fda4a`** · **Wave A `fb30bd57`** (7 visual/logic readiness, 509) · **Wave B `df4eb246`** (5 math-reasoning readiness, 584; pattern-train 5-mode boilerplate→mode-true held first-try) · **Wave C `1502cfa3`** (5 arithmetic CARRIES, 692; FIRST std-bearing; addition↔subtraction operation-true held; **math-worksheet quantities live in manifest `values`/`equations` → annotate `--source=manifest`, NOT deck.html sum-slot**).
- **Wave D `898e5915` (2026-06-16) — MIXED-band literacy ×6, 404 landings → pt fan 2189.** alphabet-train 29 / matching-LETTER 45 (the 48 themeless `name` decks auto-excluded by enum) / word-guess 92 (1º ano, L.K.2.d easy/L.1.2.d normal; 1 stray null-mode deck dropped) / word-scramble 91 / wordsearch 49 (RF.K.3.c) / **prepositions 98 MIXED via a NEW `gen-pt-readiness` per-mode `cfg.level` FUNCTION** (mc 49 educacao-infantil readiness / fillin 49 1º ano L.K.1.e — verified live). FENCE-A (word-guess MISSING vs word-scramble JUMBLED) + FENCE-C (matching som-inicial vs alphabet-train ordem-alfabética) 0 leaks; **LENGTH-RATIO held first-try** (wg/ws within-mode 0.583 — no 0.811 collapse). **crossword (47 all-themeless) + matching-NAME (47 themeless) DEFERRED to thumbnail-recovery** (joins sudoku+treasure-hunt → 4 surfaces). Gate over 2189: 0 FAIL / 0 dup / 0 lint-fail, all-pairs max 0.680. Repoint 405 / 0 missing.
  - **Wave-D fold-fix (gate-discovered, pre-existing, content-only no-repoint): the §4.B LINT was never grepped in Waves A/B/C — 263 already-LIVE pt landings carried gate-fails undetected.** (1) **h1 "undefined"** — find-objects + find-and-count (180) read `theme.h1Display` but gen passes `(mk, lvl)` → switched to `{H1}` placeholder. (2) **"NO theme-noun in P1"** — big-small + more-less (83) had 16 SKEL paragraphs lacking a noun placeholder → injected `{N_PL}` preserving SIZE/QUANTITY fences. **DURABLE LESSON: always grep BOTH the §4.B lint summary AND the similarity verdict (the picture-path precedent) — `gate.js | tail` hides the lint, which prints first.**
- **Wave E LIVE (2026-06-16, `9b2ecb67`): picture-sort 122 (readiness) → pt fan 2311 + cryptogram unlandable (stub).** New `gen-pt-picturesort.js` (clone of gen-IT — render2 dual-theme, 16×12=192 cells > 122 pairs coprime, pt contractions aos/às+dos/das, strand Agrupar e classificar) + native `pt-picturesort-frames.js` (16 SKEL + 12 P2). Title SPLIT (operator): SERP `Separar e classificar` / H1 `Separe as imagens`. COUNT-FENCE (negation-window conte/quantos/número; gráfico/diagrama hard) + SIBLING-FENCE 0 leaks; gate 0F/0dup/0lint over 2311 (picture-sort within-cluster 0.624). Repoint 122. **chart-count DEFERRED: same v32 `4th_of_july` tag-bug as treasure-hunt (48/49 decks mis-tagged) → 5-surface thumbnail-recovery backlog (sudoku, treasure-hunt, crossword, matching-name, chart-count); the bar COUNTS survive in manifest `exercises[0].counts` (gen-cc-facts reads them), only the THEME needs thumbnails.** cryptogram proven-unlandable (100 decks, 0 subjectTags). **27 of 28 types landed at Wave E.**
- **Recovery-A LIVE (2026-06-16, `ca561c56`): sudoku 122 + treasure-hunt 88 = 210 readiness → pt fan 2521, via THUMBNAIL-RECOVERY.** sudoku=themeless catalog / treasure-hunt=v32 `4th_of_july` mis-tag → 5 batched agents Read all 250 `thumbnail.png` (Read sees images) → `{slug→theme}` maps (0 invalid, 48 themes; one agent caught+fixed an image-cache scramble via JPEG re-read; 3 fresh spot-checks confirmed) → synth coords (mode+theme group) → native configs pt-readiness-sudoku (3 modes mode-true difficulty, `Raciocínio lógico`; fixed 3 hardcoded theme-wrong example nouns carrinho/cachorro/gato) + pt-readiness-treasure-hunt (cardinal-arrows/compass, `Orientação espacial`, picture-path-`labirinto` fence). Gate 0F/0dup/0lint over 2521. Repoint 249. **treasure-hunt deck slugs keep the bogus `4-de-julho` (cosmetic); landing content = real recovered theme.** Recovery flow = es/en precedent (NO script; agent-Read→map→synth-coords; paths `/var/www/lcs-media/decks/pt/<slug>/thumbnail.png`).
- **🏁 Recovery-B COMPLETE (2026-06-16, `09f2ed6a`): chart-count 39 + crossword 46 + matching-name 47 = 132 → pt fan 2653. ALL 28 pt types landed (cryptogram unlandable). pt CLOSED.** **chart-count theme was PROGRAMMATIC** (manifest `exercises[0].chartImages[].theme`+`counts` survive the v32 4th_of_july tag-bug → NO thumbnail; the heaviest gen needed no agents) — only crossword + matching-name needed thumbnails (crossword thumbnails PRINT the pt theme name → trivial). `gen-pt-chartcount.js` = clone gen-it-chartcount (OVERRIDE_PT 40 icon/plural fixes, os/as gender-articles [far simpler than it mascPlArt], Quantos/Quantas honest-count Quiz+assertion, CULL non-countable + kitchen_tools same-plural "assadeira"). **crossword = pt's FIRST 2º-ano literacy** (L.2.2.d, single mode, grade/cruzamento lexicon fenced off wordsearch/word-guess). **matching-name = 2nd `name` mode on pt-readiness-matching** (mode-fn level/standard/strand; **PER-MODE P3** — the shared letter-P3 "som inicial" leaked into name, fixed by making P3 a function; letter byte-unchanged; 1º-ano RF.K.3.c). Gate 2653: 0F/0dup/0lint (chart-count within-cluster 0.735 = the program-wide templated-prose WARN, it/fr/da carry it). **Rollout NEXT = `fi`** (catalog-blocked; a deck-publish-wave decision upstream of the landing program — pt was the last catalog-ready locale, so the 11-locale landing rollout is effectively DONE).
- **[INCIDENT 2026-06-16 — disk-full outage, RESOLVED] The Wave-E deploy's smoke-test surfaced a Postgres crash (sign-in down): `/dev/md2` hit 100%. NOT caused by the landing commit.** Root cause = `backup-decks.sh` retained 3 weekly deck tarballs (~78G each = 234G) and pruned AFTER tar, so the Sunday run briefly held 4 tarballs (~312G) + 141G live decks → overflowed the 436G volume mid-backup; `set -e` left 4 tarballs + 0 bytes free. Fix: freed ~234G (4 deck tarballs 178G; **flashcards+materials 13G = teaching-packages-nuke orphans, domain 410 but nginx still served the raw PDFs — the 2026-05-17 nuke missed the media**; /root deck-wave staging 15G; .publish-cli-staging 18G; pre-deploy snapshot pile) → disk 100%→44%. Patched `backup-decks.sh` (prune-BEFORE-create + free-space guard [abort if free < decks×1.33] + keep-2; commits `a5c81792` content + `cd95d7aa` exec-bit-100755-so-cron-can-run-it). **DOCTRINE: on a deploy smoke-fail with a DB-health 500, check `df -h /` FIRST — a full disk crashes Postgres; the landing/app commit is usually innocent. Off-host backup still owed (Scaling Arc 3 Q3) — same-host tarball gives no DR + is the disk-pressure source.**

## `it` COMPLETE (CLOSED 2026-06-16) — 28 types / 2288 landings + cryptogram proven-unlandable (10th locale) — a FINISH, not a from-scratch build
**`it` was the FIRST Romance reference fan, built in an EARLIER phase (commits `dc48c108`→`0636f7be`) BEFORE the structured keyword-rekey discipline** — so at kickoff it was already ~85% LIVE (2,060 landings, 25 types, repointed, sitemap'd) but with inside-out H1-titles, 3 missing types, a LEVEL_CHIP bug, AND a hidden pre-existing cannibalization. Finished in 3 parts on `go-luminous-moon.md`:
- **Part 1 (`3ac0031f`)** — google.it harvest → `rekey-it-titles.js` (RANGE-LED like de: `Schede di [op] entro il N [senza cambio] – Tema | da stampare PDF`; readiness Pattern-B `[Token] – Tema | schede da stampare PDF gratis`; `senza cambio` honest-fit; native tokens Tombola/Crucipuzzle/Anagrammi/Labirinti/Cruciverba; per-mode quals avoid ordinals) → all 2,060 re-keyed, 0 dup. LEVEL_CHIP fix (it bands). **THIRD GAP found+fixed: the early build shipped pattern-train with 102 cross-mode FAILs (max 0.871)** — the 5 modes shared ~70% verbatim scaffold; native author rewrote `it-readiness-pattern-train.js` boilerplate→mode-true (per-mode image-worlds AB botta-e-risposta / AAB gemelli-in-testa / ABB l'eco / AABB a-coppie / ABC semaforo) → cross-mode 0.871→0.400.
- **Part 2 (`cbdfecae`+`6256d337`)** — 3 missing types +228: **treasure-hunt 94** (infanzia, Orientamento spaziale, Caccia al tesoro, cardinal↔compass mode-true 0.240) · **bingo 37** (infanzia, Percezione visiva, Tombola illustrata — NOT lotto) · **math-worksheet 97** (STANDARD-BEARING per-coord band 70 classe-prima 1.OA.C.6 / 27 classe-seconda 2.NBT.B.5, Numeri, HIDDEN-VALUE-no-key fence). Machinery `enum-it-coords.js` + `annotate-it-bands.js`.
- **Part 3 close** — gate 0.777/0 FAIL/0 dup over 2288; bands infanzia 1285 / classe-prima 808 / classe-seconda 195; standard-bearing 784 / readiness 1504; cryptogram 100 decks 0 subjectTags = UNLANDABLE. Live-verify GREEN both bands + crypto-404.
- **TWO DURABLE LESSONS (apply to pt next):** (1) **run the full similarity gate at any locale FINISH, not just at build** — an early-phase fan can carry pre-existing cross-mode cannibalization the structured-rollout gate would've caught (the pattern-train 102-FAIL surprise). (2) **the fr↔it enum slug-field-name differs** — `enum-fr-coords` emits `canonical`, but `gen-it-readiness` reads `canonicalDeckSlug`; the fr-cloned `enum-it-coords` wrote `canonical` → 228 landings got `slug:undefined` → 404 live (caught at verify, fixed by field rename). When cloning the enum across the fr↔it boundary, match the slug field the TARGET gen reads. **NEXT LOCALE = `pt`** (clones it/fr Romance machinery).

## `fr` COMPLETE (CLOSED 2026-06-15) — 28 types / 2510 landings + cryptogram proven-unlandable (9th locale)
maternelle 1470 · cp 934 · ce1 106; standard-bearing 1040 · readiness 1470. Commits `65d44ffd` (Foundation) → `8a611edf` (A: 9 visual/logic readiness, 686) → `1de6e4a0` (B: 5 math-reasoning readiness, 528) → `2ec57254`+`7cf3f102` (C: 5 arithmetic CARRIES, 631, first fr standard-bearing) → `bc97b907` (D: 7 literacy mixed-band, 486) → `9be1ee87` (E: dedicated gens + close, +179). Clones the **Italian (Romance) + da machinery**; native fr ledger-lock (full detail in MEMORY.md fr line + `go-luminous-moon.md`).
- **3-band collapse maternelle/CP/CE1, NO offset** (France CP=age6=first formal year; CE2 OUT). Framework chip `Éducation Nationale`. fr-render = pure substitution (les/des/aux+plIndef, de/d' by vowelInit, ALL DERIVED; assertThemeTable d'/de guard). 50-theme fr-themes (œufs/objets/outils/arbres vowel-initial). Title: grade-led `[Token] [CP/CE1] [sans retenue] – [Thème] | à imprimer PDF gratuit`, readiness `[Token] – [Thème] | fiches à imprimer PDF gratuit`; head=Fiches. Native tokens Loto/Mots mêlés/Algorithmes/Labyrinthe/L'intrus.
- **Wave E NEW DOCTRINE (reusable for it/pt):** (1) **substring-collision guard** in `gen-fr-chartcount.js` — when one chart-count category plural is a whole-word substring of another in the same deck (`chapeaux`⊂`chapeaux de fête`, `tortues`⊂`tortues terrestres`), the shorter Quiz noun "Combien de chapeaux ?" is AMBIGUOUS → drop the shorter/generic, keep the specific (mirrors the de-dup honest-count discipline). (2) the French sort-not-count fence MUST include the **`dénombrer`** paradigm (the #1 France count-leak word, absent from the original ledger fence). (3) Quiz elision `de`→`d'` before vowel/mute-h but `de` before aspirate-h (ASPIRATE_H set hibou/haricot/hérisson/…) — a lookup, not spelling. (4) picture-sort operator title SPLIT (SERP noun head `Tri des images` / H1 imperative `Trier les images`) via the rekey op + gen-config h1 divergence. (5) the it/no Wave-E gens (`gen-*-picturesort`/`gen-*-chartcount` + `gen-cc-facts --locale=`) clone cleanly to Romance. cryptogram = themeless wall (101 fr decks, 0 subjectTags, `cryptogramme`=credit-card register) → DON'T-LAND, same as es/en/de/sv/da/no.
- Gate all-pairs 0.757 / 0 FAIL / 0 dup over 2510; repoint 179 deck.html/1432 repl; live-verify GREEN both bands + canonical→landing + hreflang + sitemap shard-4. **NEXT LOCALE = `it`.** Carry-forward: puppeteer verify-deorphan `fr` block (Chrome on Hetzner; locale-agnostic, applies by construction).

## `fr` IN PROGRESS (9th locale; Part 1 Foundation done 2026-06-15, commit `65d44ffd`) — clones Italian(Romance)+da machinery; 3-band maternelle/CP/CE1 (no offset); framework `Éducation Nationale`; title head `Fiches`+`à imprimer PDF gratuit`+`sans retenue`; chart-count token `Graphiques`. fr-render = pure substitution, French contractions DERIVED from plIndef+vowelInit (assertThemeTable d'/de guard). fr-themes 50 themes native. Dedicated gens deferred to Wave E. **Wave A COMPLETE (`8a611edf`): 9 visual/logic readiness, 686 landings; gate 0F/0dup max 0.725; FENCE-A/B+calque 0; treasure-hunt sharpened 0.870→0.725.** **Wave B COMPLETE (`1de6e4a0`): 5 math-reasoning readiness, 528 landings, maternelle/`Premiers outils mathématiques`; "Algorithme"=official France term; pattern-train mirror-risk avoided (doubler-anchor); more-less↔big-small fence 0-leak.** **Wave C COMPLETE (`2ec57254`+`7cf3f102`): 5 arithmetic CARRIES, 631 landings (fr fan total 1845), FIRST fr standard-bearing wave (CP/CE1, educationalAlignment + Éducation Nationale + `Nombres et calculs`); token-swap math-puzzle=Calcul mystère/math-worksheet=Fiches de calcul; annotate-fr-bands.js per-coord re-grade (add/sub uniform CP, the 3 others band-split CP/CE1); addition↔subtraction operation-true fence held first-try; sans retenue both ops; 3-calcul-faces fence; math-puzzle add↔sub mode sharpened 0.860→0.728.** **Wave D COMPLETE (`bc97b907`): 7 literacy types, 486 landings (fr fan total 2331), MIXED-band (mode-function cfg.level/standard/strand); word-guess+word-scramble both CP, prepositions split (mc maternelle/fillin CP); Fence A/B/C + calque 0; LENGTH-RATIO applied proactively (wg P2 86-94w/ws 104-119w → within-mode ≤0.58, no word-scramble collapse); gate 0F/0dup max 0.757.** **NEXT = Wave E (dedicated gens: picture-sort -vs- + chart-count Quiz + cryptogram prove-unlandable), then Part 7 fr CLOSE.** Full ledger-lock + per-wave detail in the SoT plan `go-luminous-moon.md` (now the fr plan; the no record below is the prior-locale archive).

## `no` COMPLETE (2026-06-15) — 8th locale DONE: 28 types / 2611 landings live+repointed+verified; cryptogram proven-unlandable. Bands 1-trinn 1523 · 2-trinn 802 · 3-trinn 286; sitemap shard-4 carries all incl. 221 Wave-E; gate 0F/0dup. Carry-forward: puppeteer verify-deorphan `no` block when Chrome on Hetzner (locale-agnostic, config committed). **The no fan's full per-wave record is THIS file's no-section below + git; the `go-luminous-moon.md` plan was repurposed for fr after the no close.**
**Operator workflow ruling: divide the no fan into digestible PARTS; before EACH part the operator switches to plan mode so a fresh native-expert bokmål ensemble is employed for that part's prose.** Part 1 = Foundation only (operator's call), no live landings.
- **Ledger-lock (native ensemble: linguist + LK20 educator + google.no SEO; full 50-theme table in plan sibling `go-luminous-moon-agent-a22e62e92042214f5.md`):** Framework chip `LK20`. **Bands +1 OFFSET (Norway uses `trinn` not `klasse`; starts 1. trinn at age 6, NO pre-school year): `1-trinn` = readiness floor (EN-K + ALL readiness + concrete ≤10) / `2-trinn` = symbolic arithmetic ≤20 CARRIES + chart-count + formal decoding / `3-trinn` = within-100. 4. trinn OUT. NO barnehage band** (a worksheet IS skole). Strands (bokmål): math `Tall og tallforståelse`; data `Statistikk og sannsynlighet`; readiness-math `Antall, rom og form`; readiness visual-logic `Visuell sansing og logisk tenkning`; reading `Tidlig lese- og skriveopplæring`; phonics `Språklig bevissthet`. **Title GRADE-LED** `[Op]oppgaver [N]. trinn [uten tierovergang|uten låning] – [Tema] | til utskrift PDF gratis` (non-numeric = no trinn number + tail `| oppgaver til utskrift PDF gratis`); operator chose **Addisjon/Subtraksjon** formal token; the genuine NO divergence = **two operation-distinct no-carry terms** (add `uten tierovergang` / sub `uten låning`, honest-fit within-10 only). Head `oppgaver` leads, `arbeidsark` in meta.
- **Render = the Norwegian≠Danish catch:** bokmål def-pl is uniform `-ene` BUT drops the `-er` (da KEEPS it: frugter→frugterne; no: frukter→**fruktene**). `no-render.js expectedPlDef`: decl1 STRIP-er +ene / decl2 +ene; NO consonant-doubling (whole da irregular class collapses → only `tree`/trærne irregular); double-def lint no-op'd (bokmål permits "de små dyrene"); B4 extended to catch the *frukterne form. assertThemeTable PASS 50/50 (incl. blomstene/møblene). Gender re-resolved per-locale (§A.13.58).
- **Part 1 shipped (commit `c722d002`, deployed clean, 2026-06-15):** machinery `no-render.js`/`no-themes.js`/`gen-no-readiness.js`/`rekey-no-titles.js`/`enum-no-coords.js` + `verify-deorphan.js` no block; shared maps (FILES+import; FRAMEWORK LK20; UI_STRINGS.no [NSR-flag embeds]; LEVELS `1/2/3-trinn` unprefixed; LEVEL_CHIP); empty `no.json` scaffold. Enum smoke-test on Hetzner GREEN (grid-match 47 / addition 159 4-mode / wordsearch 47 valid coords — catalog healthy ~3,074 no decks, mirrors da). rekey fixture-verified ("Addisjonsoppgaver 2. trinn uten tierovergang med bilder – Dyr | til utskrift PDF gratis"). **NO live landings.**
- **Two deferrals (per the per-part cadence):** (1) dedicated gens (`gen-no-chartcount`/`gen-no-picturesort`/`enum-no-picturesort` — Norwegian count-fence) → **Wave E** (native ensemble); (2) full per-type enum + per-coordinate re-grade (check-sum-ceiling) → **each wave's kickoff** (curricular `--level`/standard ratified by that wave's ensemble; Part 1 proved enum via the 3-type smoke-test).
- **Part 2 / WAVE A COMPLETE (2026-06-15, commit `e2e07a77`, deployed+repointed+verified): 9 visual/logic readiness types, 691 landings, all 1-trinn / no standard / native bokmål.** Wave kickoff = native ensemble (voice-guide educator + adversarial linguist/SEO reviewer). Operator forks: grid-match token `Para sammen bilder` (query-bearing, over the low-demand `Bildebrikker`); visual-logic strand `Visuell oppmerksomhet og logisk tenkning` (over `Visuell sansing` which reads clinical). Adopted: find-odd→`Finn forskjellen`, i-spy→`Finn de skjulte tingene`, sudoku→`Bildesudoku`, `for barn` in readiness title tail. 9 configs authored by native agents per OWNED-lexicon + airtight FENCES (the <0.80 guarantee): find-odd `forskjell` vs odd-one-out `passer ikke/kategori`; picture-path `labyrint/sti` vs treasure-hunt `piler/kompass/skattejakt`; grid-match `para` vs bingo `bingobrett/rope opp` vs sudoku `rad-kolonne-én-gang`; make-whole `halvdel` vs missing-pieces `hull`. **Gate PASS 0F/0dup over 691, max 0.703** (within-mode template-reuse). **DOCTRINE WIN: treasure-hunt cross-mode 0.768→0.629** via boilerplate→mode-true sharpening (cardinal-arrows foreground `piler/opp-ned-venstre-høyre`, compass `kompass/nord-sør-øst-vest`) — the collapse confirmed prose-coupling not floor. find-objects i-spy h1 → plDef (`de skjulte tilbehørene`, bokmål double-def). Repoint 817 deck.html / 6536 repl / 0 missing. Per-type: grid-match 47 · shadow-match 91 · missing-pieces 91 · find-objects 93 · bingo 39 · picture-path 87 · odd-one-out 52 · sudoku 95 · treasure-hunt 96.
- **Part 3 / WAVE B COMPLETE (2026-06-15, commit `880bcfe8`, deployed+repointed+verified): 5 math-reasoning readiness types, 576 landings (total no = 1267), all 1-trinn / no standard / strand `Antall, rom og form`.** Native ensemble unanimous (no fork). find-and-count BOTH modes math (find-AND-count, per da). pattern-worksheet token `Mønstre`→`Mønster`. **Load-bearing fence held: more-less uses `færrest` (NEVER `minst`); big-small `minst`=SIZE** (Norwegian minst=both fewest+smallest — the airtight separation). **pattern-train 5-mode distinct recipes (AB annenhver / AAB to like så én / ABB én så to like·ekko [NOT speilet] / AABB to og to·par / ABC tre forskjellige) + mode-coded h1 kept cross-mode LOW** — no pattern-train cross-mode pair in the top-10 (AVOIDED the nl 2-rewrite FAIL). pattern-train mid-build fix: agent wrote ~80w SKELs (gate floor 200) + illegal `et {H1}` article-before-placeholder → fix-agent lengthened 40 SKELs to 150-165w + removed every `et/en {H1}`. **Gate PASS 0F/0dup over 1267, max 0.749** (big-small within-mode template-reuse, structural; big-small has only 36-41 themes → denser reuse). Repoint 578 deck.html. Per-type: pattern-train 240 · pattern-worksheet 48 · big-small 77 · more-less 120 · find-and-count 91. **Wave-B carry-forward: optional future 1.-trinn standard-bearing split for more-less (K.CC.C.6) + find-and-count (K.CC.B.5).**
- **Part 4 / WAVE C COMPLETE (2026-06-15, commit `e7ab766e`, deployed+repointed+verified): 5 math CARRIES types, 625 landings (total no = 1892), ALL standard-bearing** (strand `Tall og tallforståelse` + LK20 framework + `educationalAlignment` — FIRST wave with CCSS codes live). Native ensemble; strand `Tall og tallforståelse` (reviewer rejected da's `Tall og algebra` = LK06/da). Operator forks: math-puzzle=`Regnepuslespill`, code-addition=`Tallkode`+grade-led-no-range. **Per-coordinate re-grade via NEW `annotate-no-bands.js`** (clones check-sum-ceiling read-logic, injects level+standard by child-seen max; handles the `values`/`equations` math-worksheet manifest shape + the deck.html sum-slot for code-addition): addition 159 + subtraction 157 all 2-trinn (K.OA.A.2/K.OA.A.4); math-puzzle 118 (89 2-tr + 29 3-tr); **math-worksheet 96 = 91 2-tr + 5 3-tr — CAUGHT band-split (NOT da's "all ≤10"; the verify-on-rendered-content discipline paid off — math-worksheet results can exceed 20)**; code-addition 95 (31 2-tr + 64 3-tr). No 1-trinn (arithmetic symbolic), no >100 OUT. **Gate PASS 0F/0dup over 1892, max 0.750.** The big lesson: **addition↔subtraction (the anticipated #1 risk) HELD first-try** via operation-true verb divergence (`hvor mange i alt` vs `hvor mange er igjen` — the sv 0.87→0.39 lesson, applied as separate-TYPE prose); but **two WITHIN-type multi-mode collapses appeared instead** — math-puzzle 3 modes 0.946→0.721 (I'd over-minimized the op to ~1 mention so the reveal-face crowded it out → re-author made the OPERATION load-bearing inside the shared reveal frame) + math-worksheet 2 modes 0.815→0.298 (2-vs-3-term count made load-bearing). code-addition agent wrote ~43w SKELs (way under 200-floor) → lengthened. Repoint 675 deck.html. Per-type: addition 159 · subtraction 157 · math-puzzle 118 · math-worksheet 96 · code-addition 95.
- **Part 5 / WAVE D COMPLETE (2026-06-15, commit `10cf40c2`, deployed+repointed+verified): 7 literacy types, 498 landings (total no = 2390), MIXED-band** (readiness 1-trinn + standard-bearing 2-/3-trinn) via mode-function `cfg.level/standard/strand`; `gradeFor` now LEVEL-DRIVEN (grade iff 2-/3-trinn — verified byte-safe for A/B all-1-trinn→'' and C all-grade). Native ensemble; operator forks: matching/letter=`Første lyd og bokstav`, word-scramble=`Stokk om bokstavene`, crossword=`Bildekryssord`, prepositions mc/fillin split. **Bands verified live (rendered DOM):** alphabet-train 1-trinn readiness (Bokstavkunnskap) · matching letter 1-trinn (Språklig bevissthet) / name 2-trinn RF.K.3.c (Tidlig lese- og skriveopplæring) · word-guess+word-scramble easy 2-trinn L.K.2.d / normal 3-trinn L.1.2.d (Rettskriving) · wordsearch 2-trinn RF.K.3.c · crossword 3-trinn L.2.2.d · prepositions mc 2-trinn / fillin 3-trinn L.K.1.e (Språk og begreper). **Gate PASS 0F/0dup over 2390, worst within-class 0.750.** **NEW DURABLE LESSON — within-mode similarity is driven by the P1/P2 LENGTH-RATIO:** word-scramble within-mode FAILed 0.811 even after full-body 8-angle diversification, because its P2 templates were only 8-13w while the shared SKEL P1 (156w) + P3 (100w) dominated → same-theme/same-SKEL pages converged. Fix = lengthen P2 11w→76-84w (matching the passing sibling word-guess's 73w P2) → collapsed to **0.598**. DISTINCT from the cross-mode boilerplate→mode-true lever — when same-SKEL pages converge, grow P2 (the per-theme differentiator), don't just diversify the shared template. word-guess SKELs also lengthened to the 140-154w lint floor; other 6 clean first-gen. Repoint 499 deck.html / 3992 repl / 0 missing. Per-type: alphabet-train · matching · word-guess · word-scramble · wordsearch · crossword · prepositions.
- **Part 6 / WAVE E COMPLETE (2026-06-15, commit `0ad078f2`, deployed+repointed+verified): 2 dedicated-gen types, 221 landings (180 picture-sort + 41 chart-count; no fan total 2611).** Native bokmål ensemble. Operator fork: picture-sort token=`Sortering` (H1 keeps `Sorter bildene`). **picture-sort** 1-trinn READINESS (`Logisk tenkning — sortering`, no alignment): `gen-no-picturesort.js` clones gen-da (render2 dual-theme, 16×12=192>180 pairs injective; **B5 double-def gate DROPPED** — bokmål allows "de små dyrene"; **bokmål count-fence** `/tell|talt/`+hard-tokens(`hvor mange/antall/diagram/søyle/stolpe/statistikk`)+negators(`ikke/aldri/uten/ingen/intet/verken`, NOT da `tæl`/sv `räkn`)+cross-locale-leak lint; **band-label `1. trinn` digit exempted** from the no-digit count check); native 16 SKEL+12 P2+P3 in `no-picturesort-frames.js`. **chart-count** 2-trinn STANDARD-BEARING (K.MD.B.3+LK20, `Statistikk og sannsynlighet`, grade-led): `gen-no-chartcount.js` clones gen-da (honest-count Quiz `Hvor mange <fl>?`=rendered bar from `_no-cc-facts.json`, woven `3 katter, 4 hunder og 5 ender`, 0-mismatch assertion; native p1×7/p2×6/p3×7 + native OVERRIDE_BY_NAME [**tre→trær compounds; Månen def→Måne; postbud neuter zero-plural** `et postbud/flere postbud`]; **de-dup-by-plural guard** for the Postbud/Postbud-2 duplicate-category collision; 49 facts−8 cull=41). **cryptogram=PROVEN-UNLANDABLE** (100 no decks, 0 subjectTags; DON'T-LAND, logged). **Gate PASS: all 2611 lint-pass, ALL-PAIRS max 0.750, 0F/0dup** (Wave-E within-type chart-count 0.366 / picture-sort 0.739 same-SKEL reuse). Repoint 222 deck.html. **NEW LESSON: dedicated-gen clone needs the no-digit count-fence to EXEMPT the band label** (da's readiness band had no digit; no's `1. trinn` does — `body.replace(/\d\.\s*trinn/gi,'')` before the digit test). **NEXT = Part 7 `no` CLOSE** (mechanical: per-type count table, sitemap shard-4 picks up no at revalidate, memory/parent-SoT update — no native ensemble needed).
- **WAVE-C BUILD LESSONS (durable):** (1) **multi-mode-of-ONE-type is the real cross-mode risk, not cross-type** — when a type's modes share a dominant identity frame (puzzle-reveal / picture-value), making the per-mode distinguisher (operation / term-count) LOAD-BEARING is mandatory or they collapse ≥0.80; the §22.1 boilerplate→mode-true lever applies WITHIN a type too. (2) **band-split annotation pattern** = `annotate-no-bands.js` on Hetzner (enum banded → annotate level+standard by quantity → pscp back); uniform types just `enum --level=<band>` + cfg.standard(mode). (3) **never inherit da's band ruling** — re-grade on the no catalog's actual rendered quantities (math-worksheet was band-split, not da's uniform). (4) the GEN-LINT gotcha (SKEL ~140-165w or the 200-floor fails) bit a 3rd agent — code-addition.
- **GEN-LINT GOTCHA (Wave B lesson):** the gate's ≥200-word floor is on the RENDERED body (p1+p2+p3). Authoring agents must write SKEL ~140-165w / P2 ~60-80w — a terse agent (pattern-train wrote ~80w) under-shoots. And NEVER put an indefinite article before a placeholder (`et {H1}`/`en {N_PL}`) — the theme noun is plural; the article must govern a FIXED noun (bilde/vogn). Both surfaced via `gen-no-readiness` B2 lint + the `short` count.
- **Wave-A reusables for B-E:** `gen-no-readiness.js` config engine + `rekey-no-titles.js` (token refinements folded) + `enum-no-coords.js` proven; the per-type config shape (`{type,eyebrow,strand,slotWord,level,h1(mk,theme),carousel(mk,h1Display),modes:{<mk>:{SKEL:[8],P2:[7]}},P3}`); native-agent authoring (one config/agent, OWNED-lexicon+fences in the prompt) = first-try gate pass; the boilerplate→mode-true sharpening lever for any elevated cross-mode. Voice-guide artifacts: `go-luminous-moon-agent-a76fa0de…` + `…aeecd48e…`.

## `da` COMPLETE (2026-06-12) — 28 types / 2,636 landings live + gated + repointed (8th... 7th complete locale)
Closed same-day under the Fable unified commission (SoT `commission-to-fable-twinkling-tide.md`). Commit `76905828`. **Final gate PERFECT PASS (0 FAIL / 0 WARN over 3.47M pairs, max 0.648)** — the agent-authored-config + per-type-register-fence pattern produced zero gate iterations (vs nl's two pattern-train rewrites). da ledger (autonomous, native ensemble): bands boernehaveklasse (børnehave+0.klasse merged, 3-6) / 1-klasse (≤20) / 2-klasse (21-100); strands Tal og algebra / Statistik og sandsynlighed / Matematisk opmærksomhed (statutory!) / Visuel opmærksomhed og logisk tænkning / Bogstavkendskab / Den første læsning / Stavning / Ordforråd og sprogforståelse; titles `[Op] til [N] [uden tierovergang] – [Tema] | til print PDF gratis` (numeric) / `[Type] – [Tema] | opgaver til print PDF gratis` (uden tierovergang ATTESTED → title-eligible, sv-shape); keywords opgaver-led (NEVER arbejdsark/regneark): Plusopgaver/Minusopgaver/Ordjagt/Kryds og tværs/Forlyd/Bankoplader(!)/Find skyggen/Labyrint/Byg et ord/Sortér billederne. **da render engine: Danish suffixal definiteness (decl1 +ne / decl2 +ene / doubling literals) + the B5 NO-DOUBLE-DEFINITENESS lint (de små dyr, NEVER de små dyrene — the inverse-Swedish trap). LEVELS collision solved via locale-prefixed lookup (`da:1-klasse` → '1. klasse' lowercase; de '1. Klasse' verified unregressed on rendered DOM).** math-worksheet LANDED (95, all ≤10 — 4th locale beating the inherited removal); picture-sort 211 pairs needed 16×14=224 cells (first cell-space expansion); chart-count honest-count Quiz with ~80-entry vetted Danish plural override map; prepositions fillin=2-klasse (write-load, da-specific divergence from nl); cryptogram proven-unlandable (101/101).

## `nl` COMPLETE (2026-06-12) — 27 types / 2427 landings live + gated + deployed + repointed
Closed under the **Fable unified landing-system commission** (SoT: `commission-to-fable-twinkling-tide.md` — Gate-1-ruled architecture: browse layer = sitemap shard 4 + landing mesh + hub browser, NO new indexable facet tree; sequencing nl-close → browse foundation → da→no→fr→it→pt→fi). Final commit `fb7996f5`. Key events: (a) **tracker-vs-reality**: the "484 live" math-reasoning claim was FALSE (rendered 404; local-only) — rendered-artifact rule caught it; (b) **de-de doubled-article defect** (`de {N_PL_DEF}` template composition) root-fixed across 7 configs + B3 lint added to gen-nl-readiness.js; 56 LIVE defect lines (find-objects/odd-one-out/math-puzzle) fixed by the regen+deploy; (c) pattern-train cross-mode gate FAILs (28, max 0.861) collapsed via aabb paren-architecture + abb echo-mirror rewrites + mode-coded h1 (`Patroontrein (AAB-patroon) met …`); (d) **literacy ledger ruled autonomously** (3-agent native ensemble): matching/letter=kleuters readiness (SLO eind-groep-2 letterkennis — diverges sv åk-1), prepositions/multiplechoice=groep-3 (written options force reading — diverges sv readiness), word-guess/scramble normal=groep-4 with **L.1.2.d re-anchor** (band-honest code), titles re-keyed to the nl literacy harvest (Alfabet/Beginletters/Woordjes lezen/Ontbrekende letters/Husselwoorden/Woordzoeker/Kruiswoordpuzzel met plaatjes/Voorzetsels); (e) picture-sort 139 `-vs-` pairs via gen-nl-picturesort.js (Dutch count-fence, tel-stem negation windows); (f) **cryptogram PROVEN-UNLANDABLE** (101/101 zero subjectTags + rendered cipher-only). gate-4 FULL PASS over 2427 (max 0.755); repoint 1125 deck.html. gen-nl-readiness gained cfg.level-as-function (band-split-by-mode). verify-deorphan nl config committed (puppeteer run still owes — no Chrome on Hetzner).

## Sequence (operator-ruled) — `da` NEXT
**sv → nl → da → no → fr → it → pt**, with **fi ESCALATED separately** (fi = 29 seed decks, no real catalog → needs a ~3,000-deck `publish-wave` first; deck-production upstream of the landing program). The other 7 are all catalog-ready (live DB 2026-06-11: each ~3,000 published). Novel-first front-loads the new render engines/band shapes (sv Nordic definiteness, nl groep system, no grade-offset) before the de-proven Romance bulk (fr/it/pt).

## `nl` IN PROGRESS (started 2026-06-12) — ledger-lock RULED, foundation LIVE, lead-type coords done
**SoT = plan file `C:\Users\rkgen\.claude\plans\commission-to-cc-tidy-candy.md`** (the nl ledger-lock + the STEP-2 fan plan + the BUILD PROGRESS TRACKER — resume there). **STEP-1 ledger-lock RULED (operator, 2026-06-12):**
- **Framework** chip `SLO-kerndoelen` / JSON-LD `Kerndoelen primair onderwijs`. Math `Getallen en bewerkingen`; data/chart-count `Meten en gegevens`; readiness (i) math-reasoning `Voorbereidend rekenen (ontluikende gecijferdheid)` / (ii) visual-logic `Visuele waarneming en logisch denken`; literacy `Voorbereidend lezen (fonemisch bewustzijn)` + `Taal`/`aanvankelijk lezen / spelling`; `'Language'.nl='Taal'`.
- **3-band spine (split-at-20, SLO-correct):** `kleuters` (groep 1-2, readiness/no-standard, EN Preschool+K, ages 4-6) / `groep 3` (EN K `K.OA.*` + Grade-1 `1.OA.*`, symbolic ≤20 CARRIES, ages 6-7) / `groep 4` (EN Grade-2 `2.NBT.*`, symbolic 21-100 CARRIES, ages 7-8); groep 5 OUT. groep-3 = formal-math boundary. **The operator's provisional "split-at-10" was corrected to split-at-20 by the SLO evidence (within-20 sits in groep 3).**
- **Keyword (RANGE-LED):** `Werkbladen [optellen/aftrekken] tot [N] – [Thema] | printen PDF gratis`. **no-carry qualifier OMITTED** (fork ①: "zonder tienoverschrijding" is a generator-checkbox label, not a search term; honest-fit moves to body prose; drop the no-carry title gate for nl). `printen` is the native print verb; protected trio.
- **Classification (picture-sort/odd-one-out) → visual/logic bucket** (fork ②); STANDING RULE all locales: sort/order family splits by CRITERION — number/size/quantity→math-reasoning, perceptual identity→visual/logic. big-small+more-less=quantity→math-reasoning.
- **Render: NO morphology engine** — Dutch rides es-style pure substitution (definite plural uniformly "de "+plural, gender non-load-bearing in plural; no Swedish `*djurna` trap). `nl-themes.js {gender(de/het),plIndef,plDef,h1Display}` + mass-surrogates; hand-verify every plural; prose plural-only.

**SHIPPED — 4 types / 484 landings LIVE + verified:** foundation `a6187f7f`; **grid-match** 45 (readiness/kleuters, `062ea64d`) — DEV-RENDER PASSED (de/het non-trap `de dieren` + mass-surrogate Fruit-title/vruchten-prose + Kleuters chip + Visuele waarneming strand + 0 alignment + hreflang en/de/es/sv/nl+x-default); **addition** 158 (groep-3 CARRIES, `97bb6e53`) — DEV-RENDER PASSED (range-led `tot 10` title + Groep 3 chip + K.OA.A.2 / find-addend K.OA.A.4 + kleuters-stays regression); **subtraction** 159 (groep-3 CARRIES, `58500937`, diverged from addition, op-twin 0.737 PASS); **math-puzzle** 122 (`681a60ff`, **band-split@20: 89 groep-3/1.OA.C.6 + 33 groep-4/2.NBT.B.5**, 0 over 100; 1st-draft op-twin FAIL 0.829 → re-sharpened build-up-vs-take-away → PASS). **Both novel render surfaces discharged → remaining ~23 types MECHANICAL.** Per-type loop: enum (Hetzner) → native Dutch prose config (ensemble, DIVERGE from siblings; **singular-definite §14.6 watch armed, esp. literacy**) → `gen-nl-readiness.js` → `rekey-nl-titles.js` → `gate.js frontend/content/seo-landing/nl.json` (BOTH lint+similarity, ship on 0-FAIL; re-author any FAIL) → commit+push → `deploy.sh` → `repoint-deck-canonical.js --types=<t> --locale=nl`. Banded types: enum (no level) → `check-sum-ceiling.js --locale=nl` → annotate coords with per-coord level+standard. No per-type dev-render now.

**code-addition** SHIPPED (`7804e1af`): 48, **band-split 29 groep-3/1.OA.A.2 + 19 groep-4/2.NBT.B.5** (spans 20, derived via `check-sum-ceiling --source=deck-html`; max 26, 0>100). **LIVE TOTAL now 5 types / 532.**

**▶ RESUME the mechanical fan at chart-count** (DEDICATED `gen-nl-chartcount.js` clone of `gen-sv-chartcount.js` + nl `_cc-facts.json` server dump; strand native SLO `Meten en gegevens`; groep-3 K.MD.B.3; honest-count Quiz `Hoeveel <plural>? → N`) → find-and-count (mode-split) (+Quiz, dedicated gen-nl-chartcount.js) → find-and-count (mode-split) → readiness bulk (2 buckets, diverge from grid-match) → literacy ~7 → picture-sort (-vs- dedicated) → cryptogram (prove-unlandable). math-worksheet REMOVED. Full order + per-type notes = the plan file `commission-to-cc-tidy-candy.md` BUILD PROGRESS TRACKER.

## `sv` COMPLETE (2026-06-12) — 27 types / 2478 landings live + gated + deployed + repointed
Final commit `24e9a105`; per-type `bb84ebf5`→`24e9a105`. **cryptogram PROVEN-UNLANDABLE** (themeless wall: 0 subject_tags, 0 images, generic `Bildkryptogram` — DON'T-LAND, surfaced not folded into a done-line). **math-worksheet REMOVED** (off-ceiling). Both novel render surfaces flushed + proven live; both readiness buckets + the math CARRIES core + the chart-count Quiz (41 honest-count nodes) + the picture-sort `-vs-` de-orphan all shipped; hreflang chain en/de/es/sv + x-default verified.

## sv ledger-lock rulings (operator-ruled; binding precedents for the Nordic family)
1. **within-10 SYMBOLIC arithmetic = åk-1 CARRIES** (förskola=no-standard invariant; strand `Taluppfattning och tals användning`; EN R3 anchor K.OA.A.2 direct / K.OA.A.4 find-addend; range 0–10; `utan växling` honest-fit, direct-modes-only) — addition/subtraction/math-puzzle-within-10/code-addition. Guardrail: a genuinely *concrete* count-the-pictures coord = readiness (förskola), not åk-1 → surface borderlines.
2. **chart-count = åk-1 CARRIES K.MD.B.3** (strand `Sannolikhet och statistik`; the bar-chart is a data abstraction, not a tally) + the **honest-count Quiz node** (`practiceProblems: Hur många <plural>? → N`, N = rendered bar = prose count, agree by construction). Dedicated generator `gen-sv-chartcount.js` (reads a per-deck `_sv-cc-facts.json` counts dump).
3. **two readiness buckets by MECHANIC** (förskola, no standard, dashed chip): math-flavored reasoning (pattern-train/pattern-worksheet/big-small/more-less) → `Matematiska resonemang och uttrycksformer`; pure visual-spatial/logic (grid-match/shadow-match/missing-pieces/find-objects/bingo/picture-path/odd-one-out/sudoku/picture-sort/treasure-hunt) → `Visuell perception och logiskt tänkande`. The es-calque `Matematiska begrepp och mönster` is REJECTED → must appear in NO authored sv field (gate assertion grep=0).
4. **3-band +1-year-Nordic-shift spine** (level keys `forskola`/`ak-1`/`ak-2`): `förskola` (EN Preschool+Kindergarten+all readiness, 3–6) → `åk 1` (EN Grade 1, age 7, within-20) → `åk 2` (EN Grade 2, age 8, within-100); `åk 3` (age 9) = OUT. EN-Kindergarten content → förskoleklass *readiness*, not a formal grade.
5. **locked keyword pattern (RANGE-LED):** `Arbetsblad [op] 0–[N] [utan växling] [qual] – [Tema] | skriva ut PDF gratis`. The format-trio (`skriva ut` + `gratis` + `PDF`) is protected; on overflow tighten other quals, never the trio.

## Nordic definiteness render engine (the novel surface — de-risks da/no)
`scripts/seo-landing/sv-render.js` is **PURE SUBSTITUTION, ZERO morphology** — Swedish has NO case (the de `datN` dative axis disappears); the agreement axis is DEFINITENESS (species). Each theme stores `plIndef`+`plDef` as hand-verified literals (`sv-themes.js`: `{gender, decl, plIndef, plDef, h1Display}`; gender `t`=ett/neuter `n`=en/common per §A.13.58 per-locale-gender-authority, NOT de m/f/n); templates pick the form by placeholder (`{N_PL}` indef / `{N_PL_DEF}` def / `{H1}` display) so no code computes a suffix → the `djur→djuren` never `*djurna` error is **structurally impossible**. `assertThemeTable` FAIL-halts on a wrong definite plural (A1 `*djurna` guard via `expectedPlDef` per declension). PROVEN live (grid-match `djur` artifact: `djuren`×6, zero genuine `*djurna`). **da/no reuse this engine, only the definite-suffix rule swaps; fi needs a new `fi-render.js` (agglutination + case).**

## Per-locale machinery (the playbook — clone for nl/da/no/…)
`scripts/seo-landing/`: `sv-render.js` (engine+assertion) · `sv-themes.js` (50 themes, all pass the assertion) · `gen-sv-readiness.js` (config-driven engine cloned from `gen-de-readiness.js`; runs `assertThemeTable` first then per-landing definiteness-lint + ≥200-word check, both FAIL-halt; reads per-coord `co.standard` for band-split) · `sv-readiness-<type>.js` configs + `sv-<type>-coordinates.json` (native prose, fed to the engine) · `rekey-sv-titles.js` (RANGE-LED pattern, `utan växling` honest-fit gate, ordinal dedup) · `enum-sv-coords.js` (generic enumerator, runs on Hetzner) · **dedicated** `gen-sv-chartcount.js` + `gen-sv-picturesort.js` (`-vs-` pairs, both themes via `render2`, 16×12=192 cells) + `enum-sv-picturesort.js`. **Shared-map edits per locale:** `frontend/lib/seo/landing-content.ts` (FILES import) + `frontend/app/[locale]/worksheets/[slug]/page.tsx` (LEVELS / FRAMEWORK_BY_LOCALE / UI_STRINGS) + `scripts/seo-landing/verify-deorphan.js` (anchorHub/goodRe/badRe). **Reused locale-agnostic:** `validity-gate.js`, `gate.js` thresholds, `check-sum-ceiling.js`, the `topic-decks.ts` de-orphan, carousel pass.

## Doctrines proven this run (apply to every locale)
- **divergence** — a new prose config must mechanic-true-diverge from already-shipped siblings AND same-theme pairs, or they cannibalize on the similarity gate (subtraction-vs-addition 0.87→0.39; math-puzzle template-collision 0.926→0.353); **never move the gate gauge — fix the content.**
- **gate BOTH sections** — §4.B lint (≥200 words / no banned phrases / **theme-noun in P1**) AND similarity; picture-path shipped 10 lint fails when only the similarity verdict was grepped.
- **cell-space scales** — picture-sort's 151 `-vs-` pairs needed 16×12=192 (8×7=56 collided at 0.94); cells strictly exceed breadth with the coprime scatter.
- **band by mechanic+quantity** — `check-sum-ceiling.js --locale=<loc>` per-coordinate; per-coord `standard` in the coordinates JSON drives band-split.

## Deploy gotchas
(i) never `pscp` a script that is also git-committed — the untracked server copy blocks `git pull`; `rm` it on Hetzner before `deploy.sh`, or commit-then-pull only. (ii) **no Chrome on Hetzner** → the puppeteer `verify-deorphan.js` can't run there; the de-orphan is locale-agnostic + proven 3 locales → applies by construction (sv config committed for when Chrome lands). (iii) per-type flow = gen → rekey → `gate.js`(both) → commit → `deploy.sh` → `repoint-deck-canonical.js --types=<t> --locale=sv` → live-verify. Landings use THIS flow, NOT the deck `publish-wave`.

## `nl` NEXT (its novelty)
SLO-kerndoelen; the **groep band system is the one genuinely-new band SHAPE** (non-collapse: kleuters / groep 3 / groep 4, where **groep-3 = the formal-math boundary** — math anchors groep 3+, readiness anchors kleuters); no-carry `zonder tienoverschrijding`; range-led `tot N`; preschool `kleuters`; Dutch gender (de/het); math strand `Getallen en bewerkingen`, readiness `Voorbereidend rekenen`. nl's full curriculum frame + the other 6 locales' frames + per-locale calque flags = plan file Deliverable 1 + the sibling agent reports. nl runs as its own plan-mode ledger-lock (3-agent native ensemble per §A.13.48) → operator rules → autonomous fan on green pre-flight.

Cross-refs: [[seo-landing-page-program]] · [[project-de-landing-fanout-investigation]].
