# G1-376 `plants` (K/G1/G2/G3) : FINAL design (editor merge, 2026-09-23)

Merged from `_work/G1-376-pedagogy.md` + `_work/G1-376-design-A.md` ("the soil window") + `_work/G1-376-design-B.md` ("Tag the plant"); every ruling and why: `_work/G1-376-critic.md`. (m) = re-measured 2026-09-23 by read-only node (`lib/b2-common.js entriesFor/vocab`, `cache/manifest.json`, `frontend/config/topics-taxonomy.json`, `frontend/lib/seo/strand-names.ts`, `scripts/seo-landing/gen-b3-landings.js LEVEL_KEYS`, `templates/components-b2.js:228`, `templates/components-b3/ordinal-numbers.js:151`, `templates/components-b3/picture-word-cards.js:73-100`, `primitives/trace-path.js:681`; scratch `scripts/worksheet-gen/scratchpad/G1-376-crit-*.{js,png}`) or by OPENING the picture (the editor opened every library picture this file names, contact sheets `G1-376-crit-{A,B,C,D}.png`). *est.* = the engineer measures in the real render with the shell woff2 from `file://` (`render/one.js`). No em-dashes.

**Boundary (load-bearing).** This family owns four things nobody has: (1) the DRAWN PLANT, a NEW palette-only primitive (`primitives/plant-figure.js`: a full flowering plant in a see-through soil box with 6 part ids `root stem leaf flower fruit seed`, the same plant as 6 growth stages, a flower cut-away, and small part icons); (2) the PLANT-PART vocabulary as a closed panel-authored literal set (root/stem/leaf/flower/fruit/seed; flower organs petal/sepal/stamen/pistil); (3) what a plant NEEDS judged from given conditions (both light AND water, a conjunction); (4) the seed-to-seed CYCLE on a ring that closes back to the seed. It is NOT K-202 `fruits-vegetables` (no face asks fruit-or-vegetable; the eat face asks WHICH PART, and no chip says "vegetable"), NOT K-201 living/non-living or K-214 natural/man-made (both carry `spring/flower` as one item, m by the pedagogy), NOT K-212 wants/needs (people, bins), NOT K-322 seasons + K-338..341 (spring, draw the tree), NOT G1-203 `science-sequence` (3 LIBRARY pictures of a chicken in a LINE, "Write 1, 2, 3.", m `types/g1/G1-203-chicken-life-cycle.js:7`), NOT G1-377 `animal-life-cycles` (this batch: metamorphosis, no plant), NOT K-354 `human-body` (a figure with writing LANES glued beside it and leader lines; this family puts numbered garden TAGS on threads and all writing in ONE label card), NOT G3-385 water cycle, NOT K-225 / K-235 / K-347 picture vocabulary (no library picture is ever the plant). No face uses an animal, orders library pictures in a line, or is titled by a bare theme word (`flowers` `trees` `vegetables` `fruits` `spring`, m) or by bare "life cycle(s)". **Visual signature:** a tall teal line-art plant rising out of a cream-deep soil box whose roots show through the "glass", coral numbered tags on thin teal threads, a cream label card; a teacher across the room reads "plant, roots showing" and cannot confuse it with the body figure (lanes glued to the figure) or the chicken cards.

## 1 Identity

| field | value |
|---|---|
| id / key / bands | `G1-376` / `plants` / base **G1**; F1 **K** (`K-371+ TBD by the emitter`); F2 **G1** (`G1-381+ TBD`); F3 F4 **G2** (`G2-360+ TBD`); F5 **G3** (`G3-392+ TBD`). `default_subject: science`, `default_age_range: 6-8`, `assetClass: geometry`, `exerciseType: plants`. `apps.plants` + `axes['exercise-type'].plants` ABSENT (m; `axes['exercise-type']` has 125 keys); register with the `register-b4-taxonomy.js` shape cloned for b5 + slug/name x11 (table B). The 11 slugs grepped against every `axes.*.*.slug.<loc>` for /plant, pflanz, planta, plante, pianta, vaxt, kasvi/: **0 collisions** (m); the only near hits are the `flowers` theme slugs (`blumen blommor blomster bloemen flores fleurs fiori kukat`, m), which no title here equals. |
| theme axis | **OFF** (`themeAxis:{applicable:false}`; landings `coordinate.theme:''`). The apparatus is drawn; the few library pictures (F1 gifts, F3 foods) are FIXED `(theme, noun)` pairs in the bank with `picOpened:true`, read with `fileUri(theme, noun)` directly (never `pictureFor`, which rng-picks across themes: `corn` exists in `vegetables` AND `At the Supermarket`, m). **No `unitAxis`.** |
| CCSS (en, honest) | NONE on any face (science; no `educationalAlignment`); `teaches` = "Parts of a plant (science readiness)". en PROSE names NGSS: base 1-LS1-1 (external parts); F1 K-LS1-1; F2 readiness for 3-LS1-1; F3 1-LS1-1 (parts) with the tomato-is-a-fruit note; F4 1-LS1-1 (functions); F5 4-LS1-1 **above band** (the en landing says so). pt prose cites BNCC **EF02CI06** verbatim on base, F3, F4 and **EF02CI05** on F1. Non-EN names the national framework + the strand literal of table B (the `Science` row EXISTS in `strand-names.ts:74-86`, m; no new strand row needed). |
| new primitives / data | `primitives/plant-figure.js` (§2, geometry + node gate `qa/verify-plant-figure.js`) · `templates/components-b5/plants.js` behind a NEW `templates/components-b5.js` barrel (clone of the b4 barrel) · `data/b5/plants.js` + `data/b5/locales/plants.<loc>.json` (§5) · `lib/b5-common.js bank()` over `data/b5/` (clone of `lib/b4-common.js`, which exists, m). |

| loc | genre head (panels verbatim) | ASCII slug | level K / G1 / G2 / G3 (`LEVEL_KEYS`, m) | part words (proposal, the panel signs; root stem leaf flower fruit seed) | national strand literal (= the existing `Science` row) |
|---|---|---|---|---|---|
| en | Parts of a Plant | `parts-of-a-plant` | `kindergarten` / `grade-1` / `grade-2` / `grade-3` | roots, stem, leaf, flower, fruit, seed | Science |
| de | Teile der Pflanze | `teile-der-pflanze` | `vorschule` / `1-klasse` / `2-klasse` / `3-klasse` | Wurzel, Stängel, Blatt, **Blüte**, Frucht, Samen | Sachunterricht |
| es (MX) | Partes de la planta | `partes-de-la-planta` | `preescolar` / `primer-grado` / `segundo-grado` / `tercer-grado` | raíz, tallo, hoja, flor, fruto, semilla | Conocimiento del Medio |
| pt (BR) | Partes da planta | `partes-da-planta` | `educacao-infantil` / `1o-ano` / `2o-ano` / `3o-ano` | raiz, caule, folha, flor, fruto, semente | Ciências (+ BNCC EF02CI06 / EF02CI05 in prose) |
| fr | Les parties de la plante | `les-parties-de-la-plante` | `maternelle` / `cp` / `ce1` / `ce2` | racines, tige, feuille, fleur, fruit, graine | Questionner le monde |
| it | Le parti della pianta | `le-parti-della-pianta` | `infanzia` / `classe-prima` / `classe-seconda` / `classe-terza` | radici, fusto (or stelo, ONE word for all faces), foglia, fiore, frutto, seme | Scienze |
| nl | Delen van een plant | `delen-van-een-plant` | `kleuters` / `groep-3` / `groep-4` / `groep-5` | wortel, stengel, blad, bloem, vrucht, zaad | Oriëntatie op jezelf en de wereld |
| sv | Växtens delar | `vaxtens-delar` | `forskola` / `ak-1` / `ak-2` / `ak-3` | rot, stjälk, blad, blomma, frukt, frö (indefinite only) [NSR] | Naturorienterande ämnen |
| da | Plantens dele | `plantens-dele` | `boernehaveklasse` / `1-klasse` / `2-klasse` / `3-klasse` | rod, stængel, blad, blomst, frugt, frø [NSR] | Natur/teknologi |
| no | Plantens deler | `plantens-deler` | `1-trinn` / `2-trinn` / `3-trinn` / `4-trinn` | rot, stilk (or stengel, panel), blad, blomst, frukt, frø [NSR] | Naturfag |
| fi | Kasvin osat | `kasvin-osat` | `esikoulu` / `1-luokka` / `2-luokka` / `3-luokka` | juuri, varsi, lehti, kukka, hedelmä, siemen (nominative) [NSR] | Ympäristöoppi |

**The rule that locks the type.** Every part word on every page is a whole panel literal from `plants.<loc>.json` (`partWords` / `flowerWords` / `jobs`), NEVER a vocab lookup (`root`, `stem`, `seed`, `sprout`, `soil` have no vocab entry; `flower` is de "Blume" but the part is "Blüte", m), and every answer is a FACT of the drawn figure (a tag's part id, a stage index) or a bank field (`EAT[i].part`, `NEEDS`), never a word the code forms. The code substitutes and stamps; it never inflects.

## 2 The base page

**Concept.** "Parts of a Plant: tag the plant." One 600 px plant fills the left of the page, standing in a see-through soil box so the roots are as visible as the flower (the first K-3 misconception, "plants have no roots", is answered by the apparatus). Six coral numbered tags hang on threads from six parts; each thread ends in a small teal ring round the exact part (one seed inside the opened pod, not the pod; the solid pod tip, not a leaf). On the right a cream label card holds six numbered writing rows; a six-word bank sits on top. The child follows a tag's thread, then writes that part's name on the row with the same number. B's layout + the pedagogy's pod-with-seeds + A's botany rules (roots only below the soil line, leaves attached at nodes, leaves smaller up the stem, the flower at the apex); A's two-sided lanes and magnifier are rejected (critic §1).

**Chrome budget (README ruling).** Body **722** with a 3-line title + 3-line instruction; every stack is checked at **677** (4-line fi title). `.ws-page` inner 675 (m). Root grid rows `59px 10px minmax(560px,1fr)`; the stage keeps the figure at h 600 and centres it.

**Layout d2**
```
y 0    wordBank 675 x 59 (.ws-scene-banner, 6 .ws-bankword Nunito 800 18; order = rng shuffle != tag order)  + margin 10
y 69   stage row 675 x 600 (grid 380px 16px 279px; data-ws-content)
       +---------- plant stage 380 x 600 ----------+ 16 +---- label card 279 x 478 (centred, 61 above/below) ---+
       | plantFigure({stage:'full', ground:'box',  |    | .ws-card cream, border 2, radius 16, padding 12         |
       |   h:600}) viewBox 320x600, scale 1.0,     |    | row = [badge 40][10][.ws-blankbox 205 x 64]              |
       |   x-offset 30 (figure x 30..350)          |    |   badge: teal ring r 18 stroke 3, Baloo 2 700 22 numeral |
       | 6 coral tags r 15 at TAG_SLOTS, teal 1.5  |    |   box: writingRow({w:199, h:58, glyphH:30, xHeight})     |
       |   threads, end ring r 8 teal 2.5          |    | 6 rows, row gap 14                                       |
       +-------------------------------------------+    +----------------------------------------------------------+
```
Width 380 + 16 + 279 = **675** (m). Card inner 279 − 24 = 255 = 40 + 10 + 205. Height 59 + 10 + 600 = **669 <= 677 <= 722** (m). Card 6 x 64 + 5 x 14 + 24 = **478 <= 600** (m). Bank: bare nouns only (no article literals on this type), ONE row: longest six *est.* de "Wurzel Stängel Blatt Blüte Frucht Samen" 39 chars x 7.8 + 6 x 36 ≈ 520, fi ≈ 512, < 639; the validator measures and FAILS a two-row bank (108 + 10 + 600 = 718 > 677). Writing: an 8-letter part word at glyphH 30 ≈ 18 px/letter *est.* = 144 in a 199 px row (+38 %); longest d2 literal 7 letters (de Stängel, es semilla, pt semente, fi hedelmä).

**Ladder** (resolved config; guards key on THESE, never the level index):

| key | d1 | d2 (ships) | d3 |
|---|---|---|---|
| `parts` | root stem leaf flower | **root stem leaf flower fruit seed** | same six |
| `ground / figureH` | box / 600 | **box / 600** | box / 600 |
| `bank` | true | **true** | false (recall) |
| `rows / rowH / glyphH` | 4 / 72 / 32 | **6 / 64 / 30** | 6 / 64 / 30 |
| `anchorPick` | rng | **rng (one of `ANCHORS[part]`)** | rng |

**d3 ruling:** no-bank recall is a scaffold level, not a teaching move; not shipped, no copy describes it.

**Composer.** For each part pick one candidate anchor (`ANCHORS`, §2 primitive; flower/leaf/stem/root have two each, fruit/seed one: 16 combinations); tag numbers = `rng.shuffle([1..N])`, re-drawn until NOT top-to-bottom (y-sorted) order and NOT bottom-to-top; bank order = `rng.shuffle`, re-drawn until it differs from the part order of tags 1..N AND from its reverse. The seed is locale-neutral: same anchors, numbers and bank order in all 11 locales; only the six literals change.

**Answer-hiding + uniqueness.** No part name is drawn anywhere; rows are EMPTY. The mapping lives only in stamps: each tag `<g data-lcs-tag data-lcs-part="<id>" data-lcs-n="<k>">`, each card row `data-lcs-row-n="<k>"`, each bank word `data-lcs-bank="<id>"` (via `wordBank`'s `vocabKey` field = the part id, m `components-b2.js:228`), root `[data-lcs-plant][data-lcs-stage="full"]` + `data-lcs-anchors` (px JSON). Each part on exactly one tag, each number on exactly one row, every bank word used once. `verify(page)` re-derives: bank id set <=> tag id set === `d.parts` (bijection); tag numbers === {1..N}; rows 1..N empty (no text node, no SVG `<text>`); every thread end within 1.5 px of the primitive's returned anchor; the root anchor BELOW the soil line y and every other anchor ABOVE it; tag centres pairwise >= 32 px; no thread segment intersects another thread or another tag; numbering not y-sorted either way; bank order !== tag order and !== reversed; no `<img>` on the page; every bank literal === `partWords[loc][id]` (node cross-check).

**Reused (exact).** `wordBank({words,wordPx})` (`components-b2.js:228`, m) · `writingRow({w,h,glyphH,xHeight:true})` (`primitives/trace-path.js:681`, m) · `blankNumeralBox({w,h})` (`components-b3/ordinal-numbers.js:151`, m: always stamps `data-lcs-answer`, empty unless passed) on F4 · `scissorsGlyph` + `cutLines({w,h,cols,rows})` (`components-b3/picture-word-cards.js:73,84`, m: dashed `grid` frame + dividers, no gutter) on F2 · `cardGrid({cards,cols,rows})` (`templates/layouts/card-grid.js`) on F3 · `svgRoot el roundedRect line circle label esc` (`primitives/_svg.js`) · `fileUri` (`image-cache/resolve.js`) · tokens cream creamDeep white teal tealSoft coral coralSoft ink inkSoft grid, `stroke.primitive 3 / grid 1.5 / accent 4` · `.ws-card .ws-blankbox .ws-scene-banner .ws-bankword` · `bank(name, loc)` via `lib/b5-common.js`.

**NOT used.** `bodyLabelStage` / `placeLanes` / `bodyLeaderSweep` (lanes beside the figure is the K-354 look) · `answerBox` (stamps `data-lcs-answer="undefined"` without an answer) · `science-sequence.js` / `science-pair-match.js` / `science-category-sort.js` and every `data/science/*.json` bank (fence) · `water-cycle.js` sun (F1 uses the OPENED `weather/sun`; `space/sun` stays BLOCKED) · `image-vocabulary.js` / `objForms` / `articles.js` / `approved-words-*.json` at render · `pictureFor` (rng across themes).

**NEW `primitives/plant-figure.js`** (pure SVG on tokens; Node-testable; exports `plantFigure`, `plantStage`, `flowerSection`, `plantPartIcon`, `ANCHORS`, `TAG_SLOTS`, `FLOWER_ANCHORS`, `MIN_H`). One drawing language across all four drawings: outline teal 3 px rendered at EVERY size (user units = 3/scale, the body-figure rule), round caps/joins; detail lines (veins, root hairs, pod seam, hilum) 1.5 px; fills: leaves + sepals + pod skin `tealSoft`, petals `white`, flower centre `creamDeep` with 5 `grid` dots r 2, **seeds `teal`** (the only solid dark fill, so a seed is findable in mono), stem `white`, root `white` (drawn as strokes), soil `creamDeep`, pebbles `grid` stroke 1.5 no fill, pot `creamDeep`. **Coral never appears in the plant** (tags and F2's nothing else). No text inside any drawing; no hex outside tokens.

(a) `plantFigure({ stage='full'|'flowering', ground='box'|'none', h=600, tags=[], ring=null, id }) -> { svg, width, height, scale, anchors, tagSlots }`. **viewBox `0 0 320 600`**, midline x 160, **soil line y 440**; `scale = h/600`, `width = 320 x scale`. `MIN_H = 180` (throws below; the F5 inset is the smallest caller).
| part | geometry (viewBox units) |
|---|---|
| ground `box` | rounded rect x 40 y 440 w 240 h 152 rx 14, fill creamDeep, stroke teal 3 (its top edge IS the soil line); white 3 px glass glints (52,462)-(70,452) and (52,476)-(60,471); 6 pebbles ellipse rx 5 ry 3.5 at (70,520) (236,468) (250,560) (96,578) (212,584) (128,454), each >= 8 units from any root (asserted). `none`: soil omitted, roots still drawn |
| stem | closed path fill white `M 155 440 Q 152 270 156.5 100 L 163.5 100 Q 160 270 165 440 Z` (10 wide at the soil, 7 at the flower) |
| leaves (4, alternate, attached at nodes, smaller up the stem) | blade fill tealSoft + midrib base->tip 1.5 + 2 side veins 1.5: N5 left `M 152 162 Q 118 118 76 132 Q 110 176 152 162 Z` · N4 right `M 164 174 Q 200 128 244 140 Q 208 188 164 174 Z` · N2 right `M 164 316 Q 206 266 252 282 Q 214 330 164 316 Z` · N1 left `M 152 408 Q 116 362 70 372 Q 104 420 152 408 Z` (upper-edge min y 370.2 at x 86, m) |
| flower (apex, 5 separable petals, not a tulip) | centre disc (160,64) r 14; petal k = 0..4 ellipse rx 15 ry 24, long axis radial, centred 28 from the centre at 72k degrees clockwise from straight up; bbox x 110..210, y 12..106 (petal-tip radius 52); 2 sepals tealSoft, leaf shape L 16 W 9 from (160,86) at 120 and 60 degrees |
| fruit = OPENED POD on a side stalk | stalk `M 156 232 Q 128 226 112 244` stroke 3; pod capsule axis (110,250)->(98,344) width 30, rounded ends, fill tealSoft (bottom y 359.0, m); seam line 1.5 along its right edge; cut-away WINDOW = the capsule inset 5 units, clipped to y 262..332, fill white; **4 seeds** discs r 7 fill teal at (107.6,268.8) (105.2,287.6) (102.8,306.4) (100.4,325.2). The pod tip y 336..359 stays solid skin so the FRUIT anchor never rings a seed. Pod bottom 359 vs N1 upper edge 370.2: **11.2 units clear** (m) |
| roots (below the soil line ONLY) | taproot `M 160 440 C 162 480 156 520 160 580` stroke 3; laterals stroke 2.5 `M 159 462 Q 130 470 104 500` · `M 158 500 Q 128 516 112 556` · `M 161 470 Q 192 478 218 506` · `M 159 530 Q 186 542 204 574`; 8 root hairs 1.5 (6-unit ticks, 2 per lateral at t 0.5 / 0.8) |
| draw order | ground fill, roots, ground stroke + glints + pebbles (box stroke drawn OVER the roots so they read as inside), stem, leaves, pod stalk, pod, window, seeds, flower, then tags |
| `flowering` stage | `full` without the pod, its stalk and the seeds (F5 inset) |

**`ANCHORS` -> `TAG_SLOTS`** (viewBox units; the composer picks one per part):

| part | anchor A -> tag slot A | anchor B -> tag slot B |
|---|---|---|
| flower | (190,54) petal 1 -> (272,40) | (130,54) petal 4 -> (48,40) |
| leaf | (205,156) blade N4 -> (296,156) | (111,390) blade N1 -> (24,392) |
| stem | (159,220) -> (296,220) | (157,200) -> (24,200) |
| root | (205,494) right lateral -> (296,500) | (130,476) left lateral -> (24,476) |
| fruit | (99,348) solid pod tip -> (24,352) | none |
| seed | (105.2,287.6) seed 2 -> (24,296) | none |

Clearances (B's computation, the editor re-ran the N1 edge): the stem-A thread at y 220 runs between N4's lower edge (max y ~178) and N2's upper edge (min ~280); stem-B at y 200 between N5 (max ~164) and the pod stalk (226); the fruit thread at y ~350 passes >= 18 units above N1's upper edge (370.2 at x 86, m); left tags 40/200/296/352/392/476 pairwise >= 40. **Tags** `tags:[{part, n}]`: white halo circle r 19 stroke white 4, coral disc r 15, white Baloo 2 700 numeral 18 px (sized in px, not units); thread teal 1.5 from the disc edge to the end ring; end ring r 8 stroke teal 2.5 fill none over a white halo stroke 5. **Node gate `qa/verify-plant-figure.js`** (the `verify-body-figure.js` pattern): for ALL 16 anchor combinations at h 600 AND h 570 (F4): no thread intersects another thread or another tag; each anchor inside its own part's shape (point-in-path on the emitted geometry, not a bbox); pod and N1 do not intersect; root anchors below y 440 and every other anchor above; every tag inside the viewBox; only token hexes; `MIN_H` throws; returned px anchors = viewBox x scale + offset. Minimum drawn sizes at h 600 (asserted): leaf N4 blade ~ 82 x 36, flower 100 x 94, pod 30 x 118 (window 20 wide), seed 14 x 14 (a pointed-at detail, ringed, never a target the child marks), root system 114 x 140.

(b) `plantStage({ stage, w=116, potted=false, id })` for F1 and F2 (a compact viewBox so a stage fills a card; A's geometry, B's fills). **viewBox `0 0 200 240`**, `h = w x 1.2`. Ground line y 150 x 8..192; soil rect y 150..232 bottom r 14 creamDeep; 4 pebbles (30,170) (170,176) (44,222) (160,220).
| stage | drawing |
|---|---|
| `seed` | seed oval rx 26 ry 17 at (100,190) rotated −12°, fill teal, hilum arc 1.5 white; nothing above ground |
| `sprout` | the seed at (100,196) with a V split in the coat; radicle (92,206)->(86,230) w 6->2; shoot hook (104,184)->(106,152) then arc to (98,138): the hook just breaks the ground |
| `seedling` | no coat; stem (100,150)->(100,92) w 6; two cotyledons ellipse rx 20 ry 11 tealSoft at (80,88) −25° and (120,88) +25°; taproot to (100,214) + 2 laterals |
| `young` (d3 only) | stem to (100,40); shrunk cotyledons; true leaves at nodes y 96 (L 44 W 20) and y 60 (L 32 W 15); taproot + 3 lateral pairs |
| `flowering` | young + flower at (100,30) r 20 (5 petals white) + 2 sepals |
| `fruiting` | young; the apex carries a dry receptacle (3-point star r 6, grid 1.5, no petals); from node y 70 a stalk to an OPENED POD capsule (132,74)->(146,138) width 28, window inset 4 clipped to y 84..128, **3 seeds** r 6 fill teal on the axis (the seeds the ring returns to) |
| `potted` | replaces the soil: rim rect (42,140) 116 x 16 r 4, pot trapezoid (48,156)-(152,156)-(138,232)-(62,232) creamDeep teal 3, soil top ellipse (100,150) rx 50 ry 6; roots NOT drawn |
At w 116 (scale 0.58): seed 30 x 20 px, cotyledon 23 x 13 px, flower 23 px, pod 16 x 38 px, pod seed 7 px. Root stamps `data-lcs-figure="plant-<stage>"`.

(c) `flowerSection({ h=418, tags=[] })` for F5 (B's section). **viewBox `0 0 400 440`**, side view of a cup flower with the two FRONT petals cut away: stalk capsule (200,440)->(200,330) w 12 fill white; receptacle ellipse (200,322) rx 26 ry 12 tealSoft; sepals tealSoft left `M 186 322 Q 150 330 128 360 Q 164 350 192 330 Z` + mirror about x 200; back petals white: left `M 180 320 Q 96 250 104 150 Q 150 170 186 300 Z`, mirror right, centre `M 186 316 Q 170 190 200 120 Q 230 190 214 316 Z`; pistil = ovary ellipse (200,298) rx 18 ry 20 + style rect x 196 y 190 w 8 h 90 + stigma ellipse (200,184) rx 14 ry 8, all creamDeep (LIGHT in mono); 4 stamens = filaments 2 px (188,300)->(152,206), (192,300)->(170,196) + mirrors, anthers ellipse rx 8 ry 12 fill teal (DARK in mono). **`FLOWER_ANCHORS` -> tag slots:** petal (130,220) -> (40,220) · sepal (150,344) -> (40,360) · stamen (250,202) -> (360,202) · pistil (200,236) -> (360,262) · stalk (200,400) -> (360,400) · (d3) ovary (200,298) -> (360,320). Gate: point-in-shape per anchor, no thread crossing, tags >= 32 px.

(d) `plantPartIcon({ part, size=32 })` for F3 chips, **viewBox `0 0 64 64`**, same language: root = soil line (6,14)-(58,14) teal 3 + taproot and 4 laterals below · stem = capsule x 28..36 y 4..60 fill white + two leaf stubs grid · leaf = the N4 blade scaled to 56 wide · flower = 5 petals at scale 0.55 · fruit = the pod closed, diagonal · seed = 3 teal seeds rx 9 ry 6. `<svg data-lcs-part-icon="<part>">`.

**NEW `templates/components-b5/plants.js`** (behind `templates/components-b5.js`; scoped inline CSS; no `page.css` edit): `plantTagStage({parts, anchorPick, numbers, h=600, w=380})` · `plantLabelCard({rows, rowH=64, glyphH=30, boxW=205})` · `plantBank({words})` (-> `wordBank`) · `needUnit({gifts:[{src,key}], potH=110, gift=64, grows})` · `cycleRing({slots:5, given:'seed', slot:[124,150], R=165, cx=337.5, cy=240})` (exported; G1-377 may import it, its critic rules) · `cycleStrip({stages, card:[124,150]})` (-> `scissorsGlyph` + `cutLines`) · `eatCard({src, chips:[{part, word}], pic=96, chipW=150, chipH=38})` · `jobCard({jobs:[{part, text}], boxW=52, textPx=17})` · `flowerStage({parts, decoy, h=418})`.

**Alternatives (rejected).** A's soil window with lanes both sides + a magnifier on the fruit: K-354's grammar with a plant in it (the hub and the room would blur the two families), the plant squeezed to 299 px, and the seed 8.3 x 5.8 px inside a second object a child may read as a ball on a stick. The classic potted tulip: a pot hides the roots, a tulip is a bulb with no fruit. A pressed herbarium sheet (B's alternative): no soil, and "roots grow underground" is the first thing the unit teaches.

**Risks -> mitigations.** Thread ambiguity (the seed thread crosses the pod skin): the ring encloses ONE seed and the gate proves point-in-shape; a human reads the 20-seed sweep at 100 %. Pod-as-fruit surprises a child who expects an apple: the landing says "the pod is the fruit; the seeds are inside it" (and pt/es/it write "fruto/frutto", never "fruta/frutta"). The seed is 14 px: a detail, not a target (below the G1 44 px element floor, like K-354's eye). Long locales: one-row bank asserted; rows 199 px hold 11 letters *est.* **Palette:** cream, creamDeep, white, teal, tealSoft, coral, ink, inkSoft, grid. **Font floor:** smallest text 16 (tag numerals 18, job text 17, chip words 17); footer 10 is the shell's (`qa/lints.js:86` 9 px). Every stage/grid root stamps `[data-ws-content]`. **Print check:** the engineer prints one d2 of the base, F2 and F5 on a mono laser at 100 %: tags read mid-grey with white numerals, seeds and anthers are the darkest marks, the soil box reads, one F2 card is cut and glued flat into a slot.

## 3 Faces 2-6

All five are CODE faces on ONE additive `layout` knob (`d.layout` undefined = the base path, byte-identical; a face stamps `data-lcs-layout` only when declared); each adds a `verify()` branch; guards key on the config (`d.layout`, `d.parts`, `d.mix`, `d.stages`, `d.labels`, `d.decoy`), never on the level index. None is PARAM: each changes the apparatus a teacher sees (tagged plant + card / pot rows / a cut-and-glue ring / food cards with part chips / tagged plant + job sentences / a flower section + card + decoy). Resolved d2 configs pairwise distinct (`tools/gate-variation-distinct.js` exists, m; run with `--batch=b5`). `coordinate.mode` = the `layout` string. **Density (RULED exceptions, the G3-385 water-cycle precedent):** a labelling or matching face's item count IS the anatomy, so F4 (5 jobs, G2) and F5 (5 labels + 1 decoy, G3) sit below `density.G23.items` [8, 16] and F2 (4 cards, G1) below [6, 12]; element and numeral floors still hold and the gate asserts the recorded counts exactly.

### F1 : What Plants Need to Grow (K, CODE `layout:'needs'`)
**Move:** JUDGE conditions (a conjunction): each row shows two identical potted seedlings, each "given" two pictures; the child circles the ONE plant that gets BOTH things a plant needs (light and water). The losing pot always holds one need or none, so "circle the pot with the drop" and "circle the pot with the sun" both fail; the non-needs are ordinary objects, never crossed-out icons, so "pick the one without an X" cannot exist (A's crossed-glyph grid and B's pick-one-chip rows are rejected: critic §1). Soil and air are never a variable (every pot shows soil; K-LS1-1's hydroponics caveat). **Layout:** 5 rows `minmax(120px,1fr)`, row = `[needUnit 300][39][needUnit 300]` = **639** (the lane); needUnit = white card r 14, border 2 creamDeep, padding 12: `[gift 64][14][plantStage({stage:'seedling', potted:true}) cropped to viewBox y 60..236, x 30..170, h 110 -> w 88][14][gift 64]` = 12 + 64 + 14 + 88 + 14 + 64 + 12 = **268 <= 300** (m arithmetic); height 5 x 120 + 4 x 14 = **656 <= 677** (m). K floors: gifts 64 >= 56, pot 110. **Config d2** `{layout:'needs', rows:5, pots:2, mix:{noWater:2, noLight:2, neither:1}, gift:64, potH:110, rowGap:14}`; d1 `{rows:4, mix:{noWater:2, noLight:2}}`; d3 `{rows:5, pots:3, gift:56, potH:96}` (not shipped). **Pictures (all OPENED by the editor, vocab x11 m):** needs `weather/sun` (smiling sun) = light · `weather/raindrop` (a blue drop) = water; `space/sun` BLOCKED. Non-needs pool (12): `toys/ball toys/blocks toys/car toys/dice toys/train toys/robot clothing/shoe clothing/glove music/drum music/guitar classroom/pencil classroom/book`. **Forbidden as non-needs** (each is arguably a need or a need-cue): kite, hat, cap, lamp, moon, watering can, `At the Supermarket/water` (a water bottle, opened), any drink or food, `weather/cloud` `rainy` `hot` `puddle`, `spring/rain` `spring/sun`. Gift order inside a unit `rng`; the 2 gifts of the losing pot distinct. **Verify:** per pot `data-lcs-gifts="sun,ball"` + `data-lcs-grows="1|0"`; `grows === gifts ⊇ NEEDS` from the bank; exactly one `grows=1` per row; the row multiset of missing needs === `d.mix`; the winning pot's side: left/right counts differ by <= 1, the sequence is not constant and not strictly alternating (measured on the SHIPPED seed: the seed carries no locale, so a tell ships to all 11 at once); all seedling SVGs byte-identical (no health cue); no crossed or greyed picture; no word in the body. **Refusals:** none (no words on the body). **Query face:** "what plants need to grow" · de "Was Pflanzen zum Wachsen brauchen" · es "qué necesitan las plantas para crecer" · pt "do que as plantas precisam" · nl "wat heeft een plant nodig" · sv "vad behöver växten" · fi "mitä kasvi tarvitsee".

### F2 : From Seed to Plant, the cycle wheel (G1, CODE `layout:'cycle'`)
**Move:** PLACE growth stages round a CYCLE and close it: five slots on a ring joined by clockwise arrows; slot 1 (top) is pre-filled with the SEED (a cycle has no first element, so fixing the seed makes the order unique); below, a cut strip of the four other stage cards, shuffled; the child cuts them out and glues each into the next empty slot. The last arrow runs from the fruiting plant (whose opened pod shows its seeds) back to the seed. A's cut-and-glue ring with the pedagogy's return arrow; B's growing line is rejected (it is G1-203's move, numbers under pictures in a LINE) and the pedagogy's letter wheel (706 px, over 677) too. **Layout:** ring box 675 x 450: slots 124 x 150 (dashed coral 2.5, r 10) centred at angles −90°, −18°, 54°, 126°, 198° on R 165 about (337.5, 240): centres (337.5,75) (494.4,189.0) (434.5,373.5) (240.5,373.5) (180.6,189.0); vertical span 1.809 x 165 + 150 = **448.5**, horizontal 1.902 x 165 + 124 = 437.8 (edges 118.6..556.4); gaps slot 1/2 = 32.9 horizontal, slot 2/3 = 34.5 vertical, slot 3/4 = 70.0 (m arithmetic); 5 arrows teal 3 px along the R 165 arc between slot edges, 10 px arrowheads, the 5 -> 1 arrow stamped `data-lcs-arrow="return"`. Then 16, then `scissorsGlyph` strip 30 + `cutLines({w:496, h:150, cols:4, rows:1})` over 4 cards 124 x 150 (the K-240 abutting-card idiom, m) = 180. Stack 448.5 + 16 + 180 = **644.5 <= 677** (m). Each card/slot drawing `plantStage({stage, w:116})` = 116 x 139 inside the 124 x 150 frame. **Config d2** `{layout:'cycle', stages:['seed','sprout','seedling','flowering','fruiting'], given:'seed', ring:true, slot:[124,150], R:165}`; d1 `{stages:['seed','seedling','flowering','fruiting'], R:150}` (4 slots on a square ring); d3 `{stages:+'young' (6), given:'seed'}` (hexagonal ring, not shipped). **Verify:** `data-lcs-slot=<k>` per slot with `data-lcs-expect=<stage>` = `d.stages[k-1]`; slot 1 holds `plant-seed`, slots 2..n EMPTY; the strip holds `d.stages[1..]` each once (`data-lcs-stage`), in a DERANGEMENT of slot order and not its reverse; every card size === slot size (± 0); the return arrow exists and points into slot 1; every stage SVG is `plantStage` (`data-lcs-figure="plant-<stage>"`); the fruiting card shows >= 3 seed discs; no library picture, no word in the body. **Refusals:** none. **Fence:** titles always carry the plant or the seed; where the locale's head is "ciclo de vida de las plantas / das plantas / d'une plante / ciclo vitale della pianta", the plant is in the title, never bare "life cycle(s)"; no animal, no numbers written. **Query face:** en "plant life cycle" (+ "cut and paste" in the meta) · de "Vom Samen zur Pflanze" · es "ciclo de vida de las plantas primer grado" · pt "ciclo de vida das plantas" · fr "le cycle de vie d'une plante cp" · it "ciclo vitale della pianta classe prima" · nl "van zaadje tot plant" · sv "från frö till växt" · da/no "fra frø til plante" · fi "siemenestä kasviksi".

### F3 : Which Part of the Plant Do We Eat? (G2, CODE `layout:'eat'`)
**Move:** CLASSIFY a food by the PLANT PART it is: 8 food cards, each with three part chips (a `plantPartIcon` + the part word); circle the part we eat. The chips are part words, never "fruit or vegetable" (K-202 owns food class). **Pictures print WITHOUT a food name** (m: nl carrot "Wortel" = the root chip "wortel"; fi beetroot "Punajuuri" ⊃ "juuri", cauliflower "Kukkakaali" ⊃ "kukka"; nl "Bloemkool" ⊃ "bloem"). **Layout:** `cardGrid({cols:2, rows:4})`, cards 312 x `minmax(160px,1fr)`, gap 15 / 12: 2 x 312 + 15 = **639**, 4 x 160 + 3 x 12 = **676 <= 677** (m); card inner 312 − 28 = 284 x 132: `[picture 96][12][chip column 150]` = 258 <= 284; chip column 3 x 38 + 2 x 6 = **126 <= 132** (m; the pedagogy's 3 x 40 + 2 x 8 = 136 overflowed the 132 inner); chip 150 x 38, white, teal 1.5, r 10: `[icon 32][6][word Nunito 800 17]`, word <= 100 px *est.* (~12 chars; longest d2 literal 7). G2 floors: picture 96 >= 36, chip 38 >= 36, text 17. **Config d2** `{layout:'eat', cards:8, cols:2, rows:4, mix:{root:2, leaf:2, flower:1, stem:1, seed:1, fruit:1}, chips:3, pic:96, chipH:38, chipPx:17}`; `mixNoStem` `{root:2, leaf:2, flower:1, seed:1, fruit:2}` used ONLY when the locale's `refuseItems` removes `celery` (a data decision, keyed on the bank, not the level); d1 `{cards:6, mix:{root:2, leaf:2, flower:1, fruit:1}}`; d3 `{cards:10, mix:+root 1 +fruit 1}` (not shipped). **Pool (every picture OPENED by the editor, vocab x11 m):** root `vegetables/carrot radish beetroot parsnip` (4, leafy tops) · leaf `vegetables/lettuce spinach cabbage` (3) · flower `vegetables/broccoli cauliflower` (2, stalk + leaves visible) · stem `vegetables/celery` (1, stalks with a leafy top) · seed `vegetables/corn` (cob in its husk; pinned to `vegetables`, not `At the Supermarket`) + `At the Supermarket/peas` (opened pod, peas visible) (2) · fruit `vegetables/tomato cucumber eggplant` + `At the Supermarket/pepper` (red bell pepper) (4). **Excluded after opening or by botany:** `asparagus` (the red tip reads as a flower bud), `potato` (tuber = stem, contradicts K-3 intuition), `onion garlic leek` (bulbs), `mushroom` (not a plant), `turnip` (the art is a beetroot), `pumpkin squash` (seeds and flowers also eaten: two right answers; fi `squash` = "Kesäkurpitsa" = courgette while the art is a yellow gourd), `bell_pepper chilli_pepper green_beans` (NO vocab key, m: unreachable), every `fruits/*` (an apple turns "fruit" into a food-class answer). **Single answer by construction:** each item carries `allow` = the distractor parts that are UNARGUABLY wrong for that picture: root items `[flower, fruit, seed]` (leafy tops shown and greens are eaten: leaf and stem banned) · leaf `[root, flower, fruit, seed]` · flower `[root, fruit, seed]` (broccoli stalks are eaten: stem and leaf banned) · celery `[root, flower, fruit, seed]` (botanically a leaf-stalk: leaf banned) · corn `[root, stem, flower]` (a kernel is botanically a fruit; husk leaves shown: fruit and leaf banned) · peas `[root, stem, flower]` (the pod is the fruit: fruit and leaf banned) · tomato `[root, leaf, stem]` (the seeds are eaten inside it: seed banned; the calyx is shown: flower banned) · cucumber, eggplant, pepper `[root, leaf, stem]`. Chips = answer + 2 sampled from `allow`, order `rng`. The pictures and the answers are locale-neutral, so single-answer holds in all 11 locales by construction; the only locale surface is the six chip literals (validator: pairwise distinct). **Verify:** `data-lcs-food=<theme/noun>` + `data-lcs-part=<answer>` per card, `data-lcs-chip=<part>` per chip; answer === `EAT[item].part`; the two distractors ⊂ `EAT[item].allow`; card-part multiset === the resolved mix; no chip repeated in a card; chip text === `partWords[loc]`, chip icon === `plantPartIcon(part)`; the answer's chip slot over 8 cards: each of 3 slots used 2-3 times, not constant, not a staircase (0,1,2,0,1,2...); no food name anywhere in the body; 8 distinct pictures; `img` complete. **Refusals:** none; per-ITEM `refuseItems` allowed (e.g. a de panel ruling celery = "Blattstiel") and the mix falls back to `mixNoStem`. The landing (never the sheet) explains the tomato: "a tomato is the FRUIT of the tomato plant, even if the kitchen calls it a vegetable". **Query face:** "which part of the plant do we eat" · de "Welche Pflanzenteile essen wir" · es "partes de la planta que comemos" · nl "eetbare delen van een plant" · sv "vilka växtdelar äter vi".

### F4 : Parts of a Plant and Their Functions (G2, CODE `layout:'jobs'`)
**Move:** MATCH a JOB to a part ON THE PLANT: the same tagged plant (5 tags, no bank, no part word printed anywhere); the card holds five job sentences, each with an empty numeral box; the child writes the TAG NUMBER of the part that does that job. It tests function, not spelling, and reinforces where each part is (B's apparatus; the pedagogy's separate part cards with lines are rejected as the `.ws-match` science-pair-match look, and A's markers are the same move in another dress). **Layout:** `[plant stage 314 x 600: plantFigure({h:570}) w 304][16][job card 345]` = **675**; card inner 345 − 24 = 321 = `[blankNumeralBox 52 x 48][10][job text column 259]`; job text Nunito 700 17, line-height 22, <= 3 lines = 66; row 76; 5 x 76 + 4 x 12 + 24 = **452 <= 600**; stack **600 <= 677** (m; no bank). **Parts on the page: root, stem, leaf, flower, seed** (fruit untagged: its only true one-part job needs the word "seed"). **Jobs (en SOURCE; each true, and TRUE OF ONE PART ONLY on the page; no job contains any part word or `partStems` entry):** root "takes in water from the soil" · stem "holds the plant up tall" · leaf "makes food for the plant from sunlight" · flower "calls bees with its bright colours" · seed "can grow into a new plant". Deliberately NOT used: stem carries water (fits root too), root holds the plant in the ground (fits stem), flower makes seeds (names seed). **Config d2** `{layout:'jobs', parts:['root','stem','leaf','flower','seed'], figureH:570, box:[52,48], jobPx:17, maxLines:3}`; d1 `{parts:['root','stem','leaf','flower']}`; d3 `{parts:+'fruit'}` only with a validator-clean fruit job literal (not shipped). Job row order `rng`, !== tag order and !== reversed. **Verify:** `data-lcs-job=<part>` rows, bijection with `data-lcs-tag` parts; boxes empty (`data-lcs-answer=""`); each job text === `jobs[loc][part]`; no job text matches any `partWords[loc]` or `partStems[loc]` entry (`(?<!\p{L})...(?!\p{L})`, NFC-lowercase); no `[data-lcs-bank-banner]`; tag numbers {1..5} not y-sorted; the plant is the base primitive with 5 tags. **Refusals:** none expected; a locale whose panel cannot write a one-part-only job swaps that part out of the page and records it (data), the face stays. **Query face:** "parts of a plant and their functions" · de "Wozu braucht die Pflanze ihre Teile" · es "y sus funciones" · pt "e suas funções" · it "e le loro funzioni" · fr "et leurs fonctions" · fi "kasvin osat ja niiden tehtävät".

### F5 : Parts of a Flower (G3, CODE `layout:'flower'`)
**Move:** LABEL a second structure: the cut-away FLOWER (the part that makes seeds) with five tags; the bank holds the five flower words plus ONE word that is not part of a flower; the child writes each name on its numbered row and crosses the extra word out. New object, new vocabulary, an extra reject step. B's section + card + inset (the family signature) with the pedagogy's decoy. **Band RULED G3** (content home in 8 of 11: de Klasse 3-4, fr CE2, it terza, es 3º-4º, fi 3.-4. lk, nl groep 5-6, da/no 3.-4.; en 4-LS1-1 is Grade 4): every landing states its own placement; en, fr, es, it, fi say above band where it is; the page is built (brief CCSS-honesty rule). **Layout:** wordBank 59 + 10 + stage row 596: `[flowerSection({h:418}) w 380 in a 380 stage][16][column 279: inset plantFigure({stage:'flowering', ground:'none', h:180}) w 96 centred, with a teal dashed circle r 30 round its flower][16][label card 5 x 64 + 4 x 14 + 24 = 400]` (180 + 16 + 400 = 596); stack 59 + 10 + 596 = **665 <= 677** (m). Rows: badge 40 + 10 + box 205, `writingRow({w:199, h:58, glyphH:26})` (G3 >= 24); de "Kelchblatt"/"Staubblatt" 10 letters ≈ 156 px at glyphH 26 *est.* <= 199. **Config d2** `{layout:'flower', labels:['petal','sepal','stamen','pistil','stalk'], decoy:'root', figureH:418, inset:true, rowH:64, glyphH:26}`; d1 `{labels:['petal','stamen','pistil'], decoy:null}`; d3 `{labels:+'ovary' (6), decoy:'root'}` (not shipped). **Why `root` is the decoy:** the flower section draws no root; `leaf` is out (sepals and petals are modified leaves and de "Kelchblatt"/"Kronblatt", nl "kroonblad"/"kelkblad", sv "kronblad" CONTAIN the leaf word), `seed` and `fruit` are out (the ovary holds the future seed and fruit). The inset's roots sit outside its dashed circle and carry no tag. **Verify:** as the base over `FLOWER_ANCHORS` (`data-lcs-figure="flower"`); bank = labels ∪ {decoy}, decoy stamped `data-lcs-decoy`, not an anchor of the section, not first or last in the bank on the SHIPPED seed; labels <=> rows bijection; the decoy literal is not a substring of any flower literal (NFC-lowercase); one `[data-lcs-inset]` with no tag. **Refusals:** none. **Query face:** en "parts of a flower" · de "Aufbau der Blüte" · sv "blommans delar" (the strongest Nordic cell, R2 18 on-topic) · fi "kukan osat" · da/no "blomstens dele / deler" · es "partes de la flor" · it "le parti del fiore".

**Rejected non-moves.** colour the plant by legend (a theme-free colouring of the base: the RESERVE face if a locale refuses one) · bean diary / plant diary (open-ended, no verify; reserve #2) · "circle what plants need among 6 chips" (common sense solves it) · A's crossed-glyph needs grid (the "any X means wilted" shortcut solves all four cards) · parts of a TREE (theme slug + the base move on another object) · B's growing line (G1-203's numbers-under-pictures line) · relabelling d1/d3 of the base (same move, fails `gate-variation-distinct`) · fruit-or-vegetable (K-202) · living/non-living (K-201) · spring flowers (K-322).

## 4 Native rebuild plan x11

Frames NEVER inflect: this type has NO cloze frame and reads NO `objForms` (no noun enters a sentence). Per locale the panel authors: `partWords` 6 (bare nouns, singular or plural as the locale's diagram register prints them; the figure has several roots and four leaves and the tag points at one) · `flowerWords` 5 (petal, sepal, stamen, pistil, stalk; `stalk` MAY equal `partWords.stem`) · `jobs` 5 (+ optional `fruit`) · `partStems` 3-10 (the job-leak list: inflected/compound forms of the part words) · `forbidden` (the locale's wrong-register part words) · `refuseItems` (F3 item ids, optional) · `strings` 6 titles + 6 instructions = **~30 literals**. The instruction names only apparatus on the page (tags, numbers, lines, word bank, plant, pots, pictures, arrows, boxes, cut, glue, food, chips, flower) and at K is one imperative sentence. **The EN is a SOURCE TO AUDIT**: every panel reads the en strings and reports defects in them, not only its own.

| loc | literals | forms / slots | refusal / re-target | traps |
|---|---|---|---|---|
| en | 6 part · 5 flower · 5 jobs · stems · 12 strings | none | none | "roots" plural on the diagram, "leaf" singular (panel); fruit on the pepper/tomato card is botanical, landing explains; US "colors" |
| de | Wurzel Stängel Blatt Blüte Frucht Samen · Kronblatt (or Blütenblatt) Kelchblatt Staubblatt Stempel Stiel | capital kept | F3 `celery` may be refused ("Blattstiel") -> `mixNoStem` | **Blüte ≠ Blume** (vocab `flower` = "Blume", m); Stängel (post-1996) never Stengel, never Stamm; Frucht never Obst; decoy "Wurzel" not inside any flower word (validator); engine appends "Arbeitsblatt" |
| es (MX) | raíz tallo hoja flor fruto semilla · pétalo sépalo estambre pistilo tallo | none | none | fruto never fruta; "planta del pie" noise: the head always "de la planta"; MX register |
| pt (BR) | raiz caule folha flor fruto semente · pétala sépala estame pistilo pedúnculo (or caule) | none | none | caule never tronco; fruto never fruta (EF02CI06 wording); "atividade" never in a title |
| fr | racines tige feuille fleur fruit graine · pétale sépale étamine pistil tige | NBSP before `?` `:` | none | graine never semence/pépin; "plante des pieds" noise; "fiche" never in a title |
| it | radici fusto (or stelo) foglia fiore frutto seme · petalo sepalo stame pistillo peduncolo | none | none | ONE of fusto/stelo for every face; frutto never frutta; F2 head "ciclo vitale" never "ciclo di vita" |
| nl | wortel stengel blad bloem vrucht zaad · kroonblad kelkblad meeldraad stamper steel | no articles on the page | none | vrucht never fruit; wortel = carrot (why F3 prints no names); decoy "wortel" not inside a flower word |
| sv | rot stjälk blad blomma frukt frö · kronblad foderblad ståndare pistill stjälk | indefinite only | none | never "roten"/"blomman" on chips/bank (the `bana`/banan class); stam = trunk; title "Växtens delar" definite genitive is idiomatic [NSR] |
| da | rod stængel blad blomst frugt frø · kronblad bægerblad støvdrager støvvej stilk | indefinite only | none | "planter" retail noise stays out of titles [NSR] |
| no | rot stilk blad blomst frukt frø · kronblad begerblad støvbærer støvvei (or pistill) stilk | bokmål, indefinite | none | stilk vs stengel is the panel's [NSR] |
| fi | juuri varsi lehti kukka hedelmä siemen · terälehti verholehti hede emi kukkavarsi (or varsi) | nominative on chips/bank; every job and instruction a whole literal (case) | none | theme "Kukat" never in a title; "punajuuri"/"kukkakaali" leaks avoided by picture-only F3; "sukupuu"-style autocomplete traps checked on each title [NSR] |

Every panel OPENS every picture its faces keep (F1: `weather/sun`, `weather/raindrop` + the 12 non-needs; F3: the 16 foods) and reads one render of every face in its locale.

## 5 Data + gates

`data/b5/plants.js` (locale-neutral, hand-authored) + `data/b5/locales/plants.<loc>.json` GENERATED from `i18n/.draft-b5-<loc>.json` by `tools/apply-b5-locale.js` after `tools/validate-b5-draft.js` (clones of the b4 pair, both exist, m; `data/` gitignored, `git add -f`); read through `lib/b5-common.js bank('plants', loc)`.
```
PLANTS (data/b5/plants.js):
  PARTS: ['root','stem','leaf','flower','fruit','seed'],
  FLOWER_PARTS: ['petal','sepal','stamen','pistil','stalk','ovary'],
  NEEDS: ['sun','raindrop'],
  NEED_PICS: { sun:{theme:'weather',noun:'sun',picOpened:true}, raindrop:{theme:'weather',noun:'raindrop',picOpened:true} },
  NON_NEEDS: [ {theme:'toys',noun:'ball',picOpened:true}, ... 12 (F1 list) ],
  NON_NEED_FORBIDDEN: ['kite','hat','cap','lamp','moon','watering_can','water','cloud','rainy','hot','puddle','rain','sun'],
  EAT: [ {theme:'vegetables',noun:'carrot',part:'root',allow:['flower','fruit','seed'],picOpened:true}, ... 16 (F3 pool) ],
  EAT_BLOCKED: ['asparagus','potato','onion','garlic','leek','mushroom','turnip','pumpkin','squash','bell_pepper','chilli_pepper','green_beans'],
  STAGES: ['seed','sprout','seedling','young','flowering','fruiting'],
  JOB_PARTS: ['root','stem','leaf','flower','seed'],   // fruit only with a validator-clean literal
PLANTS_LOC (data/b5/locales/plants.<loc>.json):
  { partWords:{root,stem,leaf,flower,fruit,seed}, flowerWords:{petal,sepal,stamen,pistil,stalk,ovary?},
    jobs:{root,stem,leaf,flower,seed,fruit?}, partStems:[...], forbidden:[...], refuseItems:[], refuse:[],
    strings:{ 'G1-376':{title,instruction}, <F1..F5 ids>:{title,instruction} } }
```
Helper contract: `bank('plants', loc)` returns the merged object; `partWord(loc,id)`, `flowerWord`, `jobText` THROW on a missing literal (never fall back to the vocab); `eatPool(loc)` = `EAT` minus `refuseItems`; `mixFor(d, loc)` returns `d.mix` unless the stem bucket is empty, then `d.mixNoStem`.

**`tools/validate-b5-draft.js` (plants block; every rule runs, exit 1 on any):**
1. Every `PARTS` / `FLOWER_PARTS` (minus optional `ovary`) / `JOB_PARTS` key present, non-empty, trimmed, no `{`, no digit.
2. The 6 part words pairwise distinct and the 5 flower words pairwise distinct after NFC + `toLocaleLowerCase(loc)` (`stalk` may equal `partWords.stem`, nothing else may repeat).
3. No part word equals a `forbidden` literal of its locale (de Blume, Stengel, Stamm, Obst; es fruta; pt fruta, tronco; it frutta; nl fruit; sv/da/no definite forms `-en`/`-et` endings listed by the panel).
4. Every `EAT` item: `part ∈ PARTS`, `allow.length >= 2`, `part ∉ allow`, noun ∉ `EAT_BLOCKED`, `picOpened:true`, `fileUri(theme,noun)` resolves, a vocab key exists (alt text), root items never allow leaf/stem, flower items never allow stem/leaf, tomato/cucumber/eggplant/pepper never allow seed/flower.
5. Every shipped mix bucket reachable from `eatPool(loc)`: d2 stem 1 <= pool, seed 1 <= 2, fruit 1 (or 2 under `mixNoStem`) <= 4.
6. `NON_NEEDS` disjoint from `NEEDS` and from `NON_NEED_FORBIDDEN` (by noun), each `picOpened:true`, >= 10.
7. Every job literal <= 90 chars and contains NO part word nor `partStems` entry of any part on the F4 config (`(?<!\p{L})...(?!\p{L})`, NFC-lowercase); the five jobs pairwise share no content word the panel lists as a part cue.
8. The F5 decoy is `root`; `partWords.root` is not a substring of any `flowerWords` literal (de Wurzel, nl wortel, sv rot vs "kronblad"...).
9. Titles <= 70, no worksheet-word (`WORKSHEET_WORD` regex of the b4 gate), unique in the band, never equal (case-insensitive) to `axes.theme.{flowers,tree,vegetables,fruits,spring}.name|slug.<loc>`; no title contains a bare "life cycle(s)" head without the plant/seed word (per-locale regex list); instructions <= 150; no visible string contains the locale's free-word.
10. Instructions name only apparatus of their face (per-face banned list: base/F5 never "circle", "colour", "cut", "letter boxes"; F1 never "word", "write"; F2 must contain the cut + glue verbs; F3 never "vegetable" / the locale's food-class word; F4 never "word bank").
11. Non-de part / flower words not capitalised (a vocab citation form leaking in); de nouns capitalised.
12. `strings` ids === {base + the 5 allocated face ids}; the six titles differ pairwise by at least one NOUN or verb token.

**`qa/verify-b5-plants.js`:** renders base + 5 faces x 11 locales x d2 through `render/render-instance.js` (file:// fonts) under the 722 stack AND the 4-line fi title (677); asserts `verify()` empty and `qa/lints.js` clean; then the floors ITSELF (no size lint exists): F1 gifts >= 56 and pots >= 96 (K); base/F2 rows >= 64, stage cards 124 x 150, bank words 18 px (G1); F3 pictures >= 90, chips >= 36 high at 17 px (G2); F4 numeral boxes 52 x 48, job text 17; F5 rows glyphH >= 24 (G3); figure measured on the render: every anchor inside its own shape, root anchors below the soil line, the pod contains 4 seed discs (base) / 3 (F2 fruiting), threads pairwise non-intersecting and tags >= 32 px apart; every answer surface empty (rows, slots 2..n, numeral boxes, no pre-circled pot/chip); no food name in the F3 body; no numeral in any body except tag numerals and row badges; each re-derived answer unique; position tells measured on the SHIPPED instance (F1 winner side, F3 answer slot, base/F4 numbering); body width `scrollWidth <= clientWidth` on every row; one-row bank. SWEEP: 20 seeds x d2 per face render distinct pages (anchor choice, numbering, gift pairs, strip order, food set, job order vary).

**Poison (each must FAIL for its own reason; the correct EN draft is the control):** P1 carrot `allow` contains `leaf` -> rule 4 · P2 celery `allow:['root']` (one distractor) -> rule 4 · P3 `vegetables/asparagus` in `EAT` -> rule 4 · P4 `vegetables/bell_pepper` in `EAT` (no vocab) -> rule 4 · P5 de `partWords.flower:'Blume'` -> rule 3 · P6 es `partWords.fruit:'fruta'` -> rule 3 · P7 sv `partWords.leaf === partWords.root` ("blad") -> rule 2 · P8 en job flower "makes the seeds" (seed on the page) -> rule 7 · P9 fi job root "juuri ottaa vettä" (own part word) -> rule 7 · P10 de decoy check with `flowerWords.petal:'Wurzelblatt'` -> rule 8 · P11 `NON_NEEDS` += `At the Supermarket/water` -> rule 6 · P12 en F2 title "Life Cycle" -> rule 9 · P13 base instruction "Circle the parts" -> rule 10 · P14 `EAT` item `picOpened:false` -> rule 4 · **render:** PR1 an F1 row with two `grows=1` pots -> "one winner per row" · PR2 F1 winner side sequence L,R,L,R,L -> "alternating tell" · PR3 F2 strip order === slot order -> derangement · PR4 F2 return arrow removed -> "cycle not closed" · PR5 F3 answer slots 0,1,2,0,1,2,0,1 -> staircase · PR6 F3 a food caption printed -> "food name in body" · PR7 base tag numbers y-sorted 1..6 -> numbering tell · PR8 base thread moved 20 px off its anchor -> "thread end off anchor" · PR9 base root anchor moved above the soil line -> "root above soil" · PR10 F4 a part word inside a job row -> "leak" · PR11 F5 decoy first in the bank -> "decoy position tell" · PR12 `answerBox({w:52,h:48})` in place of `blankNumeralBox` on F4 -> `data-lcs-answer="undefined"` · PR13 a two-row bank on the base at the fi 677 stack -> footer lint + "bank wraps".

**Page reads:** `PLANTS`, `PLANTS_LOC[loc]`, `plant-figure.js`, `fileUri` (F1, F3 only); never `image-vocabulary.js` (except the validator's vocab-key existence check), `objForms`, `articles.js`, `science/*.json` or `approved-words-*.json` at render.

## 6 SEO plan

| face | title pattern (Germanic / Romance / Nordic + fi) | meta MIDDLE (whole 120-170; the child's instruction, en source) | coordinate |
|---|---|---|---|
| base | Parts of a Plant · Teile der Pflanze · Delen van een plant / Partes de la planta · Partes da planta · Les parties de la plante · Le parti della pianta / Växtens delar · Plantens dele · Plantens deler · Kasvin osat | Children follow each numbered tag to a part of the plant (roots, stem, leaf, flower, fruit, seed) and write its name on the matching line | `{type:'plants', mode:'base', theme:'', level:<G1 key>}` |
| F1 | What Plants Need to Grow · Was Pflanzen zum Wachsen brauchen · Wat heeft een plant nodig? / ¿Qué necesitan las plantas para crecer? · Do que as plantas precisam para crescer · Les besoins des plantes · Di cosa ha bisogno una pianta / Vad behöver växten? · Hvad har en plante brug for? · Hva trenger en plante? · Mitä kasvi tarvitsee? | Each plant gets two things; children circle the plant that has both light and water, the two things every plant needs to grow | `mode:'needs'`, K |
| F2 | Plant Life Cycle: From Seed to Plant · Vom Samen zur Pflanze · Van zaadje tot plant / Ciclo de vida de las plantas · Ciclo de vida das plantas · Le cycle de vie d'une plante · Il ciclo vitale della pianta / Från frö till växt · Fra frø til plante · Fra frø til plante · Siemenestä kasviksi | Children cut out four growing stages and glue them round the circle, from the seed to the flower and back to new seeds | `mode:'cycle'`, G1 |
| F3 | Parts of a Plant We Eat · Welche Pflanzenteile essen wir? · Eetbare delen van een plant / Partes de la planta que comemos · Partes da planta que comemos · Les parties des plantes que l'on mange · Le parti della pianta che mangiamo / Vilka växtdelar äter vi? · Hvilke plantedele spiser vi? · Hvilke plantedeler spiser vi? · Mitä kasvin osia syömme? | Carrot, lettuce, broccoli, celery, corn or tomato: children circle which part of the plant we eat, root, leaf, flower, stem, seed or fruit | `mode:'eat'`, G2 |
| F4 | Parts of a Plant and Their Functions · Wozu braucht die Pflanze ihre Teile? · Waarvoor dient elk deel van een plant? / Partes de la planta y sus funciones · Partes da planta e suas funções · Les parties de la plante et leurs fonctions · Le parti della pianta e le loro funzioni / Växtens delar och deras uppgifter · Plantens dele og deres opgaver · Plantens deler og hva de gjør · Kasvin osat ja niiden tehtävät | Children read what each part does, find that part on the tagged plant and write its number: taking in water, holding the plant up, making food | `mode:'jobs'`, G2 |
| F5 | Parts of a Flower · Aufbau der Blüte · De delen van een bloem / Partes de la flor · Partes da flor · Les parties de la fleur · Le parti del fiore / Blommans delar · Blomstens dele · Blomstens deler · Kukan osat | Children label the petal, sepal, stamen, pistil and stalk of a flower and cross out the one word in the bank that is not part of a flower | `mode:'flower'`, G3 |

All titles are candidates the native panels rewrite (<= 70, no worksheet-word, base = the bare head, each face adds one NOUN / verb phrase, never an adjective; da/no base "Plantens dele(r)" and F2 "Fra frø til plante" differ by head noun, measured OK). de F2 "Vom Samen zur Pflanze" is the contract default; the de panel may choose the harvested "Von der Bohne zur Pflanze" only if it accepts that the drawing names no species. `coordinate.mode` is ALWAYS the face's mode string (base `'base'`), `coordinate.theme:''`, level = the band key of §1 per face. h1 = title; eyebrow = level label; strand chip = the `Science` row literal (framework NAME only). JSON-LD `LearningResource`, NO `educationalAlignment`; en prose names the NGSS codes of §1; pt prose names EF02CI06 / EF02CI05. Meta lead inherits `seo.words.free_printable` (metadata only; no visible copy claims "free"); no title, meta or landing promises an answer key (printable-only decks ship none); the MIDDLE window is measured with `tools/measure-instruction-window.js` (exists, m). `topicMeta.plants` >= 50 chars x11 + `skill-sentences.<loc>.json.plants` via the `register-b4-en-content.js` clone. F5 landings state the placement per locale (en "a Grade 4 topic, printed here as a stretch for grade 3"; fr/es/it/fi likewise).

**Non-cannibalisation** (whole-landing 3-gram Jaccard, `scripts/seo-landing/gate.js` FAIL >= 0.80 / WARN 0.65; *est.*, the gate measures):

| pair | what the copy says differently | est. |
|---|---|---|
| base vs F5 | the whole plant, six parts, root to seed vs one flower up close, petal/sepal/stamen/pistil, a word to cross out | 0.20 |
| base vs F4 | write part NAMES from a bank vs write tag NUMBERS next to jobs; no bank | 0.15 |
| base vs K-354 human-body base | numbered tags on a plant + one label card vs lanes glued to a body figure; the instructions share only "word bank" | 0.12 |
| F1 vs K-212 wants/needs, K-201 living | a plant's two needs judged on pots vs a person's wants or a living sort | 0.08 |
| F2 vs G1-203 chicken life cycle | a drawn plant on a ring, cut and glue, closes back to the seed vs three chicken pictures in a line, write 1-2-3 | 0.08 |
| F2 vs G1-377 animal-life-cycles | plant, seed, sprout, flower, pod vs egg, caterpillar, chrysalis, tadpole; every sentence names the organism | 0.15 |
| F3 vs K-202 fruits-vegetables | which PART we eat (root, leaf, flower, stem, seed, fruit) vs fruit-or-vegetable bins | 0.08 |
| F3 vs F4 | food pictures and part chips vs jobs and tag numbers | 0.10 |
| F5 vs K-322 seasons / flowers theme | the anatomy of one flower vs spring pictures | 0.05 |
| F1 vs any face | pots and gifts, no part word | < 0.06 |

Boundary sentence on every landing: "This page is about how plants are built and how they grow; sorting food into fruit and vegetables and animal life cycles have their own pages."

## 7 Hub visibility contract

A face appears under `plants` on `/[locale]/worksheets` IFF all four hold: (1) `apps.plants` exists in `frontend/config/topics-taxonomy.json` (ABSENT today, m; missing = rendered NOWHERE in the rail) with `default_subject:'science'`; (2) `axes['exercise-type'].plants` has `slug` + `name` in all 11 locales (§1 table B; 0 collisions, m); (3) exactly one landing per face per locale with `coordinate.type === 'plants'` verbatim, `mode` per §6, `theme:''`, the level key of §1 (G1 base + F2, K F1, G2 F3 + F4, G3 F5), a unique slug and `canonicalDeckSlug` = the published deck; a REFUSED face has NO landing and the expectation is lowered explicitly, never padded; (4) the landing JSON committed AND deployed (per-process cache).

Gate: `node scripts/verify-hub-type-rows.js --keys=plants` (exists, m; expectations from `b5-designs/hub-expectations.json` exported by `tools/export-hub-expectations.js --batch=b5`; poison = drop one landing / drop `apps.plants` must FAIL).

**Expected rows per locale:** en 6 · de 6 · es 6 · pt 6 · fr 6 · it 6 · nl 6 · sv 6 · da 6 · no 6 · fi 6 = **66**. No face is refused in any locale at design: every contingency is per ITEM or per PART and leaves the face buildable (F3 `refuseItems:['celery']` -> `mixNoStem`, pool 15, 8 cards still fill; F4 a part a panel cannot job cleanly is dropped to 4 jobs, face stays); a panel that refuses a whole face records it in `refuse` and lowers that locale to 5 before the wave.
