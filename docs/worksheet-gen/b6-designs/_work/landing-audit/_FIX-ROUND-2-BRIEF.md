# nt5-F (b6) fix round 2: the landing-audit findings

The en / de / es / fr landing panels opened every shipped render and reported where the page and its strings disagree (`pass1-<loc>.md` in this folder; nl follows). **You own ONE family.** Read the four finding files, take every item for your family's six ids, reproduce each on the render (`scripts/worksheet-gen/out/b6-sweep/<loc>/<ID>-null-d2-<loc>.png`, or render it yourself), then fix it AT THE SOURCE: the primitive, the component, the bank (`data/b6/<key>.js` + `data/b6/locales/<key>.<loc>.json`), the spec, or the EN strings.

Rules (the same ones fix round 1 used; see `_work/<baseId>-faces.md` "Fix round 1" and `_work/<baseId>-build.md`):

1. **Reproduce first.** Panels also file false findings (a missing picture that is actually there, a defect in the landing prose rather than the page). If it does not reproduce, write why in your record; do not "fix" it.
2. **Fix the page, not the landing.** Never move a gate threshold. Every new rule you introduce (e.g. "no picture sits straight across from its own answer", "the instruction names only apparatus that is drawn", "a heading names nothing the page does not ask") gets an assertion in `qa/verify-b6-<key>.js` (or the primitive's verify), **poison-tested in BOTH directions**, in EVERY locale that ships the face.
3. **Strings.** A string lives in three places: `scripts/worksheet-gen/i18n/.draft-b6-<loc>.json`, the bank `data/b6/locales/<key>.<loc>.json`, and the built `i18n/strings.<loc>.json`. You change the **EN** source (`data/b6/<key>.js` + the spec's `i18n.en` / `register-b6-en-content.js` path, whichever the family uses). **Do NOT author non-EN wording yourself.** When a fix changes what a non-EN string must say (an instruction, a title, a bank word, a reason), list it in your record under "## Locale strings to re-author" as `<loc> <ID> <field>: <what it must now say, and why>` — a native reconciliation round applies them. The ONE exception: a mechanical, meaning-preserving key change (e.g. a key renamed) you may carry across all drafts.
   Specific non-EN defects the panels named (es G2-379 "mucho calor" → sun; fr G2-379 "on le fait" agreement; fr G1-398 "Jungle"; es K-383 "madriguera" for the lodge; es G3-400 "cubos"; de G1-403 "dabei" vs chips; de/es/fr titles naming one habit or three homes when the page shows more) go into that list, not into the files.
4. **Pictures.** Open every picture you add or swap, at print size. The duck in the image library renders as a yellow DUCKLING that children in de/es/fr/en call a chick — check whether an adult duck image exists (`image-library-webp/`), otherwise drop the duck from any face where "chick" is a second answer.
5. **Scope.** Only your family's files, plus a shared primitive only if your family is its sole consumer (check with grep). `lib/unit-axis.js` + the G2-377 `exemplar(loc, spec)` title fix are already done by the lead — do not touch them.
6. **When done:** run `node scripts/worksheet-gen/qa/verify-b6-<key>.js` (all locales), the family's face gates, `node scripts/worksheet-gen/tools/gate-variation-distinct.js --batch=b6`, `node scripts/worksheet-gen/qa/b3-baseline.js --check` (0 drift on non-b6 types), re-render your six ids in en + de + es + fr + one Nordic locale and READ the renders at print size. Append "## Fix round 2 (landing audit)" to `docs/worksheet-gen/b6-designs/_work/<baseId>-faces.md`: every finding → reproduced? → fix → gate, and the "Locale strings to re-author" list. Do not commit.

Reply with: each finding (id, one line) → fixed / not reproduced / refused-with-reason; the gate results; the locale-string list.
