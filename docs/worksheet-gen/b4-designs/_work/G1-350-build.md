# G1-350 `cloze` : EN BASE build record (2026-09-21)

Built from `G1-350-cloze.md` (§2 base, §5 data + gates; §3 read for the additive `mode` knob: letters / choice / plural / story / match), `_SUBSTRATE.md`, the README "Cross-type rulings" and `_work/G1-350-critic.md` (rulings 1-2, 11-20 applied: 7 rows / 56 / 18 / `gapBox`; one box width per page; answers READ through `answerFor`, never typed; de `unique` forbidden; `{gap}` token; pinned `pic` + `fileUri`, never `pictureFor`; mode string `letters`; the inline lane padding; the two-row bank guard; the opened-picture floors; themeless with the global `twins` / `excludeKeys`). Exemplars read: `types/g1/G1-308-read-and-do.js` (objForms through the b3 `bank('instructions', loc)` + `fillSlots`), `types/g1/G1-335-opposites-in-a-sentence.js` (the frames layout this must be visibly different from), `types/g1/G1-352-pronouns.js` + `qa/verify-b4-pronouns.js` (the b4 conventions, the 722/677 chrome pinning). No em-dashes.

## Files (all type-scoped; nothing shared edited; `node tools/b3-baseline.js --check --quick` PASS)

| file | what |
|---|---|
| `scripts/worksheet-gen/types/g1/G1-350-cloze.js` | the spec: exerciseType `cloze`, deck slug `fill-in-the-blank-sentences`, themeless, `unitAxis:{applicable:false}`, d1 / **d2 (ships)** / d3, `build` -> `_buildWith(bank, d, {locale, globals}, ctx)` (the gate's injection seam) -> `resolveBase` (guards on the RESOLVED keys: rows 6..12, picPx >= 44, gapH >= 36, fontPx >= 16) -> `eligibleFrames` (no `{name}` unless nameSlot, text <= maxChars, answer <= maxGlyphs, an answer the bank carries) -> `compose` (rng-shuffled frames sorted by id: distinct nouns / answers, <= 2 per pic.theme, <= 1 key per global twin group, no panel `confusable` pair, `bank ∩ fits === {answer}` both directions; 300 tries then REFUSED) -> `derange` (the bank order) -> names LAST (`fillSlots` over SENTENCES names; `{gap}` parked as U+0001 so the shared fillSlots never sees it) -> `gapWidth` -> `gapRow` x rows + `gapBank`. Pure helpers exported for the gate: `answerFor`, `genderOf`, `gapWidth`, `twinGroupOf`, `eligibleFrames`, `resolveBase`, `compose`, `objFormsOf`, `hasWord`, `graphemes`, `FORMS`. `verify(page)` = the base branch (badge === n, one picture === key and on the row of its sentence, one empty non-slim box >= 150 uniform === the stamped gapW, sentence <= 64 / font >= 16 / no overflow, the answer and every bank word absent from every sentence, no key / answer / frame / src twice, bank set === answers, bank deranged and not the reverse, <= 2 pill rows, no starter / chip / stray text, lane content inside its track). `d.mode` other than `base` -> `_buildFace` THROWS "Phase 2" before the RNG is touched (the base stays byte-identical). |
| `scripts/worksheet-gen/data/b4/cloze.js` | `{ CLOZE: { twins, excludeKeys, strongThemes, en: {…} } }`: **18 singular frames** (>= 16; every one pinned to an OPENED picture from the twelve strong themes: animals 3, farm animals 2, zoo animals 2, forest creatures 2, birds 2 2, Things That Fly 2, beach 2, vehicles 2, toys 1), 12 plural frames (3 with a number word and `clones` equal to it), 6 stories (18 distinct nouns, every `fits` disjoint from the other five stories' nouns), `nameSlot:true`, `articleTable {a, an, the}`, `boxWord` "one letter in each box", `confusable` (duck/goose, hen/chicken, bus/van, boat/sailboat), `agreeingAdjectives: []`, `strings` x6 faces (base === the spec's i18n.en), `refuse:{plural:false}`. Every EN frame prints `the` (the a/an keyFor rule is implemented and poisoned, not exercised by the EN data). `data/` is gitignored: the reviewer `git add -f`. |
| `scripts/worksheet-gen/templates/components-b4/cloze.js` | the eight NEW components of §2, exported by exactly those names: `gapBox` (base), `gapRow` (base), `gapBank` (base) + `letterGap`, `choiceGap`, `cloneGapRow`, `storyBlock`, `sentenceMatch` (the faces; built to the §2 contracts incl. every throw, smoke-rendered by the gate, NOT consumed by any spec yet) + the pure helpers `derange`, `pillEstimate`, `BANK_INNER`. Barrel `templates/components-b4.js` loads them beside the sibling files with 0 duplicate names. |
| `scripts/worksheet-gen/qa/verify-b4-cloze.js` | the gate; exports `validateBank(block, loc, opts)` (rules 1-16; `tools/b4-probe-child.js` calls it as `(block, loc)`; `opts.titleLines` for rule 13, `opts.objForms` as the poison seam), `crossCheck`, `TABLE_B`, `HEADS`, `synthetic` (locale blocks generated from the REAL pool of each locale, the controls for the non-EN rules). |
| `scripts/worksheet-gen/i18n/strings.en.json` | regenerated by `node i18n/build-en.js` (607 types, title lint clean). |

## Pictures OPENED (the human gate)

All 48 pins of the EN bank (18 singular + 12 plural + 18 story) on two contact sheets built with sharp from the cached `@3x.webp` and READ at **56 px** (the base row size) and **112 px** (scratch `G1-350-sheet56.png` / `-sheet112.png`), plus `vehicles/rocket` at 44 and 112 after the story swap below. Every one shows what its key says: `birds 2/rooster` a rooster with comb and tail, `farm animals/hen` a white hen, `toys/ball` a football, `beach/turtle` a tortoise-shaped turtle, `fruits/blueberry` a single blueberry (reads as a blue ball below ~48: it sits ONLY on the plural face at 48 clones), `zoo animals/monkey` a monkey, `beach/sailboat` and `beach/dolphin` clear. No `around the house` picture is pinned anywhere (the weakest theme; `glass` = red wine is in `excludeKeys`, poisoned).

## Deviations from the design file (each measured)

1. **The "722" chrome measures 710** (a 3-line title of 74 chars + a 3-line instruction of 166 chars; the G1-352 / G2-317 finding) and **the two-row bank measures 110 + 10 margin = 120**, not the design's 109 + 10. The design's stack 109 + 10 + 7 x 78 + 6 x 8 = 713 becomes 120 + 546 + 48 = **714 > 710**. Re-budgeted with ONE change: **row-gap 8 -> 7, rowMin 78 -> 77**: 120 + 7 x 77 + 6 x 7 = **701 <= 710**; at 710 the tracks resolve to (710 - 120 - 42) / 7 = **78.3 >= 77.4** (the measured two-line row: sentence 63.4 + padding 10 + border 4), so nothing clips (measured: de-length 50-char frames at 710 -> sentences 63.4, lanes 78.3, inner/scroll 74/74). Rows at the default one-line chrome open to 91. The lane padding stays the design's inline `5px 16px`.
2. **The base at 677 (a 4-line title) does NOT fit**, as the design predicts: 701 > 677, and even a 4-line title with a 2-line instruction (700) overflows by 1 px. The gate's PR13 asserts the footer-overlap failure; validator rule 13 caps the fi base / plural / story titles at <= 3 lines (the proposed "Täydennä lauseet" measures 1 line).
3. **`helicopter` (10 glyphs) left the `sky-day` story** for `rocket` ("The {gap} blasts off into space.", fits [rocket]): story answers are capped at 9 glyphs by rule 10 (the 9-pill bank <= two rows by construction). `helicopter` stays a singular frame (the base allows 12).
4. **18 frames, not 16.** Rule 8 is a floor (>= 16); two extra frames (pig, turtle) give the composer room under the <= 2-per-theme and twin rules. The sweep shows 20 distinct row sets over 20 seeds at d2.
5. **The grid's 10 / 12 gutters** (badge 30 | 10 | picture 56 | 12 | sentence 531 = 639) are one `column-gap:10px` plus a 2 px `margin-left` on the sentence (CSS grid has one column gap); the sentence column is exactly 531.
6. **The en taxonomy slug is `cloze`, not table B's `fill-in-the-blank-sentences`**: Phase 0 registered `axes['exercise-type'].cloze.slug.en = 'cloze'` under the taxonomy invariant `slug.en === key` (the G1-352 precedent); the gate's `TABLE_B.en` follows the taxonomy, the DECK slug of the spec stays `fill-in-the-blank-sentences`. Non-en slugs are not registered yet (rule 16 reports "not registered" once a locale block exists).
7. **The `sentenceMatch` two-line left text measures 53.4, not the design's est. 47**: the 30 px slim box sits inline on line 1 (30 + 23.4); still inside the 92 px item. Block 624 in a 624 box, 0 overflow.
8. **The F2 (choice) row smoke stacks the chips naively under the sentence and measures 120**, above the design's 100: the F2 row PROPER (sentence line 36 + 6 + chips 44 = 86 + 14 = 100) is Phase 2's layout to build and measure; `choiceGap` itself renders chips 44 high (68 / 80 wide for owl / duck). The other face components meet their design numbers: letterGap 12 boxes = 358 x 28 (the letters row 70 at natural height, design 77 floor), cloneGapRow 77.4 (design 78), storyBlock 192.0 (design 192), sentenceMatch 624.
9. **NFD folding in the answer-hiding scan is stricter than the words** (the design's rule 4: NFD, case-insensitive): sv "kör" (drives) folds to "kor" (cows). A Swedish story "Sedan kör Mia traktorn" beside a cow gap would be REFUSED as printing the plural. Recorded for the sv panel (avoid "kör" on a page with `cow`, or the reviewer relaxes rule 4 to NFC for sv); the synthetic sv control avoids the verb.
10. **The de `case` stamp** rides on every de frame (`data-lcs-case`) and on stories as `cases[i]` (the design's story shape has no per-line case; the validator reads `s.cases` when present, `nom` otherwise).

## Gate output (`node qa/verify-b4-cloze.js`, 25 s)

```
bank en: clean (frames 18, plural 12, stories 6)
title lines: 722-fixture 3 · 677-fixture 4 · "Täydennä lauseet" 1
render d1 en: verify 0 lints 0 body 799 bank 60.0 lanes 114.8 x6 gapW 152 lowest 921 vs foot 921
render d2 en: verify 0 lints 0 body 799 bank 110.0 lanes 91.0 x7 gapW 184 lowest 921 vs foot 921
render d3 en: verify 0 lints 0 body 799 bank 110.0 lanes 91.0 x7 gapW 184 (9 pills: 7 + 2 frame-invalid extras)
render d2 en 3+3 chrome: body 710.0 bank 110.0 lanes 78.3 x7 inner 74 lowest 921 vs foot 921
render d2 long frames 3+3 chrome: sentences 63.4 x7 lanes 78.3 x7 inner/scroll 74/74 (the two-line row, 0 clip)
render d2 en 677 chrome (PR13): body 677 → 1 fails (footer overlap: <div class="ws-lane"> reaches 945 against the attribution band at 921)
components: letters row 70.0 (design 77) · choice row 120.0 (design 100, Phase 2) · plural row 77.4 (design 78) · storyBlock 192.0 · letterGap 358.0 x 28.0 · chips 44x68 44x80
components: sentenceMatch 624.0 · left 92/30 92/53 x5 · right zebra,cow,bee,owl,tractor,kite (deranged)
sweep d1: 20 seeds, 20 distinct sets, 20 distinct orders, 18 bank orders
sweep d2: 20 seeds, 20 distinct sets, 20 distinct orders, 20 bank orders
verify-b4-cloze: 845 assertions, 0 failures in 25s → PASS     (--quick: 143 assertions, 0 failures)
```

Poisons, **55 killed, every one for its OWN reason** (the correct EN bank + the synthetic de / fr / es / sv / da / fi controls, generated from each locale's real pool, pass): P1 (the answer in the text) + P1b (the other number printed — "the other number of the noun printed", with the control), P2, P3 (de "Der {gap}" over Kuh f -> the nom table), P4 (fr "Le {gap}" over avion -> elision), P5 (de `unique`), P6 + P6b (de foil Ziege f on Pferd n), P7, P8 (fi "Kaksi {gap}"), P9 + P9b, P10 x6 (2 sentences / cow,cow / unpictured park / a noun shared / a fits naming another story's noun / a line > 42), P11 x4 (animals bw / picOpened / glass / a non-candidate pic), P12, P13 x9 (boxWord on the base / da plural frames / sv "Singular och plural" / pt "Escreva o nome da figura" / worksheet word / "with Answers" / a free claim / "cloze" in a title / a measured 4-line fi title), P14 (sv `def` on cow: **"kon" === cone "Kon", the real sv collision the mechanical rule catches**) + P14b (def without defWhy), P15, P16 (7 unique-form frames -> 11 F1-eligible), P8t / P8n / P9c / P9d / P5e / P5f / P4s; render poisons PR1 (a stamped answer edited), PR2 (overlapping `fits` through the seam -> the composer REFUSES), PR8 (per-row widths), PR9 (two-line rows at the default lane padding at 710 -> the lane clips), PR10 (a bank forced to > 2 rows; `gapBank` refuses 9 x 14-glyph pills at build), PR12 (apple + cherry + plum twins -> REFUSED), PR13 (677), PRa (the answer printed), PRb (the bank in row order), PRc (a starter glyph), PRd (a hint chip), PRe (a picture swapped), PRf (a key stamp edited); component throws: gapBox 140 / 320 / h 30, letterGap box 24, gapRow two gaps / answer printed / picPx 40, choiceGap duplicate / idx 2 / h 40, cloneGapRow 4 clones, storyBlock 2 lines / a non-permutation, sentenceMatch a fixed point (PR6) / the answer printed (PR7), gapBank a fixed point / the reverse.

## Renders for the reviewer (LOOKED at, every one)

- `scripts/worksheet-gen/out/dev/G1-350-null-d1-en.png` (6 rows, 64 px pictures, a one-row bank of 6, gapW 152) · `G1-350-null-d2-en.png` (7 rows, the two-row bank, gapW 184 for `woodpecker`) · `G1-350-null-d3-en.png` (9 pills: two frame-invalid extras) · `G1-350-null-d2-en-v4.png` (variant 4: the `{name}` frame "Emma flies the [ ] on a windy day." on row 2)
- `out/dev/G1-350-gate-d2-en-chrome722.png` (the 3+3 chrome at 710: 7 lanes of 78.3, nothing under the footer) · `G1-350-gate-d2-long-chrome722.png` (de-length frames at 710: every sentence wraps BEFORE or AFTER the box, the box on line 1, two lines, 0 clip) · `G1-350-gate-d2-en-chrome677.png` (PR13)
- `out/dev/G1-350-gate-components.png` (letterGap row, choiceGap row, cloneGapRow with 3 cows, storyBlock with the strip in order 2,0,1) · `G1-350-gate-components-match.png` (sentenceMatch: six gap sentences left, six deranged pictures right, 133 px between the dots)
- What I saw: seven pictures down the left, seven coral boxes floating inside seven lines of text, a dashed bank on top, nothing else; every box the same width; the wrapped long frames keep the box on the row; the pictures read at 56 (owl, zebra, giraffe, woodpecker, train, sailboat, elephant); no answer anywhere but the bank; the bank never in row order.

## Open items for the faces / panels

1. Phase 2 builds the five faces on `d.mode` (`letters` / `choice` / `plural` / `story` / `match`) inside `_buildFace` + a `verify()` branch per stamp; the components exist and are smoke-measured. The F2 row proper must be laid out to the design's 100 (sentence line 36 + 6 + chips 44 + 14): the naive stack measures 120 (deviation 8).
2. **sv `kör` / `kor`** (deviation 9): the NFD fold in rule 4 + verify() equates the verb with the cow plural; the sv panel avoids "kör" on a cow page or the reviewer scopes NFD per locale.
3. **Non-en taxonomy slugs + names** (table B) are the registrar's step; rule 16 holds each locale to table B once its block exists (`de: lueckentext`, the `exercise-mode.fillin.slug.de = luckentext` near-miss asserted as a MODE key).
4. The EN frames print `the` only; a panel authoring `a / an` frames exercises `ARTICLES.en.keyFor` + `EN_AMBIGUOUS` (rule 5, poisoned P5e / P5f).
5. `predicateKind:'place'` is accepted by rule 2 but no EN frame uses it; the EN copula frame (`blueberries-blue`) carries its `exclusiveWhy`.
6. The critic's open item 7 is answered: a b4 type CAN require the b3 `bank('instructions', loc)` (`objFormsOf`), no `dir` argument needed; the two-row bank is 110 (r), the 40-box two-line row 63.4 (r), the story block 192 (r) at 3+3 chrome, the F5 two-line text 53.4 (r).
7. `strings.en.json` was regenerated by build-en (tracked; includes the sibling agents' new specs G1-351 / G1-353 / K-357).
