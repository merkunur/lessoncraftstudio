# Arc 12 commission spec — lesson-plan strand-volume continuation + Pillar 2 commissioning window OPEN + Stream A escalation re-evaluation

**Type:** `[BUILD][LESSON-PLANS]` lesson-plan strand-volume; OR `[BUILD][BUNDLES]` Pillar 2 (separate parallel); OR `[INFRA][LESSON-PLANS]` Stream A escalation (separate parallel). Up to 3 concurrent arcs at Arc 12 commencement per operator strategic timing.
**Branch:** `pivot/printable-business-toolkit`
**Estimated phases:** 4 sub-commits per arc (lesson-plan); other arc shapes TBD by operator
**Estimated LoC:** ~2500-4500 (lesson-plan); Pillar 2 + Stream A LoC TBD
**Estimated sessions:** 2-4

**Status: DRAFT — operator ratification pending at Arc 11 Phase 4 close.**

## 1. Context

Arc 11 closed at 92 master packages + 33 locale variants. **45% master-package threshold crossed.** Single-package strand count reduced from 8 → 3 (massive bundle-curation thin-substrate improvement). 5 forward-pointer resolutions in single arc (peak).

**Three operator-strategic decisions pending at Arc 12 commencement:**

1. **Pillar 2 commissioning** (window OPEN since Arc 10 close):
   - Option α: Commission Pillar 2 NOW as separate parallel arc.
   - Option β: Continue lesson-plan strand-volume Arc 12; commission Pillar 2 at Arc 13 close.
   - Option γ: Parallel concurrent arcs at Arc 12.

2. **Stream A escalation** (re-evaluation triggered at Arc 11 close per operator directive):
   - Option σ-α (CC-recommended): Commission Stream A as Arc 12 separate parallel `[INFRA][LESSON-PLANS]` arc.
   - Option σ-β: Continue async deferral; Path B continuation 8th consecutive arc.
   - Option σ-γ: Stream A subsumed into Arc 12 lesson-plan Phase 1.

3. **Lesson-plan Arc 12 Phase 2 strand-depth choice** (always-required ratification).

These decisions may compose. Operator's strategic options range from minimal (σ-β + β + lesson-plan-only) to maximal (σ-α + α + lesson-plan parallel = THREE concurrent arcs).

**Notable arc-11-recon patterns generalizing forward:**
1. Pre-Phase-1 available-target audit (3rd consecutive validated).
2. Adjudicator-forward 5-strand pick at Phase 2 (NEW Arc 11 doctrine).
3. Phase 1 + Phase 2 forward-pointer-resolution-density maximization.
4. Single-package-strand deepening as Pillar-2-readiness preparation.
5. Stream A escalation re-evaluation when chain crosses operator-defined maturity threshold.

**Saturated strands as of Arc 11 (avoid Phase 1):**
- cognitive-and-executive-function/memory-and-attention
- cognitive-and-executive-function/pattern-recognition
- cognitive-and-executive-function/sorting-and-classification
- early-literacy/multilingual-language-awareness
- fine-motor-and-visual-spatial/spatial-reasoning
- fine-motor-and-visual-spatial/visual-discrimination

**Bundle-curation feasibility post-Arc-11:**
- 30+ literacy + 18+ numeracy + 11+ world-knowledge + 17+ cognitive + 11+ fine-motor packages
- Single-package strand count: 3 (down from 8 at Arc 10 close)
- Pillar 2 mathematical gate: 42 packages-past (92/50)

Out of scope for Arc 12 lesson-plan arc: Tier 3+ locale variants when Track C reaches Tier 3 (Arc 13+); subscriber UI; Mac Studio AI enrichment.

## 2. Pre-locked architecture (do NOT relitigate)

Per Arc 1-11 ship state. CC adjudicates within these locks.

- Schema: TeachingPackage + Bundle + BundleTeachingPackage stable.
- Storage convention: per Arc 8+ pattern.
- Validator contract: frontend/scripts/author-teaching-package.ts. Pillar 2 validator extension required if commissioned.
- Materials catalog: 10 entries.
- Exercise palette: 29 §14.10 apps.
- Sparse-override pattern + substrate-audit-at-arc-commencement + cross-strand spread + cluster-review-once-per-locale (binding).
- Pre-Phase-1 available-target audit + adjudicator-forward 5-strand pick (NEW Arc 11 doctrine).
- v3 SUBSCRIPTION-SCOPE.md.

## 3. Phase plan (lesson-plan arc shape)

### Phase 1 — Pre-audit + Path triage + family-members/action-verbs OR substrate-clean continuation (1 sub-commit)

**Pre-Phase-1 available-target audit:** automated audit per Arc 9 doctrine.

**Triage at Phase 1 entry:**

**Path A:** if Stream A escalation (Option σ-α) ships AND substrate sweep completes between Arc 11 ship and Arc 12 commencement → author identify-and-name-family-members + identify-and-name-action-verbs. **family-members FULL agent depth** per Arc 5+6+7+8+9+10+11 carry-forward.

**Path B:** if Stream A still not shipped → continue substrate-clean strand-volume. Path B options at Arc 12:
- Single-package-strand deepening continuation (only 3 strands left at 1/N).
- Forward-pointer resolution: identify-months-of-year (resolves Arc 10 forward-pointer); add-within-20 (resolves Arc 11 forward-pointer); compare-by-capacity (resolves Arc 11 forward-pointer); blend-onset-rime (Arc 11 forward-pointer); follow-2-step-oral-instruction (Arc 11 forward-pointer); match-uppercase-lowercase (Arc 11 forward-pointer).
- World-knowledge depth continuation (many unauthored targets).
- Strand-saturation closing: living-things at 4/6 (2 left); letter-recognition at 3/6 (3 left); data-and-graphs at 2/4 (2 left); environment-and-weather at 2/4 (2 left).

### Phase 2 — Strand-volume content authoring (1 sub-commit)

**Operator-strategic strand-depth choice at Phase 1 ratification:**

- **Option A — vocab-acquisition continuation.** 21 already authored; 3 unauthored remaining (2 substrate-blocked).
- **Option C — early-numeracy depth.** Many available targets across measurement (compare-by-capacity remaining; ALSO: order-3-objects-by-length, tell-time-to-the-hour, tell-time-to-half-hour, identify-coin-values, recognize-money-amounts-up-to-1-unit) + addition-subtraction (12 unauthored: add-within-20 + subtract-within-5/within-10/within-20 + add-3-numbers + 2-digit + ...) + place-value (3 unauthored).
- **Option E — world-knowledge depth (continued).** Many available targets across living-things (2 unauthored: identify-plant-parts + describe-butterfly-life-cycle) + environment-and-weather (2 unauthored: describe-day-night-cycle + describe-water-cycle-basic) + community-and-roles (1 unauthored) + materials-and-properties (2 unauthored — UNAUTHORED strand!) + personal-social-emotional-development (6 unauthored) + time-and-routine (3 unauthored).
- **Option F — single-package-strand deepening continuation.** Only 3 strands left at 1/N (time-and-routine + community-and-roles already 2/3 + addition-subtraction already 2/14 + drawing-and-tracing already 2/4 — actually most strands now 2+; time-and-routine is the remaining strict-1/N strand; identify-emotions etc are now 2). Limited remaining single-package work.
- **Option G — early-literacy strand-completion (NEW emphasis at Arc 11 spec).** 4 fully-unauthored early-literacy strands: phonics-decoding (0/11) + reading-comprehension (0/10) + spelling-and-encoding (0/5) + writing-composition (0/8). Substantial cross-strand-coverage broadening.
- **Option H — measurement strand depth (NEW emphasis at Arc 12).** Builds on Arc 9 compare-by-length + Arc 11 compare-by-weight. Add compare-by-capacity (resolves Arc 11 forward-pointer) + order-3-objects-by-length + tell-time work + money work. 8 unauthored measurement targets.

**Option B + D UNAVAILABLE** (saturated).

Default Option G OR Option H (both bring strand-firsts; CC-recommended at Arc 12 — operator preference between literacy-focus and numeracy-focus).

### Phase 3 — Continue locale-variant authoring (Track-C-state-gated; 1 sub-commit)

Single-question gate at Phase 3 entry. 4 sparse-override files. Selection per Arc 4-11 fold-in pattern.

### Phase 4 — recon + Arc 13 commission spec (1 sub-commit)

Authors arc-12-recon.md + arc-13-commission-spec.md.

## 4. Adjudication delegations (CC handles without surfacing)

- Pre-Phase-1 audit run + interpretation.
- Path A vs Path B selection at Phase 1 entry (post-Stream-A-escalation-decision).
- Per-package vocabulary verification.
- Per-strand depth template selection at Phase 2.
- Sparse-override locale-variant authoring.
- Per-package agent review depth.
- Commit cadence within phases.

## 5. Surface only at

- **Arc 12 commencement triple-decision** (Pillar 2 α/β/γ + Stream A σ-α/β/γ + Phase 2 A/C/E/F/G/H).
- Phase 1 entry: surface Path A vs Path B if ambiguous post-Stream-A-decision.
- Phase 1 family-members agent review (Path A only) → batched single review.
- Phase 3 Track C gate question.
- Phase 4 commit (arc-12-recon.md + arc-13-commission-spec.md).
- If anything contradicts Arc 1-11 architectural locks.

## 6. Verification

- All authored packages validate clean (zero-error rate per Arc 10-11 pattern).
- Path A: family-members full agent review applied + iteration documented.
- Strand-internal architectural consistency.
- Sparse-override files validate clean.
- Doctrine carry-forward verified.

## 7. Out of scope (commission-locked)

- Tier 3+ locale variants except Phase 3 organic extension.
- Subscriber-facing UI.
- Mac Studio AI enrichment.
- Material additions beyond catalog.
- Numeral-tracing-strips variant (Arc 13+ candidate).
- Analogy-app addition (Arc 13+ candidate).
- App-side modifications.
- Pillar 2 + Stream A folded into THIS lesson-plan arc — both commission as separate parallel arcs per operator commitment.
- learning-targets.json target-set extension for saturated strands.

## 8. Doctrine to load before starting Arc 12

- §1, §3.4, §10.3, §17.9, §A.13.6, §A.13.11, §A.14.
- `docs/lesson-plans/arc-1-recon.md` through `arc-11-recon.md`.
- `docs/lesson-plans/substrate-gap-inventory.md`.
- `frontend/config/learning-targets.json`.
- `frontend/config/materials-catalog.json`.
- `frontend/lib/exercise-palette.json`.
- `REFERENCE TRANSLATIONS/material-generator-shared.js`.
- `REFERENCE TRANSLATIONS/image-vocabulary.js` (check Stream A status post-escalation-decision).
- `project_k3_phrasing_native_speaker_review.md`.
- `docs/SUBSCRIPTION-SCOPE.md`.
- `frontend/prisma/schema.prisma`.
- `docs/audit-results/deck-page-indexability.md`.
- `docs/audit-results/fix-seo-h1-es-title-localization.md`.

## 9. Authorization — operator ratification surface

Operator ratifies these locks at Arc 11 Phase 4 (this commit) before Arc 12 commences:

1. **Pillar 2 commissioning decision (carrying from Arc 11; window OPEN since Arc 10):** Option α (Pillar 2 separate parallel arc NOW) / Option β (continue lesson-plan; Pillar 2 at Arc 13 close) / Option γ (parallel concurrent arcs).

2. **Stream A escalation decision (NEW at Arc 12; per Arc 11 close re-evaluation):** Option σ-α (CC-recommended; Stream A separate parallel `[INFRA]` arc Arc 12) / Option σ-β (continue async deferral; Path B 8th consecutive arc) / Option σ-γ (Stream A subsumed into Arc 12 Phase 1).

3. **Phase 1 path triage:** CC adjudicates Path A vs Path B at Phase 1 entry. Path A available iff Option σ-α/γ + substrate ships in time.

4. **family-members agent depth (Path A):** full agent depth carries forward.

5. **Phase 2 strand-depth choice:** Operator selects Option A (vocab; substrate-blocked unless Stream A) / Option C (numeracy depth) / Option E (world-knowledge continuation) / Option F (single-package-strand deepening; limited remaining) / **Option G (early-literacy strand-completion; CC-recommended)** / **Option H (measurement strand depth; CC-recommended alternative)**. **Option B + D UNAVAILABLE.**

6. **Phase 3 deferred-vocab fold-in:** continues per Arc 4-11 pattern.

7. **Surface posture:** standard.

## 10. Deferral-chain audit-trail (Arc 4 → 11 → 12)

- **Arc 4-11** triaged Path A → Path B per substrate state at Phase 1.
- **Arc 11 close re-evaluation** triggered Stream A escalation decision at Arc 12 commencement (per Arc 9 spec maturity-threshold doctrine).
- **Arc 12 Phase 1 path triage** depends on Stream A escalation outcome.

The chain validates substrate-completion + content-against-completed-substrate pairing pattern across **8 arcs**. If Option σ-β (continue deferral), chain extends to Arc 12+ — at this point an additional structural escalation may be considered (e.g., separate operator-Stream-A coordination commission at Arc 13+).

## 11. Commissioning options analysis (CC default-recommendations)

Three Arc 12 operator-strategic decisions interact:

**Stream A escalation** (Pillar 2 + lesson-plan can run with or without):
- σ-α + lesson-plan + Pillar 2 = **3 concurrent arcs** (max ambitious).
- σ-α + lesson-plan = 2 concurrent arcs (substantial).
- σ-β + lesson-plan = 1 arc (minimal cadence).

**Pillar 2** (independent of Stream A):
- α: Pillar 2 commissions; first-bundles authored.
- β: defer further (now 2nd arc deferred — operator may want to commit at this point).

**Phase 2 option** (always required):
- G (literacy strand-completion): broadest cross-strand expansion.
- H (measurement depth): builds on Arc 9 + 11 measurement work; resolves multiple forward-pointers.
- C (numeracy depth broadly): more diverse numeracy work.

**CC default-recommended path:** σ-α (Stream A escalate) + α (Pillar 2 commence) + Option G (Phase 2 early-literacy strand-completion). 3-arc parallel commit pattern. Reasoning:

1. **Stream A escalation overdue.** 7-arc deferral; threshold reached at Arc 11.
2. **Pillar 2 window has been open since Arc 10.** Further deferral is delaying value-realization without operational reason.
3. **Option G strand-completion broadens lesson-plan strand coverage** — pairs naturally with Pillar 2 bundle-curation work.
4. **3 concurrent arcs are operationally feasible.** Each arc has its own commit cadence; Stream A is operator-coordination + image-authoring (low Claude-Code-time); Pillar 2 is bundle-curation (separate validator path); lesson-plan is content-authoring continuation.

Operator may select more conservative paths (σ-β + β + Option G/H = familiar 1-arc cadence) or different Phase 2 emphasis. Operator-strategic decision at Arc 12 commencement.
