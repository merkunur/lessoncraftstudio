---
name: project-seo-recovery-program
description: The SEO audit & recovery program (clicks crashed 80→15); master roadmap + diagnosis + the 12 digestible parts
metadata: 
  node_type: memory
  type: project
  originSessionId: f1b67f62-72d0-4d73-bf1a-8af3408ba0df
---

# SEO Recovery Program (started 2026-06-20)

Google clicks crashed **~80/day → ~15/day** (~81%), starting ~2026-05-31. Operator (non-technical) commissioned an exhaustive, expert-agent-driven SEO audit-and-fix program — no shortcuts, nothing skipped, decisions made by expert agents (don't ask the operator technical questions). **Master roadmap (SoT): `C:\Users\rkgen\.claude\plans\employ-expert-agents-to-optimized-stonebraker.md`.**

**Cadence (operator-locked):** each of the 12 Parts is its OWN plan-mode session with fresh multi-agent expert audits (SEO + domain experts, re-run per sub-point — title, meta, alt, sitemap, hreflang…). Operator switches to plan mode before each Part. Part 1 of the program (the roadmap itself) is DONE + approved.

**Diagnosis (live-verified 2026-06-20) — the click drop = 3 concurrent changes, all reversible:**
1. PRIMARY: deck PDFs given `X-Robots-Tag: noindex` ~2026-05-31 (CLAUDE.md §17.8.20) — deindexed PDFs that were clicked directly in SERPs.
2. AMPLIFIER: ~22k `/decks/` pages removed from sitemap (`EMIT_DECK_SITEMAP=false`, 2026-06-14; sitemap index now only shards 2/3/4).
3. BLEED+BLOCKER: deck.html canonicals repointed to `/worksheets/` landings (en/de/es/sv) contradicting their hreflang + slug-mismatched; fr/it/pt/nl/da/no/fi decks ship NO canonical at all. Landings now exist for ALL 11 locales.

**Other confirmed defects:** deck.html baked hreflang = bare `pt` (not `pt-BR`) + `x-default=da` (not en) on ~16k assets (ESM SoT `frontend/lib/seo/hreflang.ts` is correct; stale CJS copy `scripts/publish-cli/hreflang-codes.js` baked wrong); JSON-LD `educationalLevel` raw English on non-EN decks (masked on de by homograph); English exercise-mode tokens in ~8-15k non-EN slugs; English CCSS strand leak where `localizeStrand()` not wired; mini-tool shells frozen `<html lang="es">`/title regardless of `?lang`; DE alt "Arbeitsblatt-Arbeitsblatt"; tool title double-brand; standards bare `og:locale`; `not-found.tsx` hardcoded "Browse All Apps".

**Cleared as NON-defects (don't re-investigate):** "English in Italian samples" = the EN homepage's correct EN alt for an intentionally-Italian showcase card (IT homepage clean); `/it/tools/sillabe` 410 is CORRECT (sillabe=activity not tool; real tool slugs 200); alt-text templates strong + retrofit ran broadly; Next.js SSR pages clean incl correct pt-BR.

**12 Parts / waves:** W1 recover+measure = P1 Click-Drop Recovery → P2 Crawl-Health Baseline → P12 GSC monitoring. W2 = P8 thin-deck classify → one coordinated ~16k-asset retrofit batching P3 hreflang + P4 metadata + P5 structured-data + P9 localization. W3 = P7 internal-link hub mesh → P6 image SEO → P10 410 hygiene → P11 Core Web Vitals.

## PART 1 — CLICK-DROP RECOVERY: DONE + VERIFIED (2026-06-20)
Two server-side fixes, both authoritatively verified live:
1. **Un-noindexed deck PDFs.** Reversed the 2026-05-31 `X-Robots-Tag: noindex` via NEW `scripts/publish-cli/unpatch-nginx-pdf-noindex.py` (backup→remove marked block→`nginx -t`→reload→rollback-on-fail). Verified: PDFs now 200, NO X-Robots-Tag; og-image/thumbnail still indexable.
2. **Fixed ~8,900 broken (404) deck PDFs** — a MUCH bigger find than noindex. PDF files on disk kept their original ENGLISH slug name (e.g. `addition-image-image-printable.pdf`) while the deck's public slug + page links were localized (`addition-bild-bild-tiere-printable.pdf`) → every such link 404'd. Concentrated in de/es/pt/fr/it (25-35%). Fixed via NEW `scripts/publish-cli/fix-deck-pdf-filenames.py` (BACKING-DIR-driven v2 — skip symlink aliases, canon = dir basename minus `-vN`; v1 symlink-driven ping-ponged decks with TWO aliases→one backing dir; both runs logged to /root, convergent). **Authoritative DB check: ALL 45,309 published decks now have a resolving printable.pdf, miss=0.** Answer-keys: 38,082 resolve; the 7,227 without are `printable_only:true` worksheet-gen-family decks (no answer key BY DESIGN, not linked anywhere).

**Diagnosis refinement (corrected first-round lossy-WebFetch errors):** the deck→landing canonical architecture is COHERENT + correct (landings self-canonical, index,follow, clean pt-BR/x-default hreflang, in sitemap; decks defer via canonical). Did NOT revert it — that's a measure-first call for P2/P12. The genuine replacement-less loss was the PDF noindex + the 404 filename mismatch.

**FORWARD follow-up (not yet done):** the PDF-generation path bakes English-slug filenames for localized decks — future publish waves (e.g. fi catalog) will recreate broken PDFs until the publish-cli/worksheet-gen PDF naming is fixed to use the localized slug. Filed for a code fix.

## PART 1.5 — PRINTABLE-PDF METADATA SEO: DONE + VERIFIED (2026-06-20)
PDFs are image-only (no text layer) → the /Info metadata is the ONLY text Google reads. Prior metadata reused the deck.html title/desc verbatim → cannibalized the landing, used "interactive/play online" framing, leaked English ("worksheet, K-3") onto non-EN. **Fixed:** NEW composer `scripts/publish-cli/pdf-seo-meta.js` (`composePdfMetadata`) emits printable-download-intent, localized, keyword-researched /Title + /Subject + /Keywords (real per-locale SERP patterns; Nordic/Finnish native-validated — nl "als PDF"/"Gratis te downloaden", no level age-parenthetical, fi "Ilmaiseksi ladattava ja tulostettava" + dropped allative). Wired into BOTH the retrofit `regenerate-pdf-metadata.js` (now DB-list-driven via `--deck-list`, old→new dry-run, distinct answer-key Title+Subject+Keywords) AND the forward path `publish.js` (future publishes inherit). **Retrofit: 45,309/45,309 published decks, 0 failures (~9 min).** Verified (`verify-pdf-meta.js`): 33 samples × 11 locales, 0 fails — no English leak, localized print-word present, no deck-framing, canonical /Link annotation preserved, char budgets OK; idempotent (re-run = identical); answer-keys carry distinct "— Lösungen"/"Answer Key" metadata. Also auto-FIXED stale baked themes (e.g. es chart-count old title "Accesorios" → correct manifest theme). Title pattern leads the download facet ("Printable {Type} Worksheet PDF – {Theme}, {Level}") — distinct from the landing's grade-led title (anti-cannibalization); Set-N dropped (variant_id is a hash, not a sequence). **Scripts deployed via pscp to /opt/lessoncraftstudio/scripts/publish-cli/; local repo has them UNCOMMITTED (pdf-seo-meta.js new; regenerate-pdf-metadata.js + publish.js + verify-pdf-meta.js) — commit/push when the operator wants; a future deploy.sh git-pull will see them modified.**

## PART 2 — CRAWL-HEALTH BASELINE: DONE + VERIFIED (2026-06-20)
**Headline: the technical SEO foundation is HEALTHY** — nearly every first-round "defect" (from lossy-WebFetch) is an ARTIFACT, ground-truthed via raw curl: hreflang IS in `<head>` (pt-BR, x-default→en, landings+decks); educationalLevel localized on landings; non-EN slugs localized; activity strand localized (`Zählen und Mengen`/`Lehrplan`); tool titles single-brand; standards og:locale=pt_BR; sitemap entries 200/no-noindex/self-canonical; `/worksheets` hub 200 all 11 locales (the "404" was a trailing-slash 308); deck→landing canonical intact; thin intersections correctly noindex+sitemap-excluded. (Only minor real item: deck.html JSON-LD educationalLevel English on non-EN — but deck.html is non-canonical → deferred, not worth a 16k retrofit.)
**The one real structural finding — FIXED: 13,162 landing-less decks (29%) were orphaned from the sitemap.** Of 45,309 published decks, 32,147 have a landing (deck.html canonical→landing, landing in shard 4 — correctly OUT of deck shards) but 13,162 have NO landing → self-canonical + indexable yet in NO sitemap. The 2026-06-14 removal over-corrected. **Fix (committed `2b12d7cd`, deployed):** `sitemap/0.xml`+`1.xml` re-emit decks FILTERED to landing-less only (`landingSlugForDeck===null`); index restored to [0,1,2,3,4]. **Live-verified: shard0=6589 + shard1=6573 = 13,162 (exact); chart-count-winter present, addition-image-image (has-landing) excluded.** Reversible (EMIT_DECK_SITEMAP/LANDING_LESS_ONLY flags).
**Built:** `scripts/seo-audit/crawl-health.js` (reusable live indexability regression guard — 30 samples, 0 fails) + `docs/SEO/gsc-recovery-checklist.md` (operator GSC steps). Commits `c85f3289`/`e2d9c938`, all pushed; server at `e2d9c938`.

## PART 5/PROGRAM STATUS — "MEASURE FIRST" (operator-chosen 2026-06-20); active code work PAUSED
Investigating Part 5 (structured data) + Part 7 (internal linking) found BOTH essentially already DONE: homepage has Organization+WebSite JSON-LD; landings have LearningResource+AlignmentObject+BreadcrumbList; deck.html has LearningResource+ImageObject+BreadcrumbList; hubs have CollectionPage+ItemList+BreadcrumbList; activity/standards/tool have BreadcrumbList(+educationalAlignment+FAQPage). Internal-link mesh strong (landing→20 related landings+18 topics; topic→29 landings+67 decks). Only structured gap = homepage SearchAction (BLOCKED — no public search endpoint; /browse,/search=404; hub ignores ?q=; low ROI). Content depth fine; hreflang/i18n correct. **CWV unmeasurable** (PageSpeed API 429 without key, my IP + server).

**CONCLUSION (honest): after Parts 1/1.5/2 the technical/on-page/structured/internal-link SEO is COMPREHENSIVELY HEALTHY. The first-round audit massively over-estimated defects (most were lossy-WebFetch artifacts).** Remaining recovery is gated on: (1) Google re-crawl TIME + the recent landing migration maturing; (2) measurement (GSC + PageSpeed, operator-side); (3) off-page authority/backlinks + fresh content (NOT code).

Surfaced the strategic fork via AskUserQuestion; **operator chose "Measure first."** Augmented `docs/SEO/gsc-recovery-checklist.md` with a PageSpeed step (commit `7d4f6704`).

**CWV MEASURED (operator PageSpeed, 2026-06-20): ALL 3 page types PASS Core Web Vitals in the FIELD/CrUX data (the ranking signal):** homepage LCP 2.1s/INP 92ms/CLS 0.01; landing LCP 2.1s/CLS 0 (SEO 100); deck LCP 2.1s/CLS 0 (lab 93, SEO 100). Lab 76/71/93 (throttled). Repeated lab flags are deliberate trade-offs or doctrine-protected (deck-image 4h cache vs no-purge-API §15.8; baked-JPEG §5; homepage render-block crawl-bait). **VERDICT: speed passes; NO performance pass warranted.**

## ACTIVITY PAGES — audited + one fix shipped (2026-06-20)
Deep audit: activity pages `/[locale]/activities/[slug]` are **SEO-healthy on every dimension** (self-canonical, index,follow, sitemap shard 3 = 8,138 URLs, localized titles/desc, correct hreflang pt-BR/x-default, clean 404 for missing locale variants, full JSON-LD with localized strands, ~800-1200 words, strong internal linking, localized index). **One small fix shipped:** the embedded mini-tool engine HTML (`/mini-tools/<engine>-activity.html`, ~15 files) was indexable standalone (200, no robots) yet thin widgets with a static English `<title>` (lcs-shell.js sets `<html lang>` at runtime but not document.title). Added `X-Robots-Tag: noindex` for `/mini-tools/:path*.html` via `frontend/next.config.js headers()` (commit `0d50ff99`, deployed+verified: engine HTML now noindex; parent activity/tool Next pages still 200+index,follow; iframes still serve). Textbook iframe-widget treatment — OPPOSITE of deck PDFs (standalone content, stay indexable).

## WORKSHEET-MAKER PAGES — audited 2026-06-20: technically PERFECT; 0 impressions = 410-recovery lag (likely the BIGGEST recovery prize)
Operator: 363 maker landings (33 makers × 11 locales, `/[locale]/tools/<native-slug>`) get 0 impressions. **Exhaustively verified live — NO technical defect:** all 200, index,follow, self-canonical, in sitemap shard 3 WITH fresh lastmod (3006/3006 url blocks have `<lastmod>`; it renders AFTER the 12 hreflang lines — an `-A6` grep "misses" it), 12 hreflang (pt-BR), JSON-LD WebApplication+Offer+BreadcrumbList, keyword-rich single-brand titles, no X-Robots-Tag, robots.txt allows /tools, internally linked (`/worksheet-makers` hub + mesh + homepage), middleware `/tools` 410 carve-out (LIVE_TOOL_SLUGS) covers every maker slug. **ROOT CAUSE = 410 history:** per `maker-content.ts` comment, the makers "were the site's single biggest organic-traffic source before the pivot 410'd them (GSC 2026-06-14)"; 410=permanent-removal→deindex; rebuilt at ORIGINAL ranked URLs 2026-06-14 (~6 days ago; bare `/tools/`,`/apps/` still 410). Google deprioritizes re-crawling ex-410 URLs → 0 impressions at 6 days is EXPECTED. **Likely a BIGGER share of the 80→15 drop than the PDF noindex.** Action (operator, only lever): Request Indexing on top makers + sitemap resubmit (now Day-1 in `docs/SEO/gsc-recovery-checklist.md`, commit `2b09acb6`). No code fix needed. Minor optional: 11 `/worksheet-makers` hubs not in sitemap (homepage-linked anyway).

## TOPIC + LEARN PAGES — audited 2026-06-20: HEALTHY (low impressions = by-design, not a defect)
Both technically healthy (code+live, all locales; no 410 history). Topic single-axis (1,269 in sitemap shard 3): 200, self-canonical, index,follow, honest hreflang, localized curriculum-aware metadata (de "bis 20/Vorschule", es "LOMLOE"). Topic dual-axis intersections: only **698 AUTHORED indexed** (have topicProse/topicMeta); ~96% noindex BY DESIGN (`intersectionIsAuthored` single-SoT gates sitemap+robots — correct thin-page discipline). Learn pages (25 in sitemap): 200, self-canonical, index,follow, keyword-rich localized titles ("Learn Spanish — Vocabulary Worksheets…", 141 worksheets/48 themes), CollectionPage+BreadcrumbList, correct same-target-language hreflang (English→10 siblings, Spanish→en+pt). Cross-language decks 200, repoint to /worksheets/, contentLanguage-isolated. **Low-impression cause = (a) 96% intersections noindex by design → growth lever is authoring more topicProse (CONTENT, not code); (b) learn surface small (25 pages); (c) re-crawl time + authority.** Minor optional code drift (recommend DEFER): canonical-order/query-param redirects use `redirect()`=307 but §16.5.3/§16.8.3 specify 308 `permanentRedirect()` (target self-canonical already consolidates; marginal); fix at `topic/[slug]/[secondary]/page.tsx:220,585` + audit single-axis/learn/activities for same pattern; needs rebuild.

**PROGRAM STATUS: the active code-fix phase is COMPLETE.** After Parts 1/1.5/2 (real fixes) the technical/on-page/structured/internal-link/CWV SEO is all verified healthy; the first-round audit massively over-estimated defects. **No further code lever remains.** Recovery is now purely: (1) Google re-crawl TIME (PDFs days-weeks, landing migration weeks-months); (2) GSC observation (operator running the checklist); (3) off-page authority/backlinks + fresh content (NOT code). **Do NOT resume code parts unless NEW data justifies:** Part 8 thin/near-dup content (needs GSC traffic data showing the 13k re-added decks underperform), or a targeted fix if GSC surfaces a specific weak cluster. Roadmap parts 3/4/5/6/7/9/10/11 = verified already-healthy/artifacts/passes.
