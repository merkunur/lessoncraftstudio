/** K-330 — First Sound Given. nt20-C variation of K-318 (generated by tools/gen-b3var-specs.js from tools/b3var-rows/). */
'use strict';
const base = require('./K-318-sound-boxes.js');
// One object for all three levels: the waves ship d2 only, so a face must
// render identically whichever level is asked for. Spreading the base entry
// (not a JSON literal) carries function-valued params through intact.
const D = { ...base.difficulty[2], ...{"starter":true,"minG":4,"maxG":5} };
module.exports = {
  ...base,
  id: 'K-330',
  slug: 'sound-boxes-first-sound-given',
  difficulty: { 1: D, 2: D, 3: D },
  i18n: { en: { title: "First Sound Given", instruction: "The first sound is written for you. Say the picture word slowly and write one sound in each empty box. A wide box holds two letters for one sound." } },
};
