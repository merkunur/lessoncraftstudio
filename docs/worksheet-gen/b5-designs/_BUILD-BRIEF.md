# nt10-E BUILD BRIEF — the contract for building ONE family (base spec first; faces in Phase E)

You are building one printable worksheet family for the headless generator at `scripts/worksheet-gen/` from its FINAL design file `docs/worksheet-gen/b5-designs/<ID>-<key>.md`. The design file is the contract: §1 identity, **§2 the base page (build this)**, §3 the five faces (read for the additive knobs the base must already carry, but do NOT emit the face specs — Phase 2), §4 the native rebuild plan (read; the EN bank must be shaped so the panels can fill it), **§5 data + gates (build this)**, §6 SEO (read), §7 hub (read). Read `_SUBSTRATE.md` (measured facts) and the README's "Cross-type rulings" before writing a line. Where a design file and the README rulings disagree, the README wins: `unitAxis` is the ONE fan knob (files may say letterAxis/ruleAxis/familyAxis); themeless types draw pictures from `lib/b3-picture-index.js`; boundary-printing syllable faces use `lib/b3-common.js texPool` (shared, stays b3); the body is **722 px** (not 760) — re-budget every stack or use `minmax(<min>px,1fr)` rows; `.ws-lane` inner width is 639 unless you declare the inline `padding:10px 12px` override; every OPEN numeral box uses a new `blankNumeralBox`, never `answerBox` without an answer; `coordinate.mode` is never null (landing side, later).

## Deliverables (all files type-scoped; nothing shared is edited)
1. **Spec** `scripts/worksheet-gen/types/<band>/<ID>-<slug>.js` — the contract of `_SUBSTRATE.md` ("Spec contract"): `{id, slug, gradeBand, assetClass, exerciseType:<key>, themeAxis, unitAxis?, difficulty:{1,2,3}, i18n:{en:{title,instruction}}, build({theme, difficulty, locale, unit}, {rng}) → {bodyHtml, meta}, verify(page) → string[]}`. `build()` reads ONLY its bank `data/b5/<key>.js` via `require('../../lib/b5-common.js').bank('<key>', locale)` (+ `lib/b3-picture-index.js` for themeless pictures, `lib/b3-instructions.js fillSlots` for slot frames, `lib/b2-common.js` helpers) — never `image-vocabulary.js` or `approved-words-*.json` at render. A unit-bearing spec declares `unitAxis:{applicable:true, units:(loc)=>[...], exemplar:(loc)=>'…'}` and `build()` uses `unit || bank.exemplar`. Answers are NEVER printed; ground truth lives in `data-lcs-*` attributes; `verify(page)` re-derives every answer from the stamps (it runs in puppeteer `page.evaluate`, so it cannot `require`). Stamp `[data-ws-content]` on the root. Guards key on the RESOLVED config, never on the level index. A locale/theme the pool cannot satisfy THROWS (refusal) — never a filler, never a silent substitution.
2. **Components** `scripts/worksheet-gen/templates/components-b5/<key>.js` — ONLY the components the design file names NEW, exported by exactly those names (the namespace `templates/components-b5.js` merges every family file and refuses a duplicate export name — if a sibling already exports the name you need, `require` it from `../components-b5.js` instead of redefining it). New primitives only where the file says NEW (`primitives/<name>.js`, pure SVG on `primitives/_tokens.js` — token colours only, `qa/lints.js` rejects off-palette hex).
3. **EN bank** `scripts/worksheet-gen/data/b5/<key>.js` — the shape in §5, EN block hand-authored, module shape `'use strict';\nconst <NAME> = { en: {…} };\nmodule.exports = { <NAME> };` (the non-EN blocks are generated later by `apply-b5-locale.js`; `data/` is gitignored — the reviewer force-adds). Every word must be `traceable()` where the file says so, every picture must exist (`fileUri` / `pictureFor`), no localized BW marker.
4. **Gate** `scripts/worksheet-gen/qa/verify-b5-<key>.js` — the §5 gate: renders through the REAL pipeline (`render/batch.js runBatch` or `render/render-instance.js`, never a bare puppeteer page — fonts load only from `file://`), asserts `verify()` empty + `qa/lints.js` clean + the element-size floors ITSELF (K ≥56 px, G1 ≥44, G2-3 ≥36 — `qa/lints.js` has no size lint) + the seed sweeps the file specifies; then runs EVERY poison case the file lists — each must FAIL and the correct EN bank is the control; exit 1 on any real failure OR any silent poison. `--quick` may skip the sweep.
5. **Renders** via `node render/one.js <ID> <theme|null> <d> en [unit]` at d1, d2, d3 (and the fi/de-length chrome test if the file names it) → `out/dev/*.png`; LOOK at every PNG yourself and fix what you see (cut-off, sparse, tiny, overlap, a wrong picture); run `node qa/contact-sheet.js out/dev <ID> …` if useful.
6. **Report** `docs/worksheet-gen/b5-designs/_work/<ID>-build.md`: what was built, every deviation from the design file with the measured reason, the gate output (assertion count, poisons killed), the PNG paths for the reviewer, open items for the faces/panels. Also `node tools/b3-baseline.js --check --quick` must print PASS (you touched nothing shared) — paste its line.

## Rules
- Type-scoped filenames everywhere (`out/dev/<ID>-*`, scratch `<id>-*`); ≤1 agent per family; never edit `templates/components.js`, `components-b2.js`, `lib/b2-common.js`, `enumerate.js`, `cli.js`, `render/*`, `emit/*`, `qa/lints.js`, `page/*`, any existing spec or data file. Never `git commit`/`git add` — the reviewer commits.
- Measure, don't assume: pool sizes with node against the real data; widths inside the real render (`render/one.js`), never a blank page.
- No visible copy may claim "free" (sheet title/instruction) — SEO metadata is another surface and not yours.
- If a §2 element cannot be built as specified (a primitive option that does not exist, a stack that overflows 722), build the nearest faithful thing, record the deviation with the measurement, and flag it in the report — do not silently redesign.
- The `i18n.en.title` ≤ 70 chars, no "worksheet" word (validator rule), unique in its band (`node i18n/build-en.js` must pass).

## nt10-E additions (binding; the nt10-D additions below still apply, read b4 as b5)
- **Build the base page AND its NEW primitives first**, each primitive with a render-measuring verify (`qa/verify-<primitive>.js`, the `verify-body-figure.js` pattern: assert from the EMITTED markup, poison it both ways). Primitives this batch: flat-shape (K-368) · road-sign, traffic-light, road pictograms (K-369) · family-figure, family-tree (K-370) · plant-figure (G1-376) · life-stage (G1-377) · moon-phase, sky-bodies, planets (G1-378) · map-symbol, island-map, compass-rose, top-side-view, world-map + its data module (G1-379) · any unit the digraphs / synonyms / word-parts finals name. Only what the FINAL names; the `_work/*-design-[AB].md` files are provenance, never the contract.
- **The art is the product.** After every primitive: render it at the smallest and largest size the design uses, in colour AND greyscale (`render/one.js` then convert, or an SVG-only harness through the real fonts), READ the PNGs yourself, fix what a six-year-old would misread, and put the PNG paths in the build report. A drawing that "matches the numbers" but reads wrong is a defect.
- **Locale-convention data** (road-sign conventions per locale, compass letters, continent sets, hemisphere mirror, kin terms, letter-team inventories) lives in the family bank as data with the EN block filled and the other locales left for the native panels; the base must render and verify in `en` now, and the spec must REFUSE (throw) for a locale whose block is absent.
- **World map data** (G1-379): derive the outlines from Natural Earth 1:110m land (public domain) with a committed, reproducible script under `scripts/worksheet-gen/tools/` that projects + simplifies exactly as the final states and writes a data module; never hand-draw continents; the gate checks the final's land/water checkpoints.
- Add nothing to `package.json`; if a geometry library would help, write the needed function (projection, Douglas-Peucker) yourself.
- Record the release proof: `node tools/b3-baseline.js --check --quick` PASS (paste it) — the only accepted drift is none (new ids are not in the baseline).

## nt10-D additions (each bought during the design; binding)
- **Every stack is checked at 722 AND at 677** (the four-line fi title chrome) with the arithmetic in the report; the design files quote both.
- **Printable decks ship NO answer key**: no title/instruction/meta string may say "with answers" or promise a key; the gate bans it.
- **The family key for K-356 is `weather-symbols`** (the THEME axis owns `weather`); read `weather-symbols` wherever the design file says `exerciseType: weather` / `apps.weather`. `withFixedTheme(type, 'weather')` stays as designed.
- **Relation glyphs `≈` (U+2248) and `→` (U+2192) are OUTSIDE every font unicode-range** (`assets/fonts/fonts.css`): draw them as SVG; the gate bans the codepoints as text nodes. `qa/lints.js` fails text under 9 px, so a `clock.js` face needs size >= 74 px.
- **Additive primitive options must leave every live consumer byte-identical**: `primitives/thermometer.js` `bands` + `numerals:false` (G3-345 re-render poison in the gate); `node tools/b3-baseline.js --check --quick` PASS is the proof (paste it).
- **NEW primitives are specified geometrically in §2** (`primitives/tangram.js`, `body-figure.js`, `water-cycle.js`, `bin.js`); each ships with the node gate the design file names (e.g. `tools/gate-tangram-figures.js`) and a render-measuring verify (the G2-235 ruler precedent), never a derived constant.
- **Pictures the design file EXCLUDED stay excluded** (the file's `picOpened` / exclusion lists are data: encode them as bank exclusions the validator asserts); never substitute a similarly named picture.
- **Names / portraits shared between families** (pronouns -> question-words) are read from the OWNING family's bank; the build order is in the README.
- The bank loader is `lib/b4-common.js` (`bank(name, loc)` over `data/b4/<name>.js` + `data/b4/locales/`); `lib/b3-picture-index.js` and `lib/b3-instructions.js fillSlots` are SHARED libs and stay b3.
- Scratch under the session scratchpad with the `<ID>-` prefix; write scripts to files (never `node -e` with backticks).
