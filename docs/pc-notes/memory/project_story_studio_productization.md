---
name: project-story-studio-productization
description: "Story Studio premium productization (roadmap class #3) — DEPLOYED LIVE + verified 32/32 on prod 2026-07-04 (commits be52efbb..b1f7b2ec); architecture, verification, open items"
metadata: 
  node_type: memory
  type: project
  originSessionId: 8bf447dd-853d-4f63-83aa-8b4d61bb54b7
---

# Story Studio productization — **DEPLOYED LIVE + VERIFIED 2026-07-04** (commits `be52efbb`→`b1f7b2ec`)

## STUDIO FIXES COMMISSION 2026-07-06 — DEPLOYED LIVE (commits `22fed94b` + `9df3e45a` + `22b548ae`), prove-studio-tenant 35/35 + prod-DOM proof; AWAITING OPERATOR WALKTHROUGH (they must RELOAD the tab)

**ROUND 8 (2026-07-06, live) — reader page navigation in the storybook PLAYER (SVG prev/next arrows).** Operator: no way to move between pages — the player's `runPage(i)` was a self-recursing chain that advanced ONLY on activity completion, never backward. Now: circular chevron buttons (inline SVG, `.sb-ui` chrome style) at the frame edges vertically centered; prev hidden on page 1; next on the last page → The End; ArrowLeft/ArrowRight keyboard (suppressed inside `.sb-zone`); manual nav = free browsing (skipping an activity allowed; completion still auto-advances); back-nav from The End removes the overlay. **Correctness core = the `navGen` generation counter**: every `.then` boundary of the runPage chain bails when stale (kills double-advance from the celebration hold + dead-page narration; `playCues` gained a `liveFn` short-circuit; `SBAudio.stop()` on nav; a stale playCues resolution PARKS on a never-resolving promise). `prevPage`/`nextPage` chrome strings ×11. Player `?v=4` in storybook.html (the ONLY host). Gate `scripts/storybook/prove-player-nav.js` (17 asserts vs prod pips-picnic — incl. mid-narration nav + no-ghost-advance; puppeteer from REPO-ROOT node_modules, not frontend/). prove-tenant-play 6/6 regression (needs `LCS_BASE_URL=https://www.lessoncraftstudio.com` + `LCS_TEST_TOKEN` from mint-test-session). **Deploy for player-only changes = git pull + cp to /var/www/lcs-media/mini-tools/ — NO build/restart (nginx-static since R7).**

**ROUND 7b (2026-07-06, live) — sheet upload no longer hard-requires a frame named "neutral" (operator-hit on their first real TexturePacker sheet).** Root cause: `bakeCharacterSheet` names poses after the frame FILES, so real exports never produce `pose_neutral`; the library contract (card thumbnail + picker default) rejected every real sheet. Fix: `addLibraryCharacter` ALIASES an operator-chosen resting pose (default: first frame) as a second `pose_neutral` atlas key at the same rect (original names stay valid; contract passes by construction); manager modal gained a "Resting pose" select populated from the picked atlas (preselects any frame containing "neutral"); `frameSlug()` extracted from the bake so the UI name maps to the bake's slug; route field `neutral_pose`. E2E: synthetic 2-frame no-neutral sheet, both chosen + default paths, card pixel-verified renders the right frame.

**ROUND 7 (`ed26b444` + `f8232896` + `0368a1e4` + nginx patch, 2026-07-06) — library pictures as PAGE OBJECTS + the STORYBOOK LIBRARY MANAGER.** (1) **Part 2 (`ed26b444`, live):** "Use a picture from the library" no longer replaces the scene — library pictures are OBJECTS on `scene.layers` (already validated + player-rendered; editor made them first-class): `.stu-layer` selectable/draggable (z 15 — above zone chrome 10, below characters 20; the zone STOLE layer drags until the z bump), SE-handle ASPECT-LOCKED resize, nudge/delete, `addLayerHere()` = openLibrary (incl. "Upload from my computer") → probe natural size → w=min(360,natW) + aspect h → `ensureImageAsset` → push+select. Inspector: Picture step = "Pick/Change the background" + layer chips + "+ Add a picture"; the library-replace button REMOVED from the background picker. Deleting a layer splices layers ONLY (assets stay — shared/deduped, orphans harmless). Bridge suite section G (7 asserts). Bumps canvas?v=7 inspector?v=8. (2) **Part 1 (`f8232896`+`0368a1e4`, live): the library MANAGER** at `/worksheet-generators/storybook-library-manager.html` (content-manager-v2 auth scaffolding; §14.6 two-step served): tabs Characters|Backgrounds|Props-pointer; upload TexturePacker pair OR single image (one-pose synth), clips, inline en/de name+tags/theme PATCH, per-asset validation strips (broken-on-disk greyed w/ errors), delete-confirms, rebuild button. Engine: `scripts/storybook/lib/library-core.js` (manifest engine extracted from build-library-manifest; STRICT = CLI all-or-nothing, TOLERANT = broken excluded + reported per-asset; CLI is a thin wrapper, parity proven) + `frontend/lib/studio/library-admin.ts` (guarded writes, unique-id suffixes, per-process rebuild queue, bg cover-fit 1600×1000 q80) + 6 withAdmin routes under `/api/admin/storybook-library`. Live tree `/var/www/lcs-media/mini-tools/storybook-library` is authoritative (git = seed; backup-studio.sh tars it — verified 36 entries). **⚠ TWO prod-only traps burned this round:** (a) **webpack bundles DEEP CJS `require('sharp')`** inside a bundled script module → native binary can't load from a chunk → routes must INJECT sharp (`opts.sharpImpl`; the top-level TS import is externalized + works — media.ts proves it). (b) **`next start` snapshots `public/` at BOOT** — files written mid-run 404 until restart (updates to boot-time files like manifest.json DO serve, masking it!). Fixed structurally: `scripts/studio/patch-nginx-minitools-static.py` — nginx now serves `/mini-tools/` via `alias /var/www/lcs-media/mini-tools/` (worksheet-generators precedent; html+json no-cache, rest 1h; lcsasset limit kept; §20.3 doctrine now matches the vhost). Full prod E2E green: upload bg + single-image char → live manifest → PATCH → card.webp 200 → delete → clean; test session revoked.

**ROUND 6 (`b2a79b11`, 2026-07-06) — write-the-answer keypad, instant Add-text, story lines on the page, zip-upload removed.** (1) "Addition became multiple choice": the family-A mapper's palette = answers' chars + 3-5 distractors → for NUMERIC now the FULL 0-9 keypad (+'-' only if negative; letters stay curated — 26 keys is wrong for K-3); the player module ALSO normalizes any all-digit palette → already-imported packages self-fix. catalog-export ?v=43 (29 apps two-stepped); prove-sep-crop palette cases; baselines 4/4 clean; prod prove-tenant-play 6/6 incl. "11 keys". (2) "Add text doesn't function" = the click-the-canvas place-mode discoverability trap (the addCharacterHere lesson) → `addTextHere()` places IMMEDIATELY (centered title spot, +64y per existing), selected, words box focused. (3) "Story lines don't show on the page" = editor WYSIWYG gap → `.stu-caption-mock` band at the player's caption geometry (line 1 + "+N" chip). (4) Operator-directed: the .zip upload FIELD removed from sb-worksheet-exercise (endpoints stay — the FormData intake IS the bridge's path). **m7 gotcha: its `clickBtn` matches by startsWith — any new panel button starting "+ Add" shadows the activity form's exact "+ Add"; fixed with an exact-text click. studio-canvas's `el(tag, cls)` takes NO text arg (the inspector's takes 3) — set textContent explicitly.** Bumps: sb-mod-worksheet-exercise?v=5 (both htmls), canvas?v=6, inspector?v=7.

**ROUND 5 (`adb1f6b0`, 2026-07-06) — tenant preview plays, session resilience, TEXT tool.** (1) **Tenant preview showed only the scene**: `storybook-interaction.js:220` hard-coded `ctx.storyBase='/mini-tools/stories/'+storyId+'/'` — in tenant preview storyId=LINKID → sb-worksheet-exercise's descriptor fetch 404'd → '!' glyph. Player now passes `mediaBase` (tenant `src+'m/'` — the ONLY binary route is `/api/play/<link>/m/[...path]`; operator = static folder) into SBInteractionHost; storyBase = `opts.base || fallback`. Picker activities were never asset-broken (story.assets srcs server-rewritten by toClientUrls); UNCONFIGURED ones play as empty boards → the preview drawer now warns when the page's activity has validation errors. **NEW prod gate `prove-tenant-play.js`** (API-create story + SEP import + place → puppeteer the accountless `?src=/api/play/<link>/` player → board builds) — **5/5 vs prod**. (2) **Frequent logouts**: Session.token exact-match rotates on ANY refresh/sign-in; studio autosave (800ms, no retry) hit rotation races; failed refresh wiped localStorage. Fixes: studio-core `api()` retry-once-after-refresh via wrapper postMessage (`lcs-studio-refresh`/`-done`, single-flight; wrapper runs `useAuth().refreshToken()`); `subscriber-api-gate` bumps `Session.lastActivityAt` (throttled 1h — the 24h idle sweep can't reap active editors); `load()` now OFFERS the localStorage backup back (was write-only!). **NOT changed (operator-flag): the single-device deviceId session-deletion policy** — a second browser/incognito still kills the session by design. (3) **TEXT TOOL**: `page.text[]` existed in schema+validator+player (`.sb-text`, `{stringKey,x,y,w?,size?,align?}`) with NO authoring UI — Studio now has "Text on the page" recipe step, click-to-place, WYSIWYG drag/nudge/delete (selection kind 'text'), inspector panel (words/size/align/width/remove), per-locale strings. Bumps: player?v=3 interaction?v=3 core?v=3 canvas?v=5 inspector?v=6 STUDIO_CLIENT_VERSION=5. Gates: bridge 39, m1-m8, m7, audit 24/24, layout, sep-crop, word-guess chain, tenant 35/35 — all green. **⚠ PS 5.1 LESSON: NEVER Get-Content|Set-Content non-ASCII files (mojibake double-encode) — use the Edit tool.**

**ROUND 4 (`4a9b552e`, 2026-07-06) — crop fidelity + zone shrink.** (1) A deliberate crop-UI crop swallowed the NEXT stacked exercise: `_sepExpandCropToAnswers` qualified answer boxes by ANY overlap of a ±24px band → now **vertical-CENTER-in-crop** (same-row horizontal forgiveness kept) + the 14px pad applies ONLY to edges that grew; `_sepInCrop` element inclusion is center-based (no half-visible interactive neighbors; no-op for auto-crops — 4/4 golden baselines clean). Unit gate `prove-sep-crop.js`; apps `catalog-export.js?v=42` (two-stepped). (2) "Bigger but not smaller": `sb-worksheet-exercise` minZone 640×520 → **416×320** (8du-grid-aligned — a non-grid min snaps AWAY on commit, the 424 lesson; density 16px floor remains the real gate) + the zone resize now **live-clamps corner-aware at minZone** (no more free-shrink-then-spring-back). Bumps: sb-mod-worksheet-exercise?v=4 in BOTH studio + player html, canvas?v=4, inspector?v=5. prove-studio-bridge now 32 asserts (incl. the shrink gesture + zone-visual).

**ROUND 3 (`a8e07aa4` + nginx patch, 2026-07-06).** Operator walkthrough #2 surfaced: (1) **preview = nginx 429** — the player boot's ~30 `/mini-tools/*.js` + `/api/play/*` media + `/api/studio/*` autosaves all ran through the catch-all's bot-defense limit (`lcsperip` 8r/s burst=40; access-log verified). Fixed by `scripts/studio/patch-nginx-studio-limits.py` (idempotent, applied + reloaded): new `lcsasset` zone 50r/s with `/mini-tools/` + `/api/play/` (burst 300) and `/api/studio/` (burst 120) locations above `location /`; global 8r/s intact (parallel-burst verified: mini-tools 60∥→60×200, catch-all 80∥→429s past burst). Backup at `/root/nginx-backups/`. **⚠ vhost is server-side: re-run the patch after any nginx rebuild.** (2) **placed worksheet looked EMPTY** — the editor canvas never drew the SEP visual (only the player does): `studio-canvas.js?v=3` now letterboxes `descriptor.json→visual.file` inside the zone (`sepThumbs` cache; tenant `/api/play/<previewLink>/m/`, operator `/mini-tools/stories/<id>/`; `.stu-zone-sep`); prove-studio-bridge asserts naturalWidth>0 + right package. (3) `openPreview`'s per-open `Studio.on` saved-listener leak kept re-fetching DETACHED preview iframes on every save — guarded with `iframe.isConnected` (`studio-inspector.js?v=4`). **Server fact: `frontend/public/mini-tools` is a SYMLINK → `/var/www/lcs-media/mini-tools` and next-start serves public dynamically → cp alone ships mini-tools changes, no rebuild.**

**REOPEN + THE REAL ROOT CAUSE (`22b548ae`).** The operator's first walkthrough said "nothing has changed" — their screenshot showed the SITE nav+footer around the studio with no studio toolbar. Cause: **`Navigation.tsx:75` is `relative z-50`; the editor wrapper (old AND new) was `fixed inset-0 z-40` — the site nav ALWAYS out-stacked the overlay on prod and buried the studio's toolbar under ~230px of site chrome.** The redesign shipped but the top of the screen looked identical. Fix: `LocaleLayoutClient.tsx` renders `/​{locale}/studio/<storyId>` in the STANDALONE branch (the `/get/` precedent — NO Navigation, NO Footer; dashboard `/studio` keeps chrome; SSR-effective → curl-assertable: editor page `<nav>`/`<footer>` = 0/0, dashboard 1/1 — VERIFIED live). Belt-and-suspenders: the wrapper + iframe now position via INLINE styles (`position:fixed, inset:0, zIndex:60`) so no purge/stacking war can defeat them; STUDIO_CLIENT_VERSION=4. **LESSON: any full-viewport route inside [locale] layout MUST use the LocaleLayoutClient standalone branch, not z-index escalation — and prod-visual claims need a signed-in DOM check, not just chunk-content greps.**
Three operator defects fixed (plan file `commission-studio-delegated-kernighan.md`):
1. **Direct handoff (`__lcsSepBridge` receiver contract).** The in-app "Export for Storybook" button (it lives in each app's Download▼ dropdown, `#sepExportBtn`) zip-downloaded even inside the Studio — apps never read `?sepEmbed=1`. Now `catalog-export.js` (?v=40→41 all 29 apps, 0 per-app lines) detects sepEmbed + feature-detects `parent.__lcsSepBridge` at CLICK time: relabels the button "Add to my story", auto-crops, delivers the in-memory package to `receive()` — zero downloads; stale-cache skew degrades to today's zip, never a dead end. Placement (`applyWorksheetExercise`): a page's PRE-DRAWN zone is preserved, else aspect-fit-to-crop centered (cap 1400×840, floor minZone 640×520, ≤1.6× upscale — density-safe: validator boardScale is a min()); the zone lands SELECTED + a toast. onAdded now carries `descriptor`.
2. **Canvas-first shell.** Grid via CSS vars (`--railw/--panelw`, rows 44px/0); collapse classes `stu-top-closed/rail-closed/panel-closed` on #stu-app; toolbar 44px w/ "← My stories" (teacher: postMessage `lcs-studio-nav` → wrapper router.push; operator: launcher), compact savestate dot, ⋯ overflow <1280px (both button sets always in DOM — m7 text-matching safe), collapse chevron + floating Menu pill (carries savedot; savestate conflict/error FORCE-opens the bar); rail → 44px numbered strip; panel → 0 + "« Page setup" edge tab + AUTO-OPENS on selection CHANGE (`_lastSelKey`); floating zoom cluster `− % + | Fit | ⛶ Focus | ⤢`; Focus = all three collapsed (~96% canvas), transient (not persisted); `studio.ui.top/rail/panel` in localStorage; zoom NEVER persisted. **View engine (studio-canvas.js): transform stays PURE scale() + left/top translation — `duPoint()` unchanged**; setZoom cursor-anchored clamp [fit×0.5, 4]; clampPan ≥80px visible; Ctrl+wheel/wheel/Shift+wheel; space/middle-drag pan via CAPTURE-phase host pointerdown (plain drags untouched); Ctrl+0/1/=/− + Ctrl+\; ResizeObserver refit guarded `if (drag||panDrag)`. Wrapper `StudioEditorClient.tsx`: back-bar band REMOVED (iframe absolute inset-0), authLost = overlay pill, STUDIO_CLIENT_VERSION=3. `StudioInspector.setShell()/toggleFocus()` exported (tour force-opens shell).
3. **Lifecycle.** Bridge singleton (`_current.destroy()` on open-while-open), Esc closes (parent capture keydown + in-iframe relay through `__lcsSepBridge.esc` — iframe keydown never reaches the parent doc), Esc also cancels the crop UI (`_sepCropOpen` stands the relay down), "✓ Placed — back to your story" beat → onAdded FIRST then auto-close 900ms, busy makes Esc/Close inert, teardown deletes the receiver.
**New gates:** `prove-studio-bridge.js` (27 asserts: relabel/reveal in the opened dropdown, zero-download CDP+anchor-spy, zone aspect/preserve, Esc/relay/singleton/doc-intact, standalone regression) + `prove-studio-layout.js` (46: chrome ≤48px, no bottom band, canvas share 56/61/72% open + ≥95% Focus at 3 viewports, drag-du-math under zoom2+pan, persistence, wrapper static) + `shoot-studio-states.js` (before/after evidence → `docs/audit-results/storybook/studio-redesign/{before,after}/`, NOT committed — no-image rule). prove-studio m1-m8 + m7 + audit-studio 24/24 + SEP baselines pass unmodified (m7's stale `===17` module count fixed → `>=17`; catalog is 24).
**Gotchas fixed/learned:** `deploy-studio-closeout.sh` was missing `studio-canvas.js` in its sync list (added); `#sepExportBtn` reveal must use inline `style.setProperty('display','inline-block','important')` NOT class-removal (the `.lcs-admin-only + hr` sibling rule); app HTMLs serve `no-cache` at the edge (v-bump flips immediately); detached-iframe reads in puppeteer throw SYNCHRONOUSLY (read spies during the placed beat, not after close).

The teacher-facing multi-tenant Story Studio (Master Roadmap rev.2 premium class #3). Plan file: `C:\Users\rkgen\.claude\plans\the-premium-subscription-will-sorted-thompson.md`. Operator rulings: **EN+DE editor chrome at launch** (other 9 fast-follow), **curated libraries + simple single-image teacher uploads** (no spritesheet uploads for teachers). Related: [[project-interactive-storybooks]], [[project-business-strategy-2026-07]].

## Architecture (locked in build)
- **JSON in Postgres, binaries on isolated FS.** `StudioStory` (sb-1 storyJson/stringsJson, `rev` optimistic concurrency speaking the local server's etag dialect — etag = String(rev), `locale` = single authoring language, `gradeBand`, `assetBytes`, soft delete) + `StudioShareLink` (12-char linkId; kind `preview` auto-created per story = the editor's img/preview capability; kind `share` = the class link) + `StudioStoryRevision` (last 10 snapshots). Migration `20270703000000_add_studio_story_tenancy`. Binaries at `/var/www/lcs-media/studio/stories/<storyId>/` (dev: `frontend/.studio-stories/`, gitignored); same folder shape as operator stories.
- **Canonical `/studio-media/<storyId>/…` srcs in the DB; serving rewrites to `/api/play/<linkId>/m/…`** (`frontend/lib/studio/urls.ts`). **Save-time src allowlist = the security keystone** (own prefix + /mini-tools/ + /image-library-webp/ only; 16 unit assertions). Media route: 60s LRU link cache, traversal guard, `Cache-Control: private` (CF shared cache excluded by origin header).
- **`requireStudioSubscriber`** (`lib/studio/story-gate.ts`) — requireSubscriber only AUTHENTICATES (Collections went sign-in-only in the free pivot); the studio gate additionally enforces `isLcsSubscriptionActive` (admin bypass = how the operator tests while 0 subscribers).
- **Dual-mode client:** `Studio.api()` in `mini tools/studio-core.js` maps `/studio/*`→`/api/studio/*` + Bearer (localStorage.accessToken; same-origin iframe) when `?mode=teacher`; 401 → postMessage `lcs-studio-auth` to the Next wrapper. Operator local mode byte-path unchanged (prove-studio m1–m8 green throughout).
- **Player:** additive `?src=` base override (prefix-validated `/api/play/…` or `/mini-tools/stories/…`) + audio `base` threading. Operator `?story=` untouched.
- **validate-story.js core extracted** to `scripts/storybook/lib/validate-core.js` (CLI byte-identical, gate-story spawn contract intact); tenant wrapper `frontend/lib/studio/validate.ts` (webpack-bundles the CJS core → standalone-deploy safe); validate route + the share ready-gate use it.

## The product surfaces
- `/[locale]/studio` dashboard (noindex, client-gated, EN+DE via `studio-strings.ts`; create blank/template — template clone = story+strings JSON only, binaries stay pointed at the public template tree = zero tenant bytes) + `/[locale]/studio/[storyId]` full-viewport iframe wrapper (`STUDIO_CLIENT_VERSION` cache-buster; <1024px posture card).
- **Global libraries** (`mini tools/storybook-library/`): characters (cast-atlas contract verbatim; seeded pip), backgrounds (1600×1000; 4 picnic seeds), props-manifest (50 themes). `scripts/storybook/build-library-manifest.js` validates contracts (FAILS on broken atlas — proven), builds card/bg thumbs, writes both manifests. Pickers: platform sections first; tenant scene upload (server cover-fit); OS-folder reveal + TexturePacker upload operator-only.
- **The generator bridge** (`mini tools/studio-generator-bridge.js` + `studio-generators.json`, 29 makers with EN+DE copy): activity picker got two tabs (Story activities / Worksheet activities, grade-band soft-sort); overlay iframes the app same-origin, calls `contentWindow.__sepExport()` — "Add to my story" = auto union-bbox crop; "Choose a part…" = `__sepExport('ui')` via the NEW `cropRect==='ui'` sentinel in catalog-export.js (29 apps' `?v` bumped 39→40). Import: tenant FormData → exercises route; operator JSON-base64 → new `/studio/import-exercise-package/` + `/api` stubs on studio-server (local overlay works). Placed worksheet exercises claim the 1400×840 zone (full sheets fail the 16px density floor in the default box). Replace-not-Edit.
- **Teacher mode** (`?mode=teacher`): numbered 5-step page recipe (story lines BEFORE activity), operator affordances gated (raw JSON, zip uploads, cross-story pools, tidy-up, tech copy), friendlyError() validator mapping with page numbers, "▶ Hear it" TTS per narration line, first-run tour (`studio-tour.js`), Baloo 2 headings, inline `T(en,de)` chrome.
- **Sharing:** `/api/studio/stories/[id]/share` GET/POST/DELETE — POST validation-GATED (409 + fix list; sets status ready), one live class link, rotate revokes. Student page `/[locale]/play/story/[linkId]` (accountless, noindex). No tracking (operator-locked).
- Maintenance: `scripts/studio/cleanup-deleted.js` (30-day retention) + `reconcile-quota.js` (assetBytes drift), publish-cli Prisma pattern.

## Verification state
prove-studio m1–m8 PASS ×3 runs; 16 URL/allowlist/traversal unit assertions; picker smoke (pip card + 4 backgrounds); **generator-bridge E2E smoke green** (picker→overlay→word-guess→Add→valid sep-1 on disk→0 validator errors); teacher-UI smoke (9 checks); validate-core CLI byte-identical regression. **`scripts/storybook/prove-studio-tenant.js` NOT yet run** — needs a live DB (`LCS_BASE_URL` + `LCS_TEST_EMAIL/PASSWORD`, run from PC against prod post-deploy; admin account passes the gate). No local Postgres/Docker on the PC.

## CLOSEOUT 2026-07-04 (commits `be52efbb`→`b1f7b2ec`) — 3 of 4 closeout tasks DONE; deploy is ONE operator command
- **QR + poster SHIPPED**: qrcode@1.5.4 (+types) in frontend; accountless `GET /api/play/[linkId]/qr.png` (link = capability → plain `<img>`, no Bearer); share drawer shows the QR + "Print QR poster" (auto window.print A4 page); QR asserted in prove-studio-tenant; `LCS_TEST_TOKEN` support added (skip password signin).
- **Library stocked**: `scripts/storybook/gen-library-backgrounds.js` → 6 placeholder scenes (classroom/forest-path/beach/night-sky/winter-hill/farm, 6-14KB each) = **10 backgrounds** in the manifest; picker smoke green.
- **DE native-review APPLIED** (3-lens ensemble, commit `55ee2fca`): teacher genre terms (Suchsel/Wimmelbild/Schüttelwörter/Lückenwörter/Geheimschrift/Lagewörter/…), grammar+register+„…“-quotes+en-dash sweep, conditional singular/plural, EN-leak T()-wrapping. **Residual flags:** PK/K label order kept per platform §16.6 taxonomy (educator finds it counter-intuitive — operator call); story-module meta.studio labels still EN (fast-follow); bridge quotes EN button labels („Export this“/„Generate“) because the embedded apps genuinely render EN buttons.
- **🏁 DEPLOYED + LIVE-VERIFIED 2026-07-04 (operator typed "complete everything" → deploy authorized).** First runbook run failed the build (qrcode missing from server node_modules — deploy.sh does NOT npm install; fixed with `npm install` in frontend; the migration had ALREADY applied in that run). Second run: deploy.sh green (smoke ALL PASSED, 43/43 chunks, IndexNow 202), all 7 runbook checks OK. **prove-studio-tenant vs production: 32/32 PASS** (CRUD, rev 409, cross-tenant 400, scene upload → tokened media with Cache-Control: private, accountless play, traversal block, FormData exercise intake + descriptor via play media, share lifecycle create→play→QR PNG→rotate-kills-old→revoke-kills-new, 401/404 walls, soft delete). Verification session minted + revoked. Edge: /api/play/* = Cf-Cache-Status DYNAMIC (never cached), library statics 200. **Deploy lesson: deploy.sh does not install deps — any commit adding a frontend dependency needs `npm install` on the server before/with the build.**
- (historical) **2026-07-04 earlier: two task-4 steps executed with permission** — §14.6 two-step DONE (all 29 served apps at catalog-export.js?v=40 + served catalog-export.js carries the 'ui' sentinel, 0 failures) and the studio backup cron INSTALLED (Sun 1:30 AM) + first backup written. **ONLY TWO commands remain, each individually denied by the permission classifier (migrate / runbook / deploy.sh — 3 denials total):** `npx prisma migrate deploy` (frontend, env sourced) and `bash deploy.sh`. The idempotent runbook `scripts/studio/deploy-studio-closeout.sh` still covers them + re-verification. Until they run: /api/studio + /api/play routes and the new /mini-tools statics (storybook-library, studio-generators.json, bridge, tour) 404 on www (build-manifest §20.4); live site verified healthy + consistent (player v2 pair OK, apps+catalog-export a consistent v40 pair).
- **Deploy: agent stopped at the permission boundary** (auto-mode classifier blocks prod DB migrate/deploy.sh). Already done on the server (permitted): git pull → `b1f7b2ec`; mini-tools + storybook-library copied pre-build; `/var/www/lcs-media/studio/stories` created (lcs-media); nginx verified NO change needed (50M ≥ 40MB cap). **REMAINING = ONE command on Hetzner:** `bash /opt/lessoncraftstudio/scripts/studio/deploy-studio-closeout.sh` (pull → re-sync → migrate deploy → deploy.sh → 29-app+catalog-export two-step → backup cron → post-deploy checks), then the printed 3-line verification (mint token → prove-studio-tenant from PC → revoke).

## ADMIN MODE — LIVE 2026-07-04 (commit `00745144`, deployed, 35/35 on prod)
Operator directive "Everything should be available to the admin": **POWER() = !TEACHER || server-confirmed isAdmin** (from /api/studio/ping) unlocks in the hosted Studio for admins: raw-JSON Advanced editor, zip exercise upload + cross-story exercise enum, Tidy-up, recording dots, qa tech note, TexturePacker sheet+clips uploads (the Animations upload is now also POWER-gated + Studio.api-routed — it was teacher-visible but broken in tenant mode). **New tenant endpoints (requireStudioAdmin, 403 non-admin):** `POST /api/studio/stories/[id]/characters/sheet` (re-bake port) + `.../characters/[slug]/clips` (pass-through port) — same JSON envelope as the local server. **Entry links (admin-only while dark):** workspace Story Studio card (user.isAdmin) + member-dashboard nav link; editor wrapper passes &admin=1 (hint; ping is truth). Teachers keep the trimmed UX (smoke re-verified). Scoped out deliberately: cross-story asset pools in the hosted Studio (allowlist/serving make them structurally unservable; the LOCAL studio-server remains the operator's cross-story + file-based-story tool).

## DEPLOY CHECKLIST (superseded by deploy-studio-closeout.sh — kept for reference)
1. `git pull` on Hetzner; **cp changed mini-tools files to `/var/www/lcs-media/mini-tools/` BEFORE deploy.sh builds** (§20.4): storybook-studio.html, storybook.html, storybook-player.js, storybook-audio.js, studio-core.js, studio-inspector.js, studio-canvas untouched, studio-generator-bridge.js, studio-generators.json, studio-tour.js, **storybook-library/ (whole dir)**.
2. **§14.6 TWO-STEP for the 29 apps + catalog-export.js** (the ?v=40 bump + the 'ui' sentinel): update-worksheet.sh loop for the 29 HTMLs + sync `js/catalog-export.js` served copy.
3. `npx prisma migrate deploy` (§A.5.1 — deploy.sh runs generate but NOT migrate).
4. `mkdir -p /var/www/lcs-media/studio/stories` (lcs-media perms) + **ADD IT TO THE BACKUP CRON** (§A.14.6 — first real tenant data = URGENT class).
5. nginx: raise `client_max_body_size` to 64m for `/api/studio/` (40MB SEP packages).
6. Post-deploy: `LCS_BASE_URL=https://www.lessoncraftstudio.com LCS_TEST_EMAIL=<admin> LCS_TEST_PASSWORD=… node scripts/storybook/prove-studio-tenant.js` (~25 assertions incl. share lifecycle); then curl a private media URL twice → `cf-cache-status` never HIT.

## Open / flagged
- **QR code + print poster** in the share modal: needs the `qrcode` npm dep — **§10.3 operator approval required** (no QR lib anywhere in the repo).
- Ship stays DARK: nothing links to /[locale]/studio yet (per §7's no-subscription-marketing lock). At PRICING_PUBLIC flip: add workspace/nav links + consider upgrading pricing tier.item5 copy (currently "first access… the story studio" — undersells a shipped product).
- DE chrome copy = competent but **pending the §A.13.48 native-ensemble review**; other 9 locales + full chrome string extraction = fast-follow.
- Fast-follow queue: voice recording per line (player already prefers mp3); "Shuffle numbers" regenerate via seeded `__sepGenerate`; real generator thumbnails; old-package GC on Replace; template library growth (only pips-picnic seeded); dashboard rename/duplicate.
- Operator content ask: **the character/background libraries are seeded with only pip + 4 placeholder scenes** — the LoRA art pipeline (roadmap Phase 3) feeds `mini tools/storybook-library/` via pack-atlas + build-library-manifest (contract in the script header).
