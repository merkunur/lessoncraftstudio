# G1-353 `question-words` : PEDAGOGY + CONTENT (nt10-D, 2026-09-21)

Read: `_ROLE-PEDAGOGY.md`, `_STUDIO-BRIEF.md`, `_SUBSTRATE.md`, `_PANEL-FINDINGS.md` §9 + the lock (line 9: "single-answer by construction when the ANSWER sentence is printed and its answer constituent decides the question word"), `G1-350-cloze.md` §1-2 (objForms idiom, the 722 stack, the de accusative trap), `_work/G1-352-pedagogy.md` (the 32 OPENED portraits + 12 tagged names per locale, shared here), `types/g2/G2-274-fix-the-sentence.js` (`build()` + `verify()`: the corrupt-and-rewrite idiom; its `qUncued` comment on Romance yes/no questions) + `G2-282` (the `needQ` face), `types/g1/G1-308-read-and-do.js` (imperatives; `mode:'truth'` = G1-341), `types/g1/G1-341`, `types/g1/G1-249-unscramble-sentence.js`, `types/g2/G2-254-reading-comprehension.js`, `types/k/K-288-articles.js` (`articleChips` + the `sortWords` bin idiom), `data/b2/sentences.js` (8 names + the 3-7 question frames per locale that G2-274 d3 corrupts), `data/b3/instructions.js` header + `bank('instructions', loc).objForms` (the reviewed noun forms), `lib/b3-instructions.js fillSlots`, `lib/b3-picture-index.js`, `lib/number-words.js` (`numberWord(n, loc)` 0-100 x11), `primitives/clock.js` (`{h, m, size}`; numerals at `size * 0.115`), `qa/lints.js` (font floor 9 px applies to SVG `text`), `templates/components-b2.js` (`articleChips`, `wordBank`, `rulingBlock`, `countBadge`), `types/_shared/science-pair-match.js` (`.ws-match` two columns), `scripts/seo-landing/gen-b2var-landings.js LEVEL_KEYS`, `frontend/lib/seo/strand-names.ts` (`Language` row x11), `frontend/config/topics-taxonomy.json`. Every number below was measured 2026-09-21 with read-only node (scratch `G1-353-places.js`; contact sheets `G1-353-places-1.png` (25 place candidates) + `G1-353-people-things.png` (10 portraits + 10 things), every picture OPENED by me). *est.* = the engineer measures in the real render with the shell fonts. No em-dashes.

**Boundary (load-bearing).** This family owns exactly one thing nobody has: **the QUESTION WORD as the answer, decided by a printed ANSWER sentence whose marked constituent (a person name / a thing / a place phrase / a clock time / a number) is the one thing the question asks for** (base: circle it; F2: write it into the question's gap; F1: match a whole question to the short answer of its kind; F3: sort short answers under Who / What / Where; F4: write the whole question; F5: open question-writing with the six question words as starters, incl. why / how). It is NOT G2-274 / G2-281 / G2-282 / G2-307 / G2-311 `capitals-punctuation` (the child restores a capital and an END MARK; G2-282 "choose the end mark" asks whether a corrupted sentence needs "?" : here every question is printed WITH its "?" and no sentence is ever corrupted; the sv "frågetecken" / pt-fr "frases interrogativas / phrases interrogatives" heads stay theirs and are refused as faces), NOT G1-308 + G1-338..342 `read-and-do` (imperatives the child ACTS on; G1-341 true/false judges a statement against a strip; here nothing is acted on and nothing is judged true or false), NOT G2-254 `reading-comprehension` (a passage + 3 recall questions; here there is no passage and the question WORD is the object of study), NOT G1-249 / G1-282 / G1-283 / G1-302 `sentence-building` (word order over `SENTENCES[loc].frames`; the 3-7 question frames of that bank (en s3/s7/s11/s16, de s3/s7/s11/s15, fi s12/s13/s14 ...) are FORBIDDEN as literals here, validator rule 5), NOT G1-352 `pronouns` (the answer is a pronoun standing for a name; here a name is never replaced, only asked for; the 32 portraits + 12 tagged names are SHARED data, not a shared task), NOT G1-350 `cloze` (a picture-cued NOUN gap; here the gap is always the question word and the noun is printed), NOT `word-classes` G2-275/285/286/287 + G1-293/300 (chips sorted by CLASS; F3 sorts short answers by the QUESTION they answer, and a place phrase is not a word class), NOT G1-335 (adjective gap), NOT G2-278 `picture-writing` (write sentences about a picture; F5 writes QUESTIONS with printed question-word starters, the only open face), NOT K-288 (article chips: the idiom is reused, the answer is never an article). Structural fences the validator enforces: every answer chip / bank pill / bin head / starter is a member of the locale's fixed question-word table (`qwords`), never a noun, verb, name or article; every frame is `{name}` FIRST + a verb + exactly ONE complement slot of a declared kind (`thing` / `place` / `time` / `count`), so `who` is always the subject (no `wen / a quién / à qui` object-of-person forms, which are above band and would make two words fit) and the complement decides the second kind; the marked span is exactly the substituted literal of ONE slot; a question literal is authored per (frame, kind), starts with that kind's `qwords` literal and never contains the marked literal; short answers on F1 / F3 are the constituent literals themselves (a name, an `objForms` form, a place literal, a clock literal, a number word), never sentences; why / how appear ONLY on F5 (open, no verify). Visual signature: a numbered list of cream lanes, the constituent's picture at the left (portrait / thing / place / an 80 px analog clock), ONE sentence with a teal underline under one span, and a row of identical white chips under it (base) or a coral gap box at the head of a printed question (F2).

## A. Identity

| loc | genre head (the panels; title = head; ASCII slug) | school year per face (`LEVEL_KEYS`) | national strand (framework NAME; the `Language` row of `strand-names.ts` carries the literal, m) | CCSS (en only, honest) |
|---|---|---|---|---|
| en | **Question Words: Who, What, Where** (`question-words`; "wh questions" META only, it is the ESL head; 99, 14/14; HARD (iSLCollective / ESL Printables / Twinkl, "ESL and speech-therapy are half the market"); WINNABLE "wh questions worksheets for kindergarten pdf", "question words 1st grade") | base / F1 / F2 / F3 grade 1 (`grade-1`); F4 / F5 grade 2 (`grade-2`) | Language | **L.1.1.j** (produce and expand interrogative sentences) on base, F1, F2, F3 (the K code L.K.1.d "understand and use question words" is the underlying skill and is named in copy, not as the alignment: the pages are G1); **L.2.1.f** (produce complete simple sentences) on F4 / F5, F5 additionally names L.1.1.j in copy. No G2 question-word code exists. |
| de | **W-Fragen: wer, was, wo** (`w-fragen`; "Fragewörter" META; hyphen + capital W is the school spelling; 64, net ~50; grundschule-arbeitsblaetter / Grundschultante / eduki / DaF; WINNABLE "w-fragen grundschule arbeitsblatt"; "Bericht / Notruf" (Klasse 3-4 W-Fragen as a text-planning tool) is NOT this type and never appears in copy) | 2. Klasse (`2-klasse`) on every face; the reading faces' copy names "Klasse 1-2" (the measured "w fragen klasse 1" tail) | Sprache untersuchen (Lehrplan "Sprache und Sprachgebrauch untersuchen") | n/a |
| es (MX) | **Preguntas con quién, qué y dónde** (`preguntas-con-quien-que-y-donde`; "palabras interrogativas" META only (dead: 13, band 0, foreign 4); the one school-shaped string measured is "preguntas con qué quién dónde cómo"; C-tier traffic expected, said in the plan not on the page). **SEP teaching point** (the lock's demand): Lengua materna 1º-2º "Reflexión sobre la lengua": las palabras para preguntar (qué, quién, dónde, cuándo, cuántos) y los signos de interrogación, practiced in 2º inside "elaborar preguntas para una entrevista" (the panel confirms the textbook wording; no SEP text is in the repo) | base / F1 / F3 primer grado (`primer-grado`); F2 / F4 / F5 segundo grado (`segundo-grado`) | Reflexión sobre la lengua (SEP / NEM) | n/a |
| pt (BR) | **Quem, o que, onde: palavras para perguntar** (`palavras-para-perguntar`; "pronomes interrogativos" META only (C; 34, band 3; a 3º-7º + inglês term); "frases interrogativas" is `capitals-punctuation` territory and never a title here) | base / F1 / F3 2º ano (`2o-ano`); F2 / F4 / F5 3º ano (`3o-ano`) (BNCC has no K-2 question-word habilidade; the pt panel anchors the copy to "Análise linguística" and names the habilidade it judges honest, or none) | Análise linguística/semiótica (BNCC) | n/a |
| fr | **Les mots interrogatifs : qui, que, où** (`mots-interrogatifs`; B, "one B market of four"; 43, band 24; "exercices mots interrogatifs ce1 / ce2" measured; "phrases interrogatives cp / ce1 / ce2" x20 is SENTENCE TYPES (Bout de Gomme / Lutin Bazar / Maître Lucas) and is never a title here) | base / F1 / F3 CP (`cp`; copy names "CP-CE1"); F2 / F4 / F5 CE1 (`ce1`) | Étude de la langue (programmes officiels cycle 2) | n/a |
| it | **Le domande della frase: chi, che cosa, dove** (`le-domande-della-frase`; "parole interrogative" META only (dead: 21, band 0, foreign 15 "entirely ELT/FLE"); C-tier expected). **Indicazioni teaching point** (the lock's demand): Riflessione linguistica classe seconda, "le domande della frase" (chi? che cosa fa? dove? quando?) used to read and expand the frase minima, the standard classe seconda device (the panel confirms the register) | classe seconda (`classe-seconda`) on every face | Riflessione linguistica (Indicazioni nazionali) | n/a |
| nl | **Vraagwoorden: wie, wat, waar** (`vraagwoorden`; B; 27, net ~14; "a real (small) K-3 line"; F4 takes nl's measured groep-4 head **Vraagzinnen maken** verbatim) | groep 4 (`groep-4`) on every face | Taal (SLO kerndoelen) | n/a |
| sv | **Frågeord: vem, vad, var, när** (`frageord`; B-; 16 (10); WINNABLE "frågeord svenska övningar"; the four words are the measured head and are the four d2 chips verbatim) [NSR] | åk 2 (`ak-2`) on every face ("frågeord åk 2" measured; the copy names åk 1-2 on the reading faces) | Språkliga strukturer och normer (Lgr22) [NSR] | n/a |
| da | **Hv-ord: hvem, hvad, hvor** (`hv-ord`; hyphen = the school spelling; "spørgeord" META; B-; 18 (11); WINNABLE "hv-ord opgaver") [NSR] | 2. klasse (`2-klasse`) on every face | Sprog og sprogbrug (Fælles Mål) [NSR] | n/a |
| no | **Spørreord: hvem, hva, hvor** (`sporreord`; B, "the strongest of the four: 'spørreord 2 trinn' is a grade hit"; 16 (16); title is the one-word bokmål "Spørreord", never "hv ord" (leaks Danish)) [NSR] | base / F1 / F3 2. trinn (`2-trinn`, the measured grade); F2 / F4 / F5 3. trinn (`3-trinn`) | Språket som system og mulighet (LK20) [NSR] | n/a |
| fi | **Kysymyssanat: kuka, mikä, missä** (`kysymyssanat`; C+; 14 (4); S2-driven; "kuka mikä missä" is the measured head; the CHIPS carry the INFLECTED form each frame requires, see §B/§C) [NSR] | base / F1 / F3 1. luokka (`1-luokka`, "kysymyssanat 1. lk" measured); F2 / F4 / F5 2. luokka (`2-luokka`) | Kielen rakenteet ja oikeinkirjoitus (OPS 2014) [NSR] | n/a |

Measured: `apps.question-words` ABSENT, `axes['exercise-type'].question-words` ABSENT (register with the b4 shape, `default_subject:'letters'`, `default_age_range:'6-8'`); the 11 slugs above plus `fragewoerter / palabras-interrogativas / pronomes-interrogativos / parole-interrogative / spoergeord / wh-questions` grepped against every `axes.*.*.slug.<loc>`: **0 collisions** (m). Neighbour names the titles may never echo (m): `capitals-punctuation.name` ("Satzanfang und Satzzeichen", "Stor bokstav och skiljetecken", "Maiúscula e pontuação" ...), `reading-comprehension.name` ("Leseverständnis", "Interpretação de texto" ...), `sentence-building.name` ("Schüttelsätze", "Husselzinnen", "Meningsbyggnad" ...), `word-classes.name` ("Wortarten", "Ordklasser", "Sanaluokat" ...).

**Theme axis: THEMELESS** (`themeAxis:{applicable:false}`, landings `coordinate.theme:''`). RULED WITH NUMBERS: the only wave-fannable constituent is the THING, and things carry no query weight for this genre (nobody searches "W-Fragen Tiere"); a themed page would show the theme in only the `what` / `count` rows (2-3 of 7). Things come from the reviewed pool = nouns with an `objForms` entry AND a colour picture in one of the six fan themes G1-308 opened (animals · fruits · vehicles · toys · zoo animals · farm animals), m: en 133 · de 131 (m 53 / f 48 / n 30) · es 88 (m 58 / f 30) · pt 125 (m 80 / f 45) · fr 131 (m 85 / f 46; 5 multi-word plurals) · it 117 (m 68 / f 49; 3 multi-word) · nl 103 (d 91 / h 12) · sv 114 (n 111 / t 3) · da 119 · no 116 · fi 121 (`part` 121, `gen` 121); every locale has `unique` + `pl` for 100 % of its pool. A page uses at most 4 things, so the pool is never the floor. The picture for a thing is the FIRST `candidates(key, loc)` entry whose theme is one of the six (deterministic, the G1-308-opened file), never `pictureFor`'s random pick (the sv #35 lesson). Persons = the pronouns bank's 32 OPENED portraits (20 m / 12 f, `people[]`, locale-neutral) + its 12 tagged names per locale (6 f + 6 m; the 8 `SENTENCES[loc].names` + 4), read from `bank('pronouns', loc)` at load, never re-authored (§D). Places = this type's own 14 locale-neutral picture keys, ALL OPENED 2026-09-21 (`G1-353-places-1.png`): `miscellaneous/house` (a house) · `spring/garden` (flowers, a garden bed) · `summer/beach` (sand, sea, parasol, palm) · `camping/tent` (red tent) · `camping/forest` (trees) · `camping/lake` (lake with mountains) · `camping/river` (river valley) · `camping/mountain` (red peaks) · `summer/pool` (a pool) · `around the house/kitchen` (a kitchen) · `around the house/garage` (garage with a car) · `around the house/bed` (blue bed) · `around the house/sofa` (teal sofa) · `activities/playground` (a climbing frame with a slide: a PLACE despite the substrate's "activities are objects" note; opened). Also opened and REJECTED as places: `spring/nest` (two birds; a person is never in a nest), `winter/igloo` (fine art but "in the igloo" is not a K-2 place in 9 locales), `beach/lighthouse` and `beach/island` (not where a child IS), `camping/cabin` (a second house; kept as the panel's optional 15th key), `At the Supermarket/basket`, `classroom/desk`, `around the house/bathtub`, `around the house/fridge` (places for THINGS only; a `where` frame with a thing subject is rejected below, so these are out). Each locale authors the place phrase per key as a WHOLE literal or `null` (= refused for that locale). Times = 6 o'clock literals per locale `{text, h}` rendered by `primitives/clock.js` (`m:0`). No `unitAxis`.

**The one rule that locks the type.** Every item is a FRAME instance: a panel literal `text` = `{name}` FIRST + verb + exactly one complement slot whose name is a FORM SELECTOR (`{thing}` -> `objForms[key].unique` (fi: `{part}` -> `.part`), `{place}` -> `places[key].text`, `{time}` -> `times[i].text`, `{n} {pl}` -> `numberWord(n, loc)` + `objForms[key].pl` (fi: `{n} {part}`, partitive singular after a numeral)), a declared `kind` (`thing | place | time | count`), and a `q` object with exactly two question literals: `q.who` and `q.<kind>` (each starting with that kind's `qwords` literal, es after "¿", each containing every NON-asked slot and never the asked one). The row's `ask` ∈ `{who, kind}` is chosen by the composer; the ANSWER is `qwords.indexOf(ask)` (a chip index, base) / the `qwords[ask]` literal (F2) / the filled `q[ask]` (F4) / the constituent literal itself (F1 / F3, answered by kind). The marked span = the substituted literal of the asked slot (for `count`, the number word ONLY: marking "three apples" would admit `what`). The code substitutes and underlines; it never inflects, capitalises, strips or shortens a literal (a fi name is nominative because `{name}` is always the subject; a de thing is accusative because `unique` is the accusative phrase and the de panel signs every thing-frame verb as accusative-governing).

## B. The six faces

Common: `templates/components-b4/question-words.js` (`qwRow`, `markSpan`, `qwChips` = `articleChips` with `w:96, h:44, fontPx:20` (K-288 uses 66/48/24 for 2-3-char articles; 4 question words of <= 7 chars fit 96 at 20 px *est.* 77 px for "Wanneer / Hvornår / Milloin"), `qaMatch` (the `.ws-match` two columns), `gapQuestion` (`.ws-blankbox` idiom, 96 x 40, at the head of the question), `answerBins` (K-288 `sortWords` clone), `questionLane` (sentence over `rulingBlock`), `askScene` + `starterLines`). Every face is a CODE face on ONE additive `layout` knob (`d.layout` undefined = the base path, byte-identical; a face stamps `data-lcs-layout` only when declared; the G1-352 / K-288 shape) with its own `verify()` branch; guards key on the CONFIG (`d.chips`, `d.kinds`, `d.pairs`, `d.bank`, `d.bins`, `d.ruling`, `d.starters`), never on the level index. Resolved d2 configs are pairwise distinct (`tools/gate-variation-distinct.js --batch=b4`). Chip order per locale is FIXED (`qwords` order who · what · where · when · howmany) and identical on every row (K-288 verify "chip order differs (position leak)"). The picture column is **80 px** on every G1 face that carries a `when` row, because `clock.js` numerals are `size * 0.115` and `qa/lints.js:86` hard-fails any `text` under 9 px: **an analog clock is legal only at size >= 79** (m: 80 -> 9.2 px); portraits render 72-80 in that column, things 64-72, places 72-80. Density G1 items [6, 12], G2 [8, 16]; G1 min element 44, G2 36.

**Question-word tables (`qwords`, the FIXED printed order; `starters` = the F5 six; `bins` = the F3 heads with "?"):**

| loc | who · what · where · when · howmany (chips / bank / F4 heads) | F5 starters (+ why, how) | notes |
|---|---|---|---|
| en | Who · What · Where · When · How many | Who What Where When Why How | US spelling; "Which" never |
| de | Wer · Was · Wo · Wann · Wie viele | Wer Was Wo Wann Warum Wie | "Wen / Wem / Wessen" never (persons are subjects only); "Wohin / Woher" never at G1 (static `wo` only) |
| es | Quién · Qué · Dónde · Cuándo · Cuántos | Quién Qué Dónde Cuándo Por qué Cómo | accents on every chip; every question literal starts "¿" + chip; **count frames use MASCULINE-plural nouns only** (58 of 88; "cuántas" never printed) |
| pt | Quem · O que · Onde · Quando · Quantos | Quem O que Onde Quando Por que Como | count frames masculine only (80 of 125); the pt panel fixes ONE article-before-name policy ("a Sofia tem" vs "Sofia tem") for the whole type |
| fr | Qui · Que · Où · Quand · Combien | Qui Que Où Quand Pourquoi Comment | "Que" + stylistic inversion ("Que tient Léa ?"); "Qu'est-ce que" is 13 chars and does not fit a 96 chip: the fr panel either keeps "Que" or refuses the inversion register and re-titles (a data decision); NBSP before "?" inside every literal; "Combien de {pl}" is genderless |
| it | Chi · Che cosa · Dove · Quando · Quanti | Chi Che cosa Dove Quando Perché Come | count frames masculine only (68 of 117); **"Dove" + "è" elides to "Dov'è"**: the it panel authors `where` questions with a non-è verb ("Dove gioca Sofia?", "Dove dorme Sofia?") or accepts the unelided "Dove è" on F2 (one policy for the type) |
| nl | Wie · Wat · Waar · Wanneer · Hoeveel | Wie Wat Waar Wanneer Waarom Hoe | nl "Wie" = who (de "wie" = how): the two locales never share a page, no leak |
| sv | Vem · Vad · Var · När · Hur många | Vem Vad Var När Varför Hur | "Var" (static), "Vart" never (the live usage dispute; the panel writes "var" as recorded); never «grupp» in copy [NSR] |
| da | Hvem · Hvad · Hvor · Hvornår · Hvor mange | Hvem Hvad Hvor Hvornår Hvorfor Hvordan | "Hvor mange" is ONE literal (the gap on F2 removes both words); "Hv-ord" title, "spørgeord" META [NSR] |
| no | Hvem · Hva · Hvor · Når · Hvor mange | Hvem Hva Hvor Når Hvorfor Hvordan | bokmål only; "Hvor mange" one literal [NSR] |
| fi | Kuka · Mitä · Missä · Milloin · Kuinka monta | Kuka Mitä Missä Milloin Miksi Miten | **INFLECTED chips**: `thing` frames take a PARTITIVE object (`{part}`, 121 of 121 available; verbs syö / juo / lukee / piirtää / katsoo / etsii) so the what-word is "Mitä", never bare "Mikä"; `place` frames are STATIC (inessive / adessive literals "puutarhassa" / "rannalla" / "pöydällä", question "Missä"; "Mihin / Mistä" = direction, refused at 1. lk); `time` "kello kahdeksan" -> "Milloin"; count "{n} {part}" -> "Kuinka monta omenaa Aino näkee?" (the noun INSIDE the question, partitive singular). F3 bins take the NOMINATIVE "Mikä?" (cards are bare nouns) : `bins` is authored separately from `qwords`. Names stay nominative on every face because `{name}` is always the subject; if the fi panel prefers "Ainolla on ..." possession, it authors `nameForms.ade` for all 12 names (12 literals) and the count frame names `{name:ade}`; recommended: transitive verbs, no case forms [NSR] |

### Base: Question Words: Who, What, Where (G1, `G1-353`, layout undefined)
**Move:** READ a short answer sentence, LOOK at the underlined part (and its picture), and CIRCLE the question word that asks for exactly that part: a name -> who, a thing -> what, a place -> where, a clock time -> when (the K.1 / Klasse 1-2 "W-Fragen zuordnen" act; the picture is the constituent's own picture, so a pre-reader can still map person / thing / place / clock, and the underline is what a reader uses).
**Child:** "Read the sentence. Look at the underlined part. Circle the question word that asks for it." (89; apparatus: sentence, underlined part, question words.)
**Params:** d1 `{rows:6, chips:['who','what','where'], kinds:{who:2,what:2,where:2}, pic:80, chipW:96, chipH:44, chipFont:20, fontPx:18}` · **d2 (ships)** `{rows:7, chips:['who','what','where','when'], kinds:{who:2,what:2,where:2,when:1}, pic:80, chipW:96, chipH:44, chipFont:20, fontPx:18, maxAnswerChars:40}` · d3 `{rows:7, chips:['who','what','where','when','howmany'], kinds:{who:2,what:2,where:1,when:1,howmany:1}, chipW:88, chipFont:16, chipRows:2}` (unpublished; "Kuinka monta / Hvor mange / Hur många / Wie viele" at 16 px *est.* 96-106 px exceed one 5-chip row inside 507 px, so d3 wraps chips 3 + 2; UNKNOWN - engineer measures). Row *est.*: `.ws-lane` with `padding:4px 16px` -> grid `30px 80px 1fr` gaps 10 / 12 = 639; right cell = sentence 18 / 1.3 one line (24) + gap 6 + chips 44 = 74 < the 80 px picture -> row 88 at `minmax(88px,1fr)`; 7 x 88 + 6 x 6 = 652 <= 677 <= 722 (fits even the fi four-line stack). One-line sentences are a data rule (`maxAnswerChars:40`; 507 px / 9.2 px per char (de / fi / pt at Nunito 800 18, m in G1-350) = 55 chars). Composer: pick 7 frames (distinct `id`, >= 2 kinds among thing / place / time), draw `kinds` counts, assign `ask` per row, fill slots with 7 distinct names (7 <= 12), <= 3 distinct things (no key twice), distinct places, distinct times; every chip appears >= `kinds[chip]` times (K-288 `okMix`); shuffle rows; portraits matched to the name's `gender` tag via `depicted` (no `src` twice). Pictures: who -> the row name's portrait 80; what -> the thing 72; where -> the place 80; when -> `clock({h, m:0, size:80})`; (d3 howmany -> the thing 64 + `countBadge(n)` at its corner).
**verify():** root `[data-lcs-qw][data-ws-content]` (no `data-lcs-layout`); per row `data-lcs-item data-lcs-frame=<id> data-lcs-kind=<thing|place|time|count> data-lcs-ask=<who|...> data-lcs-slots=<json of the filled literals> data-lcs-chip-key=<i>`; the sentence `<p data-lcs-sentence>` with exactly one `<span data-lcs-mark>`; chips via `articleChips` (`data-lcs-chip`, `data-lcs-label`, `data-lcs-correct`). Re-derived on the render: `rows` items; exactly one `data-lcs-correct` chip per row and its index === `data-lcs-chip-key` === `chips.indexOf(ask)`; chip labels identical in order on every row and styled identically; the mark text === the stamped literal of the asked slot (node: refill the frame from the bank through `fillSlots`, compare the whole sentence AND the span; for `count` the span === `numberWord(n)` only); the sentence contains no `qwords` literal as a token (poison: "Mia weiß, wer kommt." -> FAIL); the row picture matches the ask (who: `src` ∈ `people[]` of the name's gender; what / count: `src` === the thing's pinned file; where: `src` === `places[key].pic`; when: `[data-lcs-prim="clock"]` with `data-lcs-h` === the time's `h` and `data-lcs-m="0"`, size >= 79); sentence height <= 26 (one line); every chip key appears >= `kinds[chip]`; names distinct; no portrait `src` twice; nothing pre-marked; chips >= 44 high.
**Refusals:** none as a face. Item-level: fi `what` rows are partitive only; es / pt / it `count` rows (d3) masculine nouns only.
**Query face:** the bare head. en "question words worksheet who what where" · de "W-Fragen Arbeitsblatt Klasse 2" · es "preguntas con quién qué dónde primer grado" · pt "palavras para perguntar 2º ano" · fr "mots interrogatifs CP CE1 exercices" · it "le domande della frase classe seconda" · nl "vraagwoorden groep 4" · sv "frågeord vem vad var när" (measured) · da "hv-ord opgaver" (measured) · no "spørreord 2 trinn" (measured) · fi "kysymyssanat kuka mikä missä" (measured).

### F1: Match the Question to the Answer (G1, `G1-3xx TBD`, CODE `layout:'match'`)
**Move:** MATCH by KIND: five whole questions on the left ("Wer hat den Ball?", "Was hat Ben?", "Wo ist Leo?", "Wann spielt Anna?", "Wie viele Äpfel hat Tom?") and five SHORT answers on the right, deranged, each with its picture ("Mia" + portrait, "den Ball" + ball, "im Garten" + garden, "um acht Uhr" + clock, "drei" + three apples' badge); a name answers only Wer, a place phrase only Wo, a number only Wie viele - the question WORD is the only key, because the short answers carry no other content (the panels' measured head "frågeord och svar" / "spørgsmål og svar" / "kysymys ja vastaus"). One question per kind per page, so uniqueness is by construction (two `who` questions would both take a bare name).
**Child:** "Read each question on the left. Draw a line to the answer on the right that fits it." (82; apparatus: questions, answers, line.)
**Params:** d1 `{layout:'match', pairs:3, kinds:['who','what','where'], pic:80, itemH:100, fontPx:19}` · **d2** `{layout:'match', pairs:5, kinds:['who','what','where','when','howmany'], pic:80, itemH:96, fontPx:18, qColW:330, aColW:250}` (10 `.ws-match-item` elements in the G1 window; every kind exactly once) · d3 `{layout:'match', pairs:5, blocks:2}` (two heroes, two `.ws-match` blocks of 5 stacked; unpublished, 10 pairs, needs itemH 60 and a 56 px column with NO `when` clock: engineer). Stack *est.*: `.ws-match` padding 6 40; 5 x 96 + 4 x 12 = 528 <= 677 (slack opens the item gap, `justify-content:space-evenly`). The five questions come from FIVE distinct frames (distinct names, things, places, times) so that no answer literal repeats on the page and no question contains another row's constituent; the who-row's frame is a `thing` or `place` frame (its `q.who` contains that constituent as a pin, which is harmless: the answer column has ONE name). Left item = the question text (Nunito 800 18, <= 2 lines in 330 px) + `.ws-match-dot--right`; right item = picture 72-80 + the short-answer literal (Baloo 2 700 20) + `.ws-match-dot--left`. Derangement: the right order is a permutation with no fixed point (science-pair-match idiom).
**verify():** stamps per left item `data-lcs-q data-lcs-ask=<kind> data-lcs-frame`; per right item `data-lcs-a data-lcs-kind=<kind> data-lcs-literal`; re-derived: `pairs` left = `pairs` right; the multiset of right kinds === the multiset of left asks and each kind appears ONCE (poison: two `who` questions -> FAIL); every question starts with `qwords[ask]` (es after "¿"; case-sensitive, the chip literal is sentence-initial); no question contains its own answer literal (word boundary, NFD; poison: "Wo ist Leo im Garten?" -> FAIL) and no question contains another row's answer literal; the right column is a derangement of the left; each right picture matches its kind as on the base; no line pre-drawn; dots present on every item; text >= 16; pictures >= 44.
**Refusals:** none (every locale's table has 5 kinds).
**Query face:** en "match the question to the answer worksheet who what where" · de "Fragen und Antworten zuordnen W-Fragen" · es "une la pregunta con la respuesta" · pt "ligue a pergunta à resposta" · fr "relie la question à la réponse" · it "collega la domanda alla risposta" · nl "vraag en antwoord verbinden" · sv **"frågeord och svar"** (measured) · da **"spørgsmål og svar"** (measured) · no "spørsmål og svar" · fi **"kysymys ja vastaus"** (measured).

### F2: Write the Question Word (G1, `G1-3xx TBD`, CODE `layout:'gap'`)
**Move:** WRITE the question word into a gap at the HEAD of a printed question, reading the printed answer beside it (its constituent underlined, its picture at the left) and choosing from a five-word bank across the top ("___ hat den Ball?" next to "Mia hat den Ball."): recognition becomes production of the word, and the fifth word (how many) enters the page as a bank pill (auto-width) where a chip could not hold it (the de "Fragewörter einsetzen" genre).
**Child:** "Read the answer. Find the underlined part. Write the question word from the bank in the box to finish the question." (108; apparatus: answer, underlined part, bank, box, question.)
**Params:** d1 `{layout:'gap', rows:6, bank:true, kinds:{who:2,what:2,where:2}, pic:80, gapW:110, fontPx:18}` · **d2** `{layout:'gap', rows:7, bank:true, kinds:{who:2,what:2,where:1,when:1,howmany:1}, pic:80, gapW:110, gapH:40, fontPx:18, laneMin:88}` · d3 `{layout:'gap', rows:7, bank:false, kinds:{...}}` (no bank, unpublished). Stack *est.*: `wordBank({words: qwords, wordPx:18})` one row 59 + 10 (5 pills of "Wer / Was / Wo / Wann / Wie viele" *est.* 60-140 px, sum <= 651 in every locale: fi "Kuka Mitä Missä Milloin Kuinka monta" *est.* 62 + 58 + 70 + 84 + 148 + 4 x 10 = 462); rows 7 x 88 + 6 x 6 = 652; total 721 <= 722 but > 677 -> **the fi title of this face must stay <= 3 lines** (the G1-350 §5 rule 13 precedent) or `rows:6` on fi (the engineer measures; the fallback is a config, `rows`). Row: grid `30px 80px 1fr`; right cell = answer sentence 18 (24) + gap 6 + question line: `.ws-blankbox` 110 x 40 inline-block at the head + the question rest at 18 px (40) = 70 < 80 -> row 88. `gapW` is ONE width for the page (a per-row width would leak the word length: "Wo" vs "Wie viele"); 110 holds "Kuinka monta" hand-written at ~1.6 x the 18 px print width? NO: 12 glyphs x 16 = 192 -> `gapW = clamp(round(1.6 * 10 * maxGlyphsOnPage + 24), 110, 220)` = 216 for fi / 184 for no-da / 168 for sv-de (the G1-350 formula); the question rest then wraps to a second line on fi (question line 2 x 24 + 40 -> right cell 94 -> row 102 x 7 = 714 + 36 = 750 > 722): **fi F2 needs `rows:6`** (6 x 102 + 30 + 69 = 711 <= 722; 677 only with a 3-line title) - a measured fallback the engineer confirms, recorded as fi `rows:6`. The bank is the WHOLE `qwords` table (never sorted by answer frequency; deranged order per seed). es questions print "¿" BEFORE the gap box: the literal is split at `qPrefix` (data `qPrefix:'¿'`, empty elsewhere).
**verify():** stamps per lane `data-lcs-frame data-lcs-ask data-lcs-answer=<qwords literal>`; per bank pill `data-lcs-bank-word`. Re-derived: `rows` lanes; the answer sentence carries exactly one mark whose text === the asked slot literal (node); the question line starts with an EMPTY `.ws-blankbox` (es: after "¿") and its remaining text === `q[ask]` filled minus its leading `qwords[ask]` literal (string equality after the prefix strip, node; poison: a question printed whole "Wer hat den Ball?" -> FAIL "answer printed"); the answer literal occurs nowhere in the lane (word boundary, case-insensitive); the bank set === `qwords` (5 pills, one row: <= 1 distinct pill `top`); every kind appears >= `kinds[kind]`; all `[data-lcs-gapbox]` widths equal (+-1) and >= 110; pictures as on the base; nothing pre-written.
**Refusals:** none as a face. fi ships `rows:6` (a config, not a refusal).
**Query face:** en "fill in the question word worksheet" · de **"Fragewörter einsetzen Arbeitsblatt"** · es "completa la pregunta con qué quién dónde" · pt "complete a pergunta" · fr "complète avec le bon mot interrogatif" · it "completa la domanda con chi che cosa dove" · nl "vul het vraagwoord in" · sv "skriv rätt frågeord" · da "indsæt hv-ordet" · no "sett inn spørreordet" · fi "täydennä kysymyssana".

### F3: Sort the Answers: Who, What or Where (G1, `G1-3xx TBD`, CODE `layout:'sort'`)
**Move:** CLASSIFY a whole set at once: nine picture cards on a shelf (3 names with portraits, 3 bare things with pictures, 3 place phrases with pictures) and three labelled bins "Wer?" "Was?" "Wo?" with ruled lines; the child writes every card's word(s) under the question it answers (the K-288 `sortWords` act, the fr / sv "étiquettes / sortera" idiom applied to question words; no sentence on the page, the CONSTITUENT alone decides).
**Child:** "Read each card. Write its words under the question it answers: Who, What or Where." (82; apparatus: cards, words, questions.)
**Params:** d1 `{layout:'sort', cards:6, bins:['who','what','where'], perBin:2, pic:64, lineCount:3}` · **d2** `{layout:'sort', cards:9, bins:['who','what','where'], perBin:3, pic:56, cardW:118, binH:330, lineMin:4}` · d3 `{layout:'sort', cards:12, bins:['who','what','where','when'], perBin:3, pic:80}` (unpublished; the `when` cards need the 80 px clock -> shelf rows 108 high: engineer). Stack *est.*: shelf `.ws-card` 660 wide, 2 rows of 5 / 4 cards 118 x 84 (picture 56 + caption Nunito 800 16) = 84 x 2 + 10 + 24 = 202; gap 18; bins `.ws-bin` `floor(640/3) - 12` = 201 wide x 330, 26 px pill heads (K-288: `gapY = min(58, floor(330/(lineCount+0.5)))`, `lineCount = max(lineMin, hardest bin's card count)`); 202 + 18 + 26 + 330 = 576 <= 677 (`binH` may open to 400). Cards: names = 3 tagged names with a matching portrait; things = the BARE vocab singular through `displayWord` (de keeps the capital "Ball"; sv indefinite "boll"; fi nominative "pallo", hence the fi bin "Mikä?") with the pinned picture; places = `places[key].text` literals ("im Garten", "on the beach", "puutarhassa") with the place picture. The de panel may instead put the `objForms.unique` phrase on the thing cards ("den Ball", the short-answer register of F1); ONE policy per locale, declared as `sortThingForm:'bare'|'unique'`.
**verify():** the K-288 sort contract cloned: `[data-lcs-layout="sort"]`, `data-lcs-sortword` + `data-lcs-key=<bin index>` per card, `data-lcs-sortbin` / `data-lcs-sorthead` per bin; re-derived: >= 6 cards, 3 bins, heads === `bins` literals (with "?"), no card text repeats a head or a `qwords` literal, keys in range, every bin's ruled lines >= its card count (the stranded-word rule), each bin receives exactly `perBin` cards, every card has a picture >= 44 (portrait for a name card, the pinned thing / place file otherwise), no name / thing / place twice, no card pre-placed; node: a name card's key === who, a thing card's === what (its text === `displayWord(vocab singular)` or `unique` per the declared policy), a place card's === where (its text === `places[key].text`).
**Refusals:** none as a face.
**Query face:** en "sort by question word who what where cut and paste" · de "wer was wo zuordnen Arbeitsblatt" · es "quién qué o dónde clasifica" · pt "quem o que ou onde separe" · fr "qui que ou où trier" · it "chi che cosa o dove ordina" · nl "wie wat of waar sorteren" · sv "vem vad eller var sortera" · da "hvem hvad eller hvor sortér" · no "hvem hva eller hvor sorter" · fi "kuka mikä vai missä lajittele".

### F4: Write the Question (G2, `G2-3xx TBD`, CODE `layout:'write'`)
**Move:** PRODUCE the whole question on a school line: the answer sentence is printed with its constituent underlined (the subject's portrait at the left, 48 px), and the child writes the question that asks for the underlined part; no bank, no gap, the "?" is the child's (the measured "ställ en fråga / stil et spørgsmål / lag et spørsmål / tee kysymys / Vraagzinnen maken" heads; retrieval + handwriting + the interrogative word order, which is what makes it G2).
**Child:** "Read the answer. Look at the underlined part. Write the question that asks for it on the line." (92; apparatus: answer, underlined part, line.)
**Params:** d1 `{layout:'write', rows:6, ruling:true, glyphH:28, rowH:56, pic:56, kinds:{who:2,what:2,where:2}, maxChars:26}` · **d2** `{layout:'write', rows:8, ruling:true, bank:false, glyphH:24, rowH:46, pic:48, fontPx:18, kinds:{who:2,what:2,where:2,when:1,howmany:1}, maxChars:31}` (8 items in [8, 16]) · d3 `{layout:'write', rows:8, twoAsks:true}` (each row's answer carries TWO marks and two lines; unpublished). Row *est.*: grid `48px 1fr` gap 12 -> 579 px cell: printed sentence 18 (24) + `writingRow` w 579 h 46 glyphH 24 (>= 24 at G2) + gap 6 = 76; 8 x 76 + 7 x 8 = 664 <= 677. The picture on THIS face is always the subject's PORTRAIT (48 px; a clock would need 80 and break the 8-row stack; at G2 the underline is the cue, the portrait is the hero of the sentence). Frames filtered by `maxChars` on the FILLED question with the row's actual name (31 chars x 0.75 x 24 = 558 + 16 <= 579, the G1-350 `need()` formula); the validator requires >= 8 of the locale's 24 question literals to pass 31 chars with the LONGEST tagged name (m in the bank, e.g. "Kuinka monta omenaa Aino näkee?" 31, "¿Cuántos globos tiene Sofía?" 28, "Wie viele Flugzeuge hat Emma?" 29 - the de / fi / es panels keep count frames short).
**verify():** stamps per row `data-lcs-frame data-lcs-ask data-lcs-answer=<the whole canonical question>`; re-derived: `rows` rows, each with one printed sentence carrying exactly one mark (=== the asked slot literal, node) and one EMPTY writing row (`emptyRows`) of glyphH >= 24 and w >= the answer's `need`; the canonical question is NOT printed anywhere (poison: the question as a starter -> FAIL); node: answer === `fillSlots(q[ask], slots)`; the answer starts with `qwords[ask]` and ends with "?" (es: starts "¿"); no bank on the page (poison: a `wordBank` banner -> FAIL); no name twice; portraits >= 36; no `when` clock required (the picture is the portrait: `src` ∈ `people[]` of the name's gender).
**Refusals:** none as a face; per frame by `maxChars`. README open item 6: NO answer key ships, so the canonical question lives only in the stamp (the teacher accepts natural variants; the copy never promises "with answers").
**Query face:** en "write the question worksheet 2nd grade" · de "Fragesätze schreiben Klasse 2" · es "escribe la pregunta" · pt "escreva a pergunta" · fr "écris la question" · it "scrivi la domanda" · nl **"vraagzinnen maken groep 4"** (measured; the F4 title) · sv **"ställ en fråga"** (measured) · da **"stil et spørgsmål"** (measured) · no **"lag et spørsmål"** (measured) · fi **"tee kysymys"** (measured).

### F5: Ask About the Picture: Why and How (G2, `G2-3xx TBD`, CODE `layout:'ask'`, open-ended)
**Move:** ASK: a composed scene (a portrait, a thing, a place picture, an 80-120 px clock on one cream stage) and six school lines, each opening with a printed question-word STARTER (Wer / Was / Wo / Wann / **Warum / Wie**); the child completes six questions about the scene. The only open face: why and how have no single answer from a picture, so they live here and nowhere else (the panels' measured "varför och hur / hvorfor og hvordan / miksi ja miten" query face, "the two hard ones").
**Child:** "Look at the picture. Finish each question. Start with the word already on the line." (79; apparatus: picture, lines, word on the line.)
**Params:** d1 `{layout:'ask', starters:['who','what','where','when'], rows:4, glyphH:30, rowH:64, scene:{h:240}}` · **d2** `{layout:'ask', starters:['who','what','where','when','why','how'], rows:6, glyphH:26, rowH:58, scene:{h:210, pic:120, clock:120}}` · d3 `{layout:'ask', starters:[], bank:true, rows:6}` (the six words in a bank, no starters; unpublished). Stack *est.*: scene 210 + 12; 6 x (58 + 6) = 384; total 606 <= 677. Starters are printed on the ruling by `rulingBlock({starters})` and sized by `starterFontPx` from the MEASURED metrics (`primitives/font-metrics.json`; `verify-ruling-starters.js` discovers this surface by `build()`, the 2026-09-21 rule). Scene: `askScene({portrait, thing, place, time})` = four white tiles on a cream stage, each 120 px (the clock at 120 -> 13.8 px numerals), the same four literals are NOT printed (nothing to copy); the composer draws one name (+ its portrait), one thing, one place, one time per seed.
**verify():** open-ended: no verify() (layout lints + `verify-ruling-starters.js` + the gate's floors: 6 writing rows, glyphH >= 24, six distinct starters === the `starters` literals in order, no other text on a ruling, scene tiles >= 100, a clock >= 79, `[data-ws-content]` on the stage).
**Refusals:** none.
**Query face:** en "ask questions about the picture why how worksheet" · de "Fragen zum Bild stellen warum wie" · es "preguntas sobre la imagen por qué cómo" · pt "perguntas sobre a figura" · fr "poser des questions sur l'image pourquoi comment" · it "domande sull'immagine perché come" · nl "vragen stellen bij een plaatje waarom hoe" · sv **"varför och hur"** (measured) · da **"hvorfor og hvordan"** (measured) · no "hvorfor og hvordan" · fi **"miksi ja miten"** (measured).

**Rejected non-moves.** A theme swap (themeless) · "choose the end mark" / restore the "?" (G2-282, and the Romance `qUncued` wall) · a why / how face with chips (no single answer from a picture; open only, F5) · `wen / wem / wessen / a quién / à qui` object-of-person questions (above band; would make two words fit) · direction `wohin / mihin / vart` (a second where-word at G1 is a non-task; d3 material at most) · a "which" / "welche" face (needs a set, that is K-235) · re-labelling the base's d1 (3 chips) or d3 (5 chips) as a face (same move) · a picture-only "who or what?" card face without a sentence (the picture would decide, the panels' two-valid objection in another dress) · a passage with W-questions (G2-254) · a "write the answer" face (the answer is printed by construction here; asking for it is G2-254's recall).

## C. Native rebuild x11

Frames NEVER inflect: every question literal, place phrase, time phrase, verb and question word is a stored literal; the code substitutes `{name}` (a tagged name, nominative), `{thing}` / `{part}` (an `objForms` form of a pool key), `{place}` (a `places[key].text` literal), `{time}` (a `times[i].text` literal), `{n}` (`numberWord(n, loc)`) and `{pl}` / `{part}` (an `objForms` form), and nothing else. Per locale the panel authors: `strings` (6 titles + 6 instructions), `qwords` (5) + `starters` (6) + `bins` (3-4, with "?"), 12 `frames` (3 thing + 3 place + 3 time + 3 count; each `text` + `q.who` + `q.<kind>` = 36 literals), `places` (14 keys -> a phrase or `null`), `times` (6 clock literals), `qPrefix`, optional `sortThingForm`, and audits the EN as a SOURCE. = 12 + 5 + 6 + 4 + 36 + 14 + 6 = **~85 literals** per locale (+ 12 `nameForms` only if the fi panel chooses adessive possession). Names and portraits are the pronouns bank's (12 tagged names, 32 opened portraits) and are not re-authored. Instructions name only apparatus on that face's page (the base and F2 / F4 say "underlined part"; F2 alone says "bank" and "box"; F3 alone says "cards"; F5 alone says "picture" and "word already on the line").

| loc | literals the panel authors (counts) | slots / forms needed and where they already exist | refusal / re-target | traps |
|---|---|---|---|---|
| en | 12 strings · 5 + 6 + 3 words · 12 frames x 3 = 36 · 14 places · 6 times = ~82 | `{thing}` = `objForms.unique` "the ball" (133); `{pl}` "balls"; places "in the garden / at the beach / in the tent / at home ..." (own literals); times "at eight o'clock" | none | "Who" for persons only (never animals, never "What" for a name); US spelling; question frames in present simple ("Who has the ball?", "What does Mia have?": the `what` question needs do-support, a whole literal); "How many" one pill |
| de | ~82 | `{thing}` = `unique` (ACCUSATIVE phrase "den Ball", 131) so every thing-frame verb governs the accusative (hat / sieht / malt / sucht / findet: the panel signs `objCase:'acc'` per frame; "mit {dat}" frames are legal by naming `{dat}`); `{pl}` bare "Bälle"; places "im Garten / am Strand / im Zelt / zu Hause / auf dem Spielplatz" (dative inside the literal, never generated); times "um acht Uhr" | none | Nomen keep the capital; "W-Fragen" hyphen + capital W in the title; "Wie viele" one pill; never "Wen / Wem"; the Klasse 3-4 Bericht genre never in copy; engine appends "Arbeitsblatt" |
| es (MX) | ~82 | `{thing}` = `unique` "el murciélago" (88); count frames masculine only (`genderFilter:{count:'m'}`, 58 keys); places "en el jardín / en la playa / en la casa ..."; times "a las ocho" | none as a face | every question literal opens "¿" (`qPrefix`); accents on chips (Quién, Qué, Dónde, Cuándo, Cuántos); "oraciones" never "frases"; MX register; "cuántas" never printed; the SEP wording is confirmed by the panel |
| pt (BR) | ~82 | `{thing}` = `unique` "o antílope" (125); count masculine only (80); places "no jardim / na praia / em casa / na cozinha"; times "às oito horas" | none | "O que" one pill; ONE article-before-name policy for the type; "quantas" never printed; never "atividade" in a title; "frases interrogativas" never a title (G2-274 territory) |
| fr | ~82 | `{thing}` = `unique` "l'avion" / "le ballon" (131, elision inside the literal); count "Combien de {pl}" genderless; places "dans le jardin / à la plage / à la maison"; times "à huit heures" | none | NBSP before "?" inside every question literal (validator: ends with " ?" or " ?"); "Que" + inversion or a refusal of that register (ONE policy, recorded in the landing); "fiche" never in a title; the CP landing names CP-CE1 |
| it | ~82 | `{thing}` = `unique` "l'antilope / lo zaino / la palla" (117); count masculine only (68); places "in giardino / al mare / a casa / in cucina"; times "alle otto" | none | "Che cosa" one pill; **"Dove" + "è" -> "Dov'è"**: where-questions avoid "è" or the type accepts "Dove è" (one policy); "scheda" never in a title; the title carries the Indicazioni device "le domande della frase" |
| nl | ~82 | `{thing}` = `unique` "de bal / het huis" (103); `{pl}` (103); places "in de tuin / op het strand / thuis / in de keuken"; times "om acht uur" | none | "Wie" = who; "Hoeveel" one word; F4 title is the measured "Vraagzinnen maken"; IJ irrelevant; engine appends "werkblad" |
| sv | ~82 | `{thing}` = `unique` = the DEFINITE "bollen" (114; never `def` of a noun whose definite equals another singular: `bana` -> `banan` = banana, the G1-350 mechanical check reused); `{pl}` indefinite "bollar"; places "i trädgården / på stranden / hemma / i köket"; times "klockan åtta" | none | "Var" static, never "Vart"; "Hur många" one pill; never «grupp»; engine appends "arbetsblad"; title = the four measured words [NSR] |
| da | ~82 | `{thing}` = `unique` definite "bolden" (119; same `def` equality check); `{pl}` "bolde"; places "i haven / på stranden / hjemme"; times "klokken otte" | none | "Hvor mange" one pill; "Hv-ord" hyphen in the title; "spørgeord" META [NSR] |
| no | ~82 | `{thing}` = `unique` definite "ballen" (116; same check); `{pl}` "baller"; places "i hagen / på stranda / hjemme / på kjøkkenet"; times "klokka åtte" | none; nynorsk sibling REFUSED (recorded, not faked) | bokmål only; "Hvor mange" one pill; title never "hv ord"; "Når" not "Hvornår"; engine appends "oppgaver" [NSR] |
| fi | ~82 (+12 `nameForms.ade` only if chosen) | `{part}` partitive objects "omenaa" (121) with partitive verbs; `{n} {part}` after numerals; places = inessive / adessive literals ("puutarhassa", "rannalla", "kotona", "keittiössä", "pöydällä" only if a thing subject were allowed: it is not, so person-places only); times "kello kahdeksan"; names nominative on every face | none as a face; item-level: what = partitive only, where = static only | chips are the INFLECTED forms (Kuka · Mitä · Missä · Milloin · Kuinka monta), F3 bins "Kuka? Mikä? Missä?" (bare nominative cards); F2 `rows:6`; a 4-line fi title forces the 677 check on every stack; "Kuinka monta" one pill; `\b` is ASCII-only: the gate's word-boundary checks use `(?<!\p{L})…(?!\p{L})` [NSR] |

Every panel OPENS every picture it pins (the 14 place keys are opened above and locale-neutral; a locale that authors `null` for a key drops it), signs `objCase` on de frames, reads one render of every face in its locale, and audits the EN source (the #41 / #42 lesson: the English is the locale nobody reads).

## D. Data + gates

`data/b4/question-words.js` (the EN block, hand-authored) + `data/b4/locales/question-words.<loc>.json` GENERATED from `i18n/.draft-b4-<loc>.json` by `tools/apply-b4-locale.js` after `tools/validate-b4-draft.js` (b4 clones of the b3 pair; `data/` gitignored, `git add -f`); read through `lib/b4-common.js bank('question-words', loc)` (README open item 3). Names + portraits are read from `bank('pronouns', loc)` (`names[]` with `gender`, `people[]` with `depicted` + `picOpened`); the pronouns bank must exist first (build order: G1-352 before G1-353; the validator asserts it loads). `objForms` through `bank('instructions', loc)`; number words through `lib/number-words.js`.

```
QUESTION_WORDS (data/b4/question-words.js EN block; per-locale JSON overlays; every literal whole):
  qwords:   { who:'Who', what:'What', where:'Where', when:'When', howmany:'How many' },   // fixed order who what where when howmany
  starters: ['Who','What','Where','When','Why','How'],                               // F5 only; why/how appear nowhere else
  bins:     { who:'Who?', what:'What?', where:'Where?', when:'When?' },               // F3 heads; fi what:'Mikä?'
  qPrefix:  '',                                                                        // es '¿'
  sortThingForm: 'bare',                                                              // 'bare' (displayWord singular) | 'unique'
  genderFilter: {},                                                                   // es/pt/it { count:'m' }
  frames: [                                                                           // 12: 3 per kind; {name} FIRST token
    { id:'have-thing', kind:'thing', text:'{name} has {thing}.',
      q:{ who:'Who has {thing}?', what:'What does {name} have?' } },                  // de: objCase:'acc'
    { id:'be-place',   kind:'place', text:'{name} is {place}.',
      q:{ who:'Who is {place}?', where:'Where is {name}?' } },
    { id:'play-time',  kind:'time',  text:'{name} plays {time}.',
      q:{ who:'Who plays {time}?', when:'When does {name} play?' } },
    { id:'see-count',  kind:'count', text:'{name} sees {n} {pl}.',                    // fi: '{name} näkee {n} {part}.'
      q:{ who:'Who sees {n} {pl}?', howmany:'How many {pl} does {name} see?' } },
    ...
  ],
  places: [                                                                           // 14 locale-neutral keys, every pic OPENED
    { key:'garden', pic:{theme:'spring', noun:'garden'}, text:'in the garden', picOpened:true },
    { key:'beach',  pic:{theme:'summer', noun:'beach'},  text:'at the beach',  picOpened:true },
    { key:'house',  pic:{theme:'miscellaneous', noun:'house'}, text:'at home', picOpened:true },
    ...                                                                               // tent forest lake river mountain pool kitchen garage bed sofa playground (cabin optional)
  ],
  times: [ { text:'at eight o\'clock', h:8 }, { text:'at three o\'clock', h:3 }, ... ],   // 6; rendered by primitives/clock.js m:0
  thingThemes: ['animals','fruits','vehicles','toys','zoo animals','farm animals'],   // the G1-308-opened six; first candidate wins
  numbers: [2,3,4,5],                                                                 // count frames; numberWord(n, loc)
  strings: { base:{title,instruction}, match:{…}, gap:{…}, sort:{…}, write:{…}, ask:{…} }
```
`fillFrame(frame, ctx)` (lib, pure): slots `{name}` <- `ctx.name.name`; `{thing}` <- `objForms[key].unique`; `{part}` <- `.part`; `{pl}` <- `.pl`; `{place}` <- `places[key].text`; `{time}` <- `times[i].text`; `{n}` <- `numberWord(n)`; `{name:ade}` <- `nameForms.ade[name]` (fi option) via `fillSlots` (throws on an unfilled slot); `markOf(frame, ask)` = the filled literal of the asked slot (`who` -> the name; `count` -> the number word ONLY); `questionOf(frame, ask, ctx)` = `fillSlots(frame.q[ask], ctx)`; `gapOf(loc, q, ask)` = `q` minus `qPrefix` minus the leading `qwords[ask]` (exact, case-sensitive; throws if the literal does not open with it).

**`tools/validate-b4-draft.js` (question-words block; every rule exit 1; the correct draft is the control):**
1. `qwords` has exactly the five keys, distinct literals (NFD, case-insensitive), each <= 14 chars, none equal to a name, a place / time literal, or any `objForms` literal of the locale; `starters` = the five `qwords` values in order + two more (why, how), six distinct; `bins` keys ⊆ {who, what, where, when}, each value ends with "?" (es "¿…?"), and each is `qwords[k] + '?'` unless the locale declares `binAliases` (fi `what:'Mikä?'`).
2. `frames` >= 12, >= 3 per `kind` ∈ {thing, place, time, count}; `text` starts with `{name}` (poison: "Den Ball hat {name}." -> FAIL), contains exactly one `{name}` and exactly the complement slots its kind requires (`thing`: one of `{thing}|{part}|{dat}|{gen}|{a2}`; `place`: `{place}`; `time`: `{time}`; `count`: `{n}` + one of `{pl}|{part}`), no `___`, ends with "."; no `qwords` literal as a token in `text` (poison: "{name} weiß, wer kommt." -> FAIL); `text` filled with the longest name and the longest pool literal <= 40 chars.
3. `q` has exactly `who` and `<kind>`; each starts with `qPrefix + qwords[k]` exactly (poison: "Wer hat {thing}" missing "?" -> FAIL; "wer hat {thing}?" lowercase -> FAIL); contains every slot of `text` EXCEPT the asked one (`who` questions omit `{name}` and keep the complement; `<kind>` questions keep `{name}` and omit the complement, for `count` omit `{n}` and keep `{pl}`; poison: `q.where` = "Wo ist {name} {place}?" -> FAIL "asked slot printed"); ends with "?" (fr: NBSP before it; es: opens "¿"); >= 8 (frame, ask) pairs filled with the longest tagged name are <= 31 chars (F4 `maxChars`).
4. de frames of kind `thing` carry `objCase:'acc'` (or name `{dat}` after a preposition) : poison: a de frame "{name} hilft {thing}." (dative verb) without `objCase:'dat'` + `{dat}` -> FAIL (semantic: the panel signs; the validator refuses a `{thing}` slot on a frame signed `dat`).
5. No `text` or `q` literal equals a `SENTENCES[loc].frames[].text` with its slots filled (the 4 question frames of that bank are the obvious hits; poison: "Where is the {noun}?" -> FAIL); no literal contains "{noun}" / "{color}" (the b2 slot names).
6. `places` = the EN key list in every locale (overlays may not add a key; a value may be `null` = refused); every non-null `text` is non-empty, starts lowercase unless the locale's `nounCase` keeps a capital inside ("im Garten": the noun capital is allowed, the first token is not), and >= 6 keys are non-null (a page needs <= 3 places); every `pic` resolves as a colour-index candidate for the locale (no localized B&W marker); `picOpened:true`.
7. `times` >= 6, each `{text, h}` with `h` integer 1..12, distinct `h`, distinct `text`.
8. `genderFilter.count` ∈ {undefined, 'm'}; present for es / pt / it (poison: an es draft without it -> FAIL); `qPrefix` = "¿" for es and "" elsewhere (poison: pt "¿" -> FAIL).
9. `strings`: 6 titles <= 70, no worksheet word (`seo.words.worksheet` of the locale), unique in the band, base title === the head, no title contains the locale's `capitals-punctuation.name`, `reading-comprehension.name`, `sentence-building.name` or `word-classes.name` (poison: sv "Skiljetecken" in a title -> FAIL), the F4 title is the locale's measured write-a-question head where one was measured (nl / sv / da / no / fi: a data assertion on a declared `measuredHeads` list); 6 instructions <= 150; the F2 instruction contains the locale's bank word AND box word and no other does; the F5 instruction contains the locale's picture word and no other does; no visible string carries a free-predication (the §25 lint).
10. `sortThingForm` ∈ {'bare','unique'}; when 'unique' in sv / da / no, every pool key's `unique` !== any other key's vocab singular (the `bana`/`banan` mechanical check; poison: a sv draft with `unique` and the key `track` present -> FAIL).
11. `nameForms` absent unless fi declares `possession:'adessive'`, in which case `nameForms.ade` covers all 12 names (poison: 11 -> FAIL) and count frames name `{name:ade}`.
12. The draft's EN `qwords`, `frames` ids / kinds / slot shapes and `places` keys equal the module's EN block (the EN is the source); the pronouns bank loads for the locale (`names.length === 12`, `people.length === 32`).
13. fi: `thing` frames name `{part}` only (poison: a fi frame with `{thing}` -> FAIL); `qwords.what === 'Mitä'` unless the panel declares `whatForm:'nominative'` AND every thing frame then names `{thing}` with a nominative-subject text (one policy); `places[].static:true` on every non-null fi place.

**Poison cases (each must FAIL; the correct draft passes):** (1) `text` "Den Ball hat {name}." -> rule 2; (2) "{name} weiß, wer kommt." -> rule 2; (3) `q.where` "Wo ist {name} {place}?" -> rule 3; (4) `q.who` "wer hat {thing}?" (lowercase) -> rule 3; (5) a fr `q` ending " ?" without the NBSP when the locale declares `nbsp:true` -> rule 3; (6) a de dative-verb frame carrying `{thing}` -> rule 4; (7) "Where is the {noun}?" -> rule 5; (8) an es draft without `genderFilter.count` -> rule 8; a pt draft with `qPrefix:'¿'` -> rule 8; (9) `places` overlay adding `zoo` -> rule 6; `pic:{theme:'animals bw', …}` -> rule 6; (10) sv `sortThingForm:'unique'` with the `track` key in the pool -> rule 10; (11) a fi frame with `{thing}` -> rule 13; (12) sv title "Frågeord och skiljetecken" -> rule 9; a base instruction containing the bank word -> rule 9; (13) render-side, through the `_buildWith(bank, …)` seam with the validator bypassed (the #43 lesson): a base row whose mark spans "three apples" on a count frame -> `verify()` FAILS (mark !== number word); a row whose chips are in a different order -> FAIL (position leak); an F1 page with two `who` questions -> FAIL; an F2 lane printing "Wer hat den Ball?" whole -> FAIL; an F4 row with the canonical question as a starter -> FAIL; a `when` row with a 56 px clock -> FAIL (size < 79; and `qa/lints.js` fails the 6.4 px numerals independently); a `who` row whose portrait is `occupations/nurse` (f) under the name "Ben" (m) -> FAIL (the pronouns tags cross-check); a name in object position ("Mia sieht Ben.") -> the validator's rule 2 (one `{name}`) and, injected at render, the gate's node refill FAIL.

**`qa/verify-b4-question-words.js` (render gate; every face; every locale; d2 and the 677 fi stack):** floors (pictures >= 44 on G1 faces / >= 36 on F4-F5; chips >= 44 high and >= 96 wide; text >= 16; writing glyphH >= 24 on F4 / F5; F3 bin lines >= card count; a clock >= 79 wherever `[data-lcs-prim="clock"]` exists); uniqueness (no name twice, no thing key twice, no place twice, no time twice, no portrait `src` twice, one mark per sentence, one correct chip per row, one question per kind on F1); **no answer printed** (base: the correct chip marked only by `data-lcs-correct`, never by style, the K-288 identical-style check; F2: the `qwords[ask]` literal absent from the lane (`(?<!\p{L})…(?!\p{L})`, NFD, case-insensitive); F4: the canonical question absent from every text node; F1: no question contains an answer literal); position (chip order identical on every row; F2 bank order !== `qwords` order on >= 1 of 10 seeds; F1 right column deranged); the 722 stack (`.ws-body` scrollHeight <= clientHeight at the 3-line chrome AND at the fi 4-line 677 chrome, F2 fi at `rows:6`; nothing intrudes on the footer); `[data-ws-content]` on every stage; palette tokens only; node cross-checks (every stamped sentence === `fillFrame` from the bank; every mark === `markOf`; every chip index === `qwords` order index of `ask`; every F2 gap rest === `gapOf`; every F4 answer === `questionOf`; every portrait's `depicted` === the name's `gender`; every thing `src` === the first fan-theme candidate; every place `src` === the pinned file; every clock `h` === the time literal's `h`). Wired into `deploy.sh` browser-free for the contract half (bank + validator + node refill) and run with puppeteer as the dev gate for the render half.

## E. SEO

Titles (never the worksheet word; <= 70; base = the head; each face adds ONE element: the verb of the move; F4 takes the measured "write a question" head where one exists; F5 carries "why and how"):

| pattern | base | F1 | F2 | F3 | F4 | F5 |
|---|---|---|---|---|---|---|
| Germanic en | Question Words: Who, What, Where | Match the Question to the Answer | Write the Question Word | Sort the Answers: Who, What or Where | Write the Question | Ask About the Picture: Why and How |
| Germanic de / nl | W-Fragen: wer, was, wo · Vraagwoorden: wie, wat, waar | Frage und Antwort verbinden · Verbind de vraag met het antwoord | Fragewörter einsetzen · Vul het vraagwoord in | Wer, was oder wo? Antworten sortieren · Wie, wat of waar? Antwoorden sorteren | Fragesätze schreiben · Vraagzinnen maken | Fragen zum Bild: warum und wie · Vragen bij het plaatje: waarom en hoe |
| Romance es / pt / fr / it | Preguntas con quién, qué y dónde · Quem, o que, onde: palavras para perguntar · Les mots interrogatifs : qui, que, où · Le domande della frase: chi, che cosa, dove | Une la pregunta con su respuesta · Ligue a pergunta à resposta · Relie la question à sa réponse · Collega la domanda alla risposta | Completa la pregunta · Complete a pergunta · Complète la question · Completa la domanda | ¿Quién, qué o dónde? Ordena las respuestas · Quem, o que ou onde? Separe as respostas · Qui, que ou où ? Trie les réponses · Chi, che cosa o dove? Ordina le risposte | Escribe la pregunta · Escreva a pergunta · Écris la question · Scrivi la domanda | Preguntas sobre la imagen: por qué y cómo · Perguntas sobre a figura: por que e como · Questions sur l'image : pourquoi et comment · Domande sull'immagine: perché e come |
| Nordic sv / da / no | Frågeord: vem, vad, var, när · Hv-ord: hvem, hvad, hvor · Spørreord: hvem, hva, hvor | Para ihop fråga och svar · Spørgsmål og svar · Spørsmål og svar | Skriv rätt frågeord · Indsæt hv-ordet · Sett inn spørreordet | Vem, vad eller var? Sortera svaren · Hvem, hvad eller hvor? Sortér svarene · Hvem, hva eller hvor? Sorter svarene | Ställ en fråga · Stil et spørgsmål · Lag et spørsmål | Frågor om bilden: varför och hur · Spørgsmål til billedet: hvorfor og hvordan · Spørsmål til bildet: hvorfor og hvordan |
| fi | Kysymyssanat: kuka, mikä, missä | Kysymys ja vastaus | Täydennä kysymyssana | Kuka, mikä vai missä? Lajittele vastaukset | Tee kysymys | Kysymyksiä kuvasta: miksi ja miten |

Meta MIDDLE = the face's instruction (79-108 chars in EN above), so that `"Free printable {TITLE} … {LEVEL}. {MIDDLE}. …"` lands in the 120-170 window whole; de / fi / pt run 30-40 % longer, so the panel keeps each instruction <= 120 chars and `tools/measure-instruction-window.js` picks the longest fitting candidate (the family skill sentence competes; the F2 instruction at 108 EN is the one most likely to be cut to the shorter "Read the answer and write the question word from the bank in the box." (72)). The "free" lead is metadata only (never visible, the §25 lint). de metas name "Klasse 1-2" on the reading faces and "Klasse 2" on F4 / F5; pt "2º ano" / "3º ano"; sv "åk 2" (copy "åk 1-2" on the reading faces); no "2. trinn" / "3. trinn"; fi "1. luokka" / "2. luokka"; es / it copy carries the SEP / Indicazioni device by name ("palabras para preguntar" / "le domande della frase"). README open item 6: no title, meta or landing promises "with answers / mit Lösungen".

`coordinate` per face: `{type:'question-words', mode:'base'|'match'|'gap'|'sort'|'write'|'ask', level: LEVEL_KEYS[loc][band per the §A table], theme:''}`; `canonicalDeckSlug` = the published d2 deck; one landing per face per locale, no refusals = **66 rows** for `scripts/verify-hub-type-rows.js` (the README matrix row "6 x 11 = 66 (to be confirmed)" is CONFIRMED).

Non-cannibalisation (raw word-3-gram Jaccard, the gate's FAIL >= 0.80 / WARN 0.65; all *est.*, the gate measures): base vs F2 ~0.32 (the closest pair: both "underlined part" + "question word"; circle vs write-from-bank: sharpen the F2 prose on the BANK and the GAP) · F2 vs F4 ~0.30 (word-from-bank vs whole question on a line, no bank) · base vs F4 ~0.26 · base vs F1 ~0.14 (kind-matching prose, no underline) · base vs F3 ~0.16 (cards and bins) · F1 vs F3 ~0.15 · F5 vs every other ~0.08-0.12 (why / how, starters, open) · vs the Boundary: base vs G2-282 "choose the end mark" ~0.08 (capital + end mark prose) · F4 vs G2-274 ~0.12 (both write a sentence on a line; corrupted vs printed answer) · F2 vs G1-352 F1 "replace the name" ~0.14 (a bank word into a gap; pronoun vs question word) · F3 vs G1-352 F4 / K-288 G1-292 ~0.18 (the same bin idiom; names-by-pronoun vs answers-by-question) · F1 vs G2-254 ~0.10 · F5 vs G2-278 ~0.15 (both write on lines about a picture; questions with starters vs sentences). The lever if any pair reads high: mode-true sharpening (what the child DOES on this face), never the gauge.

## F. Open questions + summary

**The engineer measures:** (1) the base row at 88 px x 7 with an 80 px picture column and a one-line 18 px sentence + 44 px chips (652 total) on the 677 fi stack; (2) the clock at `size:80`: computed numeral font-size must read >= 9.0 px in Chromium (0.115 x 80 = 9.2; if the label rounds below 9 the size becomes 88 and the row 96 x 7 = 708, still <= 722 but not 677 -> fi rows 6); (3) F2 at 7 rows + the 69 px bank = 721 (the fi 3-line-title rule or `rows:6`), and `gapW` 168-216 with the question rest wrapping on fi; (4) `articleChips` at `w:96, fontPx:20` for "Wanneer / Hvornår / Milloin / Cuándo / O que / Che cosa" (all <= 8 chars; *est.* <= 88 px); (5) F1 `.ws-match` items at 96 high with a 330 / 250 column split and 2-line questions in 330 px; (6) F3 shelf 2 x 5 cards at 118 px with a place caption ("auf dem Spielplatz" 18 chars at 16 px *est.* 150 px: the caption may need 2 lines or `cardW:130` at 5 per row = 650 + gaps > 660 -> 4 + 5 rows); (7) F4 `writingRow` at glyphH 24 x 8 rows inside 677 and the 31-char cap against the longest fi / es literals; (8) the fi 4-line title on every stack; (9) whether `bank('instructions', loc)` and `bank('pronouns', loc)` can be required from a b4 type through `lib/b4-common.js` (README open item 3) and the G1-352-before-G1-353 build order; (10) `candidates(key, loc)` first-fan-theme selection for `car` (toys vs vehicles) and `kite` (5 themes): the pin is by `thingThemes` order, never `pictureFor`; (11) the K-288 `sortWords` verify branch cloned with picture cards and 3 bins.

**Only a native panel can rule:** fr "Que" + inversion vs "Qu'est-ce que" (and NBSP typography); it "Dove è" vs a non-è verb; fi "Mitä" partitive chips vs a nominative "Mikä" frame policy, static places only, `possession` (adessive `nameForms`) vs transitive verbs, and the F3 "Mikä?" bin [NSR]; es the SEP wording ("palabras para preguntar") and the 1º / 2º split; pt the article-before-name policy and whether every face sits at 2º / 3º ano; de whether the type sits at 2. Klasse throughout or the reading faces at 1. Klasse, and `sortThingForm` (bare "Ball" vs "den Ball"); sv "var" (recorded), "Hur många" as one pill, åk 2 on every face [NSR]; da "Hv-ord" vs "Spørgeord" as h1 [NSR]; no 2. / 3. trinn split [NSR]; every locale: the 14 place literals (which keys are `null`), the 6 clock literals, the 12 frames + 24 questions, and the 4 masculine-only count nouns for es / pt / it.

**Summary.** A question-word type locked on the panel's principle: the printed ANSWER sentence (`{name}` + verb + ONE complement of a declared kind) with its asked constituent underlined and pictured (portrait / thing / place / an 80 px analog clock, the smallest clock the 9 px font lint allows) decides the one question word; six distinct moves (circle a chip · match a whole question to a short answer by kind · write the word from a bank into the question's gap · sort short answers into Who / What / Where bins · write the whole question on a school line · open why-and-how question-writing with measured-metric starters); themeless, things from the reviewed `objForms` pool over the six opened fan themes (88-133 per locale, fi 121 partitives), 14 opened place keys with per-locale phrase literals, 6 clock literals, names + 32 portraits shared from the pronouns bank; per-locale question-word tables of five with fi's inflected forms (Kuka · Mitä · Missä · Milloin · Kuinka monta), es "¿" prefix and masculine-only count nouns in es / pt / it; no refusals = 66 hub rows; levels honest per locale (de / it / nl / sv / da on one grade, en / es / pt / fr / no / fi split reading G1 vs writing G2); CCSS L.1.1.j on the four G1 faces and L.2.1.f on F4 / F5; the SEP "palabras para preguntar" and the Indicazioni "le domande della frase" named as the es / it teaching points the lock demanded; frames never inflect (every question, place, time and verb is a whole literal; the code fills `{name} {thing|part} {place} {time} {n} {pl}` and underlines the one it asked for).
