/** G1-317 — Which Place? Write the Ordinal Number. nt20-C variation of K-320 (generated by tools/gen-b3var-specs.js from tools/b3var-rows/). */
'use strict';
const base = require('../k/K-320-ordinal-numbers.js');
// One object for all three levels: the waves ship d2 only, so a face must
// render identically whichever level is asked for. Spreading the base entry
// (not a JSON literal) carries function-valued params through intact.
const D = { ...base.difficulty[2], ...{"layout":"where","strips":3,"n":8,"tile":76,"pic":64,"gap":5,"queries":2,"clone":56,"start":"left","flagScale":1.5} };
module.exports = {
  ...base,
  id: 'G1-317',
  slug: 'ordinal-numbers-which-place',
  difficulty: { 1: D, 2: D, 3: D },
  i18n: { en: { title: "Which Place? Write the Ordinal Number", instruction: "Find each small picture in its line and write which place it stands in, counting from the flag." } },
  gradeBand: "G1",
};
