/** G1-339 — Read and Do: Two-Step Instructions. nt20-C variation of G1-308 (generated by tools/gen-b3var-specs.js from tools/b3var-rows/). */
'use strict';
const base = require('./G1-308-read-and-do.js');
// One object for all three levels: the waves ship d2 only, so a face must
// render identically whichever level is asked for. Spreading the base entry
// (not a JSON literal) carries function-valued params through intact.
const D = { ...base.difficulty[2], ...{"steps":2,"rows":4,"rowMin":128,"minVerbs":4,"maxPerCue":3} };
module.exports = {
  ...base,
  id: 'G1-339',
  slug: 'read-and-do-two-step-instructions',
  difficulty: { 1: D, 2: D, 3: D },
  i18n: { en: { title: "Read and Do: Two-Step Instructions", instruction: "Each sentence tells you two things to do. Read it to the end before you start, then do both steps in the row of pictures." } },
};
