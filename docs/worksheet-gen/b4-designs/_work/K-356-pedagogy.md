# K-356 `weather` : PEDAGOGY + CONTENT (nt10-D, 2026-09-21)

Read: `_ROLE-PEDAGOGY.md`, `_STUDIO-BRIEF.md`, `_SUBSTRATE.md`, `_PANEL-FINDINGS.md` (the lock + the K-356 entry + Facts 1/8/9), `K-354-human-body.md` (density target), `types/k/K-211-hot-cold.js` + `data/science/hot-vs-cold.json`, `types/k/K-207-summer-winter-clothes.js` + `data/science/summer-vs-winter-clothes.json`, `types/g2/G2-277-read-the-calendar.js`, `types/g1/G1-146-data-sorting-table.js` + `G1-147-count-and-graph.js` + `types/_shared/graph-tasks.js` (modes `table-sort` / `pict-fill`), `types/g3/G3-345-read-thermometer.js` (measurement-tasks mode `thermometer`, title "Hot or Cold?"), `types/k/K-322-seasons.js` (the `layout` knob + the `fileUri` cross-theme pattern), `types/g1/G1-341-read-and-check-true-or-false.js` (G1-308 `mode:'truth'`), `primitives/thermometer.js`, `data/b2/calendar.js`, `templates/components-b2.js` (`letterBoxes`, `pillChoice`, `wordBank`), `lib/unit-axis.js`, `lib/b3-common.js bank()`, `scripts/seo-landing/gen-b3-landings.js LEVEL_KEYS`, `frontend/config/topics-taxonomy.json`, `frontend/lib/seo/strand-names.ts`, `data/literacy/vocab-match.json` (the K-225 pool). Every number below was measured 2026-09-21 with read-only node (scratch prefix `K-356-`); (m) = measured, *est.* = the engineer measures in the real render with the shell woff2 from `file://`. All 21 `weather` pictures + `clothing/t-shirt` were OPENED (rulings in B). No em-dashes.

**Boundary (load-bearing).** K-211 `hot-and-cold` (LIVE, `science-category-sort`) sorts 14 OBJECT pictures into hot / cold bins and its bank already holds `weather/sun` (hot), `weather/snowflake` (cold) and `winter/mittens` (cold) (m). K-207 `summer-and-winter-clothes` sorts 12 garments incl. `summer/sunglasses`, `clothing/scarf`, `clothing/t-shirt`, `winter/mittens` (m). G2-277 `read-the-calendar` owns the MONTH GRID + date questions. G1-146 / G1-147 (`graph-tasks` `table-sort` / `pict-fill`) own COUNTING kinds into a table / pictograph (the en "weather graph" strings are theirs). G3-345 `reading-a-thermometer` (title "Hot or Cold?", slug `reading-a-thermometer`) owns READING A VALUE off a 0..40 step-5 scale and WRITING it. K-322 `seasons` owns season markers and the months-to-season map. G1-341 owns "read a sentence, circle true / false". K-225 `match-word-to-picture` already draws lines between the vocab word and the picture for `sun` (2 refs) and `cloud` (3 refs) in its pool (m), never for rain / snow / storm / rainbow, and never as a six-symbol chart. K-321 owns the ORDER of the day names. Therefore this family owns exactly four things nobody has: (1) the WEATHER SYMBOL SET, six fixed concept pictures (sun · cloud · rain · storm · snow · rainbow) paired with the locale's WEATHER NOUNS (de Regen / Schnee, never the vocab singulars Regentropfen / Schneeflocke and never the capitalised vocab ADJECTIVES Sonnig / Regnerisch); (2) QUALITATIVE TEMPERATURE BANDS read off a numeral-less thermometer (low / middle / high mercury = cold / warm / hot), never a value; (3) the WATER-CYCLE DIAGRAM (NEW `primitives/water-cycle.js`, four numbered arrows, four stage words from a chip bank), titled from the water-cycle head; (4) the open WEEKLY DIARY and the READ-THE-FORECAST strip (a given Mon-Fri symbol row, questions answered by circling a day chip, nothing counted). Every face uses ONLY the six phenomena; gear (`scarf`, `t-shirt`, `sunglasses`) appears on ONE face as the band-to-picture answer chips and nowhere else (no gear word is ever printed); `hot` (a third sun picture), `cold` (a penguin), `mittens`, `umbrella`, `raincoat`, `puddle`, `thermometer` (a picture), `partly_cloudy` and `rain_boots` (no vocab, m) are OUT of every pool; no face sorts into bins, prints a month grid, counts anything into a table, prints a temperature numeral, or prints true / false chips.

## A. Identity

`apps.weather` ABSENT, `axes['exercise-type'].weather` ABSENT (m; register with the `register-b4-taxonomy.js` shape, `default_subject:'science'`, `default_age_range:'5-7'`). `axes.theme.weather` slug / name ×11 (m): en `weather` / Weather · de `wetter` / Wetter · es `clima` / Clima · pt `clima` / Clima · fr `meteo` / Météo · it `meteo` / Meteo · nl `weer` / Weer · sv `vader` / Väder · da `vejr` / Vejr · no `vaer` / Vær · fi `saa` / Sää. Every title below is a COMPOUND (the lock; the K-322 validator rule title ≠ any `axes.theme.*.slug/name.<loc>` extends to b4). The 11 compound slugs AND the 11 water-cycle slugs grepped against every `axes.*` slug / name in their locale: **0 collisions** (m). `strand-names.ts` has 0 Science rows (m): the strand column below IS the source the build adds (README open item 1).

| loc | genre head (the panels, verbatim) | ASCII slug | levels K / G1 / G3 (`LEVEL_KEYS`, m) | national strand literal | CCSS (en only, honest) |
|---|---|---|---|---|---|
| en | **Weather Symbols** (base; "Weather Chart" for the diary face: 43 of 129 strings; harvest 129 net ~117, Tier A, HARD head: Education.com / Twinkl / TPT / PreKinders; WINNABLE "weather symbols worksheet grade 2", "weather chart printable for 7 days kindergarten", "weather words worksheet 1st grade"; water cycle 109) | `weather-symbols` | `kindergarten` / `grade-1` / `grade-3` | Science (readiness) | NONE. NGSS **K-ESS2-1** (observe local weather patterns) named in PROSE on the base, the diary and the forecast face, never as `educationalAlignment`; the water cycle has **no K-3 NGSS anchor (5-ESS2-1)**: the en F4 landing says so and still builds (the lock) |
| de | **Wettersymbole** (25 of 95), diary sub-head "Wetter beobachten", F4 "Wasserkreislauf" (95 net ~86, Tier A; head HARD: Ideenreise / Frau Locke / Materialwiese / Grundschulkönig; WINNABLE "wettersymbole klasse 1 arbeitsblatt", "wetter beobachten arbeitsblatt klasse 1", "wasserkreislauf arbeitsblatt klasse 2 beschriften"; water cycle 49) | `wettersymbole` | `vorschule` / `1-klasse` / `3-klasse` | Sachunterricht (Lehrplan 1./2. Klasse "Wetter beobachten, Wettersymbole, Wetterkalender"; Wasserkreislauf 3. Klasse) | n/a |
| es (MX) | **El estado del tiempo** (the SEP term; "el clima" is what teachers type but is the theme slug AND a forecast head; 63, band 17, forecast noise ~20, Tier B; WINNABLE "el estado del tiempo para imprimir preescolar", "el clima fichas para imprimir primer grado"; water cycle 117 = the largest cell of the probe) | `el-estado-del-tiempo` | `preescolar` / `primer-grado` / `tercer-grado` | Conocimiento del Medio (SEP 1º "el estado del tiempo"; ciclo del agua 3º) | n/a |
| pt (BR) | **Como está o tempo hoje** (the EI ritual; "tempo atmosférico" in meta; BOTH UNMEASURED: the lock says re-probe before the pt title locks, OPEN 1; 48, band 12, C+; BNCC teaches tempo (weather) vs clima (climate) in 5º ano, so a K page titled "clima" is doctrinally wrong; water cycle 50) | `como-esta-o-tempo-hoje` | `educacao-infantil` / `1o-ano` / `3o-ano` | Ciências (2º ano "tempo atmosférico"; ciclo da água 3º-5º) | n/a |
| fr | **Le temps qu'il fait** (base) / "La météo du jour" (65, band 29, Tier A-; HARD at "la météo" (Météo-France); WINNABLE "la météo maternelle à imprimer", "le temps qu'il fait cp fiche", "la température ce1" = the thermometer face verbatim; water cycle 94) | `le-temps-qu-il-fait` | `maternelle` / `cp` / `ce2` | Questionner le monde (CP/CE1 "la météo"; cycle de l'eau CE2 / cycle 3) | n/a |
| it | **Il tempo atmosferico** (26, band 2, the thinnest of the batch, Tier B-; "almost no owners"; WINNABLE "il tempo atmosferico classe prima scheda"; "tabella per registrare il tempo atmosferico scuola primaria" = the diary face verbatim; water cycle 60) | `il-tempo-atmosferico` | `infanzia` / `classe-prima` / `classe-terza` | Scienze (prima / seconda; ciclo dell'acqua seconda / terza) | n/a |
| nl | **Weersymbolen**, diary sub-head "Het weer bijhouden" (36 net ~27, Tier B+; jufmilou / kleuteridee / Schooltv; WINNABLE "werkblad het weer groep 3"; water cycle 24; **waterkringloop groep 5-6 = above band**, the F4 landing says groep 5) | `weersymbolen` | `kleuters` / `groep-3` / `groep-5` | Oriëntatie op jezelf en de wereld (kerndoel 43 "het weer") | n/a |
| sv | **Vädersymboler** (base), "väderdagbok" (diary) (23 (16), Tier B; WINNABLE "väder arbetsblad"; SMHI owns the symbols, lektion.se, skolmagi paid; water cycle 24) | `vadersymboler` | `forskola` / `ak-1` / `ak-3` | Naturorienterande ämnen (Lgr22 förskoleklass / åk 1 väder; vattnets kretslopp åk 2-3, kemi) | n/a |
| da | **Vejrsymboler**, "vejret opgaver" (15 (21), Tier B-; WINNABLE "vejret opgaver 2. klasse"; DMI, Alinea / Gyldendal "Vejr og klima" 4.-6. paid; water cycle 13) | `vejrsymboler` | `boernehaveklasse` / `1-klasse` / `3-klasse` | Natur/teknologi (1./2. kl; vandets kredsløb 3.-4.) | n/a |
| no | **Værsymboler**, "været oppgaver" (16 (20), Tier B-; WINNABLE "været 2. trinn"; Yr owns the symbols, NDLA upper, Salaby paid; water cycle 13; **vannets kretsløp 4. trinn = above band**: the no G3 key IS `4-trinn` (m), so the F4 landing is honest by construction) | `vaersymboler` | `1-trinn` / `2-trinn` / `4-trinn` | Naturfag (LK20 2. trinn vær; kretsløp 4. trinn) | n/a |
| fi | **Säätilat** `[NSR-FLAG][fi]` (base h1; "säämerkit" / "sääsymbolit" in meta: the fi build panel rules which is h1, OPEN 2; 14 (18), Tier B; WINNABLE "sää tehtäviä lapsille"; Ilmatieteen laitos, Yle; water cycle 11) | `saatilat` | `esikoulu` / `1-luokka` / `3-luokka` | Ympäristöoppi (OPS 2014 1. lk "sää"; veden kiertokulku 3.-4. lk) | n/a |

**Theme axis: FIXED** via `withFixedTheme(type, 'weather')` (`themeAxis.applicable:false`; landings carry `coordinate.theme:''`). `weather` = 21 files / 19 vocab entries ×11 (m, `entriesFor`; `partly_cloudy` + `rain_boots` have no vocab, m); the family uses 6 concept pictures + 3 gear chips (one cross-theme, `clothing/t-shirt`, resolved by `fileUri` the way K-322 draws cross-theme markers). **`unitAxis`: NOT APPLICABLE, RULED.** The only candidate unit (°C / °F on the thermometer face) never prints: the K/G1 band thermometer carries NO scale numerals (three tinted bands + tick marks + the mercury height; the unit trap of the panel entry disappears), and a unit that never reaches the page is not a fan. `data-lcs-value` stays an internal coordinate. If a future face ever prints a scale it becomes G3-345's territory, not a unit fan here.

**The one rule that locks the type.** The SYMBOL is the apparatus and the answer is a FIXED PAIRING held once in a locale-neutral file: `WEATHER_SYMBOLS` (6 keys, one OPENED picture each) pairs with `symbolWords[key]` (a whole panel literal, the weather NOUN, never derived from the vocab and never an adjective); the band-to-gear map, the water-cycle stage keys and the diary / forecast day labels are global data (`calendar.js dayNames` as GIVEN labels); the code substitutes literals, shuffles, stamps and never inflects. Nothing on any page is derived from `image-vocabulary.js` at render (no `objForms` exist for the weather nouns in 10 locales; fi has 8, unused, m).

## B. The six faces

Common: `templates/components-b4/weather.js` (symbol cards, word cards, letter-box rows, diary rows, band cards, forecast strip + question rows) + NEW `primitives/water-cycle.js` (F4) + a numeral-less band thermometer (F3: either an ADDITIVE `labels:false, bands:[…]` option on `primitives/thermometer.js` with the default unchanged so G3-345 stays byte-identical under `b4-baseline`, or a NEW `primitives/band-thermometer.js`; the design agent chooses, the pedagogy requires only: no `<text>` inside the SVG). Every face is a CODE face on ONE additive `layout` knob (the K-322 pattern: `d.layout` undefined = the base path, byte-identical; a face stamps `data-lcs-layout` only when declared). Guards key on the CONFIG (`d.pairs`, `d.boxes`, `d.days`, `d.items`, `d.stages`, `d.asks`), never on the level index. Face ids are `TBD by the emitter` in the band stated.

**Symbol set (global; every picture OPENED 2026-09-21; the concept picture is FIXED, alternates are recorded for a panel swap only, never mixed on one page):**
- `sun` = a smiling yellow sun with rays (concept **sun**). Alternate: `sunny` (the same sun with orange bar rays and a face). EXCLUDED: `hot` (a third smiling sun with red-tipped spikes: it is the K-211 "hot" register and a duplicate symbol).
- `cloud` = ONE pink cloud with a face (concept **cloud**; reads as a light-grey cloud in mono; the face matches the sun's, so the set reads as one symbol family). Alternate: `cloudy` (a blue-grey cluster of clouds, no face; a panel may prefer it for a plural noun "Wolken" / "moln", OPEN 3).
- `rainy` = a grey cloud with falling drops (concept **rain**; the weather-chart symbol). Alternates: `raindrop` (a single big blue drop: reads "water", not weather, at K) and `puddle` (a pond in grass: OUT).
- `thunderstorm` = a dark cloud with an orange-yellow bolt (concept **storm**). Alternate: `stormy` (a RED cloud with a bolt and black drops; red reads "fire" at K, kept as alternate only).
- `snowflake` = a pale blue six-arm flake (concept **snow**). No alternate. (In K-211's cold bin: allowed here because the MOVE is symbol-to-word, never a sort.)
- `rainbow` = a five-band arc on two pink clouds (concept **rainbow**). No alternate.
- Gear chips (F3 only): `weather/scarf` (a red patterned scarf) · `clothing/t-shirt` (a plain green T-shirt; vocab ×11: T-Shirt / Camiseta / T-shirt / Maglietta / T-skjorte / T-paita, m) · `weather/sunglasses` (red sunglasses). OPENED, all three read as one garment each. EXCLUDED: `raincoat` (rain, not a temperature), `umbrella` (rain), `mittens` (K-211 cold + K-207 winter), `cold` (a penguin), `thermometer` (a picture of the apparatus).

### Base: Weather Symbols (K, `K-356`, layout undefined)
**Move:** RECOGNISE a weather symbol and MATCH it to its weather word: six symbol cards on the left, six word cards on the right in a different order, one pencil line per pair (a K match; the chart of six is the whole teaching point of the unit "Wettersymbole / väderdagbok").
**Child:** "Draw a line from each weather symbol to its word." (52 chars; apparatus on the page: symbols, words, lines.)
**Params:** d1 `{pairs:4, symbolPx:96, wordPx:22, keys:['sun','rain','snow','cloud']}` · **d2 (ships)** `{pairs:6, symbolPx:88, wordPx:20, keys:'all'}` · d3 `{pairs:6, symbolPx:80, wordPx:20, keys:'all', wordCase:'lower', article:true}` (de / nl / sv / da / no print the article INSIDE the word literal, `bankWords`; a genuine extra reading load, not shipped, no copy describes it). Stack *est.*: `.ws-match` two columns, 6 rows × 100 + 5 × 14 = 670 <= 722 (rows `minmax(88px,1fr)`); at the fi 677 chrome the rows shrink to 88 + gaps 12 = 588. Right column order = `rng.shuffle` re-drawn until it differs from the left order AND no pair sits on the same row (position leak; the K-225 idiom); seed locale-neutral (same left order, same right permutation ×11).
**verify():** stamps `data-lcs-symbol=<key>` per left card, `data-lcs-word=<key>` per right card, `data-lcs-pairs`. Re-derived: left key set === right key set === `d.pairs` keys of `WEATHER_SYMBOLS`; left order !== right order and no index fixed; every left card has ONE `img` whose `src` file === `WEATHER_SYMBOLS[key].noun` (dir `weather`, never a `bw` dir); every right card text === `symbolWords[key]` of the locale (node cross-check) and no right card carries an `img`; no text on a left card; picture >= 56 px (K floor) and the word >= 18 px; no `[data-lcs-answer]` anywhere; no gear key on the page.
**Refusals:** none (6 nouns per locale, authored by the panel; measured letter counts in C). Words are the weather NOUNS: de Sonne · Wolke · Regen · Gewitter · Schnee · Regenbogen (the panel's list, verbatim); the validator rejects any `symbolWords[key]` equal (case-insensitive) to the vocab adjective `sunny / cloudy / rainy / stormy` of that locale (Sonnig, Soligt, Ensoleillé, Aurinkoinen are citation forms, the lock).
**Query face:** the bare compound head: "weather symbols worksheet kindergarten" · de "Wettersymbole Klasse 1" · nl "weersymbolen werkblad" · fr "le temps qu'il fait maternelle" · es "el estado del tiempo preescolar" · sv "vädersymboler" · fi "säätilat / säämerkit".

### F1: Write the Weather Word (G1, G1-3xx TBD, CODE `layout:'write'`)
**Move:** RETRIEVE the weather word from the symbol alone and WRITE it into letter boxes; the box count is the only scaffold (no bank at d2). Differs from G1-244 (picture-card dictation on any theme, 24 px boxes) because the cue is one of six fixed SYMBOLS and the words are the panel's weather nouns, never the vocab singular; differs from the base because there is nothing to copy.
**Child:** "Look at each weather symbol. Write its word, one letter in each box." (69)
**Params:** d1 `{layout:'write', rows:4, bank:true, box:48, gap:6, maxLetters:8}` · **d2** `{layout:'write', rows:6, bank:false, box:44, gap:4, maxLetters:11, hyphenGiven:true}` · d3 `{layout:'write', rows:6, bank:false, boxes:false, ruled:true, glyphH:28}` (plain school lines, the G1-244 d3 idiom). **Letter counts of the proposed nouns (m, NFC code points, hyphens excluded), order sun cloud rain storm snow rainbow:** en 3 5 4 5 4 7 · de 5 5 5 8 6 10 · es 3 4 6 8 5 8 (arcoíris one word, RAE) · pt 3 5 5 10 4 8 (+1 printed hyphen: arco-íris = 4 + 4) · fr 6 5 5 5 5 9 (+2 printed hyphens: arc-en-ciel = 3 + 2 + 4) · it 4 6 7 9 4 10 · nl 3 4 5 6 6 9 · sv 3 4 4 4 3 8 · da 3 3 4 6 3 7 · no 3 3 4 6 3 7 · fi 7 5 4 7 4 11. Widest row: fi `sateenkaari` 11 boxes = 11 × 44 + 10 × 4 + 2 = **526 px** (m, the `letterBoxes` formula) + symbol 64 + gap 12 = 602 <= 639 lane. **The fr hyphen rule (the lock): hyphens are GIVEN, printed as separators between box groups; the child writes letters only** (`hyphenGiven:true` splits the literal on `-`, renders each group with `letterBoxes({n:group})` and a 12 px printed hyphen between groups; `data-lcs-letterboxes` = the letter total 9, `data-lcs-groups="3,2,4"`). No refusal anywhere; a 3-box-per-hyphen rendering is rejected (a child would write a letter into a hyphen box). Special letters count ONE box each: sv å / ö, da / no ø, fi ä. 6 rows × 64 + 5 × 16 = 464 <= 722.
**verify():** stamps `data-lcs-spell=<key>` + `data-lcs-letterboxes=<n>` (+ `data-lcs-groups`) per row; re-derived: `n === [...symbolWords[key].replace(/-/g,'')].length`, groups === the split of the literal on `-`; no word printed anywhere (no `[data-lcs-bank-banner]` at d2, no text but the row badge); each row has one `img` (the symbol, dir `weather`, file === noun); six distinct keys; box side >= 44 (this type asserts its own floor; G1-244 ships 24, m); every key from `WEATHER_SYMBOLS`.
**Refusals:** none (every literal is a single token or a hyphenated token; the validator rejects a literal with a space, e.g. a panel writing "gota de lluvia" instead of "lluvia").
**Query face:** "weather words worksheet 1st grade" (en WINNABLE) · de "Wetterwörter schreiben Klasse 1" · fr "vocabulaire de la météo cp" · es "palabras del estado del tiempo primer grado" · nl "weerwoorden schrijven groep 3" · sv "väderord skriva" · fi "sääsanat kirjoita".

### F2: My Weather Chart, Monday to Sunday (K, K-3xx TBD, CODE `layout:'diary'`; OPEN-ENDED, no verify)
**Move:** OBSERVE and RECORD: every day for a week the child looks outside and DRAWS the matching symbol into that day's box, copying from a six-symbol legend printed on top. The "Wetterkalender / weerkalender / väderdagbok / tabella per registrare / relógio do tempo" genre; the en "weather chart printable for 7 days" query.
**Child:** "Every day this week, look outside and draw the weather symbol for that day." (77)
**Params:** d1 `{layout:'diary', days:5, start:'mon', legend:6, legendPx:56, rowH:96}` (the school week) · **d2** `{layout:'diary', days:7, start:'mon', legend:6, legendPx:56, legendWords:true, rowH:78, drawW:200}` · d3 `{layout:'diary', days:7, start:'mon', legend:6, rowH:72, wordLane:true}` (a writing lane beside the draw box: the child writes the word too). Rows: `dayNames` from `data/b2/calendar.js NAMES[loc]` (7 ×11, m) as GIVEN labels in Mon-Sun order (the lock: the school week starts the diary in all 11; `start:'mon'` is a data flag `diaryStart`, an en / pt panel may set `'sun'` per its `weekStart`, no code change); the legend = the six symbols at 56 px with `symbolWords` under them. Stack *est.*: legend 96 + 10 + 7 × 78 + 6 × 8 = 700 <= 722; rows `minmax(72px,1fr)` give 96 + 10 + 7 × 72 + 48 = 658 <= 677 (the fi 4-line title). Widest label pt `segunda-feira` / `quarta-feira` at 20 px ~ 135 *est.* in a 180 px label cell.
**verify():** STRUCTURE ONLY (open-ended): 7 rows with `data-lcs-day=<0..6>` whose label text === `dayNames[(start + i) % 7]` of the locale; each row has exactly one empty `[data-lcs-open="draw"]` box >= 72 × 72 with no `img` and no text; the legend has 6 `[data-lcs-symbol]` with distinct keys, each `img` >= 56 px; no `[data-lcs-answer]`; no numeral on the page (no dates: G2-277). Nothing is graded.
**Refusals:** none (day names exist ×11; the lock says no verify). No month grid, no dates, no temperature column (K-211 / G2-277 / G3-345 stay clear).
**Query face:** "weather chart printable 7 days" (en, 43 of 129 strings start "weather chart") · de "Wetter beobachten Arbeitsblatt Klasse 1" (WINNABLE) · nl "het weer bijhouden werkblad" · it "tabella per registrare il tempo atmosferico" (verbatim demand) · sv "väderdagbok" · pt "como está o tempo hoje" (the EI ritual) · fr "la météo du jour".

### F3: How Warm Is It? (K, K-3xx TBD, CODE `layout:'thermometer'`)
**Move:** READ an instrument QUALITATIVELY: each card shows a thermometer whose mercury stands in one of three tinted bands (low = cold, middle = warm, high = hot; no numerals) and three gear chips; the child CIRCLES the chip that fits (cold = scarf · warm = T-shirt · hot = sunglasses). Distinct from G3-345 because no value is read or written and no scale numeral exists; distinct from K-211 because nothing is sorted and no K-211 item is a chip; distinct from K-207 because the cue is an INSTRUMENT, not a season word, and the chips are a fixed triple.
**Child:** "Look at each thermometer. Is it cold, warm or hot? Circle the picture that fits." (76)
**Params:** d1 `{layout:'thermometer', items:2, bands:['cold','hot'], chips:['scarf','sunglasses'], thermH:280}` (two bands, two chips: still a task) · **d2** `{layout:'thermometer', items:4, cols:2, rows:2, bands:['cold','warm','hot'], chips:['scarf','t-shirt','sunglasses'], thermH:240, chipPx:64, min:-10, max:40, step:5, bandEdges:[5,20]}` · d3 `{items:6, cols:3, rows:2, thermH:200, chipPx:56}`. Mercury value = `rng.pick` from the band INTERIOR, never an edge: cold {-10, -5, 0} · warm {10, 15} · hot {25, 30, 35} (the primitive accepts `min:-10`, m; the value is stamped, never printed). Band tints are palette TOKENS, not the answer: cold `tealSoft`, warm `creamDeep`, hot `coralSoft`; the MONO-SAFE signal is the mercury HEIGHT against two thick band-edge ticks (the design agent draws them at 5 and 20); the chip pictures carry no band colour. Composition: 4 items over 3 bands = every band at least once, one band twice, never the same band on both cards of a row; chip order per card `rng.shuffle`, not the same triple order on every card. Card *est.*: thermometer 240 tall + a chip column 3 × 64 + 2 × 12 = 216; card h ~292; 2 × 292 + 12 = 596 <= 722.
**verify():** stamps `data-lcs-band=<cold|warm|hot>` per card, `data-lcs-value` from the primitive, `data-lcs-chip=<gearKey> data-lcs-correct="0|1"`. Re-derived: `band === bandOf(value, bandEdges)` (the gate carries its OWN band table); the value strictly inside the band; exactly one correct chip and `GEAR_BAND[chip] === band`; the three chips distinct and === `d.chips` as a set; **no `<text>` element inside any thermometer SVG and no digit anywhere in the body** (the live `thermometer.js` prints a numeral at every tick: a poison); chips >= 56 px, `img` complete, dirs `weather` / `clothing`; the band multiset over the page covers all three; no two cards in one row share a band; no gear WORD printed.
**Refusals:** none (pictures are global; the instruction is the only language). The panel's fi phrasing "Väritä lämpömittari oikeaan kohtaan." describes the INVERSE move (picture given, colour the mercury): recorded as a non-face below, not a level.
**Query face:** "temperature worksheet kindergarten hot warm cold" · fr "la température ce1" (measured verbatim) · de "Thermometer Klasse 1 kalt warm heiß" · es "termómetro preescolar frío calor" · sv "termometer kallt varmt" · fi "lämpömittari kylmä lämmin kuuma".

### F4: The Water Cycle: Label the Diagram (G3, G3-3xx TBD, CODE `layout:'water-cycle'`; TITLED FROM THE WATER-CYCLE HEAD, the lock)
**Move:** LABEL a process diagram: one drawn scene (sun over sea at left, a cloud above, a mountain at right, a river running back to the sea) with FOUR numbered coral arrows in cycle order (1 water rises from the sea · 2 the cloud forms · 3 rain falls on the mountain · 4 the river runs back) and an empty label lane beside each number; a chip bank of the four stage words on top (shuffled, never in cycle order); the child copies each word to its arrow. G3 band in 9 of 11 (de 3. Klasse, es 3º, pt 3º-5º, fr CE2, it terza, sv åk 2-3, da 3.-4., fi 3.-4.), above band in nl (groep 5-6) and no (4. trinn), code-less in en: every landing states its own placement.
**Child:** "Read the four words in the bank. Write each one on the line next to the right arrow." (84)
**Params:** d1 `{layout:'water-cycle', stages:4, given:1, laneW:200, laneH:56, glyphH:30}` (arrow 1 pre-labelled as a worked example) · **d2** `{layout:'water-cycle', stages:4, given:0, laneW:200, laneH:56, glyphH:30, diagramH:540, bank:true}` · d3 `{layout:'water-cycle', stages:4, given:0, bank:false, boxes:true}` (no bank: letter boxes sized from the locale literal; de `Kondensation` 12, fi `tiivistyminen` 13 boxes at 36 = 492 px, the G23 floor 36). Stage KEYS are fixed and global: `evaporation · condensation · precipitation · collection`; the stage-4 LITERAL is the locale's own school word (runoff / Abfluss / avrinning / valunta are as valid as "collection": the arrow shows a river running back to the sea, which either word names). Stack *est.*: `wordBank` 59 + 10 + diagram 540 = 609 <= 722 (the diagram absorbs slack; lanes sit at the diagram's edge like K-354's label stage).
**verify():** stamps `data-lcs-stage=<key>` + `data-lcs-n=<1..4>` per lane, `data-lcs-bank=<key>` per chip, `data-lcs-arrow=<key>` per SVG arrow group. Re-derived: lane keys === bank keys === the 4 stage keys (bijection); arrows 1..4 sit in cycle order clockwise around the scene (sea-left, cloud-top, mountain-right, river-bottom: geometry asserted from the primitive's returned anchors); bank order !== cycle order and !== reversed; lanes empty (no text, no SVG `<text>`); every bank literal === `cycleLabels[key]` of the locale; no `<img>` on the page (the diagram is palette SVG only); the diagram >= 480 px tall; `given` lanes (d1) print exactly the literal of their key.
**Refusals:** none (four G3-register literals exist in every curriculum: de Verdunstung / Kondensation / Niederschlag / (Sammlung | Abfluss) · es evaporación / condensación / precipitación / (recolección | escurrimiento) · pt evaporação / condensação / precipitação / (coleta | infiltração) · fr évaporation / condensation / précipitations / ruissellement · it evaporazione / condensazione / precipitazione / raccolta · nl verdamping / condensatie / neerslag / afstroming · sv avdunstning / kondensation / nederbörd / avrinning · da fordampning / fortætning / nedbør / afstrømning · no fordamping / kondensasjon / nedbør / avrenning · fi haihtuminen / tiivistyminen / sadanta / valunta; candidates for the panel, which owns the stage-4 word, OPEN 4). The face's meta says G3 in every locale (the water-cycle labels are G3 register, the lock).
**Query face:** "water cycle worksheet label" (en, probe 109) · de "Wasserkreislauf Arbeitsblatt beschriften" (a July §1 gap) · es "el ciclo del agua para imprimir" (probe 117, the largest cell) · fr "le cycle de l'eau ce2" (94) · it "ciclo dell'acqua scuola primaria" (60) · pt "ciclo da água atividade" (50) · nl "waterkringloop werkblad" (24) · sv "vattnets kretslopp" (24) · no / da "vannets kretsløp" / "vandets kredsløb" (13 / 13) · fi "veden kiertokulku" (11).

### F5: Read the Weekly Forecast (G1, G1-3xx TBD, CODE `layout:'forecast'`; REPLACES "circle the true sentence" per the lock)
**Move:** READ a symbol chart to ANSWER a question: a GIVEN Monday-to-Friday strip (day name over a symbol) and three questions, each a whole literal ("Which day is rainy?") followed by five day chips; the child circles the day. A data-reading move (1.MD.C.4-adjacent, NOT claimed: nothing is organised or counted), language-light, buildable ×11 with the calendar bank; distinct from G1-146 because nothing is counted, from G1-341 because there is no true / false, from G2-277 because there is no month grid and no date.
**Child:** "Look at the weather for the week. Read each question and circle the right day." (79)
**Params:** d1 `{layout:'forecast', days:5, asks:2, chipMode:'abbr'}` · **d2** `{layout:'forecast', days:5, asks:3, symbolPx:72, chipMode:'abbr', chipH:44, distinctStrip:true}` · d3 `{layout:'forecast', days:5, asks:4, chipMode:'full', twoOfOne:true, askStyle:'not'}` (one symbol twice + a "which day is NOT sunny?" ask: a genuinely harder read, not shipped). **Single answer by construction:** the strip = 5 DISTINCT symbols sampled from the 6 (`distinctStrip:true`), so every ask has exactly one day; asks = 3 of the 5 strip keys, `rng.sample`, in strip-independent order (the asked days never read Mon, Tue, Wed in order: re-drawn). Day labels: strip headers = `dayNames[1..5]` (Monday..Friday, m ×11; pt `segunda-feira` ~100 px *est.* at 18 px in a 127 px column), chips = `dayAbbr[1..5]` (en Mon..Fri, de Mo..Fr, fi ma..pe; m); the chip text is the calendar's, never authored. Stack *est.*: strip 150 + 3 × (ask 26 + 8 + chips 44 + 16) = 150 + 282 = 432 <= 722.
**verify():** stamps `data-lcs-day=<1..5> data-lcs-symbol=<key>` per strip column, `data-lcs-ask=<key>` per question, `data-lcs-chip=<day>` per chip. Re-derived: strip keys distinct and ⊂ `WEATHER_SYMBOLS`; each ask key occurs EXACTLY once in the strip; the answer set (days) is not in ascending order and not all the same; every question text === `forecastAsks[key]` of the locale and contains NO day name of the locale (a question naming a day is the answer printed); every chip text === `dayAbbr[day]`, five per question, chips >= 44 px tall; headers === `dayNames[day]`; strip pictures >= 56 px, dir `weather`, file === noun; no `[data-lcs-answer]`.
**Refusals:** none (needs `dayNames` + `dayAbbr` ×11, m, and 6 `forecastAsks` literals per locale, each the full question with the weather word INFLECTED inside it: de "An welchem Tag regnet es?", fr "Quel jour pleut-il ?", fi "Minä päivänä sataa?"; verb-phrase asks are the panel's, the lock's example banks: de "Die Sonne scheint.", es "hace sol / llueve", fr "il pleut / il neige"). The word "forecast" is a title word only; no ask prints a temperature or a count.
**Query face:** "weather forecast worksheet 1st grade" · de "Wetterbericht lesen Klasse 1" · nl "weerbericht lezen groep 3" · fr "la météo de la semaine cp" · sv "läsa väderprognos" · fi "sääennuste lue".

**Rejected non-moves.** "Circle the true sentence" (G1-341, the lock) · count the symbols into a table / graph (G1-146 / G1-147) · a month grid with symbols (G2-277) · sort pictures into hot / cold or summer / winter (K-211 / K-207) · match season to weather (K-322) · read and write a temperature (G3-345) · a wind / fog / hail symbol face (no art) · the inverse thermometer (picture given, colour the mercury: a good classroom move but its answer is a pencil fill the gate cannot re-derive beyond the band; recorded for a later PARAM if the operator wants it) · a theme swap (fixed theme) · relabelling d1 / d3 of the base as a face (same move at another count; fails `gate-variation-distinct`) · alternates (`sunny`, `cloudy`, `raindrop`, `stormy`) as extra pairs (a content swap of one act; two pictures of one concept on a page also prints two answers).

## C. Native rebuild ×11

Frames NEVER inflect: this type has NO cloze frame and reads NO `objForms` (m: 0 weather nouns in en / de / es / pt / fr / it / nl / sv / da / no; fi 8, unused). Per locale the panel authors: `strings` (6 titles + 6 instructions; the F4 title carries the WATER-CYCLE head, never the weather head), `symbolWords` (6 weather NOUNS, whole literals, the capital / article / gender as the locale prints a chart label; de capitalised, fr / es / pt / it bare noun, nl bare, sv / da / no INDEFINITE bare form), `bankWords` (6, optional, the d3 article form; must END with `symbolWords[key]`), `forecastAsks` (6 full questions, one per symbol, the weather word inflected inside), `cycleLabels` (4 G3 stage literals), `bandWords` (3: cold / warm / hot, landing prose + d3 only, never printed at d2), `diaryStart` (`'mon'` default), `refuse` per face. Day names come from `calendar.js` (0 new). = **31 literals per locale** (+ 6 optional `bankWords`). The EN is a SOURCE TO AUDIT (`enAudit`).

| loc | literals (counts) | forms / slots and where they exist | refusal / re-target | traps |
|---|---|---|---|---|
| en | 12 strings · 6 symbolWords (sun cloud rain storm snow rainbow) · 6 forecastAsks ("Which day is rainy?") · 4 cycleLabels · 3 bandWords = 31 | `calendar.js en` (Sunday-first table; the diary reads Mon-Sun by `diaryStart`) | none | US spelling; "printable" beats "worksheet" in the K metas; title never "Weather" alone (theme name, m); "weather chart" is the F2 head; "with answers" may not be promised (printable-only decks ship no key, README item 6) |
| de | 12 · 6 (Sonne Wolke Regen Gewitter Schnee Regenbogen: the panel's list; capital nouns) · 6 ("An welchem Tag scheint die Sonne?" / "regnet es?" / "schneit es?" / "gibt es ein Gewitter?" / "ist es bewölkt?" / "gibt es einen Regenbogen?") · 4 (Verdunstung Kondensation Niederschlag + the panel's stage 4) · 3 (kalt warm heiß) = 31 | none | none | Gewitter (n) vs Regen (m) only matters in `bankWords`; `Regenbogen` 10 boxes fits (m); "Wind / Nebel / Niederschlag" sub-symbols have no art (out); engine appends "Arbeitsblatt"; title never "Wetter" |
| es (MX) | 12 · 6 (sol nube lluvia tormenta nieve arcoíris) · 6 ("¿Qué día hace sol?" / "llueve" / "nieva" / "hay tormenta" / "está nublado" / "hay arcoíris") · 4 (evaporación condensación precipitación + stage 4) · 3 (frío templado calor) = 31 | none | none | title "El estado del tiempo" (SEP); never "clima" in a title (theme slug + climate), "el tiempo" alone = time; `arcoíris` one word, 8 boxes (m); `lluvia` never "gota de lluvia" (space = validator fail); "hace" verb frames only inside `forecastAsks`; `tercer-grado` on F4 |
| pt (BR) | 12 · 6 (sol nuvem chuva tempestade neve arco-íris) · 6 ("Em que dia faz sol?" / "chove" / "neva" / "tem tempestade" / "está nublado" / "tem arco-íris") · 4 (evaporação condensação precipitação + stage 4) · 3 (frio ameno quente) = 31 | none | none | title "Como está o tempo hoje" UNMEASURED (OPEN 1: re-probe; "tempo atmosférico" in meta); never "clima" (BNCC 5º ano distinction); `arco-íris` = 4 + 4 boxes with a printed hyphen; `tempestade` 10 (m); "atividade" never in a title; `3o-ano` on F4 |
| fr | 12 · 6 (soleil nuage pluie orage neige arc-en-ciel) · 6 ("Quel jour fait-il beau ?" / "pleut-il ?" / "neige-t-il ?" / "y a-t-il de l'orage ?" / "est-il nuageux ?" / "y a-t-il un arc-en-ciel ?"; NBSP before `?`) · 4 (évaporation condensation précipitations ruissellement) · 3 (froid doux chaud) = 31 | none | none | `arc-en-ciel` = 3 + 2 + 4 boxes, two printed hyphens (the lock's rule, no refusal); keep OUT "la météo des émotions" (SEL) and "la météo du comportement"; "fiche" never in a title; "la température ce1" = the F3 meta; `ce2` on F4 |
| it | 12 · 6 (sole nuvola pioggia temporale neve arcobaleno) · 6 ("Che giorno c'è il sole?" / "piove" / "nevica" / "c'è il temporale" / "è nuvoloso" / "c'è l'arcobaleno") · 4 (evaporazione condensazione precipitazione raccolta) · 3 (freddo mite caldo) = 31 | none | none | "meteo" NEVER in a title (theme slug), "il tempo" alone = time; `arcobaleno` 10, `temporale` 9 (m); "scheda" never in a title; the thinnest market: expect C-tier traffic |
| nl | 12 · 6 (zon wolk regen onweer sneeuw regenboog) · 6 ("Op welke dag schijnt de zon?" / "regent het?" / "sneeuwt het?" / "onweert het?" / "is het bewolkt?" / "is er een regenboog?") · 4 (verdamping condensatie neerslag afstroming) · 3 (koud warm heet) = 31 | none | F4 above band: the landing says groep 5 (the `groep-5` key) | `bankWords` "de zon, de regen, de wind, de sneeuw, het onweer" (the panel's list; wind has no art); `regenboog` 9 (m); IJ absent from the six; engine appends "werkblad"; title never "Weer" |
| sv | 12 · 6 (sol moln regn åska snö regnbåge; indefinite, never "vädret") · 6 ("Vilken dag skiner solen?" / "regnar det?" / "snöar det?" / "åskar det?" / "är det molnigt?" / "syns en regnbåge?") · 4 (avdunstning kondensation nederbörd avrinning) · 3 (kallt varmt hett) = 31 | none | none | definite "vädret" is the forecast register (keep off the sheet); no month grid (`calendar` "Kalendern" / `seasons` "De fyra årstiderna" are neighbours); `regnbåge` 8 (m); engine appends "arbetsblad"; `[NSR]` |
| da | 12 · 6 (sol sky regn torden sne regnbue) · 6 · 4 (fordampning fortætning nedbør afstrømning) · 3 (koldt varmt hedt) = 31 | none | none | "vejret opgaver" is the demand phrase (meta); `torden` 6 (m); engine appends "opgaver"; `[NSR]` |
| no | 12 · 6 (sol sky regn torden snø regnbue; bokmål) · 6 · 4 (fordamping kondensasjon nedbør avrenning) · 3 (kaldt varmt hett) = 31 | none | F4 above band: the `4-trinn` key IS the panel's placement (honest by construction) | "vær" is also the imperative of "være": title "Værsymboler" avoids the bare word; `regnbue` 7 (m); engine appends "oppgaver"; `[NSR]` |
| fi | 12 · 6 (aurinko pilvi sade ukkonen lumi sateenkaari) · 6 ("Minä päivänä paistaa aurinko?" / "sataa?" / "sataa lunta?" / "on ukkonen?" / "on pilvistä?" / "näkyy sateenkaari?") · 4 (haihtuminen tiivistyminen sadanta valunta) · 3 (kylmä lämmin kuuma) = 31 | none | none | `[NSR-FLAG][fi]` h1 "Säätilat" vs "Säämerkit" / "Sääsymbolit" (OPEN 2); `sateenkaari` 11 boxes = 526 px fits (m); instruction cases are the panel's ("Yhdistä säämerkki oikeaan sanaan." illative); the 4-line fi title = the 677 budget on F2 (658 fits by arithmetic, engineer re-measures); `[NSR]` |

Every panel OPENS the 6 concept pictures + the 3 gear chips in its own render (the sv #35 `fruits/plum` precedent) and reads one render of every face in its locale.

## D. Data + gates

`data/b4/weather-symbols.json` (GLOBAL, one file) + `data/b4/weather.js` (EN literals) + `data/b4/locales/weather.<loc>.json` GENERATED from `i18n/.draft-b4-<loc>.json` by `tools/apply-b4-locale.js` after `tools/validate-b4-draft.js` (the b3 pair exists, m; `data/` gitignored, `git add -f`); read through `lib/b4-common.js bank('weather', loc)` (README open item 3).

```
WEATHER_SYMBOLS (data/b4/weather-symbols.json):
  symbols: [ {key:'sun',     theme:'weather', noun:'sun',          alt:['sunny'],    picOpened:true},
             {key:'cloud',   theme:'weather', noun:'cloud',        alt:['cloudy'],   picOpened:true},
             {key:'rain',    theme:'weather', noun:'rainy',        alt:['raindrop'], picOpened:true},
             {key:'storm',   theme:'weather', noun:'thunderstorm', alt:['stormy'],   picOpened:true},
             {key:'snow',    theme:'weather', noun:'snowflake',    alt:[],           picOpened:true},
             {key:'rainbow', theme:'weather', noun:'rainbow',      alt:[],           picOpened:true} ]
  excluded: ['hot','cold','mittens','umbrella','raincoat','puddle','thermometer','partly_cloudy','rain_boots']
  gear:     [ {key:'scarf',      theme:'weather',  noun:'scarf',      band:'cold', picOpened:true},
              {key:'t-shirt',    theme:'clothing', noun:'t-shirt',    band:'warm', picOpened:true},
              {key:'sunglasses', theme:'weather',  noun:'sunglasses', band:'hot',  picOpened:true} ]
  bands:    { order:['cold','warm','hot'], min:-10, max:40, step:5, edges:[5,20],
              values:{cold:[-10,-5,0], warm:[10,15], hot:[25,30,35]}, tints:{cold:'tealSoft', warm:'creamDeep', hot:'coralSoft'} }
  cycle:    ['evaporation','condensation','precipitation','collection']   // arrows 1..4 clockwise: sea-left, cloud-top, mountain-right, river-bottom
  diary:    { days:7, start:'mon' }      forecast: { days:[1,2,3,4,5] }   // calendar.js indices (Mon..Fri)
WEATHER[loc] = {
  symbolWords:  { sun, cloud, rain, storm, snow, rainbow },            // whole NOUN literals; never a vocab adjective; single token or hyphenated
  bankWords:    { ...6 } | {},                                          // d3 article forms; must END with symbolWords[key]
  forecastAsks: { sun, cloud, rain, storm, snow, rainbow },            // full questions; contain no day name
  cycleLabels:  { evaporation, condensation, precipitation, collection },
  bandWords:    { cold, warm, hot },                                    // landing prose + d3 only
  diaryStart:   'mon' | 'sun',
  refuse:       [],                                                     // 'write'|'diary'|'thermometer'|'water-cycle'|'forecast'
  strings:      { 'K-356':{title, instruction}, <F1..F5 ids>:{title, instruction} } }
```
Helper contract: `bank('weather', loc)` returns the merged object; `symbolWord(loc, key)`, `askFor`, `cycleLabel` throw on a missing literal (never fall back to the vocab); `bandOf(value)` and `gearBand(key)` read the global file.

**`tools/validate-b4-draft.js` (weather block; every rule runs, exit 1 on any):**
1. `symbols` = exactly the 6 keys; every `noun` resolves via `fileUri('weather', noun)` (throws on a missing cache entry), has a vocab entry in all 11 locales, is not in `excluded`, has `picOpened:true`; no `alt` noun appears as a `noun`; `excluded` ∩ (nouns ∪ gear nouns) = ∅.
2. `gear` = exactly 3 with distinct `band` covering `bands.order`; every gear noun resolves via `fileUri(theme, noun)` and has vocab ×11; no gear noun in `symbols`; `mittens`, `umbrella`, `raincoat`, `sun`, `snowflake` never in `gear` (the K-211 / K-207 / rain fence).
3. `bands.values[b]` all strictly inside their band (no value equals 5 or 20; none outside min..max); tints are token NAMES; `cycle` = the 4 fixed keys in order.
4. `symbolWords[key]` non-empty, matches `/^[\p{L}][\p{L}'’-]*$/u` (no space; rejects "gota de lluvia"), 6 pairwise distinct (`toLocaleLowerCase(loc)`), and NEVER equal (case-insensitive) to `vocab[sunny|cloudy|rainy|stormy][loc][0]` (the adjective ban) nor to `vocab[raindrop|snowflake|thunderstorm][loc][0]` (the panel's noun rule: Regen not Regentropfen; the rule exempts the keys whose concept noun IS the vocab noun: sun, cloud, rainbow, so en "rainbow" === vocab is legitimate).
5. `[...symbolWords[key].replace(/-/g,'')].length <= 11` (the F1 lane, 526 px measured) else `refuse` names `write`; a hyphenated literal yields groups of >= 1 letter each.
6. `forecastAsks[key]` non-empty, ends with `?` (or `?` after NBSP in fr), contains NO `dayNames` / `dayAbbr` token of the locale (word-boundary via `(?<!\p{L})…(?!\p{L})`, never `\b`), no digit, <= 70 chars; 6 distinct.
7. `cycleLabels` 4 non-empty, distinct, no space unless declared `multiWord:true` (then d3 letter boxes are refused for that locale, never faked); `bandWords` 3 distinct.
8. `bankWords[key]` (if present) ends with `symbolWords[key]` (case-insensitive).
9. Titles <= 70, no worksheet-word (`WORKSHEET_WORD` regex of the b3 gate), unique in the band, NEVER equal (case-insensitive) to `axes.theme.weather.name.<loc>` or `.slug.<loc>` and never the bare theme word as the whole first token in es / pt / it ("Clima", "Meteo"); the F4 title must NOT contain the base head's first token and MUST contain the locale's water-cycle head token (de "Wasserkreislauf", en "Water Cycle"); the F3 title must not equal G3-345's title of the locale ("Hot or Cold?" en); instructions <= 150; no visible string contains the locale's free-word; no visible string on F3 contains a digit or a degree sign.
10. `strings` ids === {base + the 5 allocated face ids}; the six titles differ pairwise by at least one NOUN or verb token (never only an adjective); `diaryStart` ∈ {mon, sun}.

**Poison (each must FAIL for its OWN reason; the correct EN draft is the control):**
P1 `symbols[2].noun = 'puddle'` -> rule 1 (excluded). P2 `symbols += {key:'wind', noun:'windy'}` -> rule 1 (7 keys / no cache entry). P3 `gear += {key:'mittens', band:'cold'}` -> rule 2. P4 `bands.values.warm = [5,15]` -> rule 3 (edge value). P5 de `symbolWords.sun = 'Sonnig'` -> rule 4 (adjective). P6 de `symbolWords.rain = 'Regentropfen'` -> rule 4 (vocab singular, not the weather noun). P7 es `symbolWords.rain = 'gota de lluvia'` -> rule 4 (space). P8 fi `symbolWords.rainbow` = a 12-letter variant -> rule 5. P9 en `forecastAsks.rain = 'Is it rainy on Monday?'` -> rule 6 (day name = the answer). P10 fr `forecastAsks.sun` without `?` -> rule 6. P11 sv title `'Väder'` -> rule 9 (theme name). P12 en F4 title `'Weather Symbols: The Water Cycle'` -> rule 9 (base head token on the water-cycle face). P13 en F3 title `'Hot or Cold?'` -> rule 9 (G3-345 collision). P14 en F3 instruction `'Circle the picture for 20 °C'` -> rule 9 (digit / degree). P15 F1 title `'Weather Symbols: Big Words'` -> rule 10 (adjective-only difference). Control: the EN draft passes 1-10.

**`qa/verify-b4-weather.js` (render gate; clone the b3 shape):** renders base + 5 faces × 11 locales × d2 through `render/render-instance.js` (file:// fonts) under the 722 stack (3-line de title + 150-char instruction) AND the 4-line fi title (677); asserts `verify()` empty and `qa/lints.js` clean, then the floors ITSELF (no size lint exists): symbol pictures >= 56 (K faces) / >= 56 on the F5 strip, word cards >= 18 px, letter boxes >= 44 (F1), diary draw boxes >= 72 × 72, gear chips >= 56, forecast chips >= 44 tall, the water-cycle diagram >= 480 tall with every arrow group present, lanes empty, the thermometer SVG contains no `<text>` and the body no digit (F3), no `[data-lcs-answer]` with a non-empty value, every printed literal === the locale bank (node cross-check), no `img` from a `bw` dir, everything above the footer, titles <= 2 lines in de / fi. SWEEP: 20 seeds × d2 per face render distinct pages (base right-column order varies and never equals the left; F1 row order varies; F3 band assignment + chip order vary; F5 strip set + asks vary); the diary (F2) is seed-invariant by design (asserted identical). **Render poisons (each must FAIL):** PR1 base right column in left order -> position leak · PR2 base with `raindrop` swapped in beside `rainy` -> "two pictures of one concept" · PR3 F1 a `wordBank` injected -> "bank present" · PR4 F1 fr `arc-en-ciel` rendered as 11 boxes (hyphen boxes) -> `groups` mismatch · PR5 F2 a row printing a date numeral -> "numeral on the diary" · PR6 F3 built with the live `thermometer.js` (numeral labels) -> "text inside the thermometer" · PR7 F3 mercury at value 20 -> "value on a band edge" · PR8 F3 two cards of one row both `hot` -> row rule · PR9 F3 a `mittens` chip -> gear fence · PR10 F4 bank in cycle order -> "bank order = cycle" · PR11 F4 a lane pre-filled with "evaporation" -> "lane not empty" · PR12 F5 strip with `rain` twice -> "ask has two days" · PR13 F5 an ask literal containing "Monday" -> "day name in the question" · PR14 F5 chips labelled by the panel instead of `dayAbbr` -> "chip text ≠ calendar" · PR15 `answerBox({w,h})` in place of an open lane -> `data-lcs-answer="undefined"` · PR16 base d2 with `pairs:8` resolved from a level-index guard -> the config guard fires before render.

Page reads at render: `WEATHER[loc]`, `WEATHER_SYMBOLS`, `calendar.js NAMES[loc]`, `fileUri`, the band thermometer + `water-cycle.js` primitives; never `image-vocabulary.js`, `articles.js`, `objForms` or `approved-words-*.json`.

## E. SEO

`coordinate = {type:'weather', mode:<face>, theme:'', level:<band key of the locale>}`; modes: base `'base'` · F1 `'write'` · F2 `'diary'` · F3 `'thermometer'` · F4 `'water-cycle'` · F5 `'forecast'`. Levels: base / F2 / F3 = the K key; F1 / F5 = the G1 key; F4 = the G3 key (`LEVEL_KEYS`, m; a locale panel publishing F3 at G1 swaps the key in its landing file only). Meta lead inherits `seo.words.free_printable` (metadata only, never visible copy); the MIDDLE is the instruction stem; the per-locale window is measured with `tools/measure-instruction-window.js` (exists, m); en middles below sit at 52-90 chars.

| face | title pattern Germanic (en / de / nl) | Romance (es / pt / fr / it) | Nordic + fi (sv / da / no / fi) | meta MIDDLE (en) |
|---|---|---|---|---|
| base | Weather Symbols · Wettersymbole · Weersymbolen | El estado del tiempo · Como está o tempo hoje · Le temps qu'il fait · Il tempo atmosferico | Vädersymboler · Vejrsymboler · Værsymboler · Säätilat | Draw a line from each weather symbol to its word |
| F1 | Weather Symbols: Write the Word · Wettersymbole: Wetterwörter schreiben · Weersymbolen: schrijf het woord | El estado del tiempo: escribe la palabra · Como está o tempo hoje: escreva a palavra · Le temps qu'il fait : écris le mot · Il tempo atmosferico: scrivi la parola | Vädersymboler: skriv ordet · Vejrsymboler: skriv ordet · Værsymboler: skriv ordet · Säätilat: kirjoita sana | Look at each weather symbol and write its word, one letter in each box |
| F2 | Weather Chart: My Week · Wetter beobachten: meine Woche · Het weer bijhouden: mijn week | El estado del tiempo: mi semana · Como está o tempo hoje: minha semana · La météo du jour : ma semaine · Il tempo atmosferico: la mia settimana | Väderdagbok: min vecka · Vejrsymboler: min uge · Værsymboler: min uke · Säätilat: minun viikkoni | Every day this week, look outside and draw the weather symbol for that day |
| F3 | Weather Symbols: How Warm Is It? · Wettersymbole: Wie warm ist es? · Weersymbolen: hoe warm is het? | El estado del tiempo: ¿frío o calor? · Como está o tempo hoje: frio ou quente? · La température : chaud ou froid ? · Il tempo atmosferico: freddo o caldo? | Vädersymboler: kallt eller varmt? · Vejrsymboler: koldt eller varmt? · Værsymboler: kaldt eller varmt? · Säätilat: kylmä vai kuuma? | Look at each thermometer, decide if it is cold, warm or hot, and circle the picture that fits |
| F4 | The Water Cycle: Label the Diagram · Wasserkreislauf beschriften · De waterkringloop: benoem de stappen | El ciclo del agua: escribe las etapas · O ciclo da água: complete as etapas · Le cycle de l'eau : légende le schéma · Il ciclo dell'acqua: completa lo schema | Vattnets kretslopp: skriv stegen · Vandets kredsløb: skriv trinene · Vannets kretsløp: skriv stegene · Veden kiertokulku: nimeä vaiheet | Read the four words in the bank and write each one on the line next to the right arrow |
| F5 | Weather Symbols: Read the Forecast · Wettersymbole: Wetterbericht lesen · Weersymbolen: het weerbericht lezen | El estado del tiempo: lee el pronóstico · Como está o tempo hoje: a previsão da semana · Le temps qu'il fait : la météo de la semaine · Il tempo atmosferico: leggi le previsioni | Vädersymboler: läs väderprognosen · Vejrsymboler: læs vejrudsigten · Værsymboler: les værmeldingen · Säätilat: lue sääennuste | Look at the weather for the week, read each question and circle the right day |

All titles are candidates the native panels rewrite (<= 70, no worksheet word, base = the bare compound head, each face adds one NOUN / verb, never an adjective; F4 carries the water-cycle head and NOT the weather head; the pt base title waits on OPEN 1). JSON-LD `LearningResource` with NO `educationalAlignment` on any face (no CCSS code is honest); en prose names NGSS K-ESS2-1 on base / F2 / F5 and states on F4 that the water cycle has no K-3 NGSS anchor (5-ESS2-1); every non-EN landing names the framework + the strand literal of A (the build adds the Science row to `strand-names.ts`, never the English fallback); nl F4 says groep 5, no F4 says 4. trinn, every F4 meta says G3. `topicMeta.weather` >= 50 ×11 + `skill-sentences.<loc>.json.weather` via the `register-b4-en-content.js` clone. The "Free printable" lead is metadata only.

**Non-cannibalisation** (whole-landing 3-gram Jaccard, `scripts/seo-landing/gate.js` FAIL >= 0.80 / WARN 0.65; *est.*, the gate measures):

| pair | what the copy says differently | est. |
|---|---|---|
| base vs F1 | draw lines symbol-to-word vs write the word into letter boxes; K vs grade 1 | 0.35 |
| base vs K-225 (word to picture) | a fixed six-symbol chart with weather nouns vs any-theme vocab cards; the base p1 names the six symbols by name | 0.20 |
| F1 vs G1-244 (write the word) | the six weather symbols, hyphen groups given, no picture cards; G1-244 is theme-generic | 0.20 |
| F2 vs G2-277 (calendar) / K-321 (days) | seven day rows to draw in, no dates, no month, no ordering task | 0.15 |
| F2 vs F5 | draw your own week (open) vs read a given week and circle a day | 0.30 |
| F3 vs G3-345 (read the thermometer) | cold / warm / hot bands and gear chips, no numerals, no writing a value | 0.25 |
| F3 vs K-211 (hot and cold sort) / K-207 (clothes sort) | an instrument read per card, three fixed chips, no bins, no sort; the boundary sentence names both | 0.20 |
| F4 vs any weather face | a diagram, four stage words, grade 3; the water-cycle head, not the weather head | 0.10 |
| F4 vs science-sequence G1-203 | label a cycle diagram vs order picture cards | 0.15 |
| F5 vs G1-146 / G1-147 (count into a table / graph) | nothing counted, a question answered by circling a day | 0.15 |
| F5 vs G1-341 (true or false) | no sentence is judged; the answer is a day chip | 0.15 |
| any face vs K-322 (seasons) | no season word, no month-to-season map | 0.10 |

Boundary sentence on every landing: "The child works with the six weather symbols: matches, writes, records and reads them, and reads a thermometer by its band. Sorting hot and cold things is on the Hot and Cold page, clothes on the Summer and Winter Clothes page, and reading a temperature on the Reading a Thermometer page."

## F. Open questions + summary

**Engineer must measure:** (1) The F3 thermometer: an additive `labels:false` + `bands` option on `primitives/thermometer.js` (default unchanged; `b4-baseline --check` proves G3-345 byte-identical) vs a NEW `band-thermometer.js`; either way the node gate asserts no `<text>` and the mercury `y` for every `bands.values` entry sits strictly between the two edge ticks. (2) The 722 / 677 stacks: base 6 rows (670 / 588), F2 (700 / 658), F3 4 cards (596), F4 (609 with a 540 diagram), F5 (432); the fi 4-line title on F2 is the tight one. (3) F1 row width at box 44: fi 11 boxes = 526 + 64 + 12 = 602 <= 639 (arithmetic; re-measure with the shell fonts). (4) NEW `primitives/water-cycle.js` geometry (the design agent specifies: viewBox, four arrow anchors, lane positions, a node gate that measures the RENDER per the G2-235 ruler precedent, arrows in cycle order, palette tokens only, no pictures). (5) pt `segunda-feira` / `quarta-feira` in a 127 px F5 column at 18 px (*est.* ~100). (6) The per-locale meta window per face (`measure-instruction-window.js`). (7) `verify-hub-type-rows.js` expectation: 6 rows × 11 = 66, exported to `hub-expectations.json`; poison a wrong `coordinate.type` before `apps.weather` lands.

**Only a native panel can rule:** OPEN 1 the pt base title ("Como está o tempo hoje" vs "Tempo atmosférico" vs "O tempo e o clima": re-probe per the lock before the title locks; never "clima"). OPEN 2 fi h1 "Säätilat" vs "Säämerkit" / "Sääsymbolit" `[NSR-FLAG][fi]` (the other becomes the meta). OPEN 3 the cloud picture: `cloud` (one pink faced cloud, matches a singular noun) vs `cloudy` (grey cluster, matches a plural / "bewölkt" reading) per locale, a data swap on `symbols[1].noun`, never both. OPEN 4 the stage-4 water-cycle literal per locale (collection vs runoff / infiltration: whichever the locale's grade-3 book prints for "the water runs back"). OPEN 5 `diaryStart` for en / pt (calendar.js says Sunday-first there; the lock says Mon-Sun): `'mon'` by default, the panel may flip. OPEN 6 whether F3 publishes at K or G1 per locale (fr "la température ce1" argues G1 in fr; a `level` key swap, no code change). OPEN 7 the F3 warm chip: `clothing/t-shirt` (default; cross-theme like K-322) vs a locale's preference; the fence forbids `raincoat` (rain, not warmth), `mittens`, `sun`, `snowflake`.

**Summary.** Six faces on one apparatus, all buildable in 11 locales with zero face refusals and zero per-word refusals: match the six symbols to their weather nouns (K) · write the weather word into letter boxes with given hyphens (G1) · an open seven-day weather chart (K) · read a numeral-less thermometer by its band and circle the gear (K) · label the water-cycle diagram from a four-word bank (G3, titled from the water-cycle head, above band in nl / no and code-less in en, each landing says so) · read a given Monday-to-Friday forecast strip and circle the day (G1, replacing the refused true-sentence face). One additive `layout` knob, one locale-neutral symbol file (6 keys, 3 gear chips, 3 bands, 4 cycle keys), 31 panel literals per locale, no `objForms`, no inflection, no `unitAxis` (the °C / °F trap is closed by printing no scale), day labels from `calendar.js`. The two new drawings are the band thermometer (or an additive option on the live primitive) and `primitives/water-cycle.js`. Hub rows: 66. Compound heads locked ×11 and measured collision-free (also the 11 water-cycle slugs).
