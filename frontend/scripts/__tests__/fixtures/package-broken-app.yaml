# Test fixture — broken (references nonexistent app in composedExercises).
# Validator must reject with a descriptive error message naming the bad appName.

targetSlug: count-objects-1-to-5
language: en
title:
  en: "Broken fixture — bad app name"
description:
  en: "Should fail validation."
durationMinutes: 20
structure:
  warmup: { durationMinutes: 5, body: "..." }
  contentActivity: { durationMinutes: 8, body: "..." }
  scaffold: { durationMinutes: 4, body: "..." }
  closure: { durationMinutes: 3, body: "..." }
composedExercises:
  - appName: this-app-does-not-exist
    exerciseMode: unified
    customizationParameters: {}
    ordering: 1
    pedagogicalRole: practice
materials: []
curriculumStandards: []
assessmentCriteria:
  en: "..."
recommendedDeckIds: []
recommendedPdfDeckIds: []
generatedBy: manual
