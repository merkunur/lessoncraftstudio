/** K-341 — Draw the Tree in Four Seasons. nt20-C variation of K-322 (generated by tools/gen-b3var-specs.js from tools/b3var-rows/). */
'use strict';
const base = require('./K-322-seasons.js');
// One object for all three levels: the waves ship d2 only, so a face must
// render identically whichever level is asked for. Spreading the base entry
// (not a JSON literal) carries function-valued params through intact.
const D = { ...base.difficulty[2], ...{"layout":"tree","figure":"tree","caption":false} };
module.exports = {
  ...base,
  id: 'K-341',
  slug: 'seasons-draw-the-tree',
  difficulty: { 1: D, 2: D, 3: D },
  i18n: { en: { title: "Draw the Tree in Four Seasons", instruction: "Draw what the tree looks like in each of the four seasons." } },
};
