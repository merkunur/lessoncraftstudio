# K-317 `letter-of-the-week` — BASE build record (2026-09-14)

Built from `K-317-letter-of-the-week.md` §2 + §5 under `_BUILD-BRIEF.md`, the README cross-type rulings (`unitAxis`, themeless picture index, body 722) and `_SUBSTRATE.md`. Nothing shared was edited; nothing committed.

## Files written (all type-scoped)
| file | what |
|---|---|
| `scripts/worksheet-gen/types/k/K-317-letter-of-the-week.js` | the spec: `themeAxis:{applicable:false}`, `unitAxis:{applicable:true, units, exemplar, tokens}` (tokens read `upper`/`L` off the bank block), `difficulty{1,2,3}` as a CONFIG (`hunt:{n,hits,cols,cardW,cardH,iconPx,foilPolicy,hitPos}`, `trace:{glyphH,laneH,reps}`, `write:{glyphH,laneH}`, `card:{w,h,glyphH}`), `build()` → `_buildWith(bank, …)` (the gate's poison seam; `build()` passes `b3-common.bank('letter-of-the-week', loc)`), `verify(page)` re-deriving every hit from the stamps |
| `scripts/worksheet-gen/templates/components-b3/letter-of-the-week.js` | NEW `letterCard({text,w,h,glyphH})` (returns `{html,width,…}`; width grows with `textGlyphs().width × scale`) + `huntCard({src,vocabKey,word,graphemes,hit,w,h,iconPx,rot})`. `positionCard` / `positionKey` / `letterChips` are NOT exported (Phase 2 — nothing on the base consumes them) |
| `scripts/worksheet-gen/data/b3/letter-of-the-week.js` | EN bank (gitignored — reviewer force-adds): `en = {level:'letter', positionMode:'letter', showWordInPositions:true, exemplar:'m', letters:[m,s,t,b], units:[], refuse:{}, strings:{'K-317':{title,instruction}}}` |
| `scripts/worksheet-gen/qa/verify-b3-letter-of-the-week.js` | the gate (bank validator rules 1-11 + real-pipeline renders + K floor + sweep + 6 poisons); exports `validateBank` |
| `scripts/worksheet-gen/i18n/strings.en.json` | regenerated by `node i18n/build-en.js` (see "baseline" below — the committed file was stale) |
| `docs/worksheet-gen/b3-designs/_work/K-317-build.md` | this record |

## Bank (measured with node against the real files)
Eligible en pool = approved ∩ colour-pictured ∩ `traceable()`, distinct by word: **796** (design (m) 796 ✓). Letters passing the FULL §5 floor (8 initial / 2 medial / 2 final / 6 foils): **18** (a b c d e f g h k l m n o p r s t w); i j q u v x y z fail (design's 22 counted the 4-initial + 4-foil string floor).

Authored (every picture opened on a contact sheet; every word joins `approved-words-en.json` by `key` → `word`, `split`; `graphemes` = the letters of the word; `pos` = first index of L; theme/noun pinned):

| L | initial | medial | final | foils | avoid-initial foils (d2) | pair-initial foils (d3) | pair / avoid |
|---|---|---|---|---|---|---|---|
| m (exemplar) | 16 | 8 | 6 | 20 | 13 | 7 | n / n w |
| s | 16 | 9 | 5 | 17 | 9 | 8 | c / c z |
| t | 16 | 8 | 8 | 19 | 11 | 7 | d / d f |
| b | 16 | 8 | 5 | 19 | 13 | 8 | p / p d |

Pictures swapped after opening them (the sv #35 rule): `fruits/mango` (reads as an orange) → `post office/mailbox`; `animals/moose` (reads as a deer) → `camping/mountain`; **`fruits/plum` is a red apple** (the exact sv #35 defect) → `breakfast/jam`; `accessories/ribbon` (a bow — "bow" starts with b) → `insects and bugs/ladybug`. `christmas/tree` (a decorated Christmas tree, the recorded trap) was never used. Digraph-initial words (shark, sheep, shoe, thumb, thermometer…) are never initial HITS on the s/t pages (a K "begins with the S sound" page must not show a shark); they stay legal as non-initial items / foils.

## Gate
```
node scripts/worksheet-gen/qa/verify-b3-letter-of-the-week.js
render d1 m: verify 0 lints 0 icons>=110 hits 3/6 · d2 m 100 4/8 · d3 m 100 4/8 · d2 s/t/b 100 4/8
render d1 long chrome (title 68 / instruction 150 chars): verify 0 lints 0 body 733 px
sweep: hits in 8/8 slots, 20 distinct hit sets
poison: P1 bank KILLED · P1 build KILLED (the spec refused the poisoned bank) · P1 verify KILLED · P2 KILLED · P3 KILLED · P4 KILLED · P5 KILLED · P6 KILLED (verify 0 lints 0 — neither sees it)
PASS (165 assertions, 6/6 poisons killed)
```
What the gate asserts itself (qa/lints.js has no size lint): `.ws-icon` ≥ 56 (measured `offsetWidth/Height` in the real render; 110 at d1, 100 at d2/d3), card count === config, stamped + counted hits === config, foil-policy + scope stamps === config, trace-lane and write-row heights === config (80/80 · 90 · 74 · 56 · 60), letter card ≥ 150, every zone 660 wide inside the body column and above the footer, an unauthored unit REFUSES (throw). Poisons are judged on the SPECIFIC message (WRONG REASON / SILENT both exit 1); the correct EN bank is the control (0 findings).

`node i18n/build-en.js` → `build-en: 482 types -> strings.en.json (title lint clean)`.

## Renders (looked at every one)
`scripts/worksheet-gen/out/dev/K-317-null-d1-en.png` · `K-317-null-d2-en.png` · `K-317-null-d3-en.png` · `K-317-null-d2-en-us.png` (unit s) · `K-317-null-d2-en-ut.png` · `K-317-null-d2-en-ub.png` · contact sheet `K-317-sheet.png` · gate renders `K-317-gate-d{1,2,3}-en.png`, `K-317-gate-d2-en-u{s,t,b}.png`, `K-317-gate-d1-en-longchrome.png` (3-line title + 2-line instruction: body 733 px, d1 stack fits), poison renders `K-317-gate-poison-P{1,6}.png`.
Nothing cut off, sparse, tiny or overlapping; the title token `{U}{L}` resolves to "Mm" on the sheet (unit-axis strings boundary); d3 shows the two pair-letter foils (nose, nurse) + one carrier (arm) + one other (watch).

## Deviations from the design file (each with the measured reason)
1. **`unitAxis` not `letterAxis`** (README ruling). `render/one.js K-317 null 2 en s` renders the s page; `tokens` returns the bank's `upper`/`L` (`ij`-class units keep their own capital).
2. **Body 722 re-budget**: stacks d1 652 · d2 604 · **d3 592** (the file says 560: 2 × 74 + 6 = 154 for zone 1, not 122). All ≤ 722; measured under a 3-line title the body is 733 and d1 still clears the footer. Zones sit in a `space-evenly` flex column, so slack is air between zones.
3. **`pair` for s = c, not z**: the file's est. s/z pair is refused by measurement — z has **1** initial word in the eligible en pool (zebra) and rule 8 needs ≥ 4; c (soft-c/s spelling confusion) is the EN placeholder, the en panel may re-decide. (m/n, t/d, b/p stand.)
4. **en `graphemes` = the letters of the word** (en has no verified chunk layer; en `level:'letter'` so hit = `word[0]`). Rule 3 in the validator compares graphemes to `chunks.flat()` ONLY in de/nl/sv/no (P2 proves it); a panel may re-author sh/ch/th as one grapheme for face 5.
5. **Foil-policy semantics made explicit** (the file only sketched them): d1 `'any'`, d2 `'avoid'` (every foil begins with an `avoid` letter), d3 `'pair-noninitial'` (2 foils begin with `pair`, 1 foil is an item carrying L NON-initially, 1 other). The file's verify text "foils ≥ 4 with NFD(word) free of the base letter" contradicts its own d3 rule, so `verify()` keys on the stamped `data-lcs-foil-policy`: under `pair-noninitial` exactly ONE foil may carry L, and it must never BEGIN with L.
6. **P1 is three layers**: the spec's own foil filter REFUSES to build from a bank whose foils carry L (the poisoned bank never renders — recorded as `P1 build`), so the page-level half of P1 is a stamp swap past that guard (a foil card re-stamped as "mango" with hit 0) which `verify()` kills. P2/P3 run as in-memory de/fr-shaped blocks through the gate's validator (only en is authored).
7. **Face-2 knobs carried on the base** (§3 F2 is a PARAM face that spreads the base): `hunt.hitPos:'noninitial'|'any'` + `minMedial` drive hits and the `data-lcs-scope="anywhere"` stamp; `verify()` re-derives `graphemes.indexOf(target) > 0` under that scope. Not rendered on the base (scope stays `initial` at every level).
8. `letterCard` text "Mm" = 138 glyph units (the file's 106 is 'Aa'); at glyphH 64 → 130 px inside the 134 px lane, so the card stays 150; wider text grows the card and the lanes shrink by the same amount (`laneW = 660 − card − 14`).
9. Hunt cards carry NO badge number (the file says no caption, no alt; numbering was not specified) — a teacher's "circle the four" needs none.
10. The sweep asserts a second thing the file did not ask: ≥ 2 distinct hit sets over 20 seeds (20/20 measured).

## `tools/b3-baseline.js --check --quick` — FAIL, fully attributed (no shared edit)
```
checked build 2760 + enum 200 in 23s: 68 drifted (0 expected), 0 missing   FAIL
```
- **Build section: 0 drift on my files.** With the COMMITTED `i18n/strings.en.json` restored the build section reports 0 drift; the 68 build "drifts" appear only with the regenerated file, and all 68 are pre-existing ids (K-308…316, G1-285…301, en d2) that the committed `strings.en.json` **lacked** (40 ids absent at HEAD, 0 changed — measured by diffing HEAD vs regenerated). `resolveStrings` then flips `source:'spec'` → `'en'` and the harness hashes `JSON.stringify(strings)` including `source`; title/instruction bytes are identical. The reviewer should decide whether the committed `strings.en.json` gets refreshed (the brief mandates `build-en`) or the harness drops `source` from its hash.
- **Enum section: 11 drifts = waves 001-011 (`types:"all"`)** — inherent to ANY new spec file: `enumerate.js` round-robins themes by the spec's index in `loadAllTypes()` (README item 11 precedent), so inserting K-317 (and the parallel K-318 / G1-305 / G1-306 on disk) shifts later types, and the non-EN `types:"all"` waves now THROW `family key "letter-of-the-week" missing taxonomy slug/name` until step 2 of the recipe (`register-b3-taxonomy.js`) runs. The nt20-C waves list their 120 ids explicitly, so this does not touch them.

## Open items (faces / panels / build session)
- Phase 2: `positionCard`, `positionKey` (F3), `letterChips` (F6), `units[]` (F5 sh/ch/th — grapheme-true subset needs the en panel's marking), the F2 PARAM spec over `hitPos:'noninitial'`.
- Panels: `pair`/`avoid` per letter are EN placeholders (s/z refused by data); exemplar per locale; open every hit picture; author `graphemes` in the 7 no-chunk-layer locales; da strict pool.
- Registration before any wave: `apps['letter-of-the-week']` + `axes['exercise-type']` slug/name × 11 (enumerate throws without it); `verify-hub-type-rows.js` expects 6 rows per locale.
- Meta lead "free printable" stays in metadata only (README open item 1); no visible copy here claims free.
