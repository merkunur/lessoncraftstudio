# K-356 `weather` (K/G3) : FINAL design (editor merge, 2026-09-21)

Merged from `_work/K-356-pedagogy.md` + `_work/K-356-design.md` (record of every ruling: `_work/K-356-critic.md`). (m) = re-measured 2026-09-21 by read-only node (`lib/b2-common.js entriesFor`, `data/b2/calendar.js NAMES`, `primitives/thermometer.js`, `types/_shared/measurement-tasks.js`, `templates/components-b2.js letterBoxes/wordBank/pillChoice`, `templates/layouts/card-grid.js`, `page/page.css`, `scripts/seo-landing/gen-b3-landings.js LEVEL_KEYS`, `frontend/config/topics-taxonomy.json`, `frontend/lib/seo/strand-names.ts`; scratch `K-356-sheet.js`) or by OPENING the picture (the editor built a 56 px colour + greyscale contact sheet of 16 pictures, `K-356-sheet.png`, and read it). *est.* = the engineer measures in the real render with the shell woff2 from `file://` (`render/one.js`). No em-dashes.

**Boundary (load-bearing).** K-211 `hot-and-cold` (LIVE, `science-category-sort`) sorts 14 OBJECT pictures into hot / cold bins and its bank holds `weather/sun` (hot) + `weather/snowflake` (cold) + `winter/mittens` (cold) (m by the pedagogy). K-207 `summer-and-winter-clothes` sorts 12 garments incl. `summer/sunglasses`, `clothing/scarf`, `clothing/t-shirt`, `winter/mittens`. G2-277 owns the MONTH GRID + dates. G1-146 / G1-147 (`graph-tasks`) own COUNTING kinds into a table / pictograph (the en "weather graph" strings are theirs). G3-345 `reading-a-thermometer` (title "Hot or Cold?", `measurement-tasks` mode `thermometer`, `{min:0, max:40, step:5, height:250}`, m) owns READING A VALUE off a numbered scale and WRITING it. K-322 owns season markers + months-to-season. G1-341 owns "read a sentence, circle true / false". K-225 draws word-to-picture lines over any theme (`sun` 2 refs, `cloud` 3 refs in its pool, m by the pedagogy), never a six-symbol chart. K-321 owns the ORDER of day names. Therefore this family owns exactly four things nobody has: (1) the WEATHER SYMBOL SET, six fixed concept pictures (sun · cloud · rain · storm · snow · rainbow) paired with the locale's WEATHER NOUNS (de Regen / Schnee, never the vocab singulars Regentropfen / Schneeflocke, never the capitalised vocab ADJECTIVES Sonnig / Regnerisch); (2) QUALITATIVE TEMPERATURE BANDS read off a numeral-less thermometer against a printed key (low / middle / high = cold / warm / hot), never a value; (3) the WATER-CYCLE DIAGRAM (NEW `primitives/water-cycle.js`, palette-only, numbered markers, four stage words from a chip bank), titled from the water-cycle head; (4) the open WEEKLY CHART and the READ-THE-FORECAST strip (a given Mon-Fri symbol row; three printed questions answered by circling a day chip; nothing counted). Every face uses ONLY the six phenomena; gear (`weather/scarf`, `clothing/t-shirt`, `weather/sunglasses`) appears on ONE face (F3) as the band-to-picture answer chips and in its key, nowhere else; no gear WORD is ever printed. `hot` (a third sun), `cold` (a penguin), `mittens`, `umbrella`, `raincoat`, `puddle` (a pond), `thermometer` (a picture), `partly_cloudy` + `rain_boots` (no vocab, m) are OUT of every pool. No face sorts into bins, prints a month grid, counts anything into a table, prints a temperature numeral or a degree sign, or prints true / false chips. Day names appear only as GIVEN labels from `calendar.js`. Visual signature: **round white symbol badges** (r 999, teal 3) holding the six pictures on every face; K-322 uses square cream tiles, K-225 bare icons.

## 1 Identity

| field | value |
|---|---|
| id / key / bands | `K-356` / `weather` / base **K**; F2 F3 **K** (`K-358+ TBD by the emitter`); F1 F5 **G1** (`G1-354+ TBD`); F4 **G3** (`G3-385+ TBD`). `default_subject: science`, `default_age_range: 5-7`, `assetClass: icon-placement`, `exerciseType: weather`. `apps.weather` + `axes['exercise-type'].weather` ABSENT (m); register with the `tools/register-b4-taxonomy.js` shape (the `apps.seasons` shape `{default_subject:'science', default_age_range:'5-7', exercise_type_axis_key}`, m) + slug/name x11. The 11 compound slugs and the 11 water-cycle slugs grepped against every `axes.*` slug / name in their locale: **0 collisions** (m by the pedagogy; the theme slugs themselves are `en weather · de wetter · es clima · pt clima · fr meteo · it meteo · nl weer · sv vader · da vejr · no vaer · fi saa`, m). |
| theme axis | **FIXED** via `withFixedTheme(type, 'weather')` (`types/_shared/fixed-theme.js`: `themeAxis.applicable:false, fixedTheme`, m); landings carry `coordinate.theme:''`. `weather` = 21 files / 19 vocab entries in all 11 locales (m: `entriesFor('weather', loc).length === 19` x11). Six concept pictures + three gear chips, one cross-theme (`clothing/t-shirt`, vocab x11, m) resolved by `fileUri` the way K-322 draws cross-theme markers. |
| unitAxis | **NOT APPLICABLE, RULED.** No numeral and no unit reaches any page: the F3 thermometer prints no scale (the live primitive's `label()` at every tick is switched off by the additive `numerals:false`); `data-lcs-value` stays an internal coordinate; the en-US degree trap is designed out, not fanned. |
| CCSS (en, honest) | NONE on any face (no `educationalAlignment`); `teaches` = "Weather symbols (readiness)". en prose names NGSS **K-ESS2-1** (observe local weather) on the base, F2 and F5; the en F4 landing states the water cycle has **no K-3 NGSS anchor (5-ESS2-1)** and still builds (the lock). Non-EN names the national framework + the science strand literal of table B (`strand-names.ts` has 0 Science rows, m: the build adds the row from table B; README open item 1). |
| new primitives / data | `primitives/water-cycle.js` (§2 F4 geometry + node gate); ADDITIVE options `bands` + `numerals` on `primitives/thermometer.js` (§2, defaults byte-identical, re-render poison); `templates/components-b4/weather.js` (7 components); `data/b4/weather-symbols.json` (global) + `data/b4/weather.js` + `data/b4/locales/weather.<loc>.json` (§5); `lib/b4-common.js bank()` over `data/b4/` (README open item 3). |

| loc | genre head (compound, the panels verbatim) | ASCII slug | level K / G1 / G3 (`LEVEL_KEYS`, m) | water-cycle head (F4 title root) · notes | national strand literal (this table IS the strand row) |
|---|---|---|---|---|---|
| en | Weather Symbols (F2 head "Weather Chart", 43 of 129 harvest strings) | `weather-symbols` | `kindergarten` / `grade-1` / `grade-3` | The Water Cycle (probe 109) · US spelling; "printable" beats "worksheet" in K metas; "with answers" never promised (printable-only decks ship no key) | Science (readiness; NGSS K-ESS2-1 in prose on base / F2 / F5; 5-ESS2-1 "no K-3 anchor" on F4) |
| de | Wettersymbole (F2 sub-head "Wetter beobachten") | `wettersymbole` | `vorschule` / `1-klasse` / `3-klasse` | Wasserkreislauf (a July §1 gap, 49) · nouns keep the capital; title never "Wetter" | Sachunterricht |
| es (MX) | El estado del tiempo (SEP term; never "clima" in a title: theme slug AND climate) | `el-estado-del-tiempo` | `preescolar` / `primer-grado` / `tercer-grado` | El ciclo del agua (117, the largest probe cell) · `arcoíris` one word (RAE); `lluvia` never "gota de lluvia" | Conocimiento del Medio |
| pt (BR) | Como está o tempo hoje `OPEN 1` (UNMEASURED; the pt panel re-probes before the title locks; never "clima": BNCC 5º ano tempo vs clima) | `como-esta-o-tempo-hoje` | `educacao-infantil` / `1o-ano` / `3o-ano` | O ciclo da água (50) · `arco-íris` = 4 + 4 boxes with a printed hyphen; "atividade" never in a title | Ciências |
| fr | Le temps qu'il fait (F2 head "La météo du jour") | `le-temps-qu-il-fait` | `maternelle` / `cp` / `ce2` | Le cycle de l'eau (94) · `arc-en-ciel` = 3 + 2 + 4 boxes, two printed hyphens; NBSP before `?`; keep OUT "météo des émotions" / "du comportement"; "fiche" never in a title | Questionner le monde |
| it | Il tempo atmosferico (never "meteo" = theme slug; bare "il tempo" = time) | `il-tempo-atmosferico` | `infanzia` / `classe-prima` / `classe-terza` | Il ciclo dell'acqua (60) · the thinnest market (26 strings, band 2): expect C-tier; "scheda" never in a title | Scienze |
| nl | Weersymbolen (F2 sub-head "Het weer bijhouden") | `weersymbolen` | `kleuters` / `groep-3` / `groep-5` | De waterkringloop (24) · **F4 above band: groep 5-6, the landing says so** (the `groep-5` key); title never "Weer" | Oriëntatie op jezelf en de wereld |
| sv | Vädersymboler (F2 head "väderdagbok") | `vadersymboler` | `forskola` / `ak-1` / `ak-3` | Vattnets kretslopp (24; Lgr22 åk 2-3 kemi) · indefinite forms only; definite "vädret" is the forecast register, off the sheet [NSR] | Naturorienterande ämnen |
| da | Vejrsymboler ("vejret opgaver" in meta) | `vejrsymboler` | `boernehaveklasse` / `1-klasse` / `3-klasse` | Vandets kredsløb (13; 3.-4. kl) [NSR] | Natur/teknologi |
| no | Værsymboler ("været oppgaver" in meta; bare "vær" is also the imperative of "være") | `vaersymboler` | `1-trinn` / `2-trinn` / `4-trinn` | Vannets kretsløp (13) · **F4 above band: 4. trinn = the no G3 key `4-trinn` (m), honest by construction** [NSR] | Naturfag |
| fi | Säätilat `[NSR-FLAG][fi]` `OPEN 2` (vs "Säämerkit" / "Sääsymbolit"; the fi build panel rules h1, the other goes to the meta) | `saatilat` | `esikoulu` / `1-luokka` / `3-luokka` | Veden kiertokulku (11) · `sateenkaari` 11 boxes = 526 px fits (m); cases are the panel's ("Yhdistä säämerkki oikeaan sanaan.") [NSR] | Ympäristöoppi |

**The rule that locks the type.** The SYMBOL is the apparatus and the answer is a FIXED PAIRING held once in a locale-neutral file: `WEATHER_SYMBOLS` (6 keys, one OPENED picture each) pairs with `symbolWords[key]` (a whole panel literal, the weather NOUN, never derived from the vocab, never an adjective, single token or hyphenated); the band-to-gear map, the water-cycle stage keys and the diary / forecast day labels are global data (`calendar.js dayNames` as GIVEN labels, read never copied); the code substitutes literals, shuffles, stamps and never inflects. Nothing on any page is derived from `image-vocabulary.js` at render (no `objForms` exist for the weather nouns in 10 locales; fi has 8, unused, m by the pedagogy).

## 2 The base page

**Concept.** "Weather Symbols: Draw a Line." Six round white weather badges down the left, six cream word signs down the right in a deranged order, one straight pencil line per pair. The morning-chart symbols a K class reads off the wall, each tied to its word. One apparatus, pencil first, nothing else printed. The design's measured `.ws-match` geometry (badge 96 / icon 72 / tile 250 x 84 / word Baloo 2 700 24) replaces the pedagogy's 88 / 20 estimate; the derangement rule is common to both.

**Chrome budget (README ruling).** Body **722** with 3-line title + 3-line instruction (814 one-line; 677 with a 4-line fi title). `.ws-page` inner 675. `.ws-match` = `flex:1 1 auto; justify-content:space-between; padding:6px 30px` (`page.css:354-360`, m) -> inner **615**; `.ws-match-col` `justify-content:space-around; gap:12px` (m) absorbs slack between rows, never inside a badge. Every stack below is quoted at 722 and checked at 677.

**Layout d2**
```
.ws-match (inner 615; data-ws-content on the wrapper)
y 6    [ badge 96 ] o . . . . . . . . . . . . . . . . . . . . o [ word tile 250 x 84   Sonne      ]
y 108  [ badge 96 ] o                                         o [ word tile            Regenbogen ]
y 210  [ badge 96 ] o                                         o [ word tile            Wolke      ]
y 312  [ badge 96 ] o                                         o [ word tile            Schnee     ]
y 414  [ badge 96 ] o                                         o [ word tile            Gewitter   ]
y 516  [ badge 96 ] o                                         o [ word tile            Regen      ]
y 612  rows 6 x 96 + 5 x 12 = 612; + padding 12 = 624 <= 722 (and <= 677)
```
Left column 100 wide: `symbolBadge` = `.ws-match-item--plain` (white, m) forced round by inline `border-radius:999px; border:3px solid teal; width:96px; height:96px`, `.ws-icon` 72 centred, `.ws-match-dot--right` (12 px coral, `right:-26px`, m). Right column 250: `.ws-match-item` (cream, border 2 `#F0E4CB`, r 14, m) 250 x 84, word Baloo 2 700 24 ink `white-space:nowrap`, `.ws-match-dot--left`. Widths 100 + 250 + 2 x 26 = 402 of 615 -> **213 px of clear line run**. Word widths at Baloo 2 700 24: `Regenbogen` 127.3 (m: 137.9 measured for `Donnerstag` at 26 in K-321, scaled 24/26), `sateenkaari` ~140 *est.*, `arc-en-ciel` ~125 *est.*, `tempestade` ~120 *est.*, all <= inner 226 (250 - 24); a measured tile text > 226 refuses that locale's WORD on the base (never a smaller font; the validator measures). K floors: badge 96 >= 56, icon 72 >= 56, word 24 >= `density.K.fontLabel` 18; 6 pairs within [4, 8].

**Ladder** (resolved config keys; guards key on THESE, never the level index):

| key | d1 | d2 (ships) | d3 |
|---|---|---|---|
| `pairs` / keys | 4 (`sun rain snow cloud`) | **6** (all) | 6 |
| `distractors` (word tiles with no badge) | 0 | 0 | 2 (`distractors[]` panel literals: weather words WITHOUT a symbol, e.g. de "Wind", "Nebel"; NEVER a gear word) |
| `badge / iconPx` | 112 / 88 | 96 / 72 | 84 / 64 |
| `tileH / wordPx` | 112 / 26 | 84 / 24 | 72 / 22 |

**d3 ruling:** 8 tiles x 72 + 7 x 12 = 660 + 12 = 672 <= 722; the design's gear-word distractors (umbrella, scarf) are REPLACED by symbol-less weather words because no gear word is ever printed on this type (the lock's quality rule). Not shipped; no copy describes it.

**Composer.** Left = the six concepts in `rng.shuffle` order; right = a DERANGEMENT of the left order (`right[i] !== left[i]` for every i), re-drawn until it is also not the reversed left order; seed locale-neutral (same left order, same permutation x11; only the literals change). At d1 the four keys are fixed, the orders shuffle.

**Answer-hiding + uniqueness.** Both columns print; the MAPPING is the answer and lives in `data-lcs-concept="<key>"` on each badge and each tile (never text, never order: the derangement makes row i never its own answer). Each concept has exactly one badge and one tile (+ `distractors` tiles with no badge at d3), so every line has one correct end. Root stamps `data-lcs-pairs`. `verify(page)`: 6 badges with distinct concepts in the six-concept set; 6 (8 at d3) tiles whose concepts are a permutation of the badges' (+ exactly `distractors` extra); no fixed point; not the reverse order; every tile text === `symbolWords[concept]` of the locale (node cross-check); NO tile text equals (case-insensitive) a vocab adjective singular `sunny rainy cloudy stormy` of the locale (m: de Sonnig Regnerisch Bewölkt Stürmisch · sv Soligt Regnigt Molnigt Stormigt · fi Aurinkoinen Sateinen Pilvinen Myrskyinen); every badge has ONE `img`, `complete && naturalWidth > 0`, `src` === `fileUri(theme, noun)` of `WEATHER_SYMBOLS[concept]` (dir `weather`, never a localized B&W marker dir); no text on a badge, no `img` on a tile; icons >= 56, word >= 18; no `[data-lcs-answer]`; no gear key on the page.

**Reused (exact).** `.ws-match .ws-match-col .ws-match-item .ws-match-item--plain .ws-match-dot(--left/--right)` (`page.css:354-391`, m) · `cardGrid({cards, cols, rows, numbered})` (`layouts/card-grid.js`, m: `grid-template-rows: repeat(rows, minmax(0,1fr))`) + `.ws-card` (cream, border 2, padding 12, m: inner = w - 28) `.ws-card-badge` (30) · `wordBank({words, wordPx})` (`components-b2.js:228`, m; `.ws-scene-banner`, 59 high + `margin-bottom:10`) · `letterBoxes({n, box, gap})` (`:291`, m: W = `n*box + (n-1)*gap + 2`, H = `box + 2` -> 46 at box 44) · `pillChoice` (`:244`, m: `.ws-pill` Baloo 2 700, padding 6 24) as the MODEL for the day chips (the component overrides padding inline) · `drawBox({w, h})` (`components-b3/read-and-do.js:124`, m: white, dashed coral 2.5, r 12, `data-lcs-drawbox`) · `writingRow({w, h, glyphH, xHeight:true})` (`primitives/trace-path.js`) · `thermometer(...)` (`primitives/thermometer.js`, m: W 110, tube x 62 w 16, bulb r 17, `topY 14`, `botY = H - 42`, a `label()` numeral at EVERY tick today; `min:-10` legal) with the additive options below · `svgRoot el roundedRect line circle label` (`primitives/_svg.js`) · `NAMES[loc].dayNames / dayAbbr / weekStart` (`data/b2/calendar.js`, m: Sunday-first arrays x11; `weekStart` en / pt 0, else 1; read never copied) · `entriesFor('weather', loc)` + `fileUri` · `withFixedTheme` · tokens `cream creamDeep white teal tealSoft coral coralSoft ink inkSoft grid`, `stroke.primitive 3 / grid 1.5 / accent 4`, `density.K 56/30/[4-8]`, `density.G1 44/26/[6-12]`, `density.G23 36/22/[8-16]` · `bank(name, loc)` via `lib/b4-common.js`.

**NOT used.** `science-pair-match` factory (d 4/5/5 pairs, square items; only the `.ws-match` classes are reused) · `science-category-sort` (bins; K-211) · `sceneStage` / `iconRows` (rotation; a symbol never rotates) · `chipRow` (numeral chips) · `answerBox` (stamps `data-lcs-answer="undefined"` without an answer) · `truthChips` (G1-341) · `unitAxis` · `lib/b3-picture-index.js pictureFor` (fixed theme; pictures are named per concept, never drawn) · `image-vocabulary.js` / `objForms` / `articles.js` / `approved-words-*.json` at render · `calendar.js monthNames / frames` (G2-277) · `thermometer.js` numerals (`numerals:false` on every call of this type).

**ADDITIVE options on `primitives/thermometer.js` (RULED: one contract, no new primitive).** Defaults preserve today's output byte-for-byte; `tools/b4-baseline.js --check` proves every live consumer and the gate re-renders G3-345 d2 (`measurement-tasks` call `{value, min:0, max:40, step:5, height:250}`, m) before and after and asserts an identical svg string (poison: flip the `numerals` default -> FAIL).
- `bands:[{key, from, to, fill}]` -> drawn BEFORE the tube: one `roundedRect` per band, x `tubeX - 22` (40), w 44, from `yFor(to)` to `yFor(from)`, r 6, fill = a palette TOKEN name, no stroke; at every INTERIOR band boundary a 3 px `ink` tick line across the 44 px band width (the mono-safe separator; the regular 2 px step ticks stay). Bands must tile `min..max` exactly, in order, non-overlapping (throws otherwise). Stamped `data-lcs-band="<key>" data-lcs-band-from data-lcs-band-to`.
- `numerals:false` -> skips the `label(...)` calls (tick `line`s stay). Default `true`.
- return gains `meta.bands = [{key, y1, y2}]` (svg-local px).
- unchanged: `data-lcs-value` (the verify channel; never printed), mercury coral, bulb coral, tube white with teal 3.

**NEW `primitives/water-cycle.js`** (pure SVG on tokens; Node-testable; exports `waterCycle`, `ANCHORS`, `MIN_W`): `waterCycle({ w = 608, markers = [], id }) -> { svg, width, height, scale, anchors:{[key]:{x, y}} (px) }`.
- **viewBox `0 0 640 400`**, y down; `height = w x 400/640` (608 -> 380; 480 -> 300). `MIN_W = 480` (throws below: a marker numeral falls under 12 px).
- **Stroke** teal `#146B5E` 3 px round joins / caps; detail lines `grid` `#C8BFAE` 1.5; arrows coral `#F2784B` 4 (`stroke.accent`); fills cream (land), tealSoft (water), white (cloud), coral (sun disc, markers). No other hex.
- **Draw order + geometry:** (1) land: `M 360 400 L 360 302 Q 450 212 540 238 Q 600 254 640 246 L 640 400 Z`, fill cream, stroke teal 3. (2) water: `M 0 300 Q 40 288 80 300 T 160 300 T 240 300 T 320 300 T 400 300 L 400 400 L 0 400 Z` fill tealSoft no stroke, then the top wave as an open stroke teal 3, two inner wave lines (the same curve translated to y 332 and 364, x 20..380) `grid` 1.5; water AFTER land so the shoreline (x 360..400) is the water's edge. (3) sun: `circle` cx 72 cy 72 r 30 fill coral; eight rays r 38 -> 54 every 45 deg, coral 4 round caps. (4) cloud: ONE closed path `M 362 160 A 40 40 0 0 1 398 86 A 56 56 0 0 1 498 92 A 38 38 0 0 1 536 160 Z`, fill white, stroke teal 3 (x 362..536, y ~50..160). (5) evaporation: three wavy vapour lines teal 3 at x 236 / 266 / 296, each `M x 286 q 10 -20 0 -40 q -10 -20 0 -40 q 10 -16 0 -28`; one coral arrow `M 200 282 Q 246 196 358 170` + a filled coral arrowhead (12 x 12) at (358,170) tangent to the curve. (6) precipitation: six dashed teal 3 lines `dash 10 8` from the cloud base (y 166) to the hill at x 396 / 420 / 444 / 468 / 492 / 516, ending on the land path (the engineer solves the curve, ~y 292..244); one coral arrow `M 550 172 L 550 236` + arrowhead down. (7) run-off: coral arrow `M 546 252 Q 470 262 406 292` + arrowhead at (406,292) into the water. (8) markers last.
- **`ANCHORS` (5, viewBox coords):** `evaporation` (300,206) beside the arrow's mid · `condensation` (449,118) inside the cloud · `precipitation` (456,214) among the rain lines · `collection` (150,344) on the water · `runoff` (486,262) on the run-off arrow (d3 only; the d2 page uses four). Returned scaled in px. Min pair distance (precipitation / runoff) >= 57 px at w 608; the primitive asserts >= 50.
- **`markers:[{anchor, n}]`:** white halo `circle` r 17 stroke white 4, coral disc r 13, numeral white Baloo 2 700 17 viewBox units (16.2 px at w 608, >= 16) centred, `data-lcs-marker="<anchor>" data-lcs-n`.
- Root `<svg data-lcs-prim="water-cycle" data-lcs-w role="img" aria-label="">`. **No picture sits on the diagram** (painted art beside a 3 px line diagram is two styles; the pale sun dies in mono where the coral disc does not; a pictured cloud has no clean edge for a marker). **Node gate `qa/verify-water-cycle.js`:** parses the emitted SVG; every anchor inside its host shape's bbox (cloud / water / land / arrow bbox); marker centres at the anchors within 2 px of the returned px; marker spacing; `MIN_W` throw; only token hexes; the RENDER is measured (the G2-235 ruler precedent), never a derived constant.

**NEW `templates/components-b4/weather.js`** (behind the `templates/components-b4.js` barrel; HTML + inline SVG on tokens; scoped inline CSS; no `page.css` edit): `symbolBadge({concept, src, d = 96, iconPx = 72, dot})` · `symbolMatch({left, right, badge, iconPx, tileW = 250, tileH = 84, wordPx = 24})` (the stage above; tiles `data-lcs-word data-lcs-concept`) · `spellRow({concept, src, word, box = 44, gap = 4, badge = 72, iconPx = 56})` (letter boxes per hyphen-separated GROUP joined by a printed hyphen glyph Baloo 2 700 22 ink, 14 wide, 8 px each side; `data-lcs-spell data-lcs-len data-lcs-groups`) · `diaryWeek({days:[{index, name}], key:[{concept, src}], cellW = 160})` (§F2) · `bandKey({bands:[{key, value, src}], thermH = 120})` + `bandThermo({value, bands, chips:[{key, src}], thermH = 220, chip = 64, iconPx = 56})` (§F3) · `forecastStrip({days:[{index, name, concept, src}], iconPx = 88})` + `forecastQuestion({n, text, chips:[{index, label}], askBy = 'word'|'symbol', src})` (§F5) · `cycleLabels({bank, lanes:[{n, anchor}], given})` (§F4).

**Alternatives (rejected).** Alt A the K-322 sandwich (six symbols above, six words below, lines across): 6 x 6 lines from a top row to a bottom row cross the signs themselves; a K child cannot aim past a sign. Alt B symbol -> circle the word among three: 18 printed words at K and it is K-225's move with a smaller pool. Alt C the diary as the base (the biggest en cell): open-ended (no verify) and its head is a FACE head in de / nl / sv, while the symbol head is the compound title the lock mandates. Alt D the pedagogy's 88 / 20 stack: not measured against the CSS; the design's 96 / 24 is.

**Risks -> mitigations.** Mono print: the six symbols were chosen on the 56 px greyscale sheet (§5 symbol set): thunderstorm and cloudy darkest, rainy's drops the darkest marks on the page, sunny's bar rays a step darker than its disc, rainbow a graded arch, snowflake the palest (its teal badge ring and white fill save it). Coral dots and mercury print ~55 % grey; the F3 band fills are NOT distinguishable in mono and are not asked to be: the 3 px ink edge ticks separate the bands and the KEY names them. The water-cycle diagram is 3 px teal line on cream with coral arrows, mono-safe by construction; markers keep their white halo. Pencil space: base lines 213 px clear; letter boxes 44; diary boxes 136 wide; lanes 270 x 52 glyphH 26; chips 64 for ringing (K) with a 6 px reserve; 44 px pills (G1) with 8 px gaps. Long locales: base tiles measured before the wave; F1 bank two rows budgeted; F5 pt short day forms (data). What only a human eye catches: a base line grazing a badge ring; the `cloudy` vs `cloud` call if the cache is refreshed (open again); whether the scarf reads "cold" to a K child (the key says so, so the task survives); the sun's face on a "science" page (accepted: every sun in the library has one). **Palette:** cream, creamDeep, white, teal, tealSoft, coral, coralSoft, ink, inkSoft, grid. **Font floor:** smallest text 16 (day pills, marker numerals) vs the 9 px lint (`qa/lints.js:86`); footer 10 is the shell's. Every stage / grid root stamps `[data-ws-content]`. **Print check:** the engineer prints one d2 of the base, F3 and F4 on a mono laser at 100 %, draws the six lines in HB, rings one chip on F3, writes `Niederschlag` in a lane, confirms the band ticks are visible and the snowflake badge is not a blank circle.

## 3 Faces 2-6

All five are CODE faces on ONE additive `layout` knob (the K-322 pattern, m: `d.layout` undefined = the base path, byte-identical; a face stamps `data-lcs-layout` only when declared); each adds a `verify()` branch; guards key on the config (`d.pairs`, `d.rows`, `d.days`, `d.items`, `d.stages`, `d.asks`), never on the level index. Resolved d2 configs are pairwise distinct (`tools/gate-variation-distinct.js --batch=b4`, exists, m). None is PARAM: each changes the apparatus a teacher sees (badges + boxes / seven draw cells + key / thermometers + chips / a line diagram + numbered lanes / a strip + questions with day chips). `coordinate.mode` = the `layout` string. Density: K faces pictures >= 56, 4-8 items; G1 faces >= 44, the two honest floors below are RULED exceptions; G3 diagram markers >= 16 px numerals.

### F1 : Write the Weather Word (G1, CODE `layout:'write'`)
**Move:** MATCH a symbol to its word in a bank and WRITE it into letter boxes, one letter per box; the box count and the given hyphens are the scaffold. Differs from G1-244 (picture-card dictation on any theme, 24 px boxes, m by the pedagogy) because the cue is one of six fixed SYMBOLS and the words are the panel's weather nouns; differs from the base because the child WRITES (the base draws a line). **Bank ruling:** bank ON at d2 (the design): six words of up to 11 letters with de capitals are a spelling test without one; the bank makes the move "find the word, copy it letter by letter" (G1 sight vocabulary); d3 removes the bank. **Layout:** `wordBank` 59 + 10 + six `spellRow`s h 76 (badge 72, icon 56) with 12 px gaps = 516 -> **585 <= 722** (rows `minmax(76px,1fr)`); bank two rows in de / fi = 108 -> 634 <= 722 (<= 677 at 634: yes). Row width: the widest legal word 11 boxes (fi `sateenkaari`) = 11 x 44 + 10 x 4 + 2 = **526** (m) + 72 + 12 = 610 <= 639; fr `arc-en-ciel` = boxes 3 + 2 + 4 with two glyphs = (3x44+2x4+2) + (2x44+4+2) + (4x44+3x4+2) + 2 x 30 = 142 + 94 + 190 + 60 = 486. **Letter counts (m, NFC code points, hyphens excluded; order sun cloud rain storm snow rainbow):** en 3 5 4 5 4 7 · de 5 5 5 8 6 10 · es 3 4 6 8 5 8 · pt 3 5 5 10 4 8 (+1 hyphen) · fr 6 5 5 5 5 9 (+2 hyphens) · it 4 6 7 9 4 10 · nl 3 4 5 6 6 9 · sv 3 4 4 4 3 8 · da 3 3 4 6 3 7 · no 3 3 4 6 3 7 · fi 7 5 4 7 4 11. Special letters count ONE box each (sv å ö, da / no ø, fi ä, de ß). **Hyphens are GIVEN** (the lock): the literal splits on `-`, each group renders `letterBoxes({n:group, box:44, gap:4})`, a printed hyphen glyph sits between groups; a 3-box-per-hyphen rendering is rejected (a child would write a letter into a hyphen box). **Config d2** `{layout:'write', rows:6, bank:true, box:44, gap:4, maxLetters:11, hyphenGiven:true}`; d1 `{rows:4, bank:true, box:48, gap:6, maxLetters:8}`; d3 `{rows:6, bank:false, box:44, maxLetters:11}`. **Verify:** 6 `[data-lcs-spell]` distinct concepts; per row `[data-lcs-letterboxes]` sums to `data-lcs-len` === `[...symbolWords[key].replace(/-/g,'')].length` and `data-lcs-groups` === the split of the literal on `-`; hyphen glyph count === hyphens in the word; box >= 44; bank set === the six row words (d1 / d2) or no `[data-lcs-bank-banner]` (d3); bank order !== row order; no row prints its word; each row has one `img` (dir `weather`, file === noun); no adjective. **Refusals:** none (every literal is a single token or hyphenated; the validator rejects a space, e.g. "gota de lluvia"; a word > 11 letters names `write` in `refuse`; none measured). **Query face:** "weather words worksheet 1st grade" (en WINNABLE) · de "Wetterwörter schreiben Klasse 1" · fr "vocabulaire de la météo cp" · es "palabras del estado del tiempo primer grado" · nl "weerwoorden schrijven groep 3" · sv "väderord skriva" · fi "sääsanat kirjoita".

### F2 : Weather Chart, Monday to Sunday (K, CODE `layout:'diary'`; OPEN-ENDED, no verify)
**Move:** OBSERVE and RECORD: every day for a week the child looks outside and DRAWS the matching symbol into that day's box, copying from the symbol KEY. The "Wetterkalender / weerkalender / väderdagbok / tabella per registrare / relógio do tempo" genre; the en "weather chart printable for 7 days" query. **Layout (the design's 2 x 4 grid, RULED over the pedagogy's 7 rows whose 78 px boxes are too short to draw a rainbow in):** `diaryWeek` = a 2 x 4 grid (gap 11 x 12) of seven day cells + the KEY card in cell 8; cells 160 wide (4 x 160 + 3 x 11 = 673 <= 675); day cell = `.ws-card`-styled cream box: header = a teal-bordered white name pill (Baloo 2 700 18, `nowrap`, `data-lcs-day="<0-6>"`, text VERBATIM from `dayNames`) 34 high, gap 8, `drawBox({w:136})` with `flex:1 1 200px; min-height:200px`; rows `minmax(326px,1fr)` -> at 722: 2 x 355 + 12 = **722**, box 280; at 677: 2 x 332 + 12 = **676**, box 258. Key card = white, teal 3, r 16, six plain 56 px icons in 2 cols x 3 rows (gap 12 = 124 x 192) centred, `data-lcs-key`, no words (a 160 px card cannot hold `Regenbogen` beside a 56 px icon at 16 px; the base and F1 teach the words). Days from MONDAY in every locale (the lock; `diaryStart` is a data flag, default `'mon'`; the en / pt panel may set `'sun'` per its `weekStart`, no code change); names VERBATIM from `dayNames` (`Donnerstag` 95.5 at 18, m via K-321; pt `segunda-feira` ~124 *est.* <= inner 134; the pt panel may set `diaryDays` short forms if the render exceeds 134). **Config d2** `{layout:'diary', days:7, start:'mon', box:'rect', legend:6, legendPx:56}`; d1 `{days:7, box:'circle'}` (a 96 px dashed coral circle: symbol only); d3 `{days:7, box:'rect', wordLane:true}` (a writing lane under the box; the child writes the word too). The design's `tempRow` (hot / snowflake chips) is DROPPED: it re-imports K-211's hot / cold pair. **Verify (structure only):** 7 `[data-lcs-day]` whose label text === `dayNames[(start + i) % 7]` in order; one empty `[data-lcs-drawbox]` (or circle) >= 136 x 200 per day, no `img`, no text; one `[data-lcs-key]` with six `[data-lcs-symbol]` distinct keys, each `img` complete >= 56; no `[data-lcs-answer]`; no numeral on the page (no dates: G2-277); no adjective. Nothing is graded. **Refusals:** none (day names exist x11, m). **Query face:** "weather chart printable 7 days" (en) · de "Wetter beobachten Arbeitsblatt Klasse 1" · nl "het weer bijhouden werkblad" · it "tabella per registrare il tempo atmosferico" (verbatim demand) · sv "väderdagbok" · pt "como está o tempo hoje" · fr "la météo du jour" · es "registro del estado del tiempo" · fi "sääpäiväkirja".

### F3 : How Warm Is It? Cold, Warm or Hot (K, CODE `layout:'thermometer'`)
**Move:** READ an instrument QUALITATIVELY against a printed KEY: a key row shows three thermometers, mercury low / middle / high, each beside the garment for that band (scarf = cold, T-shirt = warm, sunglasses = hot); below, four thermometers with no picture on them and three gear chips each; the child judges the band and CIRCLES the chip that fits. **Two rulings:** (a) the chips are the GEAR triple `weather/scarf` · `clothing/t-shirt` · `weather/sunglasses` (the pedagogy), not the design's `hot` sun / scarf / `snowflake`: the answer set must be disjoint from the six symbols (a snowflake cannot be both "snow" and "cold"; `hot` is a third sun the design itself excluded from the symbol set), and hot = `weather/sun` / cold = `snowflake` is K-211's own item pair in K-211's own hot / cold framing; sunglasses and scarf are K-207 items but K-207's move is a season-word SORT and the lock allows gear on ONE face; `mittens` stays out (in BOTH fences). (b) the design's legend pictures ON every item thermometer are REJECTED: a picture beside the mercury tip is the answer printed on the item (a visual match, not a read); the legend moves into ONE key row, which the child reads like K-354 F2's colour legend. Distinct from G3-345 (no value read or written, no scale numeral), K-211 (nothing sorted, no K-211 chip), K-207 (the cue is an INSTRUMENT, the chips a fixed triple). **Layout:** key row `.ws-lane` 675 x 148 (padding 12 16 + border 2 x 2; inner 120): three cells `[thermometer h 120 (W 110)][8][icon 56]` = 174 each, `justify-content:space-evenly`; then `cardGrid 2x2 numbered`, cards 331 wide (gap 13), rows `minmax(252px,1fr)`: card inner 303 x (h - 28) holds `[bandThermo 110 x 220][12][chip column 3 x 64 + 2 x 8 = 208]` = 186 centred, `justify-content:center`. Stack at 677: 148 + 12 + 2 x 252 + 12 = **676 <= 677**; at 722 the cards grow to 275 and the content centres. **Bands (RULED tiling, `-10..40` step 5):** cold `-10..10` tealSoft · warm `10..25` white · hot `25..40` coralSoft, 3 px ink edge ticks at 10 and 25 (m: at h 220 the tube runs y 14..178 = 164 px -> cold 65.6 / warm 49.2 / hot 49.2 px; at h 120 -> 25.6 / 19.2 / 19.2). Mercury values from the band INTERIOR only: cold `{-10, -5, 0, 5}` · warm `{15, 20}` · hot `{30, 35}` (10 and 25 are boundaries and NEVER values; 40 is excluded so the mercury never touches the tube cap; -10 is kept: an empty tube is the strongest cold cue). Key thermometers at the band middles `0 / 17.5 / 32.5` (no numeral prints, so non-step values are legal; the primitive accepts any number in `min..max`, m). Chip pictures carry no band colour; band tints are tokens, not the answer (mono: the ticks and the key carry it). **Composition:** 4 items over 3 bands = every band at least once, 4 distinct values, never the same band on both cards of a row; chip order per card `rng.shuffle`, never the same triple order on every card. **Config d2** `{layout:'thermometer', items:4, cols:2, rows:2, bands:['cold','warm','hot'], chips:['scarf','t-shirt','sunglasses'], thermH:220, keyH:120, chip:64, iconPx:56, min:-10, max:40, step:5, edges:[10,25]}`; d1 `{items:4, bands:['cold','hot'], chips:['scarf','sunglasses'], edges:[15]}` (two bands, two chips); d3 `{items:6, cols:3, rows:2, thermH:200, chip:56, chipsBelow:true}` (cards 215 wide, inner 187: chips as a row of three 56 under the thermometer, 3 x 56 + 2 x 8 = 184 <= 187; card h 252 holds 200 + 8 + 56 = 264 > 224: the d3 key row drops to 100 and the thermometer to 180: 148 - 20 + 12 + 2 x 262 + 12 = 676; not shipped). **Verify:** one `[data-lcs-key]` with three `[data-lcs-key-band]` in band order whose `data-lcs-value` sits inside its band and whose `img` === `GEAR[band]`; 4 `[data-lcs-thermo]`; each `data-lcs-value` in `bands.values[band-of]` (the gate carries its OWN band table), never on an edge; the primitive's `data-lcs-band-from/to` tile `-10..40`; exactly one `data-lcs-correct="1"` per card and its chip key === `GEAR_BAND` of the band containing the value; three chips distinct and === `d.chips` as a set; >= 3 distinct `data-lcs-band-of` at d2; no two cards in one row share a band; **no `<text>` inside any thermometer SVG and no digit or degree sign anywhere in the body** (the live primitive prints a numeral at every tick: poison); no `img` inside an item card's thermometer box (legend-on-item poison); chips >= 56, `img` complete, dirs `weather` / `clothing`; no gear WORD printed; G3-345 d2 svg byte-identical before / after the primitive change. **Refusals:** none (pictures are global; the instruction is the only language). The fi panel's "Väritä lämpömittari oikeaan kohtaan." describes the INVERSE move (picture given, colour the mercury): a non-face (below), not a level. **Query face:** "hot warm cold thermometer worksheet kindergarten" · fr "la température ce1" (measured verbatim) · de "Thermometer Klasse 1 kalt warm heiß" · es "termómetro preescolar frío calor" · sv "termometer kallt varmt" · fi "lämpömittari kylmä lämmin kuuma". `OPEN 6`: a locale panel may publish F3 at its G1 key (fr "ce1" argues it): a `level` key swap in its landing file, no code change.

### F4 : The Water Cycle: Label the Diagram (G3, CODE `layout:'water-cycle'`; TITLED FROM THE WATER-CYCLE HEAD, the lock)
**Move:** LABEL a process diagram: one drawn scene (sun over sea at left, a cloud above, a mountain at right, a river running back to the sea) with FOUR numbered coral markers in CYCLE ORDER (1 evaporation over the sea · 2 condensation in the cloud · 3 precipitation on the mountain · 4 collection on the water; RULED over the design's shuffled numbering: every textbook diagram numbers the cycle in sequence, and the sequence is the learning) and four numbered writing lanes below; a chip bank of the four stage words on top (shuffled, never in cycle order and never reversed); the child copies each word to the lane whose number matches the marker. G3 in 9 of 11 (de 3. Klasse, es 3º, pt 3º-5º, fr CE2, it terza, sv åk 2-3, da 3.-4., fi 3.-4.), above band in nl (groep 5-6) and no (4. trinn), code-less in en: every landing states its own placement. **Layout:** `wordBank` 59 + 10 + `waterCycle({w:608})` 380 centred + 12 + `cycleLabels` 2 x 2 rows of `[.ws-chip 44 (numeral n, Baloo 2 700 24)][10][.ws-blankbox holding writingRow({w:270, h:52, glyphH:26, xHeight:true})]` (330 wide; 2 per row + 15 = 675; 56 + 8 + 56 = 120) = **581 <= 722** (diagram row `minmax(380px,1fr)`); a lane holds ~14 handwritten glyphs at glyphH 26 in 270 (`Niederschlag` 12, `tiivistyminen` 13 fit; the bank pill is the printed copy). **Honest floor (RULED exception):** 4 labels sit below `density.G23.items` [8, 16]; the lock fixes the four-label genre; the gate asserts `labels` in [4, 5] and this is the family's recorded count (the fifth label, runoff, is the only headroom). **Stage keys** fixed and global: `evaporation · condensation · precipitation · collection`; the stage-4 LITERAL is the locale's own school word (`OPEN 4`: collection vs runoff / infiltration, whichever the grade-3 book prints for "the water runs back"; the arrow shows a river running back to the sea, which either names). **Config d2** `{layout:'water-cycle', stages:4, given:0, bank:true, w:608, laneW:270, laneH:52, glyphH:26}`; d1 `{stages:4, given:2, bank:true}` (two anchors print their word in a cream tile, Nunito 800 20, as worked examples); d3 `{stages:5, given:0, bank:true}` (+ `runoff`, the fifth marker and lane; rows 3 + 2). **Verify:** 4 (5) `[data-lcs-marker]` with `data-lcs-n` 1..n in CYCLE order at anchors from `ANCHORS` (px within 2 px of the primitive's return); 4 (5) `[data-lcs-label]` lanes with distinct anchors, `data-lcs-n` matching the marker of the same anchor, lanes EMPTY (no text, no SVG `<text>`); bank set === `cycleLabels[key]` for the lane keys (bijection), bank order !== cycle order and !== reversed; `given` lanes (d1) print exactly the literal of their key; NO `<img>` on the page; palette = cream tealSoft white teal coral grid only; diagram width >= 480 and every arrow group present. **Refusals:** none (four G3-register literals exist in every curriculum: candidates de Verdunstung / Kondensation / Niederschlag / (Sammlung | Abfluss) · es evaporación / condensación / precipitación / (recolección | escurrimiento) · pt evaporação / condensação / precipitação / (coleta | escoamento | infiltração) · fr évaporation / condensation / précipitations / ruissellement · it evaporazione / condensazione / precipitazione / raccolta · nl verdamping / condensatie / neerslag / (afstroming | opvang) · sv avdunstning / kondensation / nederbörd / (avrinning | uppsamling) · da fordampning / (fortætning | kondensering) / nedbør / (afstrømning | opsamling) · no fordamping / kondensasjon / nedbør / (avrenning | oppsamling) · fi haihtuminen / tiivistyminen / sadanta / (valunta | kerääntyminen); the panel signs the four). The face's meta says G3 in every locale (the labels are G3 register, the lock). **Query face:** "water cycle worksheet label" (en 109) · de "Wasserkreislauf Arbeitsblatt beschriften" (a July §1 gap) · es "el ciclo del agua para imprimir" (117) · fr "le cycle de l'eau ce2" (94) · it "ciclo dell'acqua scuola primaria" (60) · pt "ciclo da água atividade" (50) · nl "waterkringloop werkblad" (24) · sv "vattnets kretslopp" (24) · da / no "vandets kredsløb" / "vannets kretsløp" (13 / 13) · fi "veden kiertokulku" (11).

### F5 : Read the Weekly Forecast (G1, CODE `layout:'forecast'`; REPLACES "circle the true sentence" per the lock)
**Move:** READ a symbol chart to ANSWER a printed question: a GIVEN Monday-to-Friday strip (day name over a symbol) and three questions, each a whole literal ("Which day is rainy?") followed by five day chips; the child circles the day. RULED with the QUESTION TEXT at d2 (the pedagogy; the lock's own wording): it is the G1 reading move; the design's symbol-badge lane (no text) is a visual lookup a K child does without reading and survives as d1's scaffold (`askBy:'symbol'`). A data-reading move (1.MD.C.4-adjacent, NOT claimed); distinct from G1-146 (nothing counted), G1-341 (no true / false), G2-277 (no month grid, no date). **Chips print the SAME literal as the strip header** (RULED over the pedagogy's `dayAbbr` chips: abbreviations are K-321's `abbrevPairs` skill and would import a second decoding step). **Layout:** `forecastStrip` 675 x 150 (white, teal 3, r 16; five cells 123, gap 15; name pill Baloo 2 700 18 + icon 88) + 20 + three `forecastQuestion` lanes (`.ws-lane`, inner 639): `[question Nunito 800 18, one line <= 70 chars][8][five .ws-pill day chips Baloo 2 700 16, inline padding 6 10, min-width 44, min-height 44, gap 8, data-lcs-dchip="<index>"]`; lane h = 26 + 8 + 44 + 24 + 4 = 106; 3 x 106 + 2 x 12 = 342 -> **512 <= 722** (rows `minmax(106px,1fr)`; whitespace is a feature). **Single answer by construction:** the strip = 5 DISTINCT symbols sampled from the 6 (`distinctStrip:true`), so every ask has exactly one day; asks = 3 of the 5 strip keys, `rng.sample`, re-drawn until the asked days are not in Mon-Fri order and not all the same. **Day literals** = `forecastDays` (default `dayNames[1..5]`, m x11); the five-pill row must fit 639: 5 x (text + 20) + 4 x 8 <= 639 (fi maanantai..perjantai ~43 chars at 16 ~ 370 + 132 = 502 *est.*; de ~38 chars fits; pt full forms ~59 chars ~ 645 *est.* OVER -> the pt panel sets `forecastDays: [segunda, terça, quarta, quinta, sexta]` and the strip pills print the same short forms; the validator MEASURES every locale's row, never trusts the estimate). **Honest floor (RULED exception):** 3 asks sit below G1 [6, 12]; the lock fixes 3; d3 reaches 4; five distinct symbols cap it at 5; recorded. **Config d2** `{layout:'forecast', days:5, asks:3, askBy:'word', symbolPx:88, chipH:44, distinctStrip:true}`; d1 `{days:5, asks:2, askBy:'symbol'}` (a 64 px symbol badge instead of the question); d3 `{days:5, asks:4, askBy:'word'}`. **Verify:** 5 `[data-lcs-fday]` with distinct concepts ⊂ the six; 3 `[data-lcs-q]` with distinct concepts, each present in the strip exactly once; per lane 5 `[data-lcs-dchip]` indices === the strip indices in order, exactly one `data-lcs-correct="1"` whose index === the strip day carrying the concept; every question text === `forecastAsks[key]` of the locale and contains NO `dayNames` / `dayAbbr` / `forecastDays` token of the locale (a question naming a day is the answer printed); chip text === `forecastDays[i]` === the strip header text; the answer days not in ascending order and not all equal; icons >= 56 (88), pills >= 44 high, text >= 16; no `[data-lcs-answer]`. **Refusals:** none (needs `dayNames` x11, m, and 6 `forecastAsks` literals per locale, each the full question with the weather word INFLECTED inside it: de "An welchem Tag regnet es?", fr "Quel jour pleut-il ?", fi "Minä päivänä sataa?"; the verb-phrase asks are the panel's). "forecast" is a title word only; no ask prints a temperature or a count. **Query face:** "weather forecast worksheet 1st grade" · de "Wetterbericht lesen Klasse 1" · nl "weerbericht lezen groep 3" · fr "la météo de la semaine cp" · es "leer el pronóstico del tiempo" · sv "läsa väderprognosen" · fi "sääennuste".

**Rejected non-moves.** "Circle the true sentence" (G1-341, the lock) · count the symbols into a table / graph (G1-146 / G1-147) · a month grid with symbols (G2-277) · sort pictures into hot / cold or summer / winter (K-211 / K-207) · match season to weather (K-322) · read and write a temperature (G3-345) · a wind / fog / hail symbol face (no art) · the inverse thermometer (picture given, colour the mercury: its answer is a pencil fill the gate cannot re-derive beyond the band; recorded for a later PARAM if the operator wants it) · legend pictures on every item thermometer (the answer printed) · a theme swap (fixed theme) · relabelling d1 / d3 of the base as a face (same move at another count; fails `gate-variation-distinct`) · alternates (`sun`, `cloudy`'s sibling `cloud`, `raindrop`, `stormy`) as extra pairs (a content swap of one act; two pictures of one concept on a page prints two answers) · the design's `tempRow` on the diary (K-211's pair) · `hot` / `snowflake` as temperature chips (K-211's pair; a symbol as an answer).

**Symbol set (global; every picture OPENED; the editor read all 16 at 56 px in colour AND greyscale, `K-356-sheet.png`; the concept picture is FIXED, alternates are recorded for a panel swap only, never mixed on one page):**
- **sun** -> `weather/sunny` (yellow disc, twelve thick orange bar rays, smiling face; RULED over `weather/sun`: at 56 px greyscale `sun`'s thin tapered rays fade to a pale disc with a face, `sunny`'s bar rays stay a clear step darker than its disc). `sun` = `alt`. `weather/hot` (a third smiling sun with spiky red tips) = the K-211 "hot" register, a duplicate symbol: OUT of every pool.
- **cloud** -> `weather/cloudy` (two overlapping blue-grey + cream clouds, no face; RULED over `weather/cloud`: `cloud` is ONE PINK smiling cloud that at 56 px mono is a faint blob on cream, and in colour reads as candy, not weather; `cloudy` is two-tone grey and unmistakable). The noun stays the panel's (`Wolke` or `Wolken`, `moln`: singular or plural is the panel's call, `OPEN 3` closes as data). `cloud` = `alt`.
- **rain** -> `weather/rainy` (pale-to-navy cloud with ~30 dark drops; at 56 px the drops are the darkest marks on the page). `weather/raindrop` (one blue drop) reads "water", `alt` only. `weather/puddle` = a POND in grass, never used.
- **storm** -> `weather/thunderstorm` (navy cloud, red-and-yellow bolt; the strongest symbol in mono). `weather/stormy` (a RED cloud with rain + bolt; the red misreads) = `alt`.
- **snow** -> `weather/snowflake` (pale blue six-arm flake; the palest symbol, the outline keeps the star). No alternate.
- **rainbow** -> `weather/rainbow` (five bands over two pink clouds; a graded arch in mono). No alternate.
- **Gear (F3 key + chips only):** `weather/scarf` (a red patterned scarf; mid-dark, unmistakable) = cold · `clothing/t-shirt` (a plain green T-shirt; vocab x11: T-Shirt / Camiseta / T-shirt / Maglietta / T-skjorte / T-paita, m) = warm · `weather/sunglasses` (red sunglasses; thin but legible at 56 mono) = hot. OUT: `cold` (a penguin in a hat), `mittens` (K-211 + K-207), `umbrella` / `raincoat` / `rain_boots` (rain, not warmth; `rain_boots` + `partly_cloudy` have no vocab and `pictureFor` would throw), `thermometer` (the primitive draws it).

## 4 Native rebuild plan x11

Frames NEVER inflect: this type has NO cloze frame and reads NO `objForms`. Per locale the panel authors: `strings` (6 titles + 6 instructions; the F4 title carries the WATER-CYCLE head, never the weather head), `symbolWords` (6 weather NOUNS, whole literals, the capital / gender as the locale prints a chart label; de capitalised; fr / es / pt / it / nl bare noun; sv / da / no INDEFINITE bare form), `forecastAsks` (6 full questions, one per symbol, the weather word inflected inside), `cycleLabels` (4 G3 stage literals; + `runoff` optional for d3), `bandWords` (3: cold / warm / hot, landing prose only, never printed at d2), `forecastDays` (5, OPTIONAL short forms; default = `dayNames` Mon..Fri), `diaryDays` (7, optional), `diaryStart` (`'mon'` default), `bankWords` (6, optional, d3 article forms; must END with `symbolWords[key]`), `distractors` (2, optional, d3 symbol-less weather words), `refuse` per face. Day names come from `calendar.js` (0 new). = **31 literals per locale** (+ optional `forecastDays` 5 / `bankWords` 6 / `distractors` 2). The instruction names only apparatus on the page (symbols, words, lines, boxes, bank, thermometer, pictures, diagram, arrows, lines, questions, days) and at K is one imperative sentence. The EN is a SOURCE TO AUDIT (`enAudit`).

| loc | literals (counts) | forms / slots and where they exist | refusal / re-target | traps |
|---|---|---|---|---|
| en | 12 strings · 6 symbolWords (sun cloud rain storm snow rainbow) · 6 forecastAsks ("Which day is rainy?") · 4 cycleLabels · 3 bandWords = 31 | `calendar.js en` (Sunday-first; `diaryStart` may be `'sun'`, `OPEN 5`) | none | US spelling; "printable" beats "worksheet" in K metas; title never "Weather" alone (theme name, m); "weather chart" is the F2 head; "with answers" never promised |
| de | 12 · 6 (Sonne Wolke Regen Gewitter Schnee Regenbogen; capital nouns) · 6 ("An welchem Tag scheint die Sonne?" / "regnet es?" / "schneit es?" / "gibt es ein Gewitter?" / "ist es bewölkt?" / "gibt es einen Regenbogen?") · 4 (Verdunstung Kondensation Niederschlag + stage 4) · 3 (kalt warm heiß) = 31 | none | none | never the vocab singulars Regentropfen / Schneeflocke (validator); Gewitter (n) vs Regen (m) only matters in `bankWords`; `Regenbogen` 10 boxes (m); Wind / Nebel have no art (d3 distractors only); engine appends "Arbeitsblatt"; title never "Wetter" |
| es (MX) | 12 · 6 (sol nube lluvia tormenta nieve arcoíris) · 6 ("¿Qué día hace sol?" / "llueve" / "nieva" / "hay tormenta" / "está nublado" / "hay arcoíris") · 4 (evaporación condensación precipitación + stage 4) · 3 (frío templado calor) = 31 | none | none | title "El estado del tiempo" (SEP); never "clima" in a title; `tormenta` one token (vocab `Tormenta eléctrica` is two, m); `arcoíris` 8 boxes (m); `lluvia` never "gota de lluvia" (space = validator fail); `tercer-grado` on F4 |
| pt (BR) | 12 · 6 (sol nuvem chuva tempestade neve arco-íris) · 6 ("Em que dia faz sol?" / "chove" / "neva" / "tem tempestade" / "está nublado" / "tem arco-íris") · 4 (evaporação condensação precipitação + stage 4) · 3 (frio ameno quente) + `forecastDays` 5 (segunda terça quarta quinta sexta) = 36 | `weekStart 0` (m): `diaryStart` may be `'sun'` | none | title UNMEASURED (`OPEN 1`; "tempo atmosférico" in meta); never "clima"; `arco-íris` = 4 + 4 boxes with a printed hyphen; `tempestade` 10 (m); the F5 pill row needs the short day forms (~645 *est.* with the full forms); "atividade" never in a title; `3o-ano` on F4 |
| fr | 12 · 6 (soleil nuage pluie orage neige arc-en-ciel) · 6 ("Quel jour fait-il beau ?" / "pleut-il ?" / "neige-t-il ?" / "y a-t-il de l'orage ?" / "est-il nuageux ?" / "y a-t-il un arc-en-ciel ?"; NBSP before `?`) · 4 (évaporation condensation précipitations ruissellement) · 3 (froid doux chaud) = 31 | none | none | `arc-en-ciel` = 3 + 2 + 4 boxes, two printed hyphens (no refusal); keep OUT "la météo des émotions" / "du comportement"; "fiche" never in a title; "la température ce1" = the F3 meta; `ce2` on F4 |
| it | 12 · 6 (sole nuvola pioggia temporale neve arcobaleno) · 6 ("Che giorno c'è il sole?" / "piove" / "nevica" / "c'è il temporale" / "è nuvoloso" / "c'è l'arcobaleno") · 4 (evaporazione condensazione precipitazione raccolta) · 3 (freddo mite caldo) = 31 | none | none | "meteo" NEVER in a title (theme slug), bare "il tempo" = time; `arcobaleno` 10, `temporale` 9 (m); "scheda" never in a title; the thinnest market |
| nl | 12 · 6 (zon wolk regen onweer sneeuw regenboog) · 6 ("Op welke dag schijnt de zon?" / "regent het?" / "sneeuwt het?" / "onweert het?" / "is het bewolkt?" / "is er een regenboog?") · 4 (verdamping condensatie neerslag + stage 4) · 3 (koud warm heet) = 31 | none | F4 above band: the landing says groep 5-6 (the `groep-5` key) | `bankWords` "de zon, de regen, de sneeuw, het onweer" (d3 only); `regenboog` 9 (m); IJ absent from the six; engine appends "werkblad"; title never "Weer" |
| sv | 12 · 6 (sol moln regn åska snö regnbåge; indefinite) · 6 ("Vilken dag skiner solen?" / "regnar det?" / "snöar det?" / "åskar det?" / "är det molnigt?" / "syns en regnbåge?") · 4 (avdunstning kondensation nederbörd + stage 4) · 3 (kallt varmt hett) = 31 | none | none | definite "vädret" is the forecast register (off the sheet); `åska` not the vocab "Åskväder" (validator: never the vocab thunderstorm form); `regnbåge` 8 (m); engine appends "arbetsblad"; `[NSR]` |
| da | 12 · 6 (sol sky regn torden sne regnbue) · 6 · 4 (fordampning + fortætning | kondensering + nedbør + stage 4) · 3 (koldt varmt hedt) = 31 | none | none | "vejret opgaver" is the demand phrase (meta); `torden` 6 (m); engine appends "opgaver"; `[NSR]` |
| no | 12 · 6 (sol sky regn torden snø regnbue; bokmål) · 6 · 4 (fordamping kondensasjon nedbør + stage 4) · 3 (kaldt varmt hett) = 31 | none | F4 above band: the `4-trinn` key IS the placement (honest by construction) | title "Værsymboler" avoids the bare imperative "vær"; `regnbue` 7 (m); engine appends "oppgaver"; `[NSR]` |
| fi | 12 · 6 (aurinko pilvi sade ukkonen lumi sateenkaari) · 6 ("Minä päivänä paistaa aurinko?" / "sataa?" / "sataa lunta?" / "on ukkonen?" / "on pilvistä?" / "näkyy sateenkaari?") · 4 (haihtuminen tiivistyminen sadanta + stage 4) · 3 (kylmä lämmin kuuma) = 31 | none | none | `[NSR-FLAG][fi]` h1 "Säätilat" vs "Säämerkit" / "Sääsymbolit" (`OPEN 2`); `sateenkaari` 11 boxes = 526 fits (m); instruction cases are the panel's (illative "oikeaan sanaan"); the 4-line fi title = the 677 budget on F2 (676) and F3 (676): the engineer re-measures both; `[NSR]` |

Every panel OPENS the 6 concept pictures + the 3 gear chips in its own render (the sv #35 `fruits/plum` precedent) and reads one render of every face in its locale.

## 5 Data + gates

`data/b4/weather-symbols.json` (GLOBAL, one file) + `data/b4/weather.js` (EN literals, hand-authored) + `data/b4/locales/weather.<loc>.json` GENERATED from `i18n/.draft-b4-<loc>.json` by `tools/apply-b4-locale.js` after `tools/validate-b4-draft.js` (clones of the b3 pair, both exist for b3, m; `data/` gitignored, `git add -f`); read through `lib/b4-common.js bank('weather', loc)` (README open item 3).

```
WEATHER_SYMBOLS (data/b4/weather-symbols.json):
  symbols: [ {key:'sun',     theme:'weather', noun:'sunny',        alt:['sun'],      picOpened:true},
             {key:'cloud',   theme:'weather', noun:'cloudy',       alt:['cloud'],    picOpened:true},
             {key:'rain',    theme:'weather', noun:'rainy',        alt:['raindrop'], picOpened:true},
             {key:'storm',   theme:'weather', noun:'thunderstorm', alt:['stormy'],   picOpened:true},
             {key:'snow',    theme:'weather', noun:'snowflake',    alt:[],           picOpened:true},
             {key:'rainbow', theme:'weather', noun:'rainbow',      alt:[],           picOpened:true} ]
  excluded: ['hot','cold','mittens','umbrella','raincoat','puddle','thermometer','partly_cloudy','rain_boots']
  gear:     [ {key:'scarf',      theme:'weather',  noun:'scarf',      band:'cold', picOpened:true},
              {key:'t-shirt',    theme:'clothing', noun:'t-shirt',    band:'warm', picOpened:true},
              {key:'sunglasses', theme:'weather',  noun:'sunglasses', band:'hot',  picOpened:true} ]
  bands:    { order:['cold','warm','hot'], min:-10, max:40, step:5, edges:[10,25],
              values:{cold:[-10,-5,0,5], warm:[15,20], hot:[30,35]}, keyValues:{cold:0, warm:17.5, hot:32.5},
              tints:{cold:'tealSoft', warm:'white', hot:'coralSoft'} }
  cycle:    ['evaporation','condensation','precipitation','collection']   // markers 1..4 in this order: sea-left, cloud-top, mountain-right, water-bottom
  cycleExtra: ['runoff']                                                    // d3 only
  neverPrint: ['sunny','rainy','cloudy','stormy']                           // vocab adjective keys; asserted against every authored field and the render
  diary:    { days:7, start:'mon' }      forecast: { days:[1,2,3,4,5] }    // calendar.js indices (Mon..Fri)
WEATHER[loc] = {
  symbolWords:  { sun, cloud, rain, storm, snow, rainbow },        // whole NOUN literals; single token or hyphenated; never a vocab adjective
  forecastAsks: { sun, cloud, rain, storm, snow, rainbow },        // full questions; contain no day name
  cycleLabels:  { evaporation, condensation, precipitation, collection, runoff? },
  bandWords:    { cold, warm, hot },                                // landing prose only
  forecastDays: [5] | null,   diaryDays: [7] | null,   diaryStart: 'mon' | 'sun',
  bankWords:    { ...6 } | {},   distractors: [2] | [],           // d3 only
  refuse:       [],                                                 // 'write'|'diary'|'thermometer'|'water-cycle'|'forecast'
  strings:      { 'K-356':{title, instruction}, <F1..F5 ids>:{title, instruction} } }
```
Helper contract: `bank('weather', loc)` returns the merged object; `symbolWord(loc, key)`, `askFor`, `cycleLabel`, `dayLabel(loc, i)` throw on a missing literal (never fall back to the vocab); `bandOf(value)`, `gearBand(key)`, `keyValue(band)` read the global file.

**`tools/validate-b4-draft.js` (weather block; every rule runs, exit 1 on any):**
1. `symbols` = exactly the 6 keys; every `noun` resolves via `fileUri('weather', noun)` (throws on a missing cache entry), has a vocab entry in all 11 locales, is not in `excluded`, has `picOpened:true`; no `alt` noun appears as a `noun`; `excluded` ∩ (nouns ∪ gear nouns) = ∅; no gear noun in `symbols`.
2. `gear` = exactly 3 with distinct `band` covering `bands.order`; every gear noun resolves via `fileUri(theme, noun)` and has vocab x11; `mittens`, `umbrella`, `raincoat`, `hot`, `cold`, `sun`, `sunny`, `snowflake` never in `gear` (the K-211 / K-207 / symbol fence).
3. `bands` tile `min..max` exactly in `order`; every `values[b]` strictly inside its band (no value equals an edge; none outside `min..max`; `max` itself excluded); `keyValues[b]` inside its band; tints are token NAMES; `cycle` = the 4 fixed keys in order.
4. `symbolWords[key]` non-empty, matches `/^[\p{L}][\p{L}'’-]*$/u` (no space; rejects "gota de lluvia"), 6 pairwise distinct (`toLocaleLowerCase(loc)`), NEVER equal (case-insensitive) to `vocab[neverPrint][loc][0]` (the adjective ban) nor to `vocab[raindrop|snowflake|thunderstorm][loc][0]` (Regen not Regentropfen, åska not Åskväder; exempt the keys whose concept noun IS the vocab noun: sun, cloud, rainbow).
5. `[...symbolWords[key].replace(/-/g,'')].length <= 11` (the 526 px lane, m) else `refuse` names `write`; every hyphen group >= 1 letter.
6. `forecastAsks[key]` non-empty, ends with `?` (or NBSP + `?` in fr), contains NO `dayNames` / `dayAbbr` / `forecastDays` token of the locale (word boundary via `(?<!\p{L})…(?!\p{L})`, never `\b`), no digit, <= 70 chars; 6 distinct.
7. `cycleLabels` 4 (5 with `runoff`) non-empty, distinct, no space unless `multiWord:true` (then d3 letter-box variants are refused, never faked); `bandWords` 3 distinct.
8. `forecastDays` (if present) = 5 literals; the five-pill row measured at Baloo 2 700 16 with padding 10 + gap 8 <= 639 in EVERY locale (default or authored), else `refuse` names `forecast`; `diaryDays` (if present) = 7; `bankWords[key]` ends with `symbolWords[key]`; `distractors` never equal a gear noun of the locale.
9. Titles <= 70, no worksheet-word (`WORKSHEET_WORD` regex of the b3 gate), unique in the band, NEVER equal (case-insensitive) to `axes.theme.weather.name.<loc>` or `.slug.<loc>` and never the bare theme word as the whole first token in es / pt / it ("Clima", "Meteo"); the F4 title must NOT contain the base head's first token and MUST contain the locale's water-cycle head token (de "Wasserkreislauf", en "Water Cycle"); the F3 title must not equal G3-345's title of the locale ("Hot or Cold?" en); instructions <= 150; no visible string contains the locale's free-word; no visible string on F3 contains a digit or a degree sign; no authored field contains a `neverPrint` adjective form.
10. `strings` ids === {base + the 5 allocated face ids}; the six titles differ pairwise by at least one NOUN or verb token (never only an adjective); `diaryStart` ∈ {mon, sun}.

**Poison (each must FAIL for its OWN reason; the correct EN draft is the control):** P1 `symbols[2].noun = 'puddle'` -> rule 1 · P2 `symbols += {key:'wind', noun:'windy'}` -> rule 1 · P3 `gear += {key:'mittens', band:'cold'}` -> rule 2 · P4 `gear[2] = {key:'hot', noun:'hot', band:'hot'}` -> rule 2 (a sun as a chip) · P5 `bands.values.warm = [10,15]` -> rule 3 (edge value) · P6 de `symbolWords.sun = 'Sonnig'` -> rule 4 · P7 de `symbolWords.rain = 'Regentropfen'` -> rule 4 · P8 es `symbolWords.rain = 'gota de lluvia'` -> rule 4 · P9 fi `symbolWords.rainbow` = a 12-letter variant -> rule 5 · P10 en `forecastAsks.rain = 'Is it rainy on Monday?'` -> rule 6 · P11 fr `forecastAsks.sun` without `?` -> rule 6 · P12 pt `forecastDays = null` with the full names -> rule 8 (row > 639, measured) · P13 sv title `'Väder'` -> rule 9 · P14 en F4 title `'Weather Symbols: The Water Cycle'` -> rule 9 · P15 en F3 title `'Hot or Cold?'` -> rule 9 · P16 en F3 instruction `'Circle the picture for 20 °C'` -> rule 9 · P17 F1 title `'Weather Symbols: Big Words'` -> rule 10.

**`qa/verify-b4-weather.js` (render gate; clone the b3 shape):** renders base + 5 faces x 11 locales x d2 through `render/render-instance.js` (file:// fonts) under the 722 stack (3-line de title + 150-char instruction) AND the 4-line fi title (677); asserts `verify()` empty and `qa/lints.js` clean, then the floors ITSELF (no size lint exists): symbol pictures >= 56 (base / F2 key / F5 strip 88), word tiles >= 18 px, letter boxes >= 44 (F1), diary boxes >= 136 x 200, gear chips >= 56 with 64 boxes, key row present with three bands in order, forecast pills >= 44 high and text >= 16, the water-cycle diagram >= 480 wide with every arrow group present, lanes empty, the thermometer SVGs contain no `<text>` and the F3 body no digit / degree sign, no `[data-lcs-answer]` with a non-empty value, every printed literal === the locale bank (node cross-check), no `img` from a localized B&W dir, no text node equal to a `neverPrint` adjective, everything above the footer, titles <= 2 lines in de / fi, base tile text width <= 226, F5 pill rows `scrollWidth <= clientWidth`. SWEEP: 20 seeds x d2 per face render distinct pages (base right column varies and is never the left order; F1 row + bank order vary; F3 band assignment + chip order vary; F4 bank order varies while markers stay in cycle order; F5 strip set + asks vary); F2 is seed-invariant by design (asserted identical). **Render poisons (each must FAIL):** PR1 base right column in left order -> position leak · PR2 base with `raindrop` swapped in beside `rainy` -> "two pictures of one concept" · PR3 F1 d2 rendered WITHOUT a bank -> "bank absent at d2" · PR4 F1 fr `arc-en-ciel` rendered as 11 boxes -> `groups` mismatch · PR5 F2 a row printing a date numeral -> "numeral on the diary" · PR6 F3 built with `numerals:true` -> "text inside the thermometer" · PR7 F3 mercury at value 10 -> "value on a band edge" · PR8 F3 two cards of one row both `hot` -> row rule · PR9 F3 a `mittens` chip -> gear fence · PR10 F3 a 32 px gear picture placed beside an ITEM thermometer -> "legend on an item" · PR11 F3 the key row removed -> "no key" · PR12 G3-345 d2 re-rendered with the `numerals` default flipped -> byte-diff · PR13 F4 markers numbered in a shuffled order -> "markers not in cycle order" · PR14 F4 bank in cycle order -> "bank order = cycle" · PR15 F4 a lane pre-filled with "evaporation" -> "lane not empty" · PR16 F4 a marker off its anchor by 20 px -> anchor check · PR17 F5 strip with `rain` twice -> "ask has two days" · PR18 F5 an ask literal containing "Monday" -> "day name in the question" · PR19 F5 chips labelled `dayAbbr` while the strip prints `dayNames` -> "chip text ≠ strip header" · PR20 `answerBox({w,h})` in place of an open lane -> `data-lcs-answer="undefined"` · PR21 base d2 with `pairs:8` resolved from a level-index guard -> the config guard fires before render · PR22 F2 diary cells at the fi 677 chrome with `minmax(340px,1fr)` -> footer lint.

**Page reads:** `WEATHER[loc]`, `WEATHER_SYMBOLS`, `calendar.js NAMES[loc]`, `fileUri`, `thermometer.js` (with `bands` + `numerals:false`), `water-cycle.js`; never `image-vocabulary.js`, `articles.js`, `objForms` or `approved-words-*.json` at render.

## 6 SEO plan

| face | title pattern (Germanic / Romance / Nordic + fi) | meta MIDDLE (whole 120-170; the child's instruction, en) | coordinate |
|---|---|---|---|
| base | Weather Symbols · Wettersymbole · Weersymbolen / El estado del tiempo · Como está o tempo hoje · Le temps qu'il fait · Il tempo atmosferico / Vädersymboler · Vejrsymboler · Værsymboler · Säätilat | Draw a line from each weather symbol to its word | `{type:'weather', mode:'base', theme:'', level:<K key>}` |
| F1 | Weather Symbols: Write the Word · Wettersymbole: Wetterwörter schreiben · Weersymbolen: schrijf het woord / El estado del tiempo: escribe la palabra · Como está o tempo hoje: escreva a palavra · Le temps qu'il fait : écris le mot · Il tempo atmosferico: scrivi la parola / Vädersymboler: skriv ordet · Vejrsymboler: skriv ordet · Værsymboler: skriv ordet · Säätilat: kirjoita sana | Find each weather symbol's word in the bank and write it, one letter in each box | `mode:'write'`, G1 |
| F2 | Weather Chart: My Week · Wetter beobachten: meine Woche · Het weer bijhouden: mijn week / El estado del tiempo: mi semana · Como está o tempo hoje: minha semana · La météo du jour : ma semaine · Il tempo atmosferico: la mia settimana / Väderdagbok: min vecka · Vejrsymboler: min uge · Værsymboler: min uke · Säätilat: minun viikkoni | Every day this week, look outside and draw the weather symbol for that day in its box | `mode:'diary'`, K |
| F3 | Weather Symbols: How Warm Is It? · Wettersymbole: Wie warm ist es? · Weersymbolen: hoe warm is het? / El estado del tiempo: ¿frío o calor? · Como está o tempo hoje: frio ou quente? · La température : chaud ou froid ? · Il tempo atmosferico: freddo o caldo? / Vädersymboler: kallt eller varmt? · Vejrsymboler: koldt eller varmt? · Værsymboler: kaldt eller varmt? · Säätilat: kylmä vai kuuma? | Look at each thermometer, use the key to see if it is cold, warm or hot, and circle the picture that fits | `mode:'thermometer'`, K |
| F4 | The Water Cycle: Label the Diagram · Wasserkreislauf beschriften · De waterkringloop: benoem de stappen / El ciclo del agua: escribe las etapas · O ciclo da água: complete as etapas · Le cycle de l'eau : légende le schéma · Il ciclo dell'acqua: completa lo schema / Vattnets kretslopp: skriv stegen · Vandets kredsløb: skriv trinene · Vannets kretsløp: skriv stegene · Veden kiertokulku: nimeä vaiheet | Read the four words in the bank and write each one on the line with the same number as its arrow | `mode:'water-cycle'`, G3 |
| F5 | Weather Symbols: Read the Forecast · Wettersymbole: Wetterbericht lesen · Weersymbolen: het weerbericht lezen / El estado del tiempo: lee el pronóstico · Como está o tempo hoje: a previsão da semana · Le temps qu'il fait : la météo de la semaine · Il tempo atmosferico: leggi le previsioni / Vädersymboler: läs väderprognosen · Vejrsymboler: læs vejrudsigten · Værsymboler: les værmeldingen · Säätilat: lue sääennuste | Look at the weather for the week, read each question and circle the right day | `mode:'forecast'`, G1 |

All titles are candidates the native panels rewrite (<= 70, no worksheet-word, base = the bare compound head, each face adds one NOUN / verb, never an adjective; F4 carries the water-cycle head and NOT the weather head; the pt base title waits on `OPEN 1`). `coordinate.mode` is ALWAYS the face's mode string (base `'base'`), `coordinate.theme:''` (fixed theme), level = the K key for base / F2 / F3, the G1 key for F1 / F5, the G3 key for F4 (a locale publishing F3 at G1 swaps the key in its landing file only). h1 = title; eyebrow = level label; strand chip = the table-B literal (framework NAME only; the build adds the 11-locale Science row to `strand-names.ts`, never the English fallback). JSON-LD `LearningResource` with NO `educationalAlignment` on any face; en prose names NGSS K-ESS2-1 on base / F2 / F5 and states on F4 that the water cycle has no K-3 NGSS anchor (5-ESS2-1); nl F4 says groep 5-6, no F4 says 4. trinn, every F4 meta says G3. Meta lead inherits `seo.words.free_printable` (metadata only; no visible copy claims "free"; the MIDDLE is the instruction; the per-locale window is measured with `tools/measure-instruction-window.js`, exists, m). `topicMeta.weather` >= 50 x11 + `skill-sentences.<loc>.json.weather` via the `register-b4-en-content.js` clone. Printable-only decks ship NO answer key: no title, meta or landing promises one.

**Non-cannibalisation** (whole-landing 3-gram Jaccard, `scripts/seo-landing/gate.js` FAIL >= 0.80 / WARN 0.65; *est.*, the gate measures):

| pair | what the copy says differently | est. |
|---|---|---|
| base vs F1 | draw lines symbol-to-word vs copy the word from a bank into letter boxes; K vs grade 1 | 0.35 |
| base vs K-225 (word to picture) | a fixed six-symbol chart with weather nouns vs any-theme vocab cards; the base p1 names the six symbols | 0.20 |
| F1 vs G1-244 (write the word) | the six weather symbols, hyphen groups given; G1-244 is theme-generic | 0.20 |
| F2 vs G2-277 (calendar) / K-321 (days) | seven day cells to draw in, no dates, no month, no ordering task | 0.15 |
| F2 vs F5 | draw your own week (open) vs read a given week and circle a day | 0.30 |
| F3 vs G3-345 (read the thermometer) | cold / warm / hot bands read against a key, gear chips, no numerals, no writing a value | 0.25 |
| F3 vs K-211 (hot and cold sort) / K-207 (clothes sort) | an instrument read per card, three fixed chips, no bins, no sort; the boundary sentence names both | 0.20 |
| F4 vs any weather face | a diagram, four stage words, grade 3; the water-cycle head | 0.10 |
| F4 vs science-sequence G1-203 | label a cycle diagram vs order picture cards | 0.15 |
| F5 vs G1-146 / G1-147 (count into a table / graph) | nothing counted; a question answered by circling a day | 0.15 |
| F5 vs G1-341 (true or false) | no sentence is judged; the answer is a day chip | 0.15 |
| any face vs K-322 (seasons) | no season word, no month-to-season map | 0.10 |

Boundary sentence on every landing: "The child works with the six weather symbols: matches, writes, records and reads them, and reads a thermometer by its band against a key. Sorting hot and cold things is on the Hot and Cold page, clothes on the Summer and Winter Clothes page, and reading a temperature on the Reading a Thermometer page."

## 7 Hub visibility contract

A face appears under `weather` on `/[locale]/worksheets` IFF all four hold (brief): (1) `apps.weather` exists in `frontend/config/topics-taxonomy.json` (ABSENT today, m; missing = rendered NOWHERE in the rail) with `default_subject:'science'`; (2) `axes['exercise-type'].weather` has `slug` + `name` in all 11 locales (§1 table B; the 11 slugs measured collision-free); (3) exactly one landing per face per locale with `coordinate.type === 'weather'` verbatim, `mode` per §6, `theme:''`, the level key of §1 (K for base F2 F3, G1 for F1 F5, G3 for F4), a unique slug and `canonicalDeckSlug` = the published deck; a REFUSED face has NO landing and the expectation is lowered explicitly, never padded; (4) the landing JSON committed AND deployed (per-process cache).

Gate: `node scripts/verify-hub-type-rows.js --keys=weather` (script EXISTS since nt20-C, m; expectations from `hub-expectations.json` exported by `tools/export-hub-expectations.js`, exists, m; poison = drop one landing / drop `apps.weather` / a wrong `coordinate.type` must FAIL).

**Expected rows per locale:** en 6 · de 6 · es 6 · pt 6 · fr 6 · it 6 · nl 6 · sv 6 · da 6 · no 6 · fi 6 = **66**. No face is refused in any locale: symbol words, forecast questions and cycle labels are panel literals in all 11 (every candidate noun is a single or hyphenated token of <= 11 letters, m); day names exist x11 (m); pictures are global; the two honest floors (F4 four labels, F5 three asks) are RULED exceptions, not refusals; nl / no F4 above band and en F4 code-less are landing statements, not refusals. Contingencies, each recorded in the draft before the wave, none expected to fire: a base WORD wider than 226 px at Baloo 24 (worst estimate fi `sateenkaari` ~140; would refuse the word, not the face, and the face needs 6 = the K floor, so it WOULD drop that locale to 5: the validator measures before the wave); a pt F5 pill row > 639 with the panel refusing the short day forms (0 expected; the short forms are the panel's own suggestion); a locale panel refusing F3's gear framing outright (no panel signalled it).
