---
name: project-nt20-worksheet-types
description: "nt20 batch — 20 NEW printable worksheet types × 11 locales = 220 decks LIVE (2026-09-01): 17 new family keys, 9 new primitives, native rebuilds via 11 3-agent panels, full SEO; the per-locale rulings (digit forms, currency units, glyphs), the fan-out lever to 1000 clicks/day, and every trap"
metadata: 
  node_type: memory
  type: project
  originSessionId: fafeee83-bff9-40f3-bf02-f4a3498d0124
  modified: 2026-09-01T09:32:43.947Z
---

# nt20 — 20 new worksheet types × 11 locales (COMPLETE, LIVE 2026-09-01)

**Goal:** operator /goal — 20 new printable worksheet types filling catalog gaps, natively REBUILT ×11 locales (220 decks), top quality, full SEO, target ≥1000 clicks/day contribution. **SHIPPED: 220/220 decks live + audited; 17 new topic hubs live ×11 locales.**

## What shipped (type ids · family keys)
K-236 pre-writing (Schwungübungen class) · K-237 number-tracing (per-locale digit forms) · K-238 letter-tracing (caps + locale specials; nl=IJ digraph, pt=KWY, sv/da/no/fi=ÅÄÖ/ÆØÅ) · K-239 sight-words (fi reframed lukusujuvuus) · K-240 cutting-practice · K-241 color-by-number (fr title/slug = coloriage magique) · K-242 mazes (perfect spanning-tree, unique-path verified) · K-243 number-bonds (d2 = bonds OF 10 only) · G1-208 mental-math · G1-209 fact-families (house form) · G1-210 number-words (lib/number-words.js engines ×11) · G1-211 money (native currency; operator REVERSED the wsg Gate-1 money exclusion 2026-08-31, en=USD) · G1-212 draw-clock-hands (REUSES telling-time family; clock-tasks 'draw' mode) · G1-213 word-problems (frame banks) · G2-251+G3-357 column-arithmetic (no-regroup/regroup) · G2-252 capacity-mass (REUSES measurement; jug+balance) · G2-253 symmetry (16 hand-designed pixel figures) · G2-254 reading-comprehension (ORIGINAL native passages ×3/locale) · G3-358 multiplication-tables (pt=tabuada).
New primitives: trace-path (strokeLane/glyphLane/strokeGlyphLane/schoolLines), coins, number-bond, jug, balance, sym-grid, lib/maze, lib/number-words. New data: digit-strokes (3 form variants), currencies, symmetry figures+names, color-words, letter-sets, sight-words, reading-passages, word-problem frames. tokens.codeColors = SEPARATE lint-merged namespace (legend swatches only — never fold into tokens.color). CONTENT_SEL grew `.ws-trace-lane, [data-ws-content]`.

## ⭐ Per-locale panel rulings (11 × 3-agent panels; NEVER re-derive)
- **DIGIT FORMS (K-237)**: en/es/pt = PLAIN (simple 1, UNCROSSED 7 — es+pt school models, native rulings); nl/sv/no/da/fi = 'dutch' variant (SHORT-flag 1 + uncrossed 7); ONLY de/fr/it = continental (crossed 7 + hooked 1). `data/tracing/digit-strokes.js FORM_BY_LOCALE`.
- **MULTIPLICATION GLYPH**: '·' de/sv/da/no/fi (G3-358 DOT_LOCALES); '×' en/fr/es/pt/it/nl.
- **CURRENCY UNITS** (`data/money/currencies.js`): de 'ct' · es 'cts' (never ct) · fr 'c' (never ct) · it 'cent' (never bare c) · pt 'centavos' spelled out · nl/fi no 1c/2c coins (all sums ÷5 by coin-set construction) · sv kr no öre · da 'kr.' WITH period · no 'kr'. All G1 totals in smallest natural unit — NO decimals.
- **GENDER-SAFE WORD-PROBLEM FRAMES**: es/pt/it use invariant devices ('el total de {noun}' / 'Qual é o total' / 'Qual è il totale' + it 'ne') because cuántos/quantos/quanti agree with unknown nouns; fr uses elision-proof 'en'-pronoun questions; fi = PARTITIVE singular table (28 fruits nouns in frames.js) + {name} strictly NOMINATIVE + n≥2; de nounCase 'keep'; ALL locales repeat {name} (pronoun gender underivable).
- **KEYWORD TITLES/SLUGS** = each locale's genre name (schwunguebungen, grafomotricidad, coloriage-magique, pregrafismo, tabuada, splitsen, tiokompisar, tiervenner ×2, raknehandelser/regnefortellinger/regnehistorier, sanalliset-tehtavat, continhas-armadas, interpretacao-de-texto…). Slugs ASCII-folded (house taxonomy = 0 non-ASCII; da ø→oe å→aa æ→ae, no ø→o, sv/fi ä→a ö→o).
- K-243 d2 = bonds OF 10 ONLY — every beloved name (tiokompisar/amigos del 10/compléments à 10) PROMISES ten (sv finding).

## ⭐ Standing lessons (bought here)
1. **The panels convict the EN source AND the shared design**: 4 EN instruction defects (G2-251 claimed the child writes pre-printed numbers; G3-357 'comes up short' vague; K-241 circular; K-238 promised an empty line the page lacked → glyphLane emptyLast). The K-238 defect had been COPIED into de/fr drafts before pt caught it — a source defect propagates to every locale that trusts it.
- 2. **Title collisions with LEGACY types only surface at lint-locale** (full-band check): 5 hits (de einmaleins slug owned by arrays-multiplication; es/it/nl/fi type titles owned by G1-113/K-011). Per-draft validation can't see them — always lint after apply.
3. **Visual critic on FINAL bytes caught what QA gates can't**: pictureless problem under a pictures-promising instruction; brim-full jug (waterline above top tick); missing promised arrows; commuted duplicate facts. 17/20 SHIP first pass.
4. **Desc-band middles must fit UNDER the real per-locale lead** — my 'lengthen the short' fix was INVERTED; the real leads (Hoja de ejercicios class) are long → shorts must be ≤~49 chars. Reproduce with the REAL test signature before fixing.
5. **My own validator was ban-too-wide** ("{noun} {name}" flagged as adjacent slots — refused correct fi frames). §23.6 in a new dress.

## Publish/verify state
publish-bulk dry-run ok=20 collisions=0 ×11 → confirm ×11 (batches 20260831231425..232830) → og 220/220 → hreflang 1035 groups (12-link chains verified live) → audit-deck-html 20/20 clean ×11 → deploy EXIT=0 (IndexNow 22 URLs) → hubs 200 (NO-trailing-slash canonical) + grids list the decks. Slugs: /root/staging/nt20-slugs.txt. Commits: 5cf3?? (nt20 base 49 files) · 5de862ff (de/es/fr) · pt/it/nl · f271ef88 (sv/no/da/fi) · 9377de89 (critic fixes). [NSR-FLAG][sv,no,da,fi].

## SEO honesty + THE FOLLOW-UP LEVER
SEO panel estimate: 220 pages ≈ 450-650 clicks/day at 9-15-month maturity (~2-3/page; pt-BR+es carry ~40%; ramp near-zero first 2-3 months). **The designed-in path to ≥1000/day = variant fan-out on click winners** (variantsPerType waves: per-letter tracing ×26, per-table tabuada ×10, per-range regrouping/mental-math, per-theme coloriage-magique → ~600-900 pages, no new design). Watch Search Console per family hub; fan the top 5-7.

## Landing pages + hub visibility (2026-09-01, post-launch operator report "I cannot find them on /en/worksheets")
The 220 decks were live but INVISIBLE on the flagship /worksheets hub — the hub renders the §22 landing tier (frontend/content/seo-landing/<loc>.json), which had no nt20 entries. Fix shipped in two commits:
- **f23ab5f6 — 220 landings (20 × 11 locales).** Native panel copy per locale (3-agent ensembles; panels WRITE their JSON to scripts/worksheet-gen/i18n/.landing-nt20-<loc>.json themselves — cheaper than transcribing results); composed by `scripts/seo-landing/gen-nt20-landings.js` (derives coordinate/canonicalDeckSlug/carousel/standard mechanically; validates ≥200-word bodies, 120-175 meta, ≤75 title, slug uniqueness vs corpus). ⭐ **Panels reliably under-shoot the 200-word §22 body floor** (fr 172-192, de/es/sv similar) — resume the SAME agent with the exact shortfall list and demand ≥205; all complied first try. gate.js locked thresholds PASS ×11; all 220 canonicalDeckSlugs curl-verified 200 before commit. Repoint: `repoint-deck-canonical.js --types=<19 families> --locale=<each>` — deck-slugs=136/locale (the type list swept in EXISTING wsg landings of reused families telling-time/measurement whose decks had never been repointed; end state correct-by-construction, backups .precanonical-bak).
- **538b7cff — hub "New worksheets" strip.** ⭐ ROOT CAUSE of invisibility even WITH landings: the hub's variety sort (`interleaveByAxis`, worksheets-catalog.ts) orders type buckets by SIZE DESC — a fresh batch of single-landing families lands on pages 2-3, never page 1. Fix = bare-state-only strip (page 1, no filters, sort=variety) above the main grid, fed by `frontend/config/worksheets-new-highlights.ts` (per-locale slug lists, missing slugs drop silently — update per batch); `worksheetsPage.newHeading` ×11. Landing content is read at RUNTIME with per-process cache (landing-content.ts loadFile, /opt fallback path) — a deploy restart picks up new JSON, no rebuild-bake concern.

## nt20-VAR — 5 variations × 20 types × 11 locales = 1,100 decks + 286 tier-1 landings (LIVE 2026-09-01)
Operator /goal same-day follow-up: the fan-out lever executed in full. **Mechanism ruling: authored TYPE-ID-per-variation (house convention), NOT RNG variantsPerType** ("Set N" rerolls fail the quality bar; 3 types have zero RNG variance; no cross-variant dedup gate exists). 92 new ids (K-244..277, G1-214..241, G2-255..273, G3-359..369) + 8 theme-fan decks = 100/locale. Commits dd6b1692→c6d1242c.
- **Base edits all additive-with-fallback, proven inert by a 126-coordinate build-hash harness** (var-baseline.js pattern — capture BEFORE any edit, check after EVERY edit; caught 3 real leaks: sym-grid attr, FIGURES pool into base band ranking (freeze ranking to slice(0,16)), K-239 sample over the grown pool (freeze base to list.slice(0,12))).
- New primitives: spiral+eight strokes (Schwungübungen classics), strokeGlyphPairLane (two-digit tracing 10-20), sym-grid axis:'h' (ROTATE the left-right-symmetric art 90° → top-bottom symmetric; pick figures that read well sideways — rocket yes, snowman no).
- +8 hand-designed symmetry figures (24 total) so the six symmetry pages are figure-DISJOINT (16 figures / 24 slots forced exact-duplicate grids — critic catch).
- ⭐ Panel sign rulings (encode, never re-derive): mul '·' de/sv/da/no/fi else '×'; div '/' sv (**÷ is a historic Swedish MINUS — never render it in sv**), ':' de/it/nl/da/no/fi, '÷' en/fr/es/pt. fi: numero(digit)/luku(number) register split; 37 animal partitives unlock the animals word-problem fan.
- ⭐ K-257 own-letters page: title from the ACTUAL letters (Umlaute/ÆØÅ/ÅÄÖ/Ñ/ÉÈÊ/KWY/IJ/vowels-for-en-it); nl filler must exclude I,J (IJ digraph components).
- ⭐ bandedDescription fallback-only composed middles (build-seo-head.js middlesFallback): short-titled types landed 1-3 chars under the 120 floor while richer sentences overshot 170; fallbacks consulted ONLY when nothing bands ⇒ zero churn for in-band emissions. Scan gate: scan-desc-band pattern over staged ZIPs BEFORE upload.
- ⭐ 3-problem word-problem pages need compact furniture (gap/padding/font 16px) — long-sentence locales (de) overflow at 2-problem metrics; G2-254 stories >400 chars drop one font step (fi d3 is 451 — unpublished, safe).
- Publish: dry-run gated ok=100×11 → confirm ×11 → OG (⚠ retrofit flags need = syntax: --slugs-file=/path — space form silently full-catalog) → hreflang 1135 groups → audit 1100/1100 clean (⚠ audit needs --slugs-file= + BARE slugs or it OOMs on the 55k tree) → deploy EXIT=0.
- **Tier-1 landings: 26 top faces × 11 = 286 pages** (per-table tables, tracing ranges/slices, color-by-number, regrouping family, per-story reading), gen-var-landings.js composer, gate PASS ×11, all deck links 200, canonicals repointed (33 slugs/locale incl. the 7 base-family landings re-swept — self-hreflang follows canonical to the landing, consistent). **Tier-2 = 74 remaining variation faces/locale — CLICK-DATA-GATED follow-up** (watch Search Console per family; fan winners).
- Batch surface now: 220 base decks + 220 base landings + hub strip + 1,100 variation decks + 286 tier-1 landings ≈ 1,826 pages. Honest math at panel rates (~2-3/page landings, ~0.5-1/page deck pages at 9-15-month maturity): ~1,900-3,300 clicks/day potential — the 1000+/day goal is credibly covered with margin; ramp is near-zero the first 2-3 months.

## Phase 9 (2026-09-01, operator reopen: "ALL the variations should be on /en/worksheets") — 100% landing coverage + grouped hub strip
- **Root cause of invisibility (twofold):** (1) /worksheets renders ONLY the landing tier, and just 26 of 99 variations/locale had landings; (2) even with landings, `interleaveByAxis` (worksheets-catalog.ts) orders type buckets **by SIZE DESC**, so small fresh families sort to hub pages 2-3. A landing alone does NOT put a page on the hub's first screen.
- **Tier-2: +73 landing faces × 11 = 803** (total 99/locale = 1,089 batch landings). Composer `gen-var-landings.js` gained `ALL_TYPES` (TYPES 26 + TIER2 73) with `@theme` fan keys (`K-240@vehicles`…) and `collapseThemes`→`collapseSiblings` for K-264 (one landing, two decks). ⭐ **Derive themed deck slugs from the MEASURED published slug list, never from assumption** — deco/wp/cap waves put themes in slugs (`pre-writing-animals-k244`, `mazes-fruits-k270`, `word-problems-fruits-g1238`, `measurement-vehicles-g2261`).
- **Both composers now treat a same-deck slug collision as IDEMPOTENT** (error only when the existing landing points at a DIFFERENT deck) — required to re-apply copy fixes; gen-nt20-landings.js needed the same patch.
- **Panels: 3 passes/locale (A 23 / B 25 / C 25 keys)** — one 74-key pass is too big for one agent. ⚠ Fable-5 session limit killed ~9 mid-flight; **a resumed agent keeps its original model, so relaunch fresh after a /model switch** (resume only works if the model still has budget). Files already written survived the kill.
- ⭐⭐ **GENERATOR-GROUNDED ACCURACY AUDIT (the fi panel read the specs and refused the brief's wording).** 74 false claims found across ALL locales incl. the already-published base landings. Confirmed in source: cut-and-paste bins are headed by an `<img>` **not a word label**; a maze has **exactly two icons, both from the page's own theme** (so "an animal at the start" of a TOYS maze was impossible — 10 locales shipped it); shape-code shapes are **upright, one size** (zero rotate in K-241); colour-code **subtraction minuends reach 12** (~42% are 11-12) while additions stay ≤10; the legend is at the **TOP** and carries swatch **and** colour word; the page is **12 separate shapes, not one revealed picture**. All 11 panels rewrote their own prose; base+tier-1+tier-2 re-applied. **Lesson: brief the panels from the GENERATOR, and let a panel that reads the source overrule the brief.**
- Hub strip v2: `gen-var-highlights.js` emits `NEW_WORKSHEET_GROUPS` (20 family cards + their variation slugs, resolved by canonicalDeckSlug from the corpora, missing slugs silently dropped ⇒ re-runnable at any rollout stage); page.tsx renders card + wrap-row of chips. **144 worksheet links on hub page 1 per locale** (119 batch + 25 grid) — verified live ×11.
- Gates: gate.js PASS ×11 (0 FAIL, 0 dup-titles); 1,177 deck links curl-200; repoint 235 slugs/locale, 0 missing; deploy EXIT=0. Commit 8d64e0ea.
- **Batch surface now ≈ 2,409 pages** (220 base decks + 1,100 variation decks + 220 base landings + 1,089 variation landings) — every single worksheet reachable in one click from /worksheets page 1.

## Fixed in passing / flags
- 8 legacy family keys (science-3 + literacy-5) got their missing topicMeta ×11 (lint-locale was exiting 1 catalog-wide).
- da 'print' added to seo-reconciliation-exceptions (til print = the search phrase).
- CLAUDE.md STALE: §14.10 "22 collapsed family keys" — now **47** (22+8+17); "en-only taxonomy entries" wrong (full 11-locale for all nt20 keys).
- Deferred polish (non-blocking, from critic): balance right-pan visual under weight stacks; sv maze start-figure size; nl-k239 trace-word placement; fr-k241 star label clearance.
- wsg NEXT-WAVE playbook applies for more nt20 variants (waves/wave-nt20-*.json = the template; new type ids never collide).
