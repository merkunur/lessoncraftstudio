---
name: project-activities-live-inventory
description: Current live + operator-approved K-3 activities surface (updated 2026-06-04, post variety/depth arc). 3 manipulatives + 5 E1 + 10 E2 (incl. both Measurement & Data) + 1 E7 EN + **5 E8 (es/it/pt/fr/fi)** + 5 E9 (de/nl/sv/no/da) syllable decks @28 (de/en 30) + **E4 = FIVE activities ×11 (K.OA.A.3 + 1.OA.D.7 + 2.NBT.A.3 + 1.OA.D.8 + 2.NBT.A.4)** + **E12 place-value 3 ×11 = 33**. Variety/depth arc CLOSED (Nordic school-convention carve-out + strand-name localization + syllable/CVC/E4/E12 depth). non-EN activity layer cites national curriculum frameworks (name only, no code) — see [[project-activity-national-framework-localization]]. Mirror of CLAUDE.md §20 at commit 25736aeb.
metadata:
  node_type: memory
  type: project
  originSessionId: aa585226-31b9-437f-b029-655565fce24e
---

**As of 2026-06-04 (post variety/depth arc; CLAUDE.md mirror `25736aeb`):** 3 manipulatives + 5 E1 + 10 E2 + 1 E7 EN + **5 E8 fan-outs (ES/IT/PT/FR/FI)** + 5 E9 Nordic-cluster fan-outs (DE/NL/SV/NO/DA) — all E8/E9 syllable decks @ **28 words** (de/en 30) + **E4 match-pairs at FIVE activities, each 11/11** (A1 K.OA.A.3 + A2 1.OA.D.7 + A3 2.NBT.A.3 + A4 1.OA.D.8 + A5 2.NBT.A.4) + **E12 place-value at THREE activities, each 11/11 = 33** (A1 1.NBT.B.2 + A2 2.NBT.A.1 + A3 2.NBT.A.3) + **E2 choice-tap at 10 activities** incl. both Measurement & Data (Comparing Length K.MD.A.2 + Sort and Count K.MD.B.3) — all live + operator-approved.

## E10 clock / telling time — 2 activities LIVE 11/11 (1.MD.B.3 + 2.MD.C.7)

**E10 #2 — "Tell Time to the Nearest 5 Minutes" (CCSS 2.MD.C.7, Grade 2, Measurement & Data) — live all 11 locales (commit `e7d95741`).** Second coordinate on the proven `clock-core` via an **additive `minuteStep` config** (declared core-touch on the E10 engine; 0 lines to the OTHER protected cores): default 30 (#1) / 5 (#2). Only 3 spots changed — `setupTask` (accept arbitrary minute), `_snapMinute` (snap to nearest minuteStep: `round((angle/6)/step)*step%60`), keyboard minute-step (`+minuteStep`); `_handAngles`/`_snapHour`/`isCorrect`/render were ALREADY minute-general (30H+0.5M / 6M for any minute). **#1 (1.MD.B.3) re-verified 11/11 behavior-identical.** Same SET mechanic (drag the analog hands); 8 five-min rounds (3:15, 7:45, 6:10, 9:20, 4:40, 1:50, 10:05, 12:25); `isCorrect` discrete-exact. **The 5-min IDIOM is harder than the half-hour + native-owned per locale (each agent stated mappings):** quarter-past/quarter-to (de "Viertel nach/vor"), **toward-next-hour** (de "Viertel vor 8"=7:45, "fünf vor halb 1"=12:25), **half-relative** (nl "tien voor half tien"=9:20 / "tien over half vijf"=4:40; Nordic da "ti i halv ti"/no "ti på halv 10"/sv "fem i halv 1"), **Romance subtractive** (es "las ocho menos cuarto"=7:45, fr "huit heures moins le quart", it "le otto meno un quarto", pt "quinze para as oito"), **fi case-correct** (yli→nominative, vaille→partitive; "kymmentä vaille puoli kymmentä"=9:20). **a.m./p.m. DEFERRED** — not representable on a 12h analog SET (hands identical at 3:15 a.m./p.m.); a future digital/context variant. Build-gate `verify-clock-core.js` branches minute-scope by `alignment.code` (1.MD.B.3 {0,30} / 2.MD.C.7 {0,5,…,55}) — exact angles + discriminating grade + timeExpr 11/11. `local-test-clock.js` now runs BOTH activities (#1 regression + #2; keyboardSet honors minuteStep). `clock-activity.js?v=2`/`clock-core.js?v=2`; wrapper v8.74. clock already in MANIFEST_FILES (preflight 11/11). `[NSR-FLAG]` sv/da/no/fi. **Fast-follow: a.m./p.m. digital variant; or E10 done.**

### E10 #1 — "Set the Clock" (1.MD.B.3) — NEW ENGINE, LIVE 11/11 (2026-06-18)

**"Set the Clock" (CCSS 1.MD.B.3, Grade 1, Measurement & Data) — the first E10 activity, live all 11 locales (commit `7c0fe2a4`).** Opens E10 (~25 leverage). NEW core `mini tools/clock-core.js` (`window.ClockCore`) — clean sibling, **0 lines to the 7 protected cores or lcs-shell**, cloned from the fractions/array skeleton. **The platform's FIRST RADIAL-DRAG verb** (no existing core did angle/rotation; number-line/ruler are linear `LCS.drag.linear`). Mechanic: a worded time prompt ("Set the clock to half past 3") → the child DRAGS the two analog hands. **Discrete-state model** (clean exact grading, no angle-fuzz): `setHour∈1..12` + `setMinute∈{0,30}`; dragging the hour hand snaps to nearest hour (minute-offset-aware via `_snapHour` so the half-past position is unambiguous), minute snaps to {0,30}; the RENDER is COUPLED — hour angle = `30·setHour+0.5·setMinute` (hour hand sits BETWEEN hours at half-past), minute = `6·setMinute`. Digital readout mirrors the setting. `answerType:'state'`; `isCorrect = setHour==target && setMinute==target`. Radial drag = sort-bins Pointer-Events pattern (pointerdown+setPointerCapture+touch-action:none) + `atan2(dx,-dy)` angle. Code-drawn SVG face (circle, ticks, numerals 1-12). 8 canonical-time rounds (4 o'clock + 4 half-past) + `nextTask` reshuffle. **Build-gate `scripts/verify-clock-core.js`** drives the real core: hour angle === 30H+0.5M + minute === 6M (exact), discrete grade accepts target + rejects wrong hour/minute, timeExpr present 11/11, minutes∈{0,30}, ≥7. **`local-test-clock.js`** (11-locale: radial-drag smoke on minute + keyboard-deterministic grade since both hands overlap at the 12:00 start + variety + mobile). **THE HALF-HOUR IDIOM is native-owned per locale (the E10-specific risk):** toward-next-hour de "halb 8" / nl "half 8" / sv "halv 8" / da "halv 8" / no "halv 8" / **fi "puoli 8"** ALL = 7:30 (NOT 7); current-hour+half es "las siete y media" / pt "sete e meia" / fr "sept heures et demie" / it "le sette e mezza" = 7:30. Each native author STATED the mapping for verification; singular-one (es/pt/fr/it 1:30) + noon forms (midi/mezzogiorno/meio-dia). **sv/no o'clock = bare digit** (CC caught + fixed a clock-word doubling in the frame "Ställ klockan på klockan 12"→"…på 3"). Registered in `frontend/lib/activities.ts MANIFEST_FILES` (10th engine; preflight 11/11). `ACTIVITY_WRAPPER_VERSION` 8.72→8.73; `clock-core.js?v=1`/`clock-activity.js?v=1`. `[NSR-FLAG]` sv/da/no/fi. **Fast-follow E10 (~24): 2.MD.C.7 to-the-5-minutes on the same core** (extend minute snap to {0,5,…,55}).

## E13 array / multiplication-readiness — NEW ENGINE, first activity LIVE 11/11 (2026-06-18)

**"Build the Array" (CCSS 2.OA.C.4, Grade 2, Operations & Algebraic Thinking) — the first E13 activity, live all 11 locales (commit `5976de72`).** Opens E13 (~35 leverage, the next unbuilt engine after E3/E14). NEW core `mini tools/array-core.js` (`window.ArrayCore`) — a **clean sibling, 0 lines to the 7 protected cores or lcs-shell**, cloned from the fractions-core skeleton (init/setupTask/render/paint/reset + 11-locale strings + idempotent injectCSS). **Mechanic = BUILD-AN-ARRAY verb** (distinct from E2/E4 tap, E14 partition, E3 classify-place): the child taps cells of an exact R×C CSS-grid to fill it (one teal dot/cell, `pointerdown` + `touch-action:manipulation` — tap parity, NO setPointerCapture/drag), a **repeated-addition strip** assembles per completed row (`? + ? = ?` → `4 + 4 + 4 = ?`), then the child types the TOTAL on the **shell keypad** (`answerType:'number'` — zero new answer-surface code). Both 2.OA.C.4 clauses: cell-fill builds the *sum of equal addends*, keypad answers *find the total*; `check = answer === rows×cols`, correct → celebrate + lock. R,C ∈ [2,5], total ≤ 25; 8 distinct R×C rounds (wide+tall mix, e.g. 3×4 AND 4×3) + `nextTask` per-pass reshuffle. **Build-gate `scripts/verify-array-core.js`** drives the real core: fills the full grid + asserts the equation = R equal addends of C summing to rows×cols = the graded total (MEASURED, not authored) + dims 2..5 + ≥7 rounds. **`scripts/local-test-array.js`** (11-locale puppeteer: render grid+strip, tap-fill builds the equation, keypad total grades correct→celebrate/wrong→try-again, ≥7+reshuffle, mobile). 11-locale native authoring — digit-substitution-safe prompt template `"Make {rows} rows of {cols}…"` (de "mit je", fi partitive "joissa kussakin on", sv/da/no "med {cols} i hver/varje", it "righe da", nl "rijen van", fr "rangées de") + per-locale array term (Gitter/arreglo/matriz/quadrillage/schieramento/rooster/rutnät/gitter/rutenett/taulukko); `[NSR-FLAG]` sv/da/no/fi. Registered in `frontend/lib/activities.ts MANIFEST_FILES` (9th engine; preflight 11/11). `ACTIVITY_WRAPPER_VERSION` 8.71→8.72; `array-core.js?v=1`/`array-activity.js?v=1`. **Fast-follow E13 leverage (~34): equal-groups construction, larger arrays, total-given-find-a-dimension — all reuse this core.**

## E3 sort / drag-to-bin — 2 activities LIVE 11/11 (2.G.A.1 + 1.G.A.1)

**E3 #2 — "Spot the Shape" (CCSS 1.G.A.1, Grade 1, Geometry strand) — defining vs non-defining attributes, live all 11 locales (2026-06-18, commit `7c514747`).** Second coordinate on the SAME core via an ADDITIVE `mode:'defining-attributes'` (NOT a sibling core — operator-ruled fork). Mechanic: a target-family box ("Triangles") vs a "Not triangles" box; the TRUE tiles are the target shape drawn in varied COLOUR / SIZE / ORIENTATION (non-defining attributes), so the child recognises the shape by its DEFINING attribute (side count), not its look. Target rotates per round (triangle→quad→pent→hex). **The core extension is the declared core-touch** (sort-bins-core is the E3 engine, not one of the 7 protected cores); **G1 — sort-by-sides path behavior-identical, 2.G.A.1 re-verified 11/11; G2 — non-defining attributes are inert to the placement check** (read only by `_shapeSVG`; the build gate runs the real core with two seeds and asserts the answer key is side-count-derived + decoration-invariant). Per-tile colour/scale/rotation are seeded deterministically (locale-stable). Wrapper selects the defining title/instruction/prompt SYNCHRONOUSLY from `?activity` (the shell reads title at mount before init — no `lcs-shell.js` touch). 8 distinct rounds + `nextTask` reshuffle. 11-locale native authoring: positive bins REUSE the formal polygon labels (naming-standard doctrine §A.13.61); negatives are full-phrase per (target,locale) — de "Keine Dreiecke", es/pt/it plural "No son…/Não são…/Non sono…", fr "Pas des…", nl/Nordic "Geen…/Inga…/Ingen…", **fi partitive-plural "Ei kolmioita"** (NOT a `Not a {X}` token apposition, the §A.13.56 case trap). `[NSR-FLAG]` sv/da/no/fi. Gates: `verify-sort-bins-core.js` (G1 regression + G2 invariance, 16 rounds across 2 coords), `local-test-sort-bins.js` (11×2 = sides-regression + defining), **NEW `scripts/preflight-activity-routes.js`**. `ACTIVITY_WRAPPER_VERSION` 8.70→8.71; `sort-bins-core.js?v=2`/`sort-bins-activity.js?v=2`. **GATE PROMOTED: the MANIFEST_FILES registration (the 54b9a4dc 404 class) is now a BUILD-FAILING preflight (`preflight-activity-routes.js`), not just a learned sync-note** — fails on (A) any `*-activities.json` not in MANIFEST_FILES, (B) empty/non-url/duplicate slug; `--all-locales=<id>` asserts a coordinate ships 11/11 (1.G.A.1 confirmed 11/11). Reused sort-bins is already registered; preflight confirmed regardless. Remaining E3 leverage (~40): more sort/classify skills reuse this core.

## E3 #1 (2.G.A.1) — first activity LIVE 11/11 (2026-06-18)

**"Sort Shapes by Sides" (CCSS 2.G.A.1, Grade 2, Geometry strand) — the first E3 activity, live all 11 locales.** The child DRAGS shape tiles into the family bin matching each shape's NUMBER OF SIDES (Triangles / Quadrilaterals / Pentagons / Hexagons) — a classify-and-place verb distinct from the E2 tap and E14 partition. NEW core `mini tools/sort-bins-core.js` + wrapper `sort-bins-activity.js` (commits `fccf285f` engine + `54b9a4dc` MANIFEST_FILES registration fix). **0 protected-core lines** (core is a clean sibling like fractions-core; only `lcs-shell`-contract `nextTask` + `Object.assign` merge). Pointer-Events 2-D drag (touch+mouse parity, `touch-action:none`); deferred-check (fill → Check → green/coral review marks → fix → celebrate+lock); code-drawn SVG polygons (asset-free, culture-neutral — only bin LABELS localize). 8 genuinely-distinct rounds (varying bin set + shape mix) over the §A.13.60 ≥7 floor + `nextTask` per-pass order-only reshuffle. 11-locale strings by per-language native experts; German reframed to "Ecken"/"Korb", es kept formal polygon names for the 2.G.A.1 *naming* clause (CC-adjudicated over the agent's "N lados" for cross-locale consistency). `[NSR-FLAG]` sv/da/no/fi. **Build-time gate `scripts/verify-sort-bins-core.js`** (sides=vertex-count, every item→one non-empty bin, ≥7 rounds) + **`scripts/local-test-sort-bins.js`** puppeteer harness (real drag, localized labels, variety, mobile). Verified LIVE: 11/11 routes 200; real drag-play en/de/fi correct→locked→celebrate; variety gate 11/11; mobile 14/14 (280-768); pages SEO audit 342/342 (JSON-LD educationalAlignment 2.G.A.1 + 12-chain hreflang + localized framework name (de "Lehrplan", strand "Geometrie", 0 Common-Core leak)); `/en/standards/2.G.A.1` 200. `ACTIVITY_WRAPPER_VERSION` bumped 8.69→8.70; `sort-bins-core.js?v=1`/`sort-bins-activity.js?v=1`. **NEW SYNC POINT learned: a new engine MUST be added to `frontend/lib/activities.ts MANIFEST_FILES` (hardcoded list) or the route 404s for all locales** — the first deploy missed it; fixed in `54b9a4dc`. Remaining E3 leverage (~40): more sort/classify skills (e.g. 1.G.A.1 defining-vs-non-defining; sort-by-attribute) reuse this core.

**[ARC CLOSED 2026-06-04] The variety/depth arc is complete.** The Nordic school-convention arc (sv carve-out / no strict-correct / da TeX-accepted), strand-name localization (11 locales on national-curriculum domains), and the per-engine depth pass (syllable decks→28, E4 A4/A5→18, E7 CVC→20, E12 expanded-form→18) all shipped. **Residual = deferred-by-DECISION, not pending build:** fr mute-e convention review · cross-locale oral-syllable-pedagogy audit · Nordic seam root-fix / GREEN-widening · optional PDF quality pass (all per CLAUDE.md §20.9 + §A.13.57). **Cache-busters live:** `ACTIVITY_WRAPPER_VERSION='8.63'` (shared; bumped through the E14 fractions arc) · per-engine `*-activity.js?v=`: choice-board 62 · match-pairs 60 · place-value 36 · syllable-builder 14 (manifest fetch `?v=14`) · cvc-builder 1. **Variety mechanism (catalog-wide):** per-session Fisher–Yates permutation in `lcs-shell.js getTask` (fresh `Math.random` per mount) — every deck surfaces its words/rounds in a different order each visit.

## Manipulatives (no `tasks` array; pure tool mode)

| Tool | URL | CC alignment |
|---|---|---|
| **ten-frame** | `/mini-tools/ten-frame.html` | K.CC.B.4-5, K.OA.A.5 |
| **number-line** | `/mini-tools/number-line.html` | K.CC.A, K.OA.A, 1.NBT.A |
| **ruler** | `/mini-tools/ruler.html` | K.MD.A, 1.MD.A, 2.MD.A |

## E1 ten-frame-activity (5 K-Math activities)

Engine: `mini tools/ten-frame-activity-core.js` + `ten-frame-activity.js`. Templates: `make-n` (readout VISIBLE — kid drags counters to a target N shown above) / `how-many` (readout HIDDEN — kid sees N counters and types the number).

| Activity | CC code | Template |
|---|---|---|
| Show me a number (animals) | K.CC.B.4 | make-n |
| Show me a number (fruits) | K.CC.B.4 | make-n |
| How many animals are there? | K.CC.B.5 | how-many |
| Count to 20 with fruits | K.CC.A.3 | make-n |
| Teen numbers (place value) | K.NBT.A.1 | make-n |

## E2 choice-tap (highest-leverage engine — K-Math activities + the E14 fractions family)

Engine: `mini tools/choice-board-core.js` + `choice-board-activity.js`. Tile-tap with deferred Check + per-slot feedback. Supports: image tiles, text tiles, subject (image OR text OR group), Hear-it button on subject (CVC + future literacy). `task_template` branches: shape-id / count-sides / pick-bigger / which-more / even-odd / pick-smaller / how-many-group / flat-solid / **compare-length** / **sort-count** / **fraction-equal-shares** (E14 #1) / **fraction-noncongruent** (E14 #2).

### E14 fractions (recognition family, on this E2 engine) — BOTH 11/11 LIVE
**[ARC CLOSED]** Two recognition activities ride on choice-board via a fraction wrapper layer (`FRACTION_FIGURES` 29 code-drawn SVGs ~line 623 + `_fractionTiles`/`_fractionAnchor` render override ~1280-1448); **0 lines to the 5 protected cores** end-to-end. #1 **"Equal Halves and Fourths" (1.G.A.3)** + #2 **"Same Size, Different Shape" (2.G.A.3 non-congruence)** — each EN + 10-locale fan-out, all 11/11. #2 = anchor-and-match (an anchor figure renders above the board; tap the tile split into the SAME count a DIFFERENT way; foil triad = equal-count-unequal-AREA + wrong-count + not-split) with a two-sentence anchor+comparison prompt validated per-locale by native ensembles. Recognition-scope: 2.G.A.3 claimed via its recognition clause. Slugs + the per-locale grammar-trap taxonomy + the Nordic full-phrase cognate-clearance + the §20.10/content-vs-discovery split: see [[project-e14-fractions-series]] + CLAUDE.md §A.13.59. #2 closing commit `c2bb562c` (NO). Cache-busters: `choice-board-activity.js?v=62`, `ACTIVITY_WRAPPER_VERSION='8.65'`.

### E14 #3 active-partition — BUILT + LIVE 11/11 on a NEW 7th engine `fractions-core.js` (2026-06-17, `d99b2cdc`)
**`#3 "Make Equal Parts" (1.G.A.3 ACTIVE-PARTITION verb)`** is the FIRST activity on a **new sibling engine `mini tools/fractions-core.js`** (NOT choice-board; **0 lines to any of the 5 protected cores**) + `fractions-activity.js` + `fractions-activity.html` + `fractions-activities.json` (registered in `frontend/lib/activities.ts MANIFEST_FILES`). Mechanic = guided ghost cut-lines: tap faint candidate lines to partition a circle/square/rectangle into 2 (halves) or 4 (fourths) EQUAL parts; correct = committed set == correct set; snap positions closed-form exact (gate `scripts/verify-fractions-core.js` 5/5). Carries 1.G.A.3 (chip + /standards/ + JSON-LD), framework name localized §20.10. **Built-engine list is now 7: ten-frame, choice-board, match-pairs, cvc-builder, word-builder, place-value, fractions.**

**#3b "Make Thirds" (2.G.A.3 partition-into-thirds) — BUILT + LIVE 11/11 (2026-06-17, `85eb54f2`)** = a SECOND coordinate on the same `fractions-core.js` (additive N=3 extension: rect/square 2 parallel cuts at exact thirds; **circle = 3 radii 120° apart → exact equal-area wedges**, the deferred hard geometry + a `_radius()` helper; render/paint/isCorrect unchanged). Distinct from choice-board #2 (same 2.G.A.3 code; #2 recognition vs #3b partition verb). `?v=2` cores, `ACTIVITY_WRAPPER_VERSION='8.66'`. Gates: geometry 10/10 (120° wedges measured exact), interaction 11/11, #3 regression 0/3, pages 331/331, mobile 176/176. **The active-partition fractions family (1.G.A.3 halves+fourths + 2.G.A.3 thirds) is COMPLETE on fractions-core.js.** Full record + es-framework carry-forward flag: [[project-e14-fractions-series]].

**VARIETY+SHUFFLE RETROFIT (2026-06-17):** both fraction activities expanded to **8 (1.G.A.3) / 7 (2.G.A.3)** distinct rounds + a wrapper `nextTask` per-pass order-only reshuffle (`fractions-activity.js?v=3`, `ACTIVITY_WRAPPER_VERSION='8.67'`) — the first conformers to the new standing variety+shuffle Rule 5 ([[feedback-activity-variety-shuffle-rule]], CLAUDE.md §A.13.60). The other ~39 activities here are on the shell per-mount-loop (no per-pass reshuffle, several <7) = backlog, convert when next touched (gate `scripts/audit-activity-variety.js`).

**VARIETY = DIFFERENT SHAPES (2026-06-17, final):** plain rect/square/circle rejected (≈2 shapes); a themed-object dressing also rejected (over-engineered). Both fraction activities now partition genuinely DIFFERENT shapes — 1.G.A.3: circle/square/rect/triangle/ellipse/diamond/hexagon/pentagon (8); 2.G.A.3: circle/triangle/hexagon/square/rect (5, thirds-exact only; ≥7 waived). Plain translated prompt (themed code removed). Engine `_SHAPES`+`_bodySVG`+exact `_lines`; gate = point-sampling equal-area (13/13). `?v=5`, `ACTIVITY_WRAPPER_VERSION='8.69'`. Standing rule (§A.13.60): "distinct" = distinct TO THE CHILD ([[feedback-activity-variety-shuffle-rule]]).

| Activity | CC code | Locales |
|---|---|---|
| Identify the Shape | K.G.A.2 | all 11 |
| Count the Sides | K.G.B.4 | all 11 |
| Which Number Is Bigger | K.CC.C.7 | all 11 |
| Which Group Has More | K.CC.C.6 | all 11 |
| Even or Odd | 2.OA.C.3 | all 11 |
| Flat or Solid | K.G.A.3 | all 11 |
| Which Number Is Smaller | K.CC.C.7 | all 11 |
| How Many Are There | K.CC.B.5 | all 11 |
| **Comparing Length** | **K.MD.A.2** | **all 11 (LIVE 2026-06-02)** |
| **Sort and Count** | **K.MD.B.3** | **all 11 (LIVE 2026-06-03)** |

**Sort and Count (K.MD.B.3)** — 2nd Measurement & Data activity; classify-then-count. A MIXED pile (≥2 categories interleaved) is shown as the subject; prompt names ONE category ("How many animals?"); child taps the count. Distractors = correct + **total** (count-everything trap) + **other-category count** (mis-classify trap) → classification is load-bearing. Distinct from which-more (K.CC.C.6 magnitude-compare → tap a group) and how-many-group (K.CC.B.5 homogeneous count). **0 lines to the 3 cores + lcs-shell.* + Direction A CSS** held across EN base + all 10 fan-out commits (git-proven). Two activity-layer mechanisms: (1) `sort-count` branch + a **flag-guarded `render()` override** in the wrapper that swaps the homogeneous subject-group img srcs to the heterogeneous pile (the core renders only homogeneous groups — no rAF, runs right after core render); (2) per-category FULL prompt strings `promptCountAnimals/Fruits/Vehicles` (replaced the old `{category}` template + CATEGORY_LABELS) so each locale is grammatically perfect with no fragile inflection. Resolver extended: `activity-content.ts prose[id]` now accepts a full StrandTemplate object (overrides all 4 sections), needed because the MD byStrand prose is length-specific (compare-length). Folded once: `'Measurement & Data'` added to `EN_STRAND_NAMES` in `audit-activity-pages.js`. hreflang chain = 12 (pt→pt-BR). Shipped at `choice-board-activity.js?v=25`, wrapper `7.86` (E2 current now `choice-board-activity.js?v=29`, shared `ACTIVITY_WRAPPER_VERSION='8.30'`).

Executed per-locale prompts (verbatim): en How many animals/fruits/vehicles? · fi Kuinka monta eläintä/hedelmää/kulkuneuvoa? · de Wie viele Tiere/Früchte/Fahrzeuge? · fr Combien d'animaux ?/de fruits ?/de véhicules ? · es ¿Cuántos animales?/¿Cuántas frutas?/¿Cuántos vehículos? · it Quanti animali/frutti/veicoli? · pt Quantos animais?/Quantas frutas?/Quantos veículos? · nl Hoeveel dieren/vruchten/voertuigen? · sv Hur många djur/frukter/fordon? · da Hvor mange dyr/frugter/køretøjer? · no Hvor mange dyr/frukter/kjøretøy?

**Process: 3-agent native ensemble per locale (linguist + K-3 educator + B2C marketing), operator-mandated after a single-agent attempt was rejected.** The ensemble caught real issues a single agent missed: **fi case-agreement bug** — the nominative `{strand}` label "Mittaaminen ja tieto" is ungrammatical in Finnish locative/genitive slots (`osa-alueella {strand}`); fixed by writing the area inflected literally ("mittaamisen ja tiedon osa-alueella"), dropping the un-inflectable token. **Key cross-locale lesson: `{strand}` (a fixed nominative) works as an apposition after a head noun in DE/FR/ES/IT/PT/NL/SV/DA/NO ("im Bereich/dans le domaine/en el área/… {strand}") but NOT in Finnish — case-heavy languages need the area inflected literally, not the token.** Other ensemble catches: es `about` had a "comparar" register-bleed (rewritten); fr/sv/da/no `labelObjects` → "images/bilder/billeder/bilder" (native: clinical "objects" word wrong for K-3 picture-tiles; fr=images); fr/es intros were 220/206 chars → tightened to ~150 (mobile-SERP truncation). Per-locale: ensemble → synthesize → apply → commit → publish → verify (live render + 3-viewport screenshots + localized strand/framework + heterogeneous pile + cumulative hreflang set + no leak + core byte-identity sweep) → next; strict one-at-a-time, authorship-inclusive (no look-ahead). Explore-type agents sometimes refuse creative authoring ("read-only code explorer") — use **general-purpose** agents for the ensemble.

**Comparing Length (K.MD.A.2)** — platform's FIRST Measurement & Data activity; same noun at two sizes via a wrapper-injected scale transform (one image instance scaled, `cl-big`/`cl-small`), child taps taller/longer/shorter (no counting; structurally distinct from which-more cardinality). 0 lines to choice-board-core.js / lcs-shell.* / Direction A CSS — all logic in the activity layer (`compare-length` branch + wrapper `<style>`). 11-locale fan-out hardest-grammar-first (fi→de→fr→es→it→pt→nl→sv→da→no): per-locale native-expert linguist; non-EN prompts use a **gender-invariant fixed-noun anchor** (fr/es/it/pt "image", nl "het plaatje", sv "bilden", da bare "billede" [single-def], no "bildet" [double-def], de neuter "das höhere", fi partitive comparative). Strand "Measurement & Data" added to `strand-names.ts` (11 locales) — each locale's framework cited (OPS 2014 / Lehrplan / Programmes officiels / LOMLOE / Indicazioni nazionali / BNCC / SLO-kerndoelen / Lgr22 / Fælles Mål / LK20), never Common Core. hreflang chain = 12 (11 + x-default, pt→pt-BR). Shipped at `choice-board-activity.js?v=13`, wrapper `7.73` (E2 current now `?v=29` / shared `8.30`). Aria-label keeps raw-English-noun house pattern (invisible). [NSR-FLAG] sv/da/no/fi prompts+content.

**K-Math distinct-skill phase essentially done** with these 10 E2 + 5 E1 distinct K-Math skills (E2 now spans Geometry, Counting & Cardinality, Operations & Algebraic Thinking, and Measurement & Data — the latter now 2 activities: Comparing Length + Sort and Count).

## E7 CVC builder (1 EN-only activity)

Engine: `mini tools/cvc-builder-core.js` + `cvc-builder-activity.js`. Sequential multi-slot word-builder. Subject (picture + Hear-it button) → 3 slots → letter palette → Check → per-slot green/red rings.

| Activity | CC code | Locales |
|---|---|---|
| Build the CVC Word | RF.K.3 | **EN only** |

Non-EN locales return 404 by design (CVC phonics is English-specific; per the phonics safety pipeline, EN gets E7 / es/it/pt/fr/fi get E8 syllable / de/nl/sv/no/da get E9 sound-chunk).

**Depth: 20 tasks** (deepened **6 → 20** in the E7 depth pass). The 20-cap is an **image-library coverage ceiling, NOT under-building** — short-i is capped at `pig`+`fig` (no other CVC short-i nouns have images), short-o/u are thin; do not mistake the cap for an authored limit.

## E8 Syllable Builder (5 fan-outs LIVE — RF.K.2.B) ✓ COMPLETE

Engine: `mini tools/word-builder-core.js` + `syllable-builder-activity.js`. Sibling to E7's cvc-builder-core (NOT a refactor); supports multi-character syllable tiles; per-tile TTS + blended-word TTS on correct Check; Direction A card; tap-to-place mechanic. **All decks deepened to 28 words** in the depth pass.

| Activity | Locale | URL slug | Words |
|---|---|---|---|
| Forma las sílabas | ES | forma-las-silabas | **28** |
| Forma la parola con le sillabe | IT | forma-la-parola-con-le-sillabe | **28** |
| Forme as sílabas | PT | forme-as-silabas | **28** |
| Forme le mot en syllabes | FR | forme-le-mot-en-syllabes | **28** |
| Muodosta sana tavuista | FI | muodosta-sana-tavuista | **28** |

(IT + FR shipped — were "pending/flagged"; the original ES/FI/PT 8/7/7-word counts are superseded by 28.)

All word splits sourced verbatim from `approved-words-<locale>.json`. No word bypasses the gate. Gate-fix log (post gate v1.1): río (`b84113f7`) + iã+o (`ad4924da`) **+ pt.js ss/rr split `fed6838b` + pt-hiatus tonic-i/u guard + fi compound-seam + 8 settled count corrections `7393a989` + policial.pt 3→4** (sv-carve-out commit). The río/iã+o count drift is now **CLOSED** (re-enabled by the count corrections). **FR depth used the written-syllable (syllabes écrites) convention** — the oral-clapping mute-e question is filed for a separate fr-convention review (CLAUDE.md §20.9). The **OPEN** syllable backlog is the ~14 Nordic compound-seam errors (CLAUDE.md §A.13.57).

**Nordic school-convention determinations (CLAUDE.md §A.13.57 + [[project-phonics-safety-pipeline]]):** **sv** = surgical SCHOOL_DIVERGENCE carve-out within the strict gate (NOT GREEN; pool 964→984 via correction; klock-a/se-bra/hand-ske); **no** = empty divergence set (already school-correct — Norwegian splits geminates klok-ke, ng en-gel, vr ha-vre; OPPOSITE of sv); **da** = no `da.js` rule (deliberate; TeX-aligned splits accepted as-is). **Rule-file git-tracking flag:** `sv.js`/`no.js` now git-tracked (this arc); `de.js`/`nl.js` UNTRACKED; `da.js` ABSENT — any commit touching a rule-syllabifier MUST `git add` it.

## E9 Sound-Chunk Builder (5 Nordic-cluster Group-C fan-outs LIVE — RF.K.2.B) ✓ COMPLETE

Engine: `mini tools/word-builder-core.js` + `syllable-builder-activity.js` (SHARED with E8 — same engine, different word-source pools). Same mechanic: scrambled syllable tiles → child orders → tap-to-place + per-tile TTS + blended-word TTS on correct Check. **All decks deepened to 28 words** in the depth pass (DE 30).

| Activity | Locale | URL slug | Manifest id suffix | Commit |
|---|---|---|---|---|
| Bau das Wort aus Silben | DE | bau-das-wort-aus-silben | (none — pre-collision) | shipped pre-session |
| Bouw het woord met klankgroepen | NL | bouw-het-woord-met-klankgroepen | (none — pre-collision) | shipped pre-session |
| Bygg ordet av stavelser | SV | bygg-ordet-av-stavelser | (none — pre-collision) | `c2bc4249` |
| Bygg ordet av stavelser | NO | bygg-ordet-av-stavelser | `.no` | `f834efd1` |
| Byg ordet af stavelser | DA | byg-ordet-af-stavelser | `.da` | `4b014cbc` |

**Manifest-id disambiguation convention (started NO, continued DA):** every new locale's syllable-builder manifest id gets a `.<locale>` suffix to prevent `_loadActivity` find() collisions when slug-shapes overlap. Defensive even when slug is currently unique.

**ng-rule (locked across NO + DA per Part A reconciliation):** default 2-chunk walker → boundary BETWEEN n and g (engel → en-gel; finger → fin-ger). DIFFERENT from Swedish ng-as-coda special case (ängel → äng-el). For future `da.js` reference if/when built.

**DA K-1-safe pool selection:** DA STEP 1 surfaced 794/1263 approved with 392 carrying `policy_managed:true` flags (per §20.7 phonemic-divergence policy). Operator decided ship from 402 non-policy-managed K-1-safe pool. All 10 DA activity words verified `policy_managed:false`. See `[[project-phonics-safety-pipeline]]`.

**Cache-buster state (current, post depth pass):** `ACTIVITY_WRAPPER_VERSION = '8.30'` (shared); `syllable-builder-activity.js` script `?v=14` + manifest fetch `?v=14` (sv/fr/fi depth commits bumped 10→14).

## E4 Match-Pairs (11 LIVE — K.OA.A.3 "Make the Number" ROLLOUT COMPLETE) ✓ CLOSED 11/11

Engine: `mini tools/match-pairs-core.js` + `match-pairs-activity.js`. Tap-to-pair number-bond decomposition. Single-row manifest at `match-pairs-activities.json` resolves all 11 per-locale slugs from one row id (`match-pairs.make-the-number.k-oa-a-3`). 6 tasks per locale (targets 3, 5, 6, 7, 9, 10); Fisher-Yates shuffle per setupTask invocation; per-tap TTS via `LCSAudio.speak({type:'number'})`; spoken decomposition summary on Check-correct via `speakAllPairs()`.

| Locale | URL slug | Cardinals + register notes | Commit |
|---|---|---|---|
| EN | make-the-number | `'zero'..'ten'` | shipped earlier in session |
| DE | bilde-die-zahl | `'null'..'zehn'` | shipped earlier in session |
| ES | forma-el-numero | `'cero'..'diez'` | shipped earlier in session |
| IT | forma-il-numero | `'zero'..'dieci'` | shipped earlier in session |
| FR | compose-le-nombre | `'zéro'..'dix'`; `Toutes les paires font {n} !` with regular-space before `!` | shipped earlier in session |
| PT | forme-o-numero | `'zero'..'dez'` (Brazilian canonical) | shipped earlier in session |
| NL | maak-het-getal | `[1]='één'` (acute, NOT bare `een`) | shipped earlier in session |
| SV | gor-talet | `[1]='ett'` (neuter); `Alla par blir {n}!` | `3a24e3a8` |
| DA | lav-tallet | `[1]='en'` (bare; PVC-locked K-1 default per `place-value-core.js:885-886`); `Alle par bliver {n}!` (copula deviation from DA E12 `er` per operator authorization) | `61944985` |
| NO | lag-tallet | `[1]='én'` (acute Bokmål); `[7]='sju'` (modern post-1951, NOT `syv`); `Alle par blir {n}!` | `a61b9977` |
| FI | tee-luku | `[10]='kymmenen'` (cardinal-10 form, distinct from E12 place-unit `kymmen/kymmentä`); `Jokainen pari on {n}!` (singular distributive; copula `on` matches FI E12) | `4bf56943` |

**Cognate-drop calibration applied per verify** (see [[feedback-cognate-aware-verify-discipline]] for full doctrine):
- DA verify: drops `Mål`, `Kort ` from FORBIDDEN_SV (Nordic-trio cognates)
- NO verify: drops `Mål`, `Kort ` from FORBIDDEN_{SV,DA}; drops `makker`, `Tryk`, `Prøv` from FORBIDDEN_DA (substring traps `Tryk` ⊂ `Trykk`)
- FI verify: ZERO drops — Uralic distinctness; substring-trap safe (`Kort ` 5-char ⊄ `Kortti ` 7-char)

**Engine integrity throughout rollout:**
- `match-pairs-core.js` mechanics + Direction A CSS + `lcs-shell.js` + `ActivityIframe.tsx` + `[slug]/page.tsx` + `frontend/lib/activities.ts` — UNTOUCHED across all 10 sequential commissions
- PlaceValueCore NOT imported (each locale ships inline `cardSpoken.<locale>` 11-entry table value-verified against `_NUMBER_WORD_HELPERS.<locale>(n, 'cardinal')`)
- Cache buster bumped per ship: `?v=1` (EN baseline) → `?v=12` (FI close)

**Per-locale teacher chip** (kindergarten label from `messages/<locale>.json: seo.educational_level.kindergarten`): NL `Kleuterschool`, SV `Förskoleklass`, DA `Børnehaveklasse`, NO `Førskole`, FI `Esikoulu`.

## E4 Match-Pairs SECOND activity — "Addition & Subtraction Pairs" (1.OA.D.7) — 11/11 LIVE ✓ COMPLETE

Same engine (`match-pairs-core.js` + `match-pairs-activity.js`), `task_template: equal-value`. Single-row id `match-pairs.addition-and-subtraction-pairs.1-oa-d-7`. Design (operator-locked "v4", 2026-05-31): cards show a bare expression with the **result HIDDEN** (`2 + 3`, `6 − 1`); kid matches each addition to the subtraction with the **same hidden answer**; **two different answers per task**; validation = `a.value === b.value`; **no banner**; number-free celebration `speakMatchedAll` ("You matched every pair!"). Standard `1.OA.D.7` (meaning of the equal sign — NOT 1.OA.B.4/unknown-addend; opens Grade 1 OA). Cards language-neutral (digits + `+ − =`), engine + Direction-A CSS untouched.

| Locale | URL slug | Status |
|---|---|---|
| EN | addition-and-subtraction-pairs | LIVE `a94e7657`-era EN build |
| DE | plus-und-minus-paare | LIVE `a94e7657` |
| FR | paires-additions-soustractions | LIVE `1379a063` |
| DA | plus-og-minus-par | LIVE `98970e93` `[NSR]` |
| NO | pluss-og-minus-par | LIVE `6ee6e751` `[NSR]` |
| SV | plus-och-minus-par | LIVE `4c441a36` `[NSR]` |
| FI | plus-ja-miinusparit | LIVE `d5bce4cf` `[NSR]` |
| NL | optel-en-aftrekparen | LIVE (pre national-framework switch) |
| ES | parejas-de-sumas-y-restas | LIVE `765e6902` (Currículo LOMLOE; verb `Relaciona`; celebration `¡Has relacionado todas las parejas!`) |
| IT | coppie-addizioni-sottrazioni | LIVE `779f188f` (Indicazioni nazionali; verb `Collega`=live visual verb; celebration `Hai collegato tutte le coppie!`; chip strand `Operazioni e pensiero algebrico`) |
| PT | pares-de-adicoes-e-subtracoes | LIVE `c72aaf53` (BNCC; verb `Combine`=live "cartas que combinam"; celebration `Você combinou todos os pares!`; chip strand `Operações e pensamento algébrico`). Collision round verified clean: `o mesmo resultado`≠ES`el mismo resultado`, `Calcule cada carta`≠IT`Calcola ogni carta` — no drops. |

**FAN-OUT COMPLETE — 11/11 LIVE.** Romance connector verbs deconflicted: ES `Relaciona` · IT `Collega` · PT `Combine` (each grounded in its live K.OA.A.3 register, no cross-locale full-phrase collision).

Inner cache-buster `match-pairs-activity.html` at `?v=22`; `ACTIVITY_WRAPPER_VERSION` NOT bumped (the `activity=` query param is a distinct cache key, so K.OA.A.3's SSR page stays byte-identical). Fan-out cadence: one locale per explicit operator commission; cognate-aware verify per [[feedback-cognate-aware-verify-discipline]]. **es/it/pt + all future locales ship on national frameworks** per [[project-activity-national-framework-localization]] (Romance cognate map pre-flagged: ES↔PT `resultado` word-identical → bilateral drop only on byte-identical full phrases; `mismo`/`mesmo` prefix keeps them distinct; verbs `Relaciona`(ES)/`Collega`(IT)/`Ligue`(PT); tap `Toca`/`Tocca`/`Toque`).

## E4 Match-Pairs THIRD activity — "Three Ways to Show a Number" (2.NBT.A.3) — EN LIVE ✓

Opens **Grade 2 NBT** on the match-pairs engine. New `task_template: three-form`. Single-row id `match-pairs.three-ways-to-show-a-number.2-nbt-a-3`; slug.en `three-ways-to-show-a-number` (EN-only; non-EN 404 by design until fan-out). Commit `5f484d17` (2026-05-31).

**Design (operator-locked PAIRWISE, NOT three-card):** the live engine is strictly two-card; three-card grouping would rewrite `onCardTap` + `_drawLines` (locked, 22 instances) — rejected per operator criterion. Instead reuses the `equal-value` value-equality check: 6 cards = 3 pairs; each number's value on exactly 2 cards bridging two of its three representations (numeral / expanded "200 + 40 + 7" / number-word "two hundred forty-seven"). **All 3 pairing types rotated across tasks** (A numeral↔expanded, B numeral↔word, C expanded↔word; 5A·5B·5C). 5 tasks; values 100-999 incl. zero-place edges (305/420/208/940/190/560/409) + round-hundred (700, type-B only — its expanded "700" would collide with the numeral). No banner (object cards); celebration speaks the 3 matched number-words ascending then "You matched every number!".

**Engine touches — ADDITIVE ONLY (K.OA.A.3 + 1.OA.D.7 byte-identical, verified live):**
- `match-pairs-core.js`: (1) render loop adds `.mp-word` (vs `.mp-expr`) for `kind:'word'` cards; (2) `injectCSS` adds `.mp-word` wrapping face (aspect-ratio:auto, smaller wrapping font); (3) `_speakCard` gated object-branch — speaks number-word when `_speakWordTable` set (three-form only), silent for 1.OA.D.7 add/sub.
- `match-pairs-activity.js`: new `makeThreeFormTasks` + `taskThreeForm`/`hintThreeForm`/`hintTryThreeForm`/`speakThreeFormDone` (EN-only) + `THREE_FORM_WORDS_EN` flat 15-entry table value-verified vs `_NUMBER_WORD_HELPERS.en` (**NO PVC import**) + `three-form` case in `_buildTasksFromRow`.
- Cache-busters: `match-pairs-core.js?v=24`, `match-pairs-activity.js?v=27`, `ACTIVITY_WRAPPER_VERSION='7.21'`.

**Post-ship fixes (commit `352a6b75`, two operator-reported visual bugs; both three-form-only + additive, live activities byte-identical):**
- **Overflow:** 3-term expanded cards ("200 + 40 + 7") overflowed at the 3-col breakpoint under nowrap `.mp-expr`. New **`.mp-expanded`** class (wrap + relaxed aspect-ratio); render loop now routes `kind:'expanded'`→`.mp-expanded`, `kind:'word'`→`.mp-word`, else `.mp-expr` (numeral / 1.OA.D.7 add·sub).
- **Predictable placement:** plain shuffle frequently column-stacked each number's two cards ("every column is a pair"). New **`scatterThreeForm()`** in the activity module (after `setupTask`, before `render`) minimizes a visual-adjacency penalty summed over BOTH 2-col + 3-col geometries with a random tie-break. 20k-trial validation: penalty-1 (min) 98.4%, **zero clean transpositions** at either breakpoint, 48 distinct arrangements. 6 cards can't be fully non-adjacent at both breakpoints (min adjacency = 1) → at most ONE guessable pair, never the full stack. K.OA.A.3/1.OA.D.7 never call it.
- Verify harness `.verify-three-ways.js` extended with per-card overflow + rendered-grid placement assertions; ALL PASS live at 375/768/1920.

**2.NBT.A.3 vs E12:** E12 ships 2.NBT.A.1 (place-value decomposition on columns) — no 2.NBT.A.3 row. The match-pairs instantiation is a *recognize/match equivalent representations* task; the eventual E12 expanded-form would be a *construct on columns* task. Distinct; no overlap.

**10-locale fan-out (order de,fr,da,no,sv,fi,nl,es,it,pt; single-row-multi-locale; ship+verify each before next):**
- **DE LIVE** (commit `d9e5603f`): slug `drei-wege-eine-zahl-zu-zeigen`, title "Drei Wege, eine Zahl zu zeigen". 15/15 DE number-words value-verified vs `_NUMBER_WORD_HELPERS.de` (units-first one-word `zweihundertsiebenundvierzig`; `einhundert`; ß; sechzig/siebzig). Register matched to live DE E4 (du, em-dash, "versuch's nochmal"); intro cites **Lehrplan** (§20.10), code only in chip+JSON-LD; chip `2. Klasse · Zahlen und Rechnen im Zehnersystem · 2.NBT.A.3`. Verified live 375/768/1920 + EN-leakage greps clean.
- **Per-locale rails now in place (reused by locales 3-10):** `THREE_FORM_WORDS_EN` refactored → `THREE_FORM_WORDS = {en, de, …}`; `makeThreeFormTasks` sources word-card DISPLAY from `THREE_FORM_WORDS[tool.language]` at render (numeral/expanded stay language-neutral; EN byte-identical); per-locale `taskThreeForm/hintThreeForm/hintTryThreeForm/speakThreeFormDone` in `ACTIVITY_STRINGS_MP`. **Engine `.mp-word` now has `overflow-wrap:break-word;word-break:break-word;hyphens:auto` + `lang` on the word card** so spaceless long compounds wrap (DE done; FI `kaksisataa…` will rely on it). To add a locale: value-verify its 15 words vs `_NUMBER_WORD_HELPERS.<loc>`, add the `THREE_FORM_WORDS.<loc>` block + 4 strings + slug/title/intro on the row; NO new row; NO PVC import.
- Cache-busters CURRENT (post-NL): core `?v=25`, activity `?v=34`, `ACTIVITY_WRAPPER_VERSION='7.28'`. Per-locale verifier `mini tools/.verify-three-ways-<loc>.js` exists for de/fr/da/no/sv/fi/nl (out-of-tree; clone the latest for the next locale).
- **FR LIVE** (commit `15f4442f`): slug `trois-facons-de-montrer-un-nombre`, title "Trois façons de montrer un nombre". 15/15 FR words value-verified vs `_NUMBER_WORD_HELPERS.fr` — **classic convention** (space at hundred boundary + hyphen in sub-99 tail: `deux cent quarante-sept`; NOT the 1990 all-hyphen reform); `sept cents` (cent→cents only on round ×100); vigesimal `soixante-quatorze`/`quatre-vingt-dix`; bare `cent` for h=1. Register matched to live FR E4 (tu, space-before-`!`, em-dash); intro cites **"programmes officiels"** (NOT Common Core — corrected the commission premise; matches live FR D.7 + route map); chip `CE1 · Nombres et opérations en base dix · 2.NBT.A.3`. "le même nombre" (not "résultat") → Romance résultat-trap doesn't bind. NO engine change (FR words have spaces; DE rails held). Verified live 375/768/1920 + EN/DE-leakage greps clean. activity `?v=29`, wrapper `7.23`.
- **DA LIVE** (commit `26ee2878`, `[NSR-FLAG][DA]`): slug `tre-mader-at-vise-et-tal`, title "Tre måder at vise et tal på". 15/15 DA words value-verified vs `_NUMBER_WORD_HELPERS.da` — **vigesimal** (halvtreds/tres/halvfjerds/firs/halvfems = 50/60/70/80/90), units-first tens (`syvogfyrre`=7+og+40), `fyrre`=40 (not vigesimal), bare `hundrede` for h=1, **SPACED " og " joiner** (`tohundrede og syvogfyrre` — the live E12 hundreds convention, NOT a fully-compound form as the commission described; matched the helper). Register matched to live DA E4 (tryk på, em-dash, prøv igen, no space before `!`); intro cites **"Fælles Mål"** (NOT Common Core; matches live DA D.7); chip `2. klasse · Tal og regning i titalssystemet · 2.NBT.A.3`. `det samme tal` (not resultat). NO engine change (DA words have spaces). Verified live 375/768/1920 + EN/DE/FR-leakage greps clean. activity `?v=30`, wrapper `7.24`. DA strings NSR-pending.
- **NO LIVE** (commit `cf52336e`, `[NSR-FLAG][NO]`): slug `tre-mater-a-vise-et-tall`, title "Tre måter å vise et tall på". 15/15 NO words value-verified vs `_NUMBER_WORD_HELPERS.no` — modern Bokmål **FULLY-COMPOUND spaceless** (`tohundreogførtisju`), decimal tens-first (`førtisju`/`sekstito`/`syttifire`; sytti/seksti/åtti/nitti), `hundre` (not hundrede), bare `hundre` for h=1, `sju`, `én` (acute) in tails. **NO is the DE-class spaceless compound → relies on the `.mp-word` break rail (no engine change); 375px overflow check PASSED.** Register matched to live NO E4 (trykk på, em-dash, prøv igjen, Koble); intro cites **LK20**; chip `2. trinn · Tall og regning i titallssystemet · 2.NBT.A.3`. DA↔NO trap handled via full-phrase forbidden tokens. Verified live 375/768/1920 + leakage greps clean. activity `?v=31`, wrapper `7.25`.
- **SV LIVE** (commit `aee3bd1b`, `[NSR-FLAG][SV]`): slug `tre-satt-att-visa-ett-tal`, title "Tre sätt att visa ett tal". 15/15 SV words value-verified vs `_NUMBER_WORD_HELPERS.sv` — **compound with NO joiner word** (`tvåhundrafyrtiosju` — the THIRD Nordic pattern: DA spaced ` og ` / NO compound-with-`og` / SV compound-no-joiner), decimal -o tens (`fyrtio`/`sextio`/`sjuttio`/`åttio`/`nittio`), `hundra`, bare `hundra` for h=1, `ett` (neuter) in tails, `sju`/`nio`. Spaceless → DE-class break rail (no engine change; 375px overflow PASSED). Register matched to live SV E4 (tryck på, Koppla ihop, försök igen); intro cites **Lgr22**; chip `Årskurs 2 · Tal och räkning i basen tio · 2.NBT.A.3`. SV↔DA `tal` word-level identity + `Tryck`⊃`Tryk` handled via full-phrase tokens. Verified live 375/768/1920 + leakage greps clean. activity `?v=32`, wrapper `7.26`.
- **FI LIVE** (commit `8285617f`, `[NSR-FLAG][FI]`): slug `kolme-tapaa-esittaa-luku`, title "Kolme tapaa esittää luku". 15/15 FI words value-verified vs `_NUMBER_WORD_HELPERS.fi` — pure agglutination, single word, no joiner (`kaksisataaneljäkymmentäseitsemän`); tens X+kymmentä; hundreds X+sataa; bare `sata` for 100; `yksi` for 1 in tails. **★ Wrap-rail hardest test PASSED:** 32-char forms (247/836/274) are type-A → numeral/expanded only (language-neutral), never word cards; longest WORD card = 481 `neljäsataakahdeksankymmentäyksi` (31 chars, task 1) — verified live at 375px wraps fully inside card, **no overflow** (advanced the verify to task 1 to prove it). `.mp-word` break rail held; no engine change. Register matched to live FI E4 (paina, Yhdistä, full case agreement); intro cites **OPS 2014**; chip `2. luokka · Luvut ja kymmenjärjestelmä · 2.NBT.A.3`. FI Uralic → zero leakage overlap. Expanded-form term `hajautettu muoto` flagged NSR-priority (least-certain term). activity `?v=33`, wrapper `7.27`.
- **NL LIVE** (commit `0fc07205`, NOT NSR — Germanic): slug `drie-manieren-om-een-getal-te-tonen`, title "Drie manieren om een getal te tonen". 15/15 NL words value-verified vs `_NUMBER_WORD_HELPERS.nl` — units-first one-word compound (`tweehonderdzevenenveertig`); **diaeresis ë** when stem ends in e (562 `vijfhonderdtweeënzestig`, 353 `driehonderddrieënvijftig` — verified UTF-8-intact in served HTML + live render, no mojibake); **bare `een` (NO acute) for 1 in tails** (481 `vierhonderdeenentachtig` — helper convention, distinct from K.OA.A.3 cardSpoken `één`); `honderd`. Register matched to live NL E4 (tik op, Verbind, full de/het gender); intro cites **de kerndoelen** (JSON-LD `SLO-kerndoelen`); chip `Groep 4 · Getallen en bewerkingen in het tientallig stelsel · 2.NBT.A.3`. **NL↔DE tightest-Germanic call PASSED:** all 15 number-words distinct (honderd/hundert boundary) + all page strings full-phrase distinct → no bilateral drop. Spaceless (DE-class), no engine change. `uitgebreide vorm` expanded-form term flagged for operator confirm (NL has no NSR net). activity `?v=34`, wrapper `7.28`.
- **IT LIVE** (commit `cbb3cc7e`): slug `tre-modi-per-mostrare-un-numero`, title "Tre modi per mostrare un numero". 15/15 IT words value-verified vs `_NUMBER_WORD_HELPERS.it` — one-word compound, no joiner, no irregular hundreds (duecento..novecento); **vowel-elision at cento+vowel boundary** (208 `duecentotto`, 481 `quattrocentottantuno`); bare `cento` h=1 (190 `centonovanta`); **accent -tré tails** (353 `trecentocinquantatré`, served UTF-8 intact). Register matched to live IT E4 (connector `Collega`; `lo stesso numero`; `Tocca`; em-dash; **no inverted punctuation**); intro "curricolo ufficiale" generic (JSON-LD auto-emits `Indicazioni nazionali`); chip `Seconda elementare · Numeri e operazioni in base dieci · 2.NBT.A.3`; celebration `Hai collegato tutti i numeri!`; `forma scomposta` expanded-form term. One-word class → DE-class break rail (no engine change; 375px clean). `.gate-e4-threeform.js` PASS (5 tasks, no-false-correct, pairing A5·B5·C5). IT↔PT number-words **0/15 collisions**. Cold-cache verify 375/768/1920 clean; iframe leaks NONE (sibling titles `Forma il numero`/`Coppie…` appear only in the related-activities `<a href>`/JSON link mesh — correct internal linking, NOT a wrong render; h1 = `Tre modi per mostrare un numero`). **Wrapper bump done IN-commit** (ES lesson): activity `?v=36`, wrapper `7.30`.
- **PT LIVE** (commit `b4ab207a`) — **FINAL, ROLLOUT COMPLETE 11/11**: slug `tres-maneiras-de-mostrar-um-numero`, title "Três maneiras de mostrar um número" (Brazilian canonical). 15/15 PT words value-verified vs `_NUMBER_WORD_HELPERS.pt` — multi-word, **" e " joiner at EVERY level** (`duzentos e quarenta e sete` — distinct from ES which joins only within sub-99 tail); irregular hundred `quinhentos` (500 — `quinhentos e sessenta e dois`); bare `cem` h=1, `cento` in compounds (`cento e noventa`); `um` masc tail (`quatrocentos e oitenta e um`); BR teens `dezesseis`/`dezessete`/`dezenove` (helper-confirmed BR, not EU); accents (`trezentos e cinquenta e três`, served UTF-8 intact). Register matched to live PT E4 (connector `Combine`; `o mesmo número`; `Toque em`; em-dash; no inverted punctuation); intro **names BNCC directly** "Alinhado à BNCC" (PT prose names its framework — distinct from ES/IT generic "currículo oficial"/"curricolo ufficiale"; JSON-LD also emits BNCC); chip `2º ano · Números e operações na base dez · 2.NBT.A.3`; celebration `Você combinou todos os números!`; BR terms `em algarismos`/`por extenso`/`forma decomposta`. PT number-words **0/15 collisions vs BOTH ES and IT**. Multi-word class → wrap rail trivial (375px clean). `.gate-e4-threeform.js` ALL PASSED. Verify 375/768/1920 clean; iframe leaks NONE; sibling titles `Forme o número`/`Pares de adições e subtrações` appear only in the related-activities JSON link mesh (h1 = `Três maneiras de mostrar um número`). Wrapper bump in-commit: activity `?v=37`, wrapper `7.31`.
- **11/11 live — FAN-OUT COMPLETE** (EN + de, fr, da, no, sv, fi, nl, es, it, pt). Joiner orthographies: DE units-first compound / DA spaced og / NO compound og / SV compound no-joiner / FI agglutination / NL units-first compound+diaeresis / ES multi-word spaced + `y` (sub-99 tail only) / IT one-word compound + vowel-elision / PT multi-word + `e` joiner (every level).
- **ES LIVE** (commit `13b6e556` + wrapper-bump fix `fbc5cb6d`): slug `tres-maneras-de-mostrar-un-numero`, title "Tres maneras de mostrar un número". 15/15 ES words value-verified vs `_NUMBER_WORD_HELPERS.es` — multi-word SPACED; `y` joiner only in the sub-99 tens-units tail (`doscientos cuarenta y siete`), never between hundreds and tens (`trescientos cinco`); irregular hundreds quinientos/setecientos/novecientos (500/700/900); `ciento` in compounds (`ciento noventa`); `uno` (not un) in card tails (`cuatrocientos ochenta y uno`). Register matched to live ES E4 (connector `Relaciona`, `¡…!` celebration, em-dash); intro cites **"currículo oficial"** (matches live ES K.OA.A.3 + D.7 prose; JSON-LD auto-emits `Currículo LOMLOE`); chip `2.º de primaria · Números y operaciones en base diez · 2.NBT.A.3`; equality idiom `el mismo número`; celebration string `¡Has relacionado todos los números!`. ES words SPACED → no wrap-rail stress (DE-class break rail irrelevant). Cold-cache verify 375/768/1920 clean, leaks NONE. **PROCESS NOTE: first ship `13b6e556` MISSED the paired `ACTIVITY_WRAPPER_VERSION` bump (recipe step 4 / §A.13.42) — root cause was not reading this memory at session start; fixed in `fbc5cb6d` (7.28→7.29).** activity `?v=35`, wrapper `7.29`.
- **ALL 11 LIVE — three-form (2.NBT.A.3) fan-out COMPLETE** (closed at PT `b4ab207a`). No pending locales for this activity. Romance pre-flag: Activity 3 uses **"el mismo número"** (ES) / "lo stesso numero" (IT) / "o mesmo número" (PT) — NOT the `resultado`/`risultato`-class equality-tail (that was D.7's), so the ES↔PT `resultado` trap does NOT bind here. Watch `pareja`(ES)/`coppia`(IT)/`par`(PT) + `número`/`numero` word-level identity across ES↔PT (full phrases still differ via `el mismo`/`o mesmo`). Romance frameworks: ES `Currículo LOMLOE`, IT `Indicazioni nazionali`, PT `BNCC` (per live D.7 + route map).

## E4 Match-Pairs FOURTH activity — "Find the Missing Number" (1.OA.D.8) — 11/11 LIVE ✓ COMPLETE

Same engine (`match-pairs-core.js` + `match-pairs-activity.js`). Single-row id `match-pairs.find-the-missing-number.1-oa-d-8`; slug.en `find-the-missing-number`; 11 locale slugs. Opens **1.OA.D.8** (determine the unknown whole number in an addition/subtraction equation). 11/11 fan-out COMPLETE (IT `2173fc93` → FI close `48290844`); **deepened 5 → 18 tasks** in the depth pass (`10b05326`). National frameworks per §20.10; cognate-aware verify per [[feedback-cognate-aware-verify-discipline]].

## E4 Match-Pairs FIFTH activity — "Compare Three-Digit Numbers" (2.NBT.A.4) — 11/11 LIVE ✓ COMPLETE

Same engine. Single-row id `match-pairs.compare-three-digit-numbers.2-nbt-a-4`; slug.en `compare-three-digit-numbers`; 11 locale slugs. Opens **2.NBT.A.4** (compare two three-digit numbers using >, =, <). EN build `d9ea2488`; **deepened 5 → 18** (`efbd09d1`). National frameworks per §20.10.

### E4 fan-out recipe (reusable for future E4 activities)
**State: E4 = FIVE activities, all 11/11 LIVE + deepened** (A1 K.OA.A.3 · A2 1.OA.D.7 · A3 2.NBT.A.3 · A4 1.OA.D.8 · A5 2.NBT.A.4; A4 + A5 deepened 5→18). Current versions: `match-pairs-activity.js?v=60`, `ACTIVITY_WRAPPER_VERSION='8.30'` (shared). The variety/depth arc is CLOSED — no pending E4 build; further E4 activities are operator-prompted future-backlog. REMINDER: read this file FIRST next session. Recipe lessons banked: (1) canonical source is git-tracked `mini tools/` (NOT the gitignored `frontend/public/mini-tools/` mirror); (2) bump BOTH activity `?v=` AND `ACTIVITY_WRAPPER_VERSION` in the SAME commit (ES `13b6e556` missed it → follow-up `fbc5cb6d`; IT `cbb3cc7e` + PT `b4ab207a` did it inline); (3) `.gate-e4-threeform.js` is the pre-deploy gate (run it); (4) curl sibling-title "leaks" are the related-activities JSON link mesh, not body — iframe Puppeteer leak check is authoritative. Cadence LOCKED: STEP-0 report → operator green-light → build → live-verify → HOLD. Do NOT chain; do NOT act on a bare "continue." One commission per locale. Nordic/Finnic commits carry `[NSR-FLAG][<loc>]`; Germanic/Romance do not.

**Mechanical recipe per locale** (rails in place; reused unchanged DE→NL — NO engine/core.js change needed):
1. Run live `_NUMBER_WORD_HELPERS.<loc>(n,'cardinal')` (via `vm.createContext` loading `place-value-core.js`) for the SAME 15 values `247,562,305,836,481,420,615,700,208,353,940,190,560,274,409`; build flat table; verify 15/15. **NO PlaceValueCore import in the activity module** — inline flat table only.
2. `mini tools/match-pairs-activity.js`: add `THREE_FORM_WORDS.<loc>` block + `<loc>` entries on the 4 strings `taskThreeForm/hintThreeForm/hintTryThreeForm/speakThreeFormDone`. (Numeral + expanded cards stay language-neutral; only number-word card text differs.)
3. `mini tools/match-pairs-activities.json` (+ cp to gitignored mirror `frontend/public/mini-tools/`): add `slug/page_title/page_intro.<loc>` on existing row `match-pairs.three-ways-to-show-a-number.2-nbt-a-3`. **NO new row** (single-row-multi-locale → hreflang auto-emits).
4. Cache-busters per §A.13.42: bump activity `?v=N+1` in `mini tools/match-pairs-activity.html` + `ACTIVITY_WRAPPER_VERSION` in `frontend/app/[locale]/activities/[slug]/page.tsx`.
5. Update `.gate-e4-threeform.js` slug assertion (+<loc>); re-verify inlined table vs helper.
6. Commit ONLY intended files (check `git status --porcelain`; `.verify-*`/`.gate-*` dot-files + `frontend/public/mini-tools/` mirror are out-of-tree, do NOT commit). Push → plink Hetzner (git pull → cp mini-tools to `/var/www/lcs-media/mini-tools/` → chown lcs-media:lcs-media → `bash deploy.sh`) → live verify.
7. Clone `.verify-three-ways-nl.js` → `.verify-three-ways-<loc>.js`; update URL/strings/FORBIDDEN with cognate-aware full-phrase tokens (§A.13.53 — full phrases, never bare roots; drop genuine cross-locale cognates). Run 375/768/1920 + eyeball screenshots. Regression bar (5): EN A3 + all prior-locale A3 byte-identical; <loc> K.OA.A.3 + D.7 200/distinct; E12 + core.js absent from the commit `--stat`.

**[DONE — PT shipped `b4ab207a`; this forward pre-flag is historical.]** PT used framework `BNCC`, strand "Números e operações na base dez", connector `Combine`, idiom `o mesmo número`, multi-word + `e` joiner; IT↔PT + ES↔PT collisions 0/15. See the **PT LIVE** bullet above for the as-shipped record.

**Refs:** plan file `C:\Users\rkgen\.claude\plans\commission-e4-match-pairs-smooth-spark.md`; [[feedback-cognate-aware-verify-discipline]]; CLAUDE.md §A.13.53 + §20.10 + §A.13.42.

## E12 Place-Value (3 activities × 11 = 33 LIVE) ✓ — A3 expanded-form CLOSED 11/11

Engine: `mini tools/place-value-core.js` (**UNTOUCHED — 0 lines across every fan-out cycle**; `git --no-pager diff --quiet` verified each commit) + `mini tools/place-value-activity.js` (additive content layer only). **NO match-pairs import.** Config-driven build-on-columns (2-place/3-place via `places` descriptor); manipulative blocks → readout (digit) → digit-only caption (`200 + 40 + 7 = 247`) + spoken decomposition. Native-language hand-written slugs; CCSS code English-authoritative, NEVER in URL; non-EN cites national framework name per §20.10 + §A.13.49 (zero "Common Core"); grade chip inherited from `seo.educational_level.grade_2`.

| Activity | CC code | template / row id | locales |
|---|---|---|---|
| A1 Tens and Ones | 1.NBT.B.2 | `build-tens-and-ones` · `place-value.tens-and-ones.1-nbt-b-2` | 11/11 |
| A2 Hundreds, Tens, and Ones | 2.NBT.A.1 | `build-hundreds-tens-and-ones` · `place-value.hundreds-tens-and-ones.2-nbt-a-1` | 11/11 |
| A3 **Expanded Form** | 2.NBT.A.3 | `build-expanded-form` · `place-value.expanded-form.2-nbt-a-3` | **11/11 — CLOSED `97ddaad1`** |

**Pool depth:** the expanded-form value set was deepened **6 → 18** (commit `e79e7826`, the E12 depth pass). `speakExpandedForm` speaks **any** 3-digit number per locale from the per-place cardinals, so the value set was always an **authored targets pool, NEVER a verifiability hard-cap on 6** — do not treat expanded-form as capped at 6.

**A3 mechanism (the reusable pattern):** locale-generic `speakExpandedForm` (`place-value-activity.js:232`) speaks each place-VALUE summand as a bare `type:number` unit (`_numberWord(placeValue,lang)` per place — joiner/elision/trema/agglutination NEVER leak into a summand), names any zero place via `l10n.zeroTens/zeroOnes`, then a `{connective} N` blend as one `type:ui` sentence. Driven by the per-locale **`EXPANDED_FORM_L10N.<loc>`** table + `taskExpanded.<loc>` string + manifest `slug/title/intro.<loc>` (single-row-multi-locale → hreflang auto-emits the full 12-link chain). Every locale verified vs its live `_NUMBER_WORD_HELPERS.<loc>` AND its A1/A2 decomposition copula (not E4's):

| loc | connective | zeroTens / zeroOnes | slug | commit | note |
|---|---|---|---|---|---|
| en | makes | zero tens / zero ones | expanded-form | `bb1acd8a` | baseline |
| de | ergeben | null Zehner / null Einer | erweiterte-form | `7a1a16b8` | units-first one-word blend |
| da | er | nul tiere / nul enere | udvidet-form | `73068726` | copula `er` (A1/A2), NOT E4 `bliver`; spaced ` og ` in blend |
| no | blir | null tiere / null enere | utvidet-form | `5a91424a` | compound-`og` blend |
| sv | blir | noll tiotal / noll ental | utvecklad-form | `68ac5ae7` | invariant neuter place-nouns; compound no-joiner blend |
| pt | são | zero dezenas / zero unidades | forma-expandida | `cd0fd63d` | `e` joiner EVERY level (blend only); BR canonical |
| es | son | cero decenas / cero unidades | forma-desarrollada | `96889474` | `y` joiner sub-99 tail only |
| it | fanno | zero decine / zero unità | forma-estesa | `0ffe1add` | agglutinated; vowel-elision (208 `duecentotto`) in blend only |
| fr | font | **zéro dizaine / zéro unité** | forme-developpee | `d5b5b50c` | **SINGULAR** zero (FR-specific); standalone summands carry -s (`deux cents`/`quatre-vingts`), blend drops cent's -s mid-number; classic hyphenation; prose "Conforme aux …" |
| nl | maken | nul tientallen / nul eenheden | uitgebreide-vorm | `c596ed8e` | units-first `en`-joiner + trema (`drieënzestig`) in blend only; PLURAL zero; term = E4 catalog's "uitgebreide vorm" |
| fi | on | **nolla kymmentä / nolla ykköstä** | hajautettu-muoto | `97ddaad1` | pure agglutination, NO joiner; **PARTITIVE-SG** zero (count=0 rule); longest blends (≤40 ch) SPOKEN-only never rendered; copula `on`=3sg olla (NOT E4 distributive); `[NSR-FLAG][fi]` |

**Invariants confirmed every cycle:** summands bare per-place cardinals; caption digit-only; `if (n<100)` keeps A1 byte-identical; EN re-verified each cycle (byte-identical, count-agnostic hreflang); FI 32-char blend proven render-free (audio-only) at 375px.

**Frameworks (JSON-LD `educationalFramework`):** en Common Core State Standards · de Lehrplan · fr Programmes officiels (prose "Conforme aux programmes officiels") · es Currículo LOMLOE · pt BNCC · it Indicazioni nazionali · nl SLO-kerndoelen (prose "Afgestemd op de kerndoelen") · sv Lgr22 · da Fælles Mål · no LK20 · fi OPS 2014 (prose "Yhteensopiva OPS 2014:n kanssa").

**Cumulative E12+E4 regression sweep (2026-06-02):** ran all 85 `mini tools/.verify-*.js` → **56 clean / 29 baseline-only**. The 29: ≈20 chip-strand staleness ([7]/[13]/[14] strand-text hidden at narrow viewport / stale per backlog §8; grade+code PASS) + 1 NL ij-tile (E9) + 9 expanded-form sibling verifies whose hard-coded hreflang count was superseded by the now-complete **12-link chain** (pages emit the correct 12). **Zero FAIL on h1 / slug / framework / Common-Core-leak / locale-leak / 375px-overflow / caption / speech / celebrate.** No regression; no prior activity broken.

### E12 fan-out recipe (reusable for future E12 activities)
**State: E12 = 3 activities × 11 = 33 LIVE** (A1 1.NBT.B.2 · A2 2.NBT.A.1 · A3 expanded-form 2.NBT.A.3; A3 pool deepened **6 → 18** `e79e7826`). Final A3 commit `97ddaad1` (FI). Current versions: `place-value-activity.js?v=36`, core `place-value-core.js?v=25`, shared `ACTIVITY_WRAPPER_VERSION='8.30'`. Variety/depth arc CLOSED — no pending E12 build; further E12 activities are operator-prompted future-backlog. To fan out a FUTURE E12 activity: per-locale recipe = (1) run live `_NUMBER_WORD_HELPERS.<loc>` for the targets + each place-VALUE summand, report ACTUAL output; (2) verify connective + zero-place case-form against that locale's A1/A2 decomposition copula (NOT E4's) — singular/plural/partitive differs per locale (see table); (3) add `EXPANDED_FORM_L10N.<loc>` + `taskExpanded.<loc>` + manifest slug/title/intro; (4) bump BOTH `place-value-activity.js?v=` AND `ACTIVITY_WRAPPER_VERSION` same commit (§A.13.42); (5) cp the 3 place-value mini-tools files to `/var/www/lcs-media/mini-tools/` BEFORE `deploy.sh` (cp/deploy race); (6) per-locale Puppeteer verify (`[9]` joiner-cleanliness array + `[12]` interior-zero array) + EN re-verify; (7) core 0-lines + no MP import. Untracked verifiers: `mini tools/.verify-<slug>.js` (clone the latest).

## National-framework localization (2026-05-31)

The non-EN **activity layer** now cites each locale's national curriculum framework **name only** (no national code); CCSS code kept as JSON-LD `targetName` + `/standards/<code>` anchor. EN keeps Common Core; decks unchanged. Full doctrine + surfaces + bleed mechanism in [[project-activity-national-framework-localization]]. Commits `fe15a60b`/`f76588ad`/`adb1a976`.

**Strand/DOMAIN-name localization (companion, 2026-06):** beyond the framework NAME, the per-activity strand/DOMAIN name (the teacher chip's domain + JSON-LD `teaches`/`targetDescription` + byStrand prose, e.g. "Counting & Cardinality") is now localized to each locale's **national-curriculum domain name** across **all 11 locales** via `frontend/lib/seo/strand-names.ts` (commits `f049e150` de/fr/nl/sv/da/no/fi + `87196795` es/it/pt). Several CCSS strands legitimately **collapse onto one national domain** per locale (es Counting/Operations/Base-Ten → "Sentido numérico"; CLAUDE.md §17.4.3). **byStrand/chip/JSON-LD English-strand leak = 0 catalog-wide** (`audit-activity-pages.js` 266/266). The **CCSS code is preserved** as the machine identifier (JSON-LD `targetName` + `/standards/<code>`). CLAUDE.md §20.10 + §A.13.54/§A.13.56.

## Engines NOT yet built (per [[project-activities-master-queue]] leverage ranking)

- (E8 is COMPLETE — all 5 facade locales es/it/pt/fr/fi LIVE; removed from not-built)
- E4 match-pairs FURTHER activities (engine built; **5 activities live**; ~114 more K-skills unlocked from this engine — operator-prompted)
- E12 place-value (~80) — engine + **3 activities live (A1/A2/A3 ×11 = 33)**; further activities pending
- E5/E6 tracing (~73)
- E9 sight-word (~70)
- E14 fraction (~50)
- E3 sort, E13 array, E10 clock, E18 number-bond

## Total count

- **Inventory (post depth arc):** 3 manipulatives + 5 E1 + 10 E2 + 1 E7 (EN-only) + **5 E8** syllable (es/it/pt/fr/fi @28) + 5 E9 sound-chunk (de/nl/sv/no/da @28, de 30) + **5 E4 activities** (K.OA.A.3 · 1.OA.D.7 · 2.NBT.A.3 · 1.OA.D.8 · 2.NBT.A.4, each ×11) + 3 E12 activities (×11).
- **Localized URLs live ≈ 253** — prior ~229 + E8 IT/FR (**+2**) + E4 A4/A5 (2 activities ×11 = **+22**). (The prior "~229" base methodology was opaque; this increments it by the depth-arc additions — treat the precise figure as approximate, not a hard count.)
- Target: 500+ activities → ~5,500 localized URLs ([[project-activities-north-star]])

**Origin:** Live inventory as of commit `4bf56943` (FI E4 match-pairs first-activity ship, 11/11 ROLLOUT CLOSED). E4 activity sequence: DE/ES/IT/FR/PT/NL shipped earlier in session → SV `3a24e3a8` → DA `61944985` → NO `a61b9977` → FI `4bf56943`. Activity count + engine list verified against `mini tools/{ten-frame,choice-board,cvc-builder,syllable-builder,match-pairs}-activities.json` at write time.
