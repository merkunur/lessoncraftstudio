---
name: project-worksheet-gen-program
description: "The 243-type language-neutral worksheet catalog program — Gate rulings, build state (200 specs FANNED; wave-001 = 313 EN + 3,130 non-EN LIVE; +169 EN time/fractions/geometry/measurement expansion 2026-06-13 incl. NEW variant axis + 4 new clock types), architecture, next-wave playbook, variant-axis + wave-001-d2-collision gotchas"
metadata: 
  node_type: memory
  type: project
  originSessionId: 85ab19cb-de14-4503-8d41-965bb97cf483
---

# Worksheet-gen program (commission-to-fable-modular-aurora)

**SoT plan file + BUILD PROGRESS TRACKER: `C:\Users\rkgen\.claude\plans\commission-to-fable-modular-aurora.md` — READ FIRST on resume.** Emission-phase session plan: `continue-the-worksheet-gen-program-delightful-cloud.md`.

**Why:** operator commissioned ~243 printable worksheet TYPES designed once, fanned over image-library themes × 11 locales. Net-new headless generator at `scripts/worksheet-gen/`.

**Gate-1 rulings (do NOT re-litigate):** deck-pipeline integration (Option A); ~22 collapsed exercise-type family keys + type id as `variant_id`; money EXCLUDED; NO answer keys (`printable_only` schema 1.1). **Emission rulings (2026-06-13):** full EN wave in one pass (no live pilot gate); non-EN = follow-up commission.

**Gate-2: ALL 12 classes APPROVED + FANNED — 200 spec files QA-clean.**

**🏁 EMISSION PHASE SHIPPED (2026-06-13): wave-001 = 313 EN printable decks LIVE** (commits 99dd99d3..d1ac68ab). All 313 audit-clean; deck/PDF(noindex)/og 200; DB age_range per id-prefix band incl. first-ever Grade 3 (8-10) rows; topic hubs for new family keys 200. Slug shape `<family>[-easy|hard]-<theme>-<typeid>`.

**🏁 NON-EN AUTHORING + WAVES COMPLETE (2026-06-13): all 10 non-EN locales LIVE = 3,130 new printable decks (11 × 313 = 3,443 total).** Order de→es→nl→fr→it→pt→sv→da→no→fi (sv/da/no/fi [NSR-FLAG]). Each locale: 3-agent native ensemble (strings + SEO + adversarial review, `general-purpose` agents) → `i18n/apply-locale.js <loc>` (merges drafts+review into strings.<loc>.json/skill-sentences.<loc>.json/taxonomy slug+name×22/messages topicMeta×22+seo.words×2; slug-collision guard) → K-016 inline `i18n.<loc>` odd/even edit → `lint-locale.js`+all-themes sweep → commit → wave-NNN.json (seedEpoch 1, same 5 themes/d2/200 types → hreflang siblings) → generate → pscp → `publish-wave.js --locales=<loc> --confirm` → audit (all 313/locale passAll) → live verify. Full 11-locale hreflang families confirmed (da/de/en/es/fi/fr/it/nl/no/pt-BR/sv + x-default). SoT plan-file tracker = modular-aurora has per-locale commits + gotcha doctrine.

**🏁 TIME/FRACTIONS/GEOMETRY/MEASUREMENT EXPANSION (2026-06-13, EN, commits 78b0a387 + 9459c2bd): +169 new EN decks LIVE.** Operator asked for 10 worksheets per clock category + variety of 50 fractions / 20 geometry / 20 measurement. Delivered: **time 76** (8 categories ×10 in catalog), **fractions 50**, **geometry 20**, **measurement 23**.
- **NEW: variant axis** (additive, backward-compatible — default 1, gated on `variant>1` so all prior waves reproduce byte-identically). Wave plans gain `variantsPerType:N` + per-type `variants:{ID:N}` override. `lib/rng.js instanceSeed` folds `|vN` into seed (v>1 only); `enumerate.js` loops variant → `-vN` deckId suffix (v>1); `emit/manifest.js variant_id` gains `-N` suffix + `variant` field; `emit/deck-html.js` passes a **"Set N" disambiguator** (`disambiguator` for title + `variantId`/`variantLabel` for desc tail) → unique title/slug/desc per variant, clearing §17.8.17 HALT gates AT GEN TIME (preband skips printables). This is THE lever to make N distinct worksheets of one themeless type.
- **4 new telling-time types** (exerciseType `telling-time`, already taxonomy-registered ×11): G2-250 quarter-hour + G3-354 to-the-minute (reuse `read-circle`, new stepM); **2 new `clock-tasks.js` modes** — G3-355 `convert-24h` (12h/24h, alternating direction, AM/PM-flip + ±12h distractors, cols:1 full-width so 3 time chips fit) + G3-356 `time-arith` (start clock +/− duration → answerBox, 1:00–12:59 band guard). Both with `build`+`verify`. (Pre-existing 4: G1-148 oclock, G1-149 half-past, G2-249 five-min, G3-344 elapsed.)
- **DURABLE GOTCHA — wave-001 published every type at d2 ONLY**, so any new wave's (type, d2, variant1) instance COLLIDES (byte-identical slug+title) with the existing catalog. Two clean avoidances: (a) **difficulties [1,3]** (easy/hard — never published → zero collisions, also gives difficulty spread); (b) **variant≥2** (-N suffix → always unique). The variety waves use [1,3]+variants override; the time wave drops the 4 pre-existing types' v1 dupes (each type → 10 in catalog: wave-001 v1 + 9 new). Verify with `publish-wave.js <dir> --locales=en` (dry-run) reading `ok=`/`collisions=` BEFORE `--confirm`.
- measurement = 23 not 24: pre-existing layout edge (G2-235 ruler + a tall toy image overflows at d3) — single combo dropped by QA, not a regression.

**🏁 VISUAL-DEFECT FIXES (2026-06-13, commits d74152b6 + 8e71c8c2): 3 generator bugs + QA guard + remediation.** Operator review surfaced 3 defects (all root-caused in `types/_shared/` factories):
- **symmetry blank** (`geometry-tasks.js` pick-symmetric/symmetry-yn): a theme with too few mirror-symmetric nouns (e.g. **vehicles → sym=0**, side-view cars asymmetric) produced 0 cards → BLANK sheet that verify silently passed. Fix: throw when `sym < (symmetry-yn?2:1) || asym < 2` → cli alternate-theme retry; verify now fails on <1 card. **GOTCHA: themed types that gate on a content property (symmetry, mass-rank, flat-nouns) can silently emit blank — the new lint is the backstop.**
- **find-the-whole** (`fraction-tasks.js whole`): reference "one part" bar was 81px vs 36px answer cells → `size unitW*1.5` → `unitW/1.5`.
- **cube towers** (`measurement-tasks.js volume-cubes`): full l×w×h solids hid interior/back cubes (uncountable). Now **single-depth walls** (w=1, l 2-5, h 2-3) — every front face visible; answer/verify (l*w*h) unchanged.
- **QA guard** (`qa/lints.js`): new non-empty-body lint hard-fails any sheet rendering zero content elements (`.ws-card-stage`/`.ws-match-item`/`.ws-bin`/`.ws-pattern-slot`/`.ws-pattern-chip`) — blank-sheet class now unpublishable for ALL types.
- **publish-cli bug fixed** (`bulk.js`, commit 8e71c8c2, deployed to Hetzner via `git checkout origin/... -- scripts/publish-cli/bulk.js`): the `--updates-manifest` UPDATE dry-run hardcoded `thisDeckId=null` → false `TITLE_NON_UNIQUE`/`DESC_NON_UNIQUE` when title/desc unchanged (in-place image fix). Now resolves the existing row id (publish.js:207 real-path was already correct). **This is THE mechanism for in-place deck UPDATEs: `publish-wave.js <dir> --locales=en --updates-manifest=<json {zip:slug}> --confirm` (re-runs full SEO finalization, unlike single-deck --update-slug).**
- **Remediation done:** re-published the 4 EN session decks (G3-321 whole + G3-346 volume, d1/d3) in place; **unpublished 11 blank `g2248`+vehicles decks across all 11 locales** (pick-symmetric; localized slugs geometry-vehicles-g2248 / geometrie-fahrzeuge-g2248 / …; now 404). Identifier: classify themes locally with `maskSignature`+`maskIoU` (only vehicles broke), reconstruct slugs via `slug.js deriveSeedFromManifest`, intersect with published. My EN g2247-animals/g2248-fruits decks were fine (unaffected).
- **DEFERRED (operator):** the cosmetic find-the-whole/cube fixes for the OTHER 10 locales' wave-001 decks (a later sweep — same `--updates-manifest` flow per locale). EN done.

**Phase-0 substrate (commit de6907bd):** `emit/deck-html.js` per-locale skill-sentence loader; `i18n/lint-locale.js` (200 ids + per-band title uniqueness + skill/taxonomy/messages/K-016 completeness + ENGLISH_LEXICON-collision pre-check, reusing publish-cli's exported lexicon); `i18n/apply-locale.js` merge tool; `emit/deck-html.test.js` SWEEP upgraded to render every themed type × ALL 5 wave themes per authored locale (theme-name length shifts the desc band); `enumerate.js` non-EN pre-flight.

**Gotchas (durable):** (1) desc-band failures (DESCRIPTION_TOO_SHORT) surface per-THEME, not just animals — fix by tightening/lengthening skill `short` into the in-band window, NEVER move the gate (de 3 trims + K-057 rename vs shadow-match app; nl 1; da 4). (2) fr "multiplication" = ENGLISH_LEXICON entry → LOCALE_RESIDUE on 26 decks; fix = fr exception in `seo-reconciliation-exceptions.json` + lint pre-check. (3) pt hreflang code is `pt-BR` (uppercase) — grep with `[a-zA-Z-]` not `[a-z-]`. **Pre-existing legacy-catalog defects surfaced by audit (NOT this commission):** da/no legacy LOCALE_RESIDUE (find-addend English tokens), de/es/nl/sv legacy CANONICAL_NO_TRAILING_SLASH — each needs its own retrofit ruling.

**Emission machinery (all proven live):**
- `emit/` — manifest.js (schema 1.1, printable_only, family-key exercise_type, d1/d3→easy/hard mode d2→null, age_range from id prefix K/G1/G2/G3→5-7/6-8/7-9/8-10, BW themes→underscore axis keys, images scraped from rendered HTML) · deck-html.js (static preview reusing publish-cli `build-seo-head.js`; title+desc banded AT EMIT — **preband skips printables by design**; interactive-deck body contract → all publish-wave retrofits no-op; `__PDF_URL__` CTA) · assets.js (860px preview JPEG data-URI + 480×620 thumbnail) · zip.js. 16 tests incl. 200-type sweep (title ≤70, desc 120-170, unique titles/band) run REAL publish-cli gates.
- `i18n/` — strings.en.json (build-en.js + title-uniqueness lint) · strings.js resolver (locale→en→spec) · **skill-sentences.en.json** (22 per-family pairs; joins bandedDescription middle pool — THE fix for DESCRIPTION_LENGTH_TOO_SHORT on short-title/themeless types).
- `enumerate.js` + `waves/wave-NNN.json` + `cli.js generate` (resume via skip-if-ZIP-exists; QA gate; **alternate-theme retry** on theme-incompatibility throws — never duplicates a sibling slot's theme).
- publish-cli additive edits: bundle.js answer-key relaxation (+bundle.test.js) · substitute.js age_range chain + `__PDF_URL__` · publish.js per-deck ageRange · audit printable profile (Check 11 no-role, Check 13 skip) · preband printable skip · republish-seo inferAgeRange.
- Taxonomy: 22 family keys in axes.exercise-type (en-only, picture-trail precedent) + apps.* defaults + 22 en topicMeta entries. 512px derived icon cache (`image-cache/derive.js`; `fileUri {full:true}` opt-out; PDFs 3.4MB→0.4-1.4MB).

**NEXT-WAVE PLAYBOOK:** new themes need pull-themes + derive.js + taxonomy axes.theme key; `node cli.js generate --wave waves/wave-NNN.json` → pscp to `/root/staging/<wave>/` → `publish-wave.js <dir> --locales=<loc> --confirm`. d1/d3 unlock 2 more slots per (type,theme).

**DEFERRED:** non-EN authoring (per-type strings ×10 + taxonomy slug/name ×11 + skill sentences ×10 + seo.words 2 keys ×10, §A.13.48 ensembles); catalog-axes.ts inclusion (operator); landing fans for family keys (§22.5); honest-ledger items (money ×4, color-attribute ×4, K-074/G3-347 noun maps, G3-324, K-026 + story-sequencing unbuildable). **NOTE:** the previously-unbuildable id `G2-250` is now USED for the quarter-hour clock type (the free id was repurposed; no prior file existed). **Non-EN fan-out of the 4 new time types** (40 strings ×10 locales, §A.13.48 ensemble) is the natural follow-up to take this expansion to all 11 locales.

**🏁 NON-EN FAN-OUT OF THE EXPANSION COMPLETE (2026-06-13, session "witty-squid"): all 10 non-EN locales LIVE = 1,690 new printable decks (169/locale).** Fanned last session's +169 EN expansion (4 new time types + fractions/geometry/measurement d1/d3+variants) to de→es→nl→fr→it→pt→sv→da→no→fi. Each = 169 (time 76, fractions 50, geometry 20, measurement 23 — same documented g2235-toys-d3 QA drop), replicated the 4 EN waves VERBATIM (seedEpoch 1) so all are hreflang siblings of EN → the 4 new time types now carry full 11-locale hreflang families (verified G3-355: da/de/en/es/fi/fr/it/nl/no/pt-BR/sv + x-default). Per-locale commits `780f28ce`(de) `5c9ac822`(es) `7e9c3b92`(nl) `7522249e`(fr) `01c6a893`(it) `4e48eee1`(pt) `5c61bf48`(sv) `95da644d`(da) `787d719e`(no) `84950185`(fi); sv/da/no/fi `[NSR-FLAG]`.
- **Authoring per locale = ONLY 9 strings** (4 new time-type title+instruction + 1 `seo.words.set` word). Everything else (fractions/geometry/measurement strings, easy/hard mode names+slugs ×11, telling-time skill sentences, taxonomy) was already authored. 3-agent native ensemble per locale (2 parallel `general-purpose` drafters → synthesize → operator plan-mode approval), surfacing the G3-355 24h-convention fork.
- **G3-355 24h fork (de/nl/fr/it/sv/da/no/fi):** these locales use 24-hour time as the everyday norm, so G3-355 was framed around CONVERTING between two notations (umwandeln/omzetten/transformer/muunna…), dropping the family "read the clock" instruction (the EN canonical already does this — it's a notation match, not a clock read). es/pt 12h-in-speech but framed the same.
- **`seo.words.set` per locale** (was undefined everywhere incl. EN → EN variants leaked hardcoded "Set"): de Satz · es/pt Conjunto · nl/it Set · fr/it Série(fr)/Set(it) · sv/da/no Set/Sæt/Sett · fi Sarja. Mostly = the existing `variant_label`. hreflang sibling grouping uses language-independent `variant_id`, NOT the localized title, so localizing "Set" does NOT break pairing.
- **DURABLE GOTCHA — K-038 mode-word polysemy collision (de/sv/da/no ONLY):** the "heavy/light" type K-038's title contains that locale's word for LIGHT, which IS the locale's EASY mode word (de schwer/leicht, sv lätt, da let, no lett — Germanic/Nordic polysemy). The title composer dedups a mode word already present in the title → d1/d3 render IDENTICAL titles → TITLE/DESC_NON_UNIQUE HALT. Fix = rename the K-038 title to avoid the easy/hard words (de "Was wiegt mehr?", sv/da/no "Vad väger mest?"/"Hvad vejer mest?"/"Hva veier mest?"). es/nl/fr/it/pt/fi unaffected (their heavy/light words ≠ easy/hard words). Slug is family-based so unchanged; existing d2 deck untouched. **Pre-scan all d1/d3 wave-type titles for the locale's easy/hard mode-word as a standalone token before publishing a new locale** (publish-bulk dry-run also catches it as TITLE_NON_UNIQUE).
- **SCOPED PUBLISH RECIPE (operator-mandated, avoids the ~50-min catalog-wide og regen):** per locale = pscp to `/root/staging/wsg-exp-<loc>/` → drop the 4 time-v1 collision ZIPs (g1148/g1149/g2249/g3344 pre-existing clock decks, → 169 from 173) → `index.js publish-bulk <dir> --dry-run` (confirm ok=169) → `--confirm` → `populate-and-inject-hreflang --confirm --locales=<all 11>` (~16s, REQUIRED at all-11) → `regenerate-og-images --locales=<loc> --slugs-file=<new slugs>` (scoped, ~12s vs ~15min). NOT full publish-wave. The alt-text/end-links/embed-hide/img-dims/lazy-deckend retrofit steps are genuine no-ops for wsg printables (substitute.js inside publish-bulk fills all body placeholders at publish). Scoped on-disk audit over the 169 new slugs (canonical/h1/hreflang/og-png) instead of full-locale `audit-deck-html` (which surfaces pre-existing legacy noise).
- **Per-locale tooling:** clone the 4 EN waves changing only id+locales; throwaway `i18n/_merge-newtime.js` merged a `.draft-<loc>-newtime.json` (incl. optional `_K038` title fix) into strings.<loc>.json in EN key order (apply-locale.js expects a full 204-id draft → wrong tool for 4 strings). Write drafts via Write tool (clean accents/apostrophes) not shell here-strings. fr uses STRAIGHT apostrophes to match its corpus. Nordic/Romance render the mode word LOWERCASE ("lätt"/"fácil") per title-config `segmentCasing:"sentence"` — correct, by design (de title-cases "Leicht").

**SURFACED PRE-EXISTING (not this program):** `CANONICAL_NO_TRAILING_SLASH` on 3,101 legacy interactive en decks (audit 2026-06-13) — needs its own retrofit ruling.
