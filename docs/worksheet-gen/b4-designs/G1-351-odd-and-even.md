# G1-351 `odd-and-even` (G1/G3) : FINAL design (editor merge, 2026-09-21)

Merged from `_work/G1-351-pedagogy.md` + `_work/G1-351-design.md` (record of every ruling: `_work/G1-351-critic.md`). (m) = re-measured 2026-09-21 by read-only node (`frontend/config/topics-taxonomy.json`; `frontend/content/seo-landing/<loc>.json` x11 = 34,068 landings; `frontend/lib/seo/strand-names.ts`; `scripts/verify-hub-type-rows.js LEVEL_KEYS`; `lib/b3-picture-index.js pictureIndex()` x `lib/b2-common.js countable`; `data/b2/labels.js`; `data/b2/sentences.js`; `types/k/K-016-odd-or-even-pairs.js`; `types/g2/G2-218-odd-even-circle.js`; `primitives/ten-frame.js`; `page/page.css`; `primitives/_tokens.js density`; `templates/components-b3/ordinal-numbers.js blankNumeralBox:151`; `types/_shared/notation.js divGlyph`; `tools/gate-variation-distinct.js`) or by puppeteer with the shell's Baloo 2 / Nunito woff2 from `file://` (scratch `G1-351-critic-measure.js`, `document.fonts.check` true for both faces; the pedagogy's `G1-351-measure.js` numbers re-run and confirmed). *est.* = the engineer measures in the real render. No em-dashes.

**Boundary (load-bearing).** This family owns the RULE of parity and its PROOF, on numerals: numerals SORTED into an odd house and an even house (base); dots the CHILD circles into pairs with the two equal addends of 2.OA.C.3 written as `n = a + a + r` (F1); pictures dealt between two NAMED friends with "each gets" and "left over" written (F2); the ones digit isolated in a highlighted place-value box, 10-99 (F3); the parity of a sum read from the ones digits without adding (F4); 11-18 pictures the child pairs with the PAIRS count written (F5). It is NOT **K-016** `odd-or-even-pictures` (`counting-pictures`, K: 4 themed cards, counts d2 4-10 (d1 2-7, d3 6-13), the pictures ALREADY stacked in 2-icon white columns with the leftover in a dashed coral column, the numeral PRINTED, the child only circles a chip; its `i18n[loc].odd/even` are the catalogue's ONLY parity literals and this family reuses them verbatim, m), NOT **G2-218** `odd-and-even-numbers` (`number-charts`, G23: d2 16 loose numerals to 99 in `.ws-pattern-slot` cells 124 x 84, "Circle every ODD number. Look at the last digit to decide.", d3 to 999 UNSHIPPED, m), NOT **G1-130** (colour a skip path on a full 100-chart), NOT the **doubles-halves** family G1-247 + G1-270/271/286/287/288/296/297/298/299 (`mirrorGroups` reflection = the double, 2N pictures in two rows with a cut line = the half, `4 + 4 = box` / `12 = box + box`; heads `Verdoppeln und Halbieren` / `Dubbelt och hälften` / `Kaksinkertainen ja puolet` x11 (m) are BANNED as whole-word tokens in every authored string of this type), NOT **K-021 / K-131 / K-132** (`types/_shared/neighbors.js`: a `[blank][given][blank]` strip), NOT **G3-377** `division-with-remainder` (quotative rings of d with `minR:1`, q / r notation boxes, a division glyph; its `casitaFrame` is a division bracket, not a house, m), NOT `counting-frames` (the nl head "Tienraam"; NO ten-frame is drawn anywhere on this type: `primitives/ten-frame.js:31-33` fills ROW-first (`i % cols`, `floor(i / cols)`), so 7 reads "5 and 2", never "3 pairs and 1", m). Structural fences the gates enforce: `0` never appears on any page (fi "nolla" trap); `=` only inside an equation row, never in a dot row; `+` and `=` are the only glyphs and are universal; no division glyph (`divGlyph(loc)`), no `:` between digits; no numeral is ever printed beside a pile of pictures; no sum is ever printed on F4; no parity word is stamped on a numeral chip; the only words the code prints are the two K-016 chip literals, the F3 rule sentence, two captions (`leftover`, `pairs`), the two place-value header letters and two names from `SENTENCES[loc].names`. Visual signature: **two drawn houses side by side** (teal 3 px shell, a tealSoft roof and a coralSoft roof, a chimney, a word sign under the eave, dashed coral numeral boxes inside) under a strip of round numeral chips; nothing else in the catalogue draws a house, so a teacher tells the family apart across the room.

## 1 Identity

| field | value |
|---|---|
| id / key / bands | `G1-351` / `odd-and-even` / base **G1**; F2 F5 **G1** (`G1-354+ TBD by the emitter`); F1 F3 **G2** (`G2-347+ TBD`); F4 **G3** (`G3-385+ TBD`). `default_subject: math`, `default_age_range: 6-8`, `assetClass: numeral-charts` (base F1 F3 F4) / `icon-placement` (F2 F5), `exerciseType: odd-and-even`. `apps['odd-and-even']` + `axes['exercise-type']['odd-and-even']` ABSENT (m: `apps` 114 keys, `axes['exercise-type']` 115); register with the `tools/register-b4-taxonomy.js` shape `{default_subject:'math', default_age_range:'6-8', exercise_type_axis_key:'odd-and-even'}` + slug/name x11 (table B). The 11 slugs grepped against every `axes.*.*.slug.<loc>` AND every landing slug in the 11 `seo-landing/<loc>.json` files: **0 collisions** (m); no landing slug matches `odd-or-even` / `k016` / `odd-and-even` in any locale (K-016 and G2-218 have NO landing of their own; the `number-charts` en hub carries 4 landings, m). |
| theme axis | **THEMELESS on the base, F1, F3, F4** (`themeAxis:{applicable:false}`; landings `coordinate.theme:''`; numerals only, no picture library art). **THEMED on the two picture faces F2 and F5** (`themeAxis:{applicable:true, minNouns:6, excludeBw:true}`; the wave's `themeOverrides` pins ONE theme per face; landings carry that theme's axis key). Their pool = `entriesFor(theme, loc)` filtered by `countable(e)` (`lib/b2-common.js:50`: plural differs from singular, m); a page needs 6 DISTINCT nouns (one per lane, cloned n times). Catalogue-wide countable pictures per locale (m, `pictureIndex()` 880 keys x `countable`): en 722 · de 608 · es 729 · pt 741 · fr 716 · it 658 · nl 751 · sv 628 · da 659 · no 659 · fi 741; every K-320 fan theme clears 6 countable nouns in every locale by an order of magnitude (`_SUBSTRATE.md` fan-set minima 19-40). The picture is a COUNTING token only: no noun is ever printed, so no picture ruling (plum = apple) can reach a page as a word; the gate asserts `fileUri` resolves and `img.naturalWidth > 0` for every pick, and the panel OPENS every noun of its pinned theme (§4). **No `unitAxis`**: a range fan would put 1-100 on the base without the F3 scaffold; the two ranges the harvest names (bis 20 / bis 100) are the base's and F3's own titles. |
| CCSS (en, honest) | base / F2 / F5: **readiness, NO code** (2.OA.C.3 is grade 2; there is no G1 parity code; 1.OA.C.6 is NOT claimed) · F1 / F3: **2.OA.C.3** (`teaches`: odd and even, pairing, two equal addends; the F3 landing states that its 10-99 range exceeds the standard's "up to 20") · F4: **3.OA.D.9** (parity of a sum as an arithmetic pattern explained by properties of operations). `educationalAlignment` on en only, `targetName` only, no `targetUrl`. |
| strand row (the trap, m) | `strand-names.ts:45-57` `'Operations & Algebraic Thinking'` renders pt **"Álgebra"**, sv **"Algebra"**, nl **"Bewerkingen"**, all wrong for parity in those curricula; the `'Number and Operations in Base Ten'` row (`:100-112`) carries the panel literals for es / fr / it / pt / sv / fi but da "Tal og algebra (positionssystemet)" and nl "Getallen en het tientallig stelsel" carry a parenthetical / suffix that is false here. **The landing takes the per-locale strand literal from table B** (the Facts-1 mechanism for the science types), never a `strand-names.ts` row; en alone keeps "Operations & Algebraic Thinking". README open item for the strand map (§critic OPEN 1). |
| new primitives | **none.** The house and the paired dots are component-level SVG on tokens in `templates/components-b4/odd-and-even.js` (§2). No ten-frame, no base-ten blocks, no figure. |

| loc | genre head (title = head verbatim; the panels' heads) | ASCII slug | level keys G1 / G2 / G3 (`LEVEL_KEYS`, m; base F2 F5 / F1 F3 / F4) | place-value headers (F3, GIVEN labels: lock-given in bold; the rest are PROPOSALS the panel confirms) | national strand literal (framework NAME; the landing prints THIS) |
|---|---|---|---|---|---|
| en | Odd and Even Numbers (never "Even and Odd" in a title; "pdf" belongs to the meta; "chart" never in a title: `number-charts` owns it) | `odd-and-even` | `grade-1` / `grade-2` / `grade-3` | T \| O | Operations & Algebraic Thinking |
| de | Gerade und ungerade Zahlen ("gerade" lowercase, "Zahlen" capital; the RANGE strings "bis 20" (base) / "bis 100" (F3) are the de winners and sit in those two titles) | `gerade-und-ungerade-zahlen` | `1-klasse` / `2-klasse` / `3-klasse` | **Z \| E** | Zahlen und Operationen (KMK Leitidee; the panel gave no de literal: OPEN 1) |
| es (MX) | Números pares e impares (euphonic "e impares", the only correct form; "preescolar" REFUSED as a level: K-016 boundary) | `numeros-pares-e-impares` | `primer-grado` / `segundo-grado` / `tercer-grado` | **D \| U** | Sentido numérico (SEP / NEM) |
| pt (BR) | Números pares e ímpares (accented; ALWAYS the full phrase, bare "pares e ímpares" is the lottery intent) | `numeros-pares-e-impares` | `1o-ano` / `2o-ano` / `3o-ano` (the harvest's year for the base; the curriculum places parity at 2º: the landing prose states 2º ano either way, OPEN 3) | D \| U | Números (BNCC) |
| fr | Nombres pairs et impairs (programme wording; "pair impair CP" is the teacher shorthand for the meta; "pair / impair" masculine with "nombre") | `nombres-pairs-et-impairs` | `cp` / `ce1` / `ce2` | **d \| u** | Nombres et calcul (programmes officiels) |
| it | Numeri pari e dispari (never bare "Pari e dispari": a 1978 film; "scheda" never in a title) | `numeri-pari-e-dispari` | `classe-prima` / `classe-seconda` / `classe-terza` | da \| u (abaco convention) vs D \| U: OPEN 2 | Numeri (Indicazioni nazionali) |
| nl | Even en oneven getallen (always "getallen"; "Tienraam" NEVER in a title and no ten-frame is drawn) | `even-en-oneven-getallen` | `groep-3` / `groep-4` / `groep-5` | **T \| E** | Getallen (SLO kerndoelen) |
| sv | Udda och jämna tal ("tal" never "siffror"; "vecka" in NO string: the week-parity meaning pollutes the meta; F2 framing "kan delas lika i två") | `udda-och-jamna-tal` | `ak-1` / `ak-2` / `ak-3` | **T \| E** | Taluppfattning och tals användning (Lgr22) |
| da | Lige og ulige tal (never "ulige og lige"; F2 "dele ligeligt", never "dele lige") | `lige-og-ulige-tal` | `1-klasse` / `2-klasse` / `3-klasse` | T \| E | Tal og algebra (Fælles Mål) [NSR] |
| no | Partall og oddetall (bokmål; the chips are NOUNS, fine as labels; "plakat" is a companion format, not ours) | `partall-og-oddetall` | `2-trinn` / `3-trinn` / `4-trinn` | T \| E | Tall og algebra (LK20) [NSR] |
| fi | Parilliset ja parittomat luvut (question titles use "vai" never "tai"; "20:een asti" / "sataan asti" range idiom; a page never contains 0) | `parilliset-ja-parittomat-luvut` | `1-luokka` / `2-luokka` / `3-luokka` | **K \| Y** | Luvut ja laskutoimitukset (OPS 2014) [NSR] |

**The rule that locks the type.** Every item's answer is `n mod 2`, re-derived by `verify()` from the stamped `data-lcs-val` / `data-lcs-n` / `data-lcs-a` + `data-lcs-b`; no parity attribute is ever stamped on a chip or a numeral; the only words printed are whole literals from the bank (no frame has a slot; nothing inflects); `bank.chips` is asserted EQUAL to `K-016.i18n[loc].odd/.even` at validate time (one source; a divergence is a K-016 edit request, never a second literal); the two names come from `SENTENCES[loc].names` (8 per locale, m) filtered to <= 6 graphemes (it keeps 5: Sofia Giulia Aurora Alice Matteo; every other locale >= 7, m).

## 2 The base page

**Concept.** "Odd and Even Numbers: Odd House, Even House." Nine round numeral chips in a strip; below them two houses, one for odd and one for even, each with its word on a sign under the eave and eight empty dashed boxes inside. Three numbers have already moved in: they sit in a white worked tile inside their house with their pairs drawn beside them, column-first, the lone one a hollow coral ring: the worked examples (the lock's "cue on the first three items only"). The child reads a chip, decides, and WRITES the numeral into a box in that house. The cue lives ONLY beside a solved numeral inside its house, never beside an open chip: the nine open chips are bare, so nothing on the page says where a chip goes. The pedagogy's cue-under-the-first-three-strip-tiles was dropped: a cue on a strip tile prints that tile's answer while the tile is still a task.

**Chrome budget (README ruling).** Body **722** with a 3-line title + 3-line instruction; **677** under a 4-line fi title; 814 with one-line chrome. `.ws-page` inner 675 (`page.css:16`, m); `.ws-lane` (`page.css:401`, `padding:12px 16px`, border 2) inner **639** at the default padding (m); `.ws-card` (`:126`, padding 12, border 2) in a 2-column `.ws-cardgrid` (gap 14) = 330 outer / **302 inner** (m). Rows `minmax(<min>px, 1fr)`; slack opens between the strip and the houses, never inside the houses.

**Layout d2**
```
y 0     chipStrip  .ws-lane 675 x 92  (default padding; inner 639; data-ws-content)
        ( 5 )( 12 )( 18 )( 3 )( 20 )( 11 )( 8 )( 15 )( 14 )   9 x .ws-chip 64 round, gap 7 = 632 <= 639
y 112   houseRow  675 x 426  (flex, gap 15, align-items:center)
        +------ houseBin 330 x 426 ------+   +------ houseBin 330 x 426 ------+
        |          /\   roof tealSoft    |   |          /\   roof coralSoft   |
        |         /  \  chimney 28 x 52  |   |         /  \                   |
        |   _____/____\_____  eave y 120 |   |   _____/____\_____             |
        |     (  even  )   sign y 140 h44|   |     (  odd  )                  |
        |  [ 12 oo oo oo oo oo ][ 8 oooo ]  |   |  [ 7  oo oo oo O ]              |  worked tiles 136 x 60 at y 196
        |  [box][box][box][box]  y 270   |   |  [box][box][box][box]          |  blankNumeralBox 62 x 60, gap 14 / 12
        |  [box][box][box][box]  y 344   |   |  [box][box][box][box]          |  grid 284 x 134, body x 20..310
        |  |__________________________|  |   |  |__________________________|  |
        +--------------------------------+   +--------------------------------+
y 538   92 + 20 + 426 = 538 <= 677 (slack 139) <= 722 (slack 184); rows 92px minmax(426px, 1fr)
```
- Chip strip: `.ws-chip` (`page.css:190`: 52 round, teal 2.5, Baloo 2 700) overridden inline to `width:64px;height:64px;font-size:26px`; "20" = 29.0 px at Baloo 2 700 26 (m) inside a 59 px inner disc; 9 x 64 + 8 x 7 = **632 <= 639** (m arithmetic), `justify-content:center`. Chips >= the G1 element floor 44 (`_tokens.js:69`, m).
- House shell (viewBox `0 0 330 426`, y down, strokes teal 3, `stroke-linejoin:round`): chimney FIRST `roundedRect x 236 y 40 w 28 h 52 r 3` white (the right roof edge runs y 57.1 -> 78.1 across x 236..264, so 17-38 px of chimney stand above it, m arithmetic); roof polygon `(10,120) (165,4) (320,120)` fill tealSoft (even) / coralSoft (odd); body `roundedRect x 20 y 120 w 290 h 304 r 6` white, drawn AFTER the roof so the eave is the body's top edge. No door, no windows (a window pair restates the pair cue). Body inner width 290: the box grid is **4 x 62 + 3 x 12 = 284** (the design's first 298 overhung the wall by 4 px each side and was corrected; recorded) and the worked row is **136 + 12 + 136 = 284** (the design's 140 + 14 + 140 = 294 > 290 is corrected here, m arithmetic).
- Sign: `<span data-lcs-sign>` `.ws-pill` shell (`page.css:422`; white, teal 2, r 999) centred at top 140, h 44, Nunito 800 20 ink, `padding 6 18`, `white-space:nowrap`, max-width 260; widest word fi "parillinen" 90.4 + 36 = 126, de "ungerade" 89.5 + 36 = 126 (m); an overflow REFUSES the literal, never shrinks the font.
- Worked tile `workedTile({n})` 136 x 60: white, 2 px creamDeep, r 10 (the GIVEN-cell style of `lineUpStrip`, `ordinal-numbers.js:110-112`, m): `[numeral Baloo 2 700 26 ink, 36 wide][8][pairDots({n}) <= 64 x 30]` + padding 14 each side = 136. Worked numerals <= 10 (the `pairDots` ceiling: 5 columns).
- Boxes: `blankNumeralBox({w:62, h:60})` (`ordinal-numbers.js:151`; `.ws-blankbox` white, dashed coral 2.5, r 10; stamps `data-lcs-answer=""`, m) in `grid-template-columns:repeat(4,62px); gap:14px 12px` at top 270; 60 high >= the G1 answer numeral 26 with pencil room.
- House order follows the locale's TITLE order (`houseOrder` in the bank): even LEFT for de es pt fr it nl da no fi, odd LEFT for en sv. Roof colour follows the HOUSE (even tealSoft, odd coralSoft) in every locale; the sign word is the mono cue.
- G1 floors: chips 64 >= 44, boxes 62 x 60 >= 44, numerals 26 >= 26, 9 open + 3 worked = 12 numerals in [6, 12].

**Ladder** (resolved config keys; guards key on THESE, never the level index):

| key | d1 | d2 (ships) | d3 |
|---|---|---|---|
| `range` | [1, 10] | **[1, 20]** | [1, 30] (0 NEVER: the fi trap is resolved by absence, not by a rule about 0) |
| `chips` (open) | 6 (one row) | **9** | 12 (two chip rows of 6; lane h 164) |
| `worked` | 3 (<= 10; 2 + 1) | **3** (<= 10; 2 in one house, 1 in the other; both parities worked) | 2 (1 per house, <= 10) |
| `boxes` per house | 3 x 2 = 6 | **4 x 2 = 8** | 4 x 2 = 8 |
| parity split (chips + worked) | 4 + 5 or 5 + 4 | **6 + 6** | 7 + 7 |
| stack | 92 + 20 + 426 = 538 | **538** | 164 + 20 + 426 = 610 <= 677 |

**d3 ruling:** the design's `0..30` at d3 is REPLACED by `1..30`: the pedagogy's "0 never appears on any page" is cheaper than a special case ("0 is even, the fi instruction says so") on a level that never ships; the render gate asserts `0` absent at every level.

**Composer.** `rng.sample([range])` of `chips + worked` distinct numerals with exactly the split above; `worked` = numerals <= 10 with both parities present, placed 2 + 1 (the side of the 2 is a coin flip of the seed); the open chips `rng.shuffle`d and re-shuffled until the order is neither ascending nor alternating in parity for more than 4 consecutive chips. Every level asserts `boxes per house > open chips of that house's parity` AND `boxes !== that count` (a box count equal to the answer count is a leak). The seed is locale-neutral: the same numerals in all 11 locales.

**Answer-hiding + uniqueness.** Chips `<span class="ws-chip" data-lcs-val="<v>">` carry the numeral and NOTHING about parity; houses `<div data-lcs-house="odd|even" data-ws-content>`; worked tiles `<span data-lcs-given="<n>">` INSIDE a house (the only place a house name meets a numeral is a solved example); `pairDots` stamps `data-lcs-pairdots data-lcs-n data-lcs-pairs="<floor(n/2)>" data-lcs-single="<n%2>"` and is rendered ONLY inside a worked tile (the component throws when called without a `data-lcs-given` ancestor; verify asserts `[data-lcs-pairdots]` count === `worked` and each has a `[data-lcs-given]` ancestor); every open box is `blankNumeralBox` with `data-lcs-answer=""`. `verify(page)`: 2 `[data-lcs-house]` = {odd, even} in the bank's `houseOrder`; chip values distinct, in `range`, count === `chips`, none 0; worked numerals distinct from the chips, <= 10, parity === the house they sit in, pairs === `floor(n/2)`, single === `n % 2`; chips + worked = the split; per house blank boxes === `boxes` with every `data-lcs-answer === ''`, `boxes > open of that parity`, `boxes !== open`; chip order neither ascending nor >4 alternating; the signs' text === `chips.odd` / `chips.even`; no `<img>`; no `data-lcs-prim="ten-frame"`; no text node equals a `doublesHalves` token (§5 rule 6); every chip and house inside `[data-ws-content]`. Poison: a worked 7 with 4 pairs -> FAIL; a worked 8 in the odd house -> FAIL; a `pairDots` injected under an open chip -> FAIL; a box count of 4 in a house with 4 open -> FAIL.

**Reused (exact).** `blankNumeralBox` (`components-b3/ordinal-numbers.js:151`) · the GIVEN-cell style (`:110-112`) · `cardGrid({cards, cols, rows, numbered})` (`templates/layouts/card-grid.js`) + `.ws-card .ws-card-badge` (faces F1 only) · `.ws-chip` (`page.css:190`, size overridden inline) · `.ws-lane` (`:401`) · `.ws-pill` (`:422`) · `svgRoot el roundedRect line circle label esc` (`primitives/_svg.js`) · tokens `color.cream/creamDeep/white/teal/tealSoft/coral/coralSoft/ink/inkSoft/grid`, `stroke.primitive 3 / grid 1.5`, `density.G1 44/26/[6,12]`, `density.G23 36/22/[8,16]` (`_tokens.js:67-71`, m), `font.display/body` · `entriesFor sampleEntries fileUri countable` (`lib/b2-common.js`, F2 / F5) · `SENTENCES[loc].names` (`data/b2/sentences.js`) · `bank(name, loc)` via `lib/b4-common.js` (README open item 3) · the K-016 chip literals x11, asserted equal at validate time (never READ from K-016 at render). **NOT used.** `tenFrame` (row-first fill hides the pairs) · `baseTenBlocks` (rods, no digit box) · `answerBox` (stamps `data-lcs-answer="undefined"` without an answer) · `chipRow` (52 px chips with a derived font) · `equalGroups` / `dealBoxes` (G3-370 / G3-377 slot rows) · `mirrorGroups` / `dotPanel` (G1-247 territory) · `truthChips` / `yesNoChips` (yes/no keys) · `hundredsChart` (G1-130) · `casitaFrame` / `divGlyph` (division) · `countBadge` (a numeral by a pile) · `LABELS.numberOfDay.tens/ones` (the full words; the lock wants the letters) · `friendGlyph` (the design's neutral figures: REPLACED by the lock's named plates) · `image-vocabulary.js` at render (no noun is printed) · `pictureFor` (a random candidate is a picture nobody opened).

**NEW `templates/components-b4/odd-and-even.js`** (behind the `templates/components-b4.js` barrel, clone of the b3 barrel; HTML + inline SVG on tokens; scoped inline CSS; no `page.css` edit; exports exactly `chipStrip, houseBin, workedTile, pairDots, dotRowCard, shareLane, countLane, placeValueRow, tickPair, ruleStrip, parityTable`):
- `chipStrip({values, chip=64, gap=7, rows=1})` -> `.ws-lane data-lcs-chips data-ws-content`, `display:flex; flex-wrap:wrap; gap:8px 7px; justify-content:center`; asserts `values.length x (chip + gap) - gap <= 639 x rows`.
- `houseBin({parity, label, w=330, h=426, worked:[{n}], boxes:{cols:4, rows:2, w:62, h:60, gapX:12, gapY:14}})` -> the shell above + three HTML layers (sign top 140, worked row top 196 `display:flex; gap:12px; justify-content:center`, box grid top 270 `justify-content:center`); asserts `w >= 300` and `boxes.cols x w + (cols - 1) x gapX <= 290 - 6`.
- `workedTile({n})` 136 x 60 as above; `pairDots({n, r=4.5, pitch=12})` -> `svgRoot (cols x 12 + 4) x 30`, `cols = ceil(n/2)`: dot i at column `floor(i/2)`, row `i % 2` (COLUMN-first: a column is a pair), cx `2 + col x 12 + 6`, cy `2 + row x 12 + 7`; pair dots `circle r 4.5 fill teal`; the single (odd n, top row of the last column) `circle r 4.5 fill none stroke coral 2.5` (hollow vs solid = the mono cue); throws for `n < 1` or `n > 10`; max width 5 x 12 + 4 = 64.
- `dotRowCard({n, dot=16, gap=8, perRow=10, boxW=44, remW=36, remLabel})` (F1) · `shareLane({n, src, names:[a, b], leftoverLabel})` (F2) · `countLane({n, src, pairsLabel})` (F5) · `placeValueRow({digits:[t, o], headers})` (F3) · `tickPair({keys:['even','odd'], correct})` (F3 / F4: two empty 44 px `.ws-chip` circles, `data-lcs-tick="even|odd"`, one `data-lcs-correct="1"`) · `ruleStrip({text})` (F3) · `parityTable({chips})` (F4): geometry in §3.

**Alternatives (rejected).** Alt A K-016 cards with numerals instead of pictures (numeral + chips, 8 cards): G2-218's hunt in card form, proves nothing, the shape every competitor ships. Alt B a 1..20 strip the child colours by a two-swatch legend: G1-130's move, the answer rides on a colour word on a mono printer, no writing. Alt C cut-out numeral cards sorted into two boxes: the `cutting` family K-240 / K-264 owns scissors-first and a cut page carries no verify. Alt D (pedagogy) a 12-tile strip with the cue under the first three tiles and 6 cells per house, stack *est.* 720: a cue on a task tile prints its answer; the houses' cells (6 = the open count of that parity) leak; the design's 538 is measured.

**Risks -> mitigations.** Mono print: the roof fills (tealSoft / coralSoft, both L ~91 %) are NOT distinguishable in greyscale and are not asked to be; the SIGN word is the house's identity; the pair cue is solid-vs-hollow; teal 3 px lines print ~30 % grey; the engineer prints one d2 greyscale and confirms the odd house reads by its sign at arm's length. A chimney that reads as a flag at 330 px: open the render. A worked ring dot that reads as a zero: r 4.5 at stroke 2.5 next to solid dots of the same size; open the render. Nine chips inviting copying in order: the shuffle rule. **Palette:** cream, creamDeep, white, teal, tealSoft, coral, coralSoft, ink, inkSoft, grid. **Font floor:** smallest text 12 (the F1 leftover caption) vs the 9 px lint (`qa/lints.js:86`). Every strip, house, lane and card grid stamps `[data-ws-content]` (`qa/lints.js:32`).

## 3 Faces 2-6

All five variations are CODE faces on ONE additive `mode` knob (`d.mode`, absent on the base; `build()` dispatches BEFORE the base path and stamps `data-lcs-mode` only when set, so the base DOM stays byte-identical; the G1-310 precedent). Guards key on the RESOLVED config (`d.mode`, `d.range`, `d.items`, `d.worked`), never the level index. Resolved d2 configs are pairwise distinct (mode differs on every face; F1 / F3 / F4 also differ in range). `tools/gate-variation-distinct.js` has `--batch=b2|b3` only today (m: `:48-60`); the build adds `b4` (README step 4; critic OPEN 4). Density: G1 faces pictures 44, boxes 44 high, numerals 26, 6 items; G2 / G3 faces boxes >= 36, numerals 26 >= 22, 8-12 items. Pills everywhere = the K-016 pill (`.ws-chip` with `width:auto; min-width:64px; height:44px; font-size:17px; border-radius:22px; padding:0 14px`, Baloo 2 700; K-016 ships h 40 at K, this type takes 44 on the G1 faces): widest words at 17 px are fi "parillinen" 73.2 and de "ungerade" 73.0 (m), so a pill is <= 102 px; a stacked pair is 2 x 44 + 6 = 94 high. Tick circles (`tickPair`) are used where the words would repeat per row (F3, F4): the two words are printed ONCE per column above the circles.

### F1 : Odd or Even? Write Two Equal Addends (G2, CODE `mode:'proof'`)
**Move:** PROVE. The numeral is GIVEN; beneath it a single panel of n plain teal dots the CHILD rings two by two; beneath that the equation frame `n = box + box + box` where the third box is narrower, dashed coral, and takes 0 or 1 (captioned with the `leftover` literal). This is 2.OA.C.3 literally (`8 = 4 + 4 + 0`, `7 = 3 + 3 + 1`). **RULED: the frame is UNIFORM on every card** (the design's fork): a frame that grew a third box only on odd cards would print the parity before the child counted; `+ 0` on even cards is the price of answer-hiding, and the landing says so ("the small box takes 0 when nothing is left over"). No parity pills on this face (the equation IS the conclusion; pills would push the card to 190 and the grid to 802). `=` sits in the equation row only, never in the dot panel; `+` is universal; never "3 pairs + 1" as an equation. Differs from the base (nothing to sort, a written equation), from K-016 (the pairing is the child's act on bare dots), from G1-247 / G1-288 (no double, no reflection, no cut line: an equal-addend SPLIT of a GIVEN number with a remainder).
**Layout:** `cardGrid({cols:2, rows:4, numbered:true})`; card 330 / inner 302; card = `[dots panel 279 x 52][10][equation row 44][4][caption 16]` = 126 + 28 = **154**; 4 x 154 + 3 x 14 = **658 <= 677** (slack 19; <= 722 slack 64, rows `minmax(154px, 1fr)`). Dots panel `<div data-lcs-dots data-lcs-n>` white, 2 px creamDeep, r 12, `padding:4px`: `ceil(n/10)` rows of svg `circle r 8 fill teal`, pitch 24 (gap 8 = the pencil-ring reserve), a row of 10 = 232 wide, rows centred, 2 rows = 40 high; NO pairing drawn, NO coral single (the child draws the rings). Equation row `display:flex; align-items:center; justify-content:center; gap:8px; height:44px`: `[numeral Baloo 2 700 26 ink data-lcs-num][= Baloo 2 700 22 inkSoft data-lcs-eq][blankNumeralBox 44x44 data-lcs-role="a"][+ Baloo 2 700 22 teal][blankNumeralBox 44x44 data-lcs-role="b"][+][blankNumeralBox 36x40 r 8 data-lcs-role="r"]` = "20 = " 47.5 + 44 + 14.2 + 44 + 14.2 + 36 + 6 x 8 = **248 <= 302** (m glyphs, arithmetic). Caption: the `leftover` literal Nunito 800 12 inkSoft centred under the r box (de "bleibt übrig" 66.5 wide at 12, m; it overhangs the 36 px box by 15 px each side inside the card).
**Config:** d1 `{mode:'proof', range:[2,10], cards:6 (2 x 3), boxW:48}` · **d2** `{mode:'proof', range:[3,20], cards:8, split:[4,4], minTwoRow:2}` (at least 2 cards with n >= 11, two dot rows) · d3 `{mode:'proof', range:[11,20], cards:8, split:[4,4]}`. n distinct across the page. G2 floors: boxes 44 x 44 and 36 x 40 >= 36, numeral 26 >= 22, 8 items in [8, 16].
**Verify:** 8 `[data-lcs-dots]` with exactly n `circle` children and no text node (poison: a `stroke coral` circle -> FAIL; an `=` inside the panel -> FAIL); `[data-lcs-num]` === n; exactly three `.ws-blankbox` with roles a / b / r in DOM order, all `data-lcs-answer === ''`; expected a = b = `floor(n/2)`, r = `n % 2`, re-derived from n (a node cross-check, never stamped); split 4 / 4; n distinct in range; the caption text === `leftover[loc]`; no `doublesHalves` token on the page.
**Query face:** "two equal addends" / "pair proof": en "odd and even numbers two equal addends worksheet" · de "gerade Zahlen als Summe zweier gleicher Zahlen" ("Paare", never "Pärchen") · fr "nombre pair somme de deux nombres égaux" · sv "dela upp talet i två lika delar" · fi "kahden yhtä suuren luvun summa".

### F2 : Can Two Friends Share Fairly? (G1, CODE `mode:'share'`, THEMED)
**Move:** DEAL. One lane per item: n identical pictures on the left (clones of ONE noun of the wave's theme, 44 px); on the right two plates, each labelled with a NAME from `SENTENCES[loc].names` (two different names per lane, `rng.sample` over the <= 6-grapheme subset; 0 new literals), each holding one `blankNumeralBox`; then a box captioned with the `leftover` literal; then the two pills stacked. The child deals the pictures in turn (one for Mia, one for Ben, one for Mia ...; a crossed-out picture is a legal tally, no line is required), writes the count on each plate (both = `floor(n/2)`), writes the leftover (0 or 1) and rings the pill. Sharpened from yes/no to "each gets, left over 0/1" (the lock) so it is not a coin toss; the design's `each` box and neutral `friendGlyph`s are replaced by the two NAMED plates (the lock), and the `each` caption is DROPPED (the name IS the plate's label; one literal fewer). Fenced from G1-288 (2N pictures in two equal rows with a printed CUT line, `12 = box + box`, even only): here odd counts, no cut line, named plates, a leftover, a parity conclusion; from G3-377 / G3-370 (quotative rings, `minR:1`, q / r notation): here partitive between exactly two, exact sharing allowed, NO notation, no glyph; from K-016: the child deals, nothing is pre-paired.
**Layout:** 6 `.ws-lane`s with inline `padding:4px 16px` (stated: inner 639 x 94; lane = 94 + 8 + 4 = **106**), grid `gap:6px`: 6 x 106 + 5 x 6 = **666 <= 677** (slack 11; <= 722 slack 56, rows `minmax(106px, 1fr)`). Lane row `display:flex; align-items:center; gap:12px`: `[pile 284 x 94][12][plate A 64][8][plate B 64][8][leftover col 84][8][pills 102]` = 284 + 12 + 64 + 8 + 64 + 8 + 84 + 8 + 102 = **634 <= 639**. Pile `<div data-lcs-pile data-lcs-n>` `display:flex; flex-wrap:wrap; gap:6px 4px; width:284px; justify-content:flex-start; align-content:center` of n `.ws-icon` 44 x 44 (6 per row: 6 x 44 + 5 x 4 = 284; n <= 12 -> 2 rows = 94). Plate = `<div data-lcs-plate data-lcs-name="<name>">` column: name Nunito 800 14 ink, nowrap, centred, 18 high, max-width 64 (a 6-grapheme name is ~51 px *est.*; the render gate refuses a wider one) + 4 + `blankNumeralBox({w:56, h:44})` `data-lcs-role="each"` = 66 high. Leftover col: caption `leftover` Nunito 800 14 inkSoft (widest de "bleibt übrig" 77.5 <= 84, m) + 4 + `blankNumeralBox({w:44, h:44})` `data-lcs-role="r"`. Pills `display:flex; flex-direction:column; gap:6px` of the two K-016 pills h 44 (fi 101.2 <= 102, m), order = `houseOrder`, one `data-lcs-correct="1"`. G1 floors: pictures 44, boxes 44 high with a 26 px numeral, 6 items in [6, 12].
**Config:** d1 `{mode:'share', range:[3,8], lanes:6, iconPx:44, perRow:6}` · **d2** `{mode:'share', range:[5,12], lanes:6, iconPx:44, perRow:6, split:[3,3]}` (n distinct: odd {5,7,9,11} choose 3, even {6,8,10,12} choose 3) · d3 `{mode:'share', range:[7,12], lanes:6, iconPx:44, perRow:6, split:[3,3], pills:false}` (the leftover box is the conclusion; the pedagogy's 9..15 at icon 40 sits below the G1 floor and is refused as a level).
**Verify:** 6 `[data-lcs-pile]` with distinct `n` in range, split 3 / 3; exactly n `<img>` per pile with one identical `src` that `fileUri` resolves (`img.complete && naturalWidth > 0`), no localized B&W marker (`BW|SW|BN|NB|ZW|SH|PB|MV|SV`) in any `src`; 6 distinct nouns, each `countable` for the locale; two `[data-lcs-plate]` per lane with DIFFERENT `data-lcs-name`, both in `SENTENCES[loc].names`, each name's rendered width <= 64; boxes `each` x2 + `r` all `data-lcs-answer === ''`; expected each = `floor(n/2)`, r = `n % 2` (node cross-check); one `data-lcs-correct` pill === `chips[n % 2 ? 'odd' : 'even']`; no text matches `divGlyph(loc)`, `÷`, `:` between digits, or any `doublesHalves` token; no numeral printed anywhere on the lane (no `countBadge`).
**Query face:** "share fairly / between two": en "share between two odd or even worksheet" · de "gerecht teilen: gerade oder ungerade" · fr "partage équitable pair impair" · sv "dela lika i två" (the Lgr22 framing "kan delas lika i två", the strongest sv face) · da "dele ligeligt" (never "dele lige") · fi "jaetaan tasan kahdelle".

### F3 : Odd and Even Numbers 1 to 100: Look at the Ones Box (G2, CODE `mode:'ones'`)
**Move:** ISOLATE the ones digit. A rule strip at the top (the `rule` literal: "Even numbers end in 0, 2, 4, 6 or 8. Odd numbers end in 1, 3, 5, 7 or 9."); then two columns of six rows, each row = the number printed digit-by-digit in two place-value boxes under the lock-given header letters (printed ONCE per column at the top), the ONES box coralSoft-filled with a SOLID coral 2.5 border (not dashed: it is not a blank; the highlight IS the scaffold and survives mono print by its border), and two 44 px tick circles under the column heads `chips.even` / `chips.odd` (printed once per column). The child ticks. The only face that reaches 100 (the lock); the sheet draws 10-99 (a one-digit number would leave the tens box empty and 100 needs a third box: both refused as items; "1 to 100 / bis 100 / del 1 al 100" is the number space the title names, as the harvest heads do). Fenced from G2-218 (loose numerals, circle the odds, no boxes, no rule strip): here every number is decided, in boxes, from the highlighted digit.
**Layout:** `ruleStrip` = `.ws-lane` inline `padding:8px 16px`, Nunito 800 15 ink (14 for fi), centred, `minmax(48px, auto)`: en 72 chars = 497 px one line, de 87 = 616 one line, pt 85 = 616 one line, fi 107 = 684 at 14 = TWO lines (m, the pedagogy's run); two lines = 2 x 20 + 16 + 4 = **60**; cap 214 chars. Then a 2-column grid (gap 14; column 330): header row 24 `[T 48][4][O 48]` Nunito 800 14 inkSoft centred over the boxes + 16 + `[even 80][10][odd 80]` Baloo 2 700 17 ink centred over the circles (fi "parillinen" 73.2 <= 80, m) = 286 <= 330; then 6 rows h 48, gap 10: `[tens box 48 x 48 white, 2 px grid, r 8][4][ones box 48 x 48 coralSoft, coral 2.5 solid, r 8]` digits Baloo 2 700 30 ink (widest "0" 18.0 at 30, m) + 16 + `[circle 44][10][circle 44]`. Stack: 60 + 12 + 24 + 8 + 6 x 48 + 5 x 10 = **442 <= 677** (slack 235; rows `minmax(48px, 1fr)` spread the slack between rows). G2 floors: boxes 48 >= 36, digits 30 >= 22, circles 44 >= 36, 12 items in [8, 16].
**Config:** d1 `{mode:'ones', range:[10,30], items:8 (2 x 4), rule:true}` · **d2** `{mode:'ones', range:[10,99], items:12 (2 x 6), rule:true, distinctOnes:true, minEachParity:4, minHigh:4}` (no two numbers share a ones digit; at least 4 in 50..99 so the tens digit varies and the child cannot key on size) · d3 `{mode:'ones', range:[10,99], items:16 (2 x 8), rule:false}` (the strip withdrawn: the child recalls the rule; 16 x 48 / 2 + 7 x 10 + 44 = 498).
**Verify:** 12 `[data-lcs-pv data-lcs-val]` in range; tens box text === `floor(v/10)`, ones box text === `v % 10`, exactly one `data-lcs-highlight` per row and it is the ONES box (poison: highlight on tens -> FAIL); the ones box's computed background === coralSoft and the tens box's === white; header texts === `placeHeads[loc]` (2 entries, 1-2 letters, distinct) once per column, word heads === `chips[loc]` once per column; two circles per row `data-lcs-tick="even|odd"`, one `data-lcs-correct` matching `v % 2`, both EMPTY; ones digits distinct across the page; >= 4 of each parity; no value < 10 or > 99; the rule strip text === `rule[loc]` when `d.rule !== false`, absent otherwise; no plain numeral text node outside the boxes.
**Query face:** the range head: en "odd and even numbers 1 to 100 worksheet" · de "gerade und ungerade Zahlen bis 100 Arbeitsblatt Klasse 2" (the second de winner) · es "pares e impares del 1 al 100" · pt "pares e ímpares de 1 a 100" · nl "even en oneven getallen tot 100 groep 4" · fi "parilliset ja parittomat luvut sataan asti".

### F4 : Odd or Even Sum? Decide Without Adding (G3, CODE `mode:'sums'`)
**Move:** REASON about the parity of a sum (3.OA.D.9). A three-row `parityTable` at the top built ONLY from the chip words and glyphs (`even + even = even` / `odd + odd = even` / `even + odd = odd`); then two columns of five rows `a + b` in Baloo 2 700 26 with the ones digit of each addend underlined (a `<span>` with `border-bottom:2px solid coral`; a `text-decoration` underline shifts in the PDF), and two tick circles under the word heads. The child classifies each addend from its ones digit, applies the table, ticks. **RULED: three-digit addends at d2** (the pedagogy): with two-digit addends a grade-3 child adds in seconds and the instruction "do not add" is a fiction; with 100-999 the rule is faster than the sum, and this is where the measured 3-digit demand (es "del 1 al 1000" x1, sv / da / no "till / til 1000") lands under a head of its own. Never a sum printed anywhere, no `=` outside the table, no result box. Fenced from `mental-math` / `column-arithmetic` (compute) and from F3 (single numbers).
**Layout:** `parityTable` = `.ws-lane` inline `padding:8px 16px`, `width:max-content`, centred, 3 rows Baloo 2 700 16 ink, row h 24: widest de "ungerade + ungerade = gerade" 217.4, fi 204.9, no 197.6 (m) -> 3 x 24 + 16 + 4 = **92** high, <= 250 wide. Then the 2-column grid: word-head row 24 (`[even 80][10][odd 80]` over the circles, right-aligned in the column); 5 rows h 48, gap 10: `[expression "347 + 258" 107 (m at 26; "888 + 888" 112.3)][16][circle 44][10][circle 44]` = 245 <= 330. Stack: 92 + 12 + 24 + 8 + 5 x 48 + 4 x 10 = **416 <= 677** (slack 261, rows `minmax(48px, 1fr)`). G3 floors: circles 44 >= 36, numerals 26 >= 22, 10 items in [8, 16].
**Config:** d1 `{mode:'sums', range:[10,99], items:8, terms:2, table:true}` · **d2** `{mode:'sums', range:[100,999], items:10 (2 x 5), terms:2, table:true, cases:{ee:2, oo:2, eo:2, oe:2}}` (each parity case at least twice; >= 4 even and >= 4 odd sums) · d3 `{mode:'sums', range:[100,999], items:12 (2 x 6), terms:3, table:false}` ("347 + 258 + 106" ~180 *est.*; 6 x 48 + 5 x 10 + 32 = 370).
**Verify:** 10 `[data-lcs-sum data-lcs-a data-lcs-b]` (+ `data-lcs-c` at terms 3), addends in range and distinct across the page; the printed text is exactly `a + b` with the locale-neutral `+` (never a locale sign; `notation.js` is not involved); the row contains NO text node equal to `String(a + b)` (poison: a sum printed -> FAIL) and NO `=` (poison -> FAIL); each addend's LAST digit span carries `data-lcs-ones` and the border; one `data-lcs-correct` circle matching `(a + b) % 2`, both circles empty; the table rows read `chips[x] + chips[y] = chips[z]` for exactly the three true combinations in the order [ee, oo, eo] and nothing else (`table:false` = absent); the case counts; no `.ws-blankbox` on the page.
**Query face:** "parity of sums / without adding": en "odd and even sums worksheet grade 3" · de "Summe gerade oder ungerade ohne zu rechnen" (3. Klasse) · fr "somme paire ou impaire sans calculer" · it "la somma è pari o dispari?" · fi "onko summa parillinen vai pariton" (3. lk).

### F5 : Odd or Even? Count the Pictures in Pairs (G1, CODE `mode:'count'`, THEMED)
**RULING on the contradiction (the lock's provisional clause).** The pedagogy REPLACED the provisional PARAM range-to-999 with this face; the design recommended PARAM-999 because the how-many face "fails the G1 floors above n 12" on a CARD grid (its arithmetic: 2 x 3 cards at 4 per row = 742 > 722; 3 x 2 = 798; correct for cards). Re-done here on a LANE layout: 9 pictures per row at 44 px, gap 7 / 6, two rows = 436 x 94; a pairs column 56; the two pills stacked 94 x 102; lane inner 436 + 12 + 56 + 10 + 102 = **616 <= 639**; lane 106 at `padding:4px 16px`; 6 lanes = **666 <= 677**. So the face IS buildable at every G1 floor (pictures 44, boxes 44, pills 44, 6 items) for counts 11-18, with a 7 px pencil-ring reserve between pictures. Demand: the en winnable string "even and odd kindergarten worksheet pairs" exists in the harvest; the 3-digit demand is es x1 + three panel query faces (thin, the lock's own criterion for replacement) and lands honestly on F4 d2. Doctrine: a face must change what the child DOES; K-016 pre-pairs 4-10 pictures and prints the count; here 11-18 loose pictures, the pairing and the PAIRS count are the child's, and the count is unknown. PARAM-999 would be the base's sort with bigger numbers (a range on the same act, which the pedagogy's own non-move list rejects unless the scaffold changes) and, unlike the design's claim, nothing shipped reaches 999 (G2-218 d3 is unpublished): it is recorded as a spare, not built. **F5 = count the pictures in pairs.** The design's leftover box is dropped from this face on WIDTH (a leftover column would take the lane to 695 / 646 > 639 at 9 / 8 per row); the unringed lone picture is the visible proof and F1 / F2 own the written leftover; the pairs box + the pill carry the record.
**Move:** COUNT then PAIR: one lane per item with 11-18 identical pictures in rows of 9 (row-first, so the second row shows "nine and some more", never the parity); the child counts, rings the pictures two by two, writes the number of PAIRS in the box (captioned with the `pairs` literal) and rings the pill. Fenced from F1 (numeral given, dots, an equation) and F2 (deal to two friends, "each gets"): here the count is unknown and the written quantity is the number of pairs; from K-016: nothing is pre-paired, the numeral is not printed, the counts start where K-016's shipped range ends.
**Layout:** 6 `.ws-lane`s inline `padding:4px 16px` (inner 639 x 94; lane 106), grid `gap:6px`: 6 x 106 + 30 = **666 <= 677** (slack 11; <= 722 slack 56, rows `minmax(106px, 1fr)`). Lane row `display:flex; align-items:center; gap:12px`: `[pile 436 x 94][12][pairs col 56][10][pills 102]` = **616 <= 639**. Pile `<div data-lcs-pile data-lcs-n>` `display:flex; flex-wrap:wrap; gap:6px 7px; width:436px; justify-content:flex-start` of n `.ws-icon` 44 (9 per row: 9 x 44 + 8 x 7 = 436; two rows 44 + 6 + 44 = 94). Pairs col: caption `pairs` Nunito 800 14 inkSoft (widest es "parejas" 48.2 <= 56, m) + 4 + `blankNumeralBox({w:56, h:44})` `data-lcs-role="pairs"` = 66 high. Pills stacked as F2. G1 floors: pictures 44, box 44 with a 26 numeral, pills 44, 6 items.
**Config:** d1 `{mode:'count', range:[8,14], lanes:6, iconPx:44, perRow:9}` · **d2** `{mode:'count', range:[11,18], lanes:6, iconPx:44, perRow:9, split:[3,3]}` (n distinct: odd {11,13,15,17} choose 3, even {12,14,16,18} choose 3 = 16 count sets) · d3 `{mode:'count', range:[13,18], lanes:6, iconPx:44, perRow:9, split:[3,3]}` (harder counts only; the honest ceiling).
**Verify:** 6 `[data-lcs-pile]` with distinct `n` in range, split 3 / 3; exactly n `<img>` of one `src` per lane, `fileUri` resolves, no B&W marker; 6 distinct `countable` nouns; the pairs box `data-lcs-answer === ''`, expected `floor(n/2)` (node cross-check); one `data-lcs-correct` pill === `chips[n % 2 ? 'odd' : 'even']`; caption text === `pairs[loc]`; no numeral printed on the lane; no `doublesHalves` token.
**Query face:** "count in pairs / pairs": en "even and odd worksheet count pairs" · de "Bilder paarweise zählen: gerade oder ungerade" · es "cuenta y forma parejas: par o impar" · nl "tel en maak paren: even of oneven" · fi "laske kuvat pareittain".

**Rejected non-moves.** A range as a face on the SAME sort (bis 50 / bis 1000: the range earns a face only when the scaffold changes, F3) · the PARAM range-to-999 houses (thin measured demand; F3 without its scaffold; its numbers live on F4 d2; kept as a recorded spare) · a ten-frame cue (row-first fill hides the pairs) · a coloured 100-chart (G1-130) · circle-the-odds in a grid (G2-218) · pre-paired picture columns (K-016) · doubles / halves / a cut line (the G1-247 family) · a `[before][n][after]` strip (K-021) · yes / no chips on F2 (a coin toss) · a q / r notation row (G3-377) · odd / even as a COLOURING key (the read-and-color family) · a third box only on odd cards in F1 (prints the answer) · a numeral beside a pile (prints the count) · cut-out numeral cards (the cutting family) · d1 / d3 relabelled as a face.

## 4 Native rebuild plan x11

No frame in this type has a slot; nothing inflects. Every printed word is a whole literal from the bank. Per locale the panel authors **12 strings** (6 titles + 6 instructions) + **1 rule** sentence + **2 captions** (`leftover`, `pairs`) + **2 place-value letters** (lock-given where bold in §1) + `houseOrder`, and CONFIRMS the **2 chips** (reused from K-016, asserted equal): 19 literals, of which 3 are new words (rule, leftover, pairs; the lock's <= 4 cap holds). The panel also AUDITS the EN as a SOURCE (the §6 candidates), and OPENS every noun of the wave theme pinned for F2 and F5 (`picOpened` per noun; the sv #35 `fruits/plum` = a red apple precedent) since a picture that is not what its noun says still COUNTS correctly but must not be a wine glass or a bare square. Instructions are the child's sentence and name only apparatus ON the page: "house", "numbers", "boxes", "dots", "pictures", "friends", "plates", "circles", "the rule"; never "ten-frame", never "letter boxes", never "line" (no line is drawn or required). Caption widths at Nunito 800 14 (m): `leftover` 28.8 (nl "over") to 77.5 (de "bleibt übrig"), `pairs` 22.1 (sv / da / no "par") to 48.2 (es "parejas"); the reserves are 84 and 56; the validator caps each caption at 16 chars and the render gate measures.

| loc | literals (counts) | forms / slots and where they exist | refusal / re-target | traps |
|---|---|---|---|---|
| en | 12 strings · rule (72 ch, 1 line) · leftover "left over" · pairs "pairs" · T \| O · chips odd / even (K-016) · `houseOrder` odd-left = 19 | names Mia Ben Emma Leo Anna Tom Lily Max (all <= 6) | none | title never "Even and Odd" (lock); "pdf" belongs to the meta; NO "with answers" anywhere (printable-only decks ship no answer key, README open item 6); US spelling; "chart" never in a title |
| de | 12 · rule ("Gerade Zahlen enden auf 0, 2, 4, 6 oder 8. Ungerade Zahlen enden auf 1, 3, 5, 7 oder 9.", 87 ch = 616 px, 1 line, m) · leftover "bleibt übrig" (77.5) · pairs "Paare" · Z \| E · chips ungerade / gerade · even-left = 19 | names Emma Ben Mia Finn Lina Paul Lea Noah | none | chips print the BARE lowercase adjective (never "Gerade", never "gerade Zahl"); "Zahlen" capital in titles; "Paare" not "Pärchen"; "bis 20" (base) / "bis 100" (F3) in the FACE titles; no `Verdoppeln / Halbieren / Hälfte / halb` token anywhere; the engine appends "Arbeitsblatt" |
| es (MX) | 12 · rule · leftover "sobra" · pairs "parejas" (48.2) · D \| U · chips impar / par · even-left = 19 | names Sofía Mateo Lucía Diego Hugo Camila Martín (Valentina > 6 dropped) | none; level "preescolar" REFUSED (lock, K-016 boundary) | "e impares" (euphonic e) in every title; MX register; "del 1 al 100" on F3; never "mitad / doble" |
| pt (BR) | 12 · rule (85 ch = 616 px, 1 line, m) · leftover "sobra" · pairs "pares" · D \| U (proposal) · chips ímpar / par · even-left = 19 | names Sofia Miguel Alice Davi Laura Pedro Helena Lucas | none | accented "ímpares"; ALWAYS "números pares e ímpares"; "de 1 a 100" on F3; "atividade" never in a title; year placement OPEN 3 |
| fr | 12 · rule · leftover "il reste" · pairs "paires" · d \| u · chips impair / pair · even-left = 19 | names Léa Hugo Chloé Louis Emma Nathan Inès Jules | none | "nombres pairs" (masculine); F2 head "partage équitable"; "fiche" never in a title; never "moitié / double"; NBSP before "?" |
| it | 12 · rule · leftover "avanza" · pairs "coppie" · da \| u (abaco) vs D \| U: OPEN 2 · chips dispari / pari · even-left = 19 | names Sofia Giulia Aurora Alice Matteo (5 of 8 <= 6 graphemes) | none | never a title equal to or starting "Pari e dispari" (film); "scheda" never in a title; never "metà / doppio" |
| nl | 12 · rule · leftover "over" · pairs "paren" · T \| E · chips oneven / even · even-left = 19 | names Emma Daan Julia Sem Tess Finn Lotte Luuk | none | always "getallen" with the head; "Tienraam" never in a title and no ten-frame is drawn; "werkblad" is appended; never "halveren / verdubbelen" |
| sv | 12 · rule · leftover "blir över" · pairs "par" · T \| E · chips udda / jämnt · odd-left = 19 | names Elsa Hugo Alma Liam Maja Oscar Vera Elias | none | "jämnt" (neuter, agrees with "tal") on the chip, "jämna tal" in the title; "tal" never "siffror"; "vecka" in NO string; never "hälften / dubbelt"; the engine appends "arbetsblad" [NSR] |
| da | 12 · rule · leftover "tilovers" · pairs "par" · T \| E (proposal) · chips ulige / lige · even-left = 19 | names Ida Freja Oscar Alma Noah Clara Emil (William > 6 dropped) | none | "lige og ulige" order, never reversed; F2 "dele ligeligt" never "dele lige"; never "det halve / det dobbelte" [NSR] |
| no | 12 · rule · leftover "til overs" · pairs "par" · T \| E (proposal) · chips oddetall / partall · even-left = 19 | names Nora Jakob Emma Oliver Ella Aksel Sofie | none | bokmål only; the chips are NOUNS (partall / oddetall), fine as labels and column heads; never "halvparten / dobbelte" [NSR] |
| fi | 12 · rule ("Parilliset luvut päättyvät numeroon 0, 2, 4, 6 tai 8. Parittomat luvut päättyvät numeroon 1, 3, 5, 7 tai 9.", 107 ch = 684 px at 14 = TWO lines, m) · leftover "jää yli" · pairs "parit" · K \| Y · chips pariton / parillinen · even-left = 19 | names Aino Eeli Sofia Onni Aada Väinö Helmi Leo | none | question titles use "vai" never "tai" ("Parillinen vai pariton?"); "tai" is correct inside the digit list of the rule; 0 never on a page; "20:een asti" / "sataan asti" range idiom; never "puolet / puolita / tupla / kaksinkertainen"; the fi 4-line title (677) is the stack every face is measured against above [NSR] |

Every panel OPENS every picture of the theme the wave pins for F2 and F5 (one contact sheet per locale at 44 px, the shipped size) and one render of the base to confirm the house signs read in its language.

## 5 Data + gates

`data/b4/odd-and-even.js` (the EN block, hand-authored, + the GLOBAL geometry and the `doublesBan` token lists) + `data/b4/locales/odd-and-even.<loc>.json` GENERATED from `i18n/.draft-b4-<loc>.json` by `tools/apply-b4-locale.js` after `tools/validate-b4-draft.js` (clones of the b3 pair `apply-b3-locale.js` / `validate-b3-draft.js`, both exist, m; the b4 pair is absent, m; `data/` gitignored, `git add -f`); read through `lib/b4-common.js bank('odd-and-even', loc)` (absent, README open item 3; an unauthored locale REFUSES, never falls back to en). Read at render: the bank, `SENTENCES[loc].names`, `entriesFor(theme, loc)` + `countable` + `fileUri` (F2 / F5); never `image-vocabulary.js` beyond `vocab()` for the countable test, never `approved-words-*.json`, never `LABELS.numberOfDay`, never K-016 (the chips are copied into the bank at apply time after the equality check).

```
ODD_AND_EVEN (data/b4/odd-and-even.js, GLOBAL, code not data):
  worked:   { count:3, maxN:10, split:[2,1] },  ranges per face and level as §2/§3,
  tableOrder: ['ee','oo','eo'],                 // the F4 parity table rows
  doublesBan: { <loc>: ['double','doubles','half','halves', ...] }   // whole-word tokens per locale, from
              // data/b2/labels.js doublesHalves + axes['exercise-type']['doubles-halves'].name.<loc> (stop-words removed)
              // + the panel's inflected forms (de Hälfte Hälften halbe halbieren verdoppeln; sv halva hälften dubbla; ...)
ODD_AND_EVEN[loc] = {
  chips:      { odd:'odd', even:'even' },       // MUST equal K-016.i18n[loc].odd / .even (the catalogue's only parity literals)
  houseOrder: ['odd','even'] | ['even','odd'],  // the title order; en sv odd-left, the rest even-left
  rule:       'Even numbers end in 0, 2, 4, 6 or 8. Odd numbers end in 1, 3, 5, 7 or 9.',  // F3 strip, <= 214 chars
  leftover:   'left over',                      // F1 caption + F2 caption, <= 16 chars
  pairs:      'pairs',                          // F5 caption, <= 16 chars
  placeHeads: ['T', 'O'],                       // F3 column letters, 1-2 letters each, distinct (lock-given where §1 is bold)
  nameMaxGraphemes: 6,                          // the F2 name subset (>= 2 names must survive; measured 5-8 x11)
  strings:    { base:{title, instruction}, proof:{…}, share:{…}, ones:{…}, sums:{…}, count:{…} },
  refuse:     { }                               // per-face: proof | share | ones | sums | count (none expected)
}
```
Helpers (all throw rather than fall back): `parity(n)` = `n % 2 ? 'odd' : 'even'`; `pairsOf(n)` = `floor(n/2)`; `splitSample(rng, range, k, split)` = k distinct numerals with exactly `split` odd / even; `namePair(rng, loc)` = two distinct names of <= `nameMaxGraphemes` graphemes (`Array.from(name).length`); `banRegex(loc)` = one regex of every `doublesBan[loc]` token as `(?<!\p{L})token(?!\p{L})` with the `iu` flags (never `\b`: ASCII-only, blind to ä / ö / å / ü).

**`tools/validate-b4-draft.js` (odd-and-even block; every rule runs, exit 1 on any):**
1. `chips.odd` and `chips.even` non-empty, distinct (case-insensitive), no space, <= 12 chars, and `=== require('types/k/K-016-odd-or-even-pairs.js').i18n[loc].odd / .even` (one source; a divergence is a K-016 edit request).
2. de chips lowercase (`/^\p{Ll}/u`); `houseOrder` is a permutation of `['odd','even']`.
3. `rule` <= 214 chars, contains each digit 0-9 exactly once, contains both chips as whole words (`(?<!\p{L})chip(?!\p{L})`, case-insensitive, NFD), no `{`, ends in a mark; its rendered width at Nunito 800 15 (14 for fi) <= 2 x 639 (the render gate measures the line count).
4. `leftover` and `pairs` non-empty, <= 16 chars, distinct, no digit, no `{`, neither equals a chip; rendered widths at Nunito 800 14 <= 84 / <= 56 (measured by the validator's puppeteer step with the shell woff2, m: max 77.5 / 48.2).
5. `placeHeads` = exactly 2 entries, each 1-2 letters (`/^\p{L}{1,2}$/u`), distinct; lock-given locales must match §1 (de Z E · fr d u · es D U · nl T E · sv T E · fi K Y).
6. No authored string (chips, rule, captions, placeHeads, titles, instructions) matches `banRegex(loc)`; no string contains a neighbour head (nl `Tienraam`, sv `siffror` / `vecka`, de `Hundertertafel`, and every locale's `number-charts` / `counting-frames` name, m); en titles never contain "Even and Odd"; it titles never equal or start with "Pari e dispari"; pt titles never start "Pares e ímpares".
7. Titles <= 70 chars, no worksheet-word (`seo.words.worksheet` of the locale), no free-claim, unique within the band against the locale's existing strings; the base title contains the head; F3's title contains the locale's 100 idiom (a digit string "100" or fi "sataan"); instructions <= 150 ending in a mark; fi question titles contain " vai " and never " tai " outside the rule; no instruction names an absent apparatus (a lint list per face: base never "dots" / "pictures"; F1 never "house"; F3 / F4 never "circle the word" (the child TICKS a circle); F5 never "friend"; F2 never "pairs").
8. Every face title differs from every other by at least one NOUN or verb token (never only an adjective).
9. `refuse` keys name a real mode; `strings` ids === {base + the 5 modes} minus the refused.
10. `nameMaxGraphemes` leaves >= 2 names in `SENTENCES[loc].names` (m: it 5, every other locale >= 7).
11. Build probe (the b3 gate's shape): every face builds at d2 for the locale with 3 seeds; F2 / F5 find >= 6 countable nouns in every K-320 fan theme; the base DOM contains no `data-lcs-prim="ten-frame"` and no `<img>`; every stamped `data-lcs-answer` is `''`.
12. The draft's EN strings equal the module's EN block (the EN is the source; a drift is a defect); slugs: `axes['exercise-type']['odd-and-even'].slug.<loc>` equals §1 table B and differs from every `axes.*.*.slug.<loc>` and every landing slug of the locale.

**`qa/verify-b4-odd-and-even.js` (render gate; own ground truth; renders base x d1 / d2 / d3 + 5 faces x 11 locales x d2 + a 5-seed sweep per face through `render/render-instance.js` with the shell woff2 from `file://`, under the 722 stack (3-line title + 150-char instruction) AND the 4-line fi title (677)):** `verify()` empty; `qa/lints.js` clean (overflow `:37-45`, footer `:50-66`, font `:86`, palette); the floors ITSELF: base chips >= 64 with numerals >= 26 px, house boxes >= 60 high, cue dots r >= 4.5 with the single distinguishable (fill none + coral stroke); F1 dots r 8 with pitch 24 and the panel containing no text node, boxes >= 40 high, the equation row <= 302; F2 / F5 pictures >= 44 with `naturalWidth > 0`, every lane <= 106 + slack and its inner width <= 639, names <= 64, captions <= 84 / 56, pills >= 44 high and <= 102 wide; F3 boxes >= 48, ones box background === coralSoft and tens === white, the rule strip <= 2 lines, headers once per column; F4 no text node anywhere equals `a + b` for any row, the table exactly 3 rows of chip words; NO `data-lcs-prim="ten-frame"` on any face; no `÷` / `:` between digits; no `banRegex(loc)` hit in the rendered text; `0` absent from every text node on every page; the equal-parity split; the chip words rendered === the bank's; answer hiding: no `.ws-blankbox` with a non-empty text node, no stamped `data-lcs-answer` other than `''`, no `data-lcs-correct` on a chip that carries a numeral, no parity attribute on any `[data-lcs-val]`. 20-seed sweep: base chip orders vary and are never ascending; F1 / F2 / F5 count sets vary; F3 ones digits distinct every seed; F4 all four cases every seed.

**Poison (each must FAIL for its OWN rule; the correct EN draft is the control):** P1 de `chips.even = 'Gerade'` -> rule 2 · P2 `chips.even = 'odd'` -> rule 1 · P3 sv `chips.even = 'jämna'` -> rule 1 (differs from K-016's "jämnt") · P4 `rule` with "0, 2, 4, 6 or 8" only -> rule 3 (digits 1-9 missing) · P5 `leftover = 'what is left over now'` (21 ch) -> rule 4 · P6 `placeHeads = ['Tens','Ones']` -> rule 5; de `['T','O']` -> rule 5 · P7 de F2 title "Gerecht halbieren: gerade oder ungerade?" -> rule 6 (`halbieren`); MUST-PASS control: de "Gerade Zahlen bis 20" and sv "jämnt" and da "dele ligeligt" pass rule 6 · P8 en base title "Even and Odd Numbers" -> rule 6 · P9 fi F5 title "Parillinen tai pariton?" -> rule 7 · P10 F3 title "Odd and Even Numbers: Look at the Ones Box" (no "100") -> rule 7 · P11 F1 title "Odd and Even Numbers: Big Pair Proof" vs F5 "Odd and Even Numbers: Bigger Pair Proof" -> rule 8 (adjective only) · P12 sv instruction containing "vecka" -> rule 6 · P13 it base title "Pari e dispari" -> rule 6 · P14 `strings` missing `sums` with no `refuse.sums` -> rule 9 · P15 base instruction "Circle the dots two by two" -> rule 7 (absent apparatus) · P16 `nameMaxGraphemes: 3` on it -> rule 10. **Render poisons:** PR1 a pre-filled `.ws-blankbox` -> answer hiding · PR2 a swapped tick stamp (`data-lcs-correct` on the wrong circle) -> `verify()` · PR3 a `tenFrame` injected into the base -> the ten-frame assertion · PR4 a sum printed in an F4 row -> the sum scan · PR5 a `pairDots` under an open chip -> the ancestor assertion · PR6 a house with 4 boxes and 4 open chips -> the leak assertion · PR7 the old default `.ws-lane` padding on F5 (6 x 118 + 30 = 738) under the fi 4-line chrome -> the footer lint · PR8 a picture from `animals bw` on F2 -> the marker assertion · PR9 a `0` chip at d3 -> the zero assertion · PR10 fi pills at `padding:0 20px` (113 > 102) -> the pill width assertion.

**Page reads:** `ODD_AND_EVEN[loc]` (`bank('odd-and-even', loc)`), `SENTENCES[loc].names`, `entriesFor` + `countable` + `fileUri`, `vocab()` for the countable test only; never `image-vocabulary.js` for any printed word, never `pictureFor`, never `approved-words-*.json`, never `calendar.js`, never K-016 or `labels.js` at render (both are validate-time sources).

## 6 SEO plan

| face | title pattern (Germanic / Romance / Nordic + fi) | meta MIDDLE (whole 120-170; the child's instruction, en) | coordinate |
|---|---|---|---|
| base | Odd and Even Numbers: Odd House, Even House · Gerade und ungerade Zahlen bis 20 · Even en oneven getallen tot 20 / Números pares e impares hasta el 20 · Números pares e ímpares até 20 · Nombres pairs et impairs jusqu'à 20 · Numeri pari e dispari fino a 20 / Udda och jämna tal till 20 · Lige og ulige tal til 20 · Partall og oddetall til 20 · Parilliset ja parittomat luvut 20:een asti | Write each number in the house where it belongs, the even house or the odd house. Three numbers have moved in already and show their pairs | `{type:'odd-and-even', mode:'base', theme:'', level:<G1 key>}` |
| F1 | Odd or Even? Write Two Equal Addends · Gerade oder ungerade? Zwei gleiche Summanden · Even of oneven? Twee gelijke delen / ¿Par o impar? Dos sumandos iguales · Par ou ímpar? Duas parcelas iguais · Pair ou impair ? Deux nombres égaux · Pari o dispari? Due addendi uguali / Udda eller jämnt? Två lika delar · Lige eller ulige? To lige store dele · Partall eller oddetall? To like deler · Parillinen vai pariton? Kaksi yhtä suurta lukua | Circle the dots two by two. Then write the number as two equal parts and write what is left over in the small box | `mode:'proof'`, `theme:''`, G2 |
| F2 | Can Two Friends Share Fairly? · Gerecht teilen: gerade oder ungerade? · Eerlijk delen: even of oneven? / ¿Se puede repartir entre dos? Par o impar · Dá para dividir entre dois? Par ou ímpar · Partage équitable : pair ou impair ? · Si può dividere in due? Pari o dispari / Kan det delas lika i två? · Kan det deles ligeligt? · Kan det deles likt på to? · Voiko jakaa tasan kahdelle? | Share the pictures between the two friends, one each in turn. Write how many each friend gets and how many are left over, then circle odd or even | `mode:'share'`, theme = the wave's key, G1 |
| F3 | Odd and Even Numbers 1 to 100: Look at the Ones Box · Gerade und ungerade Zahlen bis 100 · Even en oneven getallen tot 100 / Números pares e impares del 1 al 100 · Números pares e ímpares de 1 a 100 · Nombres pairs et impairs jusqu'à 100 · Numeri pari e dispari fino a 100 / Udda och jämna tal till 100 · Lige og ulige tal til 100 · Partall og oddetall til 100 · Parilliset ja parittomat luvut sataan asti | Read the rule at the top. Look only at the ones box of each number and tick the circle under even or odd | `mode:'ones'`, `theme:''`, G2 |
| F4 | Odd or Even Sum? Decide Without Adding · Summe gerade oder ungerade? Ohne zu rechnen · Even of oneven som? Zonder te rekenen / ¿Suma par o impar? Sin sumar · Soma par ou ímpar? Sem somar · Somme paire ou impaire ? Sans calculer · Somma pari o dispari? Senza calcolare / Udda eller jämn summa? Utan att räkna · Lige eller ulige sum? Uden at regne · Partall eller oddetall sum? Uten å regne · Parillinen vai pariton summa? Laskematta | Do not add. Look at the underlined ones digit of each number, use the table, and tick whether the sum is even or odd | `mode:'sums'`, `theme:''`, G3 |
| F5 | Odd or Even? Count the Pictures in Pairs · Gerade oder ungerade? Bilder paarweise zählen · Even of oneven? Tel de plaatjes in paren / ¿Par o impar? Cuenta los dibujos en parejas · Par ou ímpar? Conte as figuras aos pares · Pair ou impair ? Compte les images par deux · Pari o dispari? Conta le figure a coppie / Udda eller jämnt? Räkna bilderna parvis · Lige eller ulige? Tæl billederne parvis · Partall eller oddetall? Tell bildene parvis · Parillinen vai pariton? Laske kuvat pareittain | Count the pictures and circle them two by two. Write how many pairs you made, then circle odd or even | `mode:'count'`, theme = the wave's key, G1 |

All titles are candidates the native panels rewrite (<= 70, no worksheet-word, base = the head, each face adds one NOUN / verb / range, never an adjective alone; the de base and F3 carry the range because the range strings ARE the de winners). `coordinate.mode` is ALWAYS the face's mode string (base `'base'`); `coordinate.theme:''` on the four numeral faces, the wave's theme key on F2 and F5; level = the G1 key for base / F2 / F5, the G2 key for F1 / F3, the G3 key for F4 (`LEVEL_KEYS`, m; a locale publishing the base at its curriculum year swaps the key in its landing file only); `canonicalDeckSlug` = the published d2 deck. h1 = title; eyebrow = level label; strand chip = the §1 table-B literal (framework NAME only; NOT a `strand-names.ts` row for the ten non-EN locales, critic OPEN 1). JSON-LD `LearningResource`; `educationalAlignment` on en only and only where honest (`targetName` `2.OA.C.3` on F1 / F3, `3.OA.D.9` on F4; none on base / F2 / F5), no `targetUrl`; every non-EN landing names the framework of §1. Meta lead inherits `seo.words.free_printable` (metadata only; no visible copy claims "free"); the MIDDLE is the instruction, <= 150 chars in every locale so the 30-40 % longer de / fi / pt instructions still close the 120-170 window (`tools/measure-instruction-window.js`, exists, m, picks the longest fitting candidate). `topicMeta['odd-and-even']` >= 50 x11 + `skill-sentences.<loc>.json['odd-and-even']` (full 60-180, short 15-90) via the `register-b4-en-content.js` clone; the skill sentence never mentions doubles, a chart, or a ten-frame. Printable-only decks ship NO answer key: no title, meta or landing promises one ("with answers" is a known loss, README open item 6).

**Non-cannibalisation** (whole-landing 3-gram Jaccard, `scripts/seo-landing/gate.js` FAIL >= 0.80 / WARN 0.65; *est.*, the gate measures):

| pair | what the copy says differently | est. |
|---|---|---|
| base vs F3 | houses, 1-20, three worked pairs vs place-value boxes, 10-99, a rule strip; the shared head is the only shared cluster | 0.30 |
| base vs F1 · base vs F5 | sort numerals into houses vs ring dots and write an equation · vs count pictures and write the pairs | 0.25 · 0.22 |
| F1 vs F5 | numeral given, dots, an equation vs an unknown count of pictures and a written PAIRS count | 0.35 |
| F2 vs F5 (the closest pair) | two named friends, "each gets", dealing vs counting and pairs; F2 copy never says "count" / "pairs", F5 copy never says "friend" / "share" (a lint on the landing bodies) | 0.40 |
| F4 vs F3 | two addends and a table, grade 3 vs one number and a highlighted box, grade 2 | 0.30 |
| base vs K-016 (deck page only: NO landing exists, m) | written numerals sorted into houses vs pre-paired pictures and a chip; the base never says "pair them up" | 0.15 |
| F1 vs the `doubles-halves` landings (G1-247 / G1-288, `mode:null`, themed, m) | a GIVEN number split into two EQUAL addends plus a leftover vs doubling a group / halving an even group with a cut line; no doubles token can appear (§5 rule 6) | 0.15 |
| F2 vs G1-288 halves · F2 vs G3-377 division-with-remainder | odd counts, a leftover, named plates vs even groups cut in half · share between exactly two, no notation, grade 1 vs rings of d with q and r, grade 3 | 0.20 · 0.10 |
| F3 vs G2-218 (via the `number-charts` G2 hub landing) · F3 vs `base-ten` hubs (de "Stellenwert", sv "Tiotal och ental") | boxes and a highlighted digit, tick every number vs a loose grid, circle the odds · a parity scaffold, no blocks, no expanded form | 0.15 · 0.15 |
| F4 vs `mental-math` / `column-arithmetic` hubs | never a sum written; the rule table; grade 3 | 0.10 |
| any face vs `counting-frames` (nl "Tienraam") · vs G1-130 `number-charts` colour path | no ten-frame drawn or named · no chart, no colouring | 0.05 · 0.05 |

Boundary sentence on every landing: "The child works out WHY a number is odd or even: pairs, two equal parts, the ones digit, the parity of a sum. Circling pre-paired pictures is on the Odd or Even? page; hunting odd numbers in a grid is on the Odd Number Hunt page; doubles and halves have their own pages." On F2: "the pictures are shared between two children, never divided with a division sign (Division with Remainders)." On F5: "the pictures start unpaired and the child makes the pairs (not Odd or Even? Pair the Pictures)."

## 7 Hub visibility contract

A face appears under `odd-and-even` on `/[locale]/worksheets` IFF all four hold (brief): (1) `apps['odd-and-even']` exists in `frontend/config/topics-taxonomy.json` (ABSENT today, m; missing = rendered NOWHERE in the rail) with `default_subject:'math'` and `exercise_type_axis_key:'odd-and-even'`; (2) `axes['exercise-type']['odd-and-even']` has `slug` + `name` in all 11 locales (§1 table B; 0 collisions against every axis slug and every landing slug, m); (3) exactly one landing per face per locale with `coordinate.type === 'odd-and-even'` verbatim, `mode` per §6 (a non-empty face string; `verify-hub-type-rows.js:192` refuses null, m), `theme:''` on the four numeral faces and the wave's theme key on F2 / F5, the level key of §1 (G1 for base F2 F5, G2 for F1 F3, G3 for F4; every key present in `LEVEL_KEYS`, m), a unique slug and `canonicalDeckSlug` = the published deck; a REFUSED face has NO landing and the expectation is lowered explicitly, never padded; (4) the landing JSON committed AND deployed (per-process cache).

Gate: `node scripts/verify-hub-type-rows.js --keys=odd-and-even` (script EXISTS since nt20-C, m; expectations from `hub-expectations.json` exported by `tools/export-hub-expectations.js`, exists, m; poison = drop one landing / drop `apps['odd-and-even']` / a wrong `coordinate.type` / a `mode:null` landing must FAIL).

**Expected rows per locale:** en 6 · de 6 · es 6 · pt 6 · fr 6 · it 6 · nl 6 · sv 6 · da 6 · no 6 · fi 6 = **66** (the README matrix's "odd-and-even 6 x 11 = 66 (to be confirmed)" is CONFIRMED). No refusal is measured today: the two chips exist in all 11 locales in K-016 (m); the rule, the two captions and the two letters are panel literals with measured reserves (the widest candidates 77.5 / 48.2 px and the fi two-line rule all fit); every pill is <= 102 px (m); >= 5 names of <= 6 graphemes survive in every locale (m); the countable picture pool is 608-751 keys catalogue-wide and every fan theme clears 6 (m); every stack sits under 677 (base 538 · F1 658 · F2 666 · F3 442 · F4 416 · F5 666). Contingencies, each recorded in the draft before the wave, none expected to fire: a locale whose panel rule sentence exceeds 2 lines at 14 px (a rewrite, never a row loss); a locale whose F2 / F5 pinned theme holds < 6 countable nouns in that locale (the wave re-pins; est. 0 across the fan set); a panel that cannot keep an F5 fi title to 4 lines (all faces are measured under 677, so even a 4-line title costs no row).
