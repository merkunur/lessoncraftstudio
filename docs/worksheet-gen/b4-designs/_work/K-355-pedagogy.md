# K-355 `five-senses` : PEDAGOGY + CONTENT (nt10-D, 2026-09-21)

Read: `_ROLE-PEDAGOGY.md`, `_STUDIO-BRIEF.md`, `_SUBSTRATE.md`, `_PANEL-FINDINGS.md` (lock + entry 3 + Facts), `_work/K-354-pedagogy.md` (the human-body sibling, for the Boundary handshake), `types/_shared/science-pair-match.js` + `science-category-sort.js` + `odd-one-out.js` (build + verify), `types/k/K-211-hot-cold.js`, `K-213-helper-tool.js`, `data/science/hot-vs-cold.json` + `helper-tool.json` + `food-groups.json` + `summer-vs-winter-clothes.json`, `lib/b3-picture-index.js`, `templates/components-b2.js` (`rulingBlock`, `wordBank`, `pillChoice`, `colorLegend`), `tools/gate-variation-distinct.js`, `qa/verify-b3-seasons.js`, `frontend/content/seo-landing/<loc>.json` (level keys), `frontend/config/topics-taxonomy.json`. Every number below was measured 2026-09-21 with read-only node (scratch prefix `K-355-`); *est.* = the engineer measures in the real render. **All 57 candidate pictures were OPENED** (verdicts in D). No em-dashes.

**Boundary (load-bearing).** K-211 `hot-and-cold` (science-sort, 14 items incl. `weather/sun`, `summer/campfire`, `winter/ice`, `winter/icicle`, `weather/snowflake`, `desserts and sweets/popsicle`) owns temperature; G1-207 `food-groups` (24 items incl. `At the Supermarket/cheese`, `fruits/strawberry`, `bakery/toast`) owns food classification; K-344 `all-about-me-label-the-face` (LIVE) copies eye / ear / nose / mouth / hair onto lanes pointing at a face picture and K-354 `human-body` (this batch) labels the drawn whole-body figure with body-part NOUNS and, by its own Boundary, "no face names a sense"; K-225 owns word-to-picture lines; K-043 / K-044 / K-039 (`odd-one-out.js`) own odd-one-out on IDENTICAL icons of one theme (N of noun A + 1 of noun B); K-235 owns category sort by theme; the 21 `data/science/*.json` sorts are 2-4 WORD-labelled bins; K-213 owns helper-to-tool pairs. Therefore this family owns exactly one thing nobody has: **object to PRIMARY SENSE, anchored on the five fixed organ pictures `body parts/eye ear nose tongue hand`** (the sense is a property of the OBJECT, the organ is the constant answer key `ORGAN_OF = {hear:ear, see:eye, smell:nose, taste:tongue, touch:hand}`). Structural fences, enforced by the validator and not by taste: no item noun may be in `hot-vs-cold.json` (so sun, campfire, ice, icicle, snowflake, popsicle can NEVER be sense items: they are K-211's), no taste item may be in `food-groups.json`, no item may be a K-207 clothing noun, every printed word on the type is either a sense VERB or an F5 sentence starter (never a body-part noun, never an object noun: the objects are pictures only), and the organ pictures appear ONLY as sense anchors (no face copies "eye / ear / nose" as words, which is K-344 / K-354 territory). Odd-one-out here is BY CONCEPT across themes (four DIFFERENT pictures, three sharing a sense), never the K-043 identical-icon shape.

## A. Identity

| loc | genre head (the panels; title = head verbatim; slug measured collision-free against every `axes.*` slug) | school year (brief band; base K, F1-F5 G1) | national strand literal (framework NAME; Facts 1: NO Science row exists in `strand-names.ts`, so this column IS the strand source) | CCSS (en only, honest) |
|---|---|---|---|---|
| en | **Five Senses** (slug `five-senses`; "5 senses" numeral form in the META only; unique 113, 14/14 seeds, Tier A; head HARD (Education.com / Twinkl / TPT); WINNABLE "five senses sort worksheet", "which sense worksheet kindergarten") | kindergarten (F1-F5 grade 1) | Science (readiness) | none: readiness class; NGSS 1-LS1-1 is animal structures and 4-LS1-2 is Grade 4, neither is claimed; no `educationalAlignment` on any face |
| de | **Die fünf Sinne** (slug `die-fuenf-sinne`; article kept, "fünf" lowercase; unique 57, 13/14, Tier A; **F4 title head = "Sinnesorgane"** (the Klasse 2-3 word, the organ-label face only); both lines are July §1 gaps) | Vorschule (F1-F5 1. Klasse; F4 copy may say "Klasse 1-2") | Sachunterricht | n/a (Lehrplan "Die fünf Sinne / Sinnesorgane") |
| es | **Los cinco sentidos** (slug `los-cinco-sentidos`; SEP textbook form; "5 sentidos" in meta, 60 vs 55 a tie; unique 127, band 54, Tier A; WINNABLE "fichas de los 5 sentidos para imprimir preescolar / primer grado") | preescolar (F1-F5 primer grado) | Conocimiento del Medio (the panel literal; SEP preescolar campo "Exploración y comprensión del mundo natural y social" is the K alternative, OPEN 1) | n/a (SEP / NEM) |
| pt | **Os cinco sentidos** (slug `os-cinco-sentidos`; BNCC form; "5 sentidos" leads 38 vs 34, meta only; unique 72, band 30, Tier A; "atividade os cinco sentidos 1 ano para imprimir") | educação infantil (F1-F5 1º ano) | Ciências (BNCC, no descriptor code for the senses at 1º/2º) | n/a |
| fr | **Les 5 sens** (slug `les-5-sens`; the digit form leads 43 vs 30 so the TITLE keeps the digit, "cinq" in the meta; unique 79, band 55, Tier A; Lutin Bazar owns the head; WINNABLE "fiche les 5 sens gs / cp", "la vue maternelle") | maternelle GS (F1-F5 CP) | Questionner le monde (CP; GS = "Explorer le monde", OPEN 1) | n/a |
| it | **I cinque sensi** (slug `i-cinque-sensi`; unique 82, band 37, Tier A; "THE classe prima science unit"; WINNABLE "verifica 5 sensi classe prima", "descrivere con i 5 sensi classe seconda" = F5 verbatim, measured x3) | infanzia (F1-F5 classe prima; F5 copy may say "classe seconda") | Scienze | n/a (Indicazioni nazionali) |
| nl | **De vijf zintuigen** (slug `de-vijf-zintuigen`; secondary "Zintuigen"; never the numeral in a nl title; unique 41 net ~30, Tier A-; "werkblad zintuigen groep 3") | kleuters groep 1-2 (F1-F5 groep 3) | Oriëntatie op jezelf en de wereld | n/a (SLO kerndoelen) |
| sv | **Våra sinnen** (slug `vara-sinnen`; "de fem sinnena" in copy; round 1 19 / round 2 19, Tier A; UR "Våra sinnen" is a ranking neighbour, not a collision; förskola + åk 1-3 + arbetsblad all fire) | förskoleklass (F1-F5 åk 1) | Naturorienterande ämnen (Lgr22 NO åk 1-3 "upplevelser av ljus, ljud, temperatur, smak och doft med hjälp av olika sinnen") | n/a |
| da | **Sanserne** (slug `sanserne`; "De fem sanser" in copy; 18 / 15, Tier A-; "sanserne opgaver", "sanserne indskoling") | børnehaveklasse (F1-F5 1. klasse) | Natur/teknologi (Fælles Mål) | n/a |
| no | **Sansene våre** (slug `sansene-vare`; "de fem sansene" in copy; 15 / 14, Tier B+; "sansene 1 trinn"; no "oppgaver" on this head) | 1. trinn (F1-F5 2. trinn) | Naturfag (LK20) | n/a |
| fi | **Viisi aistia** (slug `viisi-aistia`; partitive after the numeral; 15 / 11, Tier B; "aistit tehtäviä"; NEVER the bare plural "aistit" as a title or seed: polluted by "aistitieto") | esikoulu (F1-F5 1. luokka) | Ympäristöoppi (OPS 2014) | n/a |

Measured: `apps['five-senses']` and `axes['exercise-type']['five-senses']` are ABSENT (register with the `tools/register-b3-taxonomy.js` shape, `default_subject:'science'`, `default_age_range:'5-7'`); the `science` disc today = science-sort, science-sequence, science-match, seasons, animal-fact-file (+ human-body from this batch); none of the 11 slugs above nor `senses` / `sinne` / `sentidos` / `sens` / `sensi` / `zintuigen` / `aistit` collides with any `axes.*` slug (grep over every axis, 0 hits). Neighbour heads NOT to echo: `science-sort` (de "Sachunterricht-Sortieren", fr "Tri en sciences", it "Classifichiamo le scienze", sv "Sortera i naturen", ...) and nl `odd-one-out` "Welke Hoort Er Niet Thuis" (F3's nl title must add "zintuig").

**Theme axis: THEMELESS** (`themeAxis:{applicable:false}`, curated cross-theme `(theme, noun)` refs exactly like the 21 science banks; landings carry `coordinate.theme:''`). The organ anchors are the 5 fixed `body parts` pictures (34/34 nouns with vocab x11; the 5 organs present x11, all 5 opened: eye = a single brown eye with lashes; ear = a right ear; nose = a red cartoon nose, front view, two nostrils; tongue = red lips + teeth + a tongue stuck out (reads "tongue" but ALSO "mouth": see OPEN 3); hand = an open palm, five fingers). **No `unitAxis`.** Object pools measured with `entriesFor(theme, loc)` x11 AND `hasPicture(vocabKey, loc)` x11 for every candidate (every kept item = 11/11 on both; `B2_EXCLUDE` hits 0 of them; all 1536 px sources).

**The one rule that locks the type:** every item carries ONE primary sense fixed in `data/science/five-senses.json` and signed by the panel; the answer on every verified face is `ORGAN_OF[item.sense]`, a constant map, never a word; the only words the code ever prints are the locale's 5 sense VERBS (`verbs[loc][sense]`, whole literals) and the 5 F5 starters (whole literals), so nothing inflects, no object noun is printed (no `Fløyte`-is-both-whistle-and-flute, no `Sieni`-is-both-sponge-and-mushroom, no `Kallio`-is-bedrock trap can reach a page), and no body-part NOUN is ever printed (the K-344 / K-354 fence holds by construction).

## B. The six faces

Common: `templates/components-b4/five-senses.js` (organ chip, chip strip, sense card, odd row, organ lane, starter block) + the bank in D. The base reuses `makeSciencePairMatch` through ONE additive cfg hook (`cfg.samplePairs(rng, n)`, default = the factory's own `rng.sample(allPairs, n)`, so K-213 and every other science-match stays byte-identical); F1 reuses `makeScienceCategorySort` with `maxBins:5` and ONE additive cfg flag (`binIcons:true` adds a 44 px organ picture inside the label pill; default off, the 21 live banks unchanged); F2-F5 are CODE faces on one additive `layout` knob (`d.layout` undefined = the base path; a face stamps `data-lcs-layout` only when declared). Guards key on the CONFIG (`d.perBin`, `d.maxPerSense`, `d.pool`, `d.chipOrder`, `d.bank`, `d.rowsPerSense`), never on the level index. Page rule on EVERY face (config `maxPerSense`, default 2): no page carries more than 2 items of one sense (the lock's "≤2 smell items per page", generalised, because touch also sits at 5 strong items), and no page carries two items of one visual family (`families` in D). Face ids are `TBD by the emitter` in the band stated.

### Base: Five Senses: Match the Picture to the Sense (K, `K-355`, layout undefined)
**Move:** CLASSIFY by sense: five objects on the left, the five organ pictures on the right, one line per object to the organ that senses it (one object per sense, so every organ is used exactly once; a K exclusion strategy that stays honest because each object is unambiguous).
**Child:** "Draw a line from each picture to the body part you use for it: eye, ear, nose, tongue or hand." (91 chars; apparatus on the page: pictures, the five organs.)
**Params:** d1 `{pairs:4, pool:'strong', maxPerSense:1}` (4 of the 5 senses, `rng.sample` of senses) · **d2 (ships)** `{pairs:5, pool:'strong', maxPerSense:1}` (all five senses) · d3 `{pairs:5, pool:'strong+signed', maxPerSense:1}`. `samplePairs(rng, n)`: sample n senses, for each ONE item from `items` with `sense === s` and `confidence ∈ pool`, then apply the family fence; pair = `{left:{theme, noun}, right:{theme:'body parts', noun: ORGAN_OF[s]}}`; the factory derangement (no organ straight across from its object) and its `.ws-match` layout are untouched: at n = 5 `itemH = floor((720 - 56) / 5) = 132`, `iconPx = min(92, 106) = 92` (>= the K floor 56), the whole stack 5 x 132 + 4 x 14 = 716 <= 722 (the factory's own numbers).
**verify():** the factory's (column lengths equal, right column a permutation of left, no row straight across) PLUS the gate's node cross-check: for every `[data-sci-left=<noun>]` the bank sense of that noun resolves to an organ whose `fileUri('body parts', organ)` equals the `src` of the `[data-sci-right=<same noun>]` picture; every right picture is one of the 5 organ files; 5 distinct senses on the page at d2; no item from `signed` at d1/d2.
**Refusals:** none (pictures are global, no text beyond title + instruction).
**Query face:** the bare head + "match" / "sort by sense": en "five senses matching worksheet" · de "die fünf Sinne zuordnen" · es "los cinco sentidos une" · it "i cinque sensi collega" · sv "våra sinnen para ihop".

### F1: Sort by Sense (G1, G1-3xx TBD, PARAM+CODE: `makeScienceCategorySort` `{maxBins:5, binIcons:true}`)
**Move:** SORT ten different pictures into five sense bins (two per sense), the bins labelled with the sense VERB and the organ picture; differs from the base because nothing is one-to-one any more: the child must hold five categories and place ten members.
**Child:** "Look at each picture. Draw a line from its dot to the box of the sense you use for it." (85)
**Params:** d1 `{layout:'sort', perBin:1, maxPerSense:1, pool:'strong'}` (5 items) · **d2** `{layout:'sort', perBin:2, maxPerSense:2, pool:'strong'}` (10 items; `itemPx = min(78, floor(648/10) - 12) = 52` >= the G1 floor 44; 5 `.sci-bin` at `flex:1 1 0` in 675 with 4 x 18 gaps = ~120 px wide each, 185 high; label pill 17 px Baloo + a 44 px organ picture: the longest verb pill is it "ascoltare" ~ 80 + 32 padding + 44 = ~156 px > 120, so the pill overhangs its bin as the seasons pills already do at 128; *est.*, the design agent sets the pill to two lines or drops the icon to 36) · d3 `{layout:'sort', perBin:3, maxPerSense:3, pool:'strong+signed'}` (15 items at 40 px: BELOW the G1 floor 44, so d3 is legal only at `perBin:3` with `itemPx` re-measured; unpublished, say so). The factory samples `perBin` per bin then shuffles the strip: the family fence is applied inside `cfg.sampleItems` (additive hook, same shape as `samplePairs`); with the fence the smell bin at d2 = (one of the 5 flowers) + (garlic | onion) or (garlic + onion), the touch bin any 2 of 5.
**verify():** the factory's (>= 2 bins, every item's bin exists, every bin gets >= 1) PLUS the gate: bins === the 5 senses in a shuffled order that is NOT the canonical `hear see smell taste touch`, exactly `perBin` items per bin, every item picture is a bank item whose `sense === data-sci-item`, the bin label text === `verbs[loc][sense]`, the bin icon `src` === `fileUri('body parts', ORGAN_OF[sense])`, no picture repeated, family fence held, `maxPerSense` held.
**Refusals:** none anticipated (pools: hear 10 strong, see 6, taste 8, smell 7 with the family fence (3 co-occurrable), touch 5). The fence makes the smell bin's d3 (`perBin:3`) exactly one combination (flower-family + garlic + onion): legal, but the sweep must not demand variety there.
**Query face:** "five senses sort" (en WINNABLE verbatim) · de "die fünf Sinne sortieren" · nl "zintuigen sorteren groep 3" · sv "sortera efter sinne" · fi "lajittele aistin mukaan".

### F2: Which Sense? (G1, G1-3xx TBD, CODE `layout:'which'`)
**Move:** DECIDE per item: one picture, five small organ chips under it, circle the ONE organ you use (the "which sense" query verbatim); 8 independent decisions with the full choice set every time, never "circle all that apply" (lock).
**Child:** "Which sense do you use for each picture? Circle the eye, ear, nose, tongue or hand." (83)
**Params:** d1 `{layout:'which', items:6, cols:2, rows:3, chipPx:48, pic:80, chipOrder:'fixed', pool:'strong', maxPerSense:2}` · **d2** `{layout:'which', items:8, cols:2, rows:4, chipPx:44, pic:72, chipOrder:'fixed', pool:'strong', maxPerSense:2}` · d3 `{layout:'which', items:8, cols:2, rows:4, chipPx:44, pic:64, chipOrder:'shuffled', pool:'strong+signed', maxPerSense:2}`. Card = `cardGrid({cols:2, rows:4, numbered:true})`; card width (675 - 14) / 2 = 330 (*est.*); inside: the picture (72) + a chip strip of 5 organ pictures at 44 px with 8 px gaps = 252 wide (fits); row height = (722 - 3 x 12) / 4 = 171 (*est.*): picture 72 + strip 44 + badge + paddings ~ 150, fits. `chipOrder:'fixed'` = the canonical strip `eye ear nose tongue hand` on every card (reads as one key across the page; the answer position differs per item so nothing leaks); `'shuffled'` at d3 per card.
**verify():** stamps `data-lcs-sense=<sense>` on the card, `data-lcs-chip=<organ>` on each chip, `data-lcs-chip-order=fixed|shuffled` on the grid. Re-derived: exactly 5 chips per card, the 5 organ files each once, exactly one chip with `organ === ORGAN_OF[sense]`, chips equal size >= `chipPx` (the G1 tap floor 44), no chip pre-ringed, the picture is a bank item of that sense and `confidence ∈ pool`, `maxPerSense` and the family fence held, the answer organ is not the same chip index on more than `ceil(items / 5) + 1` cards (position-leak guard), at `chipOrder:'fixed'` every strip is the canonical order.
**Refusals:** none. (Locale-independent: no text on the cards.)
**Query face:** "which sense worksheet kindergarten / grade 1" (en WINNABLE verbatim) · de "welcher Sinn?" · es "¿qué sentido usas?" · fr "quel sens ?" · it "quale senso?" · nl "welk zintuig?" · fi "millä aistilla?".

### F3: Which One Does Not Belong? (G1, G1-3xx TBD, CODE `layout:'odd'`; a locale may publish it at G2 via its `level` key, OPEN 4)
**Move:** REASON about a group: four different pictures in a row, three share a sense, one does not; cross out the intruder. The child must first find the shared sense (unstated) and then exclude; this is the K-043 verb on a CONCEPT instead of an identical icon.
**Child:** "In each row, three pictures go with the same sense. Cross out the one that does not." (84)
**Params:** d1 `{layout:'odd', rows:4, items:4, pic:76, majority:['hear','taste','see'], pool:'strong'}` · **d2** `{layout:'odd', rows:5, items:4, pic:72, majority:['hear','taste','see'], pool:'strong', maxPerSense:2}` · d3 `{layout:'odd', rows:5, items:5, pic:64, majority:['hear','taste','see'], pool:'strong+signed'}`. `majority` is a CONFIG list: only senses with >= 8 strong items may be the shared sense of a row (hear 10, taste 8, see 6: see is admitted at d2 only because 3 of 6 per row x at most 2 see-rows... NO: `maxPerSense` counts ITEMS, and a majority row uses 3 of one sense, so the row rule overrides the page rule for the majority sense: config `maxPerSense` applies to the ODD items only; state it in the gate). Smell and touch are NEVER the majority (5-7 strong, and the family fence leaves smell exactly one triple), but either may be the odd item. Rows = `cardGrid({cols:1, rows})` as `odd-one-out.js` does; at 4 items `px = min(76, floor((600 - 42) / 4)) = 76` (the factory's own formula), >= the G1 floor 44.
**verify():** stamps `data-lcs-row-sense=<majority>` on the row, `data-lcs-odd="1"` on the intruder, `data-lcs-item-sense` on every picture. Re-derived: exactly one odd per row, its sense !== the row sense, the other three all === the row sense, all four pictures distinct (and distinct across the page), row sense ∈ `majority`, the odd's column index is not the same in every row (and never the same in > `ceil(rows / 2)` rows), family fence held (a flower-family item never twice on the page), the odd item's sense over the page obeys `maxPerSense`, no picture crossed out or greyed at render.
**Refusals:** none. nl title must add "zintuig" (the nl `odd-one-out` family head is "Welke Hoort Er Niet Thuis"; validator rule 9).
**Query face:** "five senses odd one out" · de "Was passt nicht? Die fünf Sinne" · nl "welk zintuig hoort er niet bij" · sv "vad passar inte in? sinnen" · fi "mikä ei kuulu joukkoon? aistit".

### F4: Sense Organs: Label with the Sense Word (G1, G1-3xx TBD, CODE `layout:'label'`)
**Move:** NAME the sense of each organ: the five organ pictures each with a writing lane; a 5-word bank of the sense VERBS; copy the right verb under the right organ (de "Sinnesorgane beschriften", es "y sus partes / órganos"). Differs from K-344 / K-354 because the word is a VERB of what the organ DOES (hear / see / smell / taste / touch), never the organ's name.
**Child:** "Copy each sense word from the bank onto the line under the body part that does it." (83)
**Params:** d1 `{layout:'label', organs:5, bank:true, starter:true, laneW:300, laneH:64, glyphH:34}` (starter = the first letter of the verb on the lane, `data-lcs-starter`, sized by `starterFontPx`) · **d2** `{layout:'label', organs:5, bank:true, starter:false, laneW:300, laneH:60, glyphH:30, organPx:72}` · d3 `{layout:'label', organs:5, bank:false, starter:false, laneW:300, laneH:56, glyphH:26}` (no bank = retrieve the verb: a G2 move, unpublished, say so). Layout: `wordBank({words: shuffled verbs})` 59 + 10, then 5 rows of [organ picture 72 | `writingRow({w:300, h:60, glyphH:30, xHeight:true})`] at ~84 each = 420 + gaps 4 x 12 = 468; total ~537 <= 722 (*est.*). The organ order on the page is shuffled and must differ from the bank order.
**verify():** stamps `data-lcs-organ=<organ>` on the row, `data-lcs-verb=<verb>` on the lane (the expected literal, never rendered), `data-lcs-bank-word` per bank word (the `wordBank` stamp). Re-derived: the bank word set === `{verbs[loc][s]}` (5, distinct, case-sensitive), row organs === the 5 organ files each once, for every row `verb === verbs[loc][SENSE_OF[organ]]`, lanes carry no text at d2 (at d1 only one `data-lcs-starter` glyph = the verb's first letter), `glyphH >= 26`, bank order !== row order, no verb equals the vocab singular of any organ in that locale (rule 5).
**Refusals:** none. fr touch chip = "toucher" (never "sentir", which is smell AND feel); it hear = "udire" or "ascoltare" (never "sentire"), touch = "toccare"; pt smell = "cheirar" (never "sentir"); no touch = "kjenne" or "føle" (panel picks ONE); de verbs lowercase infinitives (hören sehen riechen schmecken fühlen), never the G3 nouns Gehör / Geruch.
**Query face:** de "Sinnesorgane beschriften" (its own head, July §1 gap) · en "sense organs worksheet" (the "5 sense organs" tail) · es "los cinco sentidos y sus órganos" · pt "órgãos dos sentidos" · fr "les organes des sens" · it "gli organi di senso" · nl "zintuigen benoemen" · sv "sinnesorgan" · fi "aistielimet".

### F5: I Can Hear ... (G1, G1-3xx TBD, CODE `layout:'write'`, OPEN-ENDED: no verify)
**Move:** WRITE from experience: five blocks, each headed by an organ picture and a sentence starter printed on the first ruling row ("I can hear ..."), two rows per sense to finish; the it "descrivere con i 5 sensi" demand (measured x3, classe seconda writing) and the en "five senses writing" tail.
**Child:** "Finish each sentence. Write what you can see, hear, smell, taste and touch." (74)
**Params:** d1 `{layout:'write', rowsPerSense:1, rowH:52, glyphH:34, organPx:56}` · **d2** `{layout:'write', rowsPerSense:2, rowH:46, glyphH:30, organPx:56}` · d3 `{layout:'write', rowsPerSense:3, rowH:40, glyphH:26, organPx:48}`. Block = [organ 56 | `rulingBlock({rows:2, w:560, h:46, glyphH:30, starters:{0: starters[loc][sense]}, gap:6})`] = 98 + ~6 wrapper; 5 blocks x ~104 + 4 x 14 gaps = ~576 <= 722 (*est.*); at d3 5 x (3 x 40 + 12) + 56 = 716, the tight one. The starter text is sized by `starterFontPx` from `primitives/font-metrics.json` (the measured x-height 0.500, never a factor), and `qa/verify-ruling-starters.js` discovers the surfaces by `build()`; starter width must leave >= 60 % of the row writable (the G1-306 `writable >= 60` guard): en "I can hear" ~ 10 chars, de "Ich kann hören:" 15, fi "Kuulen" 6, pt "Eu posso ouvir" 14 at ~7.7 px/char = <= 116 px of 560 (*est.*).
**verify():** none (open). Layout lints + the starters gate + the gate's structural checks only: 5 blocks, each organ once, each block's starter literal === `starters[loc][SENSE_OF[organ]]`, `rowsPerSense` rows per block, every row's writable span >= 60 %, glyphH >= 26, nothing below the footer.
**Refusals:** none anticipated; fi starters are 1st-person verb forms as whole literals ("Kuulen ...", "Näen ...", "Haistan ...", "Maistan ...", "Tunnen ..."), never a `{verb}` slot; the panel may drop the "I can" frame where it reads as ability (sv "Jag hör ...", not "Jag kan höra"); es "Puedo oír ..." vs "Oigo ..." is the panel's.
**Query face:** it "descrivere con i 5 sensi" (verbatim) · en "five senses writing worksheet" · de "die fünf Sinne Sätze schreiben" · fr "les 5 sens : j'entends, je vois" · pt "os cinco sentidos frases".

**Rejected non-moves.** "Circle all the senses you use" (lock: never multi-answer) · a NOUN-of-sense label face (de Gehör / Geruch / Gesicht, es el gusto / el tacto: G3 register per the panel, and it collides with the organ-noun fence when the noun is the organ, es "el oído") · a theme swap (themeless) · "hot or cold by touch" (K-211) · "which food do you taste" (G1-207) · "label the eye / ear / nose" (K-344 / K-354) · relabelling d1 or d3 of the base as a face (same move: `gate-variation-distinct`) · "how many senses / count the organs" (a counting page, not a sense move) · a picture-with-own-shadow or identical-icon odd-one-out (K-039 / K-043) · a "colour the organs" face (colour-by-legend belongs to K-354 F2 and G1-242).

## C. Native rebuild x11

Frames NEVER inflect: this type has NO cloze frame and NO printed noun, so it needs NO `objForms` (measured: `bank('instructions', loc).objForms` holds 0 of the 5 organs in en / de / es / pt / fr / it / nl / sv / da / no and 4 of 5 in fi; nothing is read from it). Per locale the panel authors: `strings` (6 titles + 6 instructions), `verbs` (5 whole infinitive literals, also the F1 bin labels), `starters` (5 whole first-person literals for F5), `refuse` (per face), and signs `items[].sense` for `confidence:'signed'` items it admits (a per-locale `signedOk:[nouns]` list; default = the EN-signed set). = 22 literals + the signature.

| loc | literals the panel authors (counts) | slots / forms and where they exist | refusal / re-target | traps |
|---|---|---|---|---|
| en | 12 strings · 5 verbs (hear see smell taste touch) · 5 starters ("I can hear ...") = 22 | none | none | US spelling; "Five Senses" title, "5 senses" meta only; "printable" beats "worksheet" on this K head (Facts 4); "touch" is the K word (skin is G3+) |
| de | 12 · 5 (hören sehen riechen schmecken fühlen, lowercase infinitives) · 5 ("Ich kann hören: ...") = 22 | none | none | never Gehör / Geruch / Gesicht (G3 nouns); "Die fünf Sinne" keeps the article, "fünf" lowercase; F4 title head "Sinnesorgane"; engine appends "Arbeitsblatt"; the K-344 / K-354 heads (Gesicht / Körper) must not appear in any K-355 title |
| es | 12 · 5 (oír ver oler probar tocar; "oír" with the accent) · 5 ("Puedo oír ..." or "Oigo ...", panel) = 22 | none | none | MX register; never "sentir" for touch; "el oído" (sense) vs "la oreja" (organ) is exactly why only VERBS print; "5 sentidos" in the meta, "cinco" in the title; strand K vs 1º (OPEN 1) |
| pt | 12 · 5 (ouvir ver cheirar provar tocar) · 5 ("Eu posso ouvir ..." / "Eu ouço ...") = 22 | none | none | never "sentir" (smell AND feel); "paladar" / "olfato" are nouns, not printed; "atividade" never in a title; "5 sentidos" meta only |
| fr | 12 · 5 (entendre voir sentir goûter toucher) · 5 ("Je peux entendre ..." / "J'entends ...") = 22 | none | none | **"sentir" = smell AND feel: the touch verb is "toucher"** (rule 6 asserts the 5 verbs are distinct and that fr `touch !== 'sentir'`); title "Les 5 sens" keeps the digit (harvest 43 vs 30), meta "cinq"; "fiche" never in a title; elision in starters is the panel's ("J'entends") |
| it | 12 · 5 (**udire or ascoltare** for hear, vedere, annusare (or odorare), gustare (or assaggiare), **toccare**) · 5 ("Sento ..." is FORBIDDEN as a starter for hearing or touch: "Ascolto ..." / "Tocco ...") = 22 | none | none | **"sentire" = hear AND feel AND smell: never printed anywhere on the type** (rule 6); F5 owns the measured "descrivere con i 5 sensi" demand; "scheda" never in a title; classe seconda register on F5 copy |
| nl | 12 · 5 (horen zien ruiken proeven voelen) · 5 ("Ik kan horen ..." / "Ik hoor ...") = 22 | none | none | "het zintuig / de zintuigen"; never the numeral in a nl title ("De vijf zintuigen"); F3 title must contain "zintuig" (the nl odd-one-out family head); engine appends "werkblad" |
| sv | 12 · 5 (höra se lukta smaka känna, bare infinitives without "att") · 5 ("Jag hör ..." / "Jag ser ...") = 22 | none | none | sense nouns syn / hörsel / känsel / smak / lukt are all common gender but are NOT printed; title "Våra sinnen" (UR series neighbour, fine); engine appends "arbetsblad" |
| da | 12 · 5 (høre se lugte smage føle) · 5 ("Jeg kan høre ..." / "Jeg hører ...") = 22 | none | none | "følesans" / "lugtesans" nouns not printed; title "Sanserne"; engine appends "opgaver" |
| no | 12 · 5 (høre se lukte smake **kjenne or føle**: the panel picks ONE for touch and records why) · 5 ("Jeg kan høre ...") = 22 | none | none | bokmål only; "berøringssans" not printed; title "Sansene våre"; `[NSR-FLAG][no]` on the touch verb |
| fi | 12 · 5 (kuulla nähdä haistaa maistaa tuntea) · 5 first-person literals (Kuulen / Näen / Haistan / Maistan / Tunnen ...) = 22 | none (fi `part`/`gen` forms exist for 4 organs but no frame uses them) | none | title "Viisi aistia" (partitive), NEVER "Aistit"; instruction cases are the panel's ("Yhdistä esine oikeaan aistiin." illative, "Ympyröi aisti, jolla huomaat sen."); "tuntea" also = to know: the picture + organ disambiguate, the panel confirms; fi 4-line title = the 677 budget on every stack |

## D. Data + gates

Bank = `data/science/five-senses.json` (the concept bank, same directory + shape family as the 21 live science banks; locale-neutral items) + `data/b4/five-senses.js` (EN `verbs` / `starters` / `strings`) + `data/b4/locales/five-senses.<loc>.json` (GENERATED from `i18n/.draft-b4-<loc>.json` by `tools/apply-b4-locale.js`, the `apply-b3-locale.js` clone; read via `lib/b4-common.js bank(name, loc)`). `data/` is gitignored (`git add -f`).

```
FIVE_SENSES (data/science/five-senses.json, GLOBAL):
  senses:   ['hear','see','smell','taste','touch']
  organOf:  { hear:'ear', see:'eye', smell:'nose', taste:'tongue', touch:'hand' }     // theme 'body parts', all 5 opened
  bins:     [{key:'hear'}, {key:'see'}, {key:'smell'}, {key:'taste'}, {key:'touch'}]  // labels come from verbs[loc], never from here
  items: [  // ONE primary sense each; confidence 'strong' = single-sense by any child's reading; 'signed' = a second sense exists, the panel signs it in
    // hear (10 strong + 1 weak + 1 review)
    {theme:'music', noun:'drum', sense:'hear', confidence:'strong', picOpened:true},   guitar, trumpet, piano, violin, xylophone, saxophone (all music, strong),
    {theme:'christmas', noun:'bell', ...strong}, {theme:'accessories', noun:'whistle', ...strong}, {theme:'around the house', noun:'telephone', ...strong},
    {theme:'music', noun:'flute', sense:'hear', confidence:'signed', note:'a plain orange tube with holes; reads as a recorder'},
    {theme:'weather', noun:'thunderstorm', sense:'hear', confidence:'signed', note:'the picture is a cloud + a LIGHTNING bolt (a see cue); admit only where the panel says thunder'},
    // see (6 strong + 4 signed)
    rainbow (weather), moon (camping), telescope (space), binoculars (camping), star (christmas: a gold 3D star; NOT shapes/star, a pale outline shape), flashlight (camping)  = strong,
    television (around the house, signed: also hear), book (classroom, signed: read), mirror (around the house, signed), lamp (around the house, signed),
    // smell (7 strong, family-fenced)
    flower (spring: orange daisy), rose (flowers: red rose), lavender (flowers: purple spike), lilac (flowers: 3 purple blooms), jasmine (flowers: a PINK 5-petal flower, not white),
    garlic (vegetables: a striped bulb), onion (vegetables: brown onion with a green sprout),
    // taste (8 strong + 1 signed)
    lemon (fruits), honey (breakfast: a jar with a blank label, weak but readable), candy (At the Supermarket: a wrapped sweet), lollipop (desserts and sweets),
    cookie (desserts and sweets), cake (desserts and sweets: a slice with a strawberry), popcorn (At the Supermarket), chocolate (easter: a bar),
    pizza (At the Supermarket, signed: also smell),
    // touch (5 strong + 1 signed)
    feather (easter), sand (summer: a pile), sponge (kitchen tools), pillow (around the house), rock (camping: a grey stone; NOT beach/rock, a purple crystal),
    hedgehog (forest creatures, signed: "prickly" is the K convention, but it is an animal) ]
  families: [['flower','rose','lavender','lilac','jasmine']]        // never two of one family on a page
  pageRules: { maxPerSense:2, oddMajority:['hear','taste','see'] }  // defaults; every face's config restates what it uses
  excludedByRule (recorded, never items): sun · campfire · bonfire · ice · icicle · snowflake · popsicle (hot-vs-cold.json) · cheese · strawberry · bread (food-groups.json) ·
    sweater · scarf · mittens (K-207) · cushion (= pillow's word in de/nl/sv/da/no/fi) · beach/rock, shapes/star (weaker twins) · glue (reads "bottle") · rope · seashell (see/touch split) · candle (see/smell split)
FIVE_SENSES_L10N[loc] = {
  verbs:    { hear, see, smell, taste, touch },       // whole infinitive literals; also the F1 bin labels + the F4 bank
  starters: { hear, see, smell, taste, touch },       // whole first-person literals for F5, printed as rulingBlock starters
  signedOk: ['pizza','television','book', ...],       // the signed items this locale's panel admits (default = EN set); 'strong' needs no signature
  refuse:   [],                                       // per-face: 'sort'|'which'|'odd'|'label'|'write'
  strings:  { 'K-355':{title, instruction}, <F1..F5 ids>:{title, instruction} } }
```

**`tools/validate-b4-draft.js` (five-senses block; every rule runs, exit 1 on any):**
1. `senses` === the 5 keys; `organOf` maps each to one of `eye ear nose tongue hand`, each organ once; every organ resolves via `fileUri('body parts', organ)` and has a vocab singular x11.
2. Every item `{theme, noun}` resolves via `fileUri` (throws on a missing cache entry), its theme dir carries no localized B&W marker (`\bbw$` and the 10 localized markers), `picOpened:true`, `hasPicture(vocabKey, loc)` in ALL 11 locales, not `B2_EXCLUDE`d anywhere, `sense` ∈ senses, `confidence` ∈ {strong, signed}; a `signed` item carries a non-empty `note`.
3. **Boundary fences (read the neighbour banks at validate time, never on the page):** no item vocabKey appears in `hot-vs-cold.json`, `food-groups.json` or `summer-vs-winter-clothes.json` items; no item noun is one of the 5 organs or any `body parts` noun.
4. Per sense, strong items >= 5 AND co-occurrable strong items (after the family fence) >= 3; the odd majority senses each >= 8 strong; every family is a subset of one sense; no noun in two items.
5. `verbs[loc]`: 5 non-empty distinct literals, no `{`, no digit, <= 14 chars, none equal (case-insensitive, `toLocaleLowerCase(loc)`) to the vocab singular OR plural of any organ or of any item noun in that locale (the organ-noun fence: a panel writing "Ohr" under the ear FAILS).
6. Locale bans on the sense verbs and starters, each poison-tested in BOTH directions: fr `verbs.touch !== 'sentir'` and no starter for touch contains `sens`; it: the string `sentir` / `sento` / `senti` appears in NO literal of the type (hear = udire | ascoltare, touch = toccare); pt `smell !== 'sentir'`; de: no `Gehör` / `Geruch` / `Gesicht`; a MUST_PASS control per ban (fr "toucher", it "ascolto", de "hören").
7. `starters[loc]`: 5 non-empty literals ending in "..." or ":" (the design agent fixes the mark), rendered width at the d2 starter px <= 40 % of the 560 row (`measure` via the font metrics), never containing a `{slot}`.
8. `signedOk` ⊆ the signed nouns; every face's d2 pool (`strong` + this locale's `signedOk` where `pool` admits it) reaches its config (base 5 senses x 1; sort 5 x 2; which 8 under `maxPerSense:2`; odd 5 rows under `oddMajority`; label 5; write 5) else `refuse` names the face.
9. Titles <= 70 chars, no worksheet-word (`WORKSHEET_WORD` regex of the b3 gate), unique in the band, never equal to a neighbour family's `axes['exercise-type'].*.name` (science-sort, visual-discrimination, human-body); nl F3 title contains "zintuig"; fi titles never contain the bare token "Aistit" as the head; nl titles carry no digit; fr base title may carry "5" (the measured lead); instructions <= 150; six titles differ pairwise by >= 1 noun or verb token (never an adjective-only difference).
10. `strings` ids === {base + the 5 allocated face ids}; every face id present in every locale or named in `refuse`.

**Poison (each must FAIL for its OWN reason; the correct EN draft is the control):**
P1 item `{theme:'weather', noun:'sun', sense:'see'}` -> rule 3 (K-211). P2 item `{theme:'At the Supermarket', noun:'cheese', sense:'smell'}` -> rule 3 (G1-207). P3 item `{theme:'accessories', noun:'perfume'}` -> rule 2 (not cached; `fileUri` throws). P4 `organOf.touch = 'skin'` -> rule 1 (no picture). P5 `families[0] += 'garlic'` while `rose` is smell and garlic is smell too but the family then leaves < 3 co-occurrable -> rule 4. P6 de `verbs.hear = 'Ohr'` -> rule 5 (organ noun). P7 fr `verbs.touch = 'sentir'` -> rule 6 (MUST_FIRE); fr `verbs.touch = 'toucher'` PASSES (control). P8 it `starters.hear = 'Sento ...'` -> rule 6. P9 fi `starters.hear = 'Kuulen {noun}'` -> rule 7 (slot). P10 nl F3 title `'Wat hoort er niet bij?'` (no "zintuig") -> rule 9. P11 fi base title `'Aistit'` -> rule 9. P12 `strings` missing the F4 id in sv without `refuse:['label']` -> rule 10. P13 two items with noun `rock` (camping + beach) -> rule 4 (noun in two items). P14 `oddMajority += 'smell'` -> rule 4 (smell strong 7 < 8 and only one triple).

**`qa/verify-b4-five-senses.js` (render gate; clone the b3 shape):** renders base + 5 faces x 11 locales x d2 through `render/render-instance.js` (file:// fonts) under the 722 stack (3-line de title + 150-char instruction) AND the 4-line fi title (677); asserts `verify()` empty and `qa/lints.js` clean, then ITSELF (no size lint exists): every object picture >= 56 on the base (K) and >= 44 on F1-F3 (G1), organ chips >= 44 and equal, F1 bins 5 and >= 44 px organ icon in each pill, F4 lanes glyphH >= 26 and empty, F5 starters sized by `starterFontPx` and writable >= 60 % (`verify-ruling-starters.js` discovers them), the answer organ never printed as a word, no `data-lcs-answer` anywhere (no `answerBox` on the type), no numeral glyph in any body except badges, everything above the footer. NODE cross-checks per face: base pairs (sense -> organ file), F1 bins (label === verb, icon === organ, items' sense === bin), F2 (one chip === `organOf[sense]`, canonical strip order at `fixed`, position-leak cap), F3 (row sense ∈ majority, one odd, odd column varies), F4 (bank set === verbs, verb per organ, bank order !== row order), F5 (starter literal per organ); page rules (`maxPerSense`, family fence, no picture twice) on every face. SWEEP: 20 seeds x d2 per face render distinct pages (item SETS vary; the F1 smell bin at d2 has 11 legal pairs so it must vary; F2 answer positions vary; F3 odd columns vary). Render poisons (each must FAIL): PR1 base right column with the organ straight across (factory rule). PR2 base with two hear items and no touch item -> "5 distinct senses". PR3 F1 bin labelled with the organ NOUN ("Nase") -> "label !== verb". PR4 F1 two flower-family items on one page -> family fence. PR5 F2 a card with 4 chips -> "5 chips". PR6 F2 the ear chip pre-ringed (a coral ring injected) -> "chip pre-marked". PR7 F3 a row of 3 smell + 1 hear -> "majority ∉ config". PR8 F3 every odd in column 4 -> position leak. PR9 F4 a bank in the same order as the rows -> order leak. PR10 F5 a starter at 0.9 x glyphH derived px instead of `starterFontPx` -> the starters gate (the G1-306 defect class). PR11 F2 d2 config resolved from a level-index guard (`difficulty === 2 ? 'fixed' : 'shuffled'`) with d2 re-pointed to d3's config -> the config guard fires before render. PR12 the F1 d3 15-item strip at 40 px -> the G1 floor.

Page reads at render: `FIVE_SENSES`, `FIVE_SENSES_L10N[loc]`, `fileUri`; never `image-vocabulary.js`, never `approved-words-*.json`, never the neighbour banks (those are validate-time only).

## E. SEO

`coordinate = {type:'five-senses', mode:<face>, theme:'', level:<band key of the locale>}`; modes: base `'base'` · F1 `'sort'` · F2 `'which'` · F3 `'odd'` · F4 `'label'` · F5 `'write'`. Level keys as the live landing corpus spells them (measured from `frontend/content/seo-landing/<loc>.json` on the `seasons` / `feelings` rows): K = en `kindergarten` · de `vorschule` · es `preescolar` · pt `educacao-infantil` · fr `maternelle` · it `infanzia` · nl `kleuters` · sv `forskola` · da `boernehaveklasse` · no `1-trinn` · fi `esikoulu`; G1 = `grade-1` · `1-klasse` · `primer-grado` · `1o-ano` · `cp` · `classe-prima` · `groep-3` · `ak-1` · `1-klasse` · `2-trinn` · `1-luokka`. Base = K key; F1-F5 = G1 key (F3 may take the G2 key per locale, OPEN 4). Meta lead inherits `seo.words.free_printable` (metadata only, never visible copy). The MIDDLE is the instruction stem; the per-locale window is measured with `tools/measure-instruction-window.js` on the staged ZIPs; en middles below sit at 60-91 chars (*est.* inside a ~60-105 window for a ~55-char en lead).

| face | title pattern Germanic (en / de / nl) | Romance (es / pt / fr / it) | Nordic + fi (sv / da / no / fi) | meta MIDDLE (en) |
|---|---|---|---|---|
| base | Five Senses · Die fünf Sinne · De vijf zintuigen | Los cinco sentidos · Os cinco sentidos · Les 5 sens · I cinque sensi | Våra sinnen · Sanserne · Sansene våre · Viisi aistia | Draw a line from each picture to the body part you use for it: eye, ear, nose, tongue or hand |
| F1 | Five Senses: Sort by Sense · Die fünf Sinne: nach Sinn sortieren · De vijf zintuigen: sorteer op zintuig | Los cinco sentidos: clasifica por sentido · Os cinco sentidos: separe por sentido · Les 5 sens : trie par sens · I cinque sensi: classifica per senso | Våra sinnen: sortera efter sinne · Sanserne: sortér efter sans · Sansene våre: sorter etter sans · Viisi aistia: lajittele aistin mukaan | Look at each picture and draw a line from its dot to the box of the sense you use for it |
| F2 | Five Senses: Which Sense? · Die fünf Sinne: Welcher Sinn? · De vijf zintuigen: welk zintuig? | Los cinco sentidos: ¿qué sentido usas? · Os cinco sentidos: qual sentido? · Les 5 sens : quel sens ? · I cinque sensi: quale senso? | Våra sinnen: vilket sinne? · Sanserne: hvilken sans? · Sansene våre: hvilken sans? · Viisi aistia: millä aistilla? | Which sense do you use for each picture? Circle the eye, ear, nose, tongue or hand |
| F3 | Five Senses: Which One Does Not Belong? · Die fünf Sinne: Was passt nicht dazu? · De vijf zintuigen: welk zintuig hoort er niet bij? | Los cinco sentidos: ¿cuál no pertenece? · Os cinco sentidos: qual não combina? · Les 5 sens : l'intrus · I cinque sensi: l'intruso | Våra sinnen: vad passar inte in? · Sanserne: hvad passer ikke? · Sansene våre: hva passer ikke? · Viisi aistia: mikä ei kuulu joukkoon? | In each row three pictures go with the same sense: cross out the one that does not |
| F4 | Sense Organs: Label with the Sense Word · Sinnesorgane beschriften · Zintuigen benoemen | Los cinco sentidos y sus órganos · Os órgãos dos sentidos · Les organes des sens · Gli organi di senso | Sinnesorgan: skriv rätt ord · Sanseorganer: skriv ordet · Sanseorganer: skriv ordet · Aistielimet: kirjoita sana | Copy each sense word from the bank onto the line under the body part that does it |
| F5 | Five Senses: I Can Hear ... · Die fünf Sinne: Ich kann hören ... · De vijf zintuigen: ik kan horen ... | Los cinco sentidos: puedo oír ... · Os cinco sentidos: eu posso ouvir ... · Les 5 sens : j'entends ... · Descrivere con i cinque sensi | Våra sinnen: jag hör ... · Sanserne: jeg kan høre ... · Sansene våre: jeg kan høre ... · Viisi aistia: kuulen ... | Finish each sentence: write what you can see, hear, smell, taste and touch |

All titles are candidates the native panels rewrite (<= 70, no worksheet word, base = bare head, each face adds one NOUN / verb, never an adjective; fr keeps the digit, nl never a digit; F4's head is the ORGAN head in every locale, the de July §1 gap). JSON-LD `LearningResource`; NO `educationalAlignment` on any face (readiness; no honest NGSS K-1 code); non-EN prose names the framework + strand literal of A (there is no `strand-names.ts` Science row: the engineer adds a `'Science'` row with the A literals or the landing template reads them from the type's i18n; the chip must not fall back to English). `topicMeta['five-senses']` >= 50 chars x11 + `skill-sentences.<loc>.json['five-senses']` via a `tools/register-b4-en-content.js` clone.

**Non-cannibalisation** (whole-landing 3-gram Jaccard, `scripts/seo-landing/gate.js` FAIL >= 0.80 / WARN 0.65; *est.*, the gate measures):

| pair | what the copy says differently | est. |
|---|---|---|
| base vs F1 | five lines, one object per organ vs ten pictures into five labelled boxes, two per sense; K vs grade 1 | 0.35 |
| base vs F2 | lines to organs vs circle one of five chips under each picture; the "which sense" query | 0.30 |
| F1 vs F3 | put every picture in its box vs find the shared sense of a row and cross out the intruder | 0.25 |
| F2 vs F3 | one picture, five chips vs four pictures, one intruder | 0.25 |
| F4 vs base / F1 / F2 | the child WRITES the sense word under the organ; no object pictures at all on F4 | 0.20 |
| F5 vs any | open writing on ruled rows with a starter; no line, no circle, no bank | 0.10 |
| F4 vs K-344 (label the face) / K-354 base | a VERB of sense from a bank under an organ picture vs a body-part NOUN onto a lane pointing at a face / figure; F4 copy never names eye / ear / nose as targets, it names hear / see / smell | 0.25 |
| any vs K-354 (human-body) | no body figure, no body fact, no body-part word; the boundary sentence names it both ways | 0.10 |
| F1 vs K-211 / K-201 / the 19 other science sorts | `science-sort` has 0 landings in all 11 corpora (measured by the seasons file); the deck boundary is structural (hot-vs-cold items excluded); five bins, not two | n/a |
| F3 vs K-043 / K-061..063 (visual-discrimination) | four different pictures by concept vs identical icons and one different noun / a size difference; the copy says "sense" in every sentence | 0.15 |
| F1 vs G1-207 (food groups) | senses, not food groups; no food-groups item can be an item here (rule 3) | 0.10 |
| F2 vs K-319 feelings "which face" | an organ chip strip vs a face; no emotion word | 0.05 |

Boundary sentence on every landing: "The child matches, sorts and names by the five SENSES, with the eye, ear, nose, tongue and hand as the answer key. Hot and cold, the food groups and the parts of the body are their own worksheet types."

## F. Open questions + summary

**Engineer must measure:** (1) the F1 bin label pill with `binIcons:true` at the widest verb (it "ascoltare", fi "haistaa", pt "cheirar") against the ~120 px bin at 5 bins: two-line pill vs a 36 px icon vs bins at 4 + 1; the factory's inline CSS is the only place to change. (2) The F2 card at 330 x 171 (*est.*) with the 5 x 44 chip strip under a 72 px picture at the fi chrome (677). (3) The F5 d2 and d3 stacks under the fi title (5 x 104 = 576 at d2; 716 at d3 is the tight one). (4) The F4 stack: bank 69 + 5 rows x 84 (*est.* 537). (5) The starters: `starterFontPx` on every F5 row + the `verify-ruling-starters.js` discovery of the K-355 surfaces (a new type is a new MUST_HAVE entry there). (6) `samplePairs` / `sampleItems` as ADDITIVE cfg hooks on the two factories with the default path byte-identical (diff the 21 + 2 live banks' renders before / after: 0 pixels). (7) The per-locale meta window per face (`measure-instruction-window.js`). (8) `verify-hub-type-rows.js` expectation: 6 rows x 11 = 66 (no face refusal is recorded; every pool clears its d2 config x11 because the items are locale-neutral), exported to `hub-expectations.json`; poison a wrong `coordinate.type` before `apps['five-senses']` lands.

**Only a native panel can rule:** OPEN 1 es strand literal at K (SEP preescolar campo formativo) vs "Conocimiento del Medio", and fr "Explorer le monde" (GS) vs "Questionner le monde" (CP): one literal per family or a per-face override (the K-354 question, decide once for both science families). OPEN 2 the `signed` items per locale (`signedOk`): pizza (also smell), television (also hear), book / mirror / lamp (see by convention), hedgehog ("prickly" touch convention), flute (reads as a recorder), thunderstorm (the picture shows LIGHTNING: hear only where the panel says thunder). OPEN 3 the tongue picture shows lips + teeth + tongue: acceptable as the taste organ in all 11 (the K convention), or does a locale want it flagged in copy ("la lengua", not "la boca")? OPEN 4 F3 band per locale (G1 default; G2 where the curriculum places classification reasoning later: it "classe seconda", no "3. trinn" are candidates via the `level` key, no code change). OPEN 5 the no touch verb (kjenne vs føle, `[NSR-FLAG][no]`), the it hear verb (udire vs ascoltare) and the it smell / taste verbs (annusare / odorare, gustare / assaggiare). OPEN 6 the F5 starter frame per locale ("I can hear" ability frame vs the bare present "I hear"): sv / da / no / fi likely bare, Romance either; the mark ("..." vs ":") is the design agent's. OPEN 7 F1 bin pills: verb only (the panel lock) or verb + the organ picture (this file's recommendation, `binIcons:true`), per locale.

**Summary.** Six faces on one constant answer key (the five organ pictures), all buildable x11 with zero face refusals and zero inflection: match one object per sense (K) · sort ten into five labelled bins (G1) · circle the one organ chip of five per picture (G1) · cross out the intruder of a sense row (G1) · copy the sense VERB under each organ (G1) · finish "I can hear ..." on ruled rows (G1, open). The bank is 41 opened items with ONE primary sense each (hear 10 strong, see 6, smell 7 family-fenced, taste 8, touch 5; plus 9 signed the panels admit per locale), four structural fences (hot-vs-cold, food-groups, K-207, the organ-noun ban) and a page rule (never more than two of one sense, never two flowers). 22 panel literals per locale (5 verbs + 5 starters + 12 strings), no `objForms`, no printed noun anywhere. Compound heads locked in all 11 locales and measured collision-free; the de organ head "Sinnesorgane" is F4's own title.
