/** G2-325 — Rule Detective: {UNIT}. nt20-C variation of G2-315 (generated by tools/gen-b3var-specs.js from tools/b3var-rows/). */
'use strict';
const base = require('./G2-315-spelling-rules.js');
// One object for all three levels: the waves ship d2 only, so a face must
// render identically whichever level is asked for. Spreading the base entry
// (not a JSON literal) carries function-valued params through intact.
const D = { ...base.difficulty[2], ...{"detective":{"foils":0},"maxLetters":9,"pic":72} };
module.exports = {
  ...base,
  id: 'G2-325',
  slug: 'spelling-rules-rule-detective',
  difficulty: { 1: D, 2: D, 3: D },
  i18n: { en: { title: "Rule Detective: {UNIT}", instruction: "Read each word. Find the rule letters, circle them in the word, then copy them into the small box." } },
};
