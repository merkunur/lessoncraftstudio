# K-356 `weather-symbols` : EN BASE build record (2026-09-21)

Built from `K-356-weather.md` (FINAL) §2 base + §5 data + gates under `_BUILD-BRIEF.md`, `_SUBSTRATE.md` and the README cross-type rulings; every ruling of `_work/K-356-critic.md` applied (sunny / cloudy / rainy / thunderstorm / snowflake / rainbow; the gear triple on F3 only; bands [10, 25] with ink edge ticks; additive thermometer options; markers in cycle order; the `layout` knob). **The family KEY is `weather-symbols`** (Phase 0 ruling: the theme axis owns `weather`); the design file's `weather` is read as `weather-symbols` in every file name, export and stamp; `withFixedTheme(type, 'weather')` stays as designed. No em-dashes. Nothing shared edited except the RULED additive options on `primitives/thermometer.js`; the faces (G1-362 · K-364 · K-365 · G3-385 · G1-363) are NOT emitted (Phase 2). Nothing was `git add`ed.

## Files (all type-scoped)
| file | what |
|---|---|
| `scripts/worksheet-gen/types/k/K-356-weather-symbols.js` | the spec: `exerciseType:'weather-symbols'`, `module.exports = withFixedTheme(type, 'weather')` (+ `_type`, the unwrapped spec, for the gate's poison seam), `difficulty {1,2,3}` = `{pairs, keys, distractors, badge, iconPx, tileH, wordPx}` exactly the design ladder (d1 4 fixed keys `sun rain snow cloud` / 112 / 88 / 112 / 26 · **d2 6 / 96 / 72 / 84 / 24 (ships)** · d3 6 + 2 symbol-less distractors / 84 / 64 / 72 / 22); `build()` reads ONLY `bank('weather-symbols', loc)` (lib/b4-common.js) + the GLOBAL json + `fileUri`; `_buildWith({global, block})` is the poison seam; `_symbol / _word / _distractors` = the §5 helper contract (throw, never en, never the vocab); the composer = `rng.shuffle` left + a derangement of the right (no fixed point, never the reversed left; the literals are resolved BEFORE the first rng draw so the seed is locale-neutral, proved: a synthetic de block gives the same left / right orders as en); `_buildFace` = the additive `layout` knob (`d.layout` undefined = base path byte-identical; 'write' / 'diary' / 'thermometer' / 'water-cycle' / 'forecast' refuse by name until Phase 2); `verify(page)` re-derives the base from the stamps (root `data-lcs-layout="base"` + `-pairs` + `-distractors`; badges `data-lcs-badge data-lcs-concept`; tiles `data-lcs-tile data-lcs-word data-lcs-concept` / `data-lcs-distractor`): 4..6 badges with distinct concepts of the six, ONE `img` each (`complete`, dir `weather`, never a B&W dir, file === the concept's fixed noun `NOUN_OF`, never a gear file), round (`border-radius >= d/2`) and square >= 56, no text; tiles = a permutation of the badge concepts + exactly `distractors` extra with no concept, each prints its stamped word (single token or hyphenated), no picture, no duplicate word, word >= 18 px, never clipped, text width <= tile inner; no row straight across; not the reversed order; no gear picture anywhere; no `[data-lcs-answer]`; dots both sides; the stage inside the body / above the footer; line zone >= 180. |
| `scripts/worksheet-gen/data/b4/weather-symbols-global.json` | the GLOBAL data (design §5 `WEATHER_SYMBOLS`): the six `symbols` (key / theme / noun / alt / `picOpened:true` + what was seen), `excluded` (+ why), the `gear` triple (scarf cold · `clothing/t-shirt` warm · sunglasses hot), `gearNever`, `bands` (order / -10..40 step 5 / edges [10, 25] / values cold [-10,-5,0,5] warm [15,20] hot [30,35] / keyValues 0 · 17.5 · 32.5 / tints tealSoft · white · coralSoft), `cycle` + `cycleExtra`, `neverPrint` (the four adjective keys), `diary` / `forecast` day indices. gitignored (`data/`): the reviewer force-adds. |
| `scripts/worksheet-gen/data/b4/weather-symbols.js` | `{ WEATHER_SYMBOLS: { en: {symbolWords, forecastAsks, cycleLabels, bandWords, forecastDays:null, diaryDays:null, diaryStart:'mon', bankWords:{}, distractors:['wind','fog'], refuse:[], strings, strand:'Science (readiness)'} }, SYMBOL_KEYS, CYCLE }` — the EN literals (design §4 en row: sun cloud rain storm snow rainbow · "Which day is rainy?" x6 · evaporation condensation precipitation collection (+ runoff) · cold warm hot) + the base and the five face strings (§6 EN candidates under the allocated ids G1-362 write · K-364 diary · K-365 thermometer · G3-385 water-cycle · G1-363 forecast). The EN is a SOURCE TO AUDIT for the panels. gitignored: the reviewer force-adds. |
| `scripts/worksheet-gen/templates/components-b4/weather-symbols.js` | `SYMBOL_KEYS`, `symbolBadge`, `symbolMatch` (the base), `spellRow`, `diaryWeek`, `bandKey`, `bandThermo`, `forecastStrip`, `forecastQuestion`, `cycleLabels` (the §2 geometry verbatim: badge r 999 teal 3 with the icon centred; tiles 250 x tileH Baloo 2 700 nowrap; spell rows = `letterBoxes` per hyphen group joined by a printed 14 px hyphen glyph; the 2 x 4 diary grid `minmax(326px,1fr)` with the flexing draw box and the wordless key card; the key row `space-evenly` + the item card `[thermometer][12][chip column]`; the 675 forecast strip + `.ws-pill` day chips padding 6 10 min 44; the `.ws-chip` numeral + `writingRow 270 x 52 glyphH 26` lanes 2 per row). The barrel merges it beside the sibling families (verified: 0 duplicate names). Only `symbolBadge` + `symbolMatch` are consumed by the base; the seven face components render nothing until Phase 2 (a node smoke in the gate proves each builds its stamped markup). |
| `scripts/worksheet-gen/primitives/thermometer.js` | **ADDITIVE** `bands:[{key, from, to, fill}]` (drawn before the tube at x 40 w 44, r 6, a token-NAME fill, tiling `min..max` in order or THROW; a 3 px ink tick across the band at every interior boundary; `data-lcs-band / -from / -to`) + `numerals:false` (skips the `label()` at every tick; the tick lines stay) + `meta.bands [{key, y1, y2}]`. Defaults byte-identical: G3-345 d1 / d2 / d3 bodies hash to the sha1s frozen BEFORE the edit (`7cc3ace7 / 42e80762 / 113c379b`, en + de identical), asserted by the gate; `b3-baseline --check --quick` PASS below. Measured band px at h 220: cold 65.6 (y 112.4..178) · warm 49.2 · hot 49.2 = the critic's numbers. |
| `scripts/worksheet-gen/primitives/water-cycle.js` | NEW, exactly §2 F4: viewBox 640 x 400, `waterCycle({w=608, markers, id}) -> {svg, width, height, scale, anchors}`; exported `ANCHORS` (5), `HOSTS` (each anchor's host bbox, for the gate), `MIN_W 480`, `VIEW`, `landY(x)` (the first land quadratic solved by bisection so every rain line ends ON the hill: y 270.6 / 254.9 / 243.3 / 235.8 / 232.4 / 233.1 at x 396..516, measured; the design's "~y 292..244" was an estimate), `RAIN_X`, `MARKER_MIN_GAP 50`. Land · water (after the land) + inner wave lines · sun + 8 rays · the one-path cloud · 3 vapour lines + the coral evaporation arrow with a tangent 12 x 12 head · 6 dashed rain lines + the down arrow · the run-off arrow into the water · markers last (white halo r 17 / coral disc r 13 / Baloo 2 700 17 numeral). Palette cream / tealSoft / white / teal / coral / grid only; `aria-label=""`; no picture, no word. |
| `scripts/worksheet-gen/qa/verify-water-cycle.js` | the primitive's node gate: anchors inside their hosts, markers centred within 2 px of the returned px anchors, spacing >= 50, MIN_W / unknown-anchor / duplicate / n refusals, palette, draw order, every arrow group, rain lines on the land curve, no img / no text but numerals; then the RENDER measured in Chromium (svg 608 x 380 / 480 x 300; every marker disc centre 0.00 px off the returned anchor; the numeral 16.15 px at w 608 / 12.75 at 480, cross-checked against a canvas measurement of the same digit); 5 poisons each KILLED for its own reason. **PASS (50 assertions, 5 poisons killed)**. |
| `scripts/worksheet-gen/qa/verify-b4-weather-symbols.js` | the gate (exports `validateBank(block, loc[, widths])` for `tools/b4-probe-child.js`, `validateGlobal`, `measureWords`, `measurePillRow`). |

## Measured (real render, shell woff2 from file://; `out/dev/K-356-gate-*.png`)
- **d2 en:** badges 96 round (radius 999), icons 72 (floor 56), tiles 250 x 84, words Baloo 2 700 24 (floor 18; widest `rainbow` 89 px <= 226), **line zone 220 px** between the dots (design 213: the design's sum 30 + 100 + 213 + 2 x 26 + 250 + 30 = 675 counts the dots twice at 26; the dot sits at right:-26 with a 12 px body, so 7 px per side return to the zone), item extent 660 with the body pinned to 677 (`space-around` spreads the 624 stack); **fits 722 AND 677**; under the one-line en chrome (body 811) the gaps open (the PNG above).
- d1: badges 112 / icons 88 / tiles 112 / words 26, zone 206, extent 620; d3: badges 84 / icons 64 / 8 tiles of 72 / words 22 (`wind`, `fog` distractors), zone 226, extent 664. All three verify 0 / lints 0.
- Long chrome (66-char 3-line de title + 150-char 3-line instruction): body **710** measured; the 677 stack is checked by PINNING the body to 677 (and 722) in the live page and asserting every item stays inside (the K-355 precedent) — all three levels fit both.
- EN word widths at Baloo 2 700 24: sun 39 · cloud 60 · rain 43 · storm 64 · snow 58 · rainbow 89 (<= 226). P19 measured de `Regenbogen` **135.4 px** (the critic's 127.3 was scaled from a 26 px measurement) and `Regenbogenwetterlage` 253.8 > 226 (the contingency fires by measurement, never by estimate).
- F5 pill row of the DEFAULT day names (Baloo 2 700 16, padding 6 10, border 2, gap 8): en 465 px; **pt full forms `segunda-feira … sexta-feira` = 585.3 px <= 639** — the design's "~645 est. OVER" was 60 px high; the pt panel needs NO short day forms (P12 re-targeted, see deviations). A row of five long forms (`Montagmorgen …`) measures 753.9 and FAILS the rule.
- Water cycle: closest marker pair precipitation / runoff = 56.6 viewBox units = **53.8 px at w 608** (the design said ">= 57"; 53.8 >= the 50 floor); at w 480 that pair is 42.5 px, so the five-marker (d3) set REFUSES below w ~566 by the primitive's own rule and w 480 carries the four d2 markers (closest condensation / precipitation 72.2). Numerals 16.15 px at 608 (>= 16), 12.75 at 480 (>= 12).
- Sweep: 20 seeds x d2 -> 20 distinct left orders, 20 distinct right orders, every seed six concepts + a derangement + never the reverse + never the left order.
- Bank: 6 / 6 symbol nouns and 3 / 3 gear nouns resolve via `fileUri` + vocab x11, 0 excluded, no alt is a noun, the SYMBOL_KEYS / CYCLE copies (json / data module / components) agree; the band table renders through the primitive with 0 `<text>`.

## Gate output
`node qa/verify-b4-weather-symbols.js` -> **PASS (358 assertions, 33 poison verdicts clean)**; `--quick` -> PASS (276 assertions). Poisons killed 27 / 27, each for its own reason, the EN bank the control: P1 puddle (excluded) · P2 a seventh symbol wind · P3 gear mittens · P4 gear hot (a sun as a chip) · P5 warm value 10 (an edge) · P6 de "Sonnig" FIRES / "Sonne" PASSES · P7 de "Regentropfen" · P7b sv "Åskväder" FIRES / "åska" PASSES / de "Gewitter" PASSES · P8 es "gota de lluvia" · P9 fi 12 letters · P10 en ask naming Monday · P11 fr ask without "?" · P12 five long day forms 753.9 px FIRE / pt full names 585.3 PASS (measured) · P13 sv title "Väder" (the theme name) · P14 en F4 title carrying the base head · P15 en F3 title "Hot or Cold?" · P16 en F3 "20 °C" · P17 en F1 "Big Words" (no mode token) · P18 a missing symbol word -> the spec REFUSES · P19 de `Regenbogenwetterlage` 253.8 px > 226 FIRES / `Regenbogen` 135.4 PASSES (measured) · PR1 the word column in the badge order -> verify() · PR2 `raindrop` beside `rainy` -> verify() "picture ≠ the rain symbol" · PR20 an `answerBox` tile -> `[data-lcs-answer]` · PR21 pairs 8 from a level-index guard -> the config guard · PR12 G3-345 re-rendered with the `numerals` default flipped in a fresh module graph -> 3 / 3 bodies drift, the graph restored -> the frozen hashes again · P-theme `animals` on the raw type -> the fixed-theme guard · P-d3 one distractor -> REFUSES.
`node qa/verify-water-cycle.js` -> **PASS (50 assertions, 5 poisons killed)**. `node i18n/build-en.js` -> `603 types (title lint clean)`. `node tools/b4-probe-child.js en` builds K-356 with 0 `weather-symbols` validator findings (its one message is the K-357 sibling's missing gate, not this build).

## Baseline
`node tools/b3-baseline.js --check --quick` -> **`checked build 3412 + enum 255 in 17s: 0 drifted (0 expected), 0 missing` / `PASS`** — the additive thermometer options changed nothing live (G3-345 is in the quick set at en / de / sv / fi d2). `i18n/strings.en.json` is regenerated by `build-en.js` and now carries K-356 beside the sibling entries (K-353 / K-354 / K-355 / G1-352) the reviewer commits together.

## Deviations from the design file (each measured or ruled)
1. **Key / file names `weather-symbols`** (Phase 0 ruling): `types/k/K-356-weather-symbols.js`, `components-b4/weather-symbols.js`, `qa/verify-b4-weather-symbols.js`, `data/b4/weather-symbols.js` (the locale bank; export `WEATHER_SYMBOLS` per the build prompt) + `data/b4/weather-symbols-global.json` (the design's `weather-symbols.json` + `weather.js`; renamed so the global file cannot shadow the bank module `b4-common` resolves by key). Stamps `data-lcs-weather-symbols`.
2. **Rule 4 storm ban scoped.** The design's "never the vocab thunderstorm form" would ban de Gewitter / fr orage / it temporale / nl onweer / pt tempestade / fi ukkonen — the design's own panel words, which ARE the vocab nouns (measured x11). The ban is applied for rain (raindrop) and snow (snowflake) everywhere and for storm only where the vocab form is a weather-compound of the storm noun (sv Åskväder · da Tordenvejr · no Tordenvær, `STORM_COMPOUND_LOCALES`); es "Tormenta eléctrica" is caught by the no-space rule. P7b proves both directions.
3. **Rule 9 "no authored field contains a neverPrint form" -> EQUALS.** The forecast asks carry the weather ADJECTIVE inflected by design ("Which day is rainy?", de "ist es bewölkt?"), so "contains" would fail the design's own literals; the gate bans a field that EQUALS an adjective form (case-insensitive) and the render check bans a printed tile equal to one.
4. **Rule 10 "adds one NOUN / verb, never only an adjective"** has no POS tagger: implemented as a per-mode token lexicon for en (write · chart | week · warm | cold | hot · water cycle · forecast) and, for a locale without a lexicon, ">= 1 token not in the base title" + pairwise token distinctness. P17 fires on the en lexicon.
5. **Water-cycle MIN_W 480 vs the 50 px marker gap** cannot both hold for the fifth marker (runoff sits 56.6 vb units from precipitation; 53.8 px at 608, 42.5 at 480). Kept both rules as designed: the primitive refuses the five-marker set below w ~566 (a d3 at 480 would throw, never overlap); the d2 four-marker set renders at any w >= 480.
6. **P12 re-targeted**: the pt full day names FIT the pill row (585.3 <= 639, measured), so "pt full names -> FAIL" is not a defect; the poison now proves the MEASUREMENT fires on a row that does overflow (753.9) while the pt full names pass. The pt `forecastDays` short forms stay optional data.
7. **Stack arithmetic per column**: the design's d3 "8 x 72 + 7 x 12 + 12 = 672" is the TILE column; the badge column is 6 x 84 + 60 = 564. The guard takes the taller column + 12 (d1 496 · d2 624 · d3 672 <= 677).
8. **Line zone floor 180** in `verify()` / the gate (design geometry 213, measured 206 / 220 / 226); `.ws-match-col` widths are set inline (badges `max(badge, 100)`, tiles 250) so the columns stack as designed.
9. **The 226 px width contingency** is enforced twice: `validateBank(block, loc, widths)` with `measureWords()` (Baloo 2 700 24, shell font) fails a wider word unless `refuse` names `'base'`; `verify(page)` also fails a rendered word wider than the tile inner. Never a smaller font.
10. Thermometer band edge ticks use a butt cap (a round cap would overhang the 44 px band by 1.5 px); the step ticks are untouched.
11. Titles / instructions under 70 / 150 cannot reach the README's 722 body in en (long chrome measured 710); the 722 and 677 budgets are therefore asserted by pinning, as the K-355 sibling does.

## PNGs for the reviewer
`out/dev/K-356-weather-d1-en.png` · `K-356-weather-d2-en.png` · `K-356-weather-d3-en.png` (LOOKED at all three: six round white badges with the six symbols, cream word signs across a clear run, one coral dot per item, nothing printed but the chrome; the snowflake keeps its star inside the teal ring; d3 shows `fog` / `wind` as symbol-less tiles) · `K-356-gate-d2-en-longchrome.png` (3-line title + 3-line instruction, still air below) · `K-356-gate-poison-PR1/PR2/PR20.png` · `K-356-water-cycle-w608.png` / `-w480.png` (the diagram with 5 / 4 markers). Contact sheet of the 16 candidate pictures at 160 px colour + 56 px greyscale (scratchpad `K-356-sheet.png`): every picture opened and read as the critic says — `sunny` bar rays a step darker than the disc, `cloudy` two-tone grey, `rainy`'s drops the darkest marks, `thunderstorm` the strongest in mono, `snowflake` the palest, `rainbow` a graded arch; `hot` a spiky sun, `cold` a penguin, `puddle` a pond (all OUT); scarf red, T-shirt green, sunglasses red (gear, F3 only).

## Open items for the faces / panels
- Phase 2 builds `_buildFace` for the five layouts on the components already shipped, adds the face render sections + PR3-PR11 / PR13-PR19 / PR22 to the gate; `verify-ruling-starters.js` needs no entry (the type has no starter text).
- The water-cycle land path (`… L 640 400 Z`, verbatim §2) strokes its bottom and right edges in teal while the water's bottom edge is unstroked; a human-eye item for the F4 build (a clip or an open path is a §2 change, not taken here).
- Panels (design §4 / OPEN 1-6): `symbolWords` (cloud singular / plural, OPEN 3), the stage-4 cycle literal (OPEN 4), the fi h1 (OPEN 2), the pt base title (OPEN 1), `diaryStart` for en / pt (OPEN 5), the F3 level key (OPEN 6); `forecastAsks` x6 with the weather word inflected and NO day name; the base word widths are measured by the gate at wave time (the only contingency touching the hub count, expected 0: fi `sateenkaari` is the widest candidate).
- en `distractors` `wind` / `fog` are d3-only data (unshipped); `bankWords` stays `{}`.
