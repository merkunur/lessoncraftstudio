# nt20-C BUILD — the last batch: 20 printable worksheet types × (1 base + 5 variations) × 11 locales, published and visible under their type on `/[locale]/worksheets`

## Context

The design phase closed 2026-09-14 (`22b49156`; ruling `c9376362`): 20 final design files in `docs/worksheet-gen/b3-designs/` (7 sections each; `_work/` pedagogy/design/critic beside them), the README with the 11-step recipe, 12 open items, the cross-type rulings and the hub-gate expectation matrix (design total ≈1,247 of 1,320 nominal; every gap is a recorded per-locale refusal, never a filler). The operator's goal now: **build the last batch of 20 worksheet types and their expansion** = execute that recipe end to end. The 20 files are the contract; selection, demand and design are NOT re-litigated. The operator's standing OBS: every face must show under its type in the `/worksheets` left sidebar — this batch makes that true by construction and adds a build-failing gate.

The batch spans several sessions. This plan is the working spine: README order (EN base → EN faces → 10 native panels → waves → generate → landings → publish → repoint → hub verify), the prerequisites the design session left (README items 2, 3, 9, 11, 12), the cross-type rulings, and the nt20-B / nt20-B-VAR traps. README item 1 is RULED (2026-09-14): "Free printable" stays in SEO metadata only; visible copy may not claim free; the build lints visible fields, never the bare word.

**Scale:** 20 families · 120 faces (20 base + 100 variations) · 11 locales · one d2 deck per shipped face per locale ≈ 1,247 decks + 1,247 landings. Variation ids from the free blocks K-325+ · G1-311+ · G2-320+ · G3-378+ (last shipped: K-316, G1-304, G2-314, G3-376).

**Measured 2026-09-14 (three read-only surveys + one design pass):** no `b3` file exists anywhere; `tools/gen-b2-probe-jobs.js` never existed (only its job-file outputs); `deck-html.js` has no `{U}`/`{L}` token resolution and live `strings.<loc>.json` carry no `{…}` tokens; `expandHubRows` lives in `frontend/lib/worksheets-sheets.ts`; neither existing hub gate is wired into `deploy.sh`; `publish-readiness.js` and `scan-duplicate-problems.js` are hard-bound to b2var paths; the `[publish] Slug:` grep is INSERT-only (UPDATE rows report `slug=` in `_results.txt`); `taxonomy.apps` has 94 entries (math 38 · letters 28 · spatial-reasoning 13 · logic 12 · science 3) and `picture-trail` is the one axis key without `apps` (the live demonstration of the rail defect); `b2-baseline.js` hashes only en/de/fi and a hand list that EXCLUDES the division consumers; the enumerator's theme round-robin index is the spec's position in `loadAllTypes()` and has SHIFTED since wave-008 (today G2-216 would enumerate fruits+vehicles; the published sv decks are animals+shapes) → any republish must PIN themes; seed replay reproduces the published calendar renders byte-for-byte and the only affected calendar deck is sv `kalendern-g2277` (weekLater 21 → "21:e"); division blast radius = 9 decks per locale (sv-only 9, full map 63).

## Non-negotiables carried into the build

- **Nothing is re-designed.** Each design file §2/§3 is the spec; §4/§5 the data + gate contract; §6 the SEO copy pattern; §7 the hub row expectation. README cross-type rulings override per-file naming (`unitAxis`; themeless picture index; texPool; `coordinate.mode` = face string / `'base'`, never null; body 722; `.ws-lane` 639; `blankNumeralBox`; `fillSlots`).
- **0 drift on everything already published**: `tools/b3-baseline.js` (Phase 0.0) captured after the accepted glyph fix is the release baseline; `--check` after every shared-file edit; 0 drift in both sections (build hashes + enumeration snapshot) is the release condition.
- **Never edit** `templates/components.js`, `templates/components-b2.js`, `image-vocabulary.js` (§10.3), `frontend/config/interactive-exercise-types.ts` (a printable key must be ABSENT — the gate asserts it).
- **Panels ≤4 in flight, locale-scoped scratch filenames, EN handed over as a SOURCE TO AUDIT** (`enAudit` in every draft); check on-disk drafts/specs before relaunching anything (session-limit trap).
- **Every gate poison-tested in both directions before it counts**; ratchets only shrink; never move a threshold to pass; a filtered run that matches nothing is not a pass (check exit status + summary line).
- **Themes off `cli.js generate --dry-run`, never a hand map**; `themeOverrides` is the ONLY source of the shipped theme (the landing composer reads it back; themed-but-unpinned aborts).
- **Description budget is a WINDOW (120–170)**; measure per locale with `tools/measure-instruction-window.js <staging-dir>`; short titles buy instruction room; `bandedDescription` picks the LONGEST middle that fits, so a too-short instruction loses to the family skill sentence.
- **Commits per phase with explicit paths**; `data/` and `out/` are gitignored → `git add -f` the generated `data/b3/*.js`; no `git add .`; push only when a phase needs a deploy; `bash deploy.sh > log 2>&1; echo EXIT=$?` (never `| tail`). Write every multi-line script to a file (backticks in `node -e` / heredocs eat text).
- **The `/worksheets` sidebar**: `apps.<key>` (+ legal `default_subject`) + `axes['exercise-type'].<key>.{slug,name}` ×11 (no en fallback in the rail) + one landing per SHIPPED face per locale with `coordinate.type === <key>` verbatim + commit + deploy (per-process `_fileCache`) + the new gate.

## Phase 0 — prerequisites (order: baseline → glyph fix → unitAxis → hub gate → deploy → republish → only then b3 code)

0.0 **`tools/b3-baseline.js`** (clone `b2-baseline.js`; `out/b3-baseline.json`): TYPES = ALL `loadAllTypes()` ids (every `types:"all"` wave shipped every spec in 11 locales), LOCALES = all 11, DIFFS 1–3 (~27k coords, ~7 min; `--quick` = en/de/sv/fi × d2 for the edit loop), plus a **section 2 enumeration snapshot**: sha1 of `deckId=seed` lines per `waves/*.json` (the build hash cannot see deckId/seed, which is exactly what `unitAxis` touches). `--check --expect=<file>` = the drift set must EQUAL the listed keys (either direction fails). Poison via an env hook that perturbs one bodyHtml / one seed → exactly 1 drift each; "NEEDLE MATCHED NOTHING" if the hook finds nothing. Commit, then `--capture`.

0.1 **Division glyph (C1)** — DECISION: apply the FULL per-locale map, not sv-only. `÷` was the minus sign in older sv/da/no notation (the defect); `:` is the school convention in de/it/nl/da/no/fi and is already the shipped ruling on G3-369 — an sv-only branch would be a second notation table. `×`→`·` (MUL_DOT) is NOT a defect and would touch ~100 more decks → recorded as a follow-up, not done. Files: NEW `types/_shared/notation.js` (`DIV_SLASH={sv}`, `DIV_COLON={de,it,nl,da,no,fi}`, `MUL_DOT`, `divGlyph(locale)`, `mulGlyph`); G3-369 lines 18-23/62-64 switch to it (same output → 0 drift); `array-tasks.js` L74 `build({theme, difficulty, locale}, ctx)` + sites 141/151/159 `OP(div)`; `number-line-tasks.js` L35 + site 95. `notation.test.js`: glyph per locale, G2-216/217, G3-310/311 bodies at d2 for sv/de/en contain `/` : `÷` and never `÷` for sv/da/no; poison = flip one set member. Acceptance: `b3-baseline --check --expect=` exactly the 189 keys {G2-216,217,G3-309,311} × {animals,fruits} × d1–3 × {sv,de,it,nl,da,no,fi} + G3-310 × nothm × d1–3 × 7, G3-369 absent → `--capture` again = the release baseline for everything after.

0.2 **`unitAxis` (A)** — the unit list comes from the SPEC, not the enumerator (banks differ per type): `unitAxis:{applicable, units(locale), exemplar(locale), tokens?(unit, locale)}`; `build({theme, difficulty, locale, unit})` with `unit || bank[loc].exemplar`. NEW `lib/unit-axis.js` (`unitKey` = slugify folded, throws on empty; `unitListFor(spec, plan, locale)` → `[null]` unless the spec has the axis AND the plan sets `unitsPerType`/`unitOverrides[id]`; override must be in `units(locale)`; override on an axis-less spec throws; knobs reaching no spec throw; `resolveUnitTokens(strings, spec, unit, locale)` for `{U}{L}{UNIT}`, same object returned when nothing to do; `assertNoUnitTokens`). `enumerate.js`: unit loop INSIDE the locale loop; `deckIdFor(..., variant, unit)` appends `u<unitKey>`; duplicate deckIds among unit instances throw. `lib/rng.js instanceSeed` appends `|u<unit>` only when set. **Tokens resolve at the STRINGS boundary** (`render-instance.js` + `cli.js` — the sheet title is printed by `page/shell.js` and stored in `manifest.title` before deck-html runs; resolving only in deck-html would ship `{U}` on the PDF); `deck-html.js` only asserts none remain. `cli.js`: `slotKey` folds the unit, `produce()` passes `unit` to `renderInstance` + `buildManifest`, the theme-retry keeps the unit. `emit/manifest.js`: `variant_id += '-' + unitKey` and a `unit` key ONLY when configured. Tests (`enumerate.test.js`, `lib/unit-axis.test.js`, cases in `manifest.test.js`/`deck-html.test.js`) with a fake spec: control (no knobs = unchanged deckId/seed), `unitsPerType:2`, override, bad override, axis-less override, folding collision, token resolution, unresolved-token throw, manifest shapes. Acceptance: `b3-baseline --check` 0 drift in BOTH sections; `generate --dry-run` on `wave-b2var-sv.json` byte-identical stdout. The nt20-C waves ship exemplars only (no `unitsPerType`); the per-unit fan is the later Search-Console lever.

0.3 **Hub gate (B)** — B1 `tools/export-hub-expectations.js` parses the README matrix rows (expected = first integer per cell, ceiling = last, notes = refusals column; controls `articles` 4, `word-tracing` 7, `letter-tracing` 6) → `docs/worksheet-gen/b3-designs/hub-expectations.json`; `--check` diffs README vs JSON. B2 `scripts/verify-hub-type-rows.js`: loader cloned from `verify-worksheets-hub-order.js` with `esModuleInterop` + an `@/` alias so the REAL `taxonomy.ts` (over an injectable taxonomy object), `applyLandingFilters` and `expandHubRows` run — no stubs; corpus = monolingual rows + facts map exactly as `hubRows()`; per (key × locale): count == expected, unique slugs, level ∈ the locale band map (LEVEL_KEYS from `gen-b2var-landings.js`), `canonicalDeckSlug` set, and for b3 keys ONLY `mode` a non-empty string (the b2 controls are `mode:null` by convention); per key: `apps[key]` exists, `default_subject ∈ listSubjectKeys()`, `exercise_type_axis_key === key`, the static rail condition `listSubjectKeys().some(s => exerciseTypeKeysForSubject(s).includes(key))`, `slug[loc]` + `name[loc]` ×11 via `getAxisName` (the no-fallback path), key ∉ interactive set. Exit 2 = refused (empty corpus / unknown key / empty expectations); `--warn-missing-keys` turns 0-row cells into WARN until b3 lands; non-vacuity line printed. `--poison` runs over a synthetic fixture key cloned from `articles` (stable before and after b3): 12 poisons (drop landing · delete apps · wrong type · mode null · missing name.fi · dup slug · bad level · empty canonical · key in interactive set · illegal subject · wrong axis key · empty sv corpus → must REFUSE), control passes, needle-matched-nothing detected. B3 `deploy.sh` after the letter-strokes gate: `node …/verify-hub-type-rows.js --warn-missing-keys || exit 1`; the flag is dropped at close-out. The SSR `?type=` link assertion stays in `verify-worksheets-hub-render.js` (dev server, Phase 8). The matrix carries counts, so the gate asserts counts + uniqueness; a slug-list mode fed from the composer output is a later addition.

0.4 **Republish (C2/C3)** — regenerate with committed PIN waves (id kept as `wave-00N` so `deckIdFor` reproduces the published basenames): per locale two files `waves/wave-00N-divfix-{a,b}.json` with `themeOverrides` = the PUBLISHED themes (a: G2-216 animals · G2-217 animals · G3-309 fruits · G3-310 · G3-311 shapes; b: shapes · fruits · vehicles · toys); dry-run first and assert the 9 deckIds equal the 9 published basenames; calendar `--wave waves/wave-b2-sv.json --types=G2-277 --force`. Back up the original ZIPs before `--force`; NEVER run `prune-stale-zips.js` with the pin waves. Evidence per ZIP: manifest differs only in `generated_at`; deck.html `<title>`/meta byte-equal (no `transplant-seo-head` needed); sv G2-277 render differs exactly `21:e`→`21:a`. On Hetzner per locale (sv first, then da/no, then de/it/nl/fi): stage under `/var/www/lcs-media/_staging/divfix-<loc>/`; record `readlink` of every slug (rollback); `publish-bulk --dry-run` WITHOUT a manifest → `_collisions.txt` IS the DB count (a ZIP with no collision was never published → remove it, never INSERT); manifest `{zip: existing slug}` from it → `--dry-run --updates-manifest` (0 collisions) → `--confirm --updates-manifest` → slugs from `_results.txt` → OG (`--slugs-file= --locales=<11>`) → alt-text / img-dims / lazy-deckend / topic-slash → hreflang `--confirm --locales=<11>` → `audit-deck-html --slugs-file=` → after push + deploy: `repoint-deck-canonical.js --types=arrays-multiplication,calendar,number-lines --locale=<loc>` (G3-310 may not be mapped — note, don't force) → `refresh-deck-noindex-exempt.sh` → `indexnow-submit.js <urls>` → curl + pdftotext proof (0 `÷` in the sv PDF; `21:a` in the calendar PDF). Commit the pin waves + close README items 11/12 with the counts.

0.5 **`data/b3/animal-facts.json`** (README item 9): ~36–40 animals × 7 fields (class · covering · diet · habitat · legs · fly · swim) authored by a content agent from G2-318 §5; `tools/gate-fact-file-data.js` cross-checks the five `data/science/*.json` banks (mismatch = FAIL; ambiguity = `null` = animal dropped; poison: one wrong `legs`). Panel-reviewed in Phase 4; without it only G2-318's base face ships.

0.6 **Visible-copy "free" lint** (item 1 as ruled): shared `scripts/lib/free-claim.js` cloned from `scripts/verify-activity-prose-claims.js` (`(?<!\p{L})…(?!\p{L})` PRICE + CARRIER collocations; bare `frei/vrij/fritt` never banned; MUST_FIRE/MUST_PASS both directions every run) used by `qa/lints.js` (rendered sheet text nodes: title strip, instruction, footer) and by `gen-b3-landings.js` pre-write validation + `gate.js` §4.B over `h1, eyebrow, strand, p1–p3` only. `title`/`metaDescription`/`topicMeta` stay exempt by ruling. Also the U+00AD soft-hyphen grep in `gate.js` (README item 12b; poison with one injected soft hyphen).

## Phase 1 — registration + EN base build (20 types)

1.1 `tools/register-b3-taxonomy.js` (clone `register-b2-taxonomy.js` — presence-idempotent): `apps.<key>` `{default_subject (each file §1), default_age_range by band, exercise_type_axis_key}` + `axes['exercise-type'].<key>.{slug.en, name.en}`. `tools/register-b3-en-content.js` (clone `register-b2-en-content.js`): `i18n/skill-sentences.en.json` (full 60–180 / short 15–90) + `frontend/messages/en.json topicMeta.<key>` (≥50) from each file §6. Run BEFORE the EN wave (`skillSentenceFor` falls back silently to `{}`). `verify-hub-type-rows.js --warn-missing-keys` = keys registered, rows 0 (expected until Phase 6).

1.2 Per type in README id order (K-317 → K-324, G1-305 → G1-310, G2-315 → G2-319, G3-377), from the file's §2 + §5:
- spec `types/<band>/<ID>-<slug>.js` (contract in `_SUBSTRATE.md`): `build({theme, difficulty, locale, unit}, {rng})` reads ONLY `data/b3/<key>.js[loc]` (+ `lib/b3-common.js`: `approvedSyllables(loc)` over the phonics output, `graphemeHunt`, `ordinalFor`; `lib/b3-picture-index.js` vocabKey→picture for themeless types; `lib/b3-instructions.js fillSlots` for `{obj}/{form}/{ordinal}` slots); `verify(page)` re-derives from `data-lcs-*`; answers never printed; `[data-ws-content]` stamped; themeless ⇒ `themeAxis:{applicable:false}`; unit-bearing ⇒ `unitAxis:{applicable:true, units, exemplar}`;
- `templates/components-b3.js`: ONLY components a file names NEW (`cardSheet/cutLines/wordPlate/…`, `blankNumeralBox`, …); new `primitives/<name>.js` only where a file says NEW;
- `data/b3/<key>.js` EN block hand-authored from §5 (module shape = `writeModule`: `const NAME = {en:{…}}; module.exports = {NAME}`);
- `qa/verify-b3-<key>.js`: every §5 poison FAILS, the EN bank is the control; element-size floors asserted here (K 56 / G1 44 / G23 36 — `qa/lints.js` has none);
- `node i18n/build-en.js` (band title uniqueness) · `render/one.js <ID> <theme> <d> en [unit]` at d1/d2/d3 · visual critic on every PNG · I read `qa/contact-sheet.js` output at the 722-px body with the longest title+instruction · `b3-baseline --check` after any shared-file touch.
Budget: 722 px body (not 760) or `minmax(<min>px,1fr)` rows; `.ws-lane` 639 unless the inline `padding:10px 12px` override is declared; font widths only inside the real pipeline (`render/one.js`), never a bare puppeteer page. One commit per type (spec + component additions + data + gate).

## Phase 2 — EN variation faces (100)

`tools/gen-b3var-specs.js` (clone `gen-b2var-specs.js`: `ROWS = [dir, id, fileSlug, baseFile, srcLevel, overrides, enTitle, enInstruction, extra?]`; `emit` spreads the base + `{id, slug, difficulty:{1:D,2:D,3:D}, i18n}`; `pruneStale`; duplicate id/slug throws). PARAM faces = ROWS; CODE faces = additive knobs in the base spec's `build`/`verify` (stamped only when declared) then a ROW referencing them — the b2var convention. Ids from the free blocks in file order; a face whose CONTENT sits in another band takes that band's id. `tools/gate-variation-distinct.js` parametrised (`--rows=<module> --base=<wave> --var=<wave>`, b2 defaults kept) so both batches are gated; resolved d2 config must differ from the base (the "five of thirteen were the base deck with a new theme" trap). `tools/gen-b3-probe-jobs.js` (NEW; `enumerate(plan)` → `[{type, theme, difficulty, locale, unit?}]` for `render/batch.js`). Render + critic + contact sheet per face. Per-locale refusals from each file §3 → `hub-expectations.json` (README matrix updated in the same commit if a face proves unbuildable). Commit.

## Phase 3 — EN wave + generate

`tools/gen-b3-waves.js` (clone `gen-b2var-waves.js`: THEME / EXCEPT / THEMELESS from the files' §1 per-locale rulings) → `waves/wave-b3-en.json` `{id:'wave-b3-en', seedEpoch:1, locales:['en'], themes, themesPerType:1, difficulties:[2], types:[shipped ids], themeOverrides:{every themed face}}`. `cli.js generate --wave … --dry-run` → read themes back → `generate` (QA gate: lints + verify, else no ZIP) → `scan-staged-desc-band.js` + `gate-deck-title-similarity.js` + `tools/scan-duplicate-problems.js` (parametrised for b3 ROWS/waves) → `tools/publish-readiness.js` (parametrised `--batch=b3`). Fold the EN defects BEFORE any panel inherits them.

## Phase 4 — the 10 native panels (base + faces together; ≤4 in flight)

`docs/worksheet-gen/b3-panel-brief.md` (clone `b2-panel-brief.md` + `b2var-panel-brief.md`: EN is a SOURCE TO AUDIT; quote numbers from the SHIPPED config; measure the description window per locale from the EN staging; no free-claims in visible copy; superlatives are the most dangerous sentence shape; every data bank each file §4/§5 names, with its validator rules). One 3-agent native panel per locale (linguist + K-3 educator + SEO) → `i18n/.draft-b3-<loc>.json`: `{locale, types:{120 × {title, instruction}}, families:{20 × {slug, name}}, skills:{20 × {full, short}}, topicMeta:{20}, banks:{<key>: …per file §5…}, strandNames:{Language?, Writing?, 'Reading: Informational Text'?}, enAudit:[…]}` → `tools/validate-b3-draft.js` (+ `.test.js`: EN-derived draft PASSES, ≥1 corruption per rule FAILS — every §5 rule of every file) → `tools/apply-b3-locale.js <loc|all>` (validate ALL before writing ANY; writes `i18n/strings.<loc>.json`, `skill-sentences.<loc>.json`, taxonomy slug/name, `messages/<loc>.json topicMeta`, `data/b3/*.js` locale blocks as GENERATED modules, additive `strand-names.ts` rows) → `i18n/lint-locale.js <loc>` = 0. Panel-confirmed refusals update `hub-expectations.json` + README matrix. Commit per locale (force-add `data/b3`). `strand-names.ts` gaps: Language da/no; Writing (en/de/es/pt only); Reading: Informational Text (en/de only) — panels author (README items 8, 10).

## Phase 5 — waves + generate ×10

`waves/wave-b3-<loc>.json` from `gen-b3-waves.js` → dry-run → generate → the four staged gates → `publish-readiness.js` 11/11 READY; `prune-stale-zips.js` after any re-pin (b3 waves only). Contact sheets read by me for de/fi/pt (longest chrome) at minimum; visual critic on all.

## Phase 6 — landings ×11

`docs/worksheet-gen/b3-landing-brief.md` (clone `b2var-landing-brief.md` + "quote numbers from the SHIPPED config" + the free-predication ban + "a landing describing a sibling resolves that sibling's own config") → `i18n/.landing-b3-<loc>.json` `{locale, landings:{<id>: {slug, eyebrow, h1, title, metaDescription, strand, p1, p2, p3}}}` (refused faces omitted) → `scripts/seo-landing/gen-b3-landings.js <loc> <file> [--dry-run]` (clone `gen-b2var-landings.js`: TYPES from the b3 ROWS + base specs; `STANDARD` per id — CCSS en only / national framework NAME elsewhere / `null` for readiness faces per §6; `LEVEL_KEYS` per locale incl. `no` +1; `deckSlugFor = <famSlug>[-<themeSlug>]-<idlower>` with the theme from `wave-b3-<loc>.json themeOverrides` (+ `-<unitKey>` only when a unit is pinned); **`coordinate.mode` = the face mode string, `'base'` for the base — never null**; themeless `theme:''`; refuses <200 words / meta outside 120–170 / title >75 / free-claims in visible fields; idempotent by slug) → `scripts/seo-landing/gate.js <loc>.json` 0 FAIL on BOTH sections → `verify-hub-type-rows.js` hard mode on the on-disk corpus → commit `frontend/content/seo-landing/<loc>.json` + the draft.

## Phase 7 — publish ×11 (Hetzner; env `cd frontend && set -a && source .env.production && set +a`)

Per locale: stage under `/var/www/lcs-media/_staging/b3-<loc>/` (never /tmp) → `index.js publish-bulk <dir> --dry-run` (ok = shipped count, collisions 0) → `--confirm` → slugs from `grep "^\[publish\] Slug: " _confirm.log` → `regenerate-og-images.js --slugs-file=<f> --locales=<all 11>` (`=` form; default is en,es,pt!) → `populate-and-inject-hreflang.js --confirm --locales=<all 11>` → `audit-deck-html.js --slugs-file=<f> --locales=<loc>` (N/N). Commit + push landings → `deploy.sh` (the hub gate runs in it) → `repoint-deck-canonical.js --types=<20 families> --locale=<loc>` (`--k=v` form only) → `refresh-deck-noindex-exempt.sh` → `indexnow-submit.js <urls…>`. Live check after the Cloudflare 300-s TTL.

## Phase 8 — hub verification (the OBS) + close-out

`node scripts/verify-hub-type-rows.js` (hard; drop `--warn-missing-keys` from `deploy.sh`) + `verify-worksheets-hub-order.js` + `verify-worksheets-hub-render.js --base=<dev server> --locales=<11>` (asserts every corpus type has a `?type=` link); then `https://www.lessoncraftstudio.com/<loc>/worksheets?type=<key>` for de, fi and one Romance locale in a real browser on the **All** tab (printables are absent under Interactive by design). Close-out: memory `project_nt20c_worksheet_types.md` + MEMORY.md pointer, CLAUDE.md §14.10 family count 95 → 115 + §25 status, honest click math (the panels' 1,500–3,000/day at maturity is the claim on record; measured later from Search Console, never asserted). Follow-up recorded, not done: MUL_DOT (`×`→`·` in de/sv/da/no/fi, ~100 decks).

## Session discipline

Every session end: commit the phase's explicit paths; write a RESUME pointer (phase · type · locale · last commit · what is on disk in flight) into `project_nt20c_design.md` (renamed `project_nt20c_worksheet_types.md` at Phase 1 start) and refresh the MEMORY.md line. Every session start: read the pointer, `git log --oneline -5`, check on-disk drafts/specs/ZIPs before launching agents.

## Verification (end-to-end)

- `b3-baseline --check` 0 drift in both sections against the baseline re-captured after C1 (the only accepted drift ever = the 189 division coordinates, listed in the `--expect` file and the memory record).
- Gates: every `qa/verify-b3-*.js` poison FAILS with the EN bank as control; `gate-variation-distinct.js` 100/100 b3 faces distinct at d2 (b2 rows still pass); `validate-b3-draft.test.js` both directions; `verify-hub-type-rows.js --poison` 12/12 killed and the b2var control keys pass; `free-claim.js` MUST_FIRE/MUST_PASS both directions per locale; `unit-axis`/`enumerate` tests green.
- Per locale: `lint-locale` 0; desc band N/N in 120–170; title similarity 0 FAIL; duplicate problems 0; `publish-readiness` READY; `gate.js` 0 FAIL; `audit-deck-html` N/N; live `curl` 200 on a sample landing + deck.
- Republish proof: pdftotext of the sv division PDFs contains 0 `÷`; `kalendern-g2277` PDF contains `21:a`; canonicals repointed; `_results.txt` routed=UPDATE for every ZIP.
- Hub: expected rows per (key × locale) match the hub's own selection in all 11 locales; the rail carries `?type=<key>` for all 20 keys; real-browser check on the All tab.
