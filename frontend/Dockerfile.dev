FROM node:18-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Clean install dependencies with exact versions
RUN rm -rf node_modules package-lock.json && npm install --save-exact

# Copy source code
COPY . .

# Expose port
EXPOSE 3000

# Start development server without Turbopack
CMD ["sh", "-c", "npm run dev"]