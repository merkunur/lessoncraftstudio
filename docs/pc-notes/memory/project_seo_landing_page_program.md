---
name: seo-landing-page-program
description: SEO/Landing-Page Program — tier-3 deck landing pages (hub→landing→asset). 🏁 EN ARC COMPLETE — pilot+Waves 1-8 LIVE+verified+CLOSED, 1812 EN landings, audit-clean (f39a3a9e). Two subjects (math+ELA), four grade bands (Preschool/K/Gr1/Gr2 — the complete level axis), all CCSS strands + the consolidated readiness class, three alignment shapes, the de-orphan (first live-query hub change, durable), and a similarity discipline that held through every coupled slice / grade boundary / natural-floor / constraint-bound-floor without a gate failure surviving to ship. Wave 8 (Gr2) = math-puzzle 33 + code-addition 50 + crossword 44 = 127 (2.NBT.B.5 NBT + L.2.2.d Language/ELA; e7cc0bd6). NEXT PHASE = the locale fan-out (de→es→nl→fr→it→pt→sv→da→no/fi-last; native ensembles engage per-locale for the FIRST time — each authors its own compat booleans + P1 skeletons + native keyword research + strand/label localization). Full doctrine in CLAUDE.md §22 + blow-by-blow in the plan file.
metadata: 
  node_type: memory
  type: project
  originSessionId: 83928681-a8e9-4319-bc2c-e402bd05f065
---

Tier-3 **deck landing pages** that fix the thin/orphan deck.html surface: new SSR route **`/[locale]/worksheets/[slug]`** (hub → landing → asset). Full blow-by-blow record: `C:\Users\rkgen\.claude\plans\commission-to-cc-structured-hollerith.md` (Phases 1–5 + Fan-out Planning Parts 1–3 + Wave-1 Execution, all RULED) — READ IT before resuming.

**[NEXT PHASE STARTED 2026-06-12] the 8-locale rollout is live — `sv` COMPLETE (lead Nordic, 27 types / 2478 landings), `nl` NEXT.** The per-locale fan playbook + sv ledger-lock rulings + the Nordic definiteness render engine + the deploy gotchas live in [[project-8locale-landing-rollout]] (SoT plan file `commission-to-cc-swirling-bengio.md`). This file remains the EN-arc + es/en/de record.

**STATE (2026-06-08): 🏁 THE EN ARC IS COMPLETE — all 8 EN waves LIVE + verified + CLOSED → 1812 EN landings, audit-clean.** The full EN landing corpus is authored + gated + shipped + verified live, with no regression. Wave roll-up: **W1** addition/img-img·K (43) → **W1b** +img-num+mixed (144) → **W2** subtraction(cross-out+img-num)+chart-count·K (`ab11441e`/`310a1911` — no-targetUrl ratified) → **W3** Math→Grade-1 (142 Gr1; `fe8c1b9d`; per-entry level-wiring + ≤N quantity-ceiling instrument debuted) → **W4** Literacy·K, FIRST cross-strand (162; `bfa35d7a`; "Language"+Reading-Foundational strands) → **W5** Logic·K, LARGEST wave (423; `c8d4aa5f` + de-orphan `baa89b1f`→`04bfcbee` — the program's FIRST live-query hub change) → **W6** Spatial·K (392, all-readiness; `f669f448`) → **W7** Readiness+Preschool, FIRST sub-K band (285; `16fe8a37`) → **W8** Gr2, the FINAL EN wave (127; `e7cc0bd6`). **Wave 8 specifics:** math-puzzle Gr2 33 + code-addition Gr2 50 (null+secret-word) + crossword 44; all-standard — **2.NBT.B.5** (NEW NBT strand "Number & Operations in Base Ten", reused from 2.NBT activities) + **L.2.2.d** (Language/ELA, reused from W4); **four-band level axis COMPLETE** (Preschool 3-4 → K 5-6 → Gr1 6-7 → Gr2 7-8) + **four-band regression PASS** on rendered DOM; **Gr1-vs-Gr2 same-type coexistence LEAK=0**; boundaries math-puzzle Gr2↔Gr1 0.040 / code-addition 0.062 / crossword↔word-guess 0.068 (grade-saturates-copy); **constraint-bound floor** null↔secret-word 0.230 (within-100 fence forces shared vocab — accepted); **full-corpus re-grade** (check-sum-ceiling, NOT the breacher record — vindicated twice: 33 not 30, 50 not 41, 0 Gr3); repoint 212/1194/0; 0-protected-core. **Landing-presence audit (`f39a3a9e`, 2026-06-08, all 7 flagged types: chart-count/cryptogram/math-worksheet/picture-sort/treasure-hunt/word-scramble/wordsearch) = CLEAN, no regression** — chart-count 43 + picture-sort 40 live + binding (LEAK=0); the other 5 correctly landing-absent by ruling. **NEXT PHASE = the locale fan-out** (de→es→nl→fr→it→pt→sv→da→no/fi-last) — the native ensembles engage **per-locale for the FIRST time in the landing program** (each locale authors its OWN compat booleans + P1 skeletons + native keyword research + per-locale strand/label localization, never before). de first; gets its own framing when opened.

**TRUE INVENTORY = ≈22,419** generable valid landing coordinates (NOT 24,494; NOT 28,562 decks). Per-locale net level buckets in the plan file P3.a.

**LOCKED doctrine (don't re-litigate):**
- **Canonical = option (b):** landing self-canonicals (Next SSR); each deck.html repoints its baked `__CANONICAL_URL__` → its landing (string-sub, NO nginx change). PDFs stay noindex.
- **Hub→landing = landing-existence-conditional repoint** (UNIVERSAL hub mechanism): `deckLinkFor` → `/worksheets/<landing>` IFF a published landing exists (`landingSlugForDeck` reverse map), else `/decks/`. Auto-bounds the live `/topic/` mutation to the published wave; rollback = unpublish (fails closed) or git-revert. 3 sites: `VarietyStrip.tsx` + `topic/[slug]/page.tsx` + `topic/[slug]/[secondary]/page.tsx`.
- **Grade comes from the MECHANIC not the DB tag** — ledger-level-override at **(type,mode)** granularity. **math-worksheet REMOVED** (Gr3-6 algebra, above K-2). **Readiness-class policy:** visual-spatial puzzles (grid-match/missing-pieces/bingo-image/sudoku/picture-path/shadow-match/treasure-hunt-maze) fan out with NO `educationalAlignment`; `teaches` = honest readiness strand.
- **theme×mechanic compatibility = property table** (`theme-mechanic-compat.json`) + the ONE pre-render `validity-gate.js` (blocks body_parts+sum, "a tray of buns"+count). Countability gate only on count/sum/size mechanics.
- **Cell-space floor (CORRECTED at Wave 1b; supersedes "≥4 / 6–8 skeletons"):** the floor is NOT a skeleton count — **cell space (skeletons × P2 variants) must STRICTLY exceed a class's theme breadth, with a non-periodic/coprime assignment** (no two themes share a (skeleton,P2) cell while empty cells remain). A periodic `i mod N` index clusters collisions once cells ≤ themes (the 8×4=32<50 dead-zone → two at-the-line FAILs); `gen-wave1b.js` enforces via coprime-stride bijection + runtime guard (warns cells ≤ themes). mixed shipped 8×7=56>50, max 0.489. **Exception: image-number grandfathered-by-gate** (6×4=24<50 but passes at 0.783 via coprime scatter; held, trigger = actual FAIL ≥0.80). **Similarity gate** = whole-page RAW Jaccard 0.80 FAIL / 0.65 WARN (`gate.js`, multi-cluster) — **0.80 held program-wide, gauge never moved**; slot-norm cross-class only (≥0.90 defect).
- **No-targetUrl AlignmentObject (program-wide, all locales/waves):** standard-bearing landings emit `{AlignmentObject, alignmentType:'educationalSubject', targetName:<CCSS code>, educationalFramework:'Common Core State Standards'}` — **NO targetUrl** (corestandards 404s; targetName is the anchor). §20.10: code in JSON-LD only, framework in prose, "Common Core" chip; strand-only → dashed "coming soon".
- **≤N quantity-ceiling spot-check (REQUIRED at quantity-defined grade boundaries):** theme×mechanic validity is necessary-not-sufficient where a *generated* quantity sets the grade. Check per-coordinate against the **child-seen quantity wherever it lives** (manifest / generation.json / rendered deck.html sum-slot — code-addition values are render-time, only in deck.html). **"Within N" = operands AND results** (strict; "24−14" is Gr2). Caps K≤10/Gr1≤20/Gr2≤100|≤20-fluency. Instrument `check-sum-ceiling.js` (4 shapes: minuend+result / minuend-only / multi-addend running-sum / deck.html sum-slot). Caught **71 Gr2 coords** invisible to DB-tag + ledger + similarity gate.
- **Grade-split-family:** same (type,mode) spans grades (math-puzzle 12 Gr1 + 30 Gr2; code-addition 44 Gr1 + 41 Gr2). Grade set per-coordinate by quantities → **level axis + conditional repoint both bound per-coordinate** (proven live). 71 Gr2-candidates recorded out-of-tree at W3 (`~/.claude/plans/wave3-mathpuzzle-gr2-breachers.md`, max-sums) — **now SHIPPED in Wave 8**: the full-corpus re-grade re-derived the authoritative Gr2 set by quantity (math-puzzle 33, code-addition 50 — MORE than the recorded 71 combined; the record was a point-in-time exclusion artifact, now consumed). A mechanic resolving off-grade (find-addend→K) is REMOVED, not force-fit.
- **Per-entry level-wiring (debuted W3, regression-proven):** route `LEVELS` map → "Grade N" chip + JSON-LD educationalLevel/typicalAgeRange from `coordinate.level`, **K-default-safe**. K-stays-Kindergarten regression is a PERMANENT execution check whenever the level map is touched.
- **Native review tiers:** T1 (author+review the template-CLASS prose system) / T2 (=8 stratified renders/class) / T3 (automated). No MT step anywhere.
- **Per-wave source-read LEDGER-LOCK before authoring any type, never the slug; interrupt-and-surface on any grade surprise.** Ledger: `docs/seo-landing/mechanic-ledger-mathK.md` (+ Part-2 STEP-A locks for bingo/prepositions/grid-match/missing-pieces).

**Wave sequence (plan P3.b):** 🏁 **EN waves 1→8 ALL LIVE/CLOSED** (1 addition-img-img/K=43 → 1b +img-num+mixed=144 → 2 subtraction+chart-count → 3 Math-Gr1 → 4 Literacy→K → 5 Logic **+de-orphan** → 6 Spatial → 7 Readiness+Preschool → 8 Gr2). **ACTIVE NEXT PHASE = the locale fan-out: `de` (first) → es→nl→fr→it→pt→sv→da→no/fi-last** — each locale's **native ensemble** authors its OWN compat booleans + P1 skeletons + native keyword research + per-locale strand/label localization AT ENTRY (the first per-locale-ensemble engagement in the landing program; operator may reorder the middle eight on market priority). The de-orphan (global query, own verify/rollback) shipped at wave 5 and is durable (proven across W6/W7/W8).

**Key files:** route `frontend/app/[locale]/worksheets/[slug]/page.tsx` · lib `frontend/lib/seo/landing-content.ts` (`landingSlugForDeck`, `deckAssets`) · content `frontend/content/seo-landing/en.json` · generators `scripts/seo-landing/{gen-wave1.js,gen-wave1b.js}` · gates `scripts/seo-landing/{gate.js,validity-gate.js}` · compat `frontend/content/seo-landing/theme-mechanic-compat.json` · coordinate enumerator `scripts/seo-landing/enum-wave1b-coords.js` + manifest `wave1b-coordinates.json` · hub verifiers `scripts/seo-landing/{.verify-wave1-hub.js, verify-hub-autobind.js}` (the latter = standing harness, en.json-driven, `--type` arg) · preview hub `frontend/app/[locale]/preview/landing-hub/page.tsx` · ledger `docs/seo-landing/mechanic-ledger-mathK.md`.

**NEXT-WAVE RUNBOOK:** (1) ledger-lock any new types (source-read, interrupt on surprise) **+ cell-space pre-check** (report theme breadth + skel×P2 cells; never author where cells ≤ themes); (2) per-locale compat booleans + P1 skeletons if a new locale; (3) extend the `gen-wave1b.js`-style generator → author + `gate.js` clean (≥200w, 0 FAIL, multi-cluster) + `validity-gate.js` (0 invalid); (4) ship landings (new URLs) + repoint deck.html canonicals (quote-terminated page-URL only; og:image/PDF intact; `.precanonical-bak`); (5) the conditional repoint auto-applies (no code change after wave 1); (6) verify the rendered DOM — topic grid is hydration-gated → use `scripts/seo-landing/verify-hub-autobind.js` (NOT curl, §A.13.50); (7) STOP for operator ruling + eyeball.

**Commits:** pilot `32e9af1e`/`941c9501`/`8158fef9`; Part-2 `822dcc54`; Wave 1 `d2f12145`→`51ae864c`→`67a8ae18`; **Wave 1b `a681d48d`** + housekeeping `616a5a9a` (cell-space floor §22.1); **Wave 2** `ab11441e` + `310a1911` (no-targetUrl); **Wave 3 `fe8c1b9d`** (142 Gr1 + per-entry level wiring + ≤N quantity-ceiling instrument `check-sum-ceiling.js`); §22 fold `76a0cec6`; **Wave 4 `bfa35d7a`** (162 Literacy·K); **Wave 5** `c8d4aa5f` + de-orphan `baa89b1f`→`04bfcbee` (423 Logic·K; de-orphan `themeSubjectTagsWhere` in `topic-decks.ts` + verifier `verify-deorphan.js`); **Wave 6 `f669f448`** (392 Spatial·K); **Wave 7 `16fe8a37`** (285 Readiness+Preschool); **Wave 8 `e7cc0bd6`** (127 Gr2 — the EN arc's LAST wave; Gr2 LEVELS-add + 2.NBT.B.5 NBT + L.2.2.d Language); **§22 doctrine folds `e4c4dc37`** (5 Wave-8 items) + **audit `f39a3a9e`** (landing-presence audit + 4th carry-forward category). Standing tooling committed: `check-sum-ceiling.js`, `repoint-deck-canonical.js`, `verify-hub-autobind.js`, `verify-deorphan.js`, per-wave gen/enum scripts.

**Wave 5→8 doctrine (folded to CLAUDE.md §22.1/§22.2 — read there, not duplicated here):** de-orphan (`hasSome`-over-exact-`-vs-`-component-keys); operation-vs-attribute natural floor + the **constraint-bound floor** that refines it; **grade-saturates-copy** (same-type-diff-grade fences cleaner than same-grade-diff-mechanic); **within-grade-coupling vs cross-grade-differentiation**; **full-corpus re-grade over the breacher-record**; the **FOUR carry-forward categories** = (a) grade-deferred [Gr1 word-scramble/wordsearch] · (b) breadth-non-viable [cryptogram themeless, treasure-hunt ≤2-theme] · (c) browse-hub defect [`/en/topic/sudoku` 404] · (d) **content-expansion surface** [chart-count ~6 non-Wave-2 themes + picture-sort 54 uncapped `-vs-` pairs — benign expansion option, never a fix].

Cross-refs: [[content-publishing-seo-standard]] · [[native-slug-migration-and-mu-gap]] · [[thin-page-remediation]] (this program is the structural fix for the thin-deck problem that file flagged).


---

## 🏁 3-LOCALE COMPLETE + KEYWORD RE-KEY — CLOSED 2026-06-11

es + en + de are now **complete on both controllable axes**: every deck type landed AND every title+meta keyword-re-keyed to harvested demand. **es 1927 (`c0eb3650`) / en 2005 (`d3cff15f`) / de 1972 (`f1bd2550`) = ~5,904 pages, 0 dup-titles per locale.**

### Reopened-5 (the deck types deferred during the wave build) — resolved per locale
The operator reopened the 5 DON'T-LAND/deferred types (each must land OR be proven-unlandable on RENDERED content, not the deferral label):
- **math-worksheet** — LANDED. The "Gr3-6 algebra removal" premise was WRONG: rendered = symbol-substitution multi-term add/sub, operands ≤9-20, results ≤35-49, 0 over 100. Re-graded PER-DECK via a ceiling+subtraction sweep: add-only ≤20 → primer/1.OA.A.2; subtraction-present or >20 → segundo/2.OA.A.1 (es 14 primer/36 segundo; en/de all 2.OA.A.1).
- **word-scramble + wordsearch** — LANDED (literacy/Lenguajes; L.K.2.d / RF.K.3.c, primer/1.Klasse). Blocker was authoring cost, not a wall. es-first (no false hreflang sibling).
- **treasure-hunt** — LANDED via THUMBNAIL RECOVERY (the app-bug below).
- **cryptogram** — PROVEN WALL, UNLANDED, logged: rendered = decode a random sentence ("Mi mamá es bonita"), 0 images, no theme → no per-theme query exists.

### treasure-hunt APP-BUG (es + en) — generator wrote one theme for all
The treasure-hunt app (v32.0.1) wrote a SINGLE theme into every deck's manifest+slug+title (es=accessories×96, en=4th_of_july×98) — the operator's real per-deck themes survive ONLY in the rendered thumbnail JPEG (proven: deck 0e3f manifest=accessories but thumbnail=vegetables). **Recovery:** 6 agents read all ~97-99 thumbnails per locale via the Read tool (it sees images) → identified ~46-48 real themes → clean theme-matched landing slug (`busqueda-del-tesoro-<theme>` / `treasure-hunt-<theme>`) with `canonicalDeckSlug`→the real (mis-slugged) deck. **de treasure-hunt manifest was CLEAN** (48 distinct themes) — built directly, no recovery. The deck-side app theme-emit fix (so future treasure-hunt decks record the theme) for es+en remains a separate §10.3 task; the landings ship correct now. LESSON: when a theme field reads empty/uniform, the extraction is wrong (decks always have a theme) — the rendered thumbnail is ground truth.

### Keyword re-key engines + locked patterns (DATA-ONLY title+meta; H1/body/coordinate untouched)
`scripts/seo-landing/rekey-es-titles.js` + `rekey-en-titles.js` + `rekey-de-titles.js` (CLI `--type=`/`--all`/`--dry-run`; two-pass: resolve op/grade/qual/theme+identity → ordinal disambiguation by slug order → build+mutate; op may be a function for multi-mode; length-fallback suffix ladder, qual NEVER dropped; themeDisplay handles "-vs-" split + null guard). Titles/metas were null in en/de (route used h1/p1) → the re-key SETS them; the route renders `<title>=l.title||l.h1`, meta=`l.metaDescription||first-sentence-of-p1`, so re-keying SERP title+meta leaves the on-page H1 theme-warm.
- **es (grade-led):** `[operación] para [Grado] [qualifier] – [Tema] | para imprimir PDF`
- **en (grade-led):** `[Type] Worksheets for [Grade] [qualifier] – [Theme] | Free Printable PDF`
- **de (RANGE-LED, structurally distinct):** numeric `Arbeitsblätter [op] bis [N] – [Thema] | zum Ausdrucken PDF kostenlos`; non-numeric (no range) `Arbeitsblätter [op] – [Thema] | …`. de meta uses em-dash before theme (avoids the "mit Tiere"→"mit Tieren" dative slip).
- **honest-fit gating (the one rule that mattered):** the numeric qualifier appears ONLY where content supports it — es "sin llevar" / en "without regrouping"(add)·"without borrowing"(sub) / de "ohne Zehnerübergang" ALL gated to within-10 no-carry (K.OA.A.2); de **bis-N gated to the deck's band** (K.OA.A.2→bis 10, 1.OA→bis 20, 2.NBT→bis 100; non-numeric types carry NO false range); grade gated to the ledger band. find-addend → "missing number"/"con número faltante" (no false no-carry term).
- **multi-mode distinguishers + ordinal:** dropping the mode would have collapsed 828 es decks (6 multi-mode types) to dup-titles — the gold titles had per-mode distinguishers; the re-key restores them (addition/subtraction modes, pattern-train AB/AAB/ABB/AABB/ABC, matching letter/name, big-small, etc.) + an ordinal pass for variant siblings (§A.13.17). This ALSO fixed the 47 pre-existing en addition h1-dups for free. 0 dup-titles per locale.
- **length trade (standing):** keyword lead within Google's ~60 display, Google indexes full title; honest-distinguisher overflow accepted (don't truncate the keyword base). en had one tighten ("with pictures and numbers"→"pictures & numbers", "with a missing number"→"missing number"). German compounds run longest (de max 83, accepted).
- **lightweight deploy:** title+meta only → no deck.html / og / canonical / repoint change.

### NEXT (operator's call — do NOT auto-open)
1. **Remaining 8 languages** (nl/fr/it/pt/sv/da/no/fi): each needs its OWN ~20-min autocomplete harvest (right regional Google) BEFORE its build, so it's born keyword-correct (no retrofit — the es/en/de lesson). Harvests in seo-keyword-strategy.md.
2. **Tracing/handwriting deck-PRODUCTION** (grafomotricidad/Schwungübungen/tracing): 3-locale-validated demand, strongest content signal — a deck-creation product decision, NOT a landing task (no decks exist; writing/drawing-lines apps are PDF-only, outside the 29-app catalog).