# G1-352 `pronouns` : EN BASE build record (2026-09-21)

Built from `G1-352-pronouns.md` (§2 base, §5 data + gates; §3 read for the additive `layout` knob), `_SUBSTRATE.md`, the README "Cross-type rulings" and `_work/G1-352-critic.md` (rulings 8-12, 16-19 applied). Exemplars read: `types/g2/G2-317-verb-forms.js`, `templates/components-b3/verb-forms.js`, `types/k/K-288-articles.js` (chip strips + `okMix` + the position-leak verify), `qa/verify-b3-opposites.js` (the gate shape). No em-dashes.

## Files (all type-scoped; nothing shared edited; `node tools/b3-baseline.js --check --quick` PASS)

| file | what |
|---|---|
| `scripts/worksheet-gen/types/g1/G1-352-pronouns.js` | the spec: exerciseType `pronouns`, themeless, `unitAxis:{applicable:false}`, d1 / **d2 (ships)** / d3 (unpublished), `build` -> `_buildWith(bank, d, …)` (the gate's injection seam) -> `resolveBase` (locale CLASS from the bank: 3-chip incl. fi / 4-chip via `d.four` / objects via `objectMap`) -> `compose` (the deal: sex split -> singles' portraits -> pairs' types + portraits -> card order -> objects -> names LAST) -> `portraitCard` x cards -> `cardGrid` post-processed to `padding:10px 12px` (the K-354 idiom). Pure helpers exported for the gate: `keyOf`, `answerChip` (`obj:<code>` keys read `objectMap`), `fillSubject` (+ es `andBefore`), `need`, `resolveBase`, `localeClass`, `collapsesPairs`, `compose`. `verify(page)` = the base branch (one correct chip === chip-key, K-288 position-leak + identical-style, plate === names joined and never a chip label, portraits per key, no name / src / plate twice, stage overflow 0, floors 44 / plate 16, mixFloor from the root stamp, no stray text). `d.layout` set -> `_buildFace` THROWS "Phase 2" (dispatched before the RNG is touched; the base stays byte-identical). |
| `scripts/worksheet-gen/data/b4/pronouns.js` | `{ PRONOUNS: { en: {…} } }`: `people` (the ONE 32-portrait bank, m 20 / f 12, per-picture `minPx` 44|56, `picOpened:true`, pinned by theme + noun), `names` 12 (the en 8 tagged 4 f / 4 m + Zoe Ava f + Jack Eli m), `and`/`andBefore`, `chips`/`initial`, `map` x5 faces, `objectMap:null`, `objects:[]`, 12 `frames` {sg, pl}, 8 `anaphora` (contentNeutral), `possessive` (his/her/their, `byOwner`, frame sg + pl, 12 things opened, `forms:null`, `artTable:null`), `refuse:{possessive:false}`, `strings` x6 faces (base === the spec's i18n.en). `data/` is gitignored: the reviewer `git add -f`. |
| `scripts/worksheet-gen/templates/components-b4/pronouns.js` | the ten NEW components of §2, exported by exactly those names: `portraitStrip`, `namePlate`, `pronounChips`, `portraitCard` (base) + `nameGapRow`, `initialBank`, `anaphoraBlock`, `ownerLane`, `nameCardBins`, `rewriteLane` (the faces; built to the §2 contracts incl. every throw, smoke-rendered by the gate, NOT consumed by any spec yet). Barrel `templates/components-b4.js` loads them beside five-senses' file with 0 duplicate names. |
| `scripts/worksheet-gen/qa/verify-b4-pronouns.js` | the gate; exports `validateBank(block, loc, opts)` (rules 1-13; `tools/b4-probe-child.js` calls it as `(block, loc)`), `crossCheck`, `OPENED`, `EXCLUDED`, `TABLE_B`. |
| `scripts/worksheet-gen/i18n/strings.en.json` | regenerated by `node i18n/build-en.js` (602 types, title lint clean). |

## Pictures OPENED (the human gate)

All 32 portraits on two contact sheets built with sharp from `cache/themes/<theme>/<noun>@3x.webp` and READ at **64 px** (the base single size) and **160 px**: every one shows the sex the design tagged; the four no-vocab portraits (`police_officer`, `construction_worker`, `flight_attendant`, `mail_carrier_2`) resolve through `fileUri` (manifest entries exist, `themes-512` mirrors them). `minPx` kept per picture as the design opened them (the eight full figures at 56 read at 64; `coach` / `detective` / `student` are boys, `girl` a girl). The 12 possessive things (toys/ball kite doll robot train car, music/drum, classroom/book, accessories/hat, desserts and sweets/cake, pets/dog cat) read at 120 px as what they say. The record lives in the gate's `OPENED` table (rule 1 refuses any retag / add / drop, poisoned both ways).

## Deviations from the design file (each measured)

1. **The "722" chrome measures 710.** A 3-line title (73 chars, 99 px) + a 3-line instruction (173 chars, 69 px) leave **710 px** of body (the G2-317 finding again; the 733 the design's arithmetic assumes is a 3-line title + a 2-line instruction). At 710 the card is 167, inner **143** (`padding:10px 12px` + border 2 x 2), and the design's stage 64 + 3 + 26 + 3 + 4 + 44 = **144 clips 1 px**. **Re-budgeted: `.ws-card-stage` gap 3 -> 2 and the chip wrapper padding-top 4 -> 2 = 140** (pair 128); measured content 140 / 128 inside 143, overflow 0, the chips still 44, the plate 26, the portrait 64. `portraitCard` takes `gap` / `chipPad` (defaults 2 / 2); `pronounChips(padTop)` keeps 2 for F3 as designed. Every quoted stack below is at the measured 710.
2. **The base at 677 (a 4-line title + 3-line instruction) does NOT fit** as the design predicted: card 158.75, inner 135, content 140 -> stage overflow 2-5 px -> `verify()` FAILS (the gate's PR8 asserts that failure; rule 13 caps the fi base + F1 titles at <= 3 lines; the proposed "Hän vai he?" / "Korvaa nimi pronominilla" measure 1 line each). NB: the 4-line fixture needed **74 chars** of long Finnish words; a 70-char fi title can still wrap to 4 (rule 13 exists for exactly that and is measured, never char-counted).
3. **d3 (unpublished by design) is also UNDEALABLE as laddered**: 6 singles + 4 pairs need 14 names > the 12 tagged (the 4-chip 5 + 5 needs 15). The only 10-card deal under 12 names is **8 singles + 2 pairs** (`d3.singles:8, pairs:2`; `four` likewise) so the level builds; at 710 its stage (52 + 2 + 24 + 2 + 2 + 44 = 126) overflows the inner 111 by 7 px and `verify()` reports it on all 10 cards, which the gate asserts is d3's ONLY failure. d3 stays unpublished; no copy describes d1 / d3.
4. **d1 chips 52 high**: the design's `pronounChips` contract fixes `h:44` while the d1 ladder says `chipH 52`; `pronounChips` takes `h` (>= 44, else throws) so d1 renders the ladder's 96 / 52 / 26. d1 stack at 710: card 222.7, inner 198.7, content 80 + 2 + 28 + 2 + 2 + 52 = 166 (fits with 32 px of slack).
5. **The en taxonomy slug is `pronouns`, not table B's `personal-pronouns`**: Phase 0.1 (`07777114`) registered `axes['exercise-type'].pronouns.slug.en = 'pronouns'` under the taxonomy invariant `slug.en === key` (CLAUDE.md §17.8.5). The gate's `TABLE_B.en` follows the taxonomy; the spec's DECK slug stays `personal-pronouns` (K-288's `circle-the-article` precedent). Non-en slugs are not registered yet (rule 11 reports "not registered" for a locale whose block exists).
6. **The 4-chip locales' name budget contradicts rule 2 (OPEN for the fr/es/pt panels).** Ruling 10 fixes fr/es/pt base at `singles:4 (>= 2 m, >= 2 f) + mp + fp + fp + xp` = 12 names, but by GENDER that is **7 f + 5 m**, and rule 2 demands 6 + 6. The composer refuses honestly (a locale the pool cannot satisfy THROWS) — the panels must either tag 7 f + 5 m (rule 2 relaxed for 4-chip locales) or carry 13 names (7 f + 6 m). The same arithmetic hits pt F3 (`singles:4, pairs:4, pairMix:'mp,fp,fp,xp'`: 7 f). Recorded, not decided here.
7. **`resolveBase` refuses a fixed `pairMix` that cannot reach `mixFloor` on a chip only pairs supply** (P12: fr `pairs:3` -> "chip elles reaches 1 < mixFloor 2"); with `pairMix:'any'` (d1 / d3) the composer samples pair types under the name budget and re-rolls until every chip >= mixFloor (K-288's `okMix`), 400 tries then a refusal.
8. **Locale-neutral seed, within a locale CLASS.** The design wants the same portrait keys + card order in all 11 locales; the design's own configs make three classes (5 + 3 · 4 + 4 · 5 + 3 objects), so identity holds only within a class (names are drawn LAST so a 12- vs 16-name list never moves the portraits or the order).
9. **The plate width guard** is `glyphs x 0.53 x plateFont + 20 <= 302` (the design's 9.5 px/char at 18, scaled to the level's plate font); a pair that fails is re-sampled.
10. `initialBank` renders through `wordBank` (b2), whose banner is the dashed coral `.ws-scene-banner` — the design's "dashed bank" (60 px measured, r).

## Gate output (`node qa/verify-b4-pronouns.js`, 20 s)

```
bank en: clean (people 32, names 12, chips 3, frames 12, anaphora 8, things 12)
title lines: 3+3-fixture 3 · 677-fixture 4 · "Hän vai he?" 1 · "Korvaa nimi pronominilla" 1
render d1 en: verify 0 lints 0 body 799 card 257.0 inner 229.0 overflow 0 lowest 921 vs foot 921
render d2 en: verify 0 lints 0 body 799 card 189.3 inner 165.0 overflow 0 lowest 921 vs foot 921
render d2 en 3+3 chrome: body 710.0 card 167.0 inner 143.0 content 128/140/140/128/128/140/140/140 overflow 0
render d3 en 3+3 chrome (unpublished by design): card 130.8 inner 111.0 content 126 overflow 7 → 10 stage-overflow lines
render d2 en 677 chrome (PR8): body 677 inner 135.0 content 128 → 8 fails (stage overflow)
components: bank 60.0 · nameGapRow 74.0/74.0 (grid min 74) · anaphoraBlock 116.0 · ownerLane 82.6 · rewriteLane 77.4
components: shelf 218.0 · bins 58@201 x3 · captions 22/22/34/22/22/34/22/22/22/22
sweep d1: 20 seeds, 20 distinct sets, 20 distinct orders · sweep d2: 20 seeds, 20 distinct sets, 20 distinct orders
verify-b4-pronouns: 493 assertions, 0 failures → PASS     (--quick: 91 assertions, 0 failures)
```

Poisons, every one killed for its OWN reason (the correct EN bank + the synthetic de/es/fr/nl/sv/fi controls pass): P1 (en retag + a de overlay retag), P2 x3 (actor / picOpened / `toys bw`), P3, P4, P5, P6 (+ subject-not-first), P7 x2, P8 x2, P9 x3 (de `forms.p`, de `frame.pl`, fr vowel-initial f thing `orange`), P10 x3, P11 x3 (+ worksheet word, "with Answers", a free claim), P12 (resolveBase refuses fr `pairs:3`), P13, P14 (a measured 4-line fi title); render poisons PR1 (Mia tagged m -> "name Mia (f) over a portrait architect tagged m"), PL (a stamped key edited), PR6 (position leak), PR7 (default card padding at 710 -> stage overflow 1 px), PR8 (677), PR10 (a plate printing the occupation noun), PR12 (src twice), PR3 (a pronoun as the plate); component throws: same src twice, px 30, plate px 14, chip index out of range, chip h 40, nameGapRow line-2 mismatch (PR11), anaphoraBlock two "He" (PR2), ownerLane chip in the frame, nameCardBins 4 bins in a row (PR9), rewriteLane answer printed.

Measured for the faces (the design's *est.* items, at real fonts): nameGapRow lane **74.0** (r), anaphoraBlock **116.0**, ownerLane **82.6**, rewriteLane row **77.4**, initialBank **60**, nameCardBins shelf **218** with 3 bins at 201 x 330 and gapY 58, pair captions 34 <= 37.

## Renders for the reviewer (LOOKED at, every one)

- `scripts/worksheet-gen/out/dev/G1-352-null-d1-en.png` · `G1-352-null-d2-en.png` · `G1-352-null-d3-en.png` (render/one.js; d3 at the default chrome fits; it overflows only at 3+3)
- `out/dev/G1-352-gate-d2-en-chrome722.png` (the 3+3 chrome: chips at the card edge, 0 overflow) · `G1-352-gate-d2-en-chrome677.png` (PR8) · `G1-352-gate-d3-en-chrome722.png`
- `out/dev/G1-352-gate-components.png` (bank + F1 rows + F2 block + F3 lane + F5 row) · `G1-352-gate-components-sort.png` (F4 shelf + bins)
- What I saw: 8 faces, 8 name tags, 8 rows of three white pills; every plate over a portrait of its tag (Ava and Zoe over two girls; Max and Mia over a musician + a running athlete); no occupation noun anywhere; portraits read at 64 / 52 (the 56-class full figures only as singles). Nothing cut off, nothing sparse (the stage centres in the card at the default chrome with ~12 px of air above and below).

## Open items for the faces / panels

1. **fr/es/pt names by gender** (deviation 6): 7 f + 5 m or 13 names — a rule-2 ruling before the 4-chip panels author.
2. Phase 2 builds the five faces on `d.layout` (`replace` / `anaphora` / `possessive` / `sort` / `rewrite`) inside `_buildFace` + a `verify()` branch per stamp; the components exist and are smoke-measured; `tools/b4var-rows/pronouns.js` rows carry the §3 configs (F1 `bank:true`, F5 `bank:false, ruling:true`, F3 the per-locale chip widths 66 / 60 / 84, F4 `binLayout:'grid'` when `bins.length === 4`).
3. `objectCaption:'article'` (d1, de/nl) reads `bank('instructions', loc).objForms[key].unique` through `lib/b3-common.js` — the critic's open item 7 is answered: a b4 type can require it (the gate's synthetic de block exercises the objForms rule; `book` / `bed` have no de objForms and were left out of the synthetic list).
4. Non-en taxonomy slugs + names (table B) are the registrar's next step; rule 11 will hold each locale to table B once its block exists.
5. `strings.en.json` was regenerated by build-en (tracked; includes the sibling agents' new specs).

## Reviewer ruling (2026-09-21, Phase 1 commit)
OPEN 1 (the 4-chip locales fr/es/pt need 7 f + 5 m names for `singles:4 + mp+fp+fp+xp`, contradicting rule 2's 6+6): **RULED — rule 2 is a FLOOR (>= 6 f and >= 6 m), not an exact count; the fr/es/pt panels author 13 names (7 f + 6 m), fi 16 (its F2 need), every other locale 12.** The composer's honest refusal stays as the guard; `validateBank` rule 2 is read as `>=`. The 710 px chrome measurement (3-line title + 3-line instruction) supersedes the design's 722 for THIS family's stack budget; the base fails the fi 677 chrome by design (PR8) → the fi title cap (validator rule 13) is the mechanism, recorded in the README cross-type rulings at the fold.
