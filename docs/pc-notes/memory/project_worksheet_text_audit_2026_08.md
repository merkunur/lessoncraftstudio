---
name: project-worksheet-text-audit-2026-08
description: "SHIPPED 2026-08-18 — fleet-wide generated-text audit + canvas-visible fix across 33 apps (treasure-hunt per-locale clue frames + ~430 corrections, 11 native panels); commit 9e5e31d1, static-only deployed + md5-verified; deferred sr/SEO-layer + runtimeScore commissions recorded inside"
metadata: 
  node_type: memory
  type: project
  originSessionId: e7bff3f4-bcc5-4847-92d8-c9ac384f43a6
  modified: 2026-08-21T11:20:50.089Z
---

# Fleet-wide generated-text audit + fix — ✅ SHIPPED 2026-08-18

Operator commission (before regenerating FaC/prepositions decks): audit ALL worksheet generators' auto-generated text (title, instruction), especially Treasure Hunt non-EN. Scope ruled: **canvas-visible only**. Plan: `structured-stirring-glade.md`.

## Ship record
- **Commit `9e5e31d1`** (35 files: 33 app HTMLs + translations-shared.js + translations-picture-sort.js), pushed; **STATIC-ONLY deployed** via update-worksheet.sh (deploy.sh NOT run — pt-BR #1–#47 batch intact); all 35 served copies **md5-identical**; markers verified (TH `32.2.0`, `moveFrame`, `shared.name.field` ×11).
- Method: 2 Explore sweeps → direct verification of every fix-driving claim → mechanical fixes → **11 native panels (one per locale incl. EN-source)** returning exact verbatim pairs (~390 sites) → consolidated scripted application with per-pair count asserts → gates.

## What was fixed (headlines)
- **Treasure Hunt (bundleVersion 32.2.0)**: `movesToFiveInstructions` → per-locale `moveFrame` table ({steps}/{unit}/{dir}, locale word order + verbs; old fixed EN order verified broken on a live de deck); final clue keeps its period; panel rulings fi `Siirry`, it `verso l'alto/il basso` + `Inizia da`, es `hacia arriba/abajo`, no `Start på`, fr `Commence en` + NBSP; pt você-form. Gate: `th-clue-test.js` (independent hand-written oracle, poison-tested) in `~/.claude/plans/grammar-fix-artifacts/`.
- Hardcoded EN `Name:/Date:` → content-locale `shared.name.field`/`shared.date.field` (×11 in translations-shared.js) in 11 apps; `'New Text'` → `shared.text.default` in 25 apps (`?v=10→11` bumped in edited apps).
- wordsearch: `Œ` added to grid whitelist; list filter compares stripped-vs-stripped (œ/multi-word words no longer in-grid-but-unlisted); non-mix list via ImageVocab. Verified rendered: fr grid shows Œ cell + "Cœur" in the printed list.
- word-scramble `&locale=` on top-up fetch; ß-safe lowercase (derive from original, never upper→lower) word-guess+word-scramble; big-small `{seq}` follows imgCount + nl `Omcirkel` + de Kästchen/es recuadro/fr case; picture-path per-mode instructions ×11; picture-sort translated Custom Words; matching theme-header never prints raw slug; localized CAT fallbacks; draw-and-color lcsLocalizedTitle cache.
- ~390 panel corrections: sentence-case titles (fr/es/pt/it/nl/sv/da/no/fi), fr NBSP sweep, pt-BR você-register sweep (EP tu-forms, comboio→trem), 'More Less'→'More or Less' ×11, da/no/sv/fi wording, sv zero-width-space in Mönstertåget, it Trova l'intruso / Treno delle sequenze, etc.

## ✅ REGENERATION EXECUTED 2026-08-21 — TH/FaC/prepositions replaced catalog-wide
Operator regenerated 2,092 decks (fixed apps: TH 32.2.0, preps 12.4.0, FaC 19.4.0) into `decks/replacement/`; ALL replaced in production same day: **2,047 in-place updates + 45 new-theme INSERTs, 0 failed**, across 11 locales via `publish-wave --updates-manifest` (first at-scale use of the UPDATE route). SEO churn = ZERO on clean sets: new committed `scripts/publish-cli/transplant-seo-head.js` splices the OLD deck.html SEO region (+ breadcrumb) into each new version dir and restores title/desc hashes — **1,763 transplants, 100% hash-stable vs the before-snapshot**. TH en/es/pt theme pollution (296 decks, uniform wrong slugs/tags) resolved by 6-agent thumbnail reads (0 disagreement w/ the §22.4 landing maps; 9 suspects resolved personally vs image-library pools) → subjectTags/titles now theme-true (slugs stay frozen-wrong per churn freeze; fix post-freeze). `repoint-deck-canonical --types=treasure-hunt` re-run for en/es/pt. **38 broken decks remain live (no replacement ZIP)** — list at `~/.claude/plans/deck-replacement-2026-08-artifacts/leftovers-final.txt` (per-locale "all-themes"+miscellaneous prepositions decks, animals-theme TH twins ×8, 6 out-of-catalog autumn TH decks en/es/pt, es preps 5 themes, es FaC forest_creatures 3rd roll, nl ocean_life twin, no ocean_life twin, pt kitchen_tools). Full mapping/updates-manifests/theme-map/DB-snapshot in that artifacts dir + server `/root/replacement-2026-08/`. Traps hit: title/desc uniqueness pre-flight collides with the SAME-TITLE TWIN of a target (resolve by remapping the ZIP to the twin whose title it matches — en `treasure-hunt` bare, es `-animales` bare + `0615`, pt `-animais` bare); pt needed a TWO-WAVE split (plain-"4 de julho"-titled decks were targets of OTHER themes' ZIPs — their titles free up only after wave A); `*/` inside a JS docblock (og:*/twitter:*) + `db.client` being a factory fn; PREBAND self-collision defect dodged with `--no-db-check` (banding still runs).

## ✅ FINAL BATCH 2026-08-21 (same day) — 26 leftover decks replaced
Operator regenerated 26 of the 38 leftovers (`decks/replacement/Final`); all 26 published as in-place UPDATEs, 0 failed, verified 26/26 (version+1, DB hash==rendered, 3 animals twins' tags corrected). New committed tool: `scripts/publish-cli/force-reband-staged.js` — collision-aware staged-ZIP SEO rebuild for --updates-manifest batches (stock preband keys on TITLE hashes only and would UNDO pre-applied disambiguation; this one checks title+desc vs DB excluding the ZIP's own target, forces `seoOpts.disambiguator` for title + `seoOpts.variantId` for desc, writes manifest.variant_id; run waves with --skip-preband after it). 8 transplants surfaced PRE-EXISTING stale DB hashes (recomputed-truth written; rendered bytes unchanged). 🏁 **PROGRAM COMPLETE — ONE deck remains** (en TH `4th-of-july-030`, punctuation-only, operator-optional): the it misc pair AND the 9 "All themes (random)" decks all shipped same day (9/9 verified v2/12.4.0/hash-consistent; nl's stray manifest theme published as-is per operator ruling — its transient disambiguated head was replaced by the clean transplanted title). Totals: **2,086 replaced + 45 inserted, 0 failures.** Older text follows:
(superseded count trail: 38 → 12 → 10 → 1 → `~/.claude/plans/deck-replacement-2026-08-artifacts/leftovers-remaining.txt`: 9 per-locale empty-theme prepositions decks — VERIFIED genuine multi-theme decks (`settings.theme_select='all'`, subjects across the animal themes); regenerable via the prepositions app's image-selection MODE dropdown "All themes (random)" (`prepositions.html:2723`, generation path `:2945` — the manual-mode theme dropdown's "All Themes" is only the dictionary filter, the operator's confusion) + en TH `4th-of-july-030` autumn deck. Mapping when they arrive: (lang, fillin, theme=null) -> the bare per-locale slug; transplant all 9.

## Regeneration guidance (operator's list — superseded by the execution above)
- **treasure-hunt: 979 non-EN** published decks (cardinal-arrows 496 + compass 483) carry broken clue sentences → regenerate. 99 EN decks have the punctuation-only defect (run-together final question) — operator's call.
- Already listed: FaC hidden-object 410, prepositions fillin non-EN 501, prepositions MC it ~50.
- Config-conditional (counts on request): fr wordsearch decks containing œ/multi-word entries (unsolvable list), de word-guess/word-scramble lowercase-mode ß decks, nl big-small, 2-image big-small order decks.

## ✅ CONFIG-CONDITIONAL SCAN EXECUTED 2026-08-21 — the "counts on request" line resolved
Full-catalog scan (verdict + TSVs: `~/.claude/plans/deck-replacement-2026-08-artifacts/config-conditional/`): **fr-wordsearch oe/multi-word class = 0 broken** (1,640 wordsearch decks scanned via bundle-vs-transformed-vocab-form matching; 7 flags were all functional MIX-mode decks — verified by thumbnail; 1 cosmetic: en `portuguese-wordsearch-4th-of-july-332d` has English HOTDOG in its Portuguese grid, solvable, optional). **de ss class = 0** (no de word-guess/scramble deck ever used lowercase mode). **big-small {seq} class = 0** (all 494 order decks are 3-image). **Genuine remainder = 92 nl big-small decks** (ungrammatical "Cirkel het ..." canvas instruction; regen list in affected-big-small.tsv) + optional-quality de/fr/es big-small wordings (deliberately not listed). ⭐ Method lesson: substring scans over deck.html false-positive on base64 JPEG data (strip + word-boundary), and cross-language mix decks false-positive on other locales' word forms (verify by thumbnail).

## 🏁 NL BIG-SMALL BATCH 2026-08-21 — REPLACEMENT PROGRAM CLOSED
The 92-deck nl big-small wording class: 91 replaced in place + 9 new-theme INSERTs (100 ZIPs, fixed bv 17.2.0), 0 failed, 91/91 verified (v2, DB hash==rendered, "Omcirkel het grote voorwerp…" confirmed on render). No disambiguations needed → stock preband via --no-db-check (bands INSERTs too; force-reband only needed when collisions exist). **Grand program totals: 2,177 replaced + 54 inserted, 0 failures.** Remaining = 3 OPTIONAL decks (leftovers-remaining.txt): en TH 030 (punctuation), en portuguese-wordsearch 332d (cosmetic HOTDOG), nl groot-klein-vind-de-grote (theme-less big-small twin of the all-themes class — regen only if big-small has a random/all-themes image mode).

## Deferred commissions (recorded, NOT done)
1. **Deck sr/SEO text layer**: sr templates en/de-only across ~15 apps (9 locales emit ENGLISH sr text inside non-EN deck.html = SEO locale-residue); de frames carry FaC-class defects (shadow-match "Verbinde das {item}…seinem", picture-path "vom {startImage}", missing-pieces "{pieceShape}-Stück", matching "Verbinde {leftValue}"); 3 apps build template via UI-locale t() but noun via srLang (mixed-language sr; picture-path srTranslate is the correct model); math-worksheet `_mwEquationToStringEnDe` naive concatenation; picture-path endpointCountWord en/de-only; chart-count noun-splice.
2. **Shared `runtimeScore` agreement** fr ("{n} sur {total} correct")/it/es across 27 baked STRINGS_ALL blobs (FaC+preps already fixed).
3. **Alphabet contradiction**: alphabet-train:1446/1455 (de incl. ÄÖÜ interleaved, fi missing Å) vs code-addition:1622/1629 — needs operator ruling.
4. **bingo pre-existing wart**: translations-picture-bingo.js:2367 `const t = getTranslation` collides with bingo.html:53 `function t(key)` → the small define-t block dies with SyntaxError on every load (production too); benign (t resolves to getTranslation) but should be cleaned someday.
5. Minor flags: nl word-scramble title 'Letterzaak' odd (product-name call); fr find-objects/odd-one-out both 'Trouve l'intrus' (title collision); crossword ß clue/grid mismatch only in degenerate de/de mix config; wordsearch/matching sepGenerate-injected raw theme option text (harness/SEP surface); big-small/cryptogram/draw-and-color Add-Text default still UI-locale t().

## QA tooling (durable copies in ~/.claude/plans/grammar-fix-artifacts/ + session scratchpad)
`th-clue-test.js` (TH oracle gate) · `apps-smoke.js` (33-app load smoke) · `visual-spots.js` (rendered screenshots) · `apply-panels.py`/`apply-panels2.py` (the ~390 verbatim pairs, re-runnable record of every panel correction) · deck-wiring-test.js (from the FaC/preps fix).

## Lessons
- Inline bash heredocs on this machine EAT `\\` sequences (recorded pt-BR trap, re-confirmed): any backslash-bearing script must be a WRITTEN file.
- Some header tables store literal `\uXXXX` escapes (more-less checkCross) — match the ESCAPED bytes, not the rendered chars.
- Panel pairs applied with per-pair expected-count asserts caught both real collisions (da/no 'Match Skygger', da/no 'Linjetegningsøvelse' after my own earlier fix made them identical).
- The live thumbnail is ground truth: the TH defect was confirmed shipping on a published de deck before any fix was written.
