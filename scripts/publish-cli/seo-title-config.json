{
  "_comment": "Per-locale deck-title formula + keyword tuning for the title overhaul (replaces the old '{Type} {Worksheet} — {Theme} — {Level} — Set NNN | LessonCraftStudio' template). The ENGINE (segment assembly, separator join, 50-70 budget trim, HTML-escape, brand-drop) is shared in build-seo-head.js + catalog-export.js; this file supplies only the per-locale WORD ORDER + keyword/style choices a native SEO expert decides. Keyword STRINGS for type/theme/level still come from topics-taxonomy.json (do NOT duplicate here); this file tunes ORDER, the vocabulary-differentiator style, numeric-range phrasing, and which differentiator sources are enabled. '_default' is the EN-shaped fallback so any locale missing a block never crashes. numericTemplates are NOT inherited from _default for non-en locales (would leak English on themeless math decks) — a locale gets numeric phrasing only when its own block defines numericTemplates (or it is 'en'). Per CLAUDE.md §A.13.48, non-en blocks are authored by a 3-agent native ensemble (SEO + marketing + K-3 ed) in plan-mode per locale; sv/da/no/fi carry an [NSR-FLAG]. Only en/es/pt carry published decks today; the other 8 blocks ship ready for when those locales publish.",

  "_schema": {
    "titleSegmentOrder": "array of 'head'|'theme'|'diff'|'level' in render order; theme omitted automatically when the deck has no theme; level omitted when includeLevel=false; diff omitted when the differentiator kind is 'none'",
    "separator": "string joining segments (default ' — ')",
    "includeLevel": "boolean; whether the educational-level segment appears",
    "vocab": "{ maxNouns:int, joinStyle:'ampersand'|'conjunction', casing:'title'|'preserve'|'lower' } — how the featured-vocabulary differentiator renders",
    "numericTemplates": "{ '<exercise-type>': '... {max} ...', '_generic': '...' } — phrasing for the numeric-range differentiator on themeless math decks; per-locale strings",
    "diff": "{ enableVocab:bool, enableNumeric:bool, enableMode:bool } — which differentiator sources are active (experts disable enableMode for locales whose exercise-mode names are sparse, to avoid English residue)",
    "charBudget": "{ min:int, max:int } — target title length; trim order is 2-nouns -> 1-noun -> drop level -> (diff dropped last); theme is never dropped when set (protects the theme-in-title audit check)"
  },

  "_default": {
    "titleSegmentOrder": ["head", "theme", "diff", "level"],
    "separator": " — ",
    "includeLevel": true,
    "vocab": { "maxNouns": 2, "joinStyle": "ampersand", "casing": "title" },
    "numericTemplates": {
      "addition": "Sums to {max}",
      "subtraction": "Take Away to {max}",
      "code-addition": "Sums to {max}",
      "math-worksheet": "Numbers to {max}",
      "math-puzzle": "Numbers to {max}",
      "_generic": "Numbers to {max}"
    },
    "diff": { "enableVocab": true, "enableNumeric": true, "enableMode": false },
    "charBudget": { "min": 50, "max": 70 }
  },

  "en": {
    "titleSegmentOrder": ["head", "theme", "diff", "level"],
    "separator": " — ",
    "includeLevel": true,
    "vocab": { "maxNouns": 2, "joinStyle": "ampersand", "casing": "title" },
    "numericTemplates": {
      "addition": "Sums to {max}",
      "subtraction": "Take Away to {max}",
      "code-addition": "Sums to {max}",
      "math-worksheet": "Numbers to {max}",
      "math-puzzle": "Numbers to {max}",
      "_generic": "Numbers to {max}"
    },
    "diff": { "enableVocab": true, "enableNumeric": true, "enableMode": false },
    "charBudget": { "min": 50, "max": 70 }
  },

  "_comment_es": "Authored by a 3-agent native Spanish ensemble (SEO + marketing + K-3 education) 2026-05-28. Operator-locked: 5-7 level term = 'Kínder' (set via seo.educational_level.kindergarten.es in messages/es.json); head shape = 'Type then Ficha'. Consensus fixes: sentence-case (no English Title-Case), worksheet word 'Ficha', drop jargon head modes (image-image / three-symbols-add-sub), connector 'y' (joinStyle conjunction), normalize ALL-CAPS vocab via casing 'sentence'.",
  "es": {
    "titleSegmentOrder": ["head", "theme", "diff", "level"],
    "separator": " — ",
    "includeLevel": true,
    "segmentCasing": "sentence",
    "worksheetWordOverride": "Ficha",
    "headDropModes": ["image-image", "three-symbols-add-sub", "two-symbols-add-sub", "image-number", "mixed", "cardinal-arrows"],
    "vocab": { "maxNouns": 2, "joinStyle": "conjunction", "casing": "sentence" },
    "numericTemplates": {},
    "diff": { "enableVocab": true, "enableNumeric": false, "enableMode": false, "excludeTypes": ["cryptogram"] },
    "charBudget": { "min": 50, "max": 70 }
  },

  "_comment_it": "Native Italian 3-agent ensemble 2026-05-28. Operator-locked: 5-7 level = 'Prescolare' (messages/it.json seo.educational_level.kindergarten); head = 'Scheda di {type}' (headStyle of-type). Consensus: sentence-case, drop jargon head modes, connector 'e' (joinStyle conjunction), exclude cryptogram cipher, normalize ALL-CAPS vocab. Type-name softening (Crittogramma, Più o meno) flagged future (taxonomy slugs).",
  "it": {
    "titleSegmentOrder": ["head", "theme", "diff", "level"],
    "separator": " — ",
    "includeLevel": true,
    "segmentCasing": "sentence",
    "headStyle": "of-type",
    "ofTypeKeys": ["addition", "subtraction"],
    "ofTypeConnector": "di",
    "worksheetWordOverride": "Scheda",
    "headDropModes": ["image-image", "three-symbols-add-sub", "two-symbols-add-sub", "image-number", "mixed", "cardinal-arrows"],
    "vocab": { "maxNouns": 2, "joinStyle": "conjunction", "casing": "sentence" },
    "numericTemplates": {},
    "diff": { "enableVocab": true, "enableNumeric": false, "enableMode": false, "excludeTypes": ["cryptogram"] },
    "charBudget": { "min": 50, "max": 70 }
  },

  "_comment_pt": "Native Brazilian Portuguese 3-agent ensemble 2026-05-28 (unanimous). pt = pt-BR canonical (§6); current strings were European-flavored. Operator-locked: head = 'Atividade de {type}' (headStyle of-type, connector 'de'); levels Brazilianized in messages/pt.json (Educação infantil / Pré-escola / 1º-3º ano). Consensus: sentence-case, worksheet word 'Atividade', drop jargon head modes, connector 'e', exclude cryptogram cipher, normalize ALL-CAPS. Type-name flags future (taxonomy slugs): Mais ou menos→Maior ou menor, Pareamento de sombras→Associação de sombras, word-guess→Forca.",
  "pt": {
    "titleSegmentOrder": ["head", "theme", "diff", "level"],
    "separator": " — ",
    "includeLevel": true,
    "segmentCasing": "sentence",
    "headStyle": "of-type",
    "ofTypeKeys": ["addition", "subtraction"],
    "ofTypeConnector": "de",
    "worksheetWordOverride": "Atividade",
    "headDropModes": ["image-image", "three-symbols-add-sub", "two-symbols-add-sub", "image-number", "mixed", "cardinal-arrows"],
    "vocab": { "maxNouns": 2, "joinStyle": "conjunction", "casing": "sentence" },
    "numericTemplates": {},
    "diff": { "enableVocab": true, "enableNumeric": false, "enableMode": false, "excludeTypes": ["cryptogram"] },
    "charBudget": { "min": 50, "max": 70 }
  }
}
