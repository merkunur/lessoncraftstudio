# G1-353 `question-words` : EN BASE build record (2026-09-21)

Built from `G1-353-question-words.md` (§2 base, §5 data + gates; §3 read for the additive `mode` knob), `_SUBSTRATE.md`, the README "Cross-type rulings" and `_work/G1-353-critic.md` (rulings 1, 6, 7, 12-15, 17, 21, 22, 24 applied). Exemplars read: `types/g1/G1-352-pronouns.js` + `data/b4/pronouns.js` + `templates/components-b4/pronouns.js` + `qa/verify-b4-pronouns.js` (+ its build record: the "722" chrome measures 710), `types/g1/G1-341-read-and-check-true-or-false.js` / `components-b3/read-and-do.js truthChips` (the class this family's chips must not resemble), `types/k/K-288-articles.js` (chip order + identical-style verify), `lib/b3-instructions.js fillSlots`. No em-dashes.

## Files (all type-scoped; nothing shared edited; `node tools/b3-baseline.js --check --quick` → `checked build 3428 + enum 255 in 16s: 0 drifted (0 expected), 0 missing` / `PASS`)

| file | what |
|---|---|
| `scripts/worksheet-gen/types/g1/G1-353-question-words.js` | the spec: exerciseType `question-words`, themeless (`coordinate.theme:''`), `unitAxis:{applicable:false}`, d1 / **d2 (ships)** / d3 (unpublished), `build` → `_deps(loc)` (the family bank + `bank('pronouns')` + `bank('instructions').objForms`) → `_buildWith(bank, d, {locale, pron, objForms}, ctx)` (the gate's injection seam) → `resolveBase` → `compose` → `answerRow` × rows. Pure helpers exported for the gate: `chipEst chipW stripFits thingSlotOf slotValues fillFrame markOf questionOf gapOf pinThing thingPool clashKeys pictureBot resolveBase compose`. `verify(page)` = the base branch: rows === config, ask ∈ the page's kinds, exactly one `[data-lcs-mark]` per sentence (a single word-bounded occurrence, never the whole sentence, never a chip label, the name on `who` rows and NOT the name otherwise, a count mark never wider than one word), the sentence one line (≤ 26 px) and opening with the name, chips in the fixed order on every row (position leak) with one style string, no correct marker, no mark inside a strip, ≥ 44 high on ONE row inside the lane, the row geometry (picture at +40, sentence at +40 + pic + 12 — the P22 column-gap trap), one portrait ≥ 44 with a `depicted` tag, no name / src / frame twice, lane overflow 0, every kind ≥ `mixFloor`, `pictureBot ≤ botMax`. `d.mode !== 'base'` → `_buildFace` THROWS "Phase 2" (dispatched before the RNG is touched). |
| `scripts/worksheet-gen/data/b4/question-words.js` | `{ QUESTION_WORDS: { en: {…} } }`: `qwords` ×5 (fixed order), `starters` ×6, `bins` ×3, `qPrefix`, `sortThingForm:'bare'`, `genderFilter:{}`, `excludeThings:[]`, **`chipWidths`** (measured, see deviation 1), 12 `frames` (3 per kind; `{name}` first + verb + ONE complement slot; `q.who` + `q.<kind>`; `signed:true`), 15 `places` (14 + cabin, every picture OPENED), `twins`, 6 `times`, `thingThemes`, `numbers`, `faces` (the per-mode kind sets), `strings` ×6 faces. `data/` is gitignored: the reviewer `git add -f`. |
| `scripts/worksheet-gen/templates/components-b4/question-words.js` | the ten NEW components of §2, exported by exactly the design's names: `markedSpan renderMarked qwChips answerRow` (base) + `qaMatch questionFrame qwBins writeRow askScene starterLines` (the faces; built to the §2 contracts incl. every throw, smoke-rendered by the gate, NOT consumed by any spec yet). Barrel `templates/components-b4.js` loads them beside the seven sibling files with 0 duplicate names. |
| `scripts/worksheet-gen/qa/verify-b4-question-words.js` | the gate; exports `validateBank(block, loc, opts)` (rules 1-14; `tools/b4-probe-child.js` calls it as `(block, loc)`), `crossCheck`, `TABLE_B`, `HEADS`, `MEASURED_F4`, `synthetic`. |
| `scripts/worksheet-gen/i18n/strings.en.json` | regenerated by `node i18n/build-en.js` (607 types, title lint clean; the file also picks up the sibling agents' new specs K-353..K-357 / G1-350..G1-352). |

## Pictures OPENED (the human gate)

The 15 place pictures on one contact sheet (sharp from `cache/themes/<theme>/<noun>`, scratch `G1-353-places-160.png`) READ at **160 px**: house · garden (a flower bed) · beach · tent · forest · lake · river · mountain · pool · kitchen (a scene, legible at 160) · garage · bed · sofa · playground (a climbing frame with a slide: a PLACE) · cabin. Lake and river are both a water landscape, house and cabin both a house: the two twin groups confirmed. The 32 portraits are the pronouns family's OPENED bank (read through `bank('pronouns')`, never re-authored or re-tagged); the things are the mechanical pool (the six G1-308-opened themes) and are not pinned by hand.

## Deviations from the design file (each measured)

1. **The chip-strip guard estimate `24 + 12.4 × glyphs + 5` REFUSES the fi / da five-chip strips the design itself measured at 509 / 511 ≤ 531** ("Kuinka monta" 152 measured → 178 estimated; "Hvor mange" 137 → 153; fi five-chip estimate 590, da 553). Built: `chipW(bank, label)` reads the bank's **MEASURED `chipWidths[label]`** first (Baloo 2 700 20 / pad 12 through the real render) and falls back to the estimate; the gate asserts every table entry within 3 px of the rendered chip and re-measured all 52 locale labels (`MEASURED_CHIP_W` in the gate: Who 68.1 · What 75 · Where 86 · When 78.9 · How many 121.8 · … · Kuinka monta 152.2 · Hvor mange 136.5 · Hur många 125.7 — the design §2 table to the pixel). The panels author `chipWidths` from a render the same way; an unauthored table still ships every d2 four-chip strip (worst da 388 by estimate) and only the unpublished d3 five-chip strips of fi (590) and da (553) need the table (sv 453 fits on the estimate).
2. **The prompt's `[...]`-marking idiom and the `askLane` / `writeLane` names are the pre-merge design's** (critic #12 overruled the whole-literal items for the lock's slot frames; the FINAL §2 names `writeRow` / `askScene` / `starterLines`). Built to the FINAL: exactly ONE complement slot per frame → exactly one mark (`renderMarked` throws unless the span occurs exactly once, word-bounded, and is not the whole sentence). The design file is the contract (brief §1).
3. **`botMax` is a config, not a constant**: d1 (`picOf:'referent'`, the unpublished scaffold: the marked constituent's own picture at 64) is solvable by picture alone by design (`pictureBot` 1.0), so its `botMax` is 1; d2 / d3 carry **0.35** (the gate asserts `difficulty[2].botMax === 0.35` AND `who / rows ≤ 0.35` statically, P18 dynamically). `resolveBase` refuses a `when` / `howmany` row under `picOf:'referent'` (a clock at 64 → 7 px numerals; a number has no picture).
4. **The 3-line-title + 3-line-instruction chrome measures 710** (the G1-352 finding, not the README's 722): row minimum **87.4** (the design's m 87.4), intrinsic stack **659.7**, floored to **664** by `minmax(88px, 1fr)`; fits 677 (the fi four-line chrome; measured body 677, rows stretch to 89.8) and 710 (rows stretch to 94.6). The stretch opens inside the lanes as designed (the text block centres). The gate measures the intrinsic stack by forcing the grid to `repeat(n, auto)` and restoring it.
5. **Seed locale-neutrality holds for the frame ids / asks / row order only as far as the RNG sequence is shared**: names are drawn by `rng.sample` over the locale's list (12; fi 16) and things over the locale's pool (en 133 … es 88), so "names by index and things by key in all 11" is true only between locales of equal pool sizes. Recorded, not gated (the design does not gate it either).
6. **Validator rule 3's character caps** (F4 ≤ 31, F2 rest ≤ 30, F1 ≤ 44) specify only the name; the gate fills the other slots with PROBE stand-ins (thing 8 / plural 6 glyphs, the SHORTEST place / time / number word for the F4 / F2 eligibility floors, the LONGEST for F1's every-pair cap). Rule 2's "12-glyph thing" is applied verbatim.
7. **Rule 6's tile guard passes exactly six en place literals** (at home · in bed · in the tent · at the lake · on the sofa · at the pool at `round(glyphs × 10.2) ≤ 112`, i.e. ≤ 11 glyphs as the design's "~11 glyphs" states) — the "≥ 6" floor met exactly; the render gate measures the tiles (P21: "auf dem Spielplatz" 167 px text > 112 refused; "im Garten" 88 passes).
8. **`genderFilter.count` needs a gender code and `objForms` carries none**: `thingPool` attaches the locale's vocab gender CODE per key (a code, never an inflected form — the pronouns-object precedent; the vocab is otherwise read only for the sv / da / no definite clash, §5 rule 10). Measured es: pool 88, masculine 58 (the design's 58 of 88); 42 count rows over 30 d3 seeds all masculine, the control without the filter 18/42 feminine.
9. **A `faces` block** (the prompt's `faces.<id>.kinds`) records the per-mode kind sets in the bank; the validator asserts `faces.base.kinds === difficulty[2].chips` and that only `ask` carries why / how.
10. **`qaMatch` dots measure 99 px apart** (the design's arithmetic said 83: it subtracted 2 × 26 from the 135 zone; the dot centre sits at ±20 from the item edge) — more room for the child's line, not less. The F1 block measures **580** intrinsic (design 560; the clock item's inline SVG adds descender space), 97 px inside 677.
11. **The count answer on F1** ("three apples via `countBadge`") is a Phase 2 composer concern: `qaMatch` takes a picture src per answer; the smoke shows one apple + "three".
12. **Cosmetic, for the critic**: the mark's `padding:0 4px` (the design's measured +8 px) leaves a visible 4 px gap before a sentence-final period ("in the cabin ."). Kept as designed; a `margin-right:-4px` on a final mark would close it if the critic wants it.
13. `sortThingForm`, `nbsp`, `binAliases`, `whatForm`, `possession` / `nameForms`, `static` are read by the validator only (F3 / fi / fr rules); the base never consumes them.

## Gate output (`node qa/verify-b4-question-words.js`, 26 s)

```
bank en: clean (frames 12, places 15, times 6, thing pool 133)
sv thing pool 113 (clash cow)
title lines: 722-fixture 3 · 677-fixture 4
render d1 en: verify 0 lints 0 body 766 rows 6 row 121.0 strip 287.5 bot 1.00 chips Who:76 What:84 Where:96 lowest 921 vs foot 921
render d2 en: verify 0 lints 0 body 766 rows 7 row 102.6 strip 343.9 bot 0.29 chips Who:68 What:75 Where:86 When:79 lowest 921 vs foot 921
render d3 en: verify 0 lints 0 body 766 rows 7 row 102.6 strip 477.7 bot 0.29 chips Who:68 What:75 Where:86 When:79 How many:122 lowest 921 vs foot 921
render d2 en 3+3 chrome: body 710.0 row min 87.4 (stretched 94.6) intrinsic stack 659.7 (floored 664) lowest 921 vs foot 921
render d2 en 677 chrome: body 677 row 89.8 stack 677.0 lowest 921 vs foot 921
mark: plain 211.2 x 23.4 · marked 219.2 x 23.4          (+8.0 / +0, the design's measurement)
components: qaMatch 580.0 (items 100, clock 10 px, dots 99) · questionFrame 84.0/84.0 · writeRow 78.0/78.0
components: qwBins shelf 122.0 · bins 58@201 58@201 58@201 · stack 536.0
components: askScene 160.0 · starterLines 400.0 (starter font 29.5 px, clock 14 px)
sweep d1: 20 seeds, 20 distinct sets, 20 distinct orders · d2 20/20 · d3 20/20
es genderFilter: 42 count rows all masculine · control without the filter: 18/42 feminine
verify-b4-question-words: 1050 assertions, 0 failures → PASS     (--quick: 167 assertions, 0 failures)
```

Poisons, every one killed for its OWN reason (the correct EN bank + the synthetic de / es / fr / sv / fi controls pass): P1 (de "Den Ball hat {name}."), P2 ×2 (de "wer" / en "what" as a token), P3 (+ P3x thing, P3y time: the asked slot printed), P4 (lowercase "wer"), P5 (fr plain space before "?"), P6 ×2 (de dat + {thing}; unsigned), P7 ("Where is the {noun}?" = a SENTENCES frame), P8 ×2 (es without the filter; a non-es "¿"), P9 ×3 (an added `zoo` key; "animals bw"; a capitalised literal), P10 (a sv pool carrying cow — "kon" is `cone`'s form), P11 (a fi {thing}), P12 ×6 (a sv neighbour-name title; the bank word on the base; "How many" as a starter; the worksheet word; a free claim; "with Answers"), P1x (a qwords literal = a name), P2x (a count frame without its plural), P7x (a clock hour twice), P12g (`faces.base.kinds` ≠ the d2 chips); render poisons P13 (a count mark spanning "five lambs"), P14 (chips reordered → position leak), P18 (3 who rows → pictureBot 0.43), P19 (the male names tagged f → "Leo (m) over toys/girl depicted f — tag mismatch"), P22 (`column-gap:10px 12px` → "the picture sits at +30, want +40"), PRa (a correct marker), PRb (a mark inside a strip), PRc (a chip label in the sentence), PRd (a src twice), PRe (a stamped ask edited); P20 (a 64 px clock: `qa/lints.js` "font too small (7px)" + `qaMatch` throws "< the clock floor 74"); P21 (the tile guard, measured). Component throws: markedSpan '', renderMarked twice / whole / no word-bounded hit, qwChips 6 / duplicate label / a mark in a label / h 40, qaMatch a non-derangement (P15's shape), questionFrame the question printed whole (P16's shape), qwBins a tile equal to a head, writeRow the answer printed (P17's shape), askScene tile 90, starterLines a duplicate. **P15 / P16 / P17 proper (the F1 / F2 / F4 `verify()` branches) are Phase 2.**

## Renders for the reviewer (LOOKED at, every one)

- `scripts/worksheet-gen/out/dev/G1-353-null-d1-en.png` · `G1-353-null-d2-en.png` · `G1-353-null-d3-en.png` (render/one.js; d2 = the shipped face)
- `out/dev/G1-353-gate-d2-en-chrome722.png` (the 3+3 chrome, body 710) · `G1-353-gate-d2-en-chrome677.png` (the four-line fi title: the base fits with 13 px to spare at the 88 floor)
- `out/dev/G1-353-gate-components-match.png` (F1) · `G1-353-gate-components.png` (F2 lanes + F4 rows) · `G1-353-gate-components-sort.png` (F3) · `G1-353-gate-components-ask.png` (F5 scene + starters)
- What I saw on d2: seven cream lanes, a portrait on every row, one 18 px sentence per row with ONE coral-soft bar + underline that a six-year-old cannot miss ("Leo" / "in the cabin" / "the avocado" / "at six o'clock"), four identical white pills under every sentence in the same order, nothing else. The strip is one row on every render (en four-chip 344, five-chip 478 ≤ 531). d1 shows the referent's own picture (a scooter, a cabin, a mountain), as the ladder says. Nothing cut off, nothing sparse (the rows stretch evenly at the short chrome), no wrong picture.

## Open items for the faces / panels

1. **Phase 2** builds the five faces on `d.mode` (`match` / `fill` / `sort` / `write` / `ask`) inside `_buildFace` + a `verify()` branch per stamp; the components exist and are smoke-measured (F1 560 → measured 580 / F2 84 / F3 ≤ 586 → 536 / F4 78 / F5 160 + 400). The F1 count answer's `countBadge` and the F3 `sortThingForm` policy are composer work.
2. **Panels author `chipWidths`** from a render (deviation 1) — required only where a five-chip strip ships (d3, the F2 bank is a different component); every d2 strip passes on the estimate.
3. **Rule 9 pins the base title to the measured head per locale** (`HEADS` in the gate) and the F4 title to the measured write-a-question head where one exists (`MEASURED_F4`: nl / sv / da / no / fi); a panel that re-targets a head edits those two tables in the same commit.
4. **fr `Que` vs `Qu'est-ce que`** (the critic's open 2): "Qu'est-ce que" measures 150.7 px as a chip; the fr five-chip strip would be 59 + 150.7 + 53.5 + 87.2 + 106.9 + 48 = 505 ≤ 531 — either policy fits; the four-chip d2 strip is 350.
5. **it "Dov'è"**, **fi `whatForm`**, **pt levels**, **fi `possession`** — panel decisions with a validator rule each (rules 13 / 11); the gate's synthetic fi block uses the partitive policy.
6. The taxonomy carries `question-words` for en only (rule 14 reports "not registered" for the other ten until the registrar's step).

## Reviewer note (2026-09-21, Phase 1 commit)
Render read: the mark and the chip strip work; the page is full. ONE string defect to fix in the EN title before the EN wave (Phase 3): the base title names "Who, What, Where" while the d2 page also asks WHEN (row 7 "at six o'clock") — the title must name all four chips or none ("Question Words: Who, What, Where, When" ≤ 70 fits, or "Question Words: Circle the Right One"). A title that lists the chips must equal the chip set (add as validator rule: every chip literal named in the title is a d2 chip and vice versa, or the title lists none). The nt20-C lesson (a title saying tick while the page circles) in a new dress.
