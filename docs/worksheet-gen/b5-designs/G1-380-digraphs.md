# G1-380 `digraphs` (K/G1/G2) : FINAL design (editor merge, 2026-09-23)

Merged from `_work/G1-380-pedagogy.md` + `_work/G1-380-design-A.md` ("The Sound Bar": teal capsule + coral bar, "Team Table" base) + `_work/G1-380-design-B.md` ("the SOUND BEAD", three greyscale states, "Sound Abacus" base, built in the real shell); every ruling and why: `_work/G1-380-critic.md`. (m) = re-measured 2026-09-23 by read-only node over `lib/b3-common.js bank()` / `approvedWords()`, `lib/b3-picture-index.js hasPicture()`, `lib/b2-common.js vocab()`, `frontend/config/topics-taxonomy.json`, `frontend/content/seo-landing/<loc>.json`, `scripts/seo-landing/gen-b5-landings.js:82-94 LEVEL_KEYS`, `primitives/font-metrics.json` (scratch `scratchpad/G1-380-{owned,owned2,hunt,vocab2,sheet}.js`); (m B) = measured by designer B in `page/shell.js buildPage` with the shell woff2 from `file://` (`scratchpad/G1-380-B-*.png`, which the editor opened: base_fi4, f1, f2_de); (m A) = A's canvas glyph widths on the real woff2. Pictures OPENED: by A (`vehicles/ship`, `pets/fish`, `around the house/brush` = a paint brush, `furniture/chair`, `furniture/bench`, `body parts/thumb`, `easter/feather`, `animals/sheep`, `clothing/shoe`, `easter/chick`, `breakfast/cheese`, `body parts/tooth`, `body parts/mouth`, `fruits/cherry`), by B (A's set plus `farm animals/sheep`, `pets/mouse`, `classroom/book`, `around the house/key`, `forest creatures/frog`, `pets/turtle`, `clothing/shirt`, `zoo animals/sloth`, `vegetables/tomato`, `around the house/oven`, `beach/wave`, `around the house/pillow`, `fruits/pear`, `accessories/cape`, `vehicles/car`, `fruits/peach`, `christmas/chimney`, `farm animals/donkey`), by the editor for the refusal re-checks and F4 (`scratchpad/G1-380-crit-no.png`, `-crit-fi.png`: `shapes/cone`, `around the house/kitchen`, `winter/sled`, `accessories/necklace`, `breakfast/biscuit`, `At the Supermarket/cookie`, `clothing/dress`, `beach/island`, `thanksgivinng/haystack`, `At the Supermarket/jam`, `hospital/syringe`, `around the house/toolbox`, `music/flute`, `accessories/whistle`, `farm animals/goat`, `around the house/mirror`, `around the house/fireplace`, `camping/campfire`, `spring/nest`, `animals/reindeer`, `vehicles/sailboat`, `classroom/tape`, `insects and bugs/centipede`, `colors/white`, `ocean life/narwhal`, `vegetables/garlic`, `winter/walrus`, `insects and bugs/wasp`, `animals/whale`, `body parts/brain`, `animals/donkey`, `farm animals/donkey`, `kitchen tools/oven`, `vegetables/onion`, `Things That Fly/owl`, `ocean life/oyster`, `body parts/ear`, `body parts/eye`, `farm animals/cow`, `zoo animals/kangaroo`). *est.* = the engineer measures in `render/one.js`. No em-dashes.

**Boundary (load-bearing).** This family owns **decoding ACROSS three different-sound letter teams**: the ITEM is fixed (one picture, one word, one sentence) and the child decides which of the page's teams it holds, where a team is two (or three) letters a locale's first-grade reading course teaches as ONE sound unit. Four shipped families fence it. (1) `spelling-rules` G2-315 + G2-324..328 owns every grapheme that is a unit of its bank (one sound, which SPELLING; m: en magic-e c-k-ck ee-ea ai-ay floss y-ies · de tt ll nn ss ck tz ie · es b-v c-qu g-gu r-rr ll-y plural-tilde · pt s-ss r-rr ch-x m-antes-de-p-b c-ç plural-ao · it doppie c-ch g-gh gn-gli sc-sch mp-mb chi-ghi · nl d-t ei-ij · sv dubbelteckning t-tt l-ll ng-ljudet sj-ljudet · da ll ng nd flertal · no dobbel-konsonant t-tt k-kk ng-lyden sj-lyden · fi kk tt pp ll astevaihtelu): those graphemes are OUT of this inventory, and no face asks "which spelling is right" (G2-325 rule detective, G2-326 sort by rule). (2) `syllable-reading` G1-306 + G1-330..334 owns syllables, rimes and BLENDS (G1-333 tr pl gr fl bl …): a blend (two sounds) is never a team, a chip or a distractor; fr titles never say "syllabes", pt titles always say "dígrafos" (the live pt face is "Sílabas com encontro consonantal"). (3) `letter-of-the-week` G1-311 "Sound of the Week" owns the ONE-UNIT hunt (trace the unit, circle the 4 of 8 pictures that have it, write a row), K-326 the position of a SINGLE letter, K-327 circle-and-count single letters in a word list, K-328 two-letter contrast: they fence MOVES, not inventory (ruling in §1). (4) `sound-boxes` K-318 family owns counting every phoneme (its bank boxes sh ch th as one box each): no face draws a box per sound, a tie arc or hak-dots. Also NOT beginning sounds K-221/228, picture-vocabulary K-225, read-and-do G1-341, cloze G1-350 (a whole word in a sentence gap). **No face traces, no face prints a word on base / F1 / F4, and no title uses the G1-311 phrasing** (live h1s, m: en "Sound of the Week: Trace the Digraph…", es "Dos letras, un solo sonido…", pt "Duas letras, um som só…", sv "…två bokstäver som bildar ett ljud", da "…to bogstaver, én lyd", no "…to bokstaver, én lyd"). **Visual signature: the SOUND BEAD** (B): every team is printed inside ONE stadium capsule because it is one sound; three states on every face: **given** = tealSoft fill + teal ring · **choice** = white + teal ring · **blank** = white + dashed coral ring (pencil here). Letters are always ink, never coral (coral letters are the syllable-reading / spelling-rules convention and vanish on a toner-saving printer).

## 1 Identity

| field | value |
|---|---|
| id / key / bands | `G1-380` / `digraphs` / base **G1**; F1 **K** (`K-371+ TBD by the emitter`); F2 F3 F4 **G1** (`G1-381+ TBD`); F5 **G2** (`G2-360+ TBD`); ids by `tools/alloc-b5var-ids.js` (exists, m). `assetClass: icon-placement`, `exerciseType: digraphs`. `apps.digraphs` ALREADY registered by Phase C `83556185` (m: `default_subject:'letters'`, `default_age_range:'6-8'`) with `axes['exercise-type'].digraphs` slug + name **en only** (m); the other 10 locales are added by `tools/register-b5-taxonomy.js` from table B. |
| theme axis | **OFF / THEMELESS** (`themeAxis:{applicable:false}`; landings `coordinate.theme:''`): pictures via `lib/b3-picture-index.js`, pinned per item in the bank. No single theme reaches 3 teams x 3 words (pedagogy, m). **No `unitAxis`** at launch: one team set per face per locale is data (`sets.*`); a set swap is never a face (doctrine 1). |
| CCSS (en, honest) | base, F2, F3, F4 **RF.1.3.a** verbatim ("know the spelling-sound correspondences for common consonant digraphs"); F1 (K) none, readiness (G1-311 already files en sh/ch/th as K readiness, m); F5 (G2) RF.1.3.a stated as grade-1 review in connected text. Strand = the EXISTING row `'Reading: Foundational Skills'` of `frontend/lib/seo/strand-names.ts:129` (m, 11 literals: de "Schriftspracherwerb: Grundlagen", fr "Lecture et compréhension de l'écrit", pt "Leitura/escuta", nl "Lezen: technisch lezen (…)", fi "Lukemisen perustaidot"); no new strand row. |
| new primitives | `primitives/team-bead.js` (B, geometry §2) · `templates/components-b5/digraphs.js` behind `templates/components-b5.js` (barrel exists, m) · `data/b5/digraphs.js` + `data/b5/locales/digraphs.<loc>.json` read by `lib/b5-common.js bank()` (exists, m; throws a REFUSAL on an absent locale block). |

**Table B** (level keys = `gen-b5-landings.js LEVEL_KEYS`, m; shipping sets are the design proposal the native panel signs)

| loc | genre head = rail name (panel signs) | ASCII slug | K / G1 / G2 keys | sets: exemplar (base, F2, F3, F5 target = first) · k (F1) · position (F4) | national framework (prose) |
|---|---|---|---|---|---|
| en | Digraphs | `digraphs` (registered, m) | `kindergarten` / `grade-1` / `grade-2` | sh ch th · sh ch · sh ch th | Common Core RF.1.3.a |
| de | Buchstabengruppen | `buchstabengruppen` | `vorschule` / `1-klasse` / `2-klasse` | sch ch au · sch au · sch ch au | Lehrplan Deutsch (Lesen) |
| es | Dígrafos (taxonomy only; REFUSED, 0 landings) | `digrafos` | `preescolar` / `primer-grado` / `segundo-grado` | REFUSED | SEP/NEM |
| pt | Dígrafos | `digrafos` | `educacao-infantil` / `1o-ano` / `2o-ano` | nh lh qu · nh lh · (F4 REFUSED) | BNCC Língua Portuguesa |
| fr | Sons complexes | `sons-complexes` | `maternelle` / `cp` / `ce1` | ou on oi · ou on · ou on oi | programmes officiels |
| it | Digrammi (taxonomy only; REFUSED) | `digrammi` | `infanzia` / `classe-prima` / `classe-seconda` | REFUSED | Indicazioni nazionali |
| nl | Tweetekenklanken | `tweetekenklanken` | `kleuters` / `groep-3` / `groep-4` | oe ui eu · oe ui · **oo oe ui** | SLO kerndoelen |
| sv | Bokstavskombinationer (taxonomy only; REFUSED) | `bokstavskombinationer` | `forskola` / `ak-1` / `ak-2` | REFUSED | Lgr22 |
| da | Bogstavkombinationer (taxonomy only; REFUSED) | `bogstavkombinationer` | `boernehaveklasse` / `1-klasse` / `2-klasse` | REFUSED | Fælles Mål |
| no | Bokstavkombinasjoner (taxonomy only; REFUSED) | `bokstavkombinasjoner` | `1-trinn` / `2-trinn` / `3-trinn` | REFUSED | LK20 |
| fi | Pitkä vokaali (RE-TARGET: long vowels) | `pitka-vokaali` | `esikoulu` / `1-luokka` / `2-luokka` | aa uu ää · aa uu · aa uu ää | OPS 2014 |

All 11 slugs checked against every `axes.*.*.slug.*`: 0 exact collisions (m; `digraphs` is this key's own en slug). Never "Buchstabenverbindungen" (= joined handwriting, Germanic panel) and never a title containing the locale's G1-311 name.

**The rule that locks the type.** Every item word comes from `approved-words-<loc>.json` and carries a panel-signed grapheme segmentation `seg[]` AND a phoneme label per element `snd[]` (from the locale's declared closed `phonemes` list). An item is valid on a page iff (a) exactly ONE page team is an ELEMENT of `seg` (never a substring test), (b) **no foil team's SOUND occurs anywhere in `snd`** (whatever letters spell it: de `Stern` holds /ʃ/ and cannot sit on a sch page; pt `caminhão` holds /k/ and cannot sit on a qu page), and (c) on the faces that PRINT words (F2, F3, F5) no foil team's letters stand outside a team element. Teams owned by `spelling-rules` or `syllable-reading` are never teams. Two spellings of one sound never share a page (`samesound`).

**Ruling on the letter-of-the-week / sound-boxes question (asked of the editor).** They fence MOVES, not graphemes. Evidence (m): G1-311 fixes ONE unit and hunts pictures (`verify face==='unit'`: hit iff `graphemes.includes(unit)`); the live landings ship ONE exemplar unit per locale; K-318 boxes a team as one box to COUNT phonemes. Reading them as inventory fences would refuse en (sh ch th are the only honest RF.1.3.a teams; left ph 9 · ng 9 · wh 5 = 2 teams) and pt (nh lh are the BNCC dígrafo head) and strip de/fr/nl of their Klasse-1 / CP / groep-3 core, while protecting nothing: no face here traces, hunts one fixed unit, counts phonemes or positions a single letter. What they DO fence is enforced: no trace lane, no box-per-sound, no single-unit hunt, no G1-311 title phrasing, F4 only ever positions a TEAM bead (never a letter in square boxes), F5 counts a TEAM in connected sentences (never letters in a word list).

**Refusals, re-measured by the editor (m; the lock test = >= 3 unowned teams x >= 6 approved + pictured + opened words; overturned only with evidence):**

| loc | measured (approved + coloured picture) | verdict + reason |
|---|---|---|
| es | ch 29 raw (28 without chef /ʃ/) unowned; ll 53 · rr 26 · qu 15 · gue/gui 8 are units of the bank rules `ll-y` · `r-rr` · `c-qu` · `g-gu` (`spelling-rules.es.json`, m); blends owned by `syllable-reading` (`tr-pl` complexUnits, m) | **REFUSED** (1 unowned team). The fence is the BANK, as for en ck/ee, de ie/ck, nl ei/ij, pt ch; only b-v ships live today, but loosening the fence for es alone would reopen every locale's inventory. Every candidate but ch also has a same-sound spelling (ll/y yeísmo, rr/r, qu/c, gu/g), which is exactly what spelling-rules teaches. |
| it | gn 12 · gl(i) 22 · sce/sci 13 · ch 36 · gh 11 all units of `c-ch` `g-gh` `gn-gli` `sc-sch` `chi-ghi`; qu 8 = /kw/ (two sounds) | **REFUSED** (0 unowned teams). |
| sv | tj 1 (tjur; stjärna = stj, owned) · kj 1 (kjol; skjorta = skj owned; strykjärn = stryk-järn seam) · dj 1 · gj 0 · hj 3 · lj 2 (ljus, ljusslingor; lilja/fåtölj/medalj sound the l); dj/gj/hj/lj are ONE sound (j-ljudet: a samesound group, max one per page); sv has no diphthong teams; sj-, ng, ck owned | **REFUSED** (0 teams >= 6). |
| da | re-target "stumme bogstaver": hv 6 raw (hval hvalros hveps hvidløg narhval + `hvid`), **`colors/white` OPENED = a white paint drop** (a child says "dråbe") → **hv 5**; hj 4 (hjerne hjort hjerte løbehjul); ld 6 by ear (guldfisk guldsmed hylde skulder skildpadde landskildpadde; `bold`/`fodbold` panel, koldt/regnfuldt/stormfuldt are weather adjectives, bulldozer a seam); rd 6 (bord natbord skrivebord jorden jordbær tordenvejr; 3 of one stem); nd owned (`nd` rule) | **REFUSED** twice over: (1) 2 teams >= 6; (2) a silent-letter team is not ear-discriminable: the team's sound is the sound of its OTHER letter, so rule (b) removes most items (`hval` holds /l/ = ld's sound; after it hv keeps 1: hveps). Recorded for a future `spelling-rules` da fan ("stumt h", "stumt d"), where a silent letter is a spelling choice. |
| no | kj 6 raw (kjeks kjegle kjole kjøkken kjelke halskjede), **`shapes/cone` OPENED = a frustum / lampshade** → **kj 5** (`kjøleskap` is pictured but quarantined by the phonics gate); diphthongs examined (not in the pedagogy): ei 11 (geit speil peis reir reinsdyr seilbåt teip tusenbein …) · øy 6 (fløyte høystakk øy syltetøy sprøyte verktøykasse; fornøyd = emotion) · au 5 (maur tau saus sau dinosaur; the -saurus names are loans); stum d/h fail as in da | **REFUSED** (2 teams >= 6: ei, øy). Near miss recorded: kj needs ONE approved pictured word, au ONE; a future vocab commission, never padded. |

## 2 The base page

**Concept. "Which Letter Team Do You Hear?" = the Sound Abacus** (B's apparatus, measured in the real shell; the answer act is A's and the pedagogy's CIRCLE). One teal-framed abacus fills the body: across its top wire hang the three KEY beads (given, tealSoft) in page order; below, eight wires run between two teal rails, each carrying the SAME three choice beads in the SAME columns; left of each wire, outside the rail, one picture in a cream cap. The child says the picture's name and circles the one bead on that wire whose team she hears. No word anywhere on the body. A teacher checks the page in one glance down each column.

**Chrome budget.** Body **722** (3-line title + 3-line instruction); every stack also checked at **677** (4-line fi title). Lane **639** at default padding (no override).

**Layout d2**
```
.dg-abacus[data-ws-content]  639 wide, border 3 teal, r 16, fill white, padding 8 / 12 / 10 / 12  -> content 609
x:  0   20        88  104                                        609
    | # | cap 68  | 16 | rail L ======= wire zone 505 ======= rail R |
key row  h 60        key wire teal 3 @ y 30; key beads 92x52 tealSoft, Baloo 2 700 32, ink
row i    minmax(68px,1fr), gap 4: numeral Nunito 800 14 inkSoft | cap 68x68 cream, creamDeep 2, r 12,
         icon 60 contain | wire grid 2 at bead mid, rail to rail (+-0.5) | 3 choice beads 76x44,
         white, teal 2.5, Baloo 2 700 26 ink, centred on columns 505 x 1/6, 3/6, 5/6 = 84.2 / 252.5 / 420.8
vertical at the minimum: 3 + 8 + 60 + 8 x 68 + 7 x 4 + 10 + 3 = 656 <= 677 <= 722   (m B, base_fi4: 0 overflow)
at 722 rows grow to 76.25; horizontal 20 + 68 + 16 + 505 = 609 + 24 + 6 = 639
```
Air between beads 168.3 - 76 = **92 px**; outer beads 46 px from the rails; a pencil ring (bead + ~10 px each side) never reaches a neighbour horizontally (72 px spare) or vertically (row 68, bead 44: 28 px to the next bead). Floors (G1 44 / 26): bead 44 high, team text 26 px, picture 60, numerals 14 (lint 9).

**Ladder** (resolved config; guards key on THESE keys, never the level index)

| key | d1 | d2 (ships) | d3 |
|---|---|---|---|
| `mode` | `'base'` | **`'base'`** | `'base'` |
| `wires` | 6 | **8** | 10 |
| `teams` | 2 (`sets.k`) | **3 (`sets.exemplar`)** | 3 |
| `perTeam` | [3,3] | **[2,3]** | [3,4] |
| `maxRun` (same answer column, consecutive wires) | 2 | **2** | 2 |
| `iconPx` / cap | 64 / 72 | **60 / 68** | 48 / 54 |
| `rowMin` | 80 | **68** | 56 |
| `bead` | 88x44, 28 | **76x44, 26** | 76x44, 26 |
| `requireFoilLetterInWord` | false | **false** | true (a foil team's single LETTER occurs in the word, never its sound: en `thumb` holds the h of sh/ch) |
| `showWord` | false | **false** | false |

**d3 ruling:** more of the same move (10 wires, foil letters) and not shipped; at the minimum it measures 680 (m B), 3 px over 677, so if it is ever published its key row drops to 52. No copy mentions d1 or d3.

**Instruction (en source):** "Say the name of each picture. Circle the letter team you hear." (63; names pictures + letter team, both on the page; never a bead noun: pt "conta" is also a sum)

**Composer.** From the locale-neutral seed: draw 8 items from `teams[t].items` of `sets.exemplar` so each team answers 2-3 wires; reject a draw that breaks rule (a)/(b), repeats a stem (`skildpadde`-type pairs share a `stem` tag), or puts two items with one picture file on the page. Wire order: seeded shuffle re-drawn until `maxRun` holds AND the answer-column sequence is not `i mod 3` (or its reverse) for >= 6 consecutive wires AND no column answers wires 1-3. The seed carries no locale, so every check runs per locale on the SHIPPED instance.

**Answer-hiding + uniqueness.** All row beads are identical 76x44 whatever the team length (`sch` and `ch` alike), in one fixed page order under the key; nothing prints a word, a first letter or a count. Stamps: root `data-lcs-face="base" data-lcs-teams="sh|ch|th"` + `data-ws-content`; wire `data-lcs-wire=<i> data-lcs-key=<vocabKey> data-lcs-seg="sh|i|p" data-lcs-snd="ʃ|ɪ|p" data-lcs-answer-col=<j>`; row bead `data-lcs-bead=<team> data-lcs-col=<j>`; key bead `data-lcs-keybead=<team>`. `verify(page)` re-derives each wire's answer = the unique page team that is an element of `seg`; FAILS on 0 or >= 2, on a foil team's sound in `snd`, on `data-lcs-answer-col` disagreeing, on per-column counts outside `perTeam`, on `maxRun` or the staircase; and measures the render: every row bead of column j centred within +-0.5 px of key bead j, all row beads identical w/h, each bead's text width <= w - 24, wires end on the rails +-0.5, body <= available height, every picture resolves (throw, never blank).

**Reused (exact).** `positionKey({idx, cell})` (`templates/components-b3/letter-of-the-week.js:78`, m) F4 · `blankNumeralBox({w,h,answer,attrs})` (`templates/components-b3/ordinal-numbers.js:151`, m) F5 · `.ws-match` / `.ws-match-col` / `.ws-match-item` / `.ws-match-dot--left|right` (`page/page.css:354-391`, m) F1 F3 · `svgRoot el roundedRect line label` (`primitives/_svg.js`) · tokens of `primitives/_tokens.js` · `pictureFor/hasPicture` (`lib/b3-picture-index.js`, themeless, BW dirs skipped, `space/sun` blocked) · `bank('digraphs', loc)` (`lib/b5-common.js`) · the reviewed `graphemes` of `letter-of-the-week.<loc>.units[].items[]` (m: en sh 17 ch 17 th 12 · de sch 22 ch 18 ei 19 au 14 · pt nh 20 lh 14 · fr ou 27 on 27 an 24 oi 19 · nl oe 17 ui 16) as the SEED of `seg` (the panel re-signs and adds `snd`).

**NOT used.** `soundBoxes` / tie arc / `hakDots` (K-318's mark) · `letterChips` and `positionCard` (K-326's square boxes; `letterChips` 48 px does not hold `sch` at a readable size, m A) · spelling-rules `gapWord` / `ruleBox` (the spaced word with dashed letter boxes is G2-315's picture) · `syllableArcs` (a bowl means a syllable here) · `answerBox` (stamps `data-lcs-answer="undefined"` when open) · coral LETTERS anywhere · A's coral sound bar (the bead already says "one sound"; two marks for one idea) · `cardGrid` on the base · `unitAxis` / theme axis · any picture on the rejected list (§4).

**NEW `primitives/team-bead.js`** (B's primitive, pure SVG, px geometry, stroke never scaled):
```
teamBead({ text = '', w, h, mode = 'given'|'choice'|'blank', fontPx, lines = null, stubL = 0, stubR = 0, data = {} })
  -> { svg, width: w + stubL + stubR, height: h, textW }
sw = 2.5 ; rx = (h - sw) / 2 ; capsule rect x = stubL + sw/2, y = sw/2, w - sw, h - sw, rx
  given  : fill tealSoft, stroke teal 2.5            choice : fill white, stroke teal 2.5
  blank  : fill white, stroke coral 2.5 dash '6 5' (the house blankbox convention; greyscale-legible)
  text   : ONE <text> (never one per letter), Baloo 2 700 fontPx, fill ink, anchor middle,
           baseline y = round((h - (asc + desc)·fontPx)/2 + asc·fontPx) with asc 0.6719, desc 0.1875
           read from primitives/font-metrics.json "baloo2-700" (m) at build, never typed in
  lines  : (blank only) { baseline, mid } in bead px, passed in by the caller from the MEASURED metrics
           of the word printed beside it (F2); drawn grid 1.5 solid / grid 1.5 dash '3 4',
           from x = stubL + 0.7·rx to stubL + w - 0.7·rx
  stubs  : wire segments at y = h/2, grid 2, ONLY on the abacus / F4 socket wire; never inside a word
throws: h < 34 ; w < h ; text && textW > w - 24 ; mode 'blank' with text ; stubs inside a word row
textW from the committed primitives/team-bead.widths.json (tools/measure-team-beads.js, Baloo 2 700 on
the shell woff2, like measure-font-metrics.js); a team missing from the table THROWS
stamps: data-lcs-bead-mode, data-lcs-bead-text (given / choice only)
```
Measured Baloo 2 700 widths at 26 px (m B): sh 27.5 · ch 27.6 · th 25.2 · sch 40.3 · au 28.8 · ei 21.2 · eu 29.0 · nh 30.0 · lh 22.2 · qu 29.9 · ou 30.0 · on 30.0 · oi 22.1 · oe 29.1 · ui 22.0 · oo 30.1 · aa 27.7 · uu 29.9 · ää 27.7 · eau 42.9 (widest, not shipped); at 56 px sch 86.7 (m A). Band minima: K bead h >= 56 and letters >= 30; G1 h >= 44 / 26; G2 h >= 36 / 22.

**NEW `templates/components-b5/digraphs.js`**: `soundAbacus` (base) · `teamHouses` (F1) · `beadBank` + `gapWordRow` (F2) · `beadWord` (F3, inline HTML bead: `.dg-bead` inline-flex, height 42, padding 0 7, border 2.5 teal, radius 21, background tealSoft, same font as the word) · `socketCard` (F4) · `sentenceLane` (F5). Scoped inline CSS; no `page.css` edit.

**Alternatives (rejected).** A's Team Table (captain tiles over rows of quiet tiles, coral bar under each): calm and legible, but unmeasured in the shell and its bar duplicates the bead; the abacus was measured to 677 with 0 overflow and gives the family a signature no neighbour has. The pedagogy's 8 cards x 3 chips (indistinguishable from G2-325 / K-326 / G1-311 across the room). B's "colour in the bead" (needs a bead noun in 11 languages, crayon not pencil, slower). A "team train" and "team jerseys" (decoration that reads as more pictures).

**Risks -> mitigations.** A picture said with another word (the cherry-as-apple class): every item `picOpened:true`, rejected list in the bank. A foil sound hidden in an item (de st/sp, pt c/k): rule (b) on signed `snd`. 24 identical beads feel heavy: tealSoft key row is the only fill, wires in light grid. A child circles a key bead: harmless, key beads are pre-filled. **Palette:** cream creamDeep white teal tealSoft coral ink inkSoft grid. **Font floor:** smallest body text 14 px. **Print check (engineer, mono laser 100 %):** base en + fi, F2 de, F3 fr, F4 nl, F5 pt: tealSoft vs white beads distinguishable, dashed coral legible, every picture says its word.

## 3 Faces 2-6

All five are **CODE** faces on one additive `mode` knob (`'sort-two' | 'gap' | 'match' | 'position' | 'text'`); base `mode:'base'` stays byte-identical; each face stamps `data-lcs-face=<mode>`; guards key on `d.mode` and the named config. Each face changes what the child DOES (categorise by ear at K, produce, read, locate, find in connected text), has its own resolved d2 config, its own apparatus and its own query head. Every face keeps the bead.

### F1 : Sort by Letter Team (K, CODE `mode:'sort-two'`)
**Move:** CATEGORISE by ear into two teams at K density; no writing, no chips per picture. **Layout (A's frame, B's bead crown):** two TEAM HOUSES (white panel, teal 2.5, r 16) at left and right, each crowned 24 px below its top by ONE given bead 150x90, Baloo 2 700 56 (K answer floor 30, min element 56); between them one column of 6 cream picture cards 120x88 (icon 72, K floor 56), each with a `.ws-match-dot` on BOTH sides, so every picture is equidistant from both houses (no proximity cue by construction; B's hanging necklace put pictures next to one bead and needed a near/far patch, and its opened render left ~250 px of dead page). Widths **190 + 66 + 120 + 66 + 190 = 632 <= 639**; heights 6 x 88 + 5 x 12 = **588 <= 677 <= 722**, rows `minmax(88px,1fr)` (houses stretch to the stack). **Config d2** `{mode:'sort-two', pictures:6, bins:2, split:[3,3], maxRun:2, iconPx:72, houseBead:{w:150,h:90,fontPx:56}}`; d1 `{pictures:4}`, d3 `{pictures:8, split:[4,4]}` not shipped. **Verify:** `data-lcs-bin-of` per picture = the `sets.k` team in `seg`; houses === `sets.k`; split 3/3 (poison 6/0 fails); no 3 consecutive pictures of one house; both house beads identical w/h; no word printed. **Refusals:** none among the six shipping locales; the landing states K is readiness everywhere (teams are grade-1 content). **Instruction (en source):** "Say each picture's name. Draw a line from each picture to the letter team you hear." (84) **Query face:** "+ kindergarten / Vorschule / maternelle / educação infantil / kleuters / esikoulu" + sort.

### F2 : Write the Missing Letter Team (G1, CODE `mode:'gap'`)
**Move:** PRODUCE the team in writing inside the word. **Layout (B's empty bead, A's capital rule):** a `beadBank` at the top (a short teal 3 wire 360 wide with the three given beads 90x52, Baloo 32; NO frame around the bank: B's dashed-coral bank banner read as a place to write, seen in `B-f2_de.png`), 12 below; then 8 rows `minmax(64px,1fr)`, 1.5 px dashed creamDeep separators: numeral 22 · cap 64 (icon 54) · 28 · the word in Baloo 2 700 32 ink with the team replaced by ONE blank bead, **no wire stubs** (the opened B render drew "S-( )-af", which reads as hyphens). Bead size is ONE per page, fixed by the page's longest team: **96x50 for 2-letter sets, 116x50 when a 3-letter team is in the bank** (de sch), so its size never tells `ch` from `sch`. Bead lines from the MEASURED word metrics (`font-metrics.json` baloo2-700, F = 32): the bead baseline sits on the word's rendered baseline, mid = baseline - 0.5·F (x band 16 px), writing height top-to-baseline 21.5 px ascender band + the bead's own clearance (B's 0.72 h / 0.42 h factors are replaced). The bead is NOT a `data-lcs-starter` surface (the ruling-starters D/E asserts are for sentence starters); the family gate measures baseline +-1 and mid +-1 itself. **Capital rule (A, confirmed on the opened B render):** a team at the start of a capitalised noun is never gapped (de `Schaf`, `Schiff`, `Schuh`, `Schlüssel`, `Schildkröte` in B's page would need a capital the lower-case bank does not show, and B's "S" + bead + "af" split taught `ch` in Schaf); `gapInitialCapital:false` is a pool filter checked by the validator. Stack 56 + 12 + 8 x 64 + 7 x 1.5 = **591 <= 677 <= 722**; row width 22 + 64 + 28 + word <= 12 letters (~210) + bead 116 + 6 = **446 <= 639** *est.* **Config d2** `{mode:'gap', rows:8, bankTeams:3, perTeam:[2,3], gapBead:'page-max', wordPx:32, maxLetters:12, gapInitialCapital:false}`; d1 `{rows:6}`, d3 `{bank:false}` not shipped. **Verify:** `data-lcs-gap-from/len` = the team's seg span; every gap bead identical w/h (+-0.5 px, measured); frame uniqueness: `pre + t + post` for every OTHER bank team is not a word of the locale's WHOLE approved list; the team letters never printed in the row; no bead on a capital-initial team. **Refusals:** none. fi: the gap is the long vowel (2 letters, bead 96x50), bank aa uu ää; the frame rule refuses tuli/tuuli-type second readings. **Instruction (en source):** "Say each picture word. Write the missing letter team from the top in the dashed space." (84) **Query face:** missing digraph · fehlende Buchstabengruppe · complète avec le bon son · complete com o dígrafo · vul de klank in · puuttuva pitkä vokaali.

### F3 : Read and Match (G1, CODE `mode:'match'`)
**Move:** READ (eye to meaning): the direction flips from ear to print. **Layout (B):** `.ws-match` (padding 6 30): left 6 plain items 250x92 printing a word in Baloo 2 700 32 with its team inside an inline given bead (`beadWord`, height 42, tealSoft; letters stay ink); right 6 cream items 120x92, icon 80; coral match dots. Stack 6 x 92 + 5 x 12 + 12 = **624 <= 677 <= 722** (m B); widths 60 + 250 + 120 = 430, line gap 209 (arithmetic). Word width at 32 px <= 226 (measured table; m B `Schildkröte` 165 + 19); wider words leave the pool, the font never shrinks. **Config d2** `{mode:'match', pairs:6, teams:3, perTeam:2, wordPx:32, iconPx:80}`; d1 `{pairs:4}`, d3 `{beadInWord:false}` not shipped. **Verify:** `data-lcs-pair` both sides; derangement (no word in its picture's row, no reversal, not a rotation by 1); 2 words per team; one bead per word, its text === the team element of `seg`; <= 10 letters. **Refusals:** none. **Instruction (en source):** "Read each word. The letter team is marked. Draw a line to the picture the word names." (84) **Query face:** read digraph words · Wörter mit sch lesen · lire des mots avec ou · leia palavras com dígrafos · lees en koppel · lue ja yhdistä.

### F4 : Where Is the Letter Team? (G1, CODE `mode:'position'`)
**Move:** LOCATE the team's sound in the spoken word: beginning, middle or end. **Layout (B's socket wire):** 2 x 4 cards 313x150 (the one face in cards, since each item has its own team): picture 92 at left; right column 185: the item's team as a given bead 70x40 (Baloo 26), then three BLANK sockets 56x44 (G1 floor 44) threaded 8 px apart on one grid 2 wire (3 x 56 + 2 x 8 = 184), each under a `positionKey` (cell 12: first / middle / last square filled). Card interior 8 + 92 + 12 + 185 + 8 = **305 <= 313**; heights 40 + 10 + 12 + 4 + 44 = 110 <= 134; grid 2 x 313 + 13 = **639**, 4 x 150 + 3 x 12 = **636 <= 677 <= 722**. Distinct from K-326: a team bead, capsule sockets on a wire, never square boxes, never a letter. **Config d2** `{mode:'position', cards:8, set:'position', posSplit:[[2,3],[2,3],[2,3]], iconPx:92, socket:{w:56,h:44}}`; d1 `{cards:6, positions:['beginning','end']}`, d3 `{cards:10}` not shipped. **Position** = index of the team element in `seg` after dropping elements tagged `silent` (fr `loup`: p silent → end); whole-word teams (nl `ui` = onion, no `øy`) are excluded (beginning = end). **Verify:** `data-lcs-pos` re-derived from `seg` minus `silent`; each position used 2-3 times; each card's bead === its item's team ∈ `sets.position`; the answer socket position not periodic in reading order. **Refusals (m):** **pt REFUSED** (nh 31 · lh 24 · que/qui 20 pictured: no word ends in nh / lh / qu; "end" unreachable). **nl** ships with `sets.position = oo oe ui` because the exemplar reaches only 2 beginnings (uil, oester; `ui` onion is whole-word; `ocean life/oyster` OPENED reads as a shell), while oor / oog / oogst / uil give 4 and koe / kangoeroe / zeekoe / trui 4 ends. **fi CONDITIONAL:** beginnings are exactly aasi (donkey OPENED: long ears, reads donkey) · aalto (wave OPENED) · uuni (`around the house/oven` OPENED: a whole cooker, which a Finnish child may call "liesi" / "hella"); if the fi panel rejects uuni, F4 fi is refused and the matrix lowered before the wave. en / de / fr pass (m: en middle mushroom, feather, bathtub, armchair; de 59 beginnings, 34 ends; fr beginnings ours, oie, ongle, oiseau). **Instruction (en source):** "Say each picture word. Where do you hear the letter team? Colour one space: beginning, middle or end." (99) **Query face:** where is the digraph · Wo hörst du sch, ch oder au? · où entends-tu le son · waar hoor je de klank · missä kohdassa pitkä vokaali kuuluu.

### F5 : Letter Teams in Sentences (G2, CODE `mode:'text'`)
**Move:** FIND one team in CONNECTED TEXT and count per sentence (K-327 counts single letters in a word list; G2-325 hunts a spelling-rule's letters in single words). **Layout (B, one more line of headroom):** a header holding ONE big given bead 104x58 of the target team (Baloo 36), centred, nothing else; 16 below; three sentence lanes (cream, creamDeep 2, r 14, padding 10 14): numeral 22 · 10 · the sentence in Nunito 800 26 px on a 52 px line (13 px of air above and below each line so a pencil ring never crosses the next line), **<= 3 rendered lines** · 16 · `blankNumeralBox({w:56, h:52})`. Width 22 + 10 + 501 + 16 + 56 = 605 <= 611 inner. Stack at 3 lines 58 + 16 + 3 x (3 x 52 + 20) + 2 x 20 = **642 <= 677 <= 722**; typical 2-line lanes 486, `space-evenly`. G2 floors (36 / 22): box 56x52, text 26. The target team is NOT marked inside the sentences. **Config d2** `{mode:'text', sentences:3, target:'sets.exemplar[0]', hitsPerSentence:[1,4], total:[5,8], textPx:26, lineH:52, maxLines:3}`; d1 `{sentences:2, markTarget:true}`, d3 `{targets:2}` not shipped. Targets: en sh · de sch · pt nh · fr ou · nl oe · fi aa. **Verify:** count per sentence = number of target ELEMENTS in the signed token `seg` (never `split(target)`); the target is never a proper substring of another grapheme element in any token (a `ch` target over a `Schule` token FAILS; `sch` is the de target for this reason); no falsePair token; numerals never printed; each lane <= 3 rendered lines (measured). **Refusals:** none (6 sentences per locale are panel-authored; 3 ship). **Instruction (en source, the team written in by the panel, no token):** "Read the sentences. Circle every sh. Write how many you found in each sentence in its box." (88) **Query face:** digraphs in sentences · sch in Sätzen · le son ou dans des phrases · dígrafo nh em frases · tweetekenklanken in zinnen · pitkät vokaalit lauseissa.

**Rejected non-moves.** "Circle the pictures that have sh" (G1-311) · "sh or ch?" two spellings of one sound (G2-324) · count the sounds (K-318) · trace the team (G1-311) · a second team set as a face (a data swap) · en vowel teams ee/ea/ai/ay (owned) · fi diphthongs (read letter by letter in Finnish reading instruction; the fi point is length) · colour-by-team (black ink) · a word-search grid (wordsearch owns it) · da/no silent letters as a face (spelling memory, not decoding by ear; §1).

## 4 Native rebuild plan x11

Every body string is a whole literal: team letters inside beads (data), printed item words (F2, F3), panel sentences (F5), and the six titles + six instructions; the team letters of a face are WRITTEN INTO its title and instruction by the panel (no `{L}` token, no case frame; fi writes "Ympyröi jokainen aa" or the form its sentence needs). No `objForms`, no inflected vocab. Per shipping locale the panel authors: `phonemes` (closed list) · per item `seg` + `snd` + `silent` + `stem` + `picOpened` (en ~90, de ~110, pt ~75, fr ~200, nl ~120, fi ~110 items *est.*) · `falsePairs` >= 6 · `sets` {exemplar, k, position} · 6 sentences with signed tokens · 12 strings (pt 10). **The EN strings are a SOURCE TO AUDIT**: every panel reads them and reports defects, not only its own.

| loc | literals | forms / slots | refusal / re-target | traps |
|---|---|---|---|---|
| en | 12 strings; seg+snd ~90 (46 signed in letter-of-the-week); falsePairs | none | none | ch = /tʃ/ only (chef, orchid, architect, mechanic, chinchilla out); tch words out (kitchen, watch, pitcher, stretcher, crutches); ng as /ŋ/ only; th /θ ð/ one team; grasshopper, lighthouse, sunglasses, toothbrush (th + sh) out; `white` paint drop; -fish compounds <= 2 per page; rejected pictures: `fruits/cherry` + `fruits/peach` (read "apple"), `christmas/chimney` (a fireplace), `body parts/mouth` (said "lips") |
| de | 12 strings; seg from the `chunks` layer, re-signed (chunks store sp/st as chunks) | nouns keep the capital | none | chs = /ks/ (Dachs Fuchs Ochse Eidechse Sechseck) never ch; Häs-chen / Radies-chen morpheme seams out; initial st / sp carry /ʃ/ (rule b); eu / äu never together; Chinchilla, Christbaum, Chef out; F2 never gaps an initial Sch / Ch / Au; never "Buchstabenverbindungen"; rail name "Buchstabengruppen" is the panel's call |
| es | `refused:{reason, measured}` only | - | **REFUSED** (§1) | - |
| pt (BR) | 10 strings; seg+snd ~75 | none | **F4 REFUSED** | qu / gu only where u is silent before e/i (quadrado, água, língua, pinguim [gw] out); nh / lh items hold no /k/ (caminhão, cozinha, rosquinha, parquinho out) and qu items no nh / lh; ch owned (ch-x); titles always "dígrafos", never "encontro consonantal" and never "Duas letras, um som" (G1-311) |
| fr | 12 strings; phoneme tag per word (substring is not sound); `silent`; seg ~200 | NBSP before `: ? !` | none | on / an / in only when nasal (bonnet, banane, ananas, camionnette out); -ien out of en; oignon oi = /ɔ/; ain / ein words are their own grapheme; om / am / em / im owned; ch owned (syllable-reading); an/en, ai/ei, au/eau/o never on one page; never "fiche", never "syllabes" in a title |
| it | `refused` only | - | **REFUSED** (§1) | - |
| nl | 12 strings; seg from chunks, re-signed | none | none | ei/ij owned, ou/au never together; IJ is one grapheme; whole-word `ui` out of F4; loans (aubergine, auteur, beige) out; `ocean life/oyster` reads as a shell |
| sv | `refused` only | - | **REFUSED** (§1) | - |
| da | `refused` only | - | **REFUSED** (§1) | - |
| no | `refused` only | - | **REFUSED** (§1) | - |
| fi | 12 strings; seg ~110 (a long vowel = ONE element `aa`) | none | re-target long vowels; diphthongs out | compound seams (maa-apu type) out; loans (shampoo, smoothie, frisbee) out; colour and emotion adjectives out; `uuni` vs "liesi" (F4 condition); "pitkä vokaali kirjoitetaan kahdella kirjaimella" is the landing's teaching sentence; `[NSR-FLAG][fi]` |

Every panel OPENS one render of every face in its locale and every picture its bank names (`picOpened:true` is the gate), and confirms by eye that each picture says its word, each bead holds the right letters, and no picture carries a foil sound.

## 5 Data + gates

`data/b5/digraphs.js` (locale-neutral: modes, rejected pictures, bead sizes) + `data/b5/locales/digraphs.<loc>.json` GENERATED from `i18n/.draft-b5-<loc>.json` by `tools/apply-b5-locale.js` after `tools/validate-b5-draft.js` (both exist, m; `data/` gitignored, `git add -f`); read via `bank('digraphs', loc)`; strings checked by `tools/check-b5-string-parity.js` (exists, m).
```
DIGRAPHS_LOC (data/b5/locales/digraphs.<loc>.json):
{ head, refused: null | { reason, category: 'owned'|'count'|'silent-letter', measured: { '<team>': n } },
  phonemes: ['ʃ','tʃ','θ', ...],
  teams: { '<t>': { t, sound: ['ʃ'], items: [
      { vocabKey, theme, noun, word, seg: ['sh','i','p'], snd: ['ʃ','ɪ','p'], silent: [], stem, picOpened: true } ] } },
  samesound: [['ou','au'], ...],
  owned: [...],               // derived by the validator from the spelling-rules + syllable-reading banks, never typed
  falsePairs: [{ word, letters, why }],
  sets: { exemplar: ['sh','ch','th'], k: ['sh','ch'], position: ['sh','ch','th'] },
  sentences: [{ id, text, tokens: [{ w, seg }] }],          // >= 6
  rejectedPics: ['fruits/cherry','fruits/peach','christmas/chimney','colors/white','shapes/cone', ...],
  strings: { 'G1-380': {title, instruction}, '<face ids>': {title, instruction} } }
```
Helper contract: `teamItems(loc, t, {mode})` returns only items valid for the face (rule b against the page's other teams, capital rule for F2, whole-word rule for F4); `positionOf(item)`; `pageTeams(loc, mode)`; every getter THROWS on a refused locale or a missing literal (never an en fallback).

**`tools/validate-b5-draft.js` (digraphs block; every rule runs, exit 1 on any):**
1. Every item's `vocabKey` is in `approved-words-<loc>.json` with `word` === its approved word; `hasPicture` true; its theme dir carries no localized B&W marker (BW SW BN NB ZW SH PB MV SV); `picOpened:true`; `theme/noun` not in `rejectedPics`.
2. `seg.join('')` === word (case-folded, NFC); `snd.length === seg.length`; every `snd` symbol ∈ `phonemes`.
3. The item's team is an element of `seg` exactly once and its `snd` ∈ `teams[t].sound`.
4. No team ∈ `owned` (derived at run time from `bank('spelling-rules', loc)` rule `cands` + `bank('syllable-reading', loc)` units / complexUnits); no team is a blend (two different consonant phonemes).
5. **Foil sound:** for every set in `sets` and every item of a team in it, no OTHER team of that set has a sound in the item's `snd`.
6. **Foil letters (print faces):** for `exemplar` items, no other exemplar team's letters occur in the word outside a team element (F2 / F3 draw only from items passing this).
7. `falsePairs` words never appear as items of the paired team; >= 6 entries per shipping locale.
8. `samesound`: at most one member per set.
9. Floors: each team of each set >= 6 items after rules 1-8 AND after `stem` de-duplication; else the locale carries `refused` with measured counts and NO items.
10. F2: frame uniqueness against the WHOLE approved list for every other bank team; >= 3 non-capital-initial items per exemplar team.
11. F4: `positionOf` re-derived from `seg` minus `silent`; each position >= 3 items across `sets.position`, whole-word teams excluded; else F4 carries `refused` for the locale.
12. F5: >= 6 sentences; each 1-4 target elements; any 3 consecutive total 5-8; the target is never a proper substring of another grapheme element in any token.
13. Strings: titles <= 70, no worksheet word (the b5 `WORKSHEET_WORD` regex, m), no free-claim (`scripts/lib/free-claim.js`), unique in the band, never containing the locale's G1-311 phrasing (en "sound of the week", es "un solo sonido", pt "um som só", sv "två bokstäver", da / no "én lyd"), fr never "syllabe", de never "Buchstabenverbindung"; instructions <= 150 with an end mark, naming only on-page apparatus (base never "write" / "line"; F1 must "line"; F2 must name the dashed space; F4 must name beginning / middle / end; F5 must name the box); no bead noun in any locale.

**`qa/verify-b5-digraphs.js`:** renders base + 5 faces x 6 shipping locales x d2 through `render/render-instance.js` (file:// fonts) at the 722 stack AND the 4-line fi title (677); asserts `verify()` empty and `qa/lints.js` clean; then the floors ITSELF (no size lint exists): base beads 76x44 / 26 px, pictures 60; F1 house beads 150x90 / 56, pictures 72; F2 gap bead identical per page, word baseline and mid within 1 px of the bead's lines; F3 words 32, beads 42 high, pictures 80; F4 sockets 56x44, team bead 70x40; F5 text 26, box 56x52, <= 3 lines per lane; geometry from pixels (column alignment +-0.5, wires on rails, no stub inside a word row); answer tells measured on the SHIPPED instance (base answer columns, F1 house sequence, F3 right order, F4 answer sockets, F5 counts not monotone); every answer surface empty. SWEEP: 20 seeds x d2 per face render distinct pages.

**Poison (each must FAIL for its own reason; the correct EN draft is the control):** P1 en `grasshopper` as an sh item (rule 7) · P2 de `Häschen` as sch (7) · P3 de `Fuchs` as ch (3: `chs` = /ks/, so no element carries ch's sound) · P4 en `chef` as ch (3: snd /ʃ/ not in ch.sound) · P5 en `toothbrush` on a th + sh page (5) · P6 de `Stern` as an au item on a sch page (5: /ʃ/) · P7 pt `caminhão` as nh on a qu page (5: /k/) · P8 nl `ou` and `au` in one set (8) · P9 fr `banane` tagged `an` (3) · P10 es block with items and no `refused` (9) · P11 de `ie` as a team (4, owned) · P12 en `st` as a team (4, blend) · P13 fi `jääkaappi` on an ää + aa page (1 answer per rule 5 fails) · P14 fr `loup` with `silent:[]` and stored end (11) · P15 F5 sentence claiming 3 hits whose seg holds 2 (12) · P16 de F5 target `ch` over a `Schule` token (12) · P17 `picOpened:false` (1) · P18 `fruits/cherry` as an item (1) · P19 a da item `hvid` from `colors/white` (1) · **render:** PR1 an F2 row whose bead is wider than the page's others (identical w/h) · PR2 de F2 with `Schaf` gapped at its capital (capital rule) · PR3 a wire stub inside an F2 word (stub rule) · PR4 base row beads sized by team length (identical w/h) · PR5 base answer columns 0,1,2,0,1,2,0,1 (staircase) · PR6 F1 split 6/0 · PR7 F3 word opposite its own picture (derangement) · PR8 pt F4 rendered (refusal) · PR9 F5 lane at 4 lines (maxLines) · PR10 `answerBox` in place of `blankNumeralBox` in F5 (`data-lcs-answer="undefined"`).

**Page reads:** `DIGRAPHS`, `DIGRAPHS_LOC[loc]`, `primitives/team-bead.widths.json`, `primitives/font-metrics.json`, pictures via `pictureFor` of the pinned `theme/noun`; never `image-vocabulary.js` for a word form, never `approved-words` at render (the validator already copied the word), never `objForms`.

## 6 SEO plan

Titles are candidates the native panels rewrite (<= 70, no worksheet word, no G1-311 phrasing; the base owns the bare genre head, each face adds ONE noun or verb head). Locales in each cell: Germanic en / de / nl · Romance fr / pt · fi.

| face | title pattern | meta MIDDLE (whole 120-170; the child's instruction, en source) | coordinate |
|---|---|---|---|
| base | Digraphs sh, ch and th: Which Letter Team Do You Hear? / Wörter mit sch, ch und au: Welche Buchstabengruppe hörst du? / Tweetekenklanken oe, ui en eu: welke klank hoor je? · Les sons complexes ou, on, oi : quel son entends-tu ? / Dígrafos nh, lh e qu: qual você ouve? · Pitkä vokaali aa, uu, ää: mikä kuuluu sanassa? | Say the name of each picture and circle the letter team you hear, choosing from the same three teams on every row | `{type:'digraphs', mode:'base', theme:'', level:<G1 key>}` |
| F1 | Digraph Sort for Kindergarten: sh or ch / Bilder sortieren: sch oder au? / Sorteren op klank: oe of ui · Trier les images : ou ou on ? / Separe as figuras: nh ou lh · Lajittele kuvat: aa vai uu | Children say each picture's name and draw a line to the letter team they hear, sorting six pictures between two teams | `mode:'sort-two'`, K |
| F2 | Missing Digraphs: Write sh, ch or th / Fehlende Buchstabengruppe: sch, ch oder au / Vul de klank in: oe, ui of eu · Complète avec le bon son : ou, on ou oi / Complete com o dígrafo: nh, lh ou qu · Kirjoita puuttuva pitkä vokaali | Children say each picture word and write the missing letter team from the top in the dashed space inside the word | `mode:'gap'`, G1 |
| F3 | Read Digraph Words and Match the Pictures / Wörter mit sch, ch und au lesen und verbinden / Lees en koppel: woorden met oe, ui en eu · Lire des mots avec ou, on, oi et relier / Leia palavras com dígrafos e ligue · Lue sanat ja yhdistä kuviin | Children read six words with the letter team marked and draw a line from each word to the picture it names | `mode:'match'`, G1 |
| F4 | Where Is the Digraph? sh, ch and th in the Word / Wo hörst du sch, ch oder au? / Waar hoor je oo, oe of ui? · Où entends-tu le son ou, on ou oi ? / (pt refused) · Missä kohdassa pitkä vokaali kuuluu? | Children say each picture word and colour one space to show where they hear the letter team: beginning, middle or end | `mode:'position'`, G1 |
| F5 | Digraphs in Sentences: Circle and Count sh / sch in Sätzen finden und zählen / Tweetekenklanken in zinnen: zoek en tel oe · Le son ou dans des phrases : entoure et compte / Dígrafo nh em frases: circule e conte · Pitkät vokaalit lauseissa: etsi ja laske aa | Children read three sentences, circle every sh and write how many they found in each sentence in its box | `mode:'text'`, G2 |

`coordinate.mode` is always the face's mode string (base `'base'`), `theme:''`, level = §1 band key per face. h1 = title; eyebrow = level label; strand chip = `Reading: Foundational Skills` row; JSON-LD `LearningResource` with `educationalAlignment` RF.1.3.a (no `targetUrl`) on en base / F2 / F3 / F4 / F5, none on F1; non-EN prose names the national framework only. The fi landing states the re-target (long vowels, "lyhyt vai pitkä vokaali"). Meta lead inherits `seo.words.free_printable` (metadata only; no visible copy claims "free"); no title, meta or landing promises an answer key (printable-only decks ship none). MIDDLE measured with `tools/measure-instruction-window.js` (exists, m). `topicMeta.digraphs` >= 50 chars x 6 shipping locales (the refused five carry a taxonomy name only) + `skill-sentences.<loc>.json.digraphs`.

**Non-cannibalisation** (whole-landing 3-gram Jaccard via `scripts/seo-landing/gate.js`, FAIL >= 0.80 / WARN 0.65; *est.*, the gate measures):

| pair | what the copy says differently | est. |
|---|---|---|
| base vs G1-311 Sound of the Week | three teams, one picture per row, circle the team vs ONE unit, trace it, circle its pictures | 0.10 |
| F4 vs K-326 beginning / middle / end | a letter TEAM's sound, grade 1, sockets vs one LETTER, K, square boxes | 0.20 (highest; "digraph" / "Buchstabengruppe" in every head) |
| F5 vs K-327 circle and count | a team in three sentences vs single letters in six words | 0.12 |
| F5 vs G2-325 rule detective | find a decoded team in text vs find a spelling rule's letters in a word | 0.10 |
| F1 vs G2-326 sort by rule | sort by the sound heard (K) vs sort by spelling rule (G2) | 0.10 |
| F2 vs G2-315 spelling rules | write the team heard (three different sounds) vs write the rule letters | 0.14 |
| base vs K-318 sound boxes | pick one team vs count every sound in boxes | 0.08 |
| F3 vs K-225 picture vocabulary | decode a marked team vs name a picture | 0.08 |
| fr pages vs syllable-reading "Syllabes complexes br, bl, cr" | sons complexes (one sound) vs consonant clusters | 0.10 |
| pt pages vs syllable-reading "encontro consonantal" | dígrafo vs encontro consonantal (the pt lesson itself) | 0.10 |
| base vs F2 / base vs F4 (siblings) | same teams, different act | 0.18 / 0.15 |

Boundary sentence on every landing: "This page is about reading letter teams that make one sound; the sound of the week, spelling rules, syllables and sound boxes have their own pages."

## 7 Hub visibility contract

A face appears under `digraphs` on `/[locale]/worksheets` IFF: (1) `apps.digraphs` exists (PRESENT since Phase C `83556185`, m: `default_subject:'letters'`); (2) `axes['exercise-type'].digraphs` has `slug` + `name` in all 11 locales (en present, m; the other ten from table B via `tools/register-b5-taxonomy.js`, including the five refused locales, whose names never render); (3) exactly one landing per face per shipping locale with `coordinate.type === 'digraphs'` verbatim, the face's `mode`, `theme:''`, the §1 level key (F1 K, base F2 F3 F4 G1, F5 G2), a unique slug and `canonicalDeckSlug` = the published deck; a REFUSED face or locale has NO landing and the expectation is lowered explicitly, never padded; (4) the landing JSON committed AND deployed.

Gate: `node scripts/verify-hub-type-rows.js --keys=digraphs --expect=docs/worksheet-gen/b5-designs/hub-expectations.json` (exists, m; matrix exported by `tools/export-hub-expectations.js --batch=b5`; poison: drop one landing / drop `apps.digraphs` must FAIL).

**Expected rows per locale:** en 6 · de 6 · pt **5** (F4 refused: no word-final nh / lh / qu) · fr 6 · nl 6 (F4 on `sets.position` oo oe ui) · fi 6 (F4 conditional on the panel signing `uuni`; if refused, 5 and the matrix is lowered before the wave) · es **0** (1 unowned team: ch; ll rr qu gu owned) · it **0** (0 unowned teams) · sv **0** (0 teams >= 6) · da **0** (hv 5 after opening `colors/white`; silent-letter teams fail the foil-sound rule) · no **0** (ei 11, øy 6; kj 5 after opening `shapes/cone`, au 5) = **35** (34 if fi F4 falls).
