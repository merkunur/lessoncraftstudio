---
name: project-business-strategy-2026-07
description: "The 2026-07-03 monetization+SEO strategy analysis — evidence, verdicts, recommended architecture, and the build-commission queue awaiting operator ruling"
metadata: 
  node_type: memory
  type: project
  originSessionId: f78070ea-4307-4ebf-962f-3ac49de202aa
---

**Commission (2026-07-03, ANALYSIS only, delivered):** operator wants (a) offering into a subscription, (b) SEO dramatically improved. Report = `C:\Users\rkgen\.claude\plans\business-strategy-report-2026-07.md`; evidence files in the session scratchpad (`phase1-log-findings.md`, `phase2-remediation-delta.md`, `phase3-{de,nl-nordic,romance,gating}-findings.md`). Operator ruled the June premium-games north star **fully re-openable** for this analysis.

**Measured facts (don't re-derive):** ~33 Google clicks/day flat since April (GSC 2,970/3mo); 78% of clicks land on TOPIC pages, ~0 on the 30,078 landings or 45,309 decks; biggest click destination = seller-era 410s (ghost index); GSC 55,231 indexed / 87,401 not — indexing is WON, being-chosen is not (avg pos ~11, 0-CTR doom loop); 169 users, 0 subscribers; deploys cause 502 windows (~30k/14d, 600 to Googlebot); Cloudflare cache rate 4.4%; GPTBot 522k req/14d + rotating-Chrome-UA scraper fleet walks the site; Googlebot wastes 25% of crawl on redirects, fetched deck pages 10× in 14d.

**Verdicts:** programmatic corpus = "defensible but exposed" — theme fan-out (~43 noun-swap variants/type, 0.65-0.76 sibling similarity) is the HCU exposure; consolidate (noindex,follow zero-impression theme tail, keep pages live) don't expand. Market research ×10 locales: "free no-login interactive+printable paired" = EMPTY category everywhere (the differentiation thesis confirmed); everyone keeps SEO content free, monetizes adjacent; baby-flash (it) hard-gated itself out of its own SERPs (cautionary); sequenced/assembly content carries the premiums (fr 360€/yr, MX SEP, BR R$599/yr).

**Recommended architecture ("free discovery, paid depth"):** keep ALL discovery + deck play + catalog PDFs + activities free forever; premium = storybooks (class #1, lead-in first-pages-free, `isAccessibleForFree:false`+`hasPart` markup, server-side gate), games as built (#2), assembly/unit-packs+class-codes (#3), generator watermark-free/unlimited-export (access-guard exists), full workspace (dormant behind requireSubscriber). Never retro-gate; adult-facing gates only; no card-trials. Pricing: Teacher $69/yr (validated by Twinkl/Starfall/MatematikFessor), Family $39-45, School transparent $300/700/1200, BR regionalized, IT/ES = acquisition locales. June north star: shape CONFIRMED, sequence REVISED (storybooks-first, not games-first).

**SEO roadmap:** P0 zero-downtime deploys + ghost-index flush + bot rules/cache; P1 GSC-driven CTR surgery + consolidation wave + feed topic hubs; P2 Pinterest pipeline (R10, untouched, 45k pin assets ready) + storybook SEO surface + embed/backlink outreach (authority = the position-11 limiter); P3 locale depth nl/Nordics→de-Vorschule/DaZ→fr/it/pt seasonal. **Single highest-leverage move = the GSC-driven be-chosen pass (CTR surgery + theme-tail consolidation).**

**Commission #1 SHIPPED 2026-07-03** (commits `0141477e`+`1fd5930f`+`d806f110`+`1e1e414e`; evidence `C:\Users\rkgen\.claude\plans\commission-1-evidence-report.md`): zero-downtime deploys via `frontend/releases/current` symlink + `pm2 reload` (verified: 0×5xx across a full production deploy vs 1,008 502s that same morning; pm2 exec is now `releases/current/server.js` — deploy.sh GUARDS on it; one-time `server-scripts/migrate-to-releases.sh` already ran); nginx bot rules live (429 for stale-Chrome+_rsc fleet, Googlebot-exempt; 2,860 429s in first 15 min) + placeholder-410; `/audio/inventory.json` generated (200); `/xx/topic/<axis-key>` now 308s to the localized slug (fixes §22.2 sudoku class); hreflang mirror-parity test gates every deploy; indexnow-submit.js now git-tracked (server copy had vanished). **Cloudflare hardening LIVE + verified 2026-07-03 evening** (operator applied per docs/ops/cloudflare-hardening.md): Bot Fight Mode + WAF managed-challenge (Free-plan contains-form — `matches` is Business-only; fake-Googlebot challenged, REAL Googlebot passes per GSC live test) + 3 cache rules — deck pages HIT (respect-origin 300s), anonymous SSR HTML HIT via **explicit 10-min Edge-TTL override** (origin sends no-store on SSR routes — respect-origin = permanent BYPASS, the corrected doc `ebaa51ca` records this), cookie'd requests DYNAMIC. Watch cache-rate (was 4.4%) + Security Events over coming days. Commission #1 is now FULLY closed.

**CREDENTIALS ROTATED 2026-07-03 (commit `02c06de2`) — the old plaintext DB/SSH passwords in git history are DEAD.** Server access = **SSH KEY AUTH ONLY**: `ssh -i %USERPROFILE%/.ssh/id_ed25519 root@65.108.5.250` (plink/pscp -pw retired; the .ppk is passphrase-locked — use Windows OpenSSH). Root password (console fallback): `%USERPROFILE%\.lcs-secrets\hetzner-root-password.txt` / `/root/.root-password-rotated-2026-07-03`. DB: `LCS_DB_PASSWORD` in `/opt/lessoncraftstudio/.deploy-env` (root:600, gitignored) — deploy.sh SOURCES it and fails fast if absent; cron pg_dump + smoke + regression + diacritics scripts all read it. Access contract doc: `docs/ops/server-access.md`. CLAUDE.md §A.1 + .claude/settings.local.json scrubbed in worktree but UNCOMMITTED (operator's dirty files — they should include them in their next commit). Watch the next real deploy once (first end-to-end run of the sourcing guard).

**MASTER ROADMAP rev.2 (2026-07-03) ADOPTED — supersedes the report's commission queue.** "Free discovery, paid depth"; 4 premium classes: (1) Storybook Library (lead-in free, library gated), (2) Generator Creation Suite (hosted worksheets + share links + watermark-free + unlimited exports; #6 scopes from actual code), (3) Story Studio (multi-tenant productization Phase 4; private-by-default, NO public gallery, TTS narration; included in Teacher tier), (4) Unit Packs (later). **GAMES CANCELLED PERMANENTLY (operator: "there will never be games") — never resume on legacy triggers.** Pricing ruling: Teacher $69/yr·$8.99/mo launch tier / Family $39·$5.99 fast-follow / School $300-700-1200 later; no card trials; Nordics kr; BR regionalized; IT/ES acquisition locales. Phases: 0 CLOSED (deploy hygiene shipped; SEP export thread verified CLOSED 29/29 commit 0e253903; activities = 205 ground truth) → **1 SHIPPED 2026-07-03 evening (commits 72c0b901+ca219a02+c650ebec+4db83c35; evidence `commission-2-8-evidence-report.md`)**: #2 rail = pricing ×11 LIVE noindexed/unlinked at /{locale}/pricing (flip = `PRICING_PUBLIC` in frontend/config/subscription-launch.ts + sitemap + nav links + homepage.subscription reversal ×11 deferred-to-flip), rail bugs fixed (auth redirects, locale list, client gates w/ subscriber upsell, webhook email locale, billing strip); LS store DONE (commit 6ed95379): TWO products SaaS — Yearly $69 (product 1194166/variant 1866999) + Monthly $8.99 (product 1194171/variant 1867004); config has both, isLcsSubscriptionProduct accepts either, webhook derives billingInterval from product. **Rail PROVEN e2e via HMAC-signed webhooks: created→active(yearly/monthly correct), idempotent replay, bad-sig→401, cancel→canceled, cleaned up.** Optional remaining: real browser test-mode purchase (test links captured; needs LS test-mode webhook configured — signed-webhook test already covers the handler path). #8 Umami v2.20.2 LIVE (pm2 :3100, own PG db, admin pw in /root/.umami-admin-2026-07-03, website id d88b3d75-2b0d-4db9-a434-170589c70e1d; nginx first-party /lcs-insights.js + /api/send; beacon on Next pages + 45,309 decks injected; publish-wave STEP 6b; dashboard via ssh -L 3100). Gotcha: Cloudflare Bot Fight Mode challenges the server's OWN edge curls → post-deploy-smoke classifies cf-mitigated:challenge as BFM-skip (75 on a healthy deploy). TODO queued: privacy-policy analytics paragraph ×11; sep-format.md §6 stale line → 2 = #3 storybook SEO+premium, #6 creation suite, #4 CTR+consolidation (needs GSC export — STILL PENDING), #5 Pinterest → 3 = story production + ART PIPELINE (operator critical path: LoRA on GX10, reusable character/background libraries) + #9 embed outreach → 4 = Story Studio productization, #7 unit packs, school tier, locale depth. CLAUDE.md contradiction flags for operator: top AMENDMENT games north star + §20.9 loops (cancelled), §7 no-subscription-marketing lock + revenue-TBD (superseded), §11 studio-cut (reversed).

**Build queue (previously):** 2 subscription-rail revival(M) → 3 storybook SEO+premium(M) → 4 CTR+consolidation(M) → 5 Pinterest(S-M) → 6 generator gate(S-M) → 7 assembly premium(L) → 8 analytics layer(S, needs §10.3 dep approval) → 9 embed outreach(S-M) → 10 games(XL). Pending sharpener: full 16-month GSC export (queries/pages tabs). Ride-along defects queued into #1: /audio/inventory.json 404 ×1,876, stale-slug deck 404s, hreflang-codes.js hand-sync drift, /en/topic/sudoku 404.
