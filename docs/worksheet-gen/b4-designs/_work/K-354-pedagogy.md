# K-354 `human-body` : PEDAGOGY + CONTENT (nt10-D, 2026-09-21)

Read: `_ROLE-PEDAGOGY.md`, `_STUDIO-BRIEF.md`, `_SUBSTRATE.md`, `_CANDIDATES.md` row 3, the four `_selection-*.md` human-body entries (no `_PANEL-FINDINGS.md` on disk yet), `../b3-designs/K-323-all-about-me.md` (F3 = K-344), `types/k/K-323-all-about-me.js` (`_buildFaceLabels` + the face branch of `verify()`), `types/k/K-344-*.js`, `types/k/K-225-match-word-picture.js`, `types/g1/G1-242-read-and-color.js`, `types/g1/G1-244-write-the-word.js`, `templates/components-b2.js` (`letterBoxes`, `colorLegend`, `wordBank`, `pillChoice`), `templates/components-b3/ordinal-numbers.js` (`blankNumeralBox`), `types/_shared/fixed-theme.js`, `qa/verify-b3-all-about-me.js`, `data/b3/all-about-me.js` + `locales/all-about-me.de.json`, `data/b2/articles.js`, `tools/gate-variation-distinct.js`, `tools/measure-instruction-window.js`, `frontend/lib/seo/strand-names.ts`, `frontend/config/topics-taxonomy.json`. Every number below was measured 2026-09-21 with read-only node (scratch prefix `K-354-`); *est.* = the engineer measures in the real render. All 16 anchorable pictures were OPENED (list in B, F4). No em-dashes.

**Boundary (load-bearing).** K-344 `all-about-me-label-the-face` (LIVE) copies eye / ear / nose / mouth / hair (+ eyebrow / chin at d3) from a `wordBank` onto `writingRow` lanes pointing at a PHOTO-LIKE face picture (`body parts/face`, 5 anchors as fractions, `aboutMeFaceLabels`). K-225 `match-word-to-picture` (`lit-vocab-match`, `data/literacy/vocab-match.json`) already draws lines between the words eye ear nose hand foot arm leg head mouth finger and their pictures (measured: all 10 in its pool). K-355 `five-senses` (this batch) owns eye / ear / nose / tongue / hand AS SENSE ORGANS (object to organ). G1-244 `write-the-word` owns picture-card + letter-box dictation on any theme (no printable type has ever fanned on `body parts`: 0 mentions in `waves/wave-b2-*.json`; the 60 live `body_parts` landing modes are all interactive-app decks). G1-242 `read-and-color` owns "read a sentence, colour n pictures" on BW themes. K-016 owns pair-the-pictures for odd / even; G1-351 (this batch) owns parity. K-207 / K-211 own clothes and hot / cold. Therefore this family owns exactly two things nobody has: (1) the WHOLE-BODY DRAWN FIGURE (NEW `primitives/body-figure.js`, a neutral child, 16 anchorable ids `head hair eye ear nose mouth neck shoulder arm elbow hand finger leg knee foot toe`, paired ids carry `.L` / `.R` instances, every id is a fill region and can be omitted) and (2) BODY FACTS about the child's OWN body (how many, which come in twos) from a locale-neutral `data/b4/body-facts.json`. Every label face LEADS with the body set `arm leg hand foot knee elbow shoulder neck head finger toe` and admits at most 2 of the K-344 set `eye ear nose mouth hair` per page (config `faceMax:2`); no face is a bare picture-card + word page (that is K-225 / G1-244); no face is a sentence-to-colour page (G1-242); no face names a sense; no face prints teeth (count varies), organs, or a skeleton (no K art, G3 content).

## A. Identity

| loc | genre head (the panels; compound, never the `body_parts` theme slug) | school year (brief band; base K, F3 + F5 G1) | national strand literal (framework NAME only; NO Science row exists in `strand-names.ts`, so this table IS the strand source) | CCSS (en only, honest) |
|---|---|---|---|---|
| en | **Parts of the Body** (slug `parts-of-the-body`; theme slug is `body-parts`; harvest unique 131, Tier A, head HARD: Education.com / Twinkl / K5; WINNABLE "label the body parts worksheet kindergarten", "how many body parts worksheet", "parts of the body and their uses kindergarten") | kindergarten (F3/F5 grade 1) | Science (readiness) | none clean: readiness class; nearest honest L.K.5.c (real-life word connections) on the label faces; F1 may cite K.CC.B.5 (count to answer how many) because the child counts a set on her own body; no NGSS K-1 body standard |
| de | **Der Körper** (slug `der-koerper`; theme slug `korperteile`; unique 88, Tier A; "der körper arbeitsblatt grundschule", "körperteile arbeitsblatt klasse 1", DaZ tail WINNABLE) | Vorschule (F3/F5 1. Klasse) | Sachunterricht | n/a (Lehrplan: "Mein Körper: Körperteile benennen") |
| es | **El cuerpo humano** (slug `el-cuerpo-humano`; theme slug `partes-del-cuerpo`; unique 139 = the largest es head, Tier A; WINNABLE "el cuerpo humano fichas para imprimir primer grado") | preescolar (F3/F5 primer grado) | Conocimiento del Medio (the pedagogy panel's literal; SEP preescolar campo "Exploración y comprensión del mundo natural y social" is the K-band alternative: OPEN 1) | n/a (SEP / NEM) |
| pt | **O corpo humano** (slug `o-corpo-humano`; theme slug `partes-do-corpo`; unique 77, Tier A; "complete o corpo humano educação infantil" is F4 verbatim; "boneco" = the child's word for the figure) | educação infantil (F3/F5 1º ano) | Ciências | n/a; **BNCC EF01CI02 verbatim** on every face: "localizar, nomear e representar graficamente partes do corpo humano" |
| fr | **Le corps humain** (slug `le-corps-humain`; theme slug `parties-du-corps`; unique 118, Tier A; Lutin Bazar / Maître Lucas own the head; WINNABLE "nommer les parties du corps cp fiche", "les parties du corps humain à imprimer maternelle") | maternelle GS (F3/F5 CP) | Questionner le monde (CP; the GS name is "Explorer le monde": OPEN 1) | n/a (programmes officiels) |
| it | **Il corpo umano** (slug `il-corpo-umano`; theme slug `parti-del-corpo`; unique 88, Tier A; "il corpo umano classe prima", "verifica parti del corpo classe prima") | infanzia (F3/F5 classe prima) | Scienze | n/a (Indicazioni nazionali) |
| nl | **Het lichaam** (slug `het-lichaam`; theme slug `lichaamsdelen`; unique 42 net ~32, Tier A for a kleuter thema; "lichaamsdelen benoemen werkblad", jufmilou / kleuteridee) | kleuters groep 1-2 (F3/F5 groep 3) | Oriëntatie op jezelf en de wereld (SLO kerndoel 34 / 41) | n/a |
| sv | **Kroppens delar** (slug `kroppens-delar`; theme slug `kroppsdelar`; round 1 53 = the highest Nordic cell, round 2 27; Tier A; every grade word fires, "no kroppen" subject tag) | förskoleklass (F3/F5 åk 1) | Naturorienterande ämnen (Lgr22 NO åk 1-3 "Människans kroppsdelar, deras namn och funktion") | n/a |
| da | **Kroppens dele** (slug `kroppens-dele`; theme slug `kropsdele`; 21 / 20, Tier A-; "kroppen opgaver indskoling") | børnehaveklasse (F3/F5 1. klasse) | Natur/teknologi | n/a (Fælles Mål) |
| no | **Kroppen vår** `[NSR-FLAG][no]` (slug `kroppen-var`; theme slug `kroppsdeler`; 28 / 20, Tier A-; the NRK phrase; a teacher may prefer "Kroppens deler": OPEN 2) | 1. trinn (F3/F5 2. trinn) | Naturfag (LK20 "kroppen") | n/a |
| fi | **Kehon osat** (slug `kehon-osat`; theme slug `ruumiinosat`, a DIFFERENT word: never print "ruumiinosat" anywhere in the type; 20 / 12, Tier B+; "kehonosat tehtäviä" has no owner) | esikoulu (F3/F5 1. luokka) | Ympäristöoppi (OPS 2014) | n/a |

Measured: none of the 11 compound slugs above (nor the bare alternates `human-body`, `kroppen`, `corpo-humano`, ...) collides with any `axes.*` slug in its locale; `apps['human-body']` and `axes['exercise-type']['human-body']` are ABSENT (register with the `tools/register-b3-taxonomy.js` shape, `default_subject:'science'`, `default_age_range:'5-7'`). `science` disc today = 5 apps (science-sort, science-sequence, science-match, seasons, animal-fact-file); this family is the 6th.

**Theme axis: FIXED** via `withFixedTheme(type, 'body parts')` (`themeAxis:{applicable:false, fixedTheme:'body parts'}`; the pictures are cues on cards and chips, the figure is drawn). `body parts` = 34 files / 34 nouns with vocab x11 (measured, `entriesFor`); the family uses only the 16 anchorable ids (all present x11, all opened). Landings carry `coordinate.theme:''`. **No `unitAxis`** (nothing fans on a non-theme unit).

**The one rule that locks the type:** the figure is the apparatus and the answer is a FACT of the figure or of the child's body, never a word the code inflects: every printed word is either the vocab SINGULAR verbatim (`partWords[id] === displayWord(vocab[id][loc][0], loc)`, K-344 rule 4, with an audited `overrides` list) or a whole panel literal (`factLabels[factId]`), and every count is read from `body-facts.json`, once, for all 11 locales.

## B. The six faces

Common: `templates/components-b4/human-body.js` (bank, lanes, cards, chips) + NEW `primitives/body-figure.js` (`bodyFigure({h, ids, omit:[], regions:{}, anchorsFor:[...]}) -> {svg, anchors:{id:{x,y,side}}, regions:[id...]}`; palette ink teal on cream, 3 px stroke, no skin colour, no gender marker, a plain t-shirt + shorts as NON-part regions; paired ids have `.L`/`.R` instances and the label anchor is the instance on the lane's side). Every face is a CODE face on ONE additive `layout` knob (the K-323 Phase-2 pattern: `d.layout` undefined = the base path, byte-identical; a face stamps `data-lcs-layout` only when declared, never `data-lcs-face`). Guards key on the CONFIG (`d.labels`, `d.mix`, `d.boxMin`, ...), never on the level index. IDs of the faces are `TBD by the emitter` in the band stated.

### Base: Parts of the Body (K, `K-354`, layout undefined)
**Move:** LOCATE a part on the whole-body figure and COPY its name from a bank onto the lane whose leader points at it (a K bank-copy: the child matches a word shape to a line, then writes).
**Child:** "Copy each word from the bank onto the line that points to that part of the body." (81 chars; apparatus on the page: bank, lines, the figure.)
**Params:** d1 `{labels:4, perSide:2, faceMax:1, laneW:220, laneH:72, glyphH:44, figureH:560, bankPx:17}` · **d2 (ships)** `{labels:6, perSide:3, faceMax:2, laneW:200, laneH:64, glyphH:40, figureH:560, bankPx:17, bankArticle:false}` · d3 `{labels:8, perSide:4, faceMax:2, laneW:200, laneH:56, glyphH:34, figureH:560}`. Pool = the 16 ids; `rng.sample` under `faceMax` and `perSide`; leaders sorted by anchor y so they never cross (the K-344 crossing sweep is reused). Stack *est.* (design agent budgets): bank 59 + 10 + figure block 560 = 629 <= 722; lanes 3 x 64 + 2 x 20 = 232 per side inside 560.
**verify():** stamps `data-lcs-parts`, `data-lcs-anchors` (JSON, fractions with side), `data-lcs-label=<id>` per lane, `data-lcs-bank=<id>` per bank word (the `wordBank` stamp). Re-derived: bank ids <=> lane ids (bijection), lane ids === parts, every leader end inside the figure bbox and within 10 px of its anchor, no two leaders intersect, no label text within 20 px of an end, lanes empty (no text, no SVG `<text>`), `faceMax` honoured (count of ids in `{eye,ear,nose,mouth,hair}` <= config), figure `[data-lcs-figure]` present with all 16 regions rendered (nothing omitted on the base). The answer = the lane id; the bank order must not equal the top-to-bottom lane order (position leak; G1-244 idiom).
**Refusals:** none (16 singulars exist x11). fr `eye` prints "œil" (K-344 shipped it live in fr; U+0152/0153 sit in the Google latin-ext range the shell fonts were pulled with, presence in `nunito-800-latin-ext.woff2` UNKNOWN: engineer measures with `document.fonts.check` in the real render). fi `leg`/`foot` = `sääri`/`jalka` (vocab): a Finnish child says "jalka" for the leg; the fi panel rules (OPEN 3); no `neck` = `nakke` (nape) for a front-view figure: the no panel may override to "hals" (OPEN 3). `bankArticle:true` is a per-locale data flag (de / nl / sv / da print `articles.js` chip + word, pure lookup off the gender code, never inflected; K-344 printed bare words).
**Query face:** the bare head + "label": en "label the parts of the body" · de "Körperteile beschriften" · nl "lichaamsdelen benoemen" · fr "nommer les parties du corps" · sv "kroppsdelar" · fi "kehon osat".

### F1: How Many? (K, K-3xx TBD, CODE `layout:'count'`)
**Move:** RECALL a body fact and WRITE a numeral: for each pictured part, how many the child has (count on your own body, 1 / 2 / 5 / 10). Language-light: the picture is the cue, the word a label; K.CC counting to answer "how many".
**Child:** "How many of each do you have? Count on your own body and write the number in the box." (87)
**Params:** d1 `{layout:'count', cards:4, cols:2, rows:2, mix:{1:1,2:3}, pic:96, box:64, wordPx:18}` · **d2** `{layout:'count', cards:6, cols:2, rows:3, mix:{1:1,2:3,5:1,10:1}, pic:80, box:56, wordPx:18}` · d3 `{layout:'count', cards:8, cols:2, rows:4, mix:{1:2,2:3,5:2,10:1}, pic:72, box:56, wordPx:17}`. Pools (`body-facts.json`): count 1 = `head nose mouth`; count 2 = `eye ear arm hand leg foot knee elbow thumb`; count 5 = `fingersOneHand` (cue `hand`), `toesOneFoot` (cue `foot`); count 10 = `fingers` (cue `hand`), `toes` (cue `foot`). A page never carries both a 5 and a 10 item of the SAME part (finger 5 + finger 10 would be two answers to one picture); `neck` and `hair` excluded (odd to count / uncountable); `tooth` never (lock). Card = cue picture (ONE instance only, so the pictures never reveal the count) + `factLabels[factId]` + `blankNumeralBox({w:56,h:56})` (the K floor 56; `answerBox` would stamp `data-lcs-answer="undefined"`, never used).
**verify():** stamps `data-lcs-fact=<id> data-lcs-count=<n>` per card; re-derived: `count === BODY_FACTS[id].count`, the multiset of counts === config `mix`, one cue `img` per card (complete, naturalWidth > 0, `src` names the fact's cue noun), no numeral glyph in the body except `.ws-card-badge`, every `[data-lcs-answer]` empty, label === `factLabels[id]` of the locale, no two cards the same part, no (5,10) pair of one part.
**Refusals:** none. fi labels are PARTITIVE SINGULAR literals ("Kuinka monta silmää?" grammar: `silmää`, `sormea yhdessä kädessä`), never the vocab plural; es "dedos de la mano" / "dedos del pie" (the lock); it labels are panel plurals (`le braccia`, `le dita`, `le ginocchia`, `le orecchie`: gender-switching plurals, never vocab plural + code article).
**Query face:** "how many body parts" (en WINNABLE) · de "Wie viele Körperteile" (WINNABLE) · sv "hur många" · pt "1 ano".

### F2: Colour by Legend (K, K-3xx TBD, CODE `layout:'color'`)
**Move:** READ a legend (swatch + colour word + part word) and COLOUR that region of the figure; no sentence, no count, no number (that is what keeps it off G1-242).
**Child:** "Look at the legend. Colour each part of the body in the colour next to its word." (79)
**Params:** d1 `{layout:'color', legend:3, figureH:600, regions:['hair','arm','leg']}` · **d2** `{layout:'color', legend:5, figureH:600, regionPool:['hair','head','arm','hand','leg','foot'], swatch:22, wordPx:17}` · d3 `{layout:'color', legend:6, figureH:600, regionPool: the six}`. Colours from `COLOR_KEYS` (the 8 `codeColors`) x `COLOR_WORDS[loc]` (measured x11, 8 each); `rng.shuffle` the colours, `rng.sample` the regions. Regions are the LARGE fills only (eye / ear / nose / mouth are too small to colour at K; `neck`/`shoulder`/`elbow`/`knee`/`finger`/`toe` are not closed regions of their own). The t-shirt / shorts are never in the legend (free colouring). Legend = `colorLegend({entries:[{key, word: COLOR_WORDS[loc][key] + '  ' + partWords[id]}]})` OR two chips per entry (design agent); either way colour word and part word are separate literals, no agreement is ever formed (de "blau / Arm", never "blauer Arm").
**verify():** stamps `data-lcs-legend=<color>` (existing) + `data-lcs-region-target=<id>` per entry; `data-lcs-region=<id>` on every figure fill path. Re-derived: every legend target is a rendered region with `fill` = cream (nothing pre-coloured), region bbox >= 40 x 40 px at the rendered figure (*est.*, the K colouring floor the design agent sets), legend colours distinct, legend part ids distinct, legend count === config, no region appears twice, the six non-legend regions untouched.
**Refusals:** none (COLOR_WORDS x11; fi `vaaleanpunainen` 128 px at 16 is the widest and sits on its own legend row, no tile ceiling here).
**Query face:** "colour the body parts" · de "Körper ausmalen nach Legende" · es "el cuerpo humano para colorear" · fr "les grandes parties du corps" · it "corpo umano da colorare" · pt "e colorir".

### F3: Write the Word (G1, G1-3xx TBD, CODE `layout:'write'`)
**Move:** RETRIEVE the name of a part from the figure alone (no bank, no picture card) and WRITE it into letter boxes on the lane; the box count is the only scaffold. Differs from G1-244 (picture cards, any theme) because the cue is the figure part at the end of a leader; differs from the base because there is nothing to copy.
**Child:** "Write the name of each part of the body in the boxes. One letter goes in each box." (83)
**Params:** d1 `{layout:'write', labels:4, perSide:2, faceMax:1, boxes:true, starter:true, box:30, boxMin:26, laneW:240, laneH:64, glyphH:30}` · **d2** `{layout:'write', labels:6, perSide:3, faceMax:2, boxes:true, starter:false, box:30, boxMin:26, laneW:220, laneH:64, glyphH:30, figureH:560}` · d3 `{layout:'write', labels:8, perSide:4, faceMax:2, boxes:false, starter:false, laneW:200, laneH:56, glyphH:26}` (plain ruling; the G1-244 d3 idiom). Word eligibility is a CONFIG guard: `word` matches `/^[\p{L}][\p{L}'’-]*$/u` (no space: es / pt / it `toe` = "dedo del pie" / "dedo do pé" / "dito del piede" are excluded, measured MULTI), `n = [...word.replace(/['’-]/g,'')].length`, and `Math.min(box, floor((laneW - 8) / n) - 4) >= boxMin` (at laneW 220, boxMin 26: n <= 7). Measured eligible pools at d2 (16 minus the excluded): en 15 (shoulder 8) · de 14 (Schulter, Ellbogen 8) · es 15 (toe) · pt 14 (cotovelo 8, toe) · fr 14 (œil by the lock, oreille 7 ok) · it 12 (orecchio 8, ginocchio 9, toe; braccio 7 ok) · nl 14 (schouder, elleboog 8) · sv 16 · da 16 · no 16 · fi 12 (käsivarsi 9, kyynärpää 9, hiukset 7 ok, olkapää 7 ok) : all >= 6 + the `faceMax` margin. Special letters count as ONE box each: de `Fuß` 3, fi `pää` 3 (a long vowel is two letters = two boxes by construction), sv `tå` 2, nl no IJ in the 16, pt `pé` 2 (allowed; minLetters 2).
**verify():** stamps `data-lcs-word=<word> data-lcs-vocab=<id>` on the lane + `data-lcs-letterboxes=<n>` (the `letterBoxes` stamp). Re-derived: `n === [...word.replace(/['’-]/g,'')].length`, no visible text on any lane (no bank, no model word; d1 starter = one `trace-word` lane whose `data-lcs-text` is the first letter, the G1-244 rule), box side >= `boxMin`, leaders as the base, every word from the locale's eligible pool, no duplicate word (case-insensitive, `toLocaleLowerCase(loc)`), `faceMax` honoured.
**Refusals:** fr `eye` (lock: Œ; a per-WORD exclusion via `refuseWords.write:['eye']` in the fr block, not a face refusal); es / pt / it `toe` (multi-word, guard); nothing else. The `wordBank` is NEVER present on this face (verify fails on `[data-lcs-bank-banner]`).
**Query face:** "write the body parts" · de "Körperteile schreiben Buchstabenkästchen" · es "examen partes del cuerpo primer grado" · it "verifica parti del corpo classe prima" · sv "kroppens delar namn" · da "hvad hedder kroppens dele" · fi "kehon osien nimet".

### F4: What Is Missing? (K, K-3xx TBD, CODE `layout:'missing'`)
**Move:** DISCRIMINATE an incomplete figure: the figure is drawn with ONE part class omitted (both instances of a pair); the child CIRCLES the picture chip of the missing part (verified) and DRAWS it onto the figure (open, pt "complete o corpo humano"). Pure picture task, no text on the item.
**Child:** "Each body is missing one part. Circle the picture of the missing part, then draw it on." (86)
**Params:** d1 `{layout:'missing', figures:2, cols:2, rows:1, figureH:330, chips:3, chipPx:64}` · **d2** `{layout:'missing', figures:4, cols:2, rows:2, figureH:250, chips:3, chipPx:56, omitPool:['arm','leg','hand','foot','hair','mouth','eye','ear','nose']}` · d3 `{layout:'missing', figures:6, cols:3, rows:2, figureH:200, chips:4, chipPx:56, omitPool:['arm','leg','hand','foot','hair']}`. `omitPool` is keyed by figure height: at 250 px the omitted part's full-scale bbox must be >= 14 px in its smaller dimension (*est.*: the design agent measures nose / ear / eye at 250 and drops any that fail; at 200 only the five large parts). Chips = library pictures (`fileUri('body parts', noun)`, cue only), target + distractors drawn from the CHIP POOL under the CONFUSABLE fence, shuffled. **Chip pool (opened 2026-09-21):** `hand` (open palm, five fingers) · `foot` (side view, five toes) · `arm` (sleeve to open hand: it INCLUDES a hand) · `leg` (thigh to foot with a red knee dot: it INCLUDES a foot) · `eye` (one eye, lashes) · `ear` · `nose` · `mouth` (open, teeth + tongue visible) · `head` (a child's head with face and neck) · `hair` (a red-haired face with a baked CORAL ARROW at the hair) · `knee` (a bent leg with a red knee dot, shorts, sock + shoe) · `elbow` (a bent arm with a red elbow dot, no hand) · `shoulder` (a bare torso with an arrow; reads "chest" to a child: EXCLUDED from chips) · `neck` (neck with a necklace + arrow: EXCLUDED) · `finger` (a fist with the index finger up) · `toe` (foot with an arrow to the big toe). **CONFUSABLE fence** (a distractor never comes from the target's group): `{arm, hand, elbow, finger}`, `{leg, foot, knee, toe}`, `{hair, head}`, `{eye, ear, nose, mouth}` is NOT a group (the omitted face part is legible against the other three at 250 px). Distractors must be parts PRESENT on that figure (only one class is omitted, so this holds by construction; verify checks it anyway).
**verify():** stamps `data-lcs-omit=<id>` on each figure root, `data-lcs-chip=<id>` per chip, `data-lcs-region=<id>` on rendered fills. Re-derived: exactly one chip id === omit per figure; the omit id has NO rendered region on that figure and every other of the 16 ids does; distractor ids obey the fence table (the table is exported by the bank module and read by the gate); chips unmarked, equal size (`style.width` set size 1, the G1-242 leak rule), `img` complete; no text inside the item; no two figures omit the same id; chip order is not the same triple-order on every figure.
**Refusals:** none (pictures are global; no text).
**Query face:** "what is missing body parts" · de "Was fehlt?" · nl "wat ontbreekt?" · pt "complete o corpo humano" (harvest verbatim) · es "preescolar" · fr "maternelle" · sv "vad saknas".

### F5: Which Come in Twos? (G1, G1-3xx TBD, CODE `layout:'pairs'`; a locale may publish it at K via its `level` key, OPEN 4)
**Move:** CLASSIFY parts by a body fact: circle every part the child has TWO of. Cards = one picture + the SINGULAR word for every card (a plural on the pair cards would print the answer to a reader), so the picture never shows two and the word never says two; the fact lives only in the child's body.
**Child:** "Circle every part of the body that you have two of." (52; the panels may lengthen: the meta window is measured per locale)
**Params:** d1 `{layout:'pairs', cards:6, cols:3, rows:2, pairs:3, singles:3, pic:96, wordPx:18}` · **d2** `{layout:'pairs', cards:8, cols:4, rows:2, pairs:4, singles:4, pic:72, wordPx:17}` · d3 `{layout:'pairs', cards:10, cols:5, rows:2, pairs:5, singles:5, pic:64, wordPx:16}`. Pools: pairs = `eye ear arm hand leg foot knee elbow` (count 2; thumb excluded, its picture is a hand); singles = `nose mouth head neck chin tongue` (count 1; chin / tongue are K-344 / K-355 words used here only as picture cues, no label move). Count-10 parts (`finger`, `toe`) NEVER appear (a child can argue "two hands of five"); `hair` never (uncountable).
**verify():** stamps `data-lcs-card=<id> data-lcs-pair="1|0"`; re-derived: `pair === (BODY_FACTS[id].count === 2)`, pairs / singles counts === config, no `finger` / `toe` / `hair` / `thumb` on the page, every card word === `partWords[id]` (singular), one `img` per card (complete), no pre-ringed card, no numeral in the body except badges, cards shuffled (not all pairs first).
**Refusals:** none. fi title / instruction use "parit" or "kaksi", NEVER "parilliset" (= even numbers; gate regex in D). es cards say "dedo de la mano"? no: fingers are excluded, so the es dedos trap does not arise on this face. de wording "zweimal" / "Paare" is the panel's.
**Query face:** "body parts that come in pairs" · de "Was gibt es zweimal?" · sv "hur många har vi två av" · fi "parit".

**Rejected non-moves.** "Parts and their uses" (en / de / nl harvest: needs a verb phrase x11 and lands on see / hear / smell = K-355's move; deferred, not a face) · "cut and assemble the figure" (es / pt "para imprimir y armar": open, no verify; a possible template later) · "internal organs / skeleton" (G3, no K art) · a plural-word face (singular / plural family K-302..316) · a theme swap (the theme is fixed) · relabelling d1 or d3 of the base as a face (same move, not distinct under `gate-variation-distinct`).

## C. Native rebuild x11

Frames NEVER inflect: this type has NO cloze frame, so it needs NO `objForms` (measured: `bank('instructions', loc).objForms` holds 0 of the 16 body nouns in en / de / es / pt / fr / it / nl / sv / da / no and 15 in fi; nothing is read from it). Per locale the panel authors: `strings` (6 titles + 6 instructions), `partWords` (16 singulars: confirmed against the vocab by the validator; any deviation goes into `overrides[id] = {word, reason}`), `factLabels` (18 whole literals: 16 part facts in the locale's "how many X" form + `fingersOneHand` + `toesOneFoot`), `bankArticle` (bool), `refuseWords` (per face), `refuse` (per face). Colour words come from `COLOR_WORDS` (exists x11).

| loc | literals the panel authors (counts) | slots / forms and where they exist | refusal / re-target | traps |
|---|---|---|---|---|
| en | 12 strings · 16 partWords (= vocab, 0 new) · 18 factLabels ("eyes", "fingers on one hand") = 46 | none; `COLOR_WORDS.en` | none | US spelling "color"; title "Parts of the Body" never "Body Parts" (theme slug + K-344 owns the face); "pdf"/"printable" belong to the meta, not the title |
| de | 12 · 16 (capitalised: Kopf, Arm; `bankArticle:true` prints "das Auge" via `articles.js` m/f/n) · 18 ("Augen", "Finger an einer Hand") = 46 | `articles.js de` chips der/die/das off the gender code | none | ß one box (Fuß); Haare plural-only, as-is; `Schulter` / `Ellbogen` drop from F3 by the box guard (8 letters); "Paare" vs "zweimal" on F5 is the panel's; engine appends "Arbeitsblatt"; theme collision `korperteile` |
| es | 12 · 16 (toe = "dedo del pie") · 18 ("ojos", "dedos de la mano", "dedos del pie") = 46 | none | F3: `toe` (multi-word, guard) | MX register; "oreja" (depicted) never "oído"; strand K vs 1º (OPEN 1); never "partes del cuerpo" alone; "cuerpo humano" carries no worksheet ambiguity |
| pt | 12 · 16 · 18 ("olhos", "dedos da mão", "dedos do pé") = 46 | none | F3: `toe` | "boneco" is the child's word for the figure (instruction may use it); pé = 2 boxes (allowed); `cotovelo` 8 drops from F3; EF01CI02 on every landing; "atividade" never in a title |
| fr | 12 · 16 (œil, cheveux plural-only, nez invariable) · 18 ("les yeux", "les doigts d'une main") = 46 | none | F3: `eye` (lock, Œ) | irregular plurals only in factLabels (yeux, genoux); elision never printed (no article on labels); `oreille` 7 fits F3; strand GS vs CP (OPEN 1); "fiche" never in a title |
| it | 12 · 16 · 18 (gender-switching plurals as literals: "le braccia", "le dita", "le ginocchia", "le orecchie", "le mani") = 46 | none | F3: `toe`; `orecchio` / `ginocchio` drop by the guard | never vocab plural + article on F1; "scheda" never in a title; ELT tail ("in inglese") is not ours |
| nl | 12 · 16 (`bankArticle:true`: het oog, het oor, de neus, de mond, de hand, de voet) · 18 = 46 | `articles.js nl` de/het off d/h | none | "haar" also = her (harmless as a label); `schouder` / `elleboog` drop from F3; engine appends "werkblad"; theme collision `lichaamsdelen` |
| sv | 12 · 16 (indefinite only; never "ögat") · 18 ("ögon", "fingrar på en hand") = 46 | `articles.js sv` en/ett optional | none | "ben" = leg AND bone (keep, the figure disambiguates); "kinder" false friend never printed (cheeks not in the 16); title "Kroppens delar" locked (theme slug `kroppsdelar`); engine appends "arbetsblad"; definite "kroppen" fine in prose |
| da | 12 · 16 · 18 = 46 | optional en/et | none | title "Kroppens dele" (theme slug `kropsdele`); "tå" 2 boxes; engine appends "opgaver" |
| no | 12 · 16 (`neck` = "nakke" in vocab: panel may override to "hals" with a reason) · 18 = 46 | none | none | bokmål only ("kroppsdelar" is a Swedish homograph); title "Kroppen vår" `[NSR-FLAG]`; engine appends "oppgaver" |
| fi | 12 · 16 (`leg` = "sääri" / `foot` = "jalka": OPEN 3; "kehon osat" two words) · 18 PARTITIVE literals ("silmää", "sormea yhdessä kädessä") = 46 | none (fi `part`/`gen` forms exist in objForms for 15 nouns but no frame uses them) | none | "parit" never "parilliset"; "ruumiinosat" banned everywhere in the type (theme name); `käsivarsi` / `kyynärpää` drop from F3 (9 letters); instruction cases are the panel's ("Kirjoita kehon osan nimi laatikoihin"); title + instruction the longest (4-line fi title = 677 budget: every stack must survive it) |

## D. Data + gates

Bank = `data/b4/human-body.js` (EN block hand-authored) + `data/b4/locales/human-body.<loc>.json` (GENERATED from `i18n/.draft-b4-<loc>.json` by `tools/apply-b4-locale.js`, a clone of `apply-b3-locale.js`; `lib/b4-common.js bank(name, loc)` reads `data/b4/`, the additive `dir` clone the substrate names) + `data/b4/body-facts.json` (locale-neutral, one file). `data/` is gitignored (`git add -f`).

```
BODY_FACTS (data/b4/body-facts.json, GLOBAL):
  parts:  { head:{count:1, cue:'head'}, hair:{count:null, cue:'hair'},   // null = uncountable
            eye:{count:2, cue:'eye'}, ear:2, nose:1, mouth:1, neck:1, shoulder:2, arm:2, elbow:2,
            hand:2, finger:10, leg:2, knee:2, foot:2, toe:10, thumb:2 }          // never tooth
  facts:  [ {id:'fingersOneHand', part:'finger', count:5, cue:'hand'},
            {id:'toesOneFoot',    part:'toe',    count:5, cue:'foot'} ]           // + one fact per part above
  chipPool:   ['hand','foot','arm','leg','eye','ear','nose','mouth','head','hair','knee','elbow','finger','toe']  // opened; shoulder + neck excluded
  confusable: [['arm','hand','elbow','finger'], ['leg','foot','knee','toe'], ['hair','head']]
  omitPool:   { 250:['arm','leg','hand','foot','hair','mouth','eye','ear','nose'], 200:['arm','leg','hand','foot','hair'] }  // engineer measures
  pairsPool:  { pairs:['eye','ear','arm','hand','leg','foot','knee','elbow'], singles:['nose','mouth','head','neck','chin','tongue'] }
HUMAN_BODY[loc] = {
  partWords:  { head, hair, eye, ear, nose, mouth, neck, shoulder, arm, elbow, hand, finger, leg, knee, foot, toe },
              // each === displayWord(vocab[id][loc][0], loc) unless overrides[id]
  overrides:  { neck:{word:'hals', reason:'front view; nakke = nape'} },        // audited list, may be empty
  factLabels: { head, nose, mouth, eye, ear, arm, hand, leg, foot, knee, elbow, thumb, finger, toe, shoulder, neck,
                fingersOneHand, toesOneFoot },                                   // whole literals (plural / partitive / phrase)
  bankArticle: false,                                                            // true = articles.js chip + word on the bank
  refuseWords: { write:['eye'] },                                                // per-face word exclusions (fr)
  refuse:      [],                                                               // per-face refusals: 'count'|'color'|'write'|'missing'|'pairs'
  strings:     { 'K-354':{title, instruction}, <F1..F5 ids>:{title, instruction} } }
```

**`tools/validate-b4-draft.js` (human-body block; every rule runs, exit 1 on any):**
1. `BODY_FACTS.parts` keys === the 16 anchor ids; every `count` ∈ {1, 2, 10, null}; `facts[].count` ∈ {1, 2, 5, 10}; no `tooth` anywhere; `hair.count === null`.
2. Every cue resolves via `fileUri('body parts', cue)` (throws on a missing cache entry), has a vocab singular in ALL 11 locales, is not `B2_EXCLUDE`d, and is in `chipPool` only if `picOpened:true`; `shoulder` and `neck` are NOT in `chipPool`.
3. `confusable` groups are disjoint and cover only chipPool ids; every `omitPool[h]` id has a figure region.
4. `partWords[id] === displayWord(vocab[id][loc][0], loc)` for all 16, OR `overrides[id]` exists with a non-empty `reason`; no two `partWords` equal (case-insensitive, `toLocaleLowerCase(loc)`), so fi `jalka` for both leg and foot FAILS (forces OPEN 3 to be decided in data).
5. Every `factLabels` id has a non-empty literal, no `{`, no digit, <= 34 chars; `factLabels` never equals the bare vocab plural in fi (partitive rule: fi literals must differ from `vocab[id].fi[1]`); `fingersOneHand` / `toesOneFoot` present or `refuse` names `count`.
6. `refuseWords.write` ids ⊂ the 16; F3 eligible pool per locale (single-word, `n <= floor((laneW-8)/(boxMin+4))` at the d2 config) >= `labels + faceMax`, else `refuse` must name `write`.
7. `pairsPool.pairs` all `count === 2`, `singles` all `count === 1`; neither contains `finger`, `toe`, `hair`, `thumb`.
8. Titles <= 70 chars, no worksheet-word (`WORKSHEET_WORD` regex of the b3 gate), unique in the band, and NEVER equal to the locale's `axes.theme.body_parts.name` or `.slug` (the K-322 compound-head rule; fi additionally must not contain "ruumiinosa"); instructions <= 150; fi strings must not contain "parillis".
9. No literal on the type contains a colour word next to a part word in ONE string (F2 keeps them separate; guards against a panel writing "blauer Arm").
10. `strings` ids === {base + the 5 allocated face ids}; the six titles differ pairwise by more than one adjective (token-set difference >= 1 NOUN or verb; the brief's "never two faces whose titles differ only by an adjective").

**Poison (each must FAIL for its OWN reason; the correct EN draft is the control):**
P1 `parts.tooth = {count:20}` -> rule 1 (never teeth). P2 cue `{noun:'durian'}` on `fingersOneHand` -> rule 2 (not cached). P3 `chipPool += 'shoulder'` -> rule 2. P4 de `partWords.eye = 'Augen'` -> rule 4 (plural ≠ singular). P5 fi `partWords.leg = 'jalka'` with `foot = 'jalka'` -> rule 4 (duplicate word). P6 no `overrides.neck = {word:'hals'}` with `reason:''` -> rule 4. P7 fi `factLabels.eye = 'silmät'` (the vocab plural) -> rule 5. P8 `pairsPool.pairs += 'finger'` -> rule 7. P9 sv title `'Kroppsdelar'` -> rule 8 (theme name). P10 fi F5 title `'Kehon osat: parilliset'` -> rule 8. P11 de F2 legend literal `'blauer Arm'` -> rule 9. P12 F3 title `'Parts of the Body: Big Words'` vs base -> rule 10 (adjective-only difference). Control: the EN draft passes 1-10.

**`qa/verify-b4-human-body.js` (render gate; clone the b3 shape):** renders base + 5 faces x 11 locales x d2 through `render/render-instance.js` (file:// fonts) under the 722 stack (3-line de title + 150-char instruction) AND the 4-line fi title (677); asserts `verify()` empty and `qa/lints.js` clean, then the floors ITSELF (no size lint exists): every chip / cue picture >= 56 (K faces) or >= 44 (F3), numeral boxes >= 56, letter boxes >= `boxMin` 26, lanes >= 56 high at K, legend swatch 22 with word >= 17 px, the figure >= 250 px tall on every face that draws it and every rendered region's stroke = 3 px teal on cream, leaders never intersect (sweep), bank <=> lanes bijection (base), no bank on F3, one chip === omit per figure (F4), `pair` flags === facts (F5), counts === facts (F1), no numeral glyph in any body except badges, no `data-lcs-answer` with a non-empty value, every `[data-lcs-label-key]` text === the locale bank literal (node cross-check), everything above the footer, titles <= 2 lines in de / fi at the shipped chrome. SWEEP: 20 seeds x d2 per face render distinct pages (the type is seeded; the base's label SET must vary across seeds and the bank order must never equal lane order). Render poisons (each must FAIL): PR1 swap the arm anchors' sides -> crossing sweep; PR2 a `wordBank` injected on F3 -> "bank present"; PR3 F4 distractor from the target's confusable group -> fence; PR4 F1 card with the hand cue pictured twice -> "cue shown more than once (count leak)"; PR5 F5 pair cards printed with the vocab plural -> "word ≠ singular"; PR6 F2 a region pre-filled `codeBlue` -> "region not cream"; PR7 figure at 200 px omitting `nose` -> omitPool; PR8 `answerBox({w:56,h:56})` in place of `blankNumeralBox` -> `data-lcs-answer="undefined"`; PR9 the F1 d3 stack (8 cards at pic 72) under the fi chrome -> footer lint; PR10 base d2 with `faceMax:3` resolved from a level-index guard -> the config guard fires before render.

Page reads at render: `HUMAN_BODY[loc]`, `BODY_FACTS`, `COLOR_WORDS`, `fileUri`, `articles.js` (chips only); never `image-vocabulary.js` or `approved-words-*.json`.

## E. SEO

`coordinate = {type:'human-body', mode:<face>, theme:'', level:<band key of the locale>}`; modes: base `'base'` · F1 `'count'` · F2 `'color'` · F3 `'write'` · F4 `'missing'` · F5 `'pairs'`. Level keys: base / F1 / F2 / F4 = the K key of the band table (kindergarten / vorschule / preescolar / educacao-infantil / maternelle / infanzia / kleuters / forskoleklass / boernehaveklasse / 1-trinn / esikoulu, as the hub already spells them: the engineer reads the existing K-3xx landings); F3 / F5 = the G1 key. Meta lead inherits `seo.words.free_printable` (metadata only, never visible copy). The MIDDLE is the instruction stem; the exact per-locale window is measured with `tools/measure-instruction-window.js` on the staged ZIPs (the lead carries the TITLE, so the budget is per face per locale); en middles below sit at 52-87 chars, inside a ~60-105 window for a ~60-char en lead (*est.*).

| face | title pattern Germanic (en / de / nl) | Romance (es / pt / fr / it) | Nordic + fi (sv / da / no / fi) | meta MIDDLE (en) |
|---|---|---|---|---|
| base | Parts of the Body · Der Körper · Het lichaam | El cuerpo humano · O corpo humano · Le corps humain · Il corpo umano | Kroppens delar · Kroppens dele · Kroppen vår · Kehon osat | Copy each word from the bank onto the line that points to that part of the body |
| F1 | Parts of the Body: How Many? · Der Körper: Wie viele? · Het lichaam: hoeveel? | El cuerpo humano: ¿cuántos hay? · O corpo humano: quantos são? · Le corps humain : combien ? · Il corpo umano: quanti sono? | Kroppens delar: hur många? · Kroppens dele: hvor mange? · Kroppen vår: hvor mange? · Kehon osat: kuinka monta? | How many of each do you have? Count on your own body and write the number in the box |
| F2 | Parts of the Body: Color by Legend · Der Körper: nach Legende ausmalen · Het lichaam: kleur volgens de legenda | El cuerpo humano: colorea según la clave · O corpo humano: pinte pela legenda · Le corps humain : colorie selon la légende · Il corpo umano: colora con la legenda | Kroppens delar: färglägg efter nyckeln · Kroppens dele: farvelæg efter nøglen · Kroppen vår: fargelegg etter nøkkelen · Kehon osat: väritä ohjeen mukaan | Look at the legend and colour each part of the body in the colour next to its word |
| F3 | Parts of the Body: Write the Word · Der Körper: Wörter schreiben · Het lichaam: schrijf het woord | El cuerpo humano: escribe la palabra · O corpo humano: escreva a palavra · Le corps humain : écris le mot · Il corpo umano: scrivi la parola | Kroppens delar: skriv ordet · Kroppens dele: skriv ordet · Kroppen vår: skriv ordet · Kehon osat: kirjoita sana | Write the name of each part of the body in the boxes, one letter in each box |
| F4 | Parts of the Body: What Is Missing? · Der Körper: Was fehlt? · Het lichaam: wat ontbreekt? | El cuerpo humano: ¿qué falta? · O corpo humano: complete o corpo · Le corps humain : que manque-t-il ? · Il corpo umano: cosa manca? | Kroppens delar: vad saknas? · Kroppens dele: hvad mangler? · Kroppen vår: hva mangler? · Kehon osat: mikä puuttuu? | Each body is missing one part: circle the picture of the missing part, then draw it on |
| F5 | Parts of the Body: Which Come in Twos? · Der Körper: Was gibt es zweimal? · Het lichaam: wat heb je twee keer? | El cuerpo humano: ¿cuáles van en pares? · O corpo humano: quais vêm em pares? · Le corps humain : lesquels vont par deux ? · Il corpo umano: quali sono in coppia? | Kroppens delar: vilka har vi två av? · Kroppens dele: hvilke har vi to af? · Kroppen vår: hvilke har vi to av? · Kehon osat: mitkä ovat parit? | Circle every part of the body that you have two of |

All titles are candidates the native panels rewrite (<= 70, no worksheet word, base = bare compound head, each face adds one NOUN / verb, never an adjective). JSON-LD `LearningResource`; `educationalAlignment.targetName` en only and only where honest (F1 K.CC.B.5; none on base / F2 / F3 / F4 / F5: readiness), no `targetUrl`; pt landings name BNCC EF01CI02 in prose on every face; every non-EN landing names the framework of A and the strand literal of A (there is no `strand-names.ts` row: the engineer either adds a `'Science'` row with the A literals or the landing template reads them from the type's i18n; do not let the chip fall back to English). `topicMeta['human-body']` >= 50 chars x11 + `skill-sentences.<loc>.json['human-body']` via a `tools/register-b4-en-content.js` clone.

**Non-cannibalisation** (whole-landing 3-gram Jaccard, `scripts/seo-landing/gate.js` FAIL >= 0.80 / WARN 0.65; *est.*, the gate measures):

| pair | what the copy says differently | est. |
|---|---|---|
| base vs F3 | a bank to copy from vs empty letter boxes, counting letters; K vs grade 1 | 0.35 |
| base vs K-344 (label the face) | the whole body figure, arm / leg / hand / foot, vs a face picture with eye / ear / nose; the base instruction must NOT paraphrase K-344's ("onto the line that points to that part of the face") beyond the shared verb: name the figure and the body parts explicitly in p1 | 0.30 |
| base / F3 vs K-225 (word to picture) | lines to a drawn figure + writing vs matching separate picture cards; no writing on K-225 | 0.15 |
| F3 vs G1-244 (write the word, any theme) | the figure is the cue, no picture cards, six parts of one body; G1-244 copy is theme-generic | 0.20 |
| F2 vs G1-242 (read and colour) | a legend, no sentence, no counting, colour art figure vs BW pictures | 0.15 |
| F2 vs color-by-number family | words, not numbers, as the key | 0.15 |
| F1 vs F5 | write a numeral (1 / 2 / 5 / 10) vs circle the twos; no numeral on F5 | 0.30 |
| F1 vs counting-pictures K-002.. | counting on your own body vs counting printed pictures | 0.15 |
| F5 vs K-016 (odd or even pairs) / G1-351 (this batch) | a body fact vs parity; F5 never prints "even", "odd", a count or a pairing line | 0.10 |
| F4 vs missing-pieces / K-061 visual discrimination | a drawn body missing a part + picture chips vs a picture with a cut-out piece | 0.15 |
| any face vs K-355 (five senses) | no sense verb, no object-to-organ; the boundary sentence names it | 0.10 |

Boundary sentence on every landing: "The child works on the whole body: labels, counts and colours the parts of a drawn figure. Face parts alone are on the This Is Me page, the senses on the Five Senses page."

## F. Open questions + summary

**Engineer must measure:** (1) `nunito-800-latin-ext.woff2` carries U+0152 / U+0153 (fr "œil" on the base bank and F5 card) via `document.fonts.check` in the real render; if absent the fr base excludes `eye` by `refuseWords.base` (a data change) and the K-344 precedent is re-checked. (2) The 722 stack of each face at the fi 4-line title (677): base bank 69 + figure 560 = 629 fits; F1 d3 8 cards is the tight one. (3) `omitPool` per figure height: the smallest omitted bbox at 250 and 200 px. (4) F2 region bboxes at figureH 600 (>= 40 x 40 for hair / head / arm / hand / leg / foot). (5) F3 lane width vs `boxMin` 26: the n <= 7 rule at laneW 220; a 240 lane raises it to 7 still (floor(232/30) = 7), 250 to 8 (frees de Schulter / Ellbogen, nl schouder / elleboog, pt cotovelo, it orecchio). (6) The per-locale meta window per face (`measure-instruction-window.js`). (7) `verify-hub-type-rows.js` expectation: 6 rows x 11 = 66 (no FACE refusal is recorded; fr `eye` and es / pt / it `toe` are per-WORD exclusions that leave the face buildable), exported to `hub-expectations.json`; poison a wrong `coordinate.type` before `apps['human-body']` lands.

**Only a native panel can rule:** OPEN 1 es strand literal at K (SEP preescolar campo formativo) vs "Conocimiento del Medio", and fr "Explorer le monde" (GS) vs "Questionner le monde" (CP): one literal per family, or a per-face `strand` override. OPEN 2 no title "Kroppen vår" `[NSR]` vs "Kroppens deler". OPEN 3 fi leg / foot labels (`sääri` / `jalka` vs `jalka` / `jalkaterä`; the validator forbids two parts printing one word) and no `neck` "nakke" vs "hals" (an `overrides` entry with a reason). OPEN 4 F5 band per locale (G1 default; K where the curriculum places "two eyes, two ears" in the pre-school year: nl kleuters, fr GS, es preescolar are candidates via the `level` key, no code change). OPEN 5 `bankArticle` per locale (de / nl asked for it; sv / da optional; never in fr / es / pt / it where elision or gender would print: the flag defaults false there). OPEN 6 de F5 wording ("zweimal" vs "Paare"), fi "parit" vs "kaksi" phrasing.

**Summary.** Six faces on one apparatus, all buildable in 11 locales with zero face refusals: label from a bank (K) · how many, 1 / 2 / 5 / 10 from body facts (K) · colour by legend (K) · write the word in letter boxes on the figure (G1) · what is missing, circle the chip + draw (K) · which come in twos (G1). One additive `layout` knob, one locale-neutral facts file, 46 panel literals per locale, no `objForms`, no inflection anywhere; the only per-word exclusions are fr `eye` (Œ) and es / pt / it `toe` (multi-word) on the write face. Compound heads locked in all 11 locales and measured collision-free. The two new assets are `primitives/body-figure.js` (16 ids, paired instances, omit + region fills) and `data/b4/body-facts.json`.
