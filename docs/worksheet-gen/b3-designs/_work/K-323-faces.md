# K-323 `all-about-me` — the FIVE variation faces (Phase 2, 2026-09-14)

Built from `K-323-all-about-me.md` §3 (+ §1/§4/§5 for refusals and poisons) under `_FACE-BRIEF.md`, on the base substrate of `_work/K-323-build.md`. Ids from `_records/b3var-id-allocation.json` (K-342 … K-346, all `types/k/`). Nothing shared was edited (`git status`: only the family's four files + the six new ones below); nothing committed. Every measurement below is from the real render pipeline (file:// fonts) at d2 en, under the en chrome (body 766-799), the de 3-line fixture (733) and the fi 4-line fixture (**700** — the base record's measured floor, not the README's 722; every face is budgeted to it).

## Files (all family-scoped)
| file | what |
|---|---|
| `scripts/worksheet-gen/tools/b3var-rows/all-about-me.js` | NEW — 5 rows (K-342 K-343 K-344 K-345 K-346), `HANDWRITTEN []`; the only file the emitter reads |
| `scripts/worksheet-gen/types/k/K-323-all-about-me.js` | the additive `layout` knob: `_buildWith` dispatches to `_buildFace` ONLY when the resolved config carries `layout` (the base's three configs do not → the base path is byte-identical, baseline PASS); `_buildFavourites / _buildFamily / _buildFaceLabels / _buildICan / _buildName`; `_pictures()` requires the seed LAZILY (data/ is gitignored); `verify()` branches on the root stamp `data-lcs-layout` (never `data-lcs-face`, which the base's own verify fails) |
| `scripts/worksheet-gen/templates/components-b3/all-about-me.js` | the base's 5 exports output-untouched (`aboutMeDrawBox` gained the ADDITIVE `labelKey` option, default `'family'`); NEW `aboutMeOptionTiles aboutMeFavouriteRow aboutMeFamilyFrames aboutMeFaceBank aboutMeFaceLabels aboutMePlaceLanes aboutMeCanRow aboutMeCanGrid aboutMeWantLane aboutMeNameLane aboutMeNameBoxes aboutMeCountCards aboutMeCompareLane` |
| `scripts/worksheet-gen/data/b3/all-about-me.js` | EN bank: NEW `optionWords` (34 vocabKey → tile literal, each === `displayWord(vocab)`), `strings['K-342'..'K-346']`; the seed's `anchors.nose.lane: 0.73` (see deviation 5) |
| `scripts/worksheet-gen/qa/verify-b3-all-about-me.js` | the gate grows: bank rules 8 (six string blocks, titles distinct in the family) + 9 (`optionWords` === vocab singular); section 6 = 5 faces × 3 chromes through the real pipeline with the node cross-checks; the face sweep; 11 face poisons (P6 P8 P10 P11 + PN2 PT PC PK PB PL2 PU) |
| `scripts/worksheet-gen/types/k/K-34{2,3,4,5,6}-all-about-me-*.js` | emitted by `node tools/gen-b3var-specs.js` |
| `docs/worksheet-gen/b3-designs/_work/K-323-faces.md` | this record |

## The five faces

### K-342 — My Favorite Things: Circle and Write — CODE `layout:'favourites'` (F1, STRUCTURED)
- **Row:** `{...base.d2, layout:'favourites', categories:['animal','food','color'], perRow:6, tile:100, tileH:104, pic:64, tileGap:7, copyW:591, favRowMin:224, glyphH:40, headingPx:18}`.
- **Child:** in each of three `.ws-lane` rows (heading = `favHeading.<cat>`, the ONLY category text), rings ONE of six pictured options (64 px picture or a 64 px colour swatch over a 16 px label) and copies its word onto the empty 591 × 64 writing lane behind the copy arrow. `rng.sample(labelled options, 6)` per row; a vocabKey shows once on the page; the seed is the fan lever (20/20 seeds distinct).
- **Labels** come from the NEW bank block `optionWords[vocabKey]` (colours from `COLOR_WORDS[loc]`), never the vocab at render; an option with no label in a locale DROPS, a category under 6 labelled options DROPS, fewer than 2 categories REFUSES (PU control: 5 animal labels → the animal row drops, `food,color` render clean; 5 food labels too → refusal).
- **Stamps:** root `data-lcs-layout="favourites" data-lcs-rows/per/pic/tile/label-cap`; rows `data-lcs-favrow`; tiles `data-lcs-opt="<vocabKey|color:x>"` + `data-lcs-opt-label`; the strip `data-lcs-options`; the copy lane `data-lcs-copy-lane`.
- **verify():** rows = stamp ≥ 2, categories distinct; per row a ≤ 2-line unclipped heading, exactly `per` tiles, the strip's `scrollWidth` ≤ the lane inner 639 and inside the lane, every tile ≥ 56, picture complete / colour-dir / no alt (or a swatch ≥ pic on a colour tile, never both), label present, ≤ 96 px (the design's tile ceiling, stamped), unclipped, inside the tile; no option twice on the page; no tile marked; a copy arrow; the copy lane EMPTY with a writing row ≥ 56; rows in order, none overlapping; no digit / `{`; every `data-lcs-answer` empty. **Node gate:** heading === `favHeading.<cat>`, every tile label === `displayWord(vocab[key].en)` / `COLOR_WORDS.en`, every tile an option of ITS category, tile 100 × 104, label ≤ 96, row ≥ 224.
- **Poisons owned:** P8 (`tileGap:8` → the spec guard "tile row 640 > the lane inner 639" AND, past it, verify "the tile row 640 px > the lane inner 639"), PT (a starter on the copy lane + a marked tile), PL2 (`vaaleanpunainen` on a tile → "128 px > the 96 px tile ceiling"), PU (unlabelled options drop; < 2 categories refuse).
- **PNG:** `scripts/worksheet-gen/out/dev/K-342-null-d2-en.png` (gate: `K-342-gate-d2-en{,-longchrome-de,-longchrome-fi}.png`). Read: animal fish/duck/pig/giraffe/tiger/sheep · food cake/grapes/milk/pizza/carrot/cookie · colour orange/yellow/purple/blue/green/red (labels ≤ 53 px), copy lanes empty behind the arrow, rows 258 (en) / 240 (de) / 225 (fi); nothing under the footer at any chrome.

### K-343 — All About My Family: Draw and Count — CODE `layout:'family'` (F2, K.CC.A.3, STRUCTURED COUNT)
- **Row:** `{...base.d2, layout:'family', frames:['people','brothers','sisters','pets'], cell:56, box:56, drawH:220, frameMin:214, headingPx:18}`.
- **Child:** draws the family in the 675 × 220 box (`familyDraw` caption), then on four numbered `.ws-card`s (`countHeads.*`, each ending with `:`) fills an EMPTY 286 × 118 ten-frame (cell 56 = the K floor) for a self-known count (0 allowed) and writes the numeral in the 56 px box. Seedless (20 seeds byte-identical).
- **Stamps:** root `data-lcs-layout="family" data-lcs-frames-n/frame-keys/cell/box`; cards `data-lcs-frame`, box `data-lcs-count`, frame wrapper `data-lcs-tenframe`; the primitive's own `data-lcs-a="0" data-lcs-b="0"`.
- **verify():** the family draw box present, EMPTY, captioned `familyDraw`, ≥ 600 × 150; frames = stamp, keys = stamp; per card a ≤ 2-line heading ending `:` under `count-<key>`, one EMPTY `.ws-blankbox` ≥ 56, exactly one ten-frame stamped 0/0 with 0 `[data-lcs-counter]` and cell ≥ 56 inside its card, no picture; no digit outside the badges; cards pairwise non-overlapping and below the draw box.
- **Poisons owned:** PC (a counter printed on the "people" frame → "1 counters printed" + "stamps a=1 b=0").
- **PNG:** `out/dev/K-343-null-d2-en.png`. Read: draw box 220, four cards with the heading + box row and the empty frame, cards 260 (en) / 244 (de) / 227 (fi) ≥ 214.

### K-344 — This Is Me: Label the Face — CODE `layout:'face'` (F3, STRUCTURED, VERIFIABLE)
- **Row:** `{...base.d2, layout:'face', parts:['hair','nose','eye','ear','mouth'], icon:260, laneW:200, laneH:64, glyphH:40, bankPx:17, drawMin:220}`.
- **Child:** copies each of five bank words (rng-shuffled `.ws-bankword` pills, 17 px) onto the 200 × 64 lane whose teal pointer (coral dot on the feature, teal dot at the lane's near-edge midpoint) points to that part of ONE printed face (`body parts/face`, 260 px at (207, 50) of a 674 × 360 figure), then draws his own face in the flexing box below (floor 220). The bank order is the fan lever (16 distinct orders / 20 seeds).
- **Lanes** per side are placed by `aboutMePlaceLanes` (anchor-centred, pushed ≥ 84 apart, clamped to the figure) with the nose pinned at `lane: 0.73` — measured L hair@70 nose@263 · R eye@128 ear@212 mouth@296.
- **Stamps:** root `data-lcs-layout="face" data-lcs-parts data-lcs-anchors=<JSON of the 7-anchor table> data-lcs-icon/lane-w/lane-h/draw-min`; bank words `data-lcs-bank="<id>"`; lanes `.ws-blankbox[data-lcs-label="<id>"][data-lcs-side]`; the overlay `svg[data-lcs-pointers][aria-hidden]` with `line[data-lcs-pointer][data-lcs-ax/ay/lx/ly]`.
- **verify() (the one graded face):** bank ids ⇔ lane ids (bijection) ⇔ the stamped parts; no bank id / text twice; bank words unclipped; the picture complete / colour-dir / ≥ icon / the `face` noun; every lane EMPTY with a writing row, 200 × 64, `.ws-blankbox`, not over the picture, ≥ 10 px apart; one pointer per lane, its anchor end INSIDE the picture and within 1.5 px of its table position, its lane end within 1.5 px of the lane's near-edge midpoint; **the crossing sweep:** no segment within 16 px of ANY other anchor of the table (used or not) **or of its mirror** (the face is symmetric — the other eye / ear / brow are not in the table but are on the picture), no two segments intersect; no bank word within 20 px of a lane end; the overlay aria-hidden; the draw box EMPTY, captioned `drawFace`, ≥ drawMin, below the figure. **Node gate:** bank word === `faceWords[id]` === `displayWord(vocab)`, lanes/pointers = parts, picture 260, draw ≥ 220.
- **Poisons owned:** P6 (a bank word removed → "no bijection"), P10 (eye ↔ nose sides swapped → "pointer eye crosses the eye-mirror disc (0.2 px < 16)" + the ear-mirror at 13.3 — the pointer to the RIGHT eye runs through the LEFT eye; the spec accepts the swapped seed, so verify is the only guard), PN2 (the nose lane at the design's anchor-centred slot, y 196 → "pointer nose crosses the ear-mirror disc (10.5 px < 16)" — the measured reason for the `lane` pin, deviation 5).
- **PNG:** `out/dev/K-344-null-d2-en.png` (+ `-longchrome-fi`). Read: bank mouth/hair/nose/eye/ear on one line; dots on the hair crown, the right pupil, the right ear's inner edge, the nose tip, the mouth; lines to the near edge of each lane, none through another feature; draw box 346 (en) / 247 (fi).

### K-345 — I Can: Tick What You Can Do — CODE `layout:'ican'` (F4, OPEN-ENDED self-report)
- **Row:** `{...base.d2, layout:'ican', cards:8, tick:56, pic:80, textW:146, literalPx:18, rowMin:132, glyphH:40, laneH:116}`.
- **Child:** reads the repeated first-person frame on eight `.ws-card`s (`[tick 56][10][cue 80][10][literal 18/22 ≤ 2 lines in 146]`, no numeral badge) and ticks the ones that are true, then writes one thing on the `wantLearn` ruling row (639 × 64, glyphH 40). `rng.sample(actions with a can literal, 8)` — 20/20 seeds distinct; `refuse:['ican']` or < 8 literals REFUSES.
- **Stamps:** root `data-lcs-layout="ican" data-lcs-cards/tick/pic/text-w/literal-px`; cards `data-lcs-action`, tick `data-lcs-tick`, cue `data-lcs-cue`, literal `data-lcs-can`; the lane `data-lcs-wantlearn` + the primitive's `data-lcs-ruling-row`.
- **verify() = structure only:** cards = stamp ≥ 6, actions distinct, literals distinct; per card an EMPTY `.ws-blankbox` ≥ 56, a complete colour-dir cue ≥ 80 with no alt, a non-empty literal at 18 px on ≤ 2 measured lines ("REFUSE the literal, never shrink the font"), no word wider than the 146 column, no badge, content inside the card; cards non-overlapping; the lane with its literal and exactly ONE ruling row ≥ 56 carrying no starter, below the cards. **Node gate:** literal === `bank.can[id]` and ≠ the cue's vocab noun, `wantLearn` === bank, rows ≥ 132.
- **Poisons owned:** P11 (`can.swim` 41 chars → bank rule + the spec guard "> 34 chars"; past the guard a 4-line literal → verify "runs 4 lines (> 2)"), PK (seven literals → "authors 7 can literals < 8 (refuse"; `refuse:['ican']` → "refuses the face (bank.refuse)").
- **PNG:** `out/dev/K-345-null-d2-en.png` (+ `-longchrome-fi`). Read: paint / brush my teeth / sing / ride a bike / run / swim / read a book / kick a ball, two literals on 2 lines, rows 157 (en) / 141 (de) / 133 (fi) ≥ 132, the lane clear of the footer at 700.

### K-346 — My Name: Write, Count and Compare — CODE `layout:'name'` (F5, K.CC.B.5 + K.CC.C.6, STRUCTURED COUNT)
- **Row:** `{...base.d2, layout:'name', boxes:10, box:56, boxGap:6, glyphH:40, cardH:110, countBoxW:60, countBoxH:56}`.
- **Child:** writes the name on the `myName` lane (the `oneLetterPerBox` hint printed ONCE, right-aligned at 16 px inkSoft), one letter per box in ten 56 px boxes, counts them into the `lettersCount` box and names the first letter in the `firstLetter` box (two 330 × 110 cards), repeats name + boxes for a friend, and rings `me` / `my friend` under `whoHasMore.question` (pills in FIXED order). Seedless.
- **Stamps:** root `data-lcs-layout="name" data-lcs-boxes/box/count-box`; rows `data-lcs-namerow` with `data-lcs-namelane`; `data-lcs-nameboxes` + the primitive's `svg[data-lcs-letterboxes="10"]`; cards `data-lcs-countcard` with `data-lcs-count="me"` / `data-lcs-first="me"`; `data-lcs-compare` with `.ws-pill[data-lcs-pill]`.
- **verify() = structure only:** both name rows with an EMPTY writing lane ≥ 56 and a literal; both letter-box lanes with exactly 10 rects ≥ 56, no model letter, inside the lane; the hint on the my-name row and printed exactly once; the count and first-letter boxes EMPTY ≥ 56; card headings ≤ 2 lines; pills `me,friend`, non-empty, unclipped, ≥ 40, distinct; the question literal; blocks in order, none overlapping; no digit / `{`.
- **Poisons owned:** PB (the friend's boxes at 9 → "friend: 9 boxes ≠ 10").
- **PNG:** `out/dev/K-346-null-d2-en.png`. Read: name lane + hint, 10 boxes, two cards ("The first letter of my name:" wraps to 2 lines in 232), friend lane, 10 boxes, question + two pills (80 / 139 px); the 650 stack spreads over the body with `space-between`, no hole at the bottom.

## Gate output
```
node scripts/worksheet-gen/qa/verify-b3-all-about-me.js
bank en: 25 labels, 7 face words, 13 can literals, refuse []
pictures: animal 12 / food 12 / color 7 / toy 10, 13 actions, 7 anchors
render d1/d2/d3 + de/fi chromes + glue fi: unchanged (base sections 1-4)
pool: 451 labels measured (11 locales), widest fi "viinirypäleet" 94.4 · de "Dinosaurier" 88.1 · it "automobile" 85.9 · en "strawberry" 84.1
render K-342 favourites: verify 0 lints 0 body 778 px … rows animal[fish duck pig giraffe tiger sheep] food[cake grapes milk pizza carrot cookie] color[orange yellow purple blue green red] widest label 53.0 px   (de 733 · fi 700)
render K-343 family: verify 0 lints 0 body 766 px … frames people 260 / brothers 260 / sisters 260 / pets 260   (de 244 · fi 227)
render K-344 face: verify 0 lints 0 body 799 px … bank mouth hair nose eye ear lanes hair@70 nose@263 eye@128 ear@212 mouth@296   (de 733 · fi 700)
render K-345 ican: verify 0 lints 0 body 799 px … cards paint brushteeth sing bike run swim read soccer rows 157 lines 22111111   (de 141 · fi 133)
render K-346 name: verify 0 lints 0 body 766 px … boxes 10+10 pills 80/139   (de 733 · fi 700)
sweep: 20 seeds × d1/d2/d3 → 1 body each (byte-identical, seedless)
sweep faces: favourites 20 option sets, face 16 bank orders, ican 20 action sets, family/name byte-identical
poison: P1 · P2 bank/build · P3 · P4 · P5 · P7 · P9 guard/floor · P12 · P13 verify/node · PN guard/verify · PL · PH node · PH2 · PO · PD/window ·
        P6 · P8 guard/verify · P10 (eye-mirror 0.2 px, ear-mirror 13.3 px) · PN2 (10.5 px) · P11 bank/guard/verify · PT lane/tile · PC/stamp · PK seven/refuse · PB · PL2 · PU (+ control rows food,color) — all KILLED
PASS (1437 assertions, 26/26 poisons killed)          --quick: PASS (1430 assertions, 26/26 poisons killed, --quick: sweep skipped)
```
`node tools/gate-variation-distinct.js --batch=b3 --diffs=2 --family=all-about-me` → `[b3:all-about-me] compared 15 pairs over 5 faces against their bases + pairwise within family` / **`every variation differs from the deck its base publishes and from its siblings`**.
`node tools/b3-baseline.js --check --quick` → **`checked build 3068 + enum 211 in 16s: 0 drifted (0 expected), 0 missing` / `PASS`** (also PASS before the first edit and after the component edit alone).
`node i18n/build-en.js` → `538 types … (title lint clean)`; `git checkout -- i18n/strings.en.json` restored the committed file.

## Deviations from §3 (each with the measurement)
1. **Component names are type-scoped `aboutMe…`** (the design's `optionTiles / favouriteRow / familyFrames / faceLabels / canRow / nameBoxes`): the namespace loader THROWS on a duplicate export for every b3 family at once and twenty builders are landing in parallel (`nameBanner` and `drawBox` HAVE since landed from `animal-fact-file.js` / `read-and-do.js` — the base record's stand-ins stay type-scoped for the same reason; folding is the owners' call). `aboutMeDrawBox` gained `labelKey` (default `'family'`, output unchanged) so F2/F3 caption their boxes `familyDraw` / `drawFace`.
2. **F1 rows `minmax(224px,1fr)`, tiles 100 × 104** — not the design's 232 / 100 × 110: 3 × 232 + 24 = 720 fits the README 722 but OVERFLOWS the measured 700 px body of a legal 4-line fi title by 20. Tile 104 (64 picture + 3 + 20 label inside a 2 px border), heading line-height 24, the copy row 64 → 3 × 224 + 24 = 696 ≤ 700; the lane is a `space-between` column so the slack of a shorter chrome opens between the heading, the tiles and the copy lane (rows 258 under en chrome). Gap 7 → 635 ≤ 639 as designed (gap 8 = 640 is P8).
3. **F1 labels come from a NEW bank block `optionWords`** (per-locale, keyed by vocabKey) — §3 says the label "=== displayWord(vocab…)" in VERIFY while §5 says the page "never [reads] image-vocabulary.js at render"; the only way to satisfy both is a bank literal cross-checked by the gate (the K-319 precedent). EN authored here (34 keys); the other ten are generated by `apply-b3-locale.js` from the vocab (a locale without them keeps only the colour row → refused until authored). Colours read `COLOR_WORDS[loc]` as the design says.
4. **F2 grid `minmax(214px,1fr)` in a flex column under a FIXED 220 draw box** (220 + 12 + 2 × 214 + 14 = 674 ≤ 700); cards 260 / 244 / 227 under en / de / fi. Numbered badges kept (the design's "no numeral except the card badges").
5. **F3 lane positions are anchor-centred and the nose lane is PINNED (`anchors.nose.lane: 0.73`)** instead of the design's L 20/196 · R 0/148/296. Measured: at the design's nose slot (y 196, line at y ≈ 228) the nose pointer runs through the lower half of the LEFT ear (10.5 px from the ear's centre — the ear spans y 193-237 of the figure), a real defect for a child labelling "ear"; the crossing sweep can only see it because it mirrors every side-specific anchor (the left eye / ear / brow are on the picture but not in the table). At 0.73 (lane y 263, line to y 295) the nose line clears every disc by ≥ 37 px. The pin is data (the seed), not code; PN2 is that poison. The resolver (`aboutMePlaceLanes`: centre on the anchor, push ≥ laneH + 20 apart, clamp inside the figure) lands the right lanes at 128 / 212 / 296 and the hair at 70.
6. **F3's draw box FLEXES (floor 220), not the design's fixed 260**: 59 + 10 + 12 + 360 + 12 + 260 = 713 > 700; it renders 346 under en chrome and 247 under fi. The design's "swap eye and nose sides → crossing FAIL" (P10) is a MIRROR-disc failure, not a segment crossing: the swapped geometry has no intersecting segments (measured), but the pointer to the right eye passes 0.2 px from the left pupil — the sweep names it. The ~65 px hole between the bank and the face is the design's own 360 figure with the icon centred (the hair lane at 70); recorded, not changed.
7. **F4 rows `minmax(132px,1fr)`**, not the design's 136: 4 × 136 + 42 + 12 + 116 = 714 > 700; 4 × 132 + 42 + 12 + 116 = 698 ≤ 700 (card inner 104 ≥ the 80 cue). Rows measure 157 / 141 / 133. The design's lane 116 is kept (a 112 lane cannot hold 24 + 6 + 64 inside padding 8 + border 2 — the spec guard says so). I-can cards carry NO numeral badge (a K reader should not see "3" before "I can").
8. **F5 has TWO open boxes (count + first letter), not the design's "three"**: the §4/§5 literal set has no friend-count literal (`lettersCount` = "Letters in my name:"), and the friend's letters are compared one-to-one against the aligned 10-box rows above — the K.CC.C.6 matching strategy — before the pills. A `lettersCountFriend` literal is an open item for the panels if the third box is wanted. The 650 stack is `justify-content:space-between` so the 116-149 px of a shorter chrome spreads between the blocks (the design's stack would leave a hole above the footer). `oneLetterPerBox` prints once, right-aligned on the my-name row at 16 px inkSoft (the design said "prints ONCE here" without a position).
9. **d1 / d3 variants of the five faces are not built** — the emitter ships ONE config for all three levels (the waves publish d2 only), so the §3 d1/d3 rows (2-row F1, 2-card F2, 3-part F3, 6-card F4, my-name-only F5, the pillChoice on F2 d3, eyebrow/chin on F3 d3, two tick columns on F4 d3) are unreachable; the builders accept them (guards key on the resolved keys: 4..6 tiles, 2..4 frames, 3..7 parts, 6 or 8 cards, 8..12 boxes).
10. **The long-chrome fixtures are the base's** (a 70-char de title + 150-char instruction → 733; the fi 4-line title → 700): the squeeze depends only on the chrome's line count, not on which face's title it is.

## Per-locale refusals visible from the bank shape (Phase 4 `hub-expectations.json`)
- **None lowers a row today; expected rows stay 6 × 11 = 66** — CONTINGENT on each locale block carrying: `optionWords` for the 34 picture options (F1; generated from the vocab — the gate's pool gate already proves every label fits the 96 px tile in all 11, widest fi `viinirypäleet` 94.4; without the block a locale keeps only the colour row and F1 REFUSES), `countHeads.*` + `familyDraw` (F2), `faceWords` for hair/nose/eye/ear/mouth (F3 — exist ×11 per the design; a 5-pill bank at 17 px is ≤ 674 in every locale, `orecchio` 67 being the widest d2 word), ≥ 8 `can` literals ≤ 34 chars on ≤ 2 lines in 146 px at 18 (F4 — a 3-line literal REFUSES the render; fi `Osaan ajaa polkupyörällä` measured 2 lines by the design), and `myName / friendName / oneLetterPerBox / lettersCount / firstLetter / whoHasMore.*` (F5).
- **Width envelopes for the panels (measured here):** F1 tile label ≤ 96 px at 16 (gate-enforced); favourite heading ≤ 639 on one line at 18 (nowrap in the lane — the base's 2-line window rule does not apply on F1); F2 heading ≤ 2 lines in 236 at 18/20 (de `Personen in meiner Familie:` 234.9 = 1 line); F4 literal ≤ 2 lines in 146 at 18/22, no single word > 146; F5 `oneLetterPerBox` at 16 shares the 639 line with `myName` at 18 (de ≈ 330 + 90 fits), card headings ≤ 2 lines in 232 at 18/20 (de `Buchstaben in meinem Namen:` 262.8 → 2), pills `me` + `friend` + 14 ≤ 639 (de `meine Freundin` 191.7 fits); F3 bank words ≤ ~120 each.
- **`refuse:['ican']`** is the explicit F4 refusal a panel may declare (the spec throws on it; the gate's PK poison proves it).

## Open items for the panels / Phase 4
- `apply-b3-locale.js` must emit `optionWords` (34 keys = `displayWord(vocab[key][loc][0], loc)`) and `strings['K-342'..'K-346']` per locale; the gate's rule 9 fails a wrong literal, and a missing one is a silent per-option drop — the hub gate should count F1 rows, not assume them.
- F5: decide whether the friend's count needs its own box (a `lettersCountFriend` literal); today the comparison is by the aligned boxes + the pills.
- F3: the `lane` pin lives in the seed (locale-neutral); if a d3 (7 parts) is ever shipped, run the sweep on it — the eyebrow (L) and chin (R) slots have not been rendered.
- Registration (`apps['all-about-me']`, `axes['exercise-type']['all-about-me']` × 11, landings per face with `coordinate.mode` = `favourites | family | face | ican | name`, base `'base'`, `hub-expectations` 66) is the batch's `register-b3-*` step, not this build.
