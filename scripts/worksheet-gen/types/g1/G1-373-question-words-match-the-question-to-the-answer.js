/** G1-373 — Match the Question to the Answer. nt10-D variation of G1-353 (generated by tools/gen-b4var-specs.js from tools/b4var-rows/). */
'use strict';
const base = require('./G1-353-question-words.js');
// One object for all three levels: the waves ship d2 only, so a face must
// render identically whichever level is asked for. Spreading the base entry
// (not a JSON literal) carries function-valued params through intact.
const D = { ...base.difficulty[2], ...{"mode":"match","pairs":5,"kinds":["who","what","where","when","howmany"],"picPx":88,"itemH":100,"itemMax":120,"leftW":252,"rightW":248,"fontPx":18,"maxChars":44,"maxThings":4} };
module.exports = {
  ...base,
  id: 'G1-373',
  slug: 'question-words-match-the-question-to-the-answer',
  difficulty: { 1: D, 2: D, 3: D },
  i18n: { en: { title: "Match the Question to the Answer", instruction: "Read each question on the left. Draw a line to the answer on the right that fits it." } },
};
