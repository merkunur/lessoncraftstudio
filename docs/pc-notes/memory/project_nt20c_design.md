---
name: project-nt20c-design
description: "nt20-C (2026-09-13/14) — the THIRD/LAST batch of 20 printable worksheet types × (1 base + 5 variations) × 11 locales = 1,320 worksheets: DESIGN COMPLETE (20 of 20 final design files), NOT built; the studio recipe, the hub-visibility root cause, the build trigger and recipe pointer"
metadata:
  node_type: memory
  type: project
  originSessionId: 215a8fe6-4312-4c85-bb49-8f9c20e5d24e
  modified: 2026-09-14T03:52:47.119Z
---

# nt20-C — 20 new worksheet types + 5 variations each, all 11 locales (DESIGN PHASE COMPLETE 2026-09-14; NOTHING BUILT)

**Deliverable of the DESIGN phase (done):** one detailed file per type at `docs/worksheet-gen/b3-designs/<ID>-<key>.md` (7 sections: identity · base page · faces 2-6 · native rebuild ×11 · data + gates · SEO · hub contract), plus README (build recipe, open items 1-12, cross-type rulings, the hub-gate expectation matrix), memory, CLAUDE.md §25 pointer. Every final has `_work/<ID>-{pedagogy,design,critic}.md` beside it. **Nothing is built.** The BUILD is a later session, trigger: **"build the last batch of 20 worksheet types and their expansion"** → README first, then the 20 files, then the 11-step recipe. Do NOT relaunch the studio loop; do not re-research demand or selection.

## Resume state
- **FINALS 20/20** (K-317 K-318 K-319 K-320 K-321 K-322 K-323 K-324 · G1-305..310 · G2-315..319 · G3-377). Last final in: K-324 picture-word-cards (2026-09-14). Nothing in flight.
- Checkpoint commits `2953f8d8` `78e217e1` `ac127cf0` `ed0eac20` `d1acae96` `601ebf8a` `92af8fba` `22b49156` (the closing commit, "nt20-C design COMPLETE"). Docs only; no push unless the operator asks; no deploy needed.
- The README **hub-gate expectation matrix** (per key × locale rows; design total ≈1,247 of 1,320, ceiling ≈1,299) is what `verify-hub-type-rows.js` reads in the build session. Every gap is a recorded refusal (e.g. syllable-split en 4 / fr 5; spelling-rules nl/sv/no 5; compound-words 47; read-and-do sv/da/no 1→6 once the `def` tables are authored; picture-word-cards fi 5).
- **Two LIVE fixes already committed during the design (`d1acae96`):** sv ordinal rule in `scripts/worksheet-gen/data/b2/calendar.js` (1:a 2:a 21:a; 3:e 11:e — `data/` is gitignored, `git add -f`), 15 soft hyphens out of `frontend/content/seo-landing/fi.json`. Their republish (sv calendar decks with dates 1/2/21/22/31) is a build-session item.
- **Build-session prerequisites recorded in README open items:** the `÷` glyph hard-coded in `array-tasks.js:141/151/159` + `number-line-tasks.js:95` (sv G2-216/217, G3-309/310/311 print a historic minus; NOT G3-369); `animal-facts.json` to author before G2-318; the "Free printable" meta lead is RULED 2026-09-14 (metadata-only; forbidden in visible copy — README item 1, lint visible fields in the build); `strand-names.ts` Language row lacks da/no; `verify-hub-type-rows.js` to write + poison-test + wire into deploy.sh.

## The studio loop that produced the 20 files (for the record; not to be re-run)
1. **Pedagogy+content agent** (`_work/<ID>-pedagogy.md`): reads `_STUDIO-BRIEF.md`, `_SUBSTRATE.md`, its `_PANEL-FINDINGS.md` section, sibling finals, the existing specs; MEASURES pools with read-only node (type-scoped scratch filenames — agents overwrote each other once); delivers identity ×11 · six faces (move, d2 params, PARAM/CODE, verify, refusals with numbers, fan lever, query face, rejected non-moves) · native rebuild ×11 · data + gates + poison cases · SEO · open questions.
2. **Design studio agent** (`_work/<ID>-design.md`) in parallel: LOOKS at `out/b2-sweep/*.png`, reads page.css / components-b2 / primitives / an exemplar spec; delivers concept · ASCII layout + px grid for d2 · ladder · answer-hiding stamps · reused vs NEW components (named for `templates/components-b3.js`) · locale slots · five variation deltas · alternatives · risks + print check.
3. **Editor-critic agent** (final + `_work/<ID>-critic.md`): merges by "measured buildability > preference; brief > both", verifies every name in the repo, re-measures disputed numbers, OPENS pictures on art disputes, picks the 5 faces (genuine move · ≥7 locales on measured pools · distinct resolved config · distinct query face · clean boundary vs existing types), records refusals per locale and the per-locale hub row count.
4. I read the final. ≤4 agents in flight (session-limit trap: agents that wrote their file are done even if the notification was lost — check `_work/` before relaunching anything).

## Facts the studio established (do not re-derive)
- `chunks` grapheme layer exists only in de/nl/sv/no; elsewhere `chunks === split`. EN (and fr 59 %) approved syllable BOUNDARIES are often rule-only (no TeX) → boundary-printing faces use the texPool (`'TeX'` in `sources_agreed`). da K-1 strict pool = `policy_managed` ABSENT (402); G2 spelling uses the full da pool. es `playa` stored `play-a` (pipeline defect, banned by key).
- Body height is 722–814 px measured (NOT 760); `.ws-lane` inner width 639 (647 only with inline `padding:10px 12px`); font widths must be measured with the shell woff2 loaded from `file://` (else ~6 % narrow). `wordBank` 59+10, `rulingBlock` +6/row, `pillChoice` 48 high; open numeral boxes need a NEW `blankNumeralBox`; `sentence-bank.js fillFrame` is closed to `{name|n|noun|color}` → NEW `lib/b3-instructions.js fillSlots`.
- ONE fan mechanism `unitAxis` (letter/rule/family/animal/partner/range). Themeless types draw from a NEW `lib/b3-picture-index.js`, landings `coordinate.theme:''`; `coordinate.mode` = the face string, base `'base'`, never null (`coordKey` = type|mode|theme, no level). `science` is a legal `default_subject`.
- `qa/lints.js` has NO element-size lint (density never read) → floors live in each type's gate. `sceneStage` takes ONE theme. `gate-variation-distinct.js` is b2-bound. `verify-hub-type-rows.js`, `components-b3.js`, `apply-b3-locale.js`, `validate-b3-draft.js`, `register-b3-*.js` do NOT exist yet (build session).
- Opened-picture rulings: `weather/cold` = penguin in a hat, `weather/hot` = a sun, `space/sun` = black square, `activities/swimming` = goggles, `singing` = microphone, `painting` = palette (objects, not actions); `fruits/plum` is a red apple; the 10 accepted action pictures are listed in G2-317. Only 6 emotion faces are K-legible (happy sad angry scared surprised tired).

## Why these 20 (locked; four panels + harvest mining; record = `_PANEL-FINDINGS.md`)
K-317 letter-of-the-week · K-318 sound-boxes · G1-305 syllable-split · G1-306 syllable-reading · G1-307 opposites · G2-315 spelling-rules · G2-316 compound-words · G2-317 verb-forms · G1-308 read-and-do · G1-309 rhyming-words · K-319 emotions · G2-318 animal-fact-file · G1-310 hundreds-chart-puzzles · G3-377 division-with-remainder · K-320 ordinal-numbers · K-321 days-and-months · K-323 all-about-me · K-322 seasons · G2-319 logic-puzzles · K-324 picture-word-cards (operator ruled INCLUDED: the 2026-05-17 flashcards amendment covered the interactive reader, not a printable sheet). Variation ids K-325+, G1-311+, G2-320+, G3-378+.

**⭐⭐ THE HUB-VISIBILITY ROOT CAUSE (the operator's OBS):** the `/worksheets` sidebar is corpus-derived and grouped by taxonomy SUBJECT via `apps.<key>.default_subject` — a key registered in `axes['exercise-type']` but WITHOUT `apps.<key>` is rendered NOWHERE in the rail while `?type=<key>` still works when typed. Labels use `name.<locale>` with no en fallback; rows = landing corpus only (`applyLandingFilters`: exact `coordinate.type`); the old hub strip is DELETED (`56f5a388`); the landing file cache refreshes only on deploy. Fix by construction + the new gate `scripts/verify-hub-type-rows.js` (expected rows per key × locale from the README matrix, poison-tested: drop one landing AND drop `apps.<key>`, both must FAIL) — both in README.

**Backlog (evidence recorded, not built):** sentence true/false (a face of read-and-do), copywork, weather, tangram, word snake, number-sentence true/false, comparatives, units conversion, sequencing (asset-blocked), cursive (glyph-blocked — the biggest fr/it gap), five senses, rounding/magic squares/combinatorics.
