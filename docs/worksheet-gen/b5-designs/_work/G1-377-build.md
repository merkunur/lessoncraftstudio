# G1-377 `animal-life-cycles` — base build record (2026-09-23)

Contract: `../G1-377-animal-life-cycles.md` §2 (base) + §5 (data + gates). Faces F1-F5 are NOT built (Phase E); the base composer REFUSES any `layout` config.

## Files (all type-scoped; nothing shared edited)
| file | what |
|---|---|
| `scripts/worksheet-gen/primitives/life-stage.js` | NEW: the 13 stages (butterfly 4, frog 5, ladybird 4) + `spawnForm:'string'`, lens wrapper, constant-px strokes, `MIN_SIZE` 90, `STAGE_IDS` |
| `scripts/worksheet-gen/qa/verify-life-stage.js` | primitive gate: node parse of the emitted markup against its OWN biology table + a Chromium measurement at 92 px + 7 poisons + colour/grey contact sheets |
| `scripts/worksheet-gen/templates/components-b5/animal-life-cycles.js` | `lens`, `hostPlant`, `lifePlate`, `loopRow` (face components come with the faces) |
| `scripts/worksheet-gen/types/g1/G1-377-butterfly-life-cycle.js` | the spec (slug `butterfly-life-cycle`; `_buildWith` poison seam; §5 helpers `stageWord` `statement` `decoyWord` `successor` `statementPool`, each THROWS on a missing literal) |
| `scripts/worksheet-gen/data/b5/animal-life-cycles.js` | `ANIMAL_LIFE_CYCLES.en` (stage words, decoy, landing words, 11 statements, family name, 6 title+instruction pairs) + locale-neutral `ALC`. `data/` is gitignored: force-add |
| `scripts/worksheet-gen/qa/verify-b5-animal-life-cycles.js` | family gate; exports `validateBank(block, loc)` (the probe already maps it), `validateNeutral`, `enumerateArrangements` |
| `scripts/worksheet-gen/i18n/strings.en.json` | regenerated by `node i18n/build-en.js` (665 types, title lint clean) |

## Gate output
`node qa/verify-life-stage.js`: **PASS (43 assertions, 7/7 poisons killed)**. Render at 92 px: magnified butterfly egg 30.4 px (floor 28), legged-tadpole legs 23.9 / 23.9 px (14), froglet stub 7.1 px below the body (6), smallest ladybird spot 5.5 px (5). Poisons L1 stub dropped · L2 silk strand · L3 caterpillar antenna · L4 egg at true scale · L5 spot off the elytra · L6 legs at the front · L7 size 88: all KILLED.

`node qa/verify-b5-animal-life-cycles.js` (full): **PASS (145 assertions, 18/18 poisons killed)**
```
neutral: 0 findings (ARRANGEMENTS re-enumerated: egg,adult,pupa,larva | larva,adult,pupa,egg | adult,larva,egg,pupa | adult,egg,larva,pupa)
bank en: 0 findings   (+ synthetic de/pt/sv/fi/fr control drafts clean)
node sweep 400 seeds: share {A2:116, A3:89, A1:100, A0:95}, egg corners TR,BL,BR,TL, per-page placement failures 0
rule 6 (en, 359 px Nunito 700 17 px): every statement <= 2 lines (change: 2, the rest 1)
render d1/d2/d3: verify 0 lints 0, body 799 (one-line title chrome)
render d2 long chrome de: body 710 (<= 722) · fi (5-line title): body 667 (<= 677), lowest 916 < footer 921
arrangements A0-A3 forced + rendered: tethers on their spots, disjoint, tell-free
sweep 20 seeds verify-clean, all 4 arrangements drawn
P1-P12 KILLED · PR1 PR2 PR3 PR4 PR15 KILLED · PS (sparse, space-between) KILLED
PR7 = primitive L1 (the base draws no frog) · PR5 PR6 PR8-PR14 deferred to Phase E
```
`--quick` also PASS. `node i18n/build-en.js`: `665 types … (title lint clean)`.
`node tools/b3-baseline.js --check --quick`: **`checked build 3668 + enum 277 in 25s: 0 drifted (0 expected), 0 missing` / `PASS`**.

## Stacks (measured in the render)
Plate 528 + rule 18 + loop row 116 = 662. Body 799 at the en one-line chrome (137 px of slack BELOW the loop row), 710 at the de 722 fixture, 667 at the fi fixture (the fixture renders a 5-line title; 662 <= 667, bottom 5 px clear of the footer band).

## Answer-position tells (coordinator note)
The base has 4 corners and 4 answers, so every corner holds an answer on every page by construction; the real tell is ORDER. Measured per page in `verify()` (from the rendered lens centres, not the stamp): none of the 8 linear reading orders and neither corner walk from the egg reads 1-2-3-4, the adult sits in a top lens, the placement is one of the frozen four, four distinct ranks. The node sweep repeats this per page over 400 seeds (0 failures) and shows the given egg visits all four corners across the set; pooled share 89-116 per arrangement. The shipped instance (seed epoch 1) is **A2** (TL adult, TR egg, BL caterpillar, BR chrysalis), identical in all 11 locales.

## SPARSE (coordinator note)
The design's `space-evenly` column was replaced by a TOP-anchored column (slack falls below the loop row). The gate measures every blank band between consecutive blocks (body top -> plate -> rule -> loop row) at the 814, 722 and 677 chromes: 0 px everywhere, and poisons it both ways (the control passes; `space-between` produces a > 40 px band and is KILLED).

## Deviations (each with its measurement)
1. **Lily-pad notch arc written `A r r 0 1 1`**, not the design's `0 1 0`: between two points 30° apart on a circle centred on the pad, the large arc runs in the positive (sweep 1) direction; `0 1 0` makes the renderer re-centre the circle and draw a lobe. Verified in the contact sheet.
2. **Top-anchored body** (above) instead of `space-evenly`.
3. **The string spawn (pt only)** leaves r 88 by its own geometry (end spheres reach 89-92). The gate holds it to the design's own stated rule instead: centres <= 86 (true) and every sphere inside the r 96 clip.
4. **"20 seeds render distinct pages"** is unreachable for the base: it has exactly 4 legal pages. The gate asserts 20 verify-clean seeds + all 4 arrangements drawn + each arrangement forced and rendered.
5. **Two extra block fields** so rule 10 can run per locale: `pictureWord` (the base instruction must name "the number of the PICTURE") and `instructionBans` (per-face apparatus bans for non-en; en uses the gate's own regexes). Also `drop` (the statement ids a locale cannot write animal-free; `statementPool` subtracts them) and `FACES` in `ALC`.
6. **Rule 6 two-line check** is wired in the gate's browser section for en (359 px, Nunito 700 17 px); `validateBank` checks only the char caps (no browser in the draft validator).
7. **d1 is inconsistent with the one instruction**: it has no loop row but the instruction still mentions "the last box". d1 is a non-shipped warm-up (design §2); if it is ever shipped it needs its own instruction.
8. The plate caterpillar mark faces its leaf tip, so on a left leaf it faces left while the lens caterpillar faces right. Harmless (a speck at 6 px), recorded.

## Renders (read by me; the reviewer should too)
- Page: `scripts/worksheet-gen/out/dev/G1-377-null-d{1,2,3}-en.png` (render/one.js) and `out/dev/G1-377-gate/G1-377-gate-d{1,2,3}-en.png`, `…-d2-longchrome-de.png`, `…-d2-longchrome-fi.png`, `…-arr-A{0..3}.png`.
- Primitive contact sheets, every stage at 90 / 132 / 144 px: `out/dev/G1-377-life-stage-sheet-colour.png`, `out/dev/G1-377-life-stage-sheet-grey.png`.
- What I checked by eye: every stage reads as what it is at 90 px (the ladybird eggs and larva are the smallest reads, true to the design's "a cluster reads as a cluster"); greyscale carries every stage by its dark features and form; the three eggs differ by form; the tethers never cross and the rings sit on the right leaf / flower; the chrysalis hangs under its leaf on the plate; nothing clips at the fi chrome. Fixed on sight: the first loop arrow was a plain arc that read as "go back" (the cycloid sign was wrong) — it is now a real curl that crosses itself and carries on to the box.

## Open items for the faces / panels
- Face components (`pondRing`, `cutStrip`, `labelLoop`, `youngStrip`, `adultBin`, `compareTable`, `nextRow`), their verify branches and poisons PR5 PR6 PR8-PR14; the face strings are authored in the bank keyed by layout until the emitter allocates ids.
- The loop arrow's curl is small at 84 x 64; a print check should confirm a six-year-old reads it as "comes round again".
- Panels: author `pictureWord` and `instructionBans` per locale (deviation 5); pt rules `spawnForm`.

## Base FILL pass (2026-09-23)
- **Defect.** Plate 528 + rule 18 + loop row 116 left the ink at ~82 % of the 799 px body at the one-line en chrome (FILL rule: ≥ 85 % at 814, inside at 677).
- **Fix (base path only; faces byte-identical).** `loopRow` gained an optional `grow = { lensMax, maxH }` (no `grow` = the old markup, byte-identical): the row becomes a size container `flex:1 1 116px; min-height:116px; max-height:maxH` and the adult lens is `clamp(lensD, 100cqh − 12px, lensMax)`. d2/d3 carry `loopLensMax 152`, `loopRowMax 180` (guarded: lensMax ≥ lensD, rowMax ≥ lensMax + 12, lensMax ≤ 200); `cfg.loopLensMax` is stamped and `verify()` accepts the loop lens in `[loopLensD, loopLensMax]`, square. The loop box stays `boxPx` (the same size as the plate boxes). lensMax was tried at 168 first and read too dominant beside the 132 plate lenses; 152 keeps the question row one step up, not a new hero.
- **Measured:** d2 / d3 **89.1 %** (was ~82 %), de 722 fixture 99.2 % (lens 152), fi 667 fixture 99.1 % (lens 109, row at its floor) — all inside the body; SPARSE bands [0,0,0].
- **d1 stays at 66 %, recorded not fixed:** d1 (a non-shipped warm-up, design §2) has no loop row, and the plate is width-bound (639 of 675) with its tethers drawn in fixed plate px, so it has no elastic block. Growing it would mean a responsive plate geometry — a design change, not a FILL pass.
- **Gate** (`qa/verify-b5-animal-life-cycles.js`): `assertBaseFill` on d2/d3 (≥ 85 %) and "inside the body" on every base render. Poisoned both ways: FL-base-d2 / d3 (row frozen at 116) KILLED; FG-base (row forced to 200 at the fi chrome) KILLED; control 89.1 %. The PS poison now freezes the loop row before `space-between` (a growing row absorbs the slack and left PS SILENT — caught on the first run). Full gate **PASS (1429 assertions, 56/56 poisons killed)**.
- Baseline: drift only on `G1-377|nothm|d2|d3|en`; d1 and every face id unchanged. Read: d2/d3 en, the de and fi long chromes.
