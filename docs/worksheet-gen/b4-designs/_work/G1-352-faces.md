# G1-352 `pronouns` : the FIVE variation faces (Phase 2 build record, 2026-09-21)

Built from `G1-352-pronouns.md` §3 (the contract), §1 / §4 / §5 (rulings, data, gates), `_FACE-BRIEF.md`, the base record `_work/G1-352-build.md` (open items, the measured 710 chrome, the 32-portrait bank, the reviewer's rule-2 ruling) and the README cross-type rulings. Exemplars read: `tools/b4var-rows/five-senses.js` + `_work/K-355-faces.md`, `tools/b4var-rows/weather-symbols.js` + `_work/K-356-faces.md` (deviation 9, the sparse write face), `tools/b3var-rows/seasons.js`. All five are CODE faces on the base's ONE additive `layout` knob; no HANDWRITTEN spec. No em-dashes.

## Files (type-scoped; nothing shared edited; the base byte-identical)

| file | what |
|---|---|
| `scripts/worksheet-gen/tools/b4var-rows/pronouns.js` (NEW) | the five rows (ids per `_records/b4var-id-allocation.json`): F1 G1-371 `replace` · F2 G2-353 `anaphora` · F3 G2-354 `possessive` · F4 G1-372 `sort` · F5 G2-355 `rewrite`; F2 / F3 / F5 carry `extra {gradeBand:'G2'}`; EN title + instruction = the bank's `strings.<face>` (the gate asserts one source) |
| `scripts/worksheet-gen/types/g1/G1-352-pronouns.js` | `_buildFace` dispatches the knob to `buildReplace / buildAnaphora / buildPossessive / buildSort / buildRewrite`; `faceDeal` (the class-resolved config off the BANK's shape: `four` / `two` / `obj` / `ending`), `dealFace` (the base deal cloned minus chips + plate guard; the mix floor runs over the chips THIS face can reach), `possessiveClass` + `possForm` (a thing's `forms` is keyed `{m1, f1, p}`, every pair key collapses to `p`); `VERIFY_FACE` (one page-side branch per stamp: floors, uniqueness, no answer printed, SPARSE); `verify()` dispatches on `data-lcs-layout`. The base path (`resolveBase` / `compose` / `VERIFY_BASE`) is untouched: `node tools/b3-baseline.js --check --quick` PASS |
| `scripts/worksheet-gen/templates/components-b4/pronouns.js` | ADDITIVE stamps only: `nameGapRow` / `ownerLane` / `rewriteLane` take `names` (`data-lcs-names`), `anaphoraBlock` referents take `key / refs / names`, `nameCardBins` cards take `itemKey / refs / names` and the shelf stamps `data-ws-content`. No new export; no geometry changed |
| `scripts/worksheet-gen/data/b4/pronouns.js` (gitignored, the reviewer force-adds) | one additive key: `possessive.chipW: 66` (the design's F3 per-locale chip width, `max(60, widest label at Baloo 2 700 20 + 18)`: en `their` 43.7 + 18; nl 60, pt 66, de 66, fr / it 60, sv / da / no 84 per §3 F3) |
| `scripts/worksheet-gen/qa/verify-b4-pronouns.js` | the gate grows: section 5 (below); rule 6 gains `chipW` (in [60, 100], `n x chipW + (n - 1) x 12 <= 487`); rule 2 becomes the reviewer's FLOOR (>= 6 f and >= 6 m, fi >= 8 / >= 8, <= 16); rule 13 covers the base + F1 + F3 titles in EVERY locale; the synthetic fr block carries 13 names and 4 f things; the final line is `PASS (N assertions, M/M poisons killed)` |
| `scripts/worksheet-gen/types/{g1,g2}/G1-371… G2-353… G2-354… G1-372… G2-355…` | emitted by `node tools/gen-b4var-specs.js` |
| `scripts/worksheet-gen/i18n/strings.en.json` | regenerated by `node i18n/build-en.js` (653 types, title lint clean); left as generated |

## The faces

### F1 : G1-371 `Replace the Name with a Pronoun` (G1, CODE `layout:'replace'`)
- **Knob:** `{layout:'replace', rows:8, bank:true, singles:5, pairs:3, pairMix:'mp,fp,xp', four:{singles:5, pairs:3, pairMix:'mp,fp,xp'}, pic:52, pairPic:48, gapW:96, gapH:30, fontPx:18, maxLine:55, laneMin:74, laneGap:6}`.
- **The child:** reads a sentence whose subject is a name (or two), then WRITES the sentence-initial pronoun from the dashed bank (`He · She · They`, shuffled away from the chip order) into the 96 x 30 box that opens the copy of the sentence below it. The verb literal carries the number (de/nl pairs -> `Sie` / `Zij`, proven by `lesen`; fi 5 -> `Hän`, 3 -> `He`).
- **Layout:** `initialBank` (60) + a `flex:1` grid `repeat(8, minmax(74px, 1fr))` gap 6 of `nameGapRow` lanes: stack **704 at 710**, the lanes stretch to the body bottom at every chrome (measured: body 766 -> lanes 82; 710 -> 75).
- **verify():** 8 lanes === the stamp, in [6, 12]; the banner present, >= 2 distinct bank words >= 16 px; every lane's answer IS a bank word and NO bank word is printed in either line; line 2 opens with exactly one EMPTY box >= 64 x 24 and equals line 1 minus its subject; the subject holds every stamped name; portraits === the key (1 / 2) >= 44, no src or name twice; line 1 one line <= 26; both numbers present, >= 2 distinct answers; SPARSE: the lanes end within 3 px of the body bottom, blank inside a lane <= 24, lanes <= 12 apart. **Node:** `keyOf` from OPENED === the key; `initial[map.replace[key]]` === the answer; line 1 === the frame filled; line 2 === the literal minus `{subj}`; the bank set === `initial`.
- **Poisons owned:** PC1 (`bank:false` throws), PC7 (`pic:40`), PR11 (line 2 hand-edited), PR12 (src twice), PN1 (an answer stamp edited -> node), PR13 (a bank word printed in the sentence), PS1 (rows fixed at 74: "the lanes end 62 px above the body bottom"), PS1b (the grid floating 190 px under the bank).
- **PNG:** `scripts/worksheet-gen/out/dev/G1-371-null-d2-en.png`. Seen: eight cream lanes filling the body under the three-pill bank; every name over a portrait of its sex (Ben + Leo = doctor + coach, Max + Ava = judge + ballerina, Mia + Zoe = flight attendant + waitress); the box opens line 2; nothing clipped, nothing under the footer.

### F2 : G2-353 `Who Is He? Pronouns in Sentences` (G2, CODE `layout:'anaphora'`)
- **Knob:** `{layout:'anaphora', pairs:5, sentencesPerPair:2, pic:44, plateFont:16, zone:70, namesW:163, introMax:42, introMaxTwo:33, sentenceMax:36, cardMin:116, cardGap:10}`.
- **The child:** reads the intro ("Lily and Tom are in the kitchen."), then DRAWS a line from each boxed pronoun (`She bakes a cake.` / `He makes a sandwich.`) across the empty 70 px zone to the name plate it refers to. Non-fi: one f + one m per block, both sentences `sg`, a random referent order and a random sentence -> referent assignment; fi (2 chips): one name + one PAIR per block, the `sg` sentence -> the single, the `pl` -> the pair (`Hän` / `He`), 15 portraits.
- **Layout:** a `flex:1` grid `repeat(5, minmax(116px, 1fr))` gap 10 of `anaphoraBlock`s: stack **620 at 677**, the blocks stretch (body 766 -> 145; 710 -> 134; 677 -> 127; blank inside a block <= 20).
- **verify():** 5 blocks, in [4, 6]; two referents a + b with different keys, a plate carrying the stamped names, a target dot, portraits === the key >= 36; two sentences with different pronoun literals (the tile prints its literal), refs a + b once each, an anchor dot, no name in a sentence; the intro carries both names, one line; the zone EMPTY and no `svg line/path` (no line pre-drawn); no src / name twice; SPARSE as F1. **Node:** every referent key re-derived; the pronoun === `initial[map.anaphora[key of its ref]]`; every sentence rest and the filled intro are bank literals.
- **Poisons owned:** PC4 (`pairs:3`), PR2 (two "He" on one block -> "no single solution"), PR5 (a `chef` at 44 -> "below its minPx 56"), PR14 (a name inside a pronoun sentence), PR15 (a line pre-drawn in the zone), PS2 (blocks fixed at 116: "146 px above the body bottom").
- **PNG:** `scripts/worksheet-gen/out/dev/G2-353-null-d2-en.png`. Seen: five blocks; each with two portrait + plate + dot rows on the left, an empty zone, the intro and two boxed-pronoun rows with dots on the right; the pair per block always one girl + one boy; no line drawn; content centred in each block with ~20 px of air.

### F3 : G2-354 `Possessive Pronouns` (G2; en grade-1 landing, CODE `layout:'possessive'`)
- **Knob:** `{layout:'possessive', rows:8, singles:5, pairs:3, pairMix:'mp,fp,xp', four:{singles:4, pairs:4, pairMix:'mp,fp,fp,xp'}, ending:{singles:8, pairs:0}, ownerPx:44, thingPx:44, chipH:44, chipFont:20, mixFloor:2, thingGenderMin:3, maxLine:43, laneMin:83, laneGap:6}`; the chip width is the bank's `possessive.chipW`. The class comes off the possessive block: 4 chips without `frame.pl` = `ownerEnding` (de: `ending`, single owners); 4 chips with `byOwner` = `ownerFour` (pt: `four`); 3 chips with `forms` = `thingAgree` (fr / it: >= 3 f and >= 3 m/n things on the page); else `owner`.
- **The child:** looks at who owns the pictured thing ("This is Mia. This is ___ ball." / "These are Ben and Max. This is ___ hat.") and CIRCLES `his / her / their` (left-aligned identical pills). `{art}` from `artTable` (it / pt) is printed before the gap where the bank carries it.
- **Layout:** a `flex:1` grid `repeat(8, minmax(83px, 1fr))` gap 6 of `ownerLane`s: stack **706 at 710** (body 799 -> 95; 710 -> 84). Does NOT fit a four-line title (asserted: at 677 it fails), so rule 13 now caps the F3 title at 3 lines in every locale (F3 has no fi).
- **verify():** 8 lanes; chips === the count, one correct === chip-key, identical order + style on every lane (position leak), >= 44 high, the label inside its pill; one EMPTY box; no chip word in the frame; the frame holds every name, one line <= 28; owners === the key >= 36, one thing picture >= 36, no thing twice; every chip >= mixFloor, >= 2 keys; `ownerEnding`: single owners, 4 chips, no de article before the box; SPARSE as F1. **Node:** key re-derived; `possForm(key, thing)` === the chip; thing src === `fileUri`; the frame text === the bank frame filled with names + `displayWord` + `{art}`; every rendered chip === `chipW` wide with its text inside.
- **Poisons owned:** PC6 (`maxLine:60`), PC8 (a de config with pair owners throws), PB1-3 (`chipW` 40 / 300 / missing -> rule 6), PR6 (chips reordered), PR16 (a chip word in the frame), PN3 (an owner key edited), PR3 (de "Das ist der ___ Ball."), PS3 (lanes fixed at 83).
- **PNG:** `scripts/worksheet-gen/out/dev/G2-354-null-d2-en.png`. Seen: eight lanes; owner portrait(s) + the thing at 44 on the left, the frame with one dashed box and three white pills below; pairs = two owners (Lily + Emma = waitress + ballerina; Mia + Leo = athlete + pharmacist); every thing the thing it names (soccer ball, kite, drum, book, hat, dog, robot, train).

### F4 : G1-372 `Sort the Names: He, She or They` (G1, CODE `layout:'sort'`)
- **Knob:** `{layout:'sort', cards:10, singles:8, pairs:2, pairMix:'any', four:{singles:7, pairs:3, pairMix:'mp,fp,xp', maxPerSex:4}, two:{singles:7, pairs:3, pairMix:'mp,fp,xp'}, obj:{singles:6, pairs:0}, objects:4, neuterMin:2, pic:56, pairPic:44, cardW:118, cardH:92, capFont:16, binH:395, gridBinH:185, lineMin:4, lineGap:58}`; the title lists exactly the d2 chips (asserted against `bank.chips`; the "He, She or It" poison fails the title-lists-config rule).
- **The child:** reads ten name cards on the shelf (portrait + name; pairs as two portraits + "Lily and Zoe"; de/nl four captioned objects) and WRITES every name under its pronoun in the ruled bins (3 in a row; fi 2; fr/es/pt 4 in a 2 x 2 grid, since four bins in a row are 148 wide).
- **Layout:** shelf 218 + 18 + head 40 + 6 + bins: **677 with 3 / 2 bins (binH 395, 7 lines at gapY 51)**; the 2 x 2 grid **710 (gridBinH 185, 4 lines at 38)**. Fixed stack, top-anchored; slack under it 101 at the default en chrome (<= 180).
- **verify():** cards === the stamp, in [6, 12], each with a caption === its sortword (<= 37 high, >= 16, never a bin head, holding its names), portraits === the key >= 44, inside the shelf; bins === chips in order 0..n-1, distinct heads, `grid` iff 4 bins, every bin >= 175 wide, lines >= its load (pairs x 2), gapY >= 34, drawn lines === the stamp, no card pre-placed; >= 2 keys, >= 2 bins used; SPARSE: stack >= 660, slack <= 180, the bins inside the body, the last line within 64 px of the bin bottom. **Node:** every card key re-derived, bin === `map.sort[key]` (objects via `objectMap`), captions === the names joined / `displayWord`, heads === `chips`, 0 cards dropped.
- **Poisons owned:** PC5 (`lineGap:20`), PC9 (the design's 6 + 2 split: 8 cards for ten), PN4 (a card key edited), PR17 (a card pre-placed), PR18 (a head "it"), PR9 (a 4-bin page declared a row), PS4 (the design's 330 bins: "stack 612 < 660"), PS4b (4 lines in a 395 bin: "the last line sits 188 px above the bin bottom").
- **PNG:** `scripts/worksheet-gen/out/dev/G1-372-null-d2-en.png`. Seen: two rows of five cards (Lily and Zoe, Ava and Ben as pairs), three heads `he / she / they` over three dashed bins whose seven ruled lines fill them; ~100 px of paper under the bins; nothing clipped.

### F5 : G2-355 `Rewrite the Sentence with a Pronoun` (G2, + L.1.1.j, CODE `layout:'rewrite'`)
- **Knob:** `{layout:'rewrite', rows:8, ruling:true, bank:false, glyphH:24, rowH:48, w:535, pic:56, pairPic:44, fontPx:18, singles:5, pairs:3, pairMix:'mp,fp,xp', four:{singles:5, pairs:3, pairMix:'mp,fp,xp'}, maxLine:56, maxAnswer:28, rowMin:77, rowGap:7}`.
- **The child:** reads the printed sentence ("Jack and Anna jump rope.") and WRITES the whole sentence on the 535 x 48 school-line ruling with the pronoun in place of the name(s): no bank, no chips, retrieval + handwriting. Frames filtered per row by printed <= 56 and answer <= 28 glyphs (`need <= 535`).
- **Layout:** a `flex:1` grid `repeat(8, minmax(77px, 1fr))` gap 7 of `rewriteLane` rows (no lane chrome, by design): stack **668 at 677** (body 766 -> 90; 710 -> 83; 677 -> 79).
- **verify():** 8 rows; a sentence holding every name, one line <= 26; the answer stamped, its pronoun NOT in the sentence, the answer sentence printed nowhere; one EMPTY `[data-lcs-ruling-row]` with a `writing-row` svg of >= 3 rules, no `<text>`, no `[data-lcs-starter]` on the page, no bank word / chip on the page; svg width >= `need(answer)` and >= w, height >= glyphH + 20, glyphH >= 24; portraits === the key >= 36; both numbers, >= 2 pronouns; SPARSE as F1 (the ink of the last row within 3 px of the body bottom; the rows stretch). **Node:** key re-derived; the sentence === the frame filled; the answer === the literal with `initial[map.rewrite[key]]`; the row >= `need`.
- **Poisons owned:** PC2 (`bank:true`), PC3 (`glyphH:20`), PR4 (a starter on the row), PR4b (a bank banner injected), PR19 (the pronoun printed in the sentence), PN5 (an answer stamp edited), PS5 (rows fixed at 77), PR20 (the row narrowed to 300 < need 304).
- **PNG:** `scripts/worksheet-gen/out/dev/G2-355-null-d2-en.png`. Seen: eight rows, each a portrait (or two) beside a bold sentence over a three-line ruling; the rulings empty; ~16 px of paper between rows' ink; nothing under the footer.

## Gate (`node qa/verify-b4-pronouns.js`, 80 s)

```
render G1-371 replace en: verify 0 lints 0 body 766 stack 766 lanes 82 x8 · 3+3 chrome body 710 stack 710 lanes 75 x8 · 677: FAILS as designed (ink 948 under the footer 921)
render G2-353 anaphora en: verify 0 lints 0 body 766 stack 766 lanes 145 x5 · 3+3 710 lanes 134 · 677 verify 0 lints 0 lanes 127
render G2-354 possessive en: verify 0 lints 0 body 799 stack 799 lanes 95 x8 · 3+3 710 lanes 84 · 677: FAILS as designed (ink 950)
render G1-372 sort en: verify 0 lints 0 body 778 stack 677 bins 7@51 x3 row · 3+3 710 stack 677 · 677 verify 0 lints 0 stack 677
render G2-355 rewrite en: verify 0 lints 0 body 766 stack 766 lanes 90 x8 · 3+3 710 lanes 83 · 677 verify 0 lints 0 lanes 79
render synthetic de replace / possessive / sort / rewrite · fr replace / possessive / sort (grid at 710) · nl sort / possessive · fi anaphora / sort / replace / rewrite: verify 0 lints 0 cross 0
es / fi possessive: REFUSED (a throw, never a page)
sweep x5 faces: 20 seeds, 20 distinct sets, 20 distinct orders; replace bank order != chip order on 20; anaphora 2 assignment variants; possessive 19 thing sets; sort 0 cards dropped
verify-b4-pronouns: 1589 assertions, 0 failures in 80s
PASS (1589 assertions, 77/77 poisons killed)
```
`--quick`: `PASS (227 assertions, 77/77 poisons killed, --quick: sweeps + synthetic-locale face renders skipped)`.

**Distinctness:** `node tools/gate-variation-distinct.js --batch=b4 --diffs=2 --family=pronouns` -> `[b4:pronouns] compared 15 pairs over 5 faces against their bases + pairwise within family / every variation differs from the deck its base publishes and from its siblings`.
**Baseline:** `node tools/b3-baseline.js --check --quick` -> `checked build 3620 + enum 255 in 22s: 0 drifted (0 expected), 0 missing / PASS`.
**build-en:** `653 types -> i18n/strings.en.json (title lint clean)`.
**Starters:** `node qa/verify-ruling-starters.js --quick --types=G1-371,G2-353,G2-354,G1-372,G2-355` discovers 0 starter coordinates ("nothing discovered"): none of the five faces prints text on a school line (F5's ruling is EMPTY by design; its sentence sits in a `<p>` above the row), so no face id joins that gate's MUST_HAVE.

## Pictures OPENED

`out/dev/G1-352-faces-pictures.png`: the 32 portraits + the 12 possessive things at 96 AND 44 px, read: every one is what its filename says and shows its tag at 44 (the 56-class full figures read at 44 only as silhouettes and are drawn only where the pool filter allows: F4 / F5 singles at 56; F1 52/48, F2 44, F3 44, pairs 44 = the `minPx 44` class). The excluded portraits (actor firefighter paramedic veterinarian baby_girl …) are absent from the bank (rule 1) and the faces draw only from `bank.people`; every referenced picture exists on disk (rule 1 + rule 6 `fileExists(fileUri(theme, noun))`, resolving to `cache/themes-512/<theme>/<noun>@3x.webp`, the substrate's picture path). Every face's five d2 renders were read.

## Deviations from §3 (each measured or ruled)

1. **The chrome is 710, not 722** (the base record's deviation 1): F1 704 and F3 706 still fit; F4 was re-budgeted (below); F2 620 and F5 668 unchanged.
2. **F4 is ten cards from 8 + 2 singles / pairs, not the design's 6 + 2.** The design's split makes 8 cards while its shelf, its title and its "ten name cards" say 10; the name budget (12 = all) allows 8 + 2; the four-chip class 7 + 3 (the 13 names of the reviewer's ruling) and fi 7 + 3 (16); de/nl 6 + 4 objects. PC9 asserts the 6 + 2 split refuses.
3. **F4 bins fill the page (SPARSE rule): binH 395 row / 185 grid, lines fill the bin.** The design's binH 330 + `max(4, load)` lines left 112 px of blank paper at 710 and a half-empty bin; the nt10-D rule sizes the bins to the 677 budget (3 / 2 bins) and the 710 budget (the 2 x 2 grid) with `lineCount = max(lineMin, load, round((binH - 10) / 58))`; PS4 (the design's 330) and PS4b (4 lines in a 395 bin) are the poisons. The `.ws-pill` head measures **40, not the design's 26** (218 + 18 + 46 + 395 = 677; grid 218 + 18 + 2 x 231 + 12 = 710), which is why 409 / 199 became 395 / 185.
4. **The four-chip F4 caps the singles' split at 4 per sex** (`four.maxPerSex:4`): 7 singles split 2 / 5 would load a 185 bin with 5 lines (gapY 31 < 34) and force the re-deal / drop path every time; at <= 4 per sex every bin holds <= 4 lines at gapY 38.
5. **The F1 / F5 mix floor runs over the chips the face can REACH**, not the whole table: de/nl `es` / `het` is never a person's answer on those faces (the base record's synthetic de block refused every deal until the floor was scoped).
6. **`possForm` reads a thing's `forms` by `{m1, f1, p}`**: every pair key (mp / fp / xp) collapses to `p` (§5's `forms:{m1, f1, p}` shape); `byOwner` keeps the five keys.
7. **The F3 chip width is a bank literal (`possessive.chipW`)**, not computed: the design's per-locale widths are measured numbers the row cannot know; rule 6 bounds it and the gate measures every rendered label inside its pill.
8. **F5 keeps the design's "no lane chrome"**: a lane shell (8 px) would make 8 x 85.4 + 49 = 732 > 710; the rows stretch in a `flex:1` grid instead and the gate measures the blank between rows' ink (16 at the en chrome, 6 at 710).
9. **The 677 fixture for the faces is a four-line title of ORDINARY words** (`CHROME.fourClean`): the base's `four` fixture carries a 44-char Finnish word that overflows the name/date band (a fixture artefact the base's PR8 tolerated). And **the fr grid fixture is a 3-line title under fr chrome** (`CHROME.threeFr`): the fr name/date band is wider, so the en 73-char fixture wraps to four lines there and the body drops to 677.
10. **Rule 2 is the reviewer's FLOOR** (>= 6 / >= 6, fi >= 8 / >= 8, <= 16): the fr/es/pt F4 deal (7 + 3) needs 13 names; the synthetic fr block carries `Léa`.
11. **Rule 13 covers the base + F1 + F3 titles in every locale** (measured lines <= 3): none of the three fits a four-line stack, and the design capped only fi.

## Per-locale refusals visible from the bank shape (Phase 4 `hub-expectations.json`)

- **es F3 `possessive` REFUSED** (`su` invariant; `refuse.possessive:true`, `possessive:null`): the type throws "refuses the possessive face", never a page -> es 5 rows.
- **fi F3 `possessive` REFUSED** (`hänen` / `heidän` + suffix; the panel's ruling): same mechanism -> fi 5 rows. Total 64 (README matrix confirmed).
- fr / es / pt: base + F4 need **13 names (7 f + 6 m)**; the composer refuses honestly at 12 (measured in the gate before the synthetic fr block gained its 13th).
- fr F3: vowel / h-initial f things refused per THING (rule 6; "son amie"), the page needs >= 3 f and >= 3 m/n consonant-initial things (`thingGenderMin`); pt F3 needs `singles:4, pairs:4, pairMix:'mp,fp,fp,xp'` (the base's `four`, reused); de F3 single owners only (`ending`), 3. Klasse at the landing.
- The base (710), F1 (704) and F3 (706) never fit a four-line title in ANY locale: rule 13 measures those three titles per locale (a title rewrite, never a row loss).

## Open items for the panels

1. `possessive.chipW` per locale is a MEASURED number (Baloo 2 700 20, widest label + 18): nl 60, pt 66, de 66, fr / it 60, sv / da / no 84 per §3; the gate measures every rendered label against it, so a wider panel label fails loudly.
2. The fi F2 intro cap is `introMaxTwo:33` (the pair plate "Väinö ja Helmi" takes the column); the fi panel writes intros <= 33 filled.
3. de / nl F4 object captions print the bare noun (`objectCaption 'bare'`, `displayWord`); the `obj` split 6 + 4 with `neuterMin 2`.
4. fr / es / pt F4 in the 2 x 2 grid: a G1 hand at glyph 26 in a 308 x 185 bin with 4 lines at 38 (the design's 200 became 185 under the measured 40 px head); a locale whose four bin heads exceed one line at 20 px would need a shorter head, never a smaller bin.
5. The it panel's egli/ella vs lui/lei choice reaches F1 / F2 / F4 / F5 through `initial` / `chips` unchanged.
