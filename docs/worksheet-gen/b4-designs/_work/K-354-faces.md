# K-354 `human-body` — the FIVE variation faces (Phase 2, 2026-09-21)

Built from `K-354-human-body.md` §3 (+ §1/§4/§5 for the rulings, data and gates) under `_FACE-BRIEF.md`, on the base substrate of `_work/K-354-build.md` (its "open items for the faces" were the five `layout` branches, the five components, the §5 face poisons PR2-PR7 / PR11 / PR13, and the reviewer's note that the base stage sat centred with ~100 px above it — every face is top-anchored or body-filling). Ids from `_records/b4var-id-allocation.json`. Nothing shared was edited (`git status`: only this family's files); nothing committed; the baseline was NOT re-captured. Every number below is measured (node against the real files, or puppeteer through `render/render-instance.js` with the shell's `file://` fonts).

## Files (all family-scoped)
| file | what |
|---|---|
| `scripts/worksheet-gen/tools/b4var-rows/human-body.js` | NEW — 5 rows (K-360 K-361 G1-356 K-362 K-363), `HANDWRITTEN` empty; the only file the emitter reads |
| `scripts/worksheet-gen/types/k/K-354-human-body.js` | the additive `layout` knob: `_buildWith` hands a config carrying `layout` to `_buildFace` (the base's three configs do not → the base path is byte-identical; baseline PASS); `_buildCount / _buildColor / _buildWrite / _buildMissing / _buildPairs`, each guarding on the RESOLVED config; `verify()` reads the root stamp `data-lcs-layout` first and hands a face to `_verifyFace` (node loads the locale bank for the literal cross-check + the F2 / F4 region floors from the primitive's table; the browser half `faceVerify` re-derives everything from the stamps + geometry; the base's evaluate is untouched). The export now hoists the spec's own keys onto the `withFixedTheme` wrapper (the K-356 precedent: `{...base}` in an emitted face copies OWN keys only, and the wrapper owned just `themeAxis` + `build`) — same functions, output unchanged |
| `scripts/worksheet-gen/templates/components-b4/human-body.js` | the base's exports byte-untouched; NEW `bodyCountCard · bodyColorLegend · bodyArrowGlyph · bodySpellRow · bodyMissingCard · bodyPairCard` |
| `scripts/worksheet-gen/types/k/K-36{0,1,2,3}-human-body-*.js` · `types/g1/G1-356-human-body-write-the-word.js` | emitted by `node tools/gen-b4var-specs.js` (F3 carries `gradeBand: "G1"`) |
| `scripts/worksheet-gen/data/b4/human-body.js` · `body-facts.json` | the EN bank UNCHANGED (it already carried every literal the faces print: `factLabels` ×18, `plural` ×16, `partWords` ×16, the six `strings`). `body-facts.json` (gitignored, force-add): `parts.neck.cue: null`, `pairsPool.singles` = `[head, nose, mouth]`, `excluded.neck` reworded — the review ruling (2026-09-21) extends the design's chip exclusion of the necklace picture to EVERY face. `validateFacts` grew: singles ≥ 3 / pairs ≥ 5 (the shipped F5 config), no `excluded` id in any picture pool, no cue on an `excluded` part; `validateBank` rule 6 covers the face pools (count 2/3/1, color ≥ 5, missing ≥ 4, pairs 5 + 3 minus `refuseWords.<face>`, else `refuse` must name the face) |
| `scripts/worksheet-gen/primitives/body-figure.js` · `qa/verify-body-figure.js` | `MAX_H` 560 → 680 (additive: no consumer drew above 560; the F2 face draws 660 to fill the 677 fi body); the primitive gate asserts h 660 / 680 in its table and the throw at 681 |
| `scripts/worksheet-gen/qa/verify-b4-human-body.js` | the gate grows: sections 6-7 (5 faces × d2 en + the 722 de + 677 fi chrome through the real pipeline, the design's floors re-asserted from the render, the literal cross-check, the SPARSE assertions, the fi widest legend row, 8 config-guard refusals, every chip / cue picture on disk in the render cache AND as `body parts/<noun>@2x.webp` under `frontend/public/image-library-webp/themes`, a 20-seed sweep per face + the fr `refuseWords.write:['eye']` sweep) and 17 face poisons |
| `scripts/worksheet-gen/i18n/strings.en.json` | regenerated by `node i18n/build-en.js` (628 types, title lint clean); the diff also carries the sibling families' faces present in the tree |
| `docs/worksheet-gen/b4-designs/_work/K-354-faces.md` | this record |

## The five faces

### K-360 — Parts of the Body: How Many? — CODE `layout:'count'` (F1, K)
- **Row:** `{...base.d2, layout:'count', cards:6, cols:2, rows:3, mix:{1:2, 2:3, 10:1}, pic:72, box:56, wordPx:18, figureH:540}`.
- **Child:** RECALLS a body fact and WRITES a numeral: six numbered `.ws-card`s, each `[cue picture 72][6][the locale's whole "How many X?" literal, Nunito 800 18, 2-line reserve][8][blankNumeralBox 68 × 56]`; the cream 540 px figure stands beside the grid as the referent (no rings, no markers). Count 1 = 2 of `{head, nose, mouth}`, count 2 = 3 of `{eye, ear, arm, hand, leg, foot, knee, elbow}`, count 10 = `finger` (the fist with one raised finger — never reveals the count); `toe` (five toes printed), `neck` (a necklace), `hair` (uncountable) and `shoulder` (rejected picture) never. Cue pictures pairwise distinct; never a (5,10) pair of one part; cards shuffled.
- **Layout (measured):** figure 289.3 wide + 12 + grid `flex:1` (cards 181 wide, inner 153); rows `minmax(214px,1fr)` so the grid FILLS the body — en chrome (2-line title, body 766) cards 247 high; de 722-chrome 710 → 229; fi 677 → 218 ≥ 214. `heads`/`How many mouths?` wrap to 2 lines inside the 44 px reserve (measured: `scrollHeight` 45 / line-height 22 → 2 lines); `How many eyes?` one line.
- **Stamps:** root `data-lcs-layout="count" data-lcs-cards/cols/rows/mix/pic/box/figure-h/card-min/word-px`; card `data-lcs-fact="<id>" data-lcs-count="<n>"` (a fact of the child's body, never printed); `img[data-lcs-cue]`; the box `data-lcs-answer=""`.
- **verify():** cards === stamp with badges 1..n; `count === BODY_FACTS[id].count` (or the 5-fact); the multiset of counts === `mix`, ≥ 3 distinct; one complete `img` per card ≥ 64 whose file stem === the cue; cue stems distinct; parts distinct; label text === `factLabels[id]` (the node cross-check hands the bank in), unclipped, ≤ 2 lines, ≥ 18 px; box ≥ 68 × 56, empty; no bank; digits ONLY inside `.ws-card-badge`; the figure === 540, 12 regions, nothing hidden; **SPARSE:** the first card at the body top, the last card at the body bottom (±1.5 px — measured on the CARDS, never the grid box), every card ≥ `card-min` (214).
- **Poisons owned:** PR4 (the second card's cue picture replaced by the first card's → `cue "mouth" shown twice` + the gate's "6 cue pictures, 5 distinct"), PS1 (rows fixed at 214 + `align-self:flex-start` → "the count cards end 100 px above the body bottom (does not fill) — sparse"), P14 (`refuseWords.count:['head','nose']` → rule 6 "F1 count pools 1/8/1 < 2/3/1").
- **PNG:** `scripts/worksheet-gen/out/dev/K-360-null-d2-en.png` (gate: `K-360-gate-d2-en{,-longchrome-de,-longchrome-fi}.png`). Read: mouth / finger / nose / eye / leg / arm with empty boxes, the figure centred beside three rows of cards that run from the instruction to the footer line; nothing clipped, nothing under the footer; the de and fi chrome renders the same page 56 / 89 px shorter with the cards still ≥ 214.

### K-361 — Parts of the Body: Color by Legend — CODE `layout:'color'` (F2, K)
- **Row:** `{...base.d2, layout:'color', legend:5, figureH:660, regionPool:['hair','head','arm','hand','leg','foot'], swatch:36, wordPx:22}`.
- **Child:** READS a legend (swatch + colour word + arrow glyph + part word, no sentence, no number) and COLOURS that region of the WHITE figure. 5 of the 6 classes, 5 of the 8 `codeColors`; colour words from `COLOR_WORDS[loc]`, the part word a separate literal (never joined). The shirt and the neck are never in the legend.
- **Layout (measured, after the review ruling):** figure 353.6 × 660 (white) + 12 + legend column 309 × 660 = 674.6 ≤ 675; the five rows SHARE the column (`flex:1 1 0`, gap 12 → 122.4 px each, min 122), swatch 36, words Nunito 800 22, the text block wraps to a second line inside a tall row when a locale needs it. The stage is TOP-ANCHORED and FILLS the body: slack under it en (2-line title, body 766) 106 · de 722-chrome 710 → 50 · fi 677 → 17 — all ≤ 180. Widest fi row measured through a synthetic fi block: `vaaleanpunainen` / `→ käsivarret` wraps to two lines (text 57 px high) inside a 212 px row (a 3-row control), overflow false.
- **Stamps:** root `data-lcs-layout="color" data-lcs-legend/figure-h/swatch/word-px/row-h/row-w/region-pool`; row `data-lcs-legend="<id>" data-lcs-color="<key>"` (+ `[data-lcs-swatch]`, `[data-lcs-colorword]`, `[data-lcs-partword]`, `[data-lcs-arrow]`).
- **verify():** rows === `legend`, ids and colours distinct, ids ⊂ the region pool; swatch fill === `SWATCH[key]` and ≥ 36 px; colour word === `COLOR_WORDS[loc][key]`; part word === `plural[id]` for a count-2 class / `partWords[id]` for head and hair; nothing clipped or overflowing, the text block inside its row, words ≥ 22 px, rows ≥ the shared minimum (122); every legend id maps to ≥ 1 drawn `[data-lcs-region]` (both `-L/-R` for a bilateral class); the figure is `fill="white"`, every stroked region shape fills `#FFFFFF`, 12 regions, no rings / markers / `<img>` / digits; node: every legend region's narrowest drawn width ≥ 24 px at h 660 (forearm 30.6); **SPARSE:** the stage top within 1 px of the body top, stage height ≥ the figure AND ≥ 630, ≤ 180 px of paper under the stage, the legend rows equal in height and spanning the column top to bottom. The spec REFUSES `figureH < 630` (the design's 540 throws "would float over blank paper (sparse)"; `sparseSeam:true` is the gate's poison seam, never a shipped config).
- **Poisons owned:** PR6 (`leg-L` pre-filled `#2E6DA4` → "region leg-L is pre-filled #2E6DA4 (not white)"), PS2 (the stage given `margin:auto 0` → floats to the middle → "sits 53 px under the body top (not top-anchored) — sparse"), PX2 (the design's 540 build through the seam → "stage 540 < 630 — sparse" + "226 px of paper under the colouring stage (> 180) — sparse", in verify AND the gate's floor; the config guard fires without the seam; `swatch:26` throws).
- **PNG:** `out/dev/K-361-null-d2-en.png` (gate: `K-361-gate-d2-en{,-longchrome-de,-longchrome-fi}.png`, `K-361-gate-d2-fi-widest-legend.png`). Read (en): `green → legs · pink → hair · red → head · yellow → feet · blue → hands` in five tall white rows that span the left column, 36 px swatches, 22 px words, beside a white line-art child 660 tall whose toes end 106 px above the footer line; nothing clipped. Read (de 710 chrome): the same page with the stage 50 px above the footer; (fi 677 chrome): the stage ends 17 px above the footer line, rows 118 tall; the fi widest-row control shows `vaaleanpunainen` over `→ käsivarret` on two lines inside its row.

### G1-356 — Parts of the Body: Write the Word — CODE `layout:'write'` (F3, **G1**)
- **Row:** `{...base.d2, layout:'write', labels:6, faceMax:2, leadMin:4, boxes:true, box:44, gap:4, maxLetters:8, figureH:520, markerMin:30}` + `extra {gradeBand:'G1'}`.
- **Child:** RETRIEVES the name of a numbered part from the figure alone (no bank, no picture, nothing to copy) and WRITES it into letter boxes, one per letter. Six coral markers (r 13, white Baloo 700 16 numerals) on the cream figure, numbered 1..6 TOP-TO-BOTTOM; beside it six numbered blocks: the `.ws-chip` numeral (44, Baloo 26) ABOVE `letterBoxes({n, box:44, gap:4})`. The word count is the only scaffold.
- **Composer:** the eligible pool = single-token `partWords` of ≤ 8 letters minus `refuseWords.write` (en 16; the design's per-locale counts hold by the same guard: fr 15 with `eye` refused, es/pt/it `toe` multi-word, it `ginocchio` 9, fi `käsivarsi`/`kyynärpää` 9); `rng.sample` 6 under `faceMax` / `leadMin`, distinct words, a random side per bilateral id; the primitive REFUSES markers < 30 px apart and the composer redraws (bounded 40; the base's fixed set as the fallback only if every id is eligible). Measured over 20 seeds: 20 distinct pages, 0 fallbacks; with `refuseWords.write:['eye']` 0/20 pages mark the eye.
- **Layout (measured):** figure 278.6 × 520 + 12 + blocks column 382 (8 × 44 + 7 × 4 + 2) = 672.6 ≤ 675; block 94 high (44 + 4 + 46), gap 12 → stage 624; top-anchored. Slack under: en 142 · de 86 · fi 53. Markers on this seed: hair 1 / head 2 / arm 3 / elbow 4 / finger 5 / leg 6 (words 4 / 4 / 3 / 5 / 6 / 3 boxes).
- **Stamps:** root `data-lcs-layout="write" data-lcs-labels/face-max/lead-min/box/gap/max-letters/figure-h/marker-min/row-h/row-gap/chip/bank/parts/floor`; marker `[data-lcs-marker="<id>"][data-lcs-n]`; block `[data-lcs-spell="<id>"][data-lcs-n]` + `svg[data-lcs-letterboxes="<len>"]`.
- **verify():** markers === `labels` with numerals 1..n, ≥ 30 px apart (screen px), running top-to-bottom, ids === the parts stamp; blocks in numeral order, `letterboxes === [...partWords[id] minus apostrophes/hyphens].length`, every box rect ≥ 44 (the G1 floor, read from the `floor` stamp), the chip prints n and sits ABOVE its boxes, no overflow; every id in the locale's eligible pool (node computes it), words distinct, `faceMax` / `leadMin` honoured; no `<img>`, no bank unless `data-lcs-bank="1"` (d1), nothing printed but digits; **SPARSE:** stage top-anchored, height ≥ 6 × 94 + 5 × 12, figure and blocks top-anchored in it, no blank band > 13 px between blocks.
- **Poisons owned:** PR2 (a wordBank injected above the stage → "a word bank on the write face"), PR11 (config `box:40` → "letter box 40 < the G1 floor 44" AND the render floor on 40 px rects → "a letter box is 40 px < 44"), PM (markers 1 ↔ 2 swapped → "marker numerals do not run top-to-bottom"), PS3 (the root made `flex:1;justify-content:center` → "not top-anchored — sparse"), PSB (block gap 60 → "60 px of blank band above it (gap 12) — sparse"), config `faceMax:3` → the guard; a bank refusing 11 words → "eligible pool 5 < 6 labels (refuse)".
- **PNG:** `out/dev/G1-356-null-d2-en.png` (gate: `G1-356-gate-d2-en{,-longchrome-de,-longchrome-fi}.png`). Read: markers 1 on the hair, 2 on the cheek, 3 on the right upper arm, 4 on the left elbow, 5 on the right fingers, 6 on the left shin; blocks 1..6 with 4 / 4 / 3 / 5 / 6 / 3 dashed boxes; nothing else printed; the blocks end 142 px above the footer on the en page.

### K-362 — Parts of the Body: What Is Missing? — CODE `layout:'missing'` (F4, K)
- **Row:** `{...base.d2, layout:'missing', figures:4, cols:2, rows:2, figureH:300, chips:3, chip:80, pic:64, omitPool:['arm','hand','leg','foot','hair']}`.
- **Child:** DISCRIMINATES an incomplete figure: four small white figures (`cardGrid` 2 × 2, badges 1-4), each drawn with ONE class omitted — a bilateral class on one side (hiding an arm takes its hand, a leg its foot; sides alternate across the cards) or the hair; beside each a column of three equal white picture chips (80, picture 64) = the missing part + 2 distractors; the child circles the chip and draws the part on. No text on any item.
- **Composer:** 4 distinct classes of the 5 (every one measured ≥ 14 px at h 300: hair 25.7, arm 45, hand 23.6, leg 22.5, foot 22.5 `bboxMin`); distractors from the 14-chip pool under the confusable fence (`{arm hand elbow finger}`, `{leg foot knee toe}`, `{hair head}`); chips shuffled and the correct index re-rolled until it is not the same on every card. Every distractor is depicted on that figure (the `DEPICTS` map: elbow → an arm, knee → a leg, eye → the eye discs …).
- **Layout (measured):** cards 330.5 wide (inner 302.5): figure 160.7 + 16 + chip 80 = 257 centred; the grid FILLS the body (`.ws-cardgrid flex:1`): en cards 376 high, de 348, fi 331.5 ≥ 328.
- **Stamps:** root `data-lcs-layout="missing" data-lcs-figures/cols/rows/figure-h/chips/chip/pic/omit-pool`; item `data-lcs-missing="<class>" data-lcs-missing-side`; the figure `data-lcs-hidden="arm-L hand-L"` (the primitive's cascade); chip `data-lcs-chip="<id>" data-lcs-correct="1|0"`.
- **verify():** 4 items / 4 cards with badges 1-4; hidden classes distinct and in the omit pool; the figure's `data-lcs-hidden` === the class-side (+ cascade), those region elements ABSENT from the DOM (a `display:none` survivor fails), every other region and feature present, fill white; 3 distinct chips, exactly one `correct="1"` whose id === the hidden class, no distractor from the target's group, every distractor drawn on that figure, chips equal size ≥ 80 with complete pictures ≥ 64 whose stem === the id, white, no text; the correct index varies; digits only in badges; node: every hidden class ≥ 14 px at the rendered h; **SPARSE:** the cards span the body top to bottom, each ≥ 328.
- **Poisons owned:** PR3 (a distractor rewritten into the target's group — `hand` beside an arm-less figure → "a distractor from the target's confusable group"), PR7 (config `omitPool += 'nose'` → "omitPool "nose" is not measured >= 14 px at h 300"), PR13 (`<g data-lcs-region="arm-L" style="display:none">` injected into a figure stamped `hidden="arm-L hand-L"` → "hidden region arm-L is present in the DOM"), PS4 (rows fixed at 328 → "the cards end 96 px above the body bottom (does not fill) — sparse").
- **PNG:** `out/dev/K-362-null-d2-en.png` (gate: `K-362-gate-d2-en{,-longchrome-de,-longchrome-fi}.png`). Read: card 1 no left arm (nose / knee / ARM), card 2 no right leg (elbow / ear / LEG), card 3 no hair (HAIR / elbow / eye), card 4 no left foot (ear / nose / FOOT) — each figure visibly lacks its part, every chip the picture its id names, cards fill to the footer line.

### K-363 — Parts of the Body: Which Come in Twos? — CODE `layout:'pairs'` (F5, K)
- **Row:** `{...base.d2, layout:'pairs', cards:8, cols:2, rows:4, pairs:5, singles:3, pic:88, wordPx:18, figureH:440}`.
- **Child:** CLASSIFIES by a body fact: circles every card whose part the body has two of. Eight `.ws-card`s (no badges) each printing ONE picture over the SINGULAR word (a plural would print the answer); the cream 440 px figure is the referent. Pairs = 5 of `{eye, ear, arm, hand, leg, foot, knee, elbow}`, singles = `{head, nose, mouth}` (exactly 3 — the review ruling excludes the `neck` PICTURE (a necklace) from every face, so the design's 4 + 4 became 5 + 3); `finger toe hair thumb chin tongue` never; `neck.cue` is null so no face can draw it. Cards shuffled until the pair flags are not all-pairs-first (or last).
- **Layout (measured):** figure 235.7 + 12 + grid 427 (cards 207, gap 13; inner 179: picture 88 above the 18 px word) = 675; rows `minmax(144px,1fr)` FILL the body: en (1-line instruction, body 778) cards 185.5 high, de 168.5, fi 160 ≥ 144.
- **Stamps:** root `data-lcs-layout="pairs" data-lcs-cards/pairs/singles/cols/rows/pic/word-px/figure-h/row-min`; card `data-lcs-pair-card="<id>" data-lcs-pair="1|0"`; `img[data-lcs-pic]`, `[data-lcs-pair-word]`.
- **verify():** 8 cards, ids distinct, `pair === (count === 2)`, pools honoured, none of the never-list, no excluded picture (shoulder / neck / thumb / chin / tongue) on any card; the word === `partWords[id]` (the node cross-check; the plural fails as "prints the plural (the answer)"), unclipped, ≥ 18 px, distinct; one complete picture ≥ 88 whose stem === the cue; no badge, no digit, no ring; the flags not sorted; the figure 440 with 12 regions, no markers; **SPARSE:** the cards span the body top to bottom, each ≥ `row-min` (144).
- **Poisons owned:** PR5 (a pair card's word replaced by its plural → "prints the plural (the answer)" / "≠ the singular"), PSO (the cards re-ordered with every pair first → "the cards are sorted by the answer (11111000)"), PS5 (rows fixed at 144 + top-aligned → "does not fill — sparse"), PN (a single card's picture swapped for the neck picture → "the excluded picture "neck" is drawn", in verify AND the gate), P15 (`pairsPool.singles += neck` → "neck sits in a picture pool but its picture is excluded").
- **PNG:** `out/dev/K-363-null-d2-en.png` (gate: `K-363-gate-d2-en{,-longchrome-de,-longchrome-fi}.png`). Read (after the ruling): foot / nose / eye / mouth / knee / elbow / hand / head — five twos and three ones interleaved, no necklace anywhere, every word singular, every picture its id, cards fill to the footer line.

## Deviations from §3, each with the measurement
1. **F3 rows are numbered BLOCKS (chip above the boxes), not the inline `[chip 44][10][boxes]` row, and the figure is 520 not 420.** As designed, an 8-letter row is 436 wide, which pins the figure at h ≤ 423 beside it; six 64 px rows + 5 × 16 make a 464 px stage, so the page carried 302 px of blank paper under the stage at the en chrome (258 de / 213 fi) — the SPARSE defect the brief forbids (rendered and read before the change). Stacking the numeral over the boxes frees the 54 px of chip + gap, the blocks column shrinks to 382, the figure grows to 520 (w 278.6; the markers spread 24 % further apart) and six 94 px blocks with a 12 px gap make a 624 stage (slack 142 / 86 / 53). The child still scans the numeral then writes beside it. The design's d3 (8 rows) does not fit this block shape at 677 (8 × 94 + 7 × 12 = 836) — d3 is not shipped and the guard refuses it.
2. **F2 prints the SINGULAR for head and hair** (`red → head`, `pink → hair`), the plural only for a class the body has two of. The design said `plural[id]` on every row; "heads" beside a one-headed figure contradicts the family's own fact (`head.count 1`, printed as "How many heads? → 1" on F1). Keyed on `BODY_FACTS`, never on the locale; verify asserts it; hair (count null) takes the singular, which in en / de is the same literal.
3. **F2 fills the body (review ruling 2026-09-21; supersedes the first build's "slack recorded"):** the figure grew 540 → 660 (`MAX_H` raised 560 → 680 in `body-figure.js`, additively), the legend column shrank 373 → 309 with the five rows sharing the 660 (122 px each, swatch 36, words 22, two-line wrap for long locales). Slack under the stage 226 → 106 en / 50 de / 17 fi (≤ 180); the spec refuses a colouring figure under 630.
4. **F1 grid takes the width the figure leaves** (cards 181 wide, inner 153) rather than the design's 175 (which left 12 px of the body unused); the label reserve and the box are the design's.
5. **F5 cards are a COLUMN (picture 88 above the word), not the design's `[pic 64][10][word]` row at 104 high.** Four 104 px rows made a 452 stage beside a 440 figure and 300+ px of slack; with rows `minmax(144px,1fr)` the grid fills the body, and a 64 px picture in a 160-190 px card floated in air — the picture grew to 88 (the K floor is 64; the design's d1 already used 72) and the word moved under it so the card is full at every chrome. The word budget is now 179 wide (was 105), so no locale's singular can clip.
6. **The base export hoists its methods onto the `withFixedTheme` wrapper** (deviation from "base byte-identical" in FILE terms, not in output: `tools/b3-baseline.js --check --quick` PASS; the K-356 family had already needed the same for its faces).
7. **F4 uses `.ws-cardgrid`'s 14 px gap** (the class), not the design's 12: 2 × 328 + 14 = 670 ≤ 677.
8. **The brief's "assert every referenced `<themeDir>/<noun>@2x.webp` exists"** is asserted against the served library `frontend/public/image-library-webp/themes/body parts/<noun>@2x.webp` (15/15 present) AND the render cache the resolver actually serves (`cache/themes-512/body parts/<noun>@3x.webp` — the cache holds `@3x` only), because the render reads the cache, not the library.
9. **The `neck` picture is out of every face (review ruling 2026-09-21).** The design's §3 F5 accepted `neck` as a CARD ("picture OPENED … the arrow does the pointing") while §5 excluded it from chips; the ruling reads the exclusion as being about the PICTURE wherever it is shown. `neck.cue` is null, `pairsPool.singles` is `[head, nose, mouth]`, F5 ships 5 pairs + 3 singles (the child still circles 8 cards), and the gate asserts no rendered chip / card picture is an `excluded` id (poisons PN + P15). The WORD `neck` stays on the base bank and on F3's markers.

## Per-locale refusals visible from the bank / facts shape (→ `hub-expectations.json`, Phase 4)
None lowers a row: the design's 66 (6 × 11) stands. Per-WORD only, all enforced by guards the gate exercises: fr `eye` on F3 (`refuseWords.write:['eye']`, the Œ ruling; pool 15 ≥ 8; the fr sweep proves 0/20 pages mark it); es / pt / it `toe` on F3 (multi-word, the single-token guard; pools 15 / 15 / 14); it `ginocchio` and fi `käsivarsi` / `kyynärpää` on F3 (9 letters > `maxLetters` 8; pools 14 / 14 — every pool ≥ `labels` 6 + `faceMax` 2). F1 never draws `toe` (five toes printed), `neck`, `hair`, `shoulder`; F5 never `finger toe hair thumb chin tongue` nor the `neck` picture (5 pairs + 3 singles from pools of 8 + 3; a panel refusing one single word on F5 would need `refuse:['pairs']` — the pool has no spare) — from the facts file, identical in all 11. Contingency unchanged: fr `œ` on the base / F5 (`document.fonts.check`, untested here — no fr block yet). The fi F4 instruction must stay ≤ 2 lines (the 677 chrome leaves 331.5 px cards, 3.5 px over the 328 minimum — a 3-line fi instruction would push the cards under 328 and the gate's card floor fires).

## Open items for the panels
- The F2 legend prints `partWords` for head / hair and `plural` for the rest — a panel whose legend convention wants the article or a different form authors it INTO the `plural` / `partWords` literal (whole literals; the code never inflects).
- F3 eligible pools shrink by locale (fi 14 without `käsivarsi` / `kyynärpää`; it 14 without `ginocchio`, `dito del piede`): the composer draws from what is left; a panel wanting those words needs a different literal, not a guard change.
- The F3 block layout (numeral above the boxes) and the F5 column card are the two shapes a panel reads differently from the design text — both are on the render.
- The fi F4 instruction is written to ≤ 2 lines (above).
- `verify-ruling-starters.js --quick` was not needed: no face prints text on a school-line row (F3 uses letter boxes; the base lanes stay empty).

## Gate output (`node qa/verify-b4-human-body.js`, full run with sweeps — after the two review fixes)
```
render K-360 (count) d2 en: verify 0 lints 0 body 766 stage 766 slack-under 0 lowest 921 vs foot 921
render K-361 (color) d2 en: verify 0 lints 0 body 766 stage 660 slack-under 106 lowest 815 vs foot 921
render G1-356 (write) d2 en: verify 0 lints 0 body 766 stage 624 slack-under 142 lowest 779 vs foot 921
render K-362 (missing) d2 en: verify 0 lints 0 body 766 stage 766 slack-under 0 lowest 921 vs foot 921
render K-363 (pairs) d2 en: verify 0 lints 0 body 778 stage 778 slack-under 0 lowest 921 vs foot 921
(every face: d2 long chrome de body 710, fi body 677, verify 0 lints 0, lowest <= 921; K-361 lowest 871 de / 904 fi)
pictures on disk: 14 nouns (14 chips + the cues) checked in the render cache and as body parts/<noun>@2x.webp under image-library-webp/themes, 0 missing
fi widest legend row (vaaleanpunainen -> käsivarret): 309 wide x 212 high, text 57 high (two lines), overflow false, outgrows false
face sweeps: K-360 20/20, K-361 20/20, G1-356 20/20, K-362 20/20, K-363 20/20; F3 fallbacks 0; refuseWords.write [eye] 0/20
PASS (1145 assertions, 44/44 poisons killed)
```
Poisons: the base's 24 + P14 · P15 · PN · PX2 · PR2 · PR3 · PR4 · PR5 · PR6 · PR7 · PR11 (guard + floor) · PR13 · PM · PSO · PS1 · PS2 · PS3 · PS4 · PS5 · PSB — each KILLED for its own reason (the correct face renders are the controls; the verify() message AND the gate's own floor both fire where a `node` regex is listed). `qa/verify-body-figure.js` → `PASS (1891 assertions, 4/4 poisons killed)` (h 660 / 680 added to its table).

- `node tools/gate-variation-distinct.js --batch=b4 --diffs=2 --family=human-body` → `every variation differs from the deck its base publishes and from its siblings` (15 pairs over 5 faces).
- `node tools/b3-baseline.js --check --quick` → `checked build 3512 + enum 255 in 20s: 0 drifted (0 expected), 0 missing` / **PASS** (the `MAX_H` raise changes no shipped output).
- `node i18n/build-en.js` → `628 types … (title lint clean)`.
