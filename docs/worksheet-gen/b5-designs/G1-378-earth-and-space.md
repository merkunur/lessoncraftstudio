# G1-378 `earth-and-space` (G1/G2/G3) : FINAL design (editor merge, 2026-09-23)

Merged from `_work/G1-378-pedagogy.md` + `_work/G1-378-design-A.md` ("the Sky Chart") + `_work/G1-378-design-B.md` ("the Sun at the edge"); every ruling and why: `_work/G1-378-critic.md`. (m) = re-measured 2026-09-23 by read-only node (`frontend/config/topics-taxonomy.json`, `frontend/lib/seo/strand-names.ts:74`, `scripts/seo-landing/gen-b3-landings.js:98-110 LEVEL_KEYS`, `data/science/day-vs-night.json`, `types/k/K-208-day-night.js:7`, `templates/components-b3/ordinal-numbers.js:151`, `templates/components-b2.js:76,228`, `primitives/trace-path.js:681`, `primitives/water-cycle.js:122-127`, `page/page.css:406,445`) or by rendering (the moon-phase terminator recipe rasterised with `sharp`, scratch `scratchpad/G1-378-crit-moon.js`) or by OPENING the picture (the editor opened `space/{mercury venus earth mars jupiter saturn uranus neptune moon planet asteroid comet}@3x.webp` in colour AND greyscale, contact sheets `scratchpad/G1-378-crit-{colour,grey}.png`; the pedagogy agent opened all 19 `space/*` files). *est.* = the engineer measures in the real render with the shell woff2 from `file://` (`render/one.js`). No em-dashes.

**Boundary (load-bearing).** This family owns five things nobody has (m: no id under `types/`, `data/science`, `data/b3`, `data/b4` prints a moon phase, a planet name, an orbit or the Earth's rotation): (1) the three bodies Sun / Earth / Moon told apart by one-body-only FACTS (the misconception set: the Sun is a star and makes its own light, the Moon only seems to change shape); (2) the Moon's changing SHAPE as an ordered pattern, drawn by a NEW `primitives/moon-phase.js` with a hemisphere flag; (3) the four school phase NAMES; (4) day and night CAUSED by a spinning Earth facing or not facing the Sun (a model, never a sort of things); (5) the eight planets by NAME, ORDER from the Sun and SIZE CLASS. It is NOT K-208 `science-sort` "Day and Night" (m: 12 pictures into Day/Night bins, "Draw a line from each picture to the group it belongs to."; no face here sorts a picture, no title equals "Day and Night"), NOT K-211 hot/cold, NOT K-322 seasons (+K-338..341: no face names a season), NOT K-356 weather-symbols (+G3-385 water cycle: its coral sun is reused as GEOMETRY only, never its scene), NOT G1-203 `science-sequence` (library pictures in a line), NOT G1-341 read-and-check (true/false on a sentence: the base asks WHICH of three bodies a fact is true of), NOT G1-376 `plants` (this batch; it uses the opened `weather/sun` picture, this family draws its own sun). No title is the bare theme word (`axes.theme.space` slugs m: space / weltraum / espacio / espaco / espace / spazio / ruimte / rymden / rummet / verdensrommet / avaruus). **No library picture appears on any face** (§2 NOT used). **Visual signature:** flat 3 px teal line-art astronomy on white, one mid-grey (`inkSoft`) for shadow, the coral Sun always the biggest thing drawn; every page is an astronomy CHART (a table crowned by the three bodies, two night rails, an observation log, one big globe, an orbit fan, three size bins).

## 1 Identity

| field | value |
|---|---|
| id / key / bands | `G1-378` / `earth-and-space` / base **G1**; F1 **G1** (`G1-381+ TBD by the emitter`); F2 F3 **G2** (`G2-360+ TBD`); F4 F5 **G3** (`G3-392+ TBD`). `default_subject: science`, `default_age_range: 6-8`, `assetClass: geometry`, `exerciseType: earth-and-space`. `apps['earth-and-space']` + `axes['exercise-type']['earth-and-space']` ABSENT (m; 125 exercise-type keys); register with the `register-b4-taxonomy.js` shape cloned for b5 + slug/name x11 (table B). The 11 type slugs of table B grepped against every `axes.*.*.slug.<loc>` (exact + /sol, planet, mond, lune, luna, lua, maan, kuu, aurinko, stelsel/): **0 collisions** (m; the only regex hits are `mika-ei-kuulu`, `dagen-en-maanden`, `ugedage-og-maaneder`, `viikonpaivat-ja-kuukaudet`, `heinakuun-4`, all unrelated). |
| theme axis | **OFF** (`themeAxis:{applicable:false}`; landings `coordinate.theme:''`). Everything is drawn by a primitive; no `fileUri`, no `pictureFor`. **No `unitAxis`.** |
| CCSS (en, honest) | NONE on any face (science; no `educationalAlignment`); `teaches` = "Sun, Earth and Moon (science readiness)". en PROSE names NGSS: base + F1 **1-ESS1-1** (patterns of the Sun and Moon; the "Sun is a star" fact is 5-ESS1-1, the base landing says the page is an early introduction); F2 readiness beyond 1-ESS1-1 (names not required); F3 **5-ESS1-2** (above band, said); F4 F5 planets are in no K-5 NGSS DCI (above band, said). Non-EN names the national framework + the `Science` strand row literal (EXISTS, `strand-names.ts:74`, m; no new strand row). Placement per locale: table C. |
| new primitives / data | `primitives/moon-phase.js` (§2; node gate `qa/verify-moon-phase.js`) · `primitives/sky-bodies.js` (sun disc, sun edge, earth disc, full-moon disc, earth-from-the-pole, plain orbit disc) · `primitives/planets.js` (8 pattern glyphs at ONE uniform size, used ONLY on the F4 name bank) · `templates/components-b5/earth-and-space.js` behind the `templates/components-b5.js` barrel · `data/b5/earth-and-space.js` + `data/b5/locales/earth-and-space.<loc>.json` (§5) · `lib/b5-common.js bank()` (clone of `lib/b4-common.js`, which exists, m). |

**Table B**

| loc | genre head = type name (panel signs) | ASCII slug | level G1 / G2 / G3 (`LEVEL_KEYS`, m) | body names sun / earth / moon (proposal, panel signs) + hemisphere | national strand literal (= the `Science` row) |
|---|---|---|---|---|---|
| en | Sun, Moon and Planets | `sun-moon-and-planets` | `grade-1` / `grade-2` / `grade-3` | Sun / Earth / Moon · N | Science |
| de | Sonne, Mond und Planeten | `sonne-mond-und-planeten` | `1-klasse` / `2-klasse` / `3-klasse` | Sonne / Erde / Mond · N | Sachunterricht |
| es (MX) | El Sol, la Luna y los planetas | `el-sol-la-luna-y-los-planetas` | `primer-grado` / `segundo-grado` / `tercer-grado` | el Sol / la Tierra / la Luna · N | Conocimiento del Medio |
| pt (BR) | Sol, Lua e planetas | `sol-lua-e-planetas` | `1o-ano` / `2o-ano` / `3o-ano` | o Sol / a Terra / a Lua · **S** | Ciências |
| fr | Le Soleil, la Lune et les planètes | `le-soleil-la-lune-et-les-planetes` | `cp` / `ce1` / `ce2` | le Soleil / la Terre / la Lune · N | Questionner le monde |
| it | Sole, Luna e pianeti | `sole-luna-e-pianeti` | `classe-prima` / `classe-seconda` / `classe-terza` | il Sole / la Terra / la Luna · N | Scienze |
| nl | Zon, maan en planeten | `zon-maan-en-planeten` | `groep-3` / `groep-4` / `groep-5` | de zon / de aarde / de maan · N | Oriëntatie op jezelf en de wereld |
| sv | Solsystemet | `solsystemet` | `ak-1` / `ak-2` / `ak-3` | solen / jorden / månen (one definiteness) · N [NSR] | Naturorienterande ämnen |
| da | Solsystemet | `solsystemet` | `1-klasse` / `2-klasse` / `3-klasse` | solen / Jorden / månen (capitals: panel) · N [NSR] | Natur/teknologi |
| no | Solsystemet | `solsystemet` | `2-trinn` / `3-trinn` / `4-trinn` | solen / jorden / månen (bokmål) · N [NSR] | Naturfag |
| fi | Aurinkokunta | `aurinkokunta` | `1-luokka` / `2-luokka` / `3-luokka` | Aurinko / Maa / Kuu (proper nouns) · N [NSR] | Ympäristöoppi |

**Table C: where the curriculum puts each face (the landing says it; the page is built regardless).** "in" = the band of this page; "above (year)" = taught later; "ABOVE K-3" = beyond the program ceiling, stated as such.

| loc | base G1 | F1 G1 order | F2 G2 names | F3 G2 day/night | F4 F5 G3 planets |
|---|---|---|---|---|---|
| en | in (1-ESS1-1; star fact 5-ESS1-1) | in (1-ESS1-1) | names later (middle school) | ABOVE K-3 (5-ESS1-2) | ABOVE K-3 (no K-5 DCI) |
| de | above (Kl. 3/4) | above (Kl. 3-4) | above (Kl. 3-4) | above (Kl. 3/4) | in (Kl. 3-4) |
| es | above (2º-3º SEP/NEM) | above (2º) | in (2º) | in (2º-3º) | panel verifies |
| pt | above (EF03CI08, 3º ano) | ABOVE K-3 (EF05CI12, 5º ano) | ABOVE K-3 (EF05CI12) | above (EF03CI08, 3º ano) | ABOVE K-3 (5º ano) |
| fr | above (CE1-CE2, panel) | ABOVE K-3 (cycle 3) | ABOVE K-3 (cycle 3) | above (CE2) | ABOVE K-3 (cycle 3) |
| it | above (terza) | panel verifies | panel verifies | above (terza) | ABOVE K-3 (quinta) |
| nl | above (groep 4-5, kerndoel 46) | above (groep 4-5) | above (groep 4-5) | in (groep 4-5) | ABOVE K-3 (groep 6) |
| sv | in (Lgr22 NO åk 1-3) | in | in | in | in |
| da | in (Fælles Mål 1.-3. kl.) | in | in | in | in |
| no | above (LK20 after 4. trinn) | above | above | above | in (3.-4. trinn) |
| fi | in (OPS 2014, 1-2 lk) | above (2.-3. lk) | in (2.-3. lk) | in | in (3.-6. lk) |

**The rule that locks the type.** Every body name, fact, phase name, planet name and class label on every page is a whole panel literal from `earth-and-space.<loc>.json`, NEVER a vocab lookup (m by the pedagogy: vocab `sun`/`moon` are capitalised citation forms, sv/da/no `sun` = indefinite "Sol" while `earth` = definite "Jorden", fi nominative only, es/pt/fr/it lack the astronomy article), and every answer is a stamped FACT of the drawing or a bank field (fact truth vector, phase index + lit fraction + lit side, planet order index, size class, pin angle), never a word the code forms. The Moon's lit side is a LOCALE fact (`hemisphere`, `S` only in pt); the primitive mirrors on it and the gate measures it on the render.

## 2 The base page

**Concept. "The Sky Chart" (A's layout; B's "Sun biggest" discipline; the pedagogy's fact pool).** One white chart card whose three right-hand column HEADS are the bodies, drawn in one hand at honest relative size (Sun > Earth > Moon; the landing says "not to scale"), each with its name pill; eight numbered ROWS each print one short fact. The child reads a fact and ticks ONE box under the body it is true of. Names are printed once (in the heads), not 24 times; the drawing IS the answer column; the teacher checks one column pattern down the page. B's portraits + pockets (write clue numbers into 4-box pockets) and the pedagogy's three word chips per row are rejected (critic §1 row 3).

**Chrome budget (README ruling).** Body **722** with a 3-line title + 3-line instruction; every stack is checked at **677** (4-line fi title). Lane **639** at default padding (no override).

**Layout d2**
```
.es-chart[data-ws-content]  white card, border 2 creamDeep, radius 16, padding 8   (639 wide)
grid-template-columns: 30px 10px 1fr 92px 92px 92px   -> fact column 639-4-16-30-10-276 = 303
grid-template-rows:    128px repeat(8, minmax(56px, 1fr))
+----+--+--------------------------------+--------+--------+--------+
|    |  |                                |  SUN   | EARTH  |  MOON  |  head 128 = drawing box 88 (bodies bottom-aligned at y 84)
|    |  |                                |  pill  |  pill  |  pill  |            + 6 + name pill 30 + 4
+----+--+--------------------------------+--------+--------+--------+
| (1)|  | It makes its own light.        |  [  ]  |  [  ]  |  [  ]  |  row 64 (zebra white / creamDeep)
|  . |  |  ... 8 rows                    |        |        |        |  tick = .ws-blankbox 44 x 44, centred in 92 x row
| (8)|  | It is covered in craters.      |  [  ]  |  [  ]  |  [  ]  |
+----+--+--------------------------------+--------+--------+--------+
```
Width 4 (border) + 16 (padding) + 30 + 10 + 303 + 3 x 92 = **639** (m arithmetic). Height 128 + 8 x 64 + 16 + 4 = **660 <= 677 <= 722** (at the 722 case the `1fr` rows grow to 71; at the 677 case rows sit at 64.1; floor 56 gives 128 + 448 + 20 = 596). Separators: 1.5 px `grid` vertical rules between the three body columns only; zebra carries the rows. Row badge 30 px circle, teal ring 2, Baloo 2 700 17 ink numeral (a label, not an answer). Fact text Nunito 800 **18**, line-height 1.2, **max 2 lines** in 303 px (2 x 21.6 = 43 <= 56): en pool <= 44 chars = 1 line; de/fi/pt +40 % ≈ 62 chars = 2 lines *est.*; a literal that renders 3 lines FAILS the render gate and goes back to the panel. Head pills: Baloo 2 700 17, height 30, teal ring 2, white, `white-space:nowrap`, max 92; a literal wider than 88 at 17 px drops to 15 px (never wraps; gate asserts <= 92). Tick box = `blankNumeralBox({w:44, h:44, attrs:'data-lcs-tick="<body>"'})` (m: `.ws-blankbox` dashed coral 2.5, stamps `data-lcs-answer=""`).

**Head drawings** (`sky-bodies.js`, each in an 88 x 88 box, bottom of each body on y 84): `sunDisc({d:88, discR:34})` (disc Ø 59.8 px + 8 rays) · `earthDisc({d:44})` · `moonDisc({d:26})` (a FULL Moon with 3 craters; it names the body, it is not a phase face). Disc diameters **59.8 > 44 > 26** (Earth:Moon 1.69 >= 1.6); the gate asserts strictly decreasing.

**Ladder** (resolved config; guards key on THESE, never the level index):

| key | d1 | d2 (ships) | d3 |
|---|---|---|---|
| `rows` | 6 | **8** | 10 |
| `facts` per body | 2/2/2 | **>= 2 each, rng-balanced (3/3/2 in some order)** | >= 3 each |
| `sizeFacts` (`biggest`, `smallest`) | true (readable off the heads: scaffold) | **false** | false |
| `rowMin` / `tick` / `factPx` / `headBox` | 72 / 48 / 20 / 96 | **56 / 44 / 18 / 88** | 52 / 44 / 17 / 80 |

**d3 ruling:** more rows of the same move; not shipped, no copy describes it. d3 stack 128 + 10 x 52 + 20 = 668.

**Instruction (en source):** "Read each sentence. Is it about the Sun, the Earth or the Moon? Tick one box." (80)

**Composer.** Draw 8 fact ids from `FACTS` minus size facts: per-body counts a permutation of (3,3,2) (pool m by the pedagogy: sun 3 non-size, earth 5, moon 3); row order `rng.shuffle`, re-drawn until the body sequence has no run of 3, is not periodic (period 2 or 3), and row 1 is not the Sun. The seed is locale-neutral: same facts in the same rows in all 11 locales; only the literals change.

**Answer-hiding + uniqueness.** No box is ticked; heads carry only the body name (validator: a head never contains a fact word). Each fact's truth vector has exactly one `1` (FACTS, §5) and the forbidden-fact list is enforced by id AND by per-locale stems, so each row has exactly one correct box. Stamps: row `data-lcs-row=<n> data-lcs-fact=<id> data-lcs-body=<sun|earth|moon>`; tick `data-lcs-tick=<body>`; head `data-lcs-head=<body>`; the base root carries NO `data-lcs-layout` (byte-identical rule). `verify(page)` re-derives body = the single 1 in `FACTS[id].truth`; asserts 8 rows, per-body count >= 2 and in {2,3}, no run of 3, not periodic, fact text === `facts[loc][id]`, no size fact when `sizeFacts:false`, heads in order sun, earth, moon, head disc diameters strictly decreasing, every tick box empty, no `<img>` on the page.

**Reused (exact).** `blankNumeralBox({w,h,answer,attrs})` (`components-b3/ordinal-numbers.js:151`, m) base ticks + F1 · `wordBank({words,wordPx})` (`components-b2.js:228`, m) F2 F5 · `rulingBlock({rows,w,h,glyphH,gap})` (`components-b2.js:76`, m) F2 · `writingRow({w,h,glyphH,xHeight:true})` (`primitives/trace-path.js:681`, m; returns `{svg,width,height}`) F4 F5 · `.ws-achip` (`page.css:406`, m) F3 chips · `day-vs-night.json bins[].label` x11 (m; en Day/Night, de Tag/Nacht, es Día/Noche, fr Jour/Nuit, it Giorno/Notte, pt Dia/Noite, nl Dag/Nacht, sv Dag/Natt, da Dag/Nat, no Dag/Natt, fi Päivä/Yö) F3 chip words, read-only · `svgRoot el circle line label` (`primitives/_svg.js`) · the water-cycle sun RECIPE (disc r 30, 8 rays r 38 -> 54, coral, stroke 4, round caps; `water-cycle.js:122-127`, m) copied as the ratio 1 : 1.267 : 1.8, never imported · tokens cream creamDeep white teal tealSoft coral coralSoft ink inkSoft grid, `stroke.primitive 3 / grid 1.5 / accent 4` · `bank(name, loc)` via `lib/b5-common.js`.

**NOT used.** Every `space/*` library picture (opened, §6 of the critic: mercury, venus, uranus banded like gas giants, neptune grey-brown, `planet` a Saturn look-alike, `asteroid` reads as the Moon, `comet` a red smoke trail, `galaxy` and `sun` opaque squares, `sun` BLOCKED; `earth` accurate but photoreal beside flat line art; `moon` is itself a waxing gibbous lit RIGHT, wrong for pt) · `science-category-sort` / `.sci-bin` / `science-pair-match` / `.ws-match` (K-208's look) · `answerBox` (stamps `data-lcs-answer="undefined"` open) · `cardGrid` on the base (a table, not cards) · `water-cycle.js` itself · `image-vocabulary.js` / `objForms` / `articles.js` at render · `unitAxis`.

**NEW `primitives/moon-phase.js`** : `moonPhase({phase, hemisphere, d, id}) -> {svg, width, height, meta:{phase, litFraction, litSide}}`.
- viewBox `-50 -50 100 100`, disc r 46 (room for the 3 px ring). THROWS if phase is not an integer 0..7, hemisphere not in {N,S}, `d < 72`, or `phase in {1,7}` and `d < 80` (crescent lit width at the equator = 0.135 d must stay >= 10.8 px).
- θ = 45° x phase; `litFraction = (1 - cos θ)/2` = 0, .146, .5, .854, 1, .854, .5, .146; `rx = 46 |cos θ|` (32.53 at 45°/135°, 0 at 90°).
- `litSide`: N -> `right` for 1..3, `left` for 5..7; S mirrored; `none` for 0 and 4.
- Paint order: (1) `<circle r=46 fill=inkSoft data-lcs-part="dark">` (a new Moon is a grey disc: the Moon is still there); (2) the lit shape, fill **white**, no stroke, drawn for RIGHT and mirrored with `transform="scale(-1,1)"` for LEFT: phase 4 `<circle r=46>`; phase 0 omitted; quarters `M 0 -46 A 46 46 0 0 1 0 46 L 0 -46 Z`; crescent `M 0 -46 A 46 46 0 0 1 0 46 A {rx} 46 0 0 0 0 -46 Z`; gibbous the same with the last sweep flag **1**; (3) `<circle r=46 fill=none stroke=teal stroke-width=3 data-lcs-part="rim">` on top. No craters, no face, no halo, no coral.
- Root `<svg data-lcs-prim="moon-phase" data-lcs-phase data-lcs-lit="<3dp>" data-lcs-litside data-lcs-hemi role="img" aria-label="">`.
- **Measured (m, rasterised at 400 px):** lit/disc = .1452 .4983 .8511 .9952 .8509 .4982 .1452 for phases 1..7 (expected .1464 .5 .8536 1 .8536 .5 .1464; max error 0.005, all antialias), lit centroid right for 1-3 and left for 5-7 with the mirror; the arc flags are therefore correct as written. Crescent lit width 14.0 px at d 104 (12.5 inside the ring stroke), 11.9 at d 88. Greyscale: white (L 100) vs inkSoft `#8A8276` (L ≈ 55) vs teal ring (L ≈ 40): three steps on a mono laser.
- **Node gate `qa/verify-moon-phase.js`:** every phase x both hemispheres at d 80 and d 120: rasterised lit/disc within ±0.03 of `litFraction`; lit centroid x on `litSide`; crescent lit width at y 0 >= 10 px; only token hexes; every throw fires. Poison: flip the crescent sweep flag (lit area becomes the gibbous, fails), draw a pt disc with `hemisphere:'N'` (centroid fails), phase 1 at d 72 (must throw).

**NEW `primitives/sky-bodies.js`** (unit viewBox `-50 -50 100 100` unless stated; tokens only; no text inside any drawing):
1. `sunDisc({d, discR = 27.8})`: circle r `discR` fill coral; 8 rays every 45° from r `discR + 7.4` to r 50, coral, stroke 3.7 units, round caps; `data-lcs-body="sun"`. Base head uses `discR:34` (rays 41.4 -> 50).
2. `earthDisc({d})`: circle r 46 fill tealSoft stroke teal 3 (px, `vector-effect:non-scaling-stroke`); two land blobs fill teal clipped to r 44: A `M -30 -24 C -18 -34 -6 -26 -10 -14 C -14 -4 -4 4 -10 14 C -14 24 -22 30 -26 22 C -30 12 -36 -4 -30 -24 Z`, B `M 8 -30 C 22 -34 34 -22 30 -10 C 26 0 34 10 24 22 C 14 32 6 20 10 8 C 12 -4 0 -18 8 -30 Z`; one white cloud streak `M -40 4 Q -20 -2 0 6` stroke 3 round. Stylised, not a map. `data-lcs-body="earth"`. Also the F4 bank Earth glyph (scaled).
3. `moonDisc({d})`: circle r 46 fill white stroke teal 3 + craters `fill none stroke grid 3`: (-14,-12) r 9 · (16,6) r 12 · (-6,22) r 6; `data-lcs-body="moon"`. Base head only.
4. `sunEdge({side = 'left'|'right', w, h, depth})` (B): the Sun cut by the box edge. For `left`: centre `(depth - R, h/2)`, `R = max(h, (h²/4 + depth²)/(2 depth)) + 20`, fill coral, no stroke; 5 rays on the visible limb at -40°, -20°, 0°, 20°, 40° from the inward normal, from `R + 10` to `R + 30`, coral 4, round caps; everything clipped to the box (`<clipPath>` rect); `right` mirrors. Stamps `data-lcs-prim="sun-edge" data-lcs-side data-lcs-r`. Min `depth` 60 (throws). F3.
5. `earthTop({r, sunDir, pins:[{angle, n}]})` (B, F3): viewBox `0 0 (2r+40) (2r+40)`, centre `C = r + 20`; disc fill **white** stroke teal 3 (white, so nothing suggests a lit half); pole dot r 5 fill teal at C; rotation arrow = arc radius 0.32 r from screen angle 200° to 340°, drawn **counter-clockwise as seen from above the North Pole** (`sweep-flag 0`), teal 3, filled 10 x 10 arrowhead tangent at the 340° end; pins: angle from the Sun direction (0° = the rim point facing the Sun; positive counter-clockwise on screen), pin centre at radius 0.80 r (latitude ≈ 37° N in this pole view: no midnight sun), halo circle r 17 stroke white 4, coral disc r 14, white Baloo 2 700 16 numeral (the water-cycle marker recipe, m `water-cycle.js:168-169` uses r 17 / 13); `<g data-lcs-pin data-lcs-n data-lcs-angle>`. THROWS if any `min | |angle| - 90 | < 40`, any two pin centres < 50 px apart, or `r < 120`. No continents, no shading.
6. `orbitDisc({d, n})` (F4): circle d, fill white, stroke teal 3, numeral `n` Baloo 2 700 18 ink centred; IDENTICAL geometry for all eight (no ring, no band, no size); `data-lcs-slot=<n>`. Min d 36.

**NEW `primitives/planets.js`** (A's recipes, re-sized to ONE uniform disc; used ONLY on the F4 bank beside the printed name): `planetGlyph({id, box = 64})`, viewBox `0 0 100 100`, **every planet disc r 30 at (50,50)** (Saturn's ring and Uranus's ring extend beyond it). Uniform, not honest, size is the RULING (critic §1 row 2): honest radii put Mercury at 13 px where its pattern vanishes, and would print the rocky/giant split beside the ORDER task. Recipes (tokens only, outline ink 2):

| id | recipe | greyscale signal |
|---|---|---|
| mercury | disc fill `grid`; 5 craters `circle fill inkSoft` r 4 at (40,40) (58,44) (46,58) (62,62) (36,56) | mid-grey, spotted |
| venus | disc fill white; 3 swirl arcs `grid` 2.5 `M 26 44 Q 50 36 74 46`, `M 24 56 Q 50 50 76 58`, `M 30 66 Q 50 62 70 68` | WHITE, soft lines |
| earth | `earthDisc` recipe scaled to r 30 | light with dark blobs |
| mars | disc fill coral; white polar cap = the top 18 % chord; 1 inkSoft ellipse rx 7 ry 4 at (56,56) | mid tone, WHITE CAP |
| jupiter | disc fill white; 4 horizontal bands `grid` 7 units high at y 34, 44, 56, 66 clipped to the disc; spot ellipse rx 7 ry 4.5 fill coral at (60,60) | light, STRIPED, spot |
| saturn | back ring half (ellipse rx 46 ry 10, rotate -12°, stroke inkSoft 4, fill none, clipped above centre), disc fill creamDeep + 2 `grid` bands, front ring half | the ONLY horizontal ring |
| uranus | disc fill tealSoft; thin VERTICAL ring ellipse rx 5 ry 40 stroke grid 2.5 | pale, plain, VERTICAL ring |
| neptune | disc fill teal; one white streak `M 36 46 Q 50 42 64 47` stroke 2.5 | DARK, plain |

`box >= 56` (throws below). Root `<svg data-lcs-planet="<id>" data-lcs-sig="<L-class>|<ring:h|v|0>|<bands>">`. **Gate (in `qa/verify-b5-earth-and-space.js`):** the 8 glyphs rasterised to greyscale must pairwise differ in at least one of: mean L by >= 8 levels, ring orientation, band count >= 2 vs 0, presence of a white cap; every disc radius equal (±0.5 px) across the 8. Poison: venus drawn with earth's fills (fails the pair test); jupiter with honest r 36 (fails uniform radius).

**NEW `templates/components-b5/earth-and-space.js`** (behind `templates/components-b5.js`; scoped inline CSS; no `page.css` edit): `skyChart({rows, heads})` · `skyHead({body, name})` · `factRow({n, id, body, text})` · `moonRail({phases, hemisphere, cue:'grow'|'shrink', d, box})` · `growCue({dir, w:120, h:40})` · `phaseCard({phase, hemisphere, d, lineW})` · `dayNightModel({sunDir, r, pins})` · `pinTable({rows:[{n}], words})` · `orbitFan({pitch, discD})` · `planetBankCard({id, name})` · `sizeBins({labels, lines:4})`. Each face root stamps `[data-ws-content]` and (faces only) `data-lcs-layout`.

**Alternatives (rejected).** B's "three portraits, eight clues" (write clue numbers into 4-box pockets): a G1 child must shuttle between a list and three pockets, a wrong number is hard to find, and 12 boxes for 8 answers invites writing a number twice. The pedagogy's key strip + three word chips per row: names printed 24 times, the key and the answers are two apparatuses, circled chips scatter across the row. A2 an orbit scene with numbered facts: a 26 px Moon cannot hold three written numerals.

**Risks -> mitigations.** Fact subjects leaking the answer through grammatical gender (Romance "el Sol" is the only masculine body, de "der Mond" the only masculine): every fact is a whole literal with a non-agreeing subject, the panel signs it, the validator bans the per-locale `leakForms` list (§5 rule 4). The Sun head reading smaller than the Earth: disc 59.8 vs 44 (m arithmetic) and the gate measures. A 3-line fact in de/fi: the render gate fails it and the panel shortens. **Palette:** cream, creamDeep, white, teal, tealSoft, coral, ink, inkSoft, grid (no codeColors). **Font floor:** smallest text 15 (fallback pills); the 9 px lint is never near. **Print check (engineer, mono laser, 100 %):** base, F1 in en AND pt, F3, F4: the Sun head is the biggest, the crescent at d 104 is a clear sliver, the new Moon a grey disc with a ring, the pt waxing crescent lit LEFT, the eight F4 glyphs tell apart in grey, a tick fits a 44 box.

## 3 Faces 2-6

All five are CODE faces on ONE additive `layout` knob (`d.layout` undefined = the base path, byte-identical; a face stamps `data-lcs-layout` only when declared, and the layout string IS the `coordinate.mode`); each adds a `verify()` branch; guards key on the config (`d.layout`, `d.phases`, `d.rails`, `d.pins`, `d.slots`, `d.bins`), never on the level index. None is PARAM: each changes the apparatus a teacher sees (a chart / two moon rails / a moon log / one big globe / an orbit fan / three size bins). Resolved d2 configs pairwise distinct (`tools/gate-variation-distinct.js` exists, m; run with `--batch=b5`). **Density (RULED exception, the G3-385 / plants precedent):** F3 carries 6 pins at G2, below `density.G23.items` [8, 16]: a model's pin count is bounded by legibility on one globe (pins >= 50 px apart, >= 40° off the terminator); element and numeral floors hold and the gate asserts 6 exactly. Every other face is inside its band's item range (base 8, F1 10, F2 8, F4 8, F5 10).

### F1 : Moon Phases in Order (G1, CODE `layout:'moon-phases-in-order'`)
**Move:** see the PATTERN: the lit part grows night after night to a full Moon, then shrinks; number the shapes in that order (1-ESS1-1, the one in-band en standard). **Layout:** two white rail cards 639 wide (border 2 creamDeep, radius 16, padding 16 18 -> inner 599): each rail = `growCue` 120 x 40 at the top-left (B: a teal-outlined wedge `M 0 20 L 120 4 L 120 36 Z` fill tealSoft stroke teal 3 that WIDENS left -> right for `grow` and is mirrored for `shrink`, with a coral 4 px arrow along its base; NO moon icon, so the first and last shapes are not printed), 12, then 5 slots x 118 (590 <= 599, `justify-content:space-between`) each holding `moonPhase({d:104})` above a centred `blankNumeralBox({w:52, h:48})` with 14 gap. Rail height 16 + 40 + 12 + 104 + 14 + 48 + 16 + 4 = **254**; two rails + gap 28 = **536 <= 677 <= 722** (m arithmetic; slack absorbed by `1fr` spacers above/between/below). G1 floors: disc 104, box 52 x 48 (numeral 26 fits). **Config d2** `{layout:'moon-phases-in-order', rails:[{cue:'grow', phases:[0,1,2,3,4]}, {cue:'shrink', phases:[4,5,6,7,0]}], d:104, box:[52,48], givenFirst:false}`; d1 `{rails:[{cue:'grow', phases:[0,2,4]}], d:112}` (one growing rail of 3); d3 `{rails:[{cue:'cycle', phases:[0..7]}], givenFirst:true}` (the 8-cycle, needs lit-side knowledge; not shipped). **Why single-answer:** within a rail `litFraction` is strictly monotone (0 < .146 < .5 < .854 < 1), so rank is unique whichever side is lit; the side is still drawn per hemisphere because a wrong-sided Moon teaches a wrong Moon. Slot order per rail `rng.shuffle`, !== sorted, !== reversed, and the two rails' box-answer sequences differ. **Verify:** per disc `data-lcs-phase` / `-lit` / `-litside`; per box `data-lcs-answer` 1..5 (hidden), boxes rendered EMPTY; rank = ascending lit in the grow rail, descending in the shrink rail; litSide === expected for (phase, `hemisphere[loc]`); rasterised lit/disc of every disc within ±0.03 of its stamp and centroid on its side (the pt rails MUST read left for waxing); no moon glyph inside `growCue`. **Refusals:** none (no words on the body; pt renders mirrored). **Instruction (en source):** "The Moon seems to grow, then shrink. In each row, write 1 to 5 under the moons in that order." (96) **Query face:** "moon phases in order" · de "Mondphasen ordnen" · es "fases de la luna en orden" · pt "fases da lua em ordem" · nl "maanfasen op volgorde" · sv "månens faser i ordning".

### F2 : Name the Moon Phases (G2, CODE `layout:'moon-phase-names'`)
**Move:** attach the four school NAMES (new Moon, first quarter, full Moon, last quarter, per locale) to their shapes; the first/last quarter pair is the hemisphere-dependent test. **Why only {0,2,4,6}:** the Germanic/Nordic school names ("zunehmender Mond", "tiltagende måne", "kasvava kuu") cover the whole waxing half; with no crescent or gibbous on the page every name fits exactly one shape. **Layout (A's log, B's instruction):** `wordBank` of the 4 names (Nunito 800 18; 59 + margin 10), then a 2 x 4 grid of `phaseCard`s, column (639 - 12)/2 = 313.5, row `minmax(132px,1fr)`, row gap 12; card = white, border 2 creamDeep, radius 14, padding 10: `[creamDeep square 104 with moonPhase({d:88}) centred][12][rulingBlock({rows:2, w:170, h:48, glyphH:24, gap:6}) = 102 high]`; card inner width 313.5 - 4 - 20 = 289.5 >= 104 + 12 + 170 = 286. Stack 69 + 4 x 150 + 3 x 12 = **705 <= 722**; at 677 rows compress to 132 (69 + 528 + 36 = **633**), card inner 112 >= 104 (m arithmetic). Two lines because de "abnehmender Halbmond" (20 chars ≈ 240 px handwritten at glyphH 24 *est.*) cannot fit one 170 line; en "first quarter" (13 ≈ 156) fits one. If the measured bank exceeds 639 it wraps to 2 rows (+44): 749 at the 722 case FAILS, so the validator measures the 4 literals at 18 px + chip padding and returns an over-wide set to the panel (budget: 4 words + 3 gaps <= 639). **Config d2** `{layout:'moon-phase-names', phases:[0,2,4,6,0,2,4,6], bank:true, d:88, lines:2, lineW:170, glyphH:24}`; d1 `{phases:[0,2,4,6], cols:2, rows:2}`; d3 `{bank:false}` (recall; not shipped). Card order `rng.shuffle`, no two identical phases adjacent in reading order (left-right, top-bottom), bank order !== the phase order of cards 1-4 and !== its reverse. **Verify:** per card `data-lcs-phase`; bank words `data-lcs-bank=<phase>` (via `wordBank`'s `vocabKey` field = the phase id, m `components-b2.js:228`); multiset === {0,2,4,6} x 2; name = `phaseNames[loc][phase]`; litSide per hemisphere and measured on the render; lines empty; bank literals === `phaseNames[loc]` and pairwise distinct. **Refusals:** none. pt: "quarto crescente" is lit LEFT; a northern-drawn pt page is the defect the gate catches. fr: never the "lune menteuse" D/C mnemonic (northern-only, false on the pt sibling). **Instruction (en source):** "Look at each moon. Write its name on the lines. Use each word in the word bank twice." (84) **Query face:** "name the moon phases" · de "Mondphasen benennen" · es "fases de la luna con nombres" · fr "nommer les phases de la lune" · fi "kuun vaiheiden nimet".

### F3 : Why Do We Have Day and Night? (G2, CODE `layout:'day-and-night-model'`)
**Move:** use the MODEL: at one moment the half of the spinning Earth that faces the Sun has day, the other half night; decide it for six numbered places on ONE drawn Earth (causal; K-208 sorts things and cannot do this). B's single globe (one focal drawing, one Sun, r 150 so pins are legible) replaces A's and the pedagogy's six small model cards (six Suns on a page; critic §1 row 7). **Layout:** model panel 639 x 380: `sunEdge({side: sunDir, w:639, h:380, depth:96})` (R = max(380, 236) + 20 = **400** > Earth 150, m arithmetic); three coral dashed light arrows (stroke 4, dash 12 8) from the Sun's limb toward the Earth, stopping 16 px short of the rim (they show where light comes from; they shade nothing); `earthTop({r:150, sunDir, pins})` centred at x = 319.5 + 48 (Sun left; mirrored for right), box 340 <= 380; gap Sun limb (x 96) to Earth rim (x 217.5) = 121 px for the arrows. Then 20, then the pin table: 2 columns x 3 rows (pins 1-3 left, 4-6 right), row = coral badge 32 (white numeral) + 10 + two `.ws-achip` 120 x 44 (the K-208 Day / Night labels, same order every row, Baloo 2 700 20) with 12 between = 294 <= 313.5; 3 x 44 + 2 x 10 = 152. Stack 380 + 20 + 152 = **552 <= 677 <= 722** (m arithmetic). G2 floors: chips 44 high, pin discs 28 (+halo 34), Earth r 150. **Config d2** `{layout:'day-and-night-model', r:150, pins:6, dayAngles:[0,25,-25,50,-50], nightAngles:[180,155,-155,130,-130], split:[3,3], sunDir:'rng', shadeNight:false}`; d1 `{pins:4, shadeNight:true}` (the night half shaded = scaffold; not shipped); d3 `{pins:6, ask:'next'}` (which pin gets sunrise next, using the rotation arrow; not shipped). Pin angles: 3 sampled from the day set and 3 from the night set, every pair >= 50 px apart (at 0.8 r = 120 px, 25° = 51.9 px chord, m arithmetic), every pin >= 40° off ±90° (the primitive throws otherwise); pin NUMBERS assigned by `rng.shuffle` so the answer sequence by number is not DDDNNN, not NNNDDD and not alternating. `sunDir` from the locale-neutral seed. **Verify:** per pin `data-lcs-angle` + `data-lcs-n`; per table row hidden `data-lcs-answer=day|night` = day iff |angle| < 90; 3/3 split; pin angle recomputed from the pin's rendered centre vs the Earth centre and the sun side within ±5° of the stamp; rotation arrow sweep counter-clockwise; no `[data-lcs-part="night"]` and no `inkSoft`/`tealSoft` fill on the Earth when `shadeNight:false`; chips unmarked; Sun R > Earth r. **Refusals:** none (hemisphere-neutral: seen from above the North Pole, no lit-side convention; K-208 labels exist x11, m). **Instruction (en source):** "Look where the Sun is. At each numbered pin, is it day or night? Circle the word in the table." (91) **Query face:** "why do we have day and night" · de "Wie entstehen Tag und Nacht?" · es "¿por qué hay día y noche?" · fr "l'alternance du jour et de la nuit" · da "hvorfor bliver det dag og nat".

### F4 : Planets in Order from the Sun (G3, CODE `layout:'planets-in-order'`)
**Move:** recall the fixed sequence Mercury to Neptune and WRITE each name on its orbit, outward from the Sun. **Layout (B's orbit fan + A's picture bank at ONE size):** bank row of 8 `planetBankCard`s 74 x 104 (`planetGlyph({box:64})` + 6 + the name Nunito 800 16, centred; white, border 2 creamDeep, radius 12; `justify-content:space-between`, 8 x 74 = 592 <= 639) + 12; then the fan panel 639 x H: Sun = coral circle centre (-40,-40) r 110 with 4 rays on its limb (-10°..100°), clipped; row k = 1..8 at y_k = 150 + (k-1) x pitch, orbit radius ρ_k = 1.25 (y_k + 40), disc centre x_k = -40 + 0.75 (y_k + 40); orbit arc k dashed `grid` 1.5 (dash 6 6) drawn ONLY from the disc down-left to the panel's left/bottom edge (no arc crosses a writing line); `orbitDisc({d:36, n:k})` at (x_k, y_k); `writingRow({w: 639 - (x_k + 30), h:44, glyphH:24})` from x_k + 30. pitch 56: H = 150 + 7 x 56 + 28 = 570, x_1 = 102.5, x_8 = 396.5, row-8 line 212.5 wide (fi "Merkurius" ≈ 108 handwritten *est.*). Stack 104 + 12 + 570 = **686 <= 722**; at 677 pitch 50: H 528, stack **644** (m arithmetic). G3 floors: discs 36, glyphH 24, bank names 16. **Why pattern glyphs are safe HERE (the planet-drawing ruling, critic §1 row 2):** every name is already printed on its card, so a ring beside "Saturn" adds recognition and delight and cannot reveal an ORDER; the glyphs are one uniform size, so the rocky/giant split is not printed either; every ANSWER surface (the eight orbit discs) is byte-identical. **Config d2** `{layout:'planets-in-order', slots:8, bank:'glyph-cards', pitch:[50,56], discD:36, glyphH:24}`; d1 `{mode:'number', bank:'glyph-cards'}` (write 1-8 on the 8 shuffled cards; the panel's mnemonic may appear on d1 ONLY: it prints the initials); d3 `{bank:false}` (recall; not shipped). Bank order `rng.shuffle`, !== answer order, !== reverse, !== the locale's alphabetical order of the literals (checked per locale on the shared seed; the composer re-draws until clean in ALL 11). **Verify:** `data-lcs-slot` 1..8 + hidden `data-lcs-answer=<planetId>` from `PLANETS`; the eight orbit disc SVGs byte-identical except the numeral; x_k strictly increasing; no arc bbox intersects any writing row; bank card set === PLANETS, each card's glyph id === its name id; glyph greyscale signatures pairwise distinct and radii equal; lines empty; no Pluto anywhere. **Refusals:** none. **Instruction (en source):** "Write the name of each planet on its line, starting next to the Sun. Use every name in the bank once." (103) **Query face:** "planets in order" · de "Planeten Reihenfolge" · es "el sistema solar y sus planetas en orden" · pt "ordem dos planetas" · sv "planeternas ordning" · da "planeternes rækkefølge" · fi "planeetat järjestyksessä Auringosta".

### F5 : Giant and Rocky Planets (G3, CODE `layout:'planet-sizes'`)
**Move:** CLASSIFY ten names by size class and catch the misconception that the Sun and the Moon are planets. **Layout (B):** `wordBank` of 10 names (8 planets + `notPlanet.sun` + `notPlanet.moon`, Nunito 800 18; *est.* 2 rows, 110 budgeted) + 20; then 3 bins 205 x 400 (gap 12; 3 x 205 + 24 = **639**), each a white box radius 16 border 3 teal: header 108 = an abstract size glyph (giant: outline circle d 56 teal 3; rocky: outline circle d 24; not a planet: outline circle d 40 crossed by a teal 3 diagonal) + label Baloo 2 700 18 teal, <= 2 lines in 181; then **4 writing rows in EVERY bin** (`writingRow({w:181, h:64, glyphH:24})`, pitch 68 = 272; 12 lines for 10 names, so no line count prints the 4/4/2 answer; A's 4/4/2 boxes rejected) + 20 padding = 400. Stack 110 + 20 + 400 = **530 <= 677 <= 722** (m arithmetic; a 3-row bank, 154, still gives 574). No planet drawing on this face (a drawn size IS the answer). **Honest size data (validator only, NEVER printed; NASA equatorial km):** Mercury 4,879 · Venus 12,104 · Earth 12,756 · Mars 6,792 · Jupiter 142,984 · Saturn 120,536 · Uranus 51,118 · Neptune 49,528 · Moon 3,475 · Sun 1,392,700; the smallest giant is 3.9 x the largest rocky planet, so the class is unarguable; a full size ORDER is refused (Uranus/Neptune differ by 3 %, Venus/Earth by 5 %). **Labels:** "giant" never "gas" (Uranus and Neptune are ice giants). **Config d2** `{layout:'planet-sizes', items:10, bins:['giant','rocky','notPlanet'], notPlanet:['sun','moon'], linesPerBin:4}`; d1 `{items:8, bins:['giant','rocky'], linesPerBin:4}`; d3 `{items:10, bank:false}` (not shipped). Bank order `rng.shuffle`, no 3 consecutive of one class, Sun and Moon not adjacent and not both at an end. **Verify:** hidden `data-lcs-class` per bank word from `SIZE_CLASS`; counts 4/4/2; 12 empty lines, 4 per bin; no `planetGlyph`, no number, no size anywhere on the page; header glyphs are the three abstract glyphs (no ring, no band). **Refusals:** none. **Instruction (en source):** "Sort the names. Write each one in the right box: giant planet, small rocky planet or not a planet." (99) **Query face:** "giant and rocky planets" / "planet sizes" · de "Riesenplaneten und Gesteinsplaneten" · sv "planeternas storlek" · da "planeternes størrelse" · fi "planeetat koon mukaan".

**Rejected non-moves.** Label / colour the solar system with library planets (4 of 8 pictures astronomically wrong, opened) · the patterned planet glyphs as ANSWER targets (a ring prints Saturn, bands print the giants, a size prints the class) · honest-size glyphs anywhere (Mercury 13 px, pattern illegible; prints the rocky/giant split) · star, planet or moon picture sort (no drawable star set; `asteroid` reads as the Moon) · planet facts "hottest / has rings" (Venus-hottest is above band; rings need a picture set) · full size ORDER of 8 · moon diary (open, weather-bound; a d-level of F1 at most) · day/night picture sort (K-208) · seasons from the tilt (K-322; above band) · relabelling the base's d1/d3 (same move; fails `gate-variation-distinct`).

## 4 Native rebuild plan x11

Frames NEVER inflect: this type has NO cloze frame and reads NO `objForms`; every sentence is a whole literal. Per locale the panel authors: `bodies` 3 · `facts` 13 (+ `truth` sign-off of every vector) · `leakForms` (gendered subject pronouns / agreeing predicate forms that would reveal a body; §5 rule 4) · `forbiddenStems` (the locale's forms of the forbidden facts: "gives light", "goes round the Sun", "rises", numbers) · `phaseNames` 4 · `planets` 8 · `notPlanet` 2 (may equal `bodies.sun` / `bodies.moon`) · `classLabels` 3 · `mnemonic` 1 (F4 d1 only) · `hemisphere` 1 · `strings` 6 titles + 6 instructions = **~52 literals**. The instruction names only apparatus on the page (base: sentence, box; F1: moon, row, number; F2: moon, line, word bank; F3: Sun, pin, table, word; F4: planet, line, bank, Sun; F5: name, box). **The EN is a SOURCE TO AUDIT**: every panel reads the en strings and reports defects in them, not only its own.

| loc | literals | forms / slots | refusal / re-target | traps |
|---|---|---|---|---|
| en | 3 bodies · 13 facts · 4 phases · 8 planets · 2 + 3 labels · 12 strings | none | none | Sun/Earth/Moon capitalised consistently; "first quarter" is a HALF disc (landing explains); en landings say "as seen from north of the equator"; US spelling |
| de | Sonne Erde Mond · Neumond, zunehmender Halbmond (or Mond), Vollmond, abnehmender Halbmond · Merkur … Neptun | capitals kept | none | "der Mond" is the only masculine body: a fact with "er" leaks the Moon (`leakForms`: er, ihn, der, sein); "Es ist ein Stern." reads as neuter-thing; "Sonnensystem" never "Solarsystem"; Merksatz d1 only; "Weltraum" never in a title |
| es (MX) | el Sol la Tierra la Luna · luna nueva, cuarto creciente, luna llena, cuarto menguante · Mercurio … Neptuno | none | none | "el Sol" the only masculine: "Es el más caliente" leaks (`leakForms`: el más, él, lo); null-subject verbs are safe; no Plutón; northern (SEP textbooks: panel confirms); "El espacio" never in a title |
| pt (BR) | o Sol a Terra a Lua · lua nova, quarto crescente, lua cheia, quarto minguante · Mercúrio Vênus Terra Marte Júpiter Saturno Urano Netuno + `hemisphere:'S'` | none | none | **mirror** (quarto crescente lit LEFT); BR spellings; "o mais quente" leaks the Sun; "atividade" never in a title |
| fr | le Soleil la Terre la Lune · nouvelle lune, premier quartier, pleine lune, dernier quartier · Mercure … Neptune | NBSP before `?` `:` | none | "Il"/"Elle" leak (`leakForms`); "C'est une étoile." safe; no "lune menteuse"; "L'espace" never in a title |
| it | il Sole la Terra la Luna · luna nuova, primo quarto, luna piena, ultimo quarto · Mercurio Venere Terra Marte Giove Saturno Urano Nettuno | none | none | "il più caldo" leaks the Sun; F3 title uses "giorno" to match the K-208 chip "Giorno" (m) unless the panel rules "dì"; landing: planets = classe quinta |
| nl | de zon de aarde de maan · nieuwe maan, eerste kwartier, volle maan, laatste kwartier · Mercurius … Neptunus | none | none | all three "de" words: "Het is een ster." safe; "ruimte" never in a title; capitals for body names (panel) |
| sv | solen jorden månen · nymåne, första kvarter (or halvmåne), fullmåne, sista kvarter · Merkurius … Neptunus | one definiteness for the three bodies | none | all common gender: "Den" safe; vocab "Sol" (indef) vs "Jorden" (def) mismatch is why literals rule; check every definite form for a homograph (the `bana`/`banan` class); "Rymden" never in a title [NSR] |
| da | solen Jorden månen · nymåne, tiltagende halvmåne, fuldmåne, aftagende halvmåne · Merkur … Neptun | none | none | capitalisation of Jorden (panel); "Rummet" never; phase-calendar noise stays out of titles [NSR] |
| no | solen jorden månen · nymåne, voksende halvmåne, fullmåne, minkende halvmåne · Merkur … Neptun | bokmål | none | "Verdensrommet" never; regle d1 only [NSR] |
| fi | Aurinko Maa Kuu · uusikuu, kasvava puolikuu, täysikuu, vähenevä puolikuu · Merkurius Venus Maa Mars Jupiter Saturnus Uranus Neptunus | every fact a whole literal (case: "Se kiertää Maata") | none | "Se" is neutral; Kuu (proper) vs kuu (month); "Avaruus" never in a title; autocomplete-trap check on each title (the "sukupuutto" lesson) [NSR] |

Every panel OPENS one render of every face in its locale and confirms by eye: the three heads read big / middle / small, the rails' lit side (pt: waxing lit LEFT), the four F2 names each fit exactly one card shape, the F3 pins read as day or night at a glance, the eight F4 glyphs tell apart. No library picture is used, so no picture list is opened; the editor's open of the `space/*` set (critic §3) is the record of why.

## 5 Data + gates

`data/b5/earth-and-space.js` (locale-neutral, hand-authored) + `data/b5/locales/earth-and-space.<loc>.json` GENERATED from `i18n/.draft-b5-<loc>.json` by `tools/apply-b5-locale.js` after `tools/validate-b5-draft.js` (clones of the b4 pair, both exist, m; `data/` gitignored, `git add -f`); read through `lib/b5-common.js bank('earth-and-space', loc)`.
```
EARTH_AND_SPACE (data/b5/earth-and-space.js):
  BODIES: ['sun','earth','moon'],
  FACTS: {                                     // truth = [sun, earth, moon]; exactly one 1
    ownLight:{truth:[1,0,0]}, star:{truth:[1,0,0]}, hottest:{truth:[1,0,0]}, biggest:{truth:[1,0,0],size:true},
    liveOn:{truth:[0,1,0]}, oceans:{truth:[0,1,0]}, air:{truth:[0,1,0]}, planet:{truth:[0,1,0]}, spinDay:{truth:[0,1,0]},
    orbitsEarth:{truth:[0,0,1]}, seemsToChange:{truth:[0,0,1]}, craters:{truth:[0,0,1]}, smallest:{truth:[0,0,1],size:true} },
  FORBIDDEN_FACT_IDS: ['givesLight','shinesAtNight','orbitsSun','isRound','spins','seenInSky','walkedOn','noAir','reflects','sunMoves'],
  PHASES: [0,1,2,3,4,5,6,7], NAMED_PHASES: [0,2,4,6],
  PLANETS: ['mercury','venus','earth','mars','jupiter','saturn','uranus','neptune'],
  DIAMETER_KM: { mercury:4879, venus:12104, earth:12756, mars:6792, jupiter:142984, saturn:120536, uranus:51118, neptune:49528, moon:3475, sun:1392700 },
  SIZE_CLASS: { mercury:'rocky', venus:'rocky', earth:'rocky', mars:'rocky', jupiter:'giant', saturn:'giant', uranus:'giant', neptune:'giant', sun:'notPlanet', moon:'notPlanet' },
  DAY_ANGLES: [0,25,-25,50,-50], NIGHT_ANGLES: [180,155,-155,130,-130],
  REFUSED_PICS: ['space/*'],                   // no library picture on any face (critic §3)
EARTH_AND_SPACE_LOC (data/b5/locales/earth-and-space.<loc>.json):
  { hemisphere:'N'|'S', bodies:{sun,earth,moon}, facts:{<13 ids>}, leakForms:[...], forbiddenStems:[...],
    phaseNames:{'0','2','4','6'}, planets:{<8 ids>}, notPlanet:{sun,moon}, classLabels:{giant,rocky,notPlanet},
    mnemonic:'', refuse:[], strings:{ 'G1-378':{title,instruction}, <F1..F5 ids>:{title,instruction} } }
```
Helper contract: `bank('earth-and-space', loc)` returns the merged object; `factText(loc,id)`, `phaseName(loc,p)`, `planetName(loc,id)`, `bodyName(loc,id)` THROW on a missing literal (never fall back to the vocab); `hemisphereOf(loc)` throws unless `N|S`.

**`tools/validate-b5-draft.js` (earth-and-space block; every rule runs, exit 1 on any):**
1. Every key of the loc shape present, trimmed, non-empty, no `{`; 13 facts, 4 phase names, 8 planets.
2. Each `FACTS` truth has exactly one 1; no id of `FORBIDDEN_FACT_IDS` present; per body >= 2 non-size facts (d2 reachability).
3. No fact literal contains a digit or matches a `forbiddenStems` entry (`(?<!\p{L})…(?!\p{L})`, NFC-lowercase), nor names a season or a weather word from the panel's list.
4. No fact literal contains any `leakForms` entry of its locale (de/fr/es/it/pt lists REQUIRED non-empty; the panel signs `genderNeutral:true` on every fact).
5. The 4 phase names pairwise distinct and the 8 planet names pairwise distinct after NFC + `toLocaleLowerCase(loc)`; the 4 phase names at Nunito 800 18 + chip padding measured <= 639 in one row (a wider set fails).
6. No planet literal matches the per-locale Pluto list (Pluto, Plutón, Plutão, Pluton, Plutone, Pluuto …).
7. `hemisphere` ∈ {N,S} and === `S` iff loc = pt.
8. `classLabels.giant` does not contain the locale's gas stem (gas, Gas, gaz, gás, kaasu) unless the panel files a curriculum citation.
9. Titles <= 70, no worksheet-word (`WORKSHEET_WORD` regex of the b4 gate), unique in the band, none contains `axes.theme.space.name|slug.<loc>`, no F3 title equals K-208's title in that locale; instructions <= 150; no visible string contains the locale's free-word.
10. Instructions name only apparatus of their face (per-face banned list: base never "circle", "line", "write"; F1 never "name", "word"; F2 must contain the bank word; F3 never "colour", "shade"; F4 never "number" at d2; F5 never "draw").
11. Fact literals <= 80 chars (the 2-line budget at 303 px; the render gate is the final word).
12. `strings` ids === {base + the 5 allocated face ids}; the six titles differ pairwise by at least one NOUN or verb token.

**`qa/verify-b5-earth-and-space.js`:** renders base + 5 faces x 11 locales x d2 through `render/render-instance.js` (file:// fonts) under the 722 stack AND the 4-line fi title (677); asserts `verify()` empty and `qa/lints.js` clean; then the floors ITSELF (no size lint exists): base ticks 44 x 44, fact text 18 px on <= 2 lines, head discs strictly decreasing (Sun > Earth > Moon); F1 discs 104, boxes 52 x 48; F2 discs 88, ruling glyphH 24, bank one row; F3 Earth r 150, chips 44 high, pins >= 50 px apart; F4 discs 36, glyphH 24, bank names 16 px, glyph radii equal; F5 12 lines, glyphH 24; every moon disc on every face: rasterised lit/disc within ±0.03 of `data-lcs-lit` and centroid on `data-lcs-litside` expected for `hemisphere[loc]`; Sun drawn larger than any other body on every page that shows two; F3 pin angles from pixels ±5°; no `<img>` anywhere; every answer surface empty (ticks, boxes, lines, chips); position tells measured on the SHIPPED instance (the seed carries no locale, so a tell ships to all 11): base body column sequence, F1 box sequences, F3 answers by pin number, F4 bank vs slots vs alphabetical per locale, F5 class runs. SWEEP: 20 seeds x d2 per face render distinct pages.

**Poison (each must FAIL for its own reason; the correct EN draft is the control):** P1 fact `orbitsSun` in `FACTS` -> rule 2 · P2 `ownLight.truth=[1,0,1]` -> rule 2 · P3 en fact "It gives us light." (a `forbiddenStems` hit) -> rule 3 · P4 fr fact "Elle tourne autour de la Terre." -> rule 4 · P5 es fact "Es el más caliente de los tres." -> rule 4 · P6 pt `hemisphere:'N'` -> rule 7 · P7 sv `hemisphere:'S'` -> rule 7 · P8 de phaseNames 2 === 6 ("Halbmond" twice) -> rule 5 · P9 planets include "Pluto" -> rule 6 · P10 en `classLabels.giant:'gas planets'` -> rule 8 · P11 base title "Space" / de "Weltraum" -> rule 9 · P12 en fact "It is 150 million km away." -> rule 3 · **render:** PR1 an F1 pt rail drawn N -> centroid side · PR2 a crescent with the sweep flag flipped -> lit fraction · PR3 F1 grow rail shuffled into sorted order -> tell · PR4 a moon icon inside `growCue` -> "extreme printed" · PR5 F2 page with a phase 1 card -> multiset · PR6 F3 pin at angle 80 -> primitive throws / angle gate · PR7 F3 d2 with a night-shading element -> "answer printed" · PR8 F4 orbit disc 6 with a ring -> "slot discs not identical" · PR9 F4 bank order === answer order -> tell · PR10 F4 Jupiter glyph at honest r 36 -> "glyph radii not uniform" · PR11 F5 bins with 4/4/2 lines -> "count printed" · PR12 base Sun head `discR:20` (smaller than Earth) -> size order · PR13 `answerBox` in place of `blankNumeralBox` on F1 -> `data-lcs-answer="undefined"` · PR14 any `<img src=".../space/...">` -> "library picture".

**Page reads:** `EARTH_AND_SPACE`, `EARTH_AND_SPACE_LOC[loc]`, `data/science/day-vs-night.json` (F3 chip labels only), the three new primitives; never `image-vocabulary.js`, `objForms`, `articles.js`, `fileUri` / `pictureFor`, or any `space/*` picture at render.

## 6 SEO plan

| face | title pattern (Germanic / Romance / Nordic + fi) | meta MIDDLE (whole 120-170; the child's instruction, en source) | coordinate |
|---|---|---|---|
| base | Sun, Earth and Moon · Sonne, Erde und Mond · Zon, aarde en maan / El Sol, la Tierra y la Luna · O Sol, a Terra e a Lua · Le Soleil, la Terre et la Lune · Il Sole, la Terra e la Luna / Solen, jorden och månen · Solen, Jorden og Månen · Solen, jorden og månen · Aurinko, Maa ja Kuu | Children read eight short facts and tick the Sun, the Earth or the Moon, learning that the Sun is a star and the Moon only seems to change shape | `{type:'earth-and-space', mode:'base', theme:'', level:<G1 key>}` |
| F1 | Moon Phases in Order · Mondphasen ordnen · Maanfasen op volgorde / Fases de la luna en orden · Fases da lua em ordem · Les phases de la lune dans l'ordre · Le fasi lunari in ordine / Månens faser i ordning · Månens faser i rækkefølge · Månefasene i rekkefølge · Kuun vaiheet järjestykseen | Children write 1 to 5 under the moons, first as the Moon seems to grow from new moon to full moon, then as it shrinks again | `mode:'moon-phases-in-order'`, G1 |
| F2 | Name the Moon Phases · Mondphasen benennen · Maanfasen benoemen / Fases de la luna con nombres · Nomes das fases da lua · Nommer les phases de la lune · I nomi delle fasi lunari / Vad heter månens faser? · Månens faser og deres navne · Hva heter månefasene? · Kuun vaiheiden nimet | Children write the name of each moon shape, new moon, first quarter, full moon or last quarter, using each word in the word bank twice | `mode:'moon-phase-names'`, G2 |
| F3 | Why Do We Have Day and Night? · Wie entstehen Tag und Nacht? · Hoe ontstaan dag en nacht? / ¿Por qué hay día y noche? · Por que existe dia e noite? · L'alternance du jour et de la nuit · Perché ci sono il giorno e la notte? / Varför blir det dag och natt? · Hvorfor bliver det dag og nat? · Hvorfor blir det dag og natt? · Miksi on päivä ja yö? | One spinning Earth, the Sun on one side and six numbered pins: children circle day or night, because the half facing the Sun has day | `mode:'day-and-night-model'`, G2 |
| F4 | Planets in Order from the Sun · Planeten in der richtigen Reihenfolge · Planeten op volgorde / El sistema solar y sus planetas en orden · Ordem dos planetas do sistema solar · L'ordre des planètes du système solaire · I pianeti del sistema solare in ordine / Planeternas ordning · Planeternes rækkefølge · Planetenes rekkefølge · Planeetat järjestyksessä Auringosta | Children write the eight planets on their orbits from Mercury to Neptune, starting next to the Sun, using every name in the bank once | `mode:'planets-in-order'`, G3 |
| F5 | Giant and Rocky Planets · Riesenplaneten und Gesteinsplaneten · Grote en kleine planeten / Planetas rocosos y planetas gigantes · Planetas rochosos e planetas gigantes · Planètes rocheuses et planètes géantes · Pianeti rocciosi e pianeti giganti / Planeternas storlek · Planeternes størrelse · Planetenes størrelse · Planeetat koon mukaan | Children sort ten names into giant planets, small rocky planets and not a planet, and find out that the Sun and the Moon are not planets | `mode:'planet-sizes'`, G3 |

All titles are candidates the native panels rewrite (<= 70, no worksheet-word, no theme word, base = the three-body head, each face adds its own NOUN / verb head, never an adjective). The en F3 title contains but never equals K-208's "Day and Night" (m). The type name (table B) differs from the base title in every locale; that is allowed, not required. `coordinate.mode` is ALWAYS the face's mode string (base `'base'`), `coordinate.theme:''`, level = the band key of §1 per face. h1 = title; eyebrow = level label; strand chip = the `Science` row literal (framework NAME only). JSON-LD `LearningResource`, NO `educationalAlignment`; en prose names the NGSS codes of §1; every landing states the placement of table C (and "not to scale" on base, F4; "as seen from north of the equator" on the en/es F1 F2, "no hemisfério sul" on pt). Meta lead inherits `seo.words.free_printable` (metadata only; no visible copy claims "free"); no title, meta or landing promises an answer key (printable-only decks ship none); the MIDDLE window is measured with `tools/measure-instruction-window.js` (exists, m). `topicMeta['earth-and-space']` >= 50 chars x11 + `skill-sentences.<loc>.json['earth-and-space']` via the `register-b4-en-content.js` clone.

**Non-cannibalisation** (whole-landing 3-gram Jaccard, `scripts/seo-landing/gate.js` FAIL >= 0.80 / WARN 0.65; *est.*, the gate measures):

| pair | what the copy says differently | est. |
|---|---|---|
| base vs F3 | facts about three bodies, tick a box vs one spinning Earth, pins, day or night | 0.10 |
| F1 vs F2 (both moon) | put shapes in ORDER (numbers, grow/shrink) vs NAME four shapes (word bank, lines); no shared paragraph | 0.22 |
| F4 vs F5 (both planets) | ORDER on orbits from the Sun vs SIZE CLASS in three bins, Sun and Moon not planets | 0.18 |
| base vs F5 | Sun/Earth/Moon facts vs planet classes | 0.10 |
| F3 vs K-208 Day and Night | why (a spinning Earth) vs which things we see by day or night (a picture sort) | 0.08 |
| base vs K-208 | one-body facts vs a picture sort | 0.05 |
| base vs G1-341 read-and-check | which of three bodies vs true or false | 0.06 |
| F3 vs K-322 seasons, K-356 / G3-385 | day and night vs seasons, weather, water cycle | < 0.05 |
| F1 / F2 vs G1-376 plants F2, G1-377 life cycles | moon shapes vs plant or animal stages | < 0.05 |

Boundary sentence on every landing: "This page is about the Sun, the Earth, the Moon and the planets; sorting day and night pictures, seasons and weather have their own pages."

## 7 Hub visibility contract

A face appears under `earth-and-space` on `/[locale]/worksheets` IFF all four hold: (1) `apps['earth-and-space']` exists in `frontend/config/topics-taxonomy.json` (ABSENT today, m; missing = rendered NOWHERE in the rail) with `default_subject:'science'`; (2) `axes['exercise-type']['earth-and-space']` has `slug` + `name` in all 11 locales (table B; 0 collisions, m); (3) exactly one landing per face per locale with `coordinate.type === 'earth-and-space'` verbatim, `mode` per §6, `theme:''`, the level key of §1 (G1 base + F1, G2 F2 + F3, G3 F4 + F5), a unique slug and `canonicalDeckSlug` = the published deck; a REFUSED face has NO landing and the expectation is lowered explicitly, never padded; (4) the landing JSON committed AND deployed (per-process cache).

Gate: `node scripts/verify-hub-type-rows.js --keys=earth-and-space` (exists, m; expectations from `b5-designs/hub-expectations.json` exported by `tools/export-hub-expectations.js --batch=b5`; poison = drop one landing / drop `apps['earth-and-space']` must FAIL).

**Expected rows per locale:** en 6 · de 6 · es 6 · pt 6 · fr 6 · it 6 · nl 6 · sv 6 · da 6 · no 6 · fi 6 = **66**. No face is refused in any locale at design: every face is word-light or whole-literal, pt is served by the mirror, and placements above band are STATED on the landing (table C), never a refusal. A panel that cannot give four one-shape-only phase names or a leak-free fact set records it in `refuse` and lowers that locale to 5 before the wave.
