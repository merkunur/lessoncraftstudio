# G1-353 `question-words` : the FIVE variation faces (Phase 2 build record, 2026-09-21)

Built from `G1-353-question-words.md` §3 (the contract), §1 / §4 / §5 (rulings, data, gates), `_FACE-BRIEF.md`, the base record `_work/G1-353-build.md` (open items 1-6, the measured 710 chrome, the reviewer's title-lists-chips ruling, the "who rows borrow an off-page frame kind" dealer) and the README cross-type rulings. Exemplars read: `tools/b4var-rows/pronouns.js` + `_work/G1-352-faces.md`, `tools/b4var-rows/cloze.js` + `_work/G1-350-faces.md` (the match face's SPARSE precedent), `tools/b3var-rows/seasons.js`. All five are CODE faces on the base's ONE additive `mode` knob; no HANDWRITTEN spec; no PARAM face. No em-dashes.

## Files (type-scoped; nothing shared edited; the base byte-identical)

| file | what |
|---|---|
| `scripts/worksheet-gen/tools/b4var-rows/question-words.js` (NEW) | the five rows (ids per `_records/b4var-id-allocation.json`): F1 G1-373 `match` · F2 G1-374 `fill` · F3 G1-375 `sort` · F4 G2-356 `write` · F5 G2-357 `ask`; F4 / F5 carry `extra {gradeBand:'G2'}`; EN title + instruction = the bank's `strings.<mode>` (rule 12 asserts one source) |
| `scripts/worksheet-gen/types/g1/G1-353-question-words.js` | `_buildFace` dispatches the knob to `buildMatch / buildFill / buildSort / buildWrite / buildAsk`; per face a resolver on the CONFIG (`resolveMatch / resolveFill / resolveSort / resolveWrite / resolveAsk`, every unbuilt d1 / d3 shape throws by name); `dealFace` (the base deal with hooks: an explicit ask list, `whoKinds`, a per-row question filter run on every candidate thing AND on the final fill, portraits at >= their `minPx`); `VERIFY_FACE` (one page-side branch per `data-lcs-mode`: floors, uniqueness, no answer printed, position, SPARSE); `verify()` dispatches on the mode stamp. The base path (`resolveBase` / `compose` / `VERIFY_BASE`) is untouched: `node tools/b3-baseline.js --check --quick` PASS |
| `scripts/worksheet-gen/templates/components-b4/question-words.js` | ADDITIVE params only, no new export: `qaMatch` gains `itemMax` (items grow 100..120, `data-lcs-itemmax`), `badge` (the K-287 `countBadge` on a count answer's picture), `picKind` / `depicted` stamps on the right pictures, `name` / `slots` stamps on the left items, and the clock in a `flex:0 0 auto` span (it shrank to 81 of 88 beside a wrapping literal, measured); `questionFrame` / `writeRow` gain the `kind` stamp |
| `scripts/worksheet-gen/data/b4/question-words.js` (the reviewer force-adds) | one additive key: `bankWidths` = the MEASURED Nunito 800 18 widths of the five bank words (Who 42.3 · What 49 · Where 58.7 · When 52.3 · How many 91.2) for the F2 gap-box formula (the design's own figures to the pixel); an unauthored table falls back to `chipWidths - 29` (the Baloo-20 proxy, within 8 px on every measured locale) |
| `scripts/worksheet-gen/qa/verify-b4-question-words.js` | the gate grows: section 5 (below); rule 9 gains the **title-lists-kinds** check (a title that names question words names EXACTLY its page's kind set; the ask title names its two extra starters why + how; an instruction names only kinds on its page; the bins literals alias their kind for fi's nominative "mikä"); rule 12 gains `bankWidths` (20..200 px, qwords literals only) and `faces.<f>.kinds === the emitted spec's d2 config` + `<spec>.i18n.en === strings.<f>`; `withDeps` lets a synthetic locale inject all three banks; the final line is `PASS (N assertions, M/M poisons killed)` |
| `scripts/worksheet-gen/types/{g1,g2}/G1-373… G1-374… G1-375… G2-356… G2-357…` | emitted by `node tools/gen-b4var-specs.js` (50 faces from 10/10 families) |
| `scripts/worksheet-gen/i18n/strings.en.json` | regenerated by `node i18n/build-en.js` (658 types, title lint clean); left as generated |

## The faces

### F1 : G1-373 `Match the Question to the Answer` (G1, CODE `mode:'match'`)
- **Knob:** `{mode:'match', pairs:5, kinds:['who','what','where','when','howmany'], picPx:88, itemH:100, itemMax:120, leftW:252, rightW:248, fontPx:18, maxChars:44, maxThings:4}`.
- **The child:** reads five printed questions on the picture-less left ("How many meerkats does Anna find?" · "What does Ava draw?" · "Where does Jack read?" · "Who likes the coconut?" · "When does Eli sleep?"), one per question word, and DRAWS a line to the short answer of that kind on the right: a name over its portrait, a thing over its picture, a place phrase over its picture, a clock time over an 88 px clock, a number word over the thing's picture wearing a "3" badge. The five frames are distinct with distinct names, things, places and times; the `who` question sits on a thing or place frame; the right column is a derangement.
- **Layout:** `qaMatch` at padding 6 20: items 252 / 248 wide, `min-height:100px; max-height:120px; flex:1 1 auto` in the `.ws-match-col` (space-around): items **120 at every chrome**, bands 33 (body 766) / 22 (710) / 15 (677), slack under the last item 17 / 11 / 8.
- **verify():** 5 questions + 5 answers === the stamp, in [3, 7]; every ask distinct and === the page kinds; the right kinds multiset === the left asks; every question opens with `qPrefix + qwords[ask]` and ends "?"; no question carries ANY answer literal (word-bounded, NFD, ci); no picture on the question side; each answer's picture matches its kind (who: a portrait with a depicted tag; what / howmany: a thing; where: a place; when: a clock >= 74 with 9+ px numerals, no picture); a count badge on the howmany answer only; the literal printed === its stamp, Baloo 20 (>= 20), <= 2 lines; questions Nunito 18 (>= 18), <= 2 lines; the rendered order === the order stamp and no answer at its own question's position; dots on every item; the only svgs are clocks and badges (no line pre-drawn); no src / name / literal twice; SPARSE = the G1-368 precedent: items in [itemH, itemMax], the band between consecutive items <= 44 (the G1 element floor), the slack under the last item <= 50, the first item within 50 of the stage top. **Node:** every question === `questionOf(frame, ask, ctx)` from the stamped slots; every literal === the name / `markOf`; who -> the pronouns bank's portrait of the name's gender at >= 88 (its minPx); what / howmany -> `pinThing` (+ the badge === n); where -> `places[key].pic`; when -> the clock's `h` === the time literal's `h`; the question-side picture-bot is 0 by construction (no picture on the left).
- **Poisons owned:** PC1 (a kind twice), PC2 (picPx 64 under a when answer), PC3 (itemMax 200), P15 (two who questions), P15b (the first answer claims question 1), P15c (an answer literal injected into a question), P15d (a picture on the question side), P15e (a person picture on a what answer), PS1 (items fixed at 100 under a one-line chrome: 62 px bands), PN1 (a literal stamp edited).
- **PNG:** `scripts/worksheet-gen/out/dev/G1-373-null-d2-en.png`. Seen: five cream question cards down the left, five white answer cards down the right, every card 120 high with ~33 px of paper between; the athlete portrait + "Zoe", the river + "at the river", the raspberry + "the raspberry", a 7 o'clock clock + "at seven o'clock", a meerkat with a teal "3" badge + "three"; no line drawn; the two-line question ("How many meerkats does Anna find?") fits its card; nothing clipped, nothing under the footer. The title wraps to two lines at the en letter width (a chrome fact, not a face defect).

### F2 : G1-374 `Fill In the Question Word` (G1, CODE `mode:'fill'`)
- **Knob:** `{mode:'fill', rows:6, bank:true, kinds:{who:1,what:1,where:1,when:1,howmany:1}, maxPerKind:2, picPx:56, gapH:40, gapMin:110, gapMax:220, fontPx:18, maxRest:30, maxChars:40, maxThings:4, rowMin:84, rowGap:8}`.
- **The child:** reads the answer sentence with its highlighted words ("Emma sees [four] tigers." with Emma's portrait), then COPIES the question word from the deranged five-pill bank (How many · What · Who · Where · When) into the dashed 170 x 40 box at the head of the printed question ("[   ] tigers does Emma see?"). Five kinds each once + a sixth row of any kind (<= 2 per kind); the 5-pill bank is the WHOLE `qwords` table, never in the fixed order.
- **Layout:** `wordBank` 60 + margin 10 over a `flex:1` grid `repeat(6, minmax(84px, 1fr))` gap 8 of `questionFrame` lanes: intrinsic 614; the lanes stretch to 115 (body 799) / 100 (710) / 95 (677). `gapW` = `clamp(round(1.6 x widest bank word at Nunito 800 18 + 24), 110, 220)` = **170** for en (the design's number; de 153 · es 136 · fr 144 · sv 176 · fi 213 on the synthetic blocks).
- **verify():** the bank present, === the qwords table, one row, >= 16 px, NOT in the fixed order (position leak); 6 lanes === the stamp, in [6, 12]; every lane: the text before the ONE empty gap box === the prefix, the box === the page gapW (+- 1, >= 110 x 36), the rest ends "?", the answer literal absent from the question line AND the sentence (word-bounded, NFD, ci), the question line one line and inside its column; the sentence one line with ONE word-bounded mark (the name on who rows, never the name otherwise, a single word on count rows), no question word printed in it; a portrait >= 44 with a depicted tag; no chip; no name / src / frame twice; every bank kind asked >= 1 and <= maxPerKind; pictureBot (a portrait on every row -> the who rows) <= 0.35; SPARSE: the lanes end within 3 px of the body bottom, blank inside a lane <= 24, lanes <= 8 apart. **Node:** sentence === `fillFrame`, mark === `markOf`, rest === `gapOf(bank, questionOf, ask)`, answer === `qwords[ask]`, gapW === the formula over `bankWidths`, every rendered bank word's text within 3 px of `bankWidths`, portraits === the pronouns bank (gender + file + minPx).
- **Poisons owned:** PC4 (`bank:false`), PC5 (maxRest 60 fails the 531 line guard), P16 (the question word printed before the box), P16b (the bank in the fixed order), P16c (one box 120: a per-row width), P16d (a bank word missing), P16e (3 who rows of 7 -> pictureBot 0.43), PS2 (lanes fixed at 84: "185 px above the body bottom"), PN2 (an answer stamp edited), PB5 (`bankWidths` out of range).
- **PNG:** `scripts/worksheet-gen/out/dev/G1-374-null-d2-en.png`. Seen: the dashed coral bank across the top with five white pills (How many · What · Who · Where · When), six cream lanes filling the body; each lane a numbered badge, a portrait (librarian f for Emma, doctor m for Leo, florist f for Ava, judge m for Jack, barber m for Max, mail carrier f for Anna), the question with its dashed box at the head and the marked sentence below ("four", "on the mountain", "the ship", "at eight o'clock", "the iguana", "Anna"); nothing clipped.

### F3 : G1-375 `Sort the Answers: Who, What or Where` (G1, CODE `mode:'sort'`)
- **Knob:** `{mode:'sort', tiles:9, bins:['who','what','where'], perBin:3, tileGuard:112, binW:201, lineMin:5, stack:677}`; the title lists exactly the d2 bins (rule 9, PB1).
- **The child:** reads nine word tiles on a two-row shelf (three tagged names, three bare things through `displayWord` of the vocab singular, three place phrases under the tile guard) and WRITES each under the question it answers in the three ruled bins headed Who? What? Where?. No sentence, no picture: the constituent alone decides.
- **Layout:** `qwBins` in a FIXED **677** stack (the SPARSE rule; the K-288 / G1-372 idiom): shelf 122 (2 rows, the composer's `shelfRowsEst` keeps it at 2 by the tile-width estimate) + 18 + head 40 + 6 + bins **491** with `lineCount = max(5, 3, round(481 / 58)) = 8` lines at gapY 55 (the last line 51 px above the bin bottom). Slack under the bins 89 (body 766) / 33 (710) / 0 (677).
- **verify():** tiles === the stamp (9), in [6, 12]; bins === kinds === heads, heads === the stamped literals ending "?"; every tile prints its stamp, distinct, never a head or a question word (the "?" stripped on both sides), key in range, >= 44 high, text <= 124 (the render guard), on the shelf; the shelf <= 2 rows; every bin 201 x 491, lines >= its load (exactly perBin each), gapY >= 34, the last line within 64 of the bin bottom, nothing pre-placed, no text in a bin; no `<img>`, no chip / sentence; SPARSE: stack === 677 (+- 4) and >= 660, slack <= 180, the shelf at the stage top. **Node:** name tiles ∈ `names[]`, thing tiles === `tileTextOf` (the `bare` / `unique` policy) of their key with a vocab entry in the locale, place tiles === `places[key].text`; every key === the bin of its kind; the authoring tile guard (10.2 px/glyph) and the two-row estimate re-run.
- **Poisons owned:** PC6 (a when bin), PC7 (stack 600), PR-sort-a (a tile pre-placed in a bin), PR-sort-b (a tile renamed "Who?"), PR-sort-c (a picture injected on the shelf), PR-sort-d (a head swapped to When?), PS3 (the design's 350 bins: "stack 536 != the fixed 677"), PN3 (a name tile keyed what), PB1 (the title lists When), PB6 (`faces.sort.kinds` != the G1-375 config).
- **PNG:** `scripts/worksheet-gen/out/dev/G1-375-null-d2-en.png`. Seen: a cream shelf with two rows of white tiles (avocado · at the pool · Ben · Anna · Lily · foal · zebra / in the tent · in bed), three teal pill heads Who? What? Where? over three tall dashed-topped bins whose eight ruled lines fill them to 51 px of the bottom; ~90 px of paper under the bins; nothing clipped.

### F4 : G2-356 `Write the Question` (G2, CODE `mode:'write'`)
- **Knob:** `{mode:'write', rows:8, ruling:true, bank:false, h:48, glyphH:24, picPx:48, fontPx:18, kinds:{who:2,what:2,where:2,when:1,howmany:1}, maxChars:31, sentenceMax:40, maxThings:4, rowMin:78, rowGap:6}`; `extra {gradeBand:'G2'}`.
- **The child:** reads the printed answer with its highlighted words beside the subject's 48 px portrait ("Leo sees [three] taxis.") and WRITES the whole question on the 575 x 48 school-line ruling ("How many taxis does Leo see?"): no bank, no gap, the "?" is the child's. Frames filtered per row by the question <= 31 glyphs AND `need = glyphs x 18 + 16 <= 575`.
- **Layout:** `writeRow` x 8 (LANE-LESS, as designed: a lane shell would be 714 / 730 > 677 / 710) in a `flex:1` grid `repeat(8, minmax(78px, 1fr))` gap 6: intrinsic 666; the rows stretch to 95 (799) / 84 (710) / 79 (677). The ruling w = 675 (the page inner, the rows are lane-less) - 52 - 48 = **575** (the design's number; the first build read the lane's 639 and got 539, caught by the resolver's own guard: "maxChars 31 needs 574 px > the 539 ruling").
- **verify():** 8 rows === the stamp, in [6, 16]; no bank, no chip, no `[data-lcs-starter]` anywhere; every row: the answer stamp opens with `qPrefix + qwords[ask]`, ends "?", is printed NOWHERE on the page, and `need(answer) <= w`; one sentence with ONE mark (as on F2), one ruling cell with exactly one EMPTY `writing-row` svg (no `<text>`) >= w wide, >= glyphH + 20 high, >= 3 school lines; portraits >= 36 (48 shipped) with a depicted tag; no name / src / frame twice; every page kind asked >= 1, >= 2 kinds; pictureBot <= 0.35; SPARSE as F2 (rows end at the body bottom, blank inside a row <= 24, rows <= 6 apart). **Node:** sentence === `fillFrame`, mark === `markOf`, answer === `questionOf`, need <= the rendered ruling, portraits === the pronouns bank at >= their minPx.
- **Poisons owned:** PC8 (`bank:true`), PC9 (glyphH 20), P17 (the canonical question printed as a starter), P17b (a bank banner injected), P17c (the ruling narrowed to 300), PS4 (rows fixed at 78: "133 px above the body bottom"), PN4 (an answer stamp edited), PB4 (the instruction names Why).
- **PNG:** `scripts/worksheet-gen/out/dev/G2-356-null-d2-en.png`. Seen: eight white rows filling the body, each a numbered badge, a bust-class portrait (athlete, detective, ballerina, teacher, singer, waitress, flight attendant, architect: every one a 44-class bust, the 56-class full figures excluded at 48), the marked sentence and an empty three-line ruling under it; the marks "Mia" / "Eli" / "in the kitchen" / "at three o'clock" / "two" / "the calf" / "the nectarine" / "at home"; nothing under the footer.

### F5 : G2-357 `Ask About the Picture: Why and How` (G2, CODE `mode:'ask'`, open-ended)
- **Knob:** `{mode:'ask', starters:['who','what','where','when','why','how'], rows:6, h:72, glyphH:26, gap:8, scene:{tile:120, clock:120}}`; `extra {gradeBand:'G2'}`; the title names exactly its two extra starters (rule 9, PB2).
- **The child:** looks at ONE scene (a portrait, a thing, a place picture, a 120 px clock in four white tiles on a cream stage) and FINISHES six questions, each ruling opening with a printed starter Who · What · Where · When · Why · How at the metrics-sized 29.5 px. The only open face; nothing else is printed.
- **Layout:** `askScene` 160 + 12 + `starterLines` (6 x 72 + 5 x 8 + 24 = 496) = **668** (<= 677); slack under 98 (766) / 42 (710) / 9 (677).
- **verify():** no `verify()` re-derivation (open-ended): the four tiles in order person · thing · place · clock, >= 112 wide, pictures >= 120 with the right `pickind`, the clock >= 74 with `h` === the scene stamp and `m` 0, 13+ px numerals, no text on the scene; six ruling rows === the starters stamp === the kinds, the first four === `qwords` who..when, the last two outside the qwords table, each row's svg carrying exactly one `text[data-lcs-starter]` printing its starter (>= 16 px), rows === h, >= 600 wide, <= 8 apart; no sentence / chip / bank / mark on the page; SPARSE: stack >= 660, slack <= 180, the scene at the stage top, <= 12 between the scene and the rulings. **Node:** the portrait === the person key's file with depicted === the name's gender, the thing tile === `pinThing`, the place tile === `places[key].pic`, the time ∈ the bank's times === the clock, the starters === `bank.starters`, one starter size.
- **Poisons owned:** PC10 (a howmany starter), PC11 (h 40), PR-ask-a (the scene's hour stamp edited), PR-ask-b (a starter replaced by "How many"), PS5 (the design's h 56: "stack 572 < 660"), PR-ask-c (every name tagged f: a boy's name over a girl's portrait), PB2 (the title names Why and When).
- **PNG:** `scripts/worksheet-gen/out/dev/G2-357-null-d2-en.png`. Seen: four white tiles on a cream stage (a construction worker, a pear, a house, a 9 o'clock clock), a cream lane with six school-line rulings each starting with a grey starter word sitting on its baseline (Who / What / Where / When / Why / How), ~100 px of paper under the lane; nothing clipped.

## Gate (`node qa/verify-b4-question-words.js`, 99 s)

```
render G1-373 match en: verify 0 lints 0 cross 0 body 766 items 120 x5 bands 33 slack-under 17 · 3+3 body 710 bands 22 slack-under 11 · 677 body 677 bands 15 slack-under 8
render G1-374 fill en: verify 0 lints 0 cross 0 body 799 gapW 170 lanes 115 x6 · 3+3 body 710 lanes 100 · 677 body 677 lanes 95
render G1-375 sort en: verify 0 lints 0 cross 0 body 766 stack 677 shelf 122 bins 8@55 491 x3 slack-under 89 · 3+3 body 710 slack-under 33 · 677 body 677 slack-under 0
render G2-356 write en: verify 0 lints 0 cross 0 body 799 rows 95 x8 ruling 575 x 48 · 3+3 body 710 rows 84 · 677 body 677 rows 79
render G2-357 ask en: verify 0 lints 0 cross 0 body 766 stack 668 rows 72 x6 starter 29.5 px clock 14 px slack-under 98 · 3+3 body 710 slack-under 42 · 677 body 677 slack-under 9
render synthetic de / es / fr / sv / fi x match fill sort write ask: verify 0 lints 0 cross 0 (es "¿" on every question, fr NBSP + "?" on every answer, fi {part}; gapW de 153 es 136 fr 144 sv 176 fi 213)
sweep match / fill / sort / write / ask: 20 seeds, 20 distinct sets, 20 / 18 / 20 / 20 / 20 distinct orders; the fill bank never in the fixed order
verify-b4-question-words: 1658 assertions, 0 failures in 99s -> PASS
PASS (1658 assertions, 101/101 poisons killed)
```
`--quick`: `PASS (356 assertions, 101/101 poisons killed, --quick: sweeps + synthetic-locale face renders skipped)`.

**Distinctness:** `node tools/gate-variation-distinct.js --batch=b4 --diffs=2 --family=question-words` -> `[b4:question-words] compared 15 pairs over 5 faces against their bases + pairwise within family / every variation differs from the deck its base publishes and from its siblings`.
**Baseline:** `node tools/b3-baseline.js --check --quick` -> `checked build 3640 + enum 255 in 18s: 0 drifted (0 expected), 0 missing / PASS`.
**build-en:** `658 types -> i18n/strings.en.json (title lint clean)`.
**Starters:** `node qa/verify-ruling-starters.js --quick --types=G1-373,G1-374,G1-375,G2-356,G2-357` -> `discovered 3 starter coordinates over 1 types: G2-357/null/d1x6 d2x6 d3x6 … PASS: 18 starters measured over 3 renders, 0 failure(s)` (px 29.5 on every row). **G2-357 prints starters: the reviewer adds `G2-357` to that gate's `MUST_HAVE`** (F4's rulings are EMPTY by design; F1-F3 have no ruling).

## Pictures OPENED

`out/dev/G1-353-faces-pictures.png` (scratch sheet, read with the Read tool): the whole 133-key thing pool at 88 (the F1 answer / F5 scene size), the 15 places at 88 and the 32 portraits at 48 (the F4 size). Read: every thing is what its key says (the en `crane` pins `vehicles/crane`, the machine, which is what the en word means; es / sv / fi have no crane candidate at all, `B2_EXCLUDE`); `doll` (toys) reads as a small girl figure at 88, a note for the panels, never a leak (on F1 the picture is the ANSWER side, the literal "the doll" is printed beside it, and no face scores a thing picture as a person); the 15 places as the base recorded; the eight `minPx 56` full figures (carpenter, chef, gardener, construction_worker, cashier, florist, nurse, tailor) read at 48 only as silhouettes, so `dealFace` / `buildAsk` draw a portrait only at >= its `minPx` (F4 at 48 draws the 24 bust-class portraits; F1 88 / F2 56 / F5 120 draw all 32). The gate asserts on disk (`fileExists(fileUri(theme, noun))`, `cache/themes-512/<theme>/<noun>@3x.webp`) every pinned thing, place and portrait: 0 of 180 missing; every rendered picture `complete` with a natural width (verify). Every face's d2 render was read; the eleven gate renders per face (default / 710 / 677 / five synthetic locales / poisons) are in `out/dev/G1-37x-…` / `G2-35x-…`.

## Deviations from §3 (each measured or ruled)

1. **The chrome is 710, not 722** (the base record's deviation 4): every face fits it and the fi four-line 677 (F1 560..612, F2 614, F3 677 exactly, F4 666, F5 668).
2. **F1 items grow 100..120 and the bands between them are measured, instead of the design's fixed 100 + `space-around`** (the G1-368 cloze precedent: with fixed items the page's slack became 60 px bands at the tallest en chrome, PS1). A first build stretched the items to fill the column (141 at 766) and left a one-line question floating in 57 px of cream above and below: SPARSE in the other direction; `itemMax = itemH + 20` keeps the 88 px picture within 16 px of its card edges and the bands at 33 / 22 / 15. The resolver refuses `itemMax` outside `[itemH, itemH + 44]`.
3. **The F1 count answer is the thing's picture wearing the K-287 count badge beside the number word** ("three" + a meerkat with a "3"), not three copies of the picture: three 88 px pictures do not fit the 224 px inner of a 248 item beside a 128 px literal, and a 44 px mini-grid is under the G1 floor for this face (the base record's deviation 11 left it to the composer).
4. **The F1 clock sits in a `flex:0 0 auto` span** (the base's smoke measured its numerals, never its width): an inline svg shrinks beside a wrapping literal, measured 81 of 88 on the first gate run; the cross-check now asserts the clock >= 88.
5. **F3 is a FIXED 677 stack** (the SPARSE rule; the pronouns F4 precedent): the design's `.ws-bin` 350 with `max(5, load)` lines left 234 px of paper under the bins at the en chrome (PS3 is exactly the design's number: "stack 536 != the fixed 677"). Bins 491 with 8 lines at 55 (the line count from `round((binH - 10) / 58)`, the K-288 gapY ceiling); the shelf is held at TWO rows by the composer (`shelfRowsEst`, a greedy wrap over the 10.2 px/glyph estimate + 28 px of tile chrome, with the widest guard tile 140 as the wrap-loss margin), because a fixed stack keys on the shelf height; the render asserts exactly 2 rows. The `.ws-pill` head measures 40 (the design's 26 is the pronouns finding again).
6. **F3's tile guard is the design's authoring estimate (`round(glyphs x 10.2) <= 112`) at build and the measured `<= 124` at render.** Measured at Baloo 20 the estimate is honest but not tight: "at the beach" (111), "in the forest" (110.7), "at the river" (99.7), "in the cabin" (104.1) would pass the render guard yet fail the estimate, and "pomegranate" (11 glyphs, est 112) measures 121.1 (passes 124). en keeps the design's six place tiles (at home · in the tent · at the lake · at the pool · in bed · on the sofa); a panel that wants the four longer places on F3 authors a measured `tileWidths` table (not built; recorded).
7. **F4's ruling is 575 from the PAGE inner (675), not the lane's 639**: the rows are lane-less, so `w = 675 - 52 - picPx`; the design's arithmetic (`grid 30 10 48 12 1fr -> 575`) is right and the first build's 639 base was wrong (its own guard caught it).
8. **F4 draws portraits at >= their `minPx`**: the pronouns bank tags eight full figures `minPx 56`; at 48 they read as silhouettes and the node cross-check refused them ("portrait 48.0 < 56 (its minPx)"). `dealFace` filters `people` by `minPx <= picPx` for every face (F4 keeps 16 m + 8 f busts, enough for 8 rows at <= 6 per gender).
9. **F5's rulings are h 72, not the design's 56**: at 56 the stack is 572 and 194 px of paper sit under the lane at the en chrome (PS5 is the design's number). Growing h to 72 fills the 677 budget (668) with the line set and the 29.5 px starter UNCHANGED (`textLaneGeometry` is glyphH-bound: measured 29.5 at h 56 and at h 72); glyphH stays 26 so the child's hand size is the design's.
10. **The F2 gap-box formula reads a MEASURED `bankWidths` table** (Nunito 800 18) with the Baloo-20 chip proxy as the fallback: the design's per-locale widths were re-measured to the pixel (en "How many" 91.2 -> 170; fi "Kuinka monta" 118 -> 213; da / no "Hvor mange" 104.2 -> 191) and live in the gate's `MEASURED_BANK_W` for the synthetic blocks; the panels author `bankWidths` from a render as they do `chipWidths` (the gate measures every rendered bank word against the table, within 3 px).
11. **The rule 9 title-lists-kinds check aliases the bins literals**: the fi head "Kysymyssanat: kuka, mikä, missä" names the nominative "mikä" while the chip is "Mitä"; the fi bank's `bins.what = 'Mikä?'` (declared `binAliases`) makes the title list who / what / where as the page does. The synthetic fi control now carries that alias (the design's nominative F3 head).
12. **The sv measured head lists FOUR words while the base's d2 config ships THREE chips** ("Frågeord: vem, vad, var, när" over who / what / where): the new rule is RIGHT to fire and would refuse the real sv draft. This is a BASE + panel item (design §1 said sv's d2 chips are the four words verbatim; the base shipped one 3-chip config for all locales): either the base reads a per-locale chip set (`faces.base.kinds`) for its d2 page or the sv panel re-targets the head. Filtered on the SYNTHETIC sv control only, by the exact message, with the OPEN comment in `ctrlFilter`; not hidden on real data.
13. **`text()` on the page side folds ASCII whitespace only**: fr's NBSP before "?" is a literal the cross-check compares (a `\s` fold turned "read ?" into a plain space and failed every fr question).
14. **F2's sixth row draws any kind capped at `maxPerKind` 2** (the design's "the 6th row draws any kind; no kind > 2"), asserted per page.

## Per-locale refusals visible from the bank shape (Phase 4 `hub-expectations.json`)

- **None at the face level**: every locale's `qwords` table has five members (the F2 bank, the F1 / F4 kind sets), every stack fits the fi 677 chrome, and the composers refuse per ITEM, never per face. The design's per-item refusals hold: de "auf dem Spielplatz" (18 glyphs) and fi "leikkikentällä" (14) fail the F3 tile guard and are simply not drawn on F3 (the design's count: de / fi lose one long phrase each and keep >= 10 places under the guard; rule 6 requires >= 6); F3 needs only 3 places under the guard per page (the resolver refuses a locale with < 3).
- **F3 things**: a locale whose vocab singular is missing for a pool key skips that key on F3 (`tileTextOf` returns null); sv / da / no `sortThingForm` is the panel's ONE policy ('bare' indefinite "boll" or 'unique' definite "bollen").
- **F4 minPx**: every locale draws from the same 24 bust-class portraits at 48 (locale-neutral).
- **The sv 4-word head** (deviation 12) is a base-level item, not a row loss: 66 rows stand (README matrix confirmed) once it is resolved.

## Open items for the panels / the reviewer

1. **G2-357 joins `verify-ruling-starters.js` MUST_HAVE** (the reviewer's edit of that shared file).
2. **`bankWidths` per locale** (deviation 10): five measured numbers per locale, authored from a render like `chipWidths`; the fallback proxy is within 8 px, so an unauthored table still ships every d2 face (the gap box is uniform per page either way).
3. **The sv d2 chip set** (deviation 12): a base decision before the sv wave; the validator refuses a 3-chip `faces.base.kinds` under the 4-word head.
4. **F3 `tileWidths`** (deviation 6): optional; only if a panel wants "at the beach"-class places (estimate-refused, render-passed) on the sort face.
5. **`doll`** reads as a girl figure at 88 on F1 / 120 on F5: harmless by construction (the answer side prints its literal); a panel may still prefer `excludeThings:['doll']` per locale, which also removes it from the base's pool (the base draws the same `thingPool`; en's base is baselined, so en keeps it).
6. The fr policy "Que" + inversion vs "Qu'est-ce que" (the base's open item 4) reaches F1 / F2 / F4 through `qwords.what` and `gapOf` unchanged; a "Qu'est-ce que" head makes the F2 rest shorter and the bank pill wider (150.7 px chip; the 5-pill bank still one row).
