# K-355 `five-senses` : EN BASE build record (2026-09-21)

Built from `K-355-five-senses.md` (FINAL) §2 base + §5 data + gates under `_BUILD-BRIEF.md`, `_SUBSTRATE.md` and the README cross-type rulings; every ruling of `_work/K-355-critic.md` applied (NEW `senseMatch`, never the factory; `layout` knob; 45-item bank; `data-lcs-*` stamps; no verb stamp on F4). No em-dashes. Nothing shared edited; the faces (G1-357..G1-361) are NOT emitted (Phase 2).

## Files (all type-scoped)
| file | what |
|---|---|
| `scripts/worksheet-gen/types/k/K-355-five-senses.js` | the spec: `exerciseType:'five-senses'`, themeless, `difficulty {1,2,3}` = `{pairs, itemH, iconPx, pool, maxPerSense, organWords}` exactly the design ladder (d1 4 / 140 / 100 · d2 5 / 122 / 92 · d3 5 / 122 / 76 strong+signed organWords); `build()` reads ONLY `bank('five-senses', loc)` (lib/b4-common.js) + the GLOBAL json + `fileUri`; `_buildWith({global, block})` is the gate's poison seam; `_verb / _starter / _organOf / _itemsFor / _familyOf` = the §5 helper contract (throw, never en); `_buildFace` = the additive `layout` knob (`d.layout` undefined = base path byte-identical; 'sort' / 'which' / 'odd' / 'label' / 'write' refuse by name until Phase 2); `verify(page)` re-derives everything from the stamps (root `data-lcs-layout="base"`; left `data-lcs-item` + `data-lcs-sense`; right `data-lcs-organ` + `data-lcs-sense`): 5 distinct senses, the 5 organ files each once and `=== ORGAN_OF[sense]` (or the `taste:'mouth'` override), a derangement (no row straight across), every `img.complete`, icons >= 56, no text inside `.ws-match` (d3: exactly one `.fs-organ-word` per organ), no `[data-lcs-answer]`, dots on both sides, the stage inside the body / above the footer, the line zone >= 120. |
| `scripts/worksheet-gen/data/science/five-senses.json` | the GLOBAL concept bank: `senses`, `organOf`, `organFallback {taste:mouth}`, 45 items (38 strong + 7 signed, `picOpened:true` + opener tags + the critic's notes), `families [[flower, rose, lavender, lilac, jasmine]]`, `pageRules {maxPerSense:2, majorityRows {hear:2, taste:2, see:1}}`, `excludedByRule` (recorded, never items). |
| `scripts/worksheet-gen/data/b4/five-senses.js` | `{ FIVE_SENSES: { en: {verbs, starters, signedOk, organOf:{}, refuse:[], strings, strand} }, SENSES, ORGAN_OF }` — the ONLY 10 literals the type prints (5 verbs + 5 starters) + the base and the five face strings (§6 EN candidates under the allocated ids G1-357..G1-361, rule 10) + `strand:'Science (readiness)'`. gitignored: the reviewer force-adds. |
| `scripts/worksheet-gen/templates/components-b4/five-senses.js` | `ORGANS`, `ORGAN_OF`, `SENSE_OF`, `senseMatch`, `senseSortStage`, `organChips`, `whichSenseRow`, `senseOddRow`, `organLabelRow`, `senseLane` (the §2 geometry verbatim: tiles 84 / icon 64, bins 123 x 185, pills padding 6 12 Baloo 17, chips 60 / icon 46 rounded-square, which-row grid `30px 72px 1fr 340px`, odd boxes 96 / icon 72, label lane `84px 440px` gap 16 + `writingRow 440 x 62 glyphH 36`, write lane `64px 560px` gap 12 + `rulingBlock rows 2 h 46 glyphH 30` sized by `starterFontPx`). The barrel merges it beside the sibling families (verified: 0 duplicate names). Only `senseMatch` is consumed by the base; the six face components render nothing until Phase 2 and are unconsumed on purpose. |
| `scripts/worksheet-gen/qa/verify-b4-five-senses.js` | the gate (exports `validateBank(block, loc)` for `tools/b4-probe-child.js`, `validateGlobal`, `measureLiterals`, `coOccurrable`). |

## Measured (real render, shell woff2 from file://; `out/dev/K-355-gate-*.png`)
- d2 en: items 170 x 122, icons 92 (floor 56), **line zone 207 px** between the dots (design 203: the design's sum 40 + 170 + 203 + 26 + 170 + 40 = 649, not 675; the 4 px go to the zone), item stack 664 px with the body pinned to 677; **fits 722 AND 677**; under the one-line en chrome (body 811) `space-around` opens the gaps.
- d1: icons 100, stack 648; d3: icons 76 + the verb under the organ (Baloo 2 700 18 teal), stack 664. All three verify 0 / lints 0.
- Long chrome (69-char 3-line de title + 150-char instruction): body **733** measured (the README's 722 assumes a 3-line instruction; a 150-char instruction wraps to 2 lines at 17 px over ~630 px, and a 70-char title cannot reach 4 lines at 30 px over ~390 px, so 733 is the tallest chrome the 70 / 150 limits produce). The 677 stack is therefore checked by PINNING the body to 677 (and 722) in the live page and asserting the lowest item stays inside it; the factory's 720 stack overflows exactly there (PR13: 708 px of items, 722 fits, 677 fails) while the base fits both.
- EN widths: verbs 35 / 27 / 41 / 40 / 44 (+29 <= 123); starters 190 / 173 / 206 / 199 / 208 <= 280 at 34.5 px (P9 measured "Ich kann schmecken: " = **326.6** px, the critic's 327).
- Bank: 45 / 45 resolve via fileUri + entriesFor + hasPicture x11, 0 excluded, 0 neighbour-bank hits, 0 body-parts nouns, 0 duplicate nouns; per sense strong / co-occurrable: hear 10 / 10 · see 7 / 7 · smell 7 / 3 · taste 9 / 9 · touch 5 / 5.
- Sweep: 20 seeds x d2 → 20 distinct item sets, 19 distinct organ orders, every seed the five senses + a derangement + the family fence.

## Gate output
`node qa/verify-b4-five-senses.js` → **PASS (321 assertions, 29 poison verdicts clean)**; `--quick` → PASS (259 assertions). Poisons killed 20 / 20, each for its own reason, the EN bank the control: P1 sun (K-211) · P2 cheese (G1-207) · P3 teddy_bear (vocabKey null) · P4 organOf.touch = skin · P5 majorityRows += smell (3 < 6) · P6 de "Ohr" + "Gehör" FIRE / "hören" PASSES · P7 fr "sentir" FIRES / "toucher" PASSES · P8 it "Sento ..." FIRES / "Ascolto ..." PASSES · P8b pt "sentir" FIRES / "cheirar" PASSES · P9 de "Ich kann schmecken: " 326.6 > 280 FIRES / "Ich schmecke ..." PASSES · P10 fi `{noun}` slot · P11 nl F3 title without "zintuig" · P12 fi "Aistit" head · P13 sv F4 strings missing without refuse FIRES / with `refuse:['label']` PASSES · P14 beach/rock twice · P15 d3 organWords with a missing verb → the spec REFUSES · P16 a sense pool emptied → REFUSES (no filler) · PR1 organ straight across → verify() · PR2 two hear objects → verify() · PR13 the factory stack under 677 → overflow.
`node i18n/build-en.js` → `600 types (title lint clean)`. `node tools/b4-probe-child.js en` builds K-355 with 0 validator findings.

## Baseline
`node tools/b3-baseline.js --check --quick` → **PASS** with the committed `strings.en.json` K-336 title (`checked build 3400 + enum 255 in 16s: 0 drifted (0 expected), 0 missing / PASS`): nothing shared drifted. ⚠ `i18n/build-en.js` (a required step) regenerates `strings.en.json` from the specs and surfaces a PRE-EXISTING stale line: the committed K-336 title is "… Find the Flag" while the spec (since `f074e2e6`) says "… Start at the Flag", so the rebuilt file drifts `K-336|animals|d2|en` + `K-336|fruits|d2|en` (proved: restoring that one title gives 0 drift; `--expect` with the two keys PASSES). Not this build's doing; the reviewer records it (or re-captures) — left as build-en produced it (it also carries the sibling G1-352 entry).

## Deviations from the design file (each measured)
1. `--quick`-independent 677 check by body PINNING (above): the en chrome cannot reach 677 under the 70 / 150 limits; pinning measures the same layout the fi four-line title would give. The design's "renders … under the 677 stack (4-line fi title)" is met by measurement, not by a fixture.
2. Line zone 207, not 203 (the design's arithmetic; the dots sit at right:-26 with a 12 px dot). Asserted >= 200.
3. P4 fires on rule 1's enumeration ("skin is not one of eye ear nose tongue hand") before the picture check; same rule, earlier predicate.
4. Rule 8 is enforced from the co-occurrable pool per face (sort 5 x >= 2, which three senses >= 2, odd `majorityRows[s] <= floor(co/3)`) rather than by building the faces (not yet buildable).
5. `lib/b3-instructions.js fillSlots` is NOT imported (no slot exists on the type; the design's "imported for the K-333 refusal path" would be an unused import); rule 9 bans `{}` in instructions instead.
6. The prompt's "additive `mode` knob" = the design's `layout` config key (the K-354 sibling's shape); `mode` is the landing `coordinate.mode` string ('base' / 'sort' / 'which' / 'odd' / 'label' / 'write').

## PNGs for the reviewer
`out/dev/K-355-null-d1-en.png` · `K-355-null-d2-en.png` · `K-355-null-d3-en.png` (LOOKED at all three: five big objects left, five pink organs right, one dot each, nothing printed but the chrome; the nose reads as a nose at 92 and 76) · `K-355-gate-d2-en-longchrome.png` (3-line title) · `K-355-gate-poison-PR13.png`. Contact sheets of all 45 items + 6 organ candidates at 92 and 46 px (scratchpad `K-355-sheet-92.png` / `-46.png`): every picture opened and read as the bank notes say; the nose at 46 is a pink nose with two nostrils (readable), the tongue = lips + teeth + tongue, `vegetables/garlic` the purple-striped bulb, `desserts and sweets/cake` the strawberry slice, `camping/rock` the grey stone, `kitchen tools/sponge` a yellow slab.

## Open items for the faces / panels
- Phase 2 builds `_buildFace` for the five layouts on the components already shipped, adds the face render sections + PR3-PR12 / PR14 to the gate, and the `MUST_HAVE` entry for G1-361 in `verify-ruling-starters.js`.
- Panels: `signedOk` per locale (nothing shipped reads it), the tongue-vs-`mouth` override, the no touch verb (kjenne / føle), it hear / smell / taste register, nl / da / no "kan" frame at 268 px vs 280 (the gate measures), F3 band per locale.
- The one-line en chrome (body 811) leaves 141 px slack that `space-around` spreads into the gaps: acceptable (the design's "One-line chrome (814): space-around opens the gaps to ~48").
